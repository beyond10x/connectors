# Provider evidence supplement to the immutable initial review

The initial `report.md` remains unchanged: **needs-revision, eight P2 findings**. This supplement records common primary documentation received from root after that report was frozen. It adds no finding ID and selects no final profile design. Six exact source files were copied into `provider-sources/`; every SHA-256 matches root's source manifest, preserved there with URLs and sizes. No provider or runtime call was made by this reviewer.

- **LD-A-01/03:** Pinned Loki v3.7.0 `loki-v3.7.0-http-api.md:464–475` specifies `[start,end)` for log range queries. The same endpoint also accepts metric queries, for which the line limit does not apply; a selected log-only binding must therefore make its accepted result kind and bounds truthful. `loki-v3.7.0-logcli-query.go:177–200` refuses batching when timestamp overlap fills the configured batch, and adjusts the backward end by one nanosecond to retain overlap. This supports the saturated-tie counterexample. It does not prove exactly-once preservation of arbitrary identical occurrences for a new Connectors algorithm.
- **LD-A-04/08:** `confluence-v2.openapi.json`, JSON pointer `/paths/~1pages/get/parameters`, declares page-ID and space-ID filters, body-format and limit; its operation description says returned pages must be visible to the provider principal. This supplies a concrete candidate for a receiver-fixed scoped lookup while retaining useful document reads. It is not itself proof of a transaction across independent requests or a global revocation guarantee. The frozen current v1 specification contains neither `/wiki/rest/api/content/{id}` nor `/rest/api/content/{id}`. Thus the old catalog's selected endpoint needs an explicit current binding/migration decision; absence from this file alone does not prove runtime endpoint removal.
- **LD-A-04/05:** `jira.openapi.json`, `/paths/~1rest~1api~12~1issue~1{issueIdOrKey}/get/description`, explicitly documents moved-key resolution without an HTTP redirect and returning the resolved issue key. Rejecting redirects or checking an input key prefix alone therefore cannot establish project membership. The enhanced search operation `/paths/~1rest~1api~12~1search~1jql/get` accepts scoped JQL and `reconcileIssues`; ordinary search may lag updates. `jira-search-reconcile.html` explains that stronger consistency applies only to listed issue IDs, at most 50. A bounded canonical-ID lookup followed by scoped reconciled retrieval is a possible selected design, but root must specify actual request fields, admission, byte bounds, exact returned identity/project checks and failure handling; generic search alone is not fresh membership proof.

These are documentation/source observations. Root fetched the common inputs after reporting the Connectors search capability gap. This reviewer inspected no peer output, made no independent remote attestation, and does not claim conformance of a particular live tenant, proxy, SDK or runtime. Complete hashes and source URLs are in `provider-sources/source-hashes.json`.
