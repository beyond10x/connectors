# Final review snapshot scope

The authoritative final reviewed-input manifest is `source-hashes-reviewed.json` (55 files). The initial bulk snapshot also mechanically copied ten archived report/provenance files nested under the evidence directory. Their text was not displayed to or inspected by this reviewer and did not inform the verdict. They are excluded from the reviewed-input manifest, together with aggregate dispositions and mutable planning workflow. The original bulk manifest is retained only to preserve acquisition provenance; it is not the review scope.
