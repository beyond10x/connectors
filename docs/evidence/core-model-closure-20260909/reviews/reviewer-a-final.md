approve

Independent whole-stage model review A. Exact source: 7e8c718303a43bc7ec4a02d6a76581d92802bf6b. Baseline: 8a5cf563fc717fd4b23e4dd7d470d0c967f39461. Review mode: non-interactive; tracked source and planning read-only. This approves the complete selected model-closure stage, including auth, immutable artifact provenance, discovery/route/configuration values, execution audit and the coordinator joins. It is not runtime-conformance or publication approval.

MCA-01 is resolved. Connection now embeds the existing optional EvidenceSnapshot at ess/domains/auth_bindings.yaml:97. The owning rules in contracts/auth/connection/v1alpha1/semantics.md:146 select one aggregate for the active generation, at most six distinct applicable baseline checks, exact binding/current-profile equality, publication/recollection invalidation, bounded retention and absence before publication or for no-child-material bindings. Exact-operation evidence remains request-scoped. This supplies the previously missing typed home without inventing an evidence entity or changing the immutable capture. The original needs-revision report remains unchanged.

No further actionable finding was established in the selected scope.

Manual semantic review:

- Re-read AGENTS.md and the changed design inventory, preserving the earlier read of design direction and auth/specification boundaries. Reviewed every model/prose delta since the initial source and rechecked the auth/artifact selections against the full baseline delta and actual compiled graph. Source, Bundle, AuthProfile, Connection, Acquisition, CustodyVersion, DiscoveryCollection, ResourceObservation and AuditRecord are the nine added entities. The inventory correctly separates embedded values, transient DispatchAdmission, existing first-slice runtime and future persistence enforcement.
- Auth qualification, private allocation before capture/publication, logical profile versus immutable revision, terminal acquisition consumption, original expiry, failed repair preservation and uncertain acknowledgement remain coherent. Existing refresh source consumption and publication/revocation/dispatch ordering are preserved. Custody versions are independently referenced; their retirement cannot delete capture identities or refresh/audit safety facts. Explicit credentialless bindings manufacture no generation, custody or provider account.
- Artifact Source and Bundle retain independent immutable identities. Adapter/bundle source joins are references; bundle specification digest remains distinct from logical adapter identity. Ordered transforms, original digests, license evidence and authored curation do not manufacture implementation or license permission. Replacing an optional index cannot mutate or delete shared artifacts. Strict adapter/public reader syntax is unchanged.
- DiscoveryCollection owns its bounded rows and references the actual source Connection, instance and authored operation. Scope/epoch and observation identities prevent cross-scope withdrawal or resurrection after lost continuity. Withdrawn can only retire; Retired cannot reopen. Row effects occur within whole-publication/retirement transactions. View retirement advances the predecessor fence; superseded or missing publication receipts return unavailable, not proof of non-commit. Physical retired records count toward capacity until cleanup. Historical mediated-route coordinates impose no live observation foreign key and cannot pin rows or repoint a child after a new scope epoch. Only matching route evidence may advance. CompositionDeclaration is admitted immutable configuration, with instance/revision resolution explicitly a binding predicate.
- Audit's private injective identity distinguishes equal public refs across instances. Its acknowledged anchor precedes audited read/mutation dispatch; optional attempt/Connection links reference independently retained records. One final observation supports same-id/same-content acknowledgement recovery and rejects conflicting appends without redispatch. Early refusals cannot satisfy the execution gate. Gateway and leaf records remain independent, and capacity/retention cannot evict required audit to make room. The public SourceAudit projection is unchanged.
- Checked the coordinator's real Connection joins, refreshed owner comments, service/operations/delegation audit references, contracts index and CHANGELOG. The milestone text distinguishes selected Kubernetes including discovery, GitLab and SQL models from deferred capabilities and from runtime implementation. No new runtime implementation is claimed by model compilation.

Independently executed checks:

1. git rev-parse HEAD returned the exact final source; git status --short was empty on entry.
2. /home/timo/beyond10x/connectors_v2/.local/toolchains/ess/0.20.0/bin/ess specify validate --path ess — exit 0: connectors v1 — 18 file(s), valid.
3. The same pinned binary, specify compile --path ess --out .local/model-closure-20260909/final-ir.json — exit 0: 18 files, 314 declarations.
4. Archived only baseline ess/ into labeled scratch with git archive 8a5cf563fc717fd4b23e4dd7d470d0c967f39461 ess, then compiled .local/model-closure-20260909/final-baseline/ess with the same pinned binary — exit 0: 14 files, 218 declarations.
5. jq comparison of those actual compiled IRs — exit 0: baseline_entities=11, current_entities=20, old_entities_preserved=true, old_types_preserved=true. The comparison checks each original identity and lifecycle, every complete original field entry, and every original named type. Separate compiled-IR inspection confirmed the actual optional evidence/route fields and exactly two ownership edges: AdapterSpecification to OperationDeclaration and DiscoveryCollection to ResourceObservation.
6. git diff --check 8a5cf563fc717fd4b23e4dd7d470d0c967f39461 7e8c718303a43bc7ec4a02d6a76581d92802bf6b — exit 0.

Execution limits: unit verification/scenario documents were read as author claims and checked against source, not adopted as semantic verdicts. The coordinator reports a passing full gate and deterministic schema generation; I did not execute those Rust/build/scenario suites. I ran no runtime integration, provider request, model-specific executable helper, source fix, planning mutation or commit. Actual durable storage, trusted current-authority decisions, clocks, identity allocation/equality, atomic field assignment, capacity accounting, byte limits, codecs, dispatch and cleanup remain explicit implementation/conformance obligations. Compilation validates typed declarations, references and lifecycle causation, not those predicates. No unresolved selected ownership/lifecycle contradiction was found within these limits.

Retained evidence: final-brief.md, final-ir.json and final-baseline-ir.json are beside this report in .local/model-closure-20260909. Initial-review.md remains the immutable initial verdict. The assigned managed tree is handed back with no tracked changes and this reviewer's lease released.

```findings
[]
```
