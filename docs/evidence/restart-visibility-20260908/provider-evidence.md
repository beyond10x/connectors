# Provider evidence for lifecycle semantics — 2026-09-08

This packet uses exact source bytes, preserved as gzip with [hashes](provider-source-hashes.json). It does not invoke a daemon, cluster or business operation. Connectors' operation search returned no web-search operation; that gap was reported before using official web/HTTPS sources. Four sources were gathered during the preceding classification checkpoint and their hashes rechecked for this work. Reviewer B subsequently retrieved four more official files at the same pinned Kubernetes commit to establish the conditional-version edge; their exact bytes and URL/hash provenance are included unchanged.

| Source | What was checked | Limit |
|---|---|---|
| [Docker Engine API v1.56 Swagger](https://docs.docker.com/reference/api/engine/version/v1.56.yaml), SHA256 f09a22ad220d551dca5d6c402052204d3e9fd1f16943cd78ba6677ea1266616f | Swagger 2.0 / API version 1.56; all nine listed HTTP path/method declarations; lifecycle response and parameter declarations | No live daemon or provider conformance; strict prefix/version support must be implemented before advertisement |
| [Kubernetes API concepts, release-1.35 source](https://github.com/kubernetes/website/blob/8817d358864b70a9710c5ad34f78df6a239be33d/content/en/docs/reference/using-api/api-concepts.md#updates-to-existing-resources) | Conditional resourceVersion updates/PATCH and stale-version conflict; pass versions unchanged; documented scoped version ordering in newer Kubernetes | The selected contract uses equality, without claiming the vendor universally forbids ordering |
| [Kubernetes v1.35.0 ObjectMeta update validation](https://github.com/kubernetes/kubernetes/blob/66452049f3d692768c39c797b21b793dce80314e/staging/src/k8s.io/apimachinery/pkg/api/validation/objectmeta.go#L310) | Update requires resourceVersion; UID/name/namespace are immutable fields | This validation source alone is not proof of every PATCH implementation, status response or admission-plugin side effect |
| Old Connectors local_workloads.rs at 81459ac42ddd518d3942f4b079841e9e0ed6efc8, lines 297–350 | Original UID/resourceVersion copied into strategic merge PATCH; a generated decimal epoch-ms annotation; response identity checks and patch_accepted result | Historical behavior; broad old 409/422 error mapping is not adopted as universal no-effect proof |
| [Deployment strategy](https://github.com/kubernetes/kubernetes/blob/66452049f3d692768c39c797b21b793dce80314e/pkg/registry/apps/deployment/strategy.go#L148) and [API object versioner](https://github.com/kubernetes/kubernetes/blob/66452049f3d692768c39c797b21b793dce80314e/staging/src/k8s.io/apiserver/pkg/storage/api_object_versioner.go#L73) | Built-in Deployment allows unconditional update; nonempty resourceVersion is parsed with unsigned decimal 64-bit semantics | Zero and all-zero spellings can select the unconditional path; noncanonical positive spellings do not have arbitrary string equality |
| [Generic update store](https://github.com/kubernetes/kubernetes/blob/66452049f3d692768c39c797b21b793dce80314e/staging/src/k8s.io/apiserver/pkg/registry/generic/registry/store.go#L660) and [strategic PATCH handler](https://github.com/kubernetes/kubernetes/blob/66452049f3d692768c39c797b21b793dce80314e/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/patch.go#L700) | PATCH delegates the transformed object to Update; parsed zero plus the strategy flag replaces the submitted version with the current one; the conditional branch compares parsed values | Source-derived mechanism proof for this binding, not a live PATCH race or a universal API/version lexical rule |

The [Docker endpoint audit](docker-endpoint-audit.json) records the parsed operation IDs, response codes and parameter names. Start/stop distinguish successful acknowledgement from an already-satisfied response; restart has no declared historical deduplication outcome. The lifecycle acknowledgement carries no synchronized running/stopped state snapshot. The result therefore names desired_state and disposition, and permits process exit/restart-policy interference before the response as well as between calls. No follow-up inspect is implied. The selected natural/none classifications and immutable target/preflight rules are Connectors semantic decisions based on that evidence. They do not promise identical replies or at-most-once state transitions across external interference.

The Kubernetes evidence supports an exact-object optimistic concurrency guard only with the selected valid conditional-version admission. Canonical positive decimal in 1..18446744073709551615 excludes zero/unconditional aliases and alternate positive spellings; accepted bytes are copied unchanged. This is an explicit Connectors binding choice inferred from the pinned strategy/versioner/store path. Nonempty String alone is insufficient, and ESS schema acceptance does not execute the predicate. Whether the first request, another writer, or a no-op explains a later version/marker observation remains a separate question. A later conflict or matching readback cannot by itself settle the original lost reply. The contract preserves that uncertainty and adds host-keyed replay with a fixed marker per prepared candidate.

Source retrieval notes: Docker's interactive explorer links failed in the browser reader; the official versioned YAML download succeeded. The Kubernetes release tag is annotated: using its tag-object hash in a raw source URL returned 404, then the peeled commit above returned the source. These retrieval failures supplied no vendor facts. The evidence script's first assertion also encountered PyYAML integer response-code keys; normalizing those parser keys to strings corrected the audit, which then passed. Neither correction changed vendor bytes or contract meaning.
