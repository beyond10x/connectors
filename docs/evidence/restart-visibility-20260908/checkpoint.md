# Restart and visibility checkpoint — 2026-09-08

Completed `contracts-restart-idempotency` (F11/E08) and `contracts-mutation-visibility` (E12) for the active specification-stabilization goal. Docker lifecycle calls bind exact daemon/container intent, fixed signal/wait and bounded preflight; their acknowledgements do not claim synchronized current state. Kubernetes retains original UID and a valid canonical positive conditional version, fixes one marker/body per candidate and separates host replay from deliberate new intent and lost-response uncertainty. The extended visibility matrix gives current policy, freshness, admitted lookup/input/key and candidate-only readiness one consistent order while preserving legacy behavior.

Both final independent [review A](reviews/a-recheck2-report.md) and [review B](reviews/b-recheck2-report.md) approve both stories with zero residual findings. [Dispositions](dispositions.md) map twelve individual findings to exactly one owner/outcome each: nine initial and three first-recheck findings. Four new raw review records and their complete frozen archives are preserved unchanged; the preceding classification packet retains the two initial reviews. Reviewer approval covers the frozen normative/evidence inputs, excluding mutable planning, dispositions, this checkpoint and final audit.

[Verification](verification.md): ESS 0.20.0 validates 13 files / 215 declarations. Two projections match all 222 ordered artifacts; seven new private values have unmodified generated schema copies. Forty-six shape expectations include nine rejected shapes and nineteen intentionally accepted semantic counterexamples. Forty-eight textual cases and eight exact provider archives record precise selected semantics and source evidence. Generic schemas do not execute conditional-version, identity, permission, marker, replay or visibility predicates.

The existing full gate passed 50 Rust tests and MSRV 1.88.0, compiling 222 scenarios including 34 authored. Sessions were compiled separately: 13 authored / 201 total. The narrow final corrections changed an ESS comment and normative prose/evidence; validation and compilation confirm byte-identical IR to the gated version. The full runtime gate was not rerun. No new runtime, public codec, adapter-kind schema or provider binding implementation was added.

[Final audit](final-audit.json) checks exact raw review bodies, archive members, final approved source hashes, one outcome per finding, model boundaries, provider/compiled evidence, local links and the original 48-item ledger. [Planning validation](planning-validation.log) preserves the complete CLI result, including 65 warnings: 63 historical prose-only reviews and the two new final reviews whose explicit empty findings blocks are also labeled prose-only by this CLI. Both final reports contain findings: [] in the fenced serialization and approve with zero residuals; immutable reports were not rewritten or given fake findings to suppress the warning.

Current progress: **32/48 original findings fixed, 16 open; 20/27 owner stories implemented for specification scope, 7 draft.** Remaining original work covers logs, document admission, read/refresh retry, media controls, index, evidence precision and supported vocabulary. Broader catalog/stack/open-decision/model review is still needed for the full goal. All work and recovery copies remain local; no external publication or Atlas mutation.
