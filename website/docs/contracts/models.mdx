---
title: ESS model reference
slug: /contracts/models
sidebar_position: 2
---

import ReferenceCards from '@site/src/components/ReferenceCards';

# Read the typed model

ESS turns the authored model into a typed reference with linked declarations and lifecycle diagrams. These pages are regenerated from the pinned compiler. No website author maintains a second list of fields or transitions.

The shared root declares **20 entities**. Many command behaviors remain unimplemented; a lifecycle diagram does not establish runtime enforcement.

<ReferenceCards kind="model" />
