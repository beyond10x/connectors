{"entity":"epic","version":1,"id":"remote-secrets","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Outcome\n\nConnectors stores credential bytes in the released Secrets service while retaining provider exchange, refresh and revocation ownership.\n\n## Acceptance\n\nThe runtime uses Secrets for normal and prepared operations, preserves owner identity, and migrates Vault values without writing plaintext to disk.\n","summary":"Replace runtime Vault custody with the shared Secrets API.","title":"Use shared encrypted secret custody"},"args":{"command":"create-entity","data":{"body":"## Outcome\n\nConnectors stores credential bytes in the released Secrets service while retaining provider exchange, refresh and revocation ownership.\n\n## Acceptance\n\nThe runtime uses Secrets for normal and prepared operations, preserves owner identity, and migrates Vault values without writing plaintext to disk.\n","status":"draft","summary":"Replace runtime Vault custody with the shared Secrets API.","title":"Use shared encrypted secret custody"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/remote-secrets"},"payload":{"actor":"human:timo","at":1788261022259,"causation":"cmd-new-epic-remote-secrets","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:remote-secrets@1#0~0ccdebc78ca43b05","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"secrets-adapter","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nAn official remote adapter maps every SecretStore and PreparedSecretStore operation, preserves tenant and reference identity, and refuses secret bytes in errors or URLs.\n","summary":"Map connector storage ports onto the released Secrets client.","title":"Implement the Secrets remote-store adapter"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nAn official remote adapter maps every SecretStore and PreparedSecretStore operation, preserves tenant and reference identity, and refuses secret bytes in errors or URLs.\n","status":"draft","summary":"Map connector storage ports onto the released Secrets client.","title":"Implement the Secrets remote-store adapter"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/secrets-adapter"},"payload":{"actor":"human:timo","at":1788261022537,"causation":"cmd-new-story-secrets-adapter","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:secrets-adapter@1#0~267179c45993ccfe","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"secrets-adapter","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:remote-secrets"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000004","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788261022549,"causation":"cmd-story-secrets-adapter-derived_from-epic-remote-secrets","change":{"change":"related","relation":"derived_from","target":"epic:remote-secrets"},"correlation":"protocol-artifact-relate","event_id":"story:secrets-adapter@1#0~88743c0732f7ff5a","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"owner-custody","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nCredential creation and refresh send the authenticated owner subject to stores that support ownership without breaking existing store implementations.\n","summary":"Carry owner subject through the additive storage port.","title":"Preserve credential ownership at custody"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nCredential creation and refresh send the authenticated owner subject to stores that support ownership without breaking existing store implementations.\n","status":"draft","summary":"Carry owner subject through the additive storage port.","title":"Preserve credential ownership at custody"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/owner-custody"},"payload":{"actor":"human:timo","at":1788261022684,"causation":"cmd-new-story-owner-custody","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:owner-custody@1#0~f43f550edbb16f0a","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"owner-custody","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:remote-secrets"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000009","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788261022690,"causation":"cmd-story-owner-custody-derived_from-epic-remote-secrets","change":{"change":"related","relation":"derived_from","target":"epic:remote-secrets"},"correlation":"protocol-artifact-relate","event_id":"story:owner-custody@1#0~ef7f88815d34008f","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"vault-migration","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nA bounded, idempotent operator command copies scoped Vault values into Secrets entirely in memory, reports references only, and never removes the source.\n","summary":"Migrate existing custody without automatic source deletion.","title":"Provide a safe Vault migration command"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nA bounded, idempotent operator command copies scoped Vault values into Secrets entirely in memory, reports references only, and never removes the source.\n","status":"draft","summary":"Migrate existing custody without automatic source deletion.","title":"Provide a safe Vault migration command"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/vault-migration"},"payload":{"actor":"human:timo","at":1788261022825,"causation":"cmd-new-story-vault-migration","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:vault-migration@1#0~fa02f860189274dd","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"vault-migration","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:remote-secrets"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000014","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788261022834,"causation":"cmd-story-vault-migration-derived_from-epic-remote-secrets","change":{"change":"related","relation":"derived_from","target":"epic:remote-secrets"},"correlation":"protocol-artifact-relate","event_id":"story:vault-migration@1#0~52d948b0dfd79701","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"runtime-cutover","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nRuntime configuration selects Secrets, its exact projected-token audience and endpoint; Vault remains available only for an explicit migration window.\n","summary":"Compose the remote backend and bounded transition mode.","title":"Cut the runtime over to Secrets"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nRuntime configuration selects Secrets, its exact projected-token audience and endpoint; Vault remains available only for an explicit migration window.\n","status":"draft","summary":"Compose the remote backend and bounded transition mode.","title":"Cut the runtime over to Secrets"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/runtime-cutover"},"payload":{"actor":"human:timo","at":1788261022969,"causation":"cmd-new-story-runtime-cutover","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:runtime-cutover@1#0~9f34a735ca1c81b7","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"runtime-cutover","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:remote-secrets"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000019","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788261022976,"causation":"cmd-story-runtime-cutover-derived_from-epic-remote-secrets","change":{"change":"related","relation":"derived_from","target":"epic:remote-secrets"},"correlation":"protocol-artifact-relate","event_id":"story:runtime-cutover@1#0~ecf1bf60b4c4639c","executor":null,"recorded_at":"2026-09-01T11:10:22Z"}}
{"entity":"story","version":1,"id":"secrets-adapter","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788261700000,"causation":"cmd-evidence-story-secrets-adapter-test_result-1788261700093","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:secrets-adapter@1#0~92a2ee464b0c3657","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"secrets-adapter","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000010","to":"proposed"},"payload":{"actor":"human:timo","at":1788261700230,"causation":"cmd-move-story-secrets-adapter-1788261700230","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:secrets-adapter@2#0~5e4eb49b5a0c7a66","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"secrets-adapter","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000010","to":"active"},"payload":{"actor":"human:timo","at":1788261700239,"causation":"cmd-move-story-secrets-adapter-1788261700239","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:secrets-adapter@3#0~8c2e7f67b8a83bb2","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"secrets-adapter","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000010","to":"implemented"},"payload":{"actor":"human:timo","at":1788261700246,"causation":"cmd-move-story-secrets-adapter-1788261700246","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:secrets-adapter@4#0~6d4e4482cba0f992","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"owner-custody","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788261700000,"causation":"cmd-evidence-story-owner-custody-test_result-1788261700320","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:owner-custody@1#0~6b4c0c4f5299f446","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"owner-custody","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"human:timo","at":1788261700454,"causation":"cmd-move-story-owner-custody-1788261700454","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:owner-custody@2#0~b850bcf7f4596533","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"owner-custody","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"active"},"payload":{"actor":"human:timo","at":1788261700466,"causation":"cmd-move-story-owner-custody-1788261700466","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:owner-custody@3#0~0a5bb0365687551b","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"owner-custody","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"implemented"},"payload":{"actor":"human:timo","at":1788261700473,"causation":"cmd-move-story-owner-custody-1788261700473","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:owner-custody@4#0~e3e875b0e74f0bd5","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"vault-migration","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788261700000,"causation":"cmd-evidence-story-vault-migration-test_result-1788261700545","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:vault-migration@1#0~89f9c9464624c9ce","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"vault-migration","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"proposed"},"payload":{"actor":"human:timo","at":1788261700677,"causation":"cmd-move-story-vault-migration-1788261700677","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:vault-migration@2#0~124e003a36b3f08f","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"vault-migration","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"active"},"payload":{"actor":"human:timo","at":1788261700686,"causation":"cmd-move-story-vault-migration-1788261700686","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:vault-migration@3#0~b2b815730b95babf","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"vault-migration","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"implemented"},"payload":{"actor":"human:timo","at":1788261700695,"causation":"cmd-move-story-vault-migration-1788261700695","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:vault-migration@4#0~7b149e8637f603a1","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"runtime-cutover","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788261700000,"causation":"cmd-evidence-story-runtime-cutover-test_result-1788261700771","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:runtime-cutover@1#0~731c314f56c9700f","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"runtime-cutover","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000007","to":"proposed"},"payload":{"actor":"human:timo","at":1788261700902,"causation":"cmd-move-story-runtime-cutover-1788261700902","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:runtime-cutover@2#0~a8606860e45f68ca","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"runtime-cutover","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000007","to":"active"},"payload":{"actor":"human:timo","at":1788261700911,"causation":"cmd-move-story-runtime-cutover-1788261700911","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:runtime-cutover@3#0~5eca84ffbe5168be","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"story","version":1,"id":"runtime-cutover","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000007","to":"implemented"},"payload":{"actor":"human:timo","at":1788261700917,"causation":"cmd-move-story-runtime-cutover-1788261700917","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:runtime-cutover@4#0~c7cacbbd7e642f56","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"epic","version":1,"id":"remote-secrets","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788261700000,"causation":"cmd-evidence-epic-remote-secrets-test_result-1788261700990","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"epic:remote-secrets@1#0~400a3f4f3a1d8af9","executor":null,"recorded_at":"2026-09-01T11:21:40Z"}}
{"entity":"epic","version":1,"id":"remote-secrets","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000001","to":"proposed"},"payload":{"actor":"human:timo","at":1788261701122,"causation":"cmd-move-epic-remote-secrets-1788261701122","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:remote-secrets@2#0~099d3a54299a5f38","executor":null,"recorded_at":"2026-09-01T11:21:41Z"}}
{"entity":"epic","version":1,"id":"remote-secrets","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000001","to":"active"},"payload":{"actor":"human:timo","at":1788261701130,"causation":"cmd-move-epic-remote-secrets-1788261701130","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:remote-secrets@3#0~9e921d3950b544d4","executor":null,"recorded_at":"2026-09-01T11:21:41Z"}}
{"entity":"epic","version":1,"id":"remote-secrets","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000001","to":"implemented"},"payload":{"actor":"human:timo","at":1788261701137,"causation":"cmd-move-epic-remote-secrets-1788261701137","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:remote-secrets@4#0~8b5dbd24c7dd32b4","executor":null,"recorded_at":"2026-09-01T11:21:41Z"}}
{"entity":"story","version":1,"id":"secrets-adapter","revision":4,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788261739000,"causation":"cmd-evidence-story-secrets-adapter-test_result-1788261739717","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:secrets-adapter@4#0~92a2ee464b0c3657","executor":null,"recorded_at":"2026-09-01T11:22:19Z"}}
{"entity":"story","version":1,"id":"owner-custody","revision":4,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788261739000,"causation":"cmd-evidence-story-owner-custody-test_result-1788261739913","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:owner-custody@4#0~6b4c0c4f5299f446","executor":null,"recorded_at":"2026-09-01T11:22:19Z"}}
{"entity":"story","version":1,"id":"vault-migration","revision":4,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788261740000,"causation":"cmd-evidence-story-vault-migration-test_result-1788261740107","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:vault-migration@4#0~89f9c9464624c9ce","executor":null,"recorded_at":"2026-09-01T11:22:20Z"}}
{"entity":"story","version":1,"id":"runtime-cutover","revision":4,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788261740000,"causation":"cmd-evidence-story-runtime-cutover-test_result-1788261740304","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"story:runtime-cutover@4#0~731c314f56c9700f","executor":null,"recorded_at":"2026-09-01T11:22:20Z"}}
{"entity":"epic","version":1,"id":"remote-secrets","revision":4,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788261740000,"causation":"cmd-evidence-epic-remote-secrets-test_result-1788261740502","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"focused workspace tests and final gate"},"correlation":"protocol-artifact-evidence","event_id":"epic:remote-secrets@4#0~400a3f4f3a1d8af9","executor":null,"recorded_at":"2026-09-01T11:22:20Z"}}
{"entity":"story","version":1,"id":"generated-service-factory","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\n- A generator-targetable service factory contributes stable service metadata, a complete operation catalog, and the backend dispatch operations it owns through the existing `ConnectorBackend` seam.\n- Registering a factory is inert: it neither exposes an operation nor supplies endpoints, credentials, grants, or provider identity.\n- An explicit deployment overlay assigns the permanent provider reference and reverse-DNS authority and states each operation's exposure, risk, approval posture, endpoint bindings, credential bindings, and grant references.\n- Bundle construction is deterministic and refuses invalid manifests or deployments, duplicate service/provider/authority/operation identities, unknown or repeated deployments, incomplete operation overlays, factory bind failures, and catalog/dispatch/ownership drift.\n- Focused conformance tests cover inert registration, deterministic composition and policy projection, and the closed refusal vocabulary.\n","summary":"Bind generated service catalogs and dispatch behind explicit deployment policy without granting authority at registration.","title":"Compose generated services through a fail-closed Connector factory"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\n- A generator-targetable service factory contributes stable service metadata, a complete operation catalog, and the backend dispatch operations it owns through the existing `ConnectorBackend` seam.\n- Registering a factory is inert: it neither exposes an operation nor supplies endpoints, credentials, grants, or provider identity.\n- An explicit deployment overlay assigns the permanent provider reference and reverse-DNS authority and states each operation's exposure, risk, approval posture, endpoint bindings, credential bindings, and grant references.\n- Bundle construction is deterministic and refuses invalid manifests or deployments, duplicate service/provider/authority/operation identities, unknown or repeated deployments, incomplete operation overlays, factory bind failures, and catalog/dispatch/ownership drift.\n- Focused conformance tests cover inert registration, deterministic composition and policy projection, and the closed refusal vocabulary.\n","status":"draft","summary":"Bind generated service catalogs and dispatch behind explicit deployment policy without granting authority at registration.","title":"Compose generated services through a fail-closed Connector factory"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/generated-service-factory"},"payload":{"actor":"agent:connectors-factory","at":1788299725075,"causation":"cmd-new-story-generated-service-factory","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:generated-service-factory@1#0~a211caf23f9bb4a5","executor":null,"recorded_at":"2026-09-01T21:55:25Z"}}
{"entity":"story","version":1,"id":"generated-service-factory","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"agent:connectors-factory","at":1788299725237,"causation":"cmd-move-story-generated-service-factory-1788299725237","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:generated-service-factory@2#0~d1b8d2c3ff0c3c64","executor":null,"recorded_at":"2026-09-01T21:55:25Z"}}
{"entity":"story","version":1,"id":"generated-service-factory","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"active"},"payload":{"actor":"agent:connectors-factory","at":1788299725244,"causation":"cmd-move-story-generated-service-factory-1788299725244","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:generated-service-factory@3#0~e48f181f24deda08","executor":null,"recorded_at":"2026-09-01T21:55:25Z"}}
{"entity":"story","version":1,"id":"generated-service-factory","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"complete Connectors canonical gate","target":"01MEM0000000000000004"},"payload":{"actor":"agent:connectors-factory","at":1788300160000,"causation":"cmd-evidence-story-generated-service-factory-test_result-1788300160789","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"complete Connectors canonical gate"},"correlation":"protocol-artifact-evidence","event_id":"story:generated-service-factory@3#0~f126fc0ea155fbcd","executor":null,"recorded_at":"2026-09-01T22:02:40Z"}}
{"entity":"story","version":1,"id":"generated-service-factory","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"implemented"},"payload":{"actor":"agent:connectors-factory","at":1788300161088,"causation":"cmd-move-story-generated-service-factory-1788300161088","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:generated-service-factory@4#0~e3e875b0e74f0bd5","executor":null,"recorded_at":"2026-09-01T22:02:41Z"}}
{"entity":"story","version":1,"id":"generated-service-activation","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\n- Hosted composition activates an explicit generated ServiceBundle and publishes its reviewed provider/Connection identity.\n- Deployment grant references become durable exact-operation grants without weakening read-path or approval enforcement.\n- A human Identity principal can issue one short-lived, exact-input approval through a typed hosted route; it remains one-time at invocation.\n- Realm stays an optional verified PrincipalContext claim and never becomes a route, selector, or operation input.\n- Focused conformance and the full repository gate pass.\n","summary":"Compose generated service bundles, durable grants, and exact-input approvals into hosted Connectors.","title":"Activate generated services in the hosted runtime"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\n- Hosted composition activates an explicit generated ServiceBundle and publishes its reviewed provider/Connection identity.\n- Deployment grant references become durable exact-operation grants without weakening read-path or approval enforcement.\n- A human Identity principal can issue one short-lived, exact-input approval through a typed hosted route; it remains one-time at invocation.\n- Realm stays an optional verified PrincipalContext claim and never becomes a route, selector, or operation input.\n- Focused conformance and the full repository gate pass.\n","status":"draft","summary":"Compose generated service bundles, durable grants, and exact-input approvals into hosted Connectors.","title":"Activate generated services in the hosted runtime"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/generated-service-activation"},"payload":{"actor":"human:timo","at":1788309715730,"causation":"cmd-new-story-generated-service-activation","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:generated-service-activation@1#0~b6bbf4f01be046a1","executor":null,"recorded_at":"2026-09-02T00:41:55Z"}}
{"entity":"story","version":1,"id":"generated-service-activation","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"story:generated-service-factory"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000027","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788309715739,"causation":"cmd-story-generated-service-activation-derived_from-story-generated-service-factory","change":{"change":"related","relation":"derived_from","target":"story:generated-service-factory"},"correlation":"protocol-artifact-relate","event_id":"story:generated-service-activation@1#0~c5f6e37bfe5613a0","executor":null,"recorded_at":"2026-09-02T00:41:55Z"}}
{"entity":"story","version":1,"id":"generated-service-activation","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"human:timo","at":1788309715872,"causation":"cmd-move-story-generated-service-activation-1788309715872","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:generated-service-activation@2#0~d1b8d2c3ff0c3c64","executor":null,"recorded_at":"2026-09-02T00:41:55Z"}}
{"entity":"story","version":1,"id":"generated-service-activation","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"active"},"payload":{"actor":"human:timo","at":1788309715882,"causation":"cmd-move-story-generated-service-activation-1788309715882","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:generated-service-activation@3#0~e48f181f24deda08","executor":null,"recorded_at":"2026-09-02T00:41:55Z"}}
{"entity":"story","version":1,"id":"generated-service-activation","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/tmp/b10x-connectors-gate-20260902.log","source":"bash scripts/gate.sh","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788311779000,"causation":"cmd-evidence-story-generated-service-activation-test_result-1788311779121","change":{"change":"evidence","kind":"test_result","reference":"/tmp/b10x-connectors-gate-20260902.log","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:generated-service-activation@3#0~7f37ae57db9e3fec","executor":null,"recorded_at":"2026-09-02T01:16:19Z"}}
{"entity":"story","version":1,"id":"generated-service-activation","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"implemented"},"payload":{"actor":"human:timo","at":1788311779251,"causation":"cmd-move-story-generated-service-activation-1788311779251","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:generated-service-activation@4#0~e3e875b0e74f0bd5","executor":null,"recorded_at":"2026-09-02T01:16:19Z"}}
{"entity":"story","version":1,"id":"cli-shell-completions","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Context\n\n`connectors` is a clap derive CLI (`crates/connectors-cli/src/lib.rs:36`) with 14 top-level\nsubcommands and nested `auth`, `connection`, `event`, `operation` and `admin` groups. Nothing prints\na completion script, so an operator types every verb and flag from memory or from `--help`. clap\nships `clap_complete`, which renders the parser's own `Command` tree into each shell's syntax, so\nthe script cannot drift from the surface it completes.\n\nTimo asked for this on 2026-09-04: \"for my clap cli install this completion thing and implement it\".\n\n## Acceptance\n\n`connectors completions <shell>` prints to stdout a completion script for each of bash, zsh, fish,\nelvish and powershell that names every top-level subcommand, and the architecture fence in\n`crates/catalog-build/tests/main/architecture_fence.rs` admits the new dependency and the new line\ncount by name.\n\n## Scope\n\n- `crates/connectors-cli/Cargo.toml` — add `clap_complete`, same major as `clap`.\n- `crates/connectors-cli/Cargo.lock` — refreshed for the new dependency.\n- `crates/connectors-cli/src/lib.rs` — `Command::Completions { shell }`, one dispatch arm, one test.\n- `crates/catalog-build/tests/main/architecture_fence.rs` — `clap_complete` on `CLI_DEPENDENCIES`;\n  `CLI_TOTAL_LINE_LIMIT` raised with a dated note.\n- `README.md` — how to install the script for a shell.\n- `CHANGELOG.md` — Unreleased → Added.\n\n## Notes\n\n- The script is generated for the binary name `connectors`. Zwirn embeds this library as\n  `zwirn connectors …` and owns its own completion; `run_from` receives argv and does not know which\n  front door called it.\n- The generated script ignores `--output`: it is a shell program, not a result.\n- Dynamic completion (`clap_complete::env`) is unstable in 4.6 and is not used.\n","summary":"connectors completions <shell> emits a clap_complete script for bash, zsh, fish, elvish and PowerShell.","title":"The connectors CLI prints its own shell completion script"},"args":{"command":"create-entity","data":{"body":"## Context\n\n`connectors` is a clap derive CLI (`crates/connectors-cli/src/lib.rs:36`) with 14 top-level\nsubcommands and nested `auth`, `connection`, `event`, `operation` and `admin` groups. Nothing prints\na completion script, so an operator types every verb and flag from memory or from `--help`. clap\nships `clap_complete`, which renders the parser's own `Command` tree into each shell's syntax, so\nthe script cannot drift from the surface it completes.\n\nTimo asked for this on 2026-09-04: \"for my clap cli install this completion thing and implement it\".\n\n## Acceptance\n\n`connectors completions <shell>` prints to stdout a completion script for each of bash, zsh, fish,\nelvish and powershell that names every top-level subcommand, and the architecture fence in\n`crates/catalog-build/tests/main/architecture_fence.rs` admits the new dependency and the new line\ncount by name.\n\n## Scope\n\n- `crates/connectors-cli/Cargo.toml` — add `clap_complete`, same major as `clap`.\n- `crates/connectors-cli/Cargo.lock` — refreshed for the new dependency.\n- `crates/connectors-cli/src/lib.rs` — `Command::Completions { shell }`, one dispatch arm, one test.\n- `crates/catalog-build/tests/main/architecture_fence.rs` — `clap_complete` on `CLI_DEPENDENCIES`;\n  `CLI_TOTAL_LINE_LIMIT` raised with a dated note.\n- `README.md` — how to install the script for a shell.\n- `CHANGELOG.md` — Unreleased → Added.\n\n## Notes\n\n- The script is generated for the binary name `connectors`. Zwirn embeds this library as\n  `zwirn connectors …` and owns its own completion; `run_from` receives argv and does not know which\n  front door called it.\n- The generated script ignores `--output`: it is a shell program, not a result.\n- Dynamic completion (`clap_complete::env`) is unstable in 4.6 and is not used.\n","status":"draft","summary":"connectors completions <shell> emits a clap_complete script for bash, zsh, fish, elvish and PowerShell.","title":"The connectors CLI prints its own shell completion script"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/cli-shell-completions"},"payload":{"actor":"human:timo","at":1788476655848,"causation":"cmd-new-story-cli-shell-completions","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:cli-shell-completions@1#0~257c74c248c1b075","executor":null,"recorded_at":"2026-09-03T23:04:15Z"}}
{"entity":"story","version":1,"id":"release-gate-gitlab-size-fence","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Context\n\n`gh run list --workflow=release.yml` on 2026-09-04 shows v0.5.3, v0.5.4, v0.5.5 and v0.5.6 all\n`failure`; the latest published GitHub release is v0.5.2. Each run fails in job `gate .` at\n`architecture_fence::production_modules_obey_the_named_size_fence` (run 33765153362):\n`crates/integration-gitlab/src/backend.rs` grew to 2895 lines beyond its 2869-line waiver. Commit\n`d3707aa` (gitlab: support nested repository file paths, merged as PR #5) added the lines without\ntouching the waiver.\n\nThe same runs would then have failed the matrix leg's `--version` check: 28 crate manifests,\n`crates/connectors-cli/Cargo.toml` among them, still carry `version = \"0.5.3\"` while\n`[workspace.package] version` is 0.5.6, so the built binary reports 0.5.3\n(`grep -rl '^version = \"0.5.3\"' crates/*/Cargo.toml`). The v0.5.3 release commit `3df0b1f` bumped\nevery manifest; the v0.5.4 to v0.5.6 commits bumped only the root.\n\n## Acceptance\n\n`cargo test -p catalog-build --test main architecture_fence` passes at the root, every manifest\nunder `crates/` carries the released version, and the next `v*` tag's release workflow reaches the\n`publish` job.\n\n## Scope\n\n- `crates/integration-gitlab/src/backend.rs` and/or\n  `crates/catalog-build/tests/main/architecture_fence.rs` — bring the file back under its waiver,\n  or record the raise with the commit that caused it.\n- The 28 manifests carrying `version = \"0.5.3\"` — bumped as part of the next release cut.\n","summary":"backend.rs is 2895 lines against a 2869-line waiver, and 28 manifests still say 0.5.3; every tag since v0.5.3 fails before building.","title":"Release runs v0.5.3 to v0.5.6 fail the size fence on the GitLab backend"},"args":{"command":"create-entity","data":{"body":"## Context\n\n`gh run list --workflow=release.yml` on 2026-09-04 shows v0.5.3, v0.5.4, v0.5.5 and v0.5.6 all\n`failure`; the latest published GitHub release is v0.5.2. Each run fails in job `gate .` at\n`architecture_fence::production_modules_obey_the_named_size_fence` (run 33765153362):\n`crates/integration-gitlab/src/backend.rs` grew to 2895 lines beyond its 2869-line waiver. Commit\n`d3707aa` (gitlab: support nested repository file paths, merged as PR #5) added the lines without\ntouching the waiver.\n\nThe same runs would then have failed the matrix leg's `--version` check: 28 crate manifests,\n`crates/connectors-cli/Cargo.toml` among them, still carry `version = \"0.5.3\"` while\n`[workspace.package] version` is 0.5.6, so the built binary reports 0.5.3\n(`grep -rl '^version = \"0.5.3\"' crates/*/Cargo.toml`). The v0.5.3 release commit `3df0b1f` bumped\nevery manifest; the v0.5.4 to v0.5.6 commits bumped only the root.\n\n## Acceptance\n\n`cargo test -p catalog-build --test main architecture_fence` passes at the root, every manifest\nunder `crates/` carries the released version, and the next `v*` tag's release workflow reaches the\n`publish` job.\n\n## Scope\n\n- `crates/integration-gitlab/src/backend.rs` and/or\n  `crates/catalog-build/tests/main/architecture_fence.rs` — bring the file back under its waiver,\n  or record the raise with the commit that caused it.\n- The 28 manifests carrying `version = \"0.5.3\"` — bumped as part of the next release cut.\n","status":"draft","summary":"backend.rs is 2895 lines against a 2869-line waiver, and 28 manifests still say 0.5.3; every tag since v0.5.3 fails before building.","title":"Release runs v0.5.3 to v0.5.6 fail the size fence on the GitLab backend"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/release-gate-gitlab-size-fence"},"payload":{"actor":"human:timo","at":1788476655991,"causation":"cmd-new-story-release-gate-gitlab-size-fence","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:release-gate-gitlab-size-fence@1#0~2289523d8e94b08c","executor":null,"recorded_at":"2026-09-03T23:04:15Z"}}
{"entity":"story","version":1,"id":"cli-shell-completions","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"human:timo","at":1788476656126,"causation":"cmd-move-story-cli-shell-completions-1788476656126","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:cli-shell-completions@2#0~d1b8d2c3ff0c3c64","executor":null,"recorded_at":"2026-09-03T23:04:16Z"}}
{"entity":"story","version":1,"id":"cli-shell-completions","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"active"},"payload":{"actor":"human:timo","at":1788476656262,"causation":"cmd-move-story-cli-shell-completions-1788476656262","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:cli-shell-completions@3#0~e48f181f24deda08","executor":null,"recorded_at":"2026-09-03T23:04:16Z"}}
{"entity":"story","version":1,"id":"release-gate-gitlab-size-fence","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"proposed"},"payload":{"actor":"human:timo","at":1788476656397,"causation":"cmd-move-story-release-gate-gitlab-size-fence-1788476656397","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:release-gate-gitlab-size-fence@2#0~c531405e856b932f","executor":null,"recorded_at":"2026-09-03T23:04:16Z"}}
{"entity":"story","version":1,"id":"release-gate-gitlab-size-fence","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"active"},"payload":{"actor":"human:timo","at":1788476656531,"causation":"cmd-move-story-release-gate-gitlab-size-fence-1788476656531","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:release-gate-gitlab-size-fence@3#0~c833b53be4960cdf","executor":null,"recorded_at":"2026-09-03T23:04:16Z"}}
{"entity":"story","version":1,"id":"cli-shell-completions","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/Cargo.lock"},{"confidence":"cited","path":"crates/connectors-cli/Cargo.toml"},{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/Cargo.lock"},{"confidence":"cited","path":"crates/connectors-cli/Cargo.toml"},{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788476671228,"causation":"cmd-protocol-artifact-scope-story-cli-shell-completions-1788476671228","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-shell-completions@4#0~3ce976572ba7761c","executor":null,"recorded_at":"2026-09-03T23:04:31Z"}}
{"entity":"story","version":1,"id":"release-gate-gitlab-size-fence","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src"}]},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788476671307,"causation":"cmd-protocol-artifact-scope-story-release-gate-gitlab-size-fence-1788476671307","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:release-gate-gitlab-size-fence@4#0~b6477810a78bd554","executor":null,"recorded_at":"2026-09-03T23:04:31Z"}}
{"entity":"story","version":1,"id":"cli-shell-completions","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/connectors-gate/gate2.log","source":"bash scripts/gate.sh (full, 11 workspaces + --final), exit 0","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788477484000,"causation":"cmd-evidence-story-cli-shell-completions-test_result-1788477484771","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/connectors-gate/gate2.log","source":"bash scripts/gate.sh (full, 11 workspaces + --final), exit 0"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-shell-completions@4#0~b8341b12aee36ef6","executor":null,"recorded_at":"2026-09-03T23:18:04Z"}}
{"entity":"story","version":1,"id":"cli-shell-completions","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"implemented"},"payload":{"actor":"human:timo","at":1788477484900,"causation":"cmd-move-story-cli-shell-completions-1788477484900","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:cli-shell-completions@5#0~e3e875b0e74f0bd5","executor":null,"recorded_at":"2026-09-03T23:18:04Z"}}
{"entity":"story","version":1,"id":"release-gate-gitlab-size-fence","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/connectors-gate/gate2.log","source":"bash scripts/gate.sh (full, 11 workspaces + --final), exit 0","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788477484000,"causation":"cmd-evidence-story-release-gate-gitlab-size-fence-test_result-1788477484975","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/connectors-gate/gate2.log","source":"bash scripts/gate.sh (full, 11 workspaces + --final), exit 0"},"correlation":"protocol-artifact-evidence","event_id":"story:release-gate-gitlab-size-fence@4#0~01368b0bfea92171","executor":null,"recorded_at":"2026-09-03T23:18:04Z"}}
{"entity":"story","version":1,"id":"release-gate-gitlab-size-fence","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000016","to":"implemented"},"payload":{"actor":"human:timo","at":1788477485107,"causation":"cmd-move-story-release-gate-gitlab-size-fence-1788477485107","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:release-gate-gitlab-size-fence@5#0~5eeef370d6c71538","executor":null,"recorded_at":"2026-09-03T23:18:05Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Context\n\nEvery informative subcommand builds a `serde_json::Value` and hands it to `output::emit`\n(`crates/connectors-cli/src/lib.rs`, 13 call sites). In `text` — the default — that value is walked\nby one generic JSON pretty-printer, `output.rs:168`, which knows nothing about what it is printing:\n`key: value` per scalar, two spaces per nesting level, a bare `-` line per array element.\n\nMeasured on this machine against `connectors 0.5.7`:\n\n| command | lines | records |\n|---|---|---|\n| `connectors doctor` | 26 | 6 checks |\n| `connectors providers` | 927 | 65 providers |\n| `connectors providers --query jira` | 22 | 1 provider |\n| `connectors auth status` | 133 | 7 providers |\n\n`doctor` spends four lines per check — a bare `-`, then `check:`, `status:`, `detail:` — and the\none `warn` it found here renders identically to the five `ok` lines above it. The module that\nproduces the report already ranks the three states and says why (`doctor.rs:11`: \"`fail` is *this\ncannot work*; `warn` is *this works and you should know*\"), and the renderer discards that rank at\nthe last inch. `providers` averages 14 lines per provider, so the catalogue does not fit on a\nscreen and cannot be scanned.\n\n`-o compact` is closer to readable — `doctor` is 6 lines there — but it is not the default and it\nloses data: `compact` unwraps the single array field (`output.rs:126`, `output.rs:137`) and\ndiscards the scalar siblings beside it, so `healthy: true` is absent from every compact `doctor`\nrun. Verified: `connectors -o compact doctor | grep -c healthy` prints 0.\n\n## Acceptance\n\n`connectors doctor`, `providers` and `auth status` each render in `text` as one aligned row per\nrecord with the severity or readiness of a row distinguishable without reading its text, no output\nformat drops a field the value carries, and the bytes of `-o json` and `-o yaml` are unchanged.\n\n## Scope\n\n- `crates/connectors-console/src/output.rs` — the renderer. `text` gains a table form for an array\n  of uniform objects; `compact` stops discarding scalar siblings.\n- `crates/connectors-console/src/doctor.rs` — if severity needs to reach the renderer as more than\n  the string `\"ok\"`.\n- No change to `crates/connectors-cli` — the frontend passes a value and a format, and that is the\n  right seam. The thin-frontend line cap does not move for this story.\n\n## Notes\n\n- **The renderer is shared, so this is one change and every informative command gets it.** Thirteen\n  `output::emit` call sites, one `text` function. A per-command renderer would be thirteen places\n  for the format to drift.\n- **`json` and `yaml` are contracts and do not move.** `output.rs:1` states why a structured format\n  carries its failures on stdout; a caller parsing those bytes is not the reader this story is for.\n- **Colour is a decision, not an assumption.** If severity is carried by colour it needs TTY\n  detection and `NO_COLOR`; a symbol or a leading status column needs neither and works in a pipe.\n  Prefer the form that survives redirection, and treat colour as an addition on top of it.\n- **The compact data loss is a defect, not a preference.** It is written here because it lives in\n  the same function as the readability work, and it can be split into its own story if that is\n  wanted.\n","summary":"doctor, providers and auth status share one generic pretty-printer that spends 26 lines on 6 checks, hides the one warning among the ok rows, and drops fields in compact.","title":"Informative subcommands render as a scannable report, not a JSON dump"},"args":{"command":"create-entity","data":{"body":"## Context\n\nEvery informative subcommand builds a `serde_json::Value` and hands it to `output::emit`\n(`crates/connectors-cli/src/lib.rs`, 13 call sites). In `text` — the default — that value is walked\nby one generic JSON pretty-printer, `output.rs:168`, which knows nothing about what it is printing:\n`key: value` per scalar, two spaces per nesting level, a bare `-` line per array element.\n\nMeasured on this machine against `connectors 0.5.7`:\n\n| command | lines | records |\n|---|---|---|\n| `connectors doctor` | 26 | 6 checks |\n| `connectors providers` | 927 | 65 providers |\n| `connectors providers --query jira` | 22 | 1 provider |\n| `connectors auth status` | 133 | 7 providers |\n\n`doctor` spends four lines per check — a bare `-`, then `check:`, `status:`, `detail:` — and the\none `warn` it found here renders identically to the five `ok` lines above it. The module that\nproduces the report already ranks the three states and says why (`doctor.rs:11`: \"`fail` is *this\ncannot work*; `warn` is *this works and you should know*\"), and the renderer discards that rank at\nthe last inch. `providers` averages 14 lines per provider, so the catalogue does not fit on a\nscreen and cannot be scanned.\n\n`-o compact` is closer to readable — `doctor` is 6 lines there — but it is not the default and it\nloses data: `compact` unwraps the single array field (`output.rs:126`, `output.rs:137`) and\ndiscards the scalar siblings beside it, so `healthy: true` is absent from every compact `doctor`\nrun. Verified: `connectors -o compact doctor | grep -c healthy` prints 0.\n\n## Acceptance\n\n`connectors doctor`, `providers` and `auth status` each render in `text` as one aligned row per\nrecord with the severity or readiness of a row distinguishable without reading its text, no output\nformat drops a field the value carries, and the bytes of `-o json` and `-o yaml` are unchanged.\n\n## Scope\n\n- `crates/connectors-console/src/output.rs` — the renderer. `text` gains a table form for an array\n  of uniform objects; `compact` stops discarding scalar siblings.\n- `crates/connectors-console/src/doctor.rs` — if severity needs to reach the renderer as more than\n  the string `\"ok\"`.\n- No change to `crates/connectors-cli` — the frontend passes a value and a format, and that is the\n  right seam. The thin-frontend line cap does not move for this story.\n\n## Notes\n\n- **The renderer is shared, so this is one change and every informative command gets it.** Thirteen\n  `output::emit` call sites, one `text` function. A per-command renderer would be thirteen places\n  for the format to drift.\n- **`json` and `yaml` are contracts and do not move.** `output.rs:1` states why a structured format\n  carries its failures on stdout; a caller parsing those bytes is not the reader this story is for.\n- **Colour is a decision, not an assumption.** If severity is carried by colour it needs TTY\n  detection and `NO_COLOR`; a symbol or a leading status column needs neither and works in a pipe.\n  Prefer the form that survives redirection, and treat colour as an addition on top of it.\n- **The compact data loss is a defect, not a preference.** It is written here because it lives in\n  the same function as the readability work, and it can be split into its own story if that is\n  wanted.\n","status":"draft","summary":"doctor, providers and auth status share one generic pretty-printer that spends 26 lines on 6 checks, hides the one warning among the ok rows, and drops fields in compact.","title":"Informative subcommands render as a scannable report, not a JSON dump"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/informative-command-readability"},"payload":{"actor":"human:timo","at":1788479003628,"causation":"cmd-new-story-informative-command-readability","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:informative-command-readability@1#0~db3de0f040297415","executor":null,"recorded_at":"2026-09-03T23:43:23Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"}]},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788479003702,"causation":"cmd-protocol-artifact-scope-story-informative-command-readability-1788479003702","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:informative-command-readability@2#0~a1a066e8258cb883","executor":null,"recorded_at":"2026-09-03T23:43:23Z"}}
{"entity":"epic","version":1,"id":"beyond-http","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: beyond-http` key in 6 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-023` `story:beyond-http-facts-enter-the-document` — done — Beyond-HTTP facts enter the connector document orthogonally\n- `S-024` `story:one-zero-io-plan-seam-selects-a-closed-driver` — in-progress — One zero-IO plan seam selects a closed built-in driver\n- `S-025` `story:satellite-federation-has-a-threat-modeled-contract` — backlog — Satellite federation has a threat-modeled contract\n- `S-026` `story:one-real-non-http-driver-proves-the-model` — in-progress — One real non-HTTP driver proves the five-axis model\n- `S-027` `story:direct-byte-session-establishment-is-operation-scoped` — in-progress — Direct-byte session establishment is operation-scoped\n- `S-028` `story:external-driver-artifacts-stay-deferred` — blocked — External driver artifacts stay deferred behind attestation\n\n## Status\n\n`active`, derived from its stories (backlog: 1, blocked: 1, done: 1, in-progress: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Beyond http"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: beyond-http` key in 6 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-023` `story:beyond-http-facts-enter-the-document` — done — Beyond-HTTP facts enter the connector document orthogonally\n- `S-024` `story:one-zero-io-plan-seam-selects-a-closed-driver` — in-progress — One zero-IO plan seam selects a closed built-in driver\n- `S-025` `story:satellite-federation-has-a-threat-modeled-contract` — backlog — Satellite federation has a threat-modeled contract\n- `S-026` `story:one-real-non-http-driver-proves-the-model` — in-progress — One real non-HTTP driver proves the five-axis model\n- `S-027` `story:direct-byte-session-establishment-is-operation-scoped` — in-progress — Direct-byte session establishment is operation-scoped\n- `S-028` `story:external-driver-artifacts-stay-deferred` — blocked — External driver artifacts stay deferred behind attestation\n\n## Status\n\n`active`, derived from its stories (backlog: 1, blocked: 1, done: 1, in-progress: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Beyond http"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/beyond-http"},"payload":{"actor":"human:timo","at":1788479995934,"causation":"cmd-new-epic-beyond-http","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:beyond-http@1#0~2df03b30a62ed83d","executor":null,"recorded_at":"2026-09-03T23:59:55Z"}}
{"entity":"epic","version":1,"id":"beyond-http","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"proposed"},"payload":{"actor":"human:timo","at":1788479996069,"causation":"cmd-move-epic-beyond-http-1788479996069","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:beyond-http@2#0~081adaf6578f49c7","executor":null,"recorded_at":"2026-09-03T23:59:56Z"}}
{"entity":"epic","version":1,"id":"beyond-http","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"active"},"payload":{"actor":"human:timo","at":1788479996204,"causation":"cmd-move-epic-beyond-http-1788479996204","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:beyond-http@3#0~8c7e859d3507c067","executor":null,"recorded_at":"2026-09-03T23:59:56Z"}}
{"entity":"epic","version":1,"id":"build-order","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: build-order` key in 4 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-007` `story:m2-the-platform-skeleton-serves` — backlog — M2 — the platform skeleton serves in both postures\n- `S-008` `story:m3-connect-a-provider-and-invoke-it` — backlog — M3 — connect a real provider, grant it, invoke it\n- `S-009` `story:m4-events-reach-a-client-by-push-and-by-pull` — backlog — M4 — a provider event reaches a client by push and by pull, with provenance\n- `S-010` `story:m5-flux-re-points-and-the-gitlab-plugin-retires` — blocked — M5 — flux re-points at the platform and the gitlab plugin is deleted\n\n## Status\n\n`active`, derived from its stories (backlog: 3, blocked: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Build order"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: build-order` key in 4 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-007` `story:m2-the-platform-skeleton-serves` — backlog — M2 — the platform skeleton serves in both postures\n- `S-008` `story:m3-connect-a-provider-and-invoke-it` — backlog — M3 — connect a real provider, grant it, invoke it\n- `S-009` `story:m4-events-reach-a-client-by-push-and-by-pull` — backlog — M4 — a provider event reaches a client by push and by pull, with provenance\n- `S-010` `story:m5-flux-re-points-and-the-gitlab-plugin-retires` — blocked — M5 — flux re-points at the platform and the gitlab plugin is deleted\n\n## Status\n\n`active`, derived from its stories (backlog: 3, blocked: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Build order"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/build-order"},"payload":{"actor":"human:timo","at":1788479996341,"causation":"cmd-new-epic-build-order","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:build-order@1#0~d34d9eae694936da","executor":null,"recorded_at":"2026-09-03T23:59:56Z"}}
{"entity":"epic","version":1,"id":"build-order","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"human:timo","at":1788479996476,"causation":"cmd-move-epic-build-order-1788479996476","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:build-order@2#0~d1b8d2c3ff0c3c64","executor":null,"recorded_at":"2026-09-03T23:59:56Z"}}
{"entity":"epic","version":1,"id":"build-order","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"active"},"payload":{"actor":"human:timo","at":1788479996611,"causation":"cmd-move-epic-build-order-1788479996611","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:build-order@3#0~e48f181f24deda08","executor":null,"recorded_at":"2026-09-03T23:59:56Z"}}
{"entity":"epic","version":1,"id":"carried-constraints","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: carried-constraints` key in 5 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-011` `story:deployment-declared-destination-aperture` — backlog — Deployment-declared destination aperture\n- `S-012` `story:declarative-webhook-routing-grammar` — backlog — Webhook verification and attribution are declared as data, not as a per-provider script\n- `S-013` `story:connect-session-oauth-custody-in-personal-posture` — backlog — Decide the connect-session ↔ OAuth-callback custody chain in personal posture\n- `S-014` `story:auth-as-tool-result` — backlog — Not-connected is a next step: the response carries a connect URL\n- `S-030` `story:raw-proxy-is-break-glass-not-a-model-capability` — backlog — Raw proxy is break-glass authority, not a model capability\n\n## Status\n\n`draft`, derived from its stories (backlog: 5). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Carried constraints"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: carried-constraints` key in 5 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-011` `story:deployment-declared-destination-aperture` — backlog — Deployment-declared destination aperture\n- `S-012` `story:declarative-webhook-routing-grammar` — backlog — Webhook verification and attribution are declared as data, not as a per-provider script\n- `S-013` `story:connect-session-oauth-custody-in-personal-posture` — backlog — Decide the connect-session ↔ OAuth-callback custody chain in personal posture\n- `S-014` `story:auth-as-tool-result` — backlog — Not-connected is a next step: the response carries a connect URL\n- `S-030` `story:raw-proxy-is-break-glass-not-a-model-capability` — backlog — Raw proxy is break-glass authority, not a model capability\n\n## Status\n\n`draft`, derived from its stories (backlog: 5). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Carried constraints"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/carried-constraints"},"payload":{"actor":"human:timo","at":1788479996745,"causation":"cmd-new-epic-carried-constraints","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:carried-constraints@1#0~69b8a6e2a89a4a80","executor":null,"recorded_at":"2026-09-03T23:59:56Z"}}
{"entity":"epic","version":1,"id":"catalog-adoptions","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: catalog-adoptions` key in 3 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-004` `story:adopt-token-response-metadata` — backlog — An OAuth token response can carry declared metadata into the connection, not the credential store\n- `S-005` `story:header-name-rate-limit-retry` — backlog — A rate limit the vendor discloses at runtime can be declared by header name\n- `S-006` `story:per-service-verification-probes` — backlog — A service declares how a credential is verified, and what a failure means\n\n## Status\n\n`draft`, derived from its stories (backlog: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Catalog adoptions"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: catalog-adoptions` key in 3 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-004` `story:adopt-token-response-metadata` — backlog — An OAuth token response can carry declared metadata into the connection, not the credential store\n- `S-005` `story:header-name-rate-limit-retry` — backlog — A rate limit the vendor discloses at runtime can be declared by header name\n- `S-006` `story:per-service-verification-probes` — backlog — A service declares how a credential is verified, and what a failure means\n\n## Status\n\n`draft`, derived from its stories (backlog: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Catalog adoptions"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/catalog-adoptions"},"payload":{"actor":"human:timo","at":1788479996880,"causation":"cmd-new-epic-catalog-adoptions","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:catalog-adoptions@1#0~320d8a7ccd519d67","executor":null,"recorded_at":"2026-09-03T23:59:56Z"}}
{"entity":"epic","version":1,"id":"catalog-day-one","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: catalog-day-one` key in 4 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-001` `story:the-document-carries-the-callers-contract` — done — The document carries the caller's contract, so nothing at runtime parses source\n- `S-002` `story:effects-are-read-never-derived` — blocked — Per-operation effects are read from the document, never derived\n- `S-003` `story:the-lockfile-gets-a-verifier` — done — `catalog check` verifies every addressable hash and refuses unverifiable claims\n- `S-015` `story:retire-the-quirks-umbrella` — done — Retire the `quirks` umbrella — pagination, rate limits and error envelopes are ordinary facts\n\n## Status\n\n`active`, derived from its stories (blocked: 1, done: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Catalog day one"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: catalog-day-one` key in 4 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-001` `story:the-document-carries-the-callers-contract` — done — The document carries the caller's contract, so nothing at runtime parses source\n- `S-002` `story:effects-are-read-never-derived` — blocked — Per-operation effects are read from the document, never derived\n- `S-003` `story:the-lockfile-gets-a-verifier` — done — `catalog check` verifies every addressable hash and refuses unverifiable claims\n- `S-015` `story:retire-the-quirks-umbrella` — done — Retire the `quirks` umbrella — pagination, rate limits and error envelopes are ordinary facts\n\n## Status\n\n`active`, derived from its stories (blocked: 1, done: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Catalog day one"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/catalog-day-one"},"payload":{"actor":"human:timo","at":1788479997016,"causation":"cmd-new-epic-catalog-day-one","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:catalog-day-one@1#0~af04f05fcb9a110f","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"catalog-day-one","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"proposed"},"payload":{"actor":"human:timo","at":1788479997151,"causation":"cmd-move-epic-catalog-day-one-1788479997151","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:catalog-day-one@2#0~035cef12cd9405cc","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"catalog-day-one","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"active"},"payload":{"actor":"human:timo","at":1788479997285,"causation":"cmd-move-epic-catalog-day-one-1788479997285","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:catalog-day-one@3#0~489ac3135f862260","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"contract-release","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: contract-release` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-031` `story:contract-bundles-are-versioned-and-pinned` — backlog — Connector contract bundles are versioned, signed, and pinned\n\n## Status\n\n`draft`, derived from its stories (backlog: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Contract release"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: contract-release` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-031` `story:contract-bundles-are-versioned-and-pinned` — backlog — Connector contract bundles are versioned, signed, and pinned\n\n## Status\n\n`draft`, derived from its stories (backlog: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Contract release"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/contract-release"},"payload":{"actor":"human:timo","at":1788479997423,"causation":"cmd-new-epic-contract-release","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:contract-release@1#0~dba893682f1171da","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"credential-production","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: credential-production` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-034` `story:production-credential-custody-closes` — backlog — Production credential custody closes with owner evidence\n\n## Status\n\n`draft`, derived from its stories (backlog: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Credential production"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: credential-production` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-034` `story:production-credential-custody-closes` — backlog — Production credential custody closes with owner evidence\n\n## Status\n\n`draft`, derived from its stories (backlog: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Credential production"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/credential-production"},"payload":{"actor":"human:timo","at":1788479997558,"causation":"cmd-new-epic-credential-production","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:credential-production@1#0~b09a438701f1bef7","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"deployment-packs","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: deployment-packs` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-075` `story:deployment-catalogs-are-external-packs` — backlog — Deployment catalogs are external immutable packs\n\n## Status\n\n`draft`, derived from its stories (backlog: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Deployment packs"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: deployment-packs` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-075` `story:deployment-catalogs-are-external-packs` — backlog — Deployment catalogs are external immutable packs\n\n## Status\n\n`draft`, derived from its stories (backlog: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Deployment packs"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/deployment-packs"},"payload":{"actor":"human:timo","at":1788479997694,"causation":"cmd-new-epic-deployment-packs","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:deployment-packs@1#0~238a1705c5a0a1ea","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"endpoint-plane","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: endpoint-plane` key in 8 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-058` `story:mysql-and-postgresql-become-connectors` — done — MySQL and PostgreSQL become connectors\n- `S-059` `story:kubernetes-discovers-database-endpoints` — done — Kubernetes discovers database endpoints\n- `S-060` `story:monitoring-joins-the-mcp-toolset` — done — Monitoring joins the MCP toolset\n- `S-061` `story:a-logged-in-person-needs-no-configuration` — done — A logged-in person needs no configuration\n- `S-062` `story:discovery-reads-the-cluster-scoped-database-resources` — done — Discovery reads the cluster-scoped database resources\n- `S-064` `story:monitoring-tool-schemas-tell-the-truth` — done — Monitoring tool schemas tell the truth\n- `S-065` `story:monitoring-refusals-name-the-upstream` — done — Monitoring refusals name the upstream\n- `S-066` `story:the-transport-refusal-names-its-class` — done — The transport refusal names its class\n\n## Status\n\n`implemented`, derived from its stories (done: 8). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Endpoint plane"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: endpoint-plane` key in 8 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-058` `story:mysql-and-postgresql-become-connectors` — done — MySQL and PostgreSQL become connectors\n- `S-059` `story:kubernetes-discovers-database-endpoints` — done — Kubernetes discovers database endpoints\n- `S-060` `story:monitoring-joins-the-mcp-toolset` — done — Monitoring joins the MCP toolset\n- `S-061` `story:a-logged-in-person-needs-no-configuration` — done — A logged-in person needs no configuration\n- `S-062` `story:discovery-reads-the-cluster-scoped-database-resources` — done — Discovery reads the cluster-scoped database resources\n- `S-064` `story:monitoring-tool-schemas-tell-the-truth` — done — Monitoring tool schemas tell the truth\n- `S-065` `story:monitoring-refusals-name-the-upstream` — done — Monitoring refusals name the upstream\n- `S-066` `story:the-transport-refusal-names-its-class` — done — The transport refusal names its class\n\n## Status\n\n`implemented`, derived from its stories (done: 8). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Endpoint plane"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/endpoint-plane"},"payload":{"actor":"human:timo","at":1788479997831,"causation":"cmd-new-epic-endpoint-plane","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:endpoint-plane@1#0~bd729ccdff72cf0b","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"endpoint-plane","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000025","to":"proposed"},"payload":{"actor":"human:timo","at":1788479997967,"causation":"cmd-move-epic-endpoint-plane-1788479997967","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:endpoint-plane@2#0~b0cdceaaa91e388d","executor":null,"recorded_at":"2026-09-03T23:59:57Z"}}
{"entity":"epic","version":1,"id":"endpoint-plane","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000025","to":"active"},"payload":{"actor":"human:timo","at":1788479998103,"causation":"cmd-move-epic-endpoint-plane-1788479998103","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:endpoint-plane@3#0~8cecb13e8118b9bd","executor":null,"recorded_at":"2026-09-03T23:59:58Z"}}
{"entity":"epic","version":1,"id":"endpoint-plane","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000025","to":"implemented"},"payload":{"actor":"human:timo","at":1788479998238,"causation":"cmd-move-epic-endpoint-plane-1788479998238","change":{"change":"moved","decided_on":{},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:endpoint-plane@4#0~cf34dd050cc07f87","executor":null,"recorded_at":"2026-09-03T23:59:58Z"}}
{"entity":"epic","version":1,"id":"enforced-authority","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: enforced-authority` key in 8 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-043` `story:admission-proofs-become-unforgeable` — done — Admission proofs become unforgeable\n- `S-044` `story:a-grant-decision-is-evaluated-not-asserted` — done — A Grant decision is evaluated, not asserted\n- `S-045` `story:an-approval-is-redeemed-exactly-once` — done — An approval is redeemed exactly once\n- `S-046` `story:the-hosted-route-enforces-instead-of-refusing` — done — The hosted route enforces instead of refusing\n- `S-047` `story:effect-backends-trust-only-proofs` — done — Effect backends trust only proofs\n- `S-048` `story:a-companion-reply-is-claimed-exactly-once-locally` — done — A companion reply is claimed exactly once, locally too\n- `S-049` `story:a-session-signal-carries-authority` — done — A session signal carries authority\n- `S-050` `story:the-claims-journal-survives-a-full-life` — backlog — The claims journal survives a full life\n\n## Status\n\n`active`, derived from its stories (backlog: 1, done: 7). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Enforced authority"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: enforced-authority` key in 8 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-043` `story:admission-proofs-become-unforgeable` — done — Admission proofs become unforgeable\n- `S-044` `story:a-grant-decision-is-evaluated-not-asserted` — done — A Grant decision is evaluated, not asserted\n- `S-045` `story:an-approval-is-redeemed-exactly-once` — done — An approval is redeemed exactly once\n- `S-046` `story:the-hosted-route-enforces-instead-of-refusing` — done — The hosted route enforces instead of refusing\n- `S-047` `story:effect-backends-trust-only-proofs` — done — Effect backends trust only proofs\n- `S-048` `story:a-companion-reply-is-claimed-exactly-once-locally` — done — A companion reply is claimed exactly once, locally too\n- `S-049` `story:a-session-signal-carries-authority` — done — A session signal carries authority\n- `S-050` `story:the-claims-journal-survives-a-full-life` — backlog — The claims journal survives a full life\n\n## Status\n\n`active`, derived from its stories (backlog: 1, done: 7). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Enforced authority"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/enforced-authority"},"payload":{"actor":"human:timo","at":1788479998374,"causation":"cmd-new-epic-enforced-authority","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:enforced-authority@1#0~9aac0e402a10c3cf","executor":null,"recorded_at":"2026-09-03T23:59:58Z"}}
{"entity":"epic","version":1,"id":"enforced-authority","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000028","to":"proposed"},"payload":{"actor":"human:timo","at":1788479998510,"causation":"cmd-move-epic-enforced-authority-1788479998510","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:enforced-authority@2#0~36bd28f03013dbfa","executor":null,"recorded_at":"2026-09-03T23:59:58Z"}}
{"entity":"epic","version":1,"id":"enforced-authority","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000028","to":"active"},"payload":{"actor":"human:timo","at":1788479998648,"causation":"cmd-move-epic-enforced-authority-1788479998648","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:enforced-authority@3#0~0bc80c40ef8e4bee","executor":null,"recorded_at":"2026-09-03T23:59:58Z"}}
{"entity":"epic","version":1,"id":"local-product","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: local-product` key in 4 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-035` `story:the-cli-runs-without-being-configured-by-hand` — in-progress — The CLI runs without being configured by hand\n- `S-036` `story:the-os-keyring-becomes-the-local-store` — in-progress — The OS keyring becomes the local credential store\n- `S-041` `story:state-becomes-a-port` — in-progress — State becomes a port, with a SQLite backend\n- `S-042` `story:one-composed-local-placement` — backlog — One composed local placement, called by both the CLI and Zwirn\n\n## Status\n\n`active`, derived from its stories (backlog: 1, in-progress: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Local product"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: local-product` key in 4 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-035` `story:the-cli-runs-without-being-configured-by-hand` — in-progress — The CLI runs without being configured by hand\n- `S-036` `story:the-os-keyring-becomes-the-local-store` — in-progress — The OS keyring becomes the local credential store\n- `S-041` `story:state-becomes-a-port` — in-progress — State becomes a port, with a SQLite backend\n- `S-042` `story:one-composed-local-placement` — backlog — One composed local placement, called by both the CLI and Zwirn\n\n## Status\n\n`active`, derived from its stories (backlog: 1, in-progress: 3). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Local product"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/local-product"},"payload":{"actor":"human:timo","at":1788479998783,"causation":"cmd-new-epic-local-product","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:local-product@1#0~5fe5caca468e44c0","executor":null,"recorded_at":"2026-09-03T23:59:58Z"}}
{"entity":"epic","version":1,"id":"local-product","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000031","to":"proposed"},"payload":{"actor":"human:timo","at":1788479998922,"causation":"cmd-move-epic-local-product-1788479998922","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:local-product@2#0~33e0d05c2489b694","executor":null,"recorded_at":"2026-09-03T23:59:58Z"}}
{"entity":"epic","version":1,"id":"local-product","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000031","to":"active"},"payload":{"actor":"human:timo","at":1788479999060,"causation":"cmd-move-epic-local-product-1788479999060","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:local-product@3#0~8fa7c2e811289db8","executor":null,"recorded_at":"2026-09-03T23:59:59Z"}}
{"entity":"epic","version":1,"id":"mcp-entry","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: mcp-entry` key in 5 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-053` `story:the-hosted-server-speaks-mcp-through-the-admission-seam` — done — The hosted server speaks MCP through the admission seam\n- `S-054` `story:pod-logs-are-a-hosted-read-only-kubernetes-operation` — done — Pod logs are a hosted read-only Kubernetes operation\n- `S-055` `story:a-self-service-scope-subset-needs-no-operator` — done — A self-service scope subset needs no operator\n- `S-056` `story:the-cli-bridges-mcp-over-stdio` — done — The CLI bridges MCP over stdio\n- `S-063` `story:the-deployment-list-pages-instead-of-refusing` — done — The deployment list pages instead of refusing\n\n## Status\n\n`implemented`, derived from its stories (done: 5). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Mcp entry"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: mcp-entry` key in 5 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-053` `story:the-hosted-server-speaks-mcp-through-the-admission-seam` — done — The hosted server speaks MCP through the admission seam\n- `S-054` `story:pod-logs-are-a-hosted-read-only-kubernetes-operation` — done — Pod logs are a hosted read-only Kubernetes operation\n- `S-055` `story:a-self-service-scope-subset-needs-no-operator` — done — A self-service scope subset needs no operator\n- `S-056` `story:the-cli-bridges-mcp-over-stdio` — done — The CLI bridges MCP over stdio\n- `S-063` `story:the-deployment-list-pages-instead-of-refusing` — done — The deployment list pages instead of refusing\n\n## Status\n\n`implemented`, derived from its stories (done: 5). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Mcp entry"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/mcp-entry"},"payload":{"actor":"human:timo","at":1788479999198,"causation":"cmd-new-epic-mcp-entry","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:mcp-entry@1#0~88dbae902aa954f3","executor":null,"recorded_at":"2026-09-03T23:59:59Z"}}
{"entity":"epic","version":1,"id":"mcp-entry","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000034","to":"proposed"},"payload":{"actor":"human:timo","at":1788479999335,"causation":"cmd-move-epic-mcp-entry-1788479999335","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:mcp-entry@2#0~1a89715c0ab4ce77","executor":null,"recorded_at":"2026-09-03T23:59:59Z"}}
{"entity":"epic","version":1,"id":"mcp-entry","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000034","to":"active"},"payload":{"actor":"human:timo","at":1788479999476,"causation":"cmd-move-epic-mcp-entry-1788479999476","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:mcp-entry@3#0~2eb905fb95f71fd7","executor":null,"recorded_at":"2026-09-03T23:59:59Z"}}
{"entity":"epic","version":1,"id":"mcp-entry","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000034","to":"implemented"},"payload":{"actor":"human:timo","at":1788479999612,"causation":"cmd-move-epic-mcp-entry-1788479999612","change":{"change":"moved","decided_on":{},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:mcp-entry@4#0~20239da96daf9769","executor":null,"recorded_at":"2026-09-03T23:59:59Z"}}
{"entity":"epic","version":1,"id":"native-voice","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: native-voice` key in 2 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-032` `story:sip-driver-terminates-one-governed-call` — in-progress — The SIP driver terminates one governed call\n- `S-033` `story:neutral-rtvbp-bridges-the-call-to-an-application-channel` — in-progress — Neutral RTVBP bridges the call to an application channel\n\n## Status\n\n`active`, derived from its stories (in-progress: 2). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Native voice"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: native-voice` key in 2 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-032` `story:sip-driver-terminates-one-governed-call` — in-progress — The SIP driver terminates one governed call\n- `S-033` `story:neutral-rtvbp-bridges-the-call-to-an-application-channel` — in-progress — Neutral RTVBP bridges the call to an application channel\n\n## Status\n\n`active`, derived from its stories (in-progress: 2). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Native voice"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/native-voice"},"payload":{"actor":"human:timo","at":1788479999749,"causation":"cmd-new-epic-native-voice","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:native-voice@1#0~75e46b8d61c36313","executor":null,"recorded_at":"2026-09-03T23:59:59Z"}}
{"entity":"epic","version":1,"id":"native-voice","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000037","to":"proposed"},"payload":{"actor":"human:timo","at":1788479999886,"causation":"cmd-move-epic-native-voice-1788479999886","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:native-voice@2#0~26ba678c3a4fcf4e","executor":null,"recorded_at":"2026-09-03T23:59:59Z"}}
{"entity":"epic","version":1,"id":"native-voice","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000037","to":"active"},"payload":{"actor":"human:timo","at":1788480000023,"causation":"cmd-move-epic-native-voice-1788480000023","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:native-voice@3#0~288ba5d3b1384faa","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"oauth-consolidation","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: oauth-consolidation` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-069` `story:one-oauth-implementation-not-three` — done — One OAuth implementation, not three\n\n## Status\n\n`implemented`, derived from its stories (done: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Oauth consolidation"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: oauth-consolidation` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-069` `story:one-oauth-implementation-not-three` — done — One OAuth implementation, not three\n\n## Status\n\n`implemented`, derived from its stories (done: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Oauth consolidation"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/oauth-consolidation"},"payload":{"actor":"human:timo","at":1788480000161,"causation":"cmd-new-epic-oauth-consolidation","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:oauth-consolidation@1#0~f10a59dfb344c808","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"oauth-consolidation","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000040","to":"proposed"},"payload":{"actor":"human:timo","at":1788480000298,"causation":"cmd-move-epic-oauth-consolidation-1788480000298","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:oauth-consolidation@2#0~717b52c4816dc414","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"oauth-consolidation","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000040","to":"active"},"payload":{"actor":"human:timo","at":1788480000435,"causation":"cmd-move-epic-oauth-consolidation-1788480000435","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:oauth-consolidation@3#0~ee79c1853ef76f38","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"oauth-consolidation","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000040","to":"implemented"},"payload":{"actor":"human:timo","at":1788480000570,"causation":"cmd-move-epic-oauth-consolidation-1788480000570","change":{"change":"moved","decided_on":{},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:oauth-consolidation@4#0~24289ea74833e880","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"post-m1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: post-m1` key in 5 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-018` `story:the-explorer-works-against-the-new-site-json` — done — The web explorer works against the site JSON M1 actually emits\n- `S-019` `story:retire-the-flux-connectors-identity` — done — Retire the flux-connectors identity from the artifacts\n- `S-020` `story:a-ci-gate-exists` — in-progress — A CI gate exists, and it runs what the monorepo claims it runs\n- `S-021` `story:coverage-regains-its-second-direction` — backlog — Coverage regains its second direction: every gap between declared and published has a reason\n- `S-022` `story:orphaned-inputs-are-removed-or-re-owned` — done — Orphaned inputs are removed or re-owned\n\n## Status\n\n`active`, derived from its stories (backlog: 1, done: 3, in-progress: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Post m1"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: post-m1` key in 5 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-018` `story:the-explorer-works-against-the-new-site-json` — done — The web explorer works against the site JSON M1 actually emits\n- `S-019` `story:retire-the-flux-connectors-identity` — done — Retire the flux-connectors identity from the artifacts\n- `S-020` `story:a-ci-gate-exists` — in-progress — A CI gate exists, and it runs what the monorepo claims it runs\n- `S-021` `story:coverage-regains-its-second-direction` — backlog — Coverage regains its second direction: every gap between declared and published has a reason\n- `S-022` `story:orphaned-inputs-are-removed-or-re-owned` — done — Orphaned inputs are removed or re-owned\n\n## Status\n\n`active`, derived from its stories (backlog: 1, done: 3, in-progress: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Post m1"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/post-m1"},"payload":{"actor":"human:timo","at":1788480000709,"causation":"cmd-new-epic-post-m1","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:post-m1@1#0~c57dfcf102dcdbbe","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"post-m1","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000043","to":"proposed"},"payload":{"actor":"human:timo","at":1788480000848,"causation":"cmd-move-epic-post-m1-1788480000848","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:post-m1@2#0~452cec5bb2cf4d41","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"post-m1","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000043","to":"active"},"payload":{"actor":"human:timo","at":1788480000985,"causation":"cmd-move-epic-post-m1-1788480000985","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:post-m1@3#0~f22f644cdebc4ca9","executor":null,"recorded_at":"2026-09-04T00:00:00Z"}}
{"entity":"epic","version":1,"id":"public-surface","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: public-surface` key in 2 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-067` `story:the-server-serves-its-own-openapi-contract` — done — The server serves its own OpenAPI contract\n- `S-068` `story:a-public-documentation-page-renders-the-contract` — done — A public documentation page renders the contract\n\n## Status\n\n`implemented`, derived from its stories (done: 2). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Public surface"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: public-surface` key in 2 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-067` `story:the-server-serves-its-own-openapi-contract` — done — The server serves its own OpenAPI contract\n- `S-068` `story:a-public-documentation-page-renders-the-contract` — done — A public documentation page renders the contract\n\n## Status\n\n`implemented`, derived from its stories (done: 2). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Public surface"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/public-surface"},"payload":{"actor":"human:timo","at":1788480001121,"causation":"cmd-new-epic-public-surface","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:public-surface@1#0~89d1d883dcd970cf","executor":null,"recorded_at":"2026-09-04T00:00:01Z"}}
{"entity":"epic","version":1,"id":"public-surface","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000046","to":"proposed"},"payload":{"actor":"human:timo","at":1788480001256,"causation":"cmd-move-epic-public-surface-1788480001256","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:public-surface@2#0~6454e9f49733dcce","executor":null,"recorded_at":"2026-09-04T00:00:01Z"}}
{"entity":"epic","version":1,"id":"public-surface","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000046","to":"active"},"payload":{"actor":"human:timo","at":1788480001397,"causation":"cmd-move-epic-public-surface-1788480001397","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:public-surface@3#0~875da470df07212a","executor":null,"recorded_at":"2026-09-04T00:00:01Z"}}
{"entity":"epic","version":1,"id":"public-surface","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000046","to":"implemented"},"payload":{"actor":"human:timo","at":1788480001536,"causation":"cmd-move-epic-public-surface-1788480001536","change":{"change":"moved","decided_on":{},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:public-surface@4#0~d25c69f7eb628eda","executor":null,"recorded_at":"2026-09-04T00:00:01Z"}}
{"entity":"epic","version":1,"id":"sources","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: sources` key in 2 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-016` `story:sources-are-processed-by-code` — ready — Sources are processed by code: the index is validated, checksummed and refreshed by the tool\n- `S-017` `story:mint-source-entries-from-the-mined-catalogs` — backlog — Mint source entries from the mined competitor catalogs\n\n## Status\n\n`active`, derived from its stories (backlog: 1, ready: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Sources"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: sources` key in 2 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-016` `story:sources-are-processed-by-code` — ready — Sources are processed by code: the index is validated, checksummed and refreshed by the tool\n- `S-017` `story:mint-source-entries-from-the-mined-catalogs` — backlog — Mint source entries from the mined competitor catalogs\n\n## Status\n\n`active`, derived from its stories (backlog: 1, ready: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Sources"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/sources"},"payload":{"actor":"human:timo","at":1788480001672,"causation":"cmd-new-epic-sources","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:sources@1#0~c261ef9f39ddb414","executor":null,"recorded_at":"2026-09-04T00:00:01Z"}}
{"entity":"epic","version":1,"id":"sources","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000052","to":"proposed"},"payload":{"actor":"human:timo","at":1788480001809,"causation":"cmd-move-epic-sources-1788480001809","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:sources@2#0~45b55d5eb4735747","executor":null,"recorded_at":"2026-09-04T00:00:01Z"}}
{"entity":"epic","version":1,"id":"sources","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000052","to":"active"},"payload":{"actor":"human:timo","at":1788480001947,"causation":"cmd-move-epic-sources-1788480001947","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:sources@3#0~eb50843a62d691e7","executor":null,"recorded_at":"2026-09-04T00:00:01Z"}}
{"entity":"epic","version":1,"id":"subscription-custody","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: subscription-custody` key in 7 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-070` `story:a-provider-can-hold-a-credential-it-cannot-spend` — done — A provider can hold a credential it cannot spend\n- `S-071` `story:claude-code-joins-the-catalog` — done — Claude Code joins the catalog as a custody-only provider\n- `S-072` `story:the-anthropic-api-key-arrives-through-a-connect-session` — backlog — The Anthropic API key arrives through a Connect Session\n- `S-073` `story:the-hosted-posture-connects-a-catalogued-provider` — backlog — The hosted posture connects a catalogued provider\n- `S-074` `story:the-cli-drives-a-hosted-connection` — backlog — The CLI drives a hosted connection\n- `S-076` `story:an-agent-attempt-can-lease-a-user-subscription` — done — An agent attempt can lease a user's subscription\n- `S-077` `story:claude-subscriptions-connect-with-pkce-and-refresh` — done — Claude subscriptions connect with PKCE and refresh\n\n## Status\n\n`active`, derived from its stories (backlog: 3, done: 4). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Subscription custody"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: subscription-custody` key in 7 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-070` `story:a-provider-can-hold-a-credential-it-cannot-spend` — done — A provider can hold a credential it cannot spend\n- `S-071` `story:claude-code-joins-the-catalog` — done — Claude Code joins the catalog as a custody-only provider\n- `S-072` `story:the-anthropic-api-key-arrives-through-a-connect-session` — backlog — The Anthropic API key arrives through a Connect Session\n- `S-073` `story:the-hosted-posture-connects-a-catalogued-provider` — backlog — The hosted posture connects a catalogued provider\n- `S-074` `story:the-cli-drives-a-hosted-connection` — backlog — The CLI drives a hosted connection\n- `S-076` `story:an-agent-attempt-can-lease-a-user-subscription` — done — An agent attempt can lease a user's subscription\n- `S-077` `story:claude-subscriptions-connect-with-pkce-and-refresh` — done — Claude subscriptions connect with PKCE and refresh\n\n## Status\n\n`active`, derived from its stories (backlog: 3, done: 4). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Subscription custody"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/subscription-custody"},"payload":{"actor":"human:timo","at":1788480002083,"causation":"cmd-new-epic-subscription-custody","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:subscription-custody@1#0~77314ca399877042","executor":null,"recorded_at":"2026-09-04T00:00:02Z"}}
{"entity":"epic","version":1,"id":"subscription-custody","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000055","to":"proposed"},"payload":{"actor":"human:timo","at":1788480002223,"causation":"cmd-move-epic-subscription-custody-1788480002223","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:subscription-custody@2#0~d0e0a485cd3cae52","executor":null,"recorded_at":"2026-09-04T00:00:02Z"}}
{"entity":"epic","version":1,"id":"subscription-custody","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000055","to":"active"},"payload":{"actor":"human:timo","at":1788480002359,"causation":"cmd-move-epic-subscription-custody-1788480002359","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:subscription-custody@3#0~24685af584a6cb56","executor":null,"recorded_at":"2026-09-04T00:00:02Z"}}
{"entity":"epic","version":1,"id":"substrate-integration","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Provenance\n\nRead from the `epic: substrate-integration` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-029` `story:substrate-events-enter-durable-delivery-with-gap-recovery` — blocked — Substrate events enter durable delivery with gap recovery\n\n## Status\n\n`active`, derived from its stories (blocked: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","title":"Substrate integration"},"args":{"command":"create-entity","data":{"body":"## Provenance\n\nRead from the `epic: substrate-integration` key in 1 file(s) under `docs/stories/`. No source document\nexists for this epic itself; the grouping is a frontmatter value and nothing else. Migrated\n2026-09-04 by the `aep-planning:story-migration` skill. **read**\n\n## Stories\n\n- `S-029` `story:substrate-events-enter-durable-delivery-with-gap-recovery` — blocked — Substrate events enter durable delivery with gap recovery\n\n## Status\n\n`active`, derived from its stories (blocked: 1). **inferred** — no source document states a status\nfor this epic, so the rung follows the work underneath it: all done is `implemented`, any work\nstarted is `active`, nothing started is `draft`.\n","status":"draft","title":"Substrate integration"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/substrate-integration"},"payload":{"actor":"human:timo","at":1788480002497,"causation":"cmd-new-epic-substrate-integration","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:substrate-integration@1#0~197b1f2263392e5f","executor":null,"recorded_at":"2026-09-04T00:00:02Z"}}
{"entity":"epic","version":1,"id":"substrate-integration","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000058","to":"proposed"},"payload":{"actor":"human:timo","at":1788480002636,"causation":"cmd-move-epic-substrate-integration-1788480002636","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:substrate-integration@2#0~2414956f5e765fb5","executor":null,"recorded_at":"2026-09-04T00:00:02Z"}}
{"entity":"epic","version":1,"id":"substrate-integration","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000058","to":"active"},"payload":{"actor":"human:timo","at":1788480002773,"causation":"cmd-move-epic-substrate-integration-1788480002773","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:substrate-integration@3#0~cc753053f9767bd5","executor":null,"recorded_at":"2026-09-04T00:00:02Z"}}
{"entity":"epic","version":1,"id":"beyond-http","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"title":"Beyond HTTP"},"args":{"changes":{"title":"Beyond HTTP"},"command":"update-entity","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788480009728,"causation":"cmd-protocol-artifact-set-epic-beyond-http-1788480009728","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"epic:beyond-http@4#0~8ebb809e8053712f","executor":null,"recorded_at":"2026-09-04T00:00:09Z"}}
{"entity":"epic","version":1,"id":"mcp-entry","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"title":"MCP entry"},"args":{"changes":{"title":"MCP entry"},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788480009811,"causation":"cmd-protocol-artifact-set-epic-mcp-entry-1788480009811","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"epic:mcp-entry@5#0~d28ea8f2917f2f4a","executor":null,"recorded_at":"2026-09-04T00:00:09Z"}}
{"entity":"epic","version":1,"id":"oauth-consolidation","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"title":"OAuth consolidation"},"args":{"changes":{"title":"OAuth consolidation"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788480009886,"causation":"cmd-protocol-artifact-set-epic-oauth-consolidation-1788480009886","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"epic:oauth-consolidation@5#0~514116d147dd600a","executor":null,"recorded_at":"2026-09-04T00:00:09Z"}}
{"entity":"epic","version":1,"id":"post-m1","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"title":"Post-M1"},"args":{"changes":{"title":"Post-M1"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788480009961,"causation":"cmd-protocol-artifact-set-epic-post-m1-1788480009961","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"epic:post-m1@4#0~024a1b560c3690ef","executor":null,"recorded_at":"2026-09-04T00:00:09Z"}}
{"entity":"story","version":1,"id":"one-connection-config-shape","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-051-one-connection-config-shape.md:19`. **read**\n\n- connectors-config exposes a single connection config shape consumed by both call sites, or a\n  doc comment on each type states the deliberate distinction and a test pins their divergence\n  as intentional.\n- Existing personal config files parse unchanged; no serialized field name moves.\n\n## Context\n\nSince S-048 retired `approval_evidence_ref`, `ConnectionConfig` and `B10xConnectionConfig`\nin connectors-config are structurally identical (S-048 implementation notes, 2026-08-23). Two\nnames for one shape invite drift: a field added to one silently diverges the other. Merge them\ninto one connection config shape — or document why the b10x-provider connection is a\ndistinct type on purpose.\n\nSource frontmatter: pillar Platform · areas [config, integrations]. **read**\n\n## Status\n\n`backlog` in the source, migrated to `draft`.\nQuoted from `docs/stories/S-051-one-connection-config-shape.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-051-one-connection-config-shape.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 1 revision(s)\n- Legacy id `S-051`, recorded as the reference `legacy:S-051`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-051"}],"title":"One connection config shape"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-051-one-connection-config-shape.md:19`. **read**\n\n- connectors-config exposes a single connection config shape consumed by both call sites, or a\n  doc comment on each type states the deliberate distinction and a test pins their divergence\n  as intentional.\n- Existing personal config files parse unchanged; no serialized field name moves.\n\n## Context\n\nSince S-048 retired `approval_evidence_ref`, `ConnectionConfig` and `B10xConnectionConfig`\nin connectors-config are structurally identical (S-048 implementation notes, 2026-08-23). Two\nnames for one shape invite drift: a field added to one silently diverges the other. Merge them\ninto one connection config shape — or document why the b10x-provider connection is a\ndistinct type on purpose.\n\nSource frontmatter: pillar Platform · areas [config, integrations]. **read**\n\n## Status\n\n`backlog` in the source, migrated to `draft`.\nQuoted from `docs/stories/S-051-one-connection-config-shape.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-051-one-connection-config-shape.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 1 revision(s)\n- Legacy id `S-051`, recorded as the reference `legacy:S-051`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-051"}],"status":"draft","title":"One connection config shape"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/one-connection-config-shape"},"payload":{"actor":"human:timo","at":1788480052247,"causation":"cmd-new-story-one-connection-config-shape","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:one-connection-config-shape@1#0~6d7271e17e96cfc8","executor":null,"recorded_at":"2026-09-04T00:00:52Z"}}
{"entity":"story","version":1,"id":"the-platform-connector-is-named-platform","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-052-the-platform-connector-is-named-platform.md:25`. **read**\n\n- No non-allowlisted `b10x` remains in crate names, type/function/variant names, or prose;\n  `scripts/check-brand.sh`'s allowlist shrinks accordingly and stays negative-tested.\n- An existing personal config carrying the old section name parses unchanged, proven by a test.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nD5 is resolved: the platform's name is \"platform\" (extraction plan resolution note, 2026-08-23).\nThe brand sweep left ~154 code-level occurrences blocked on that decision. Rename the code-level\nfamily: crate `integration-b10x` becomes `integration-platform`, and\n`B10xConnectionConfig`, `B10xIntegrationConfig`, `B10xBackend`,\n`ConnectionInitiator::B10x`, `b10x_only()` and companions follow, with prose\n(\"B10x Identity/Integration/Provider/Session Authority\", \"Authorize B10x\" consent\npages) moving to platform naming. Explicitly kept, each on the brand-fence allowlist: provider id\n`b10x` and authority `io.b10x` (public identity, irreversible), `urn:b10x:*`,\n`x-b10x-*` headers, `b10x.*.vN` contract ids, hash domains, and serialized state\n(keyring service attribute). The serde-visible `[b10x]` config section moves only behind a\nserde alias or a documented migration — an existing personal config must keep parsing.\n\nSource frontmatter: pillar Platform · areas [integrations, config, server]. **read**\n\n## Status\n\n`done` in the source, migrated to `implemented`.\nQuoted from `docs/stories/S-052-the-platform-connector-is-named-platform.md:5`: `status: done`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-052-the-platform-connector-is-named-platform.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-052`, recorded as the reference `legacy:S-052`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-052"}],"title":"The platform connector is named platform"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-052-the-platform-connector-is-named-platform.md:25`. **read**\n\n- No non-allowlisted `b10x` remains in crate names, type/function/variant names, or prose;\n  `scripts/check-brand.sh`'s allowlist shrinks accordingly and stays negative-tested.\n- An existing personal config carrying the old section name parses unchanged, proven by a test.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nD5 is resolved: the platform's name is \"platform\" (extraction plan resolution note, 2026-08-23).\nThe brand sweep left ~154 code-level occurrences blocked on that decision. Rename the code-level\nfamily: crate `integration-b10x` becomes `integration-platform`, and\n`B10xConnectionConfig`, `B10xIntegrationConfig`, `B10xBackend`,\n`ConnectionInitiator::B10x`, `b10x_only()` and companions follow, with prose\n(\"B10x Identity/Integration/Provider/Session Authority\", \"Authorize B10x\" consent\npages) moving to platform naming. Explicitly kept, each on the brand-fence allowlist: provider id\n`b10x` and authority `io.b10x` (public identity, irreversible), `urn:b10x:*`,\n`x-b10x-*` headers, `b10x.*.vN` contract ids, hash domains, and serialized state\n(keyring service attribute). The serde-visible `[b10x]` config section moves only behind a\nserde alias or a documented migration — an existing personal config must keep parsing.\n\nSource frontmatter: pillar Platform · areas [integrations, config, server]. **read**\n\n## Status\n\n`done` in the source, migrated to `implemented`.\nQuoted from `docs/stories/S-052-the-platform-connector-is-named-platform.md:5`: `status: done`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-052-the-platform-connector-is-named-platform.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-052`, recorded as the reference `legacy:S-052`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-052"}],"status":"draft","title":"The platform connector is named platform"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-platform-connector-is-named-platform"},"payload":{"actor":"human:timo","at":1788480052407,"causation":"cmd-new-story-the-platform-connector-is-named-platform","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-platform-connector-is-named-platform@1#0~a374e8de80724ee5","executor":null,"recorded_at":"2026-09-04T00:00:52Z"}}
{"entity":"story","version":1,"id":"the-platform-connector-is-named-platform","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000088","to":"proposed"},"payload":{"actor":"human:timo","at":1788480052545,"causation":"cmd-move-story-the-platform-connector-is-named-platform-1788480052545","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-platform-connector-is-named-platform@2#0~af12439ae99bf910","executor":null,"recorded_at":"2026-09-04T00:00:52Z"}}
{"entity":"story","version":1,"id":"the-platform-connector-is-named-platform","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000088","to":"active"},"payload":{"actor":"human:timo","at":1788480052683,"causation":"cmd-move-story-the-platform-connector-is-named-platform-1788480052683","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-platform-connector-is-named-platform@3#0~e7080621c9f929fc","executor":null,"recorded_at":"2026-09-04T00:00:52Z"}}
{"entity":"story","version":1,"id":"kubernetes-joins-the-catalog","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-057-kubernetes-joins-the-catalog.md:25`. **read**\n\n- `providers/kubernetes.toml` (repository-authored spec with provenance) catalogs the current\n  operation surface; the generic `CatalogBackend` executes it in-cluster behind the same\n  admission outcomes the hand-written backend produced, proven by the existing hosted tests\n  running against the declarative path.\n- The generic executor's new primitives (file-sourced bearer, namespace group policy, bounded\n  text response) are declared, not hard-coded to Kubernetes.\n- The deleted Rust is actually deleted; `integration-kubernetes` retains only what no\n  declaration can express, each retention justified in its module doc.\n\n## Context\n\nKubernetes is the last hand-coded HTTP surface: every other REST provider is declared in\n`providers/*.toml` and executed by the one generic adapter (`integration-catalog` — \"One adapter\nfor every declared provider\"). The Kubernetes integration stays Rust only because the generic\nexecutor lacks three primitives: a file-sourced bearer that is re-read per call (the in-cluster\nServiceAccount token), per-namespace group policy (`namespace_access.read_groups` is finer than\ntenant Grants), and bounded plain-text responses (pod logs). Teach the generic executor those\nprimitives, declare the Kubernetes API surface as a repository-authored spec + provider TOML\n(deployment status, rollout restart, pod logs — grounded in the official API reference, per the\nauthored-spec discipline), and delete the corresponding hand-written paths from\n`integration-kubernetes`. The workloads datasource (pods+events join) may remain code; it is a\nprojection, not an HTTP call.\n\nSource frontmatter: pillar Platform · areas [integrations, catalog, server]. **read**\n\n## Status\n\n`backlog` in the source, migrated to `draft`.\nQuoted from `docs/stories/S-057-kubernetes-joins-the-catalog.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-057-kubernetes-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 1 revision(s)\n- Legacy id `S-057`, recorded as the reference `legacy:S-057`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-057"}],"title":"Kubernetes joins the catalog"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-057-kubernetes-joins-the-catalog.md:25`. **read**\n\n- `providers/kubernetes.toml` (repository-authored spec with provenance) catalogs the current\n  operation surface; the generic `CatalogBackend` executes it in-cluster behind the same\n  admission outcomes the hand-written backend produced, proven by the existing hosted tests\n  running against the declarative path.\n- The generic executor's new primitives (file-sourced bearer, namespace group policy, bounded\n  text response) are declared, not hard-coded to Kubernetes.\n- The deleted Rust is actually deleted; `integration-kubernetes` retains only what no\n  declaration can express, each retention justified in its module doc.\n\n## Context\n\nKubernetes is the last hand-coded HTTP surface: every other REST provider is declared in\n`providers/*.toml` and executed by the one generic adapter (`integration-catalog` — \"One adapter\nfor every declared provider\"). The Kubernetes integration stays Rust only because the generic\nexecutor lacks three primitives: a file-sourced bearer that is re-read per call (the in-cluster\nServiceAccount token), per-namespace group policy (`namespace_access.read_groups` is finer than\ntenant Grants), and bounded plain-text responses (pod logs). Teach the generic executor those\nprimitives, declare the Kubernetes API surface as a repository-authored spec + provider TOML\n(deployment status, rollout restart, pod logs — grounded in the official API reference, per the\nauthored-spec discipline), and delete the corresponding hand-written paths from\n`integration-kubernetes`. The workloads datasource (pods+events join) may remain code; it is a\nprojection, not an HTTP call.\n\nSource frontmatter: pillar Platform · areas [integrations, catalog, server]. **read**\n\n## Status\n\n`backlog` in the source, migrated to `draft`.\nQuoted from `docs/stories/S-057-kubernetes-joins-the-catalog.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-057-kubernetes-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 1 revision(s)\n- Legacy id `S-057`, recorded as the reference `legacy:S-057`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-057"}],"status":"draft","title":"Kubernetes joins the catalog"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/kubernetes-joins-the-catalog"},"payload":{"actor":"human:timo","at":1788480053139,"causation":"cmd-new-story-kubernetes-joins-the-catalog","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:kubernetes-joins-the-catalog@1#0~646bf0c92874bdd0","executor":null,"recorded_at":"2026-09-04T00:00:53Z"}}
{"entity":"story","version":1,"id":"the-document-carries-the-callers-contract","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-001-the-document-carries-the-callers-contract.md:35`. **read**\n\n- [x] `catalog/connector-document.schema.json` gains all three, **additively**: a caller-facing\n      symbol per parameter, the projection description, and the contract `input_schema` in its\n      **lowered, caller-typed** form — computed at build time by `catalog-build` (which has the\n      lowering, `src/contract.rs`) and stored as data. Every committed canonical document was\n      regenerated whole-catalogue. → `schema_version` stays 1, not a bump: additive per the\n      schema's own rule and C-537; the resolution is design 04 §2.\n- [x] A consumer constructs the **complete** caller-facing contract — name, description,\n      `input_schema`, `expose` — from the document's data alone.\n      → `catalog::Operation::{contract_description, input_schema, expose}`;\n      `consumer_api.rs::the_caller_contract_is_document_data_alone` names the consumer path and\n      proves the absence (no source-form parser in the crate's dependency closure).\n- [x] The symbol is written by the lowering, and the second reproduction of the allocator retires:\n      `crates/connector-resolve` reads the document field (its copy survives only as the\n      pre-S-001-document fallback, exercised by no build here). Disagreement is refused at build:\n      the fixed-point invariant catches any committed symbol the allocator would not produce, and\n      `the_contract_and_the_params_state_the_same_symbols` names the operation whose two symbol\n      statements drift.\n- [x] The const-pinned trap is closed: the allocator reserves a symbol for every body parameter,\n      `const`-pinned ones included, and the document states the shifted result.\n      → `contract.rs::a_const_pinned_body_field_reserves_the_symbol_a_later_field_must_shift_past`\n      (build side) and `connector-resolve/document.rs`'s reader-side twin.\n- [x] The `format = \"origin\"` blind spot is closed.\n      → `catalog_invariants.rs::every_format_origin_field_lowers_to_the_origin_slot`, with the\n      cannot-go-blind counter (gitlab is the shipped case).\n- [x] **`CredentialRequirement` stops being derived.** The document publishes\n      `credential_requirement` (C-206's tokens), computed at build where `Operation::auth`'s\n      `Option` still distinguishes declared-empty from never-declared;\n      `table.rs::credential_requirement` reads it and the default-resolving derivation is gone.\n      → failing-first pair: `table.rs::tests::the_document_tells_apart_the_pair_the_derivation_could_not`.\n- [x] **`Acquisition::Minted` is reachable** — the carry arm, not the deferral: the document\n      gains the optional `produces_credential { credential, secret }` join (zero byte cost on\n      every shipped document; none declares one) and `table.rs` constructs the variant from it,\n      refusing conflicting provenance by name.\n      → `table.rs::tests::a_minting_join_in_the_document_reaches_acquisition_minted`.\n- [x] Determinism is unbroken: the fixed-point and two-plans invariants are green over the\n      regenerated tree, and the one-time differential against the predecessor ran against its\n      C-552 regenerated documents (the shared fields, which its pack predates): **835/835\n      operations, 1518/1518 symbols** — `symbol`, `contract.description`, `contract.input_schema`\n      all byte-equal to the engine-derived values. Effects excluded (S-002).\n\n## Context\n\nClose the gap C-538 measured in the predecessor and C-552 was filed to fix: three things a caller or\na model receives are recovered by **parsing emitted source**, because the canonical document does not\ncarry them. Widen `catalog/connector-document.schema.json` and the lowering so a consumer builds the\ncomplete caller-facing contract from document data alone — the precondition for architecture §5's\nabsolute rule, *no runtime parsing of any source form, ever; the plan is derived from document data\nonly.*\n\nThe three fields, as C-552 measured them:\n\n1. **the caller-facing symbol** — the document publishes a parameter's IR `name` (`time.start`,\n   `$top`) and its `wire` name, but the contract a caller sees advertises the allocated symbol\n   (`time_start`, `_top`, `response_2`). The predecessor reproduced the allocator in a second crate,\n   held honest only by a differential gate comparing against the emitted declaration;\n2. **the error-envelope-extended description** — the projected description appends the\n   error-envelope paragraph that the document's one-line summary lacks (measured on\n   `airtable-record-get`);\n3. **the contract `input_schema`** — lowered from the engine's types, where the document carries the\n   vendor's raw JSON Schema (`int64 integer` vs `number`).\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve] · design `../design/04-the-callers-contract.md`. **read**\n\nSource `note:` field, quoted: “ported from flux-connectors C-552 (measured by C-538). Architecture §2 day-one change 1, first half: the caller-facing symbol, the error-envelope-extended description and the contract input_schema. Per-operation effects — C-552's fourth field — are S-002, because they have a second consumer (grant admission) and a mistake of their own to close”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-001-the-document-carries-the-callers-contract.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-001-the-document-carries-the-callers-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 6 revision(s)\n- Legacy id `S-001`, recorded as the reference `legacy:S-001`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-001"}],"title":"The document carries the caller's contract, so nothing at runtime parses source"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-001-the-document-carries-the-callers-contract.md:35`. **read**\n\n- [x] `catalog/connector-document.schema.json` gains all three, **additively**: a caller-facing\n      symbol per parameter, the projection description, and the contract `input_schema` in its\n      **lowered, caller-typed** form — computed at build time by `catalog-build` (which has the\n      lowering, `src/contract.rs`) and stored as data. Every committed canonical document was\n      regenerated whole-catalogue. → `schema_version` stays 1, not a bump: additive per the\n      schema's own rule and C-537; the resolution is design 04 §2.\n- [x] A consumer constructs the **complete** caller-facing contract — name, description,\n      `input_schema`, `expose` — from the document's data alone.\n      → `catalog::Operation::{contract_description, input_schema, expose}`;\n      `consumer_api.rs::the_caller_contract_is_document_data_alone` names the consumer path and\n      proves the absence (no source-form parser in the crate's dependency closure).\n- [x] The symbol is written by the lowering, and the second reproduction of the allocator retires:\n      `crates/connector-resolve` reads the document field (its copy survives only as the\n      pre-S-001-document fallback, exercised by no build here). Disagreement is refused at build:\n      the fixed-point invariant catches any committed symbol the allocator would not produce, and\n      `the_contract_and_the_params_state_the_same_symbols` names the operation whose two symbol\n      statements drift.\n- [x] The const-pinned trap is closed: the allocator reserves a symbol for every body parameter,\n      `const`-pinned ones included, and the document states the shifted result.\n      → `contract.rs::a_const_pinned_body_field_reserves_the_symbol_a_later_field_must_shift_past`\n      (build side) and `connector-resolve/document.rs`'s reader-side twin.\n- [x] The `format = \"origin\"` blind spot is closed.\n      → `catalog_invariants.rs::every_format_origin_field_lowers_to_the_origin_slot`, with the\n      cannot-go-blind counter (gitlab is the shipped case).\n- [x] **`CredentialRequirement` stops being derived.** The document publishes\n      `credential_requirement` (C-206's tokens), computed at build where `Operation::auth`'s\n      `Option` still distinguishes declared-empty from never-declared;\n      `table.rs::credential_requirement` reads it and the default-resolving derivation is gone.\n      → failing-first pair: `table.rs::tests::the_document_tells_apart_the_pair_the_derivation_could_not`.\n- [x] **`Acquisition::Minted` is reachable** — the carry arm, not the deferral: the document\n      gains the optional `produces_credential { credential, secret }` join (zero byte cost on\n      every shipped document; none declares one) and `table.rs` constructs the variant from it,\n      refusing conflicting provenance by name.\n      → `table.rs::tests::a_minting_join_in_the_document_reaches_acquisition_minted`.\n- [x] Determinism is unbroken: the fixed-point and two-plans invariants are green over the\n      regenerated tree, and the one-time differential against the predecessor ran against its\n      C-552 regenerated documents (the shared fields, which its pack predates): **835/835\n      operations, 1518/1518 symbols** — `symbol`, `contract.description`, `contract.input_schema`\n      all byte-equal to the engine-derived values. Effects excluded (S-002).\n\n## Context\n\nClose the gap C-538 measured in the predecessor and C-552 was filed to fix: three things a caller or\na model receives are recovered by **parsing emitted source**, because the canonical document does not\ncarry them. Widen `catalog/connector-document.schema.json` and the lowering so a consumer builds the\ncomplete caller-facing contract from document data alone — the precondition for architecture §5's\nabsolute rule, *no runtime parsing of any source form, ever; the plan is derived from document data\nonly.*\n\nThe three fields, as C-552 measured them:\n\n1. **the caller-facing symbol** — the document publishes a parameter's IR `name` (`time.start`,\n   `$top`) and its `wire` name, but the contract a caller sees advertises the allocated symbol\n   (`time_start`, `_top`, `response_2`). The predecessor reproduced the allocator in a second crate,\n   held honest only by a differential gate comparing against the emitted declaration;\n2. **the error-envelope-extended description** — the projected description appends the\n   error-envelope paragraph that the document's one-line summary lacks (measured on\n   `airtable-record-get`);\n3. **the contract `input_schema`** — lowered from the engine's types, where the document carries the\n   vendor's raw JSON Schema (`int64 integer` vs `number`).\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve] · design `../design/04-the-callers-contract.md`. **read**\n\nSource `note:` field, quoted: “ported from flux-connectors C-552 (measured by C-538). Architecture §2 day-one change 1, first half: the caller-facing symbol, the error-envelope-extended description and the contract input_schema. Per-operation effects — C-552's fourth field — are S-002, because they have a second consumer (grant admission) and a mistake of their own to close”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-001-the-document-carries-the-callers-contract.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-001-the-document-carries-the-callers-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 6 revision(s)\n- Legacy id `S-001`, recorded as the reference `legacy:S-001`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-001"}],"status":"draft","title":"The document carries the caller's contract, so nothing at runtime parses source"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-document-carries-the-callers-contract"},"payload":{"actor":"human:timo","at":1788480108130,"causation":"cmd-new-story-the-document-carries-the-callers-contract","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-document-carries-the-callers-contract@1#0~ed5c0bb7b04d3e2b","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"the-document-carries-the-callers-contract","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:catalog-day-one"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000107","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788480108144,"causation":"cmd-story-the-document-carries-the-callers-contract-derived_from-epic-catalog-day-one","change":{"change":"related","relation":"derived_from","target":"epic:catalog-day-one"},"correlation":"protocol-artifact-relate","event_id":"story:the-document-carries-the-callers-contract@1#0~0a42adb2d042e73b","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"the-document-carries-the-callers-contract","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000091","to":"proposed"},"payload":{"actor":"human:timo","at":1788480108282,"causation":"cmd-move-story-the-document-carries-the-callers-contract-1788480108282","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-document-carries-the-callers-contract@2#0~b7774a8295922c72","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"the-document-carries-the-callers-contract","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000091","to":"active"},"payload":{"actor":"human:timo","at":1788480108422,"causation":"cmd-move-story-the-document-carries-the-callers-contract-1788480108422","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-document-carries-the-callers-contract@3#0~c48ea9b6340156f6","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"effects-are-read-never-derived","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-002-effects-are-read-never-derived.md:43`. **read**\n\n- [x] `catalog/connector-document.schema.json` carries `effects` **per operation** as a closed\n      vocabulary (domain model: risk, idempotency, effects, direction). An unknown effect value is a\n      build error refused by name — never a downgrade, never a default, never a catch-all arm that\n      answers a level it has not heard of with a plausible wrong one.\n- [x] Every operation in every provider declaration states its effects, proven by a whole-catalog\n      declared-count gate: an operation with no effects declaration fails the build. Nothing infers\n      effects from an HTTP verb, from `hosts`, or from `risk`.\n- [ ] Grant admission in `crates/domain` reads the effects the document carries. A fence or test\n      proves there is **no derivation path**: a seeded document declaring a non-network effect must\n      change the admission answer, which is exactly what the derived function could not do.\n- [ ] Failing-first test: an operation declaring an effect beyond `network` is admitted only by a\n      selector whose effect subset contains that effect, and is refused by\n      `with_effects_within([network])`. Under the predecessor's rule this test passes vacuously —\n      name it so, so the regression is visible if derivation ever returns.\n- [x] The predecessor's two compensating assertions are deliberately **not** carried, and the story\n      records why: they existed to bound a derivation that no longer exists. A grep proving neither\n      spelling survives the migration is cheap and worth having.\n- [x] The relationship to the semantic tier is decided and recorded: host-resource effects (what a\n      call touches) and semantic effects (what it *means* — money, delete, send-external, from\n      flux-connectors C-155) are distinct axes, and collapsing them is what lost `money` in the\n      predecessor. Either the schema carries both fields or the second is explicitly deferred with a\n      named reason.\n\n## Context\n\nMake `effects` a declared fact of every operation — carried by the schema, stated by every provider\ndeclaration, and **read** by grant admission — so the domain model's invariant holds by construction:\n*the facts the authorization gate decides on are the facts the catalogue publishes.* This is\narchitecture §2 day-one change 1, second half, and it closes a mistake the predecessor documented\nagainst itself rather than fixed.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, domain]. **read**\n\nSource `note:` field, quoted: “the mistake being closed is recorded in the predecessor's own source: flux-exchange crates/exchange-host/src/grant.rs:105-124 — OperationFacts::of inserts Effect::Network when hosts is non-empty and emits nothing else, so a selector written with_effects_within([Network]) is exact today and silently over-admits the day an operation with another effect ships”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-002-effects-are-read-never-derived.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-002-effects-are-read-never-derived.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-002`, recorded as the reference `legacy:S-002`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-002"}],"title":"Per-operation effects are read from the document, never derived"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-002-effects-are-read-never-derived.md:43`. **read**\n\n- [x] `catalog/connector-document.schema.json` carries `effects` **per operation** as a closed\n      vocabulary (domain model: risk, idempotency, effects, direction). An unknown effect value is a\n      build error refused by name — never a downgrade, never a default, never a catch-all arm that\n      answers a level it has not heard of with a plausible wrong one.\n- [x] Every operation in every provider declaration states its effects, proven by a whole-catalog\n      declared-count gate: an operation with no effects declaration fails the build. Nothing infers\n      effects from an HTTP verb, from `hosts`, or from `risk`.\n- [ ] Grant admission in `crates/domain` reads the effects the document carries. A fence or test\n      proves there is **no derivation path**: a seeded document declaring a non-network effect must\n      change the admission answer, which is exactly what the derived function could not do.\n- [ ] Failing-first test: an operation declaring an effect beyond `network` is admitted only by a\n      selector whose effect subset contains that effect, and is refused by\n      `with_effects_within([network])`. Under the predecessor's rule this test passes vacuously —\n      name it so, so the regression is visible if derivation ever returns.\n- [x] The predecessor's two compensating assertions are deliberately **not** carried, and the story\n      records why: they existed to bound a derivation that no longer exists. A grep proving neither\n      spelling survives the migration is cheap and worth having.\n- [x] The relationship to the semantic tier is decided and recorded: host-resource effects (what a\n      call touches) and semantic effects (what it *means* — money, delete, send-external, from\n      flux-connectors C-155) are distinct axes, and collapsing them is what lost `money` in the\n      predecessor. Either the schema carries both fields or the second is explicitly deferred with a\n      named reason.\n\n## Context\n\nMake `effects` a declared fact of every operation — carried by the schema, stated by every provider\ndeclaration, and **read** by grant admission — so the domain model's invariant holds by construction:\n*the facts the authorization gate decides on are the facts the catalogue publishes.* This is\narchitecture §2 day-one change 1, second half, and it closes a mistake the predecessor documented\nagainst itself rather than fixed.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, domain]. **read**\n\nSource `note:` field, quoted: “the mistake being closed is recorded in the predecessor's own source: flux-exchange crates/exchange-host/src/grant.rs:105-124 — OperationFacts::of inserts Effect::Network when hosts is non-empty and emits nothing else, so a selector written with_effects_within([Network]) is exact today and silently over-admits the day an operation with another effect ships”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-002-effects-are-read-never-derived.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-002-effects-are-read-never-derived.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-002`, recorded as the reference `legacy:S-002`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-002"}],"status":"draft","title":"Per-operation effects are read from the document, never derived"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/effects-are-read-never-derived"},"payload":{"actor":"human:timo","at":1788480108578,"causation":"cmd-new-story-effects-are-read-never-derived","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:effects-are-read-never-derived@1#0~3e6d0ecaab5e3ccb","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"effects-are-read-never-derived","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:catalog-day-one"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000112","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788480108587,"causation":"cmd-story-effects-are-read-never-derived-derived_from-epic-catalog-day-one","change":{"change":"related","relation":"derived_from","target":"epic:catalog-day-one"},"correlation":"protocol-artifact-relate","event_id":"story:effects-are-read-never-derived@1#0~cf9bc2e2efdd0cc9","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"effects-are-read-never-derived","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"proposed"},"payload":{"actor":"human:timo","at":1788480108725,"causation":"cmd-move-story-effects-are-read-never-derived-1788480108725","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:effects-are-read-never-derived@2#0~60376ba333507a6a","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"effects-are-read-never-derived","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"active"},"payload":{"actor":"human:timo","at":1788480108865,"causation":"cmd-move-story-effects-are-read-never-derived-1788480108865","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:effects-are-read-never-derived@3#0~ce5da4ae628174de","executor":null,"recorded_at":"2026-09-04T00:01:48Z"}}
{"entity":"story","version":1,"id":"the-lockfile-gets-a-verifier","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:31`. **read**\n\n- [x] `catalog check` recomputes every hash in `connectors.lock` whose bytes are identified by the\n      current v1 format — each provider's\n      declaration bytes, each vendored spec document, each committed artifact by repository-relative\n      path — and **exits non-zero** on any mismatch, naming the provider and which input moved. A\n      populated predecessor-reserved hash without addressable bytes is refused as unverifiable.\n      The ignored site projection is regenerated build output, not reviewed state, so it is not\n      counted as a verified committed artifact.\n- [x] `check` performs **no network IO**, so CI runs it offline and hermetically. Upstream drift\n      (has the vendor's document moved?) is a different question with a different answer and is not\n      folded in here.\n- [x] Four drift classes are distinct, named failures, each proven by a seeded case: a mutated\n      artifact; an edited provider declaration (**including a comment-only edit** — the hash domain\n      is file bytes, and a comment can change what an operator believes the connector does); a\n      re-vendored spec whose bytes changed; and a lock row that no longer matches its artifact.\n- [x] Coverage is checked in both directions: a provider with no lock row fails, and a lock row with\n      no provider fails. A verifier that only checks the rows it finds cannot see a deletion.\n- [x] Failing-first test: a mutated committed artifact makes `check` exit non-zero and name it; a\n      clean tree exits zero and prints the counts it verified (providers, artifacts) rather than a\n      bare \"ok\".\n- [x] `check` is in the repository's gate, and the gate's definition says so — a verifier nothing\n      runs is the same as no verifier.\n\n## Context\n\nGive `connectors.lock` the verifier it never had, so vision principle 1 — *review equals execution;\nevery derived form is byte-identical to the committed canonical documents* — is enforced by a command\nCI runs rather than asserted in a CHANGELOG. Drift is not preventable; it is **detectable**, and that\nis the whole design.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “the predecessor's lockfile has a writer (C-189) and no verifier (C-14, never built), so provenance is computed, committed, and never checked. Architecture §2 day-one change 2. Review-equals-execution is a claim until something recomputes it in CI”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-003-the-lockfile-gets-a-verifier.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-003`, recorded as the reference `legacy:S-003`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-003"}],"title":"`catalog check` verifies every addressable hash and refuses unverifiable claims"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:31`. **read**\n\n- [x] `catalog check` recomputes every hash in `connectors.lock` whose bytes are identified by the\n      current v1 format — each provider's\n      declaration bytes, each vendored spec document, each committed artifact by repository-relative\n      path — and **exits non-zero** on any mismatch, naming the provider and which input moved. A\n      populated predecessor-reserved hash without addressable bytes is refused as unverifiable.\n      The ignored site projection is regenerated build output, not reviewed state, so it is not\n      counted as a verified committed artifact.\n- [x] `check` performs **no network IO**, so CI runs it offline and hermetically. Upstream drift\n      (has the vendor's document moved?) is a different question with a different answer and is not\n      folded in here.\n- [x] Four drift classes are distinct, named failures, each proven by a seeded case: a mutated\n      artifact; an edited provider declaration (**including a comment-only edit** — the hash domain\n      is file bytes, and a comment can change what an operator believes the connector does); a\n      re-vendored spec whose bytes changed; and a lock row that no longer matches its artifact.\n- [x] Coverage is checked in both directions: a provider with no lock row fails, and a lock row with\n      no provider fails. A verifier that only checks the rows it finds cannot see a deletion.\n- [x] Failing-first test: a mutated committed artifact makes `check` exit non-zero and name it; a\n      clean tree exits zero and prints the counts it verified (providers, artifacts) rather than a\n      bare \"ok\".\n- [x] `check` is in the repository's gate, and the gate's definition says so — a verifier nothing\n      runs is the same as no verifier.\n\n## Context\n\nGive `connectors.lock` the verifier it never had, so vision principle 1 — *review equals execution;\nevery derived form is byte-identical to the committed canonical documents* — is enforced by a command\nCI runs rather than asserted in a CHANGELOG. Drift is not preventable; it is **detectable**, and that\nis the whole design.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “the predecessor's lockfile has a writer (C-189) and no verifier (C-14, never built), so provenance is computed, committed, and never checked. Architecture §2 day-one change 2. Review-equals-execution is a claim until something recomputes it in CI”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-003-the-lockfile-gets-a-verifier.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-003`, recorded as the reference `legacy:S-003`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-003"}],"status":"draft","title":"`catalog check` verifies every addressable hash and refuses unverifiable claims"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-lockfile-gets-a-verifier"},"payload":{"actor":"human:timo","at":1788480109021,"causation":"cmd-new-story-the-lockfile-gets-a-verifier","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-lockfile-gets-a-verifier@1#0~9e3915fd57ae9358","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"the-lockfile-gets-a-verifier","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:catalog-day-one"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000117","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788480109031,"causation":"cmd-story-the-lockfile-gets-a-verifier-derived_from-epic-catalog-day-one","change":{"change":"related","relation":"derived_from","target":"epic:catalog-day-one"},"correlation":"protocol-artifact-relate","event_id":"story:the-lockfile-gets-a-verifier@1#0~031b37c9547a27a0","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"the-lockfile-gets-a-verifier","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000097","to":"proposed"},"payload":{"actor":"human:timo","at":1788480109171,"causation":"cmd-move-story-the-lockfile-gets-a-verifier-1788480109171","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-lockfile-gets-a-verifier@2#0~0863d3a3540daea0","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"the-lockfile-gets-a-verifier","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000097","to":"active"},"payload":{"actor":"human:timo","at":1788480109309,"causation":"cmd-move-story-the-lockfile-gets-a-verifier-1788480109309","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-lockfile-gets-a-verifier@3#0~025921c04218a40c","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"adopt-token-response-metadata","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-004-adopt-token-response-metadata.md:23`. **read**\n\n- [ ] The OAuth2 declaration can name extra token-response fields to extract, addressed by path, into\n      connection metadata. The set is **closed and declared** — named fields only, never\n      \"keep whatever else came back\", which would make an arbitrary vendor payload a stored value\n      nobody reviewed.\n- [ ] Extracted values land in connection metadata and are **unrepresentable** in the credential\n      store — by type if possible, by a refusal test if not. A declaration naming a field the vendor\n      returns as credential material (a refresh token, a client secret) is refused at build, by name.\n- [ ] Slack is the fixture: a recorded token response yields exactly `incoming_webhook.url` and\n      `bot_user_id` as connection metadata, and the credential store receives nothing beyond the\n      token set it already receives.\n- [ ] A declared field absent from a real token response is **absent** from metadata — not an empty\n      string, not a null placeholder — and its absence does not fail the acquisition. Vendors omit\n      optional fields routinely; an acquisition that fails on one is worse than the gap.\n- [ ] Metadata is readable by the operator and by a grant-admitted client through the connection\n      surface, and is value-free in audit records (an audit row may say metadata was written; it may\n      never carry the value).\n- [ ] Failing-first test named — today there is no field to declare and nowhere for the value to go.\n\n## Context\n\nLet a provider declare the extra fields its token response returns beside the token — Slack's\n`incoming_webhook.url` and `bot_user_id` are the canonical case — so those values reach **connection\nmetadata** at acquisition time instead of being dropped on the floor. Adopted from Nango's\n`token_response_metadata` (24/957 providers), with our own invariant attached: metadata is not\ncredential material and never enters the credential store.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec, service]. **read**\n\nSource `note:` field, quoted: “research/catalog-precedents.md gap table: token_response_metadata, 24 of Nango's 957 providers. 'Adopt. No equivalent; today such values would be lost at acquisition time.' The domain model already fixes where they land — connection metadata, never the credential store”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-004-adopt-token-response-metadata.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-004-adopt-token-response-metadata.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-004`, recorded as the reference `legacy:S-004`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-004"}],"title":"An OAuth token response can carry declared metadata into the connection, not the credential store"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-004-adopt-token-response-metadata.md:23`. **read**\n\n- [ ] The OAuth2 declaration can name extra token-response fields to extract, addressed by path, into\n      connection metadata. The set is **closed and declared** — named fields only, never\n      \"keep whatever else came back\", which would make an arbitrary vendor payload a stored value\n      nobody reviewed.\n- [ ] Extracted values land in connection metadata and are **unrepresentable** in the credential\n      store — by type if possible, by a refusal test if not. A declaration naming a field the vendor\n      returns as credential material (a refresh token, a client secret) is refused at build, by name.\n- [ ] Slack is the fixture: a recorded token response yields exactly `incoming_webhook.url` and\n      `bot_user_id` as connection metadata, and the credential store receives nothing beyond the\n      token set it already receives.\n- [ ] A declared field absent from a real token response is **absent** from metadata — not an empty\n      string, not a null placeholder — and its absence does not fail the acquisition. Vendors omit\n      optional fields routinely; an acquisition that fails on one is worse than the gap.\n- [ ] Metadata is readable by the operator and by a grant-admitted client through the connection\n      surface, and is value-free in audit records (an audit row may say metadata was written; it may\n      never carry the value).\n- [ ] Failing-first test named — today there is no field to declare and nowhere for the value to go.\n\n## Context\n\nLet a provider declare the extra fields its token response returns beside the token — Slack's\n`incoming_webhook.url` and `bot_user_id` are the canonical case — so those values reach **connection\nmetadata** at acquisition time instead of being dropped on the floor. Adopted from Nango's\n`token_response_metadata` (24/957 providers), with our own invariant attached: metadata is not\ncredential material and never enters the credential store.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec, service]. **read**\n\nSource `note:` field, quoted: “research/catalog-precedents.md gap table: token_response_metadata, 24 of Nango's 957 providers. 'Adopt. No equivalent; today such values would be lost at acquisition time.' The domain model already fixes where they land — connection metadata, never the credential store”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-004-adopt-token-response-metadata.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-004-adopt-token-response-metadata.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-004`, recorded as the reference `legacy:S-004`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-004"}],"status":"draft","title":"An OAuth token response can carry declared metadata into the connection, not the credential store"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/adopt-token-response-metadata"},"payload":{"actor":"human:timo","at":1788480109462,"causation":"cmd-new-story-adopt-token-response-metadata","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:adopt-token-response-metadata@1#0~b6189cae81e2e2a1","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"adopt-token-response-metadata","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:catalog-adoptions"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000122","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788480109469,"causation":"cmd-story-adopt-token-response-metadata-derived_from-epic-catalog-adoptions","change":{"change":"related","relation":"derived_from","target":"epic:catalog-adoptions"},"correlation":"protocol-artifact-relate","event_id":"story:adopt-token-response-metadata@1#0~1cad17b58636b221","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"header-name-rate-limit-retry","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-005-header-name-rate-limit-retry.md:35`. **read**\n\n- [ ] The rate-limit declaration distinguishes at least three states: a **fixed** pair (what exists),\n      a **discovered** budget that names the response headers carrying it, and an\n      **unknown-by-tier** budget. Whether that is one enum or a struct with an optional pair is the\n      implementor's call; record the reason where the schema documents itself.\n- [ ] A discovered budget names the **actual headers** (`Retry-After`, `X-RateLimit-Remaining`,\n      `X-RateLimit-Reset`, …) rather than implying a convention. Vendors disagree about these, and\n      the disagreement is the entire difficulty; a convention would be a guess stored as data.\n- [ ] `hubspot` and `discord` both stop being exceptions: each declares what it knows, and the prose\n      in its description shrinks to what the declaration cannot carry. Two spellings of one fact is\n      the defect to avoid here.\n- [ ] What a consumer is expected to **do** with a discovered budget is **stated** in the schema's\n      own documentation and the consuming path is **named** (the invocation egress path) — a\n      declaration nothing can act on is prose with a schema. Implementing the backoff is a separate\n      story and is explicitly out of scope here.\n- [ ] Failing-first test: a connector declares a header-discovered budget and it reaches the canonical\n      document. It cannot be expressed today. Name the test.\n\n## Context\n\nLet a connector state a rate limit it genuinely knows the shape of, so the information reaches the\ncanonical document instead of surviving only in a description a machine cannot act on — and so the\nthe declared invocation path, and any later operator-only proxy, can normalize backoff from catalog\nmetadata rather than rediscovering it at runtime.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec]. **read**\n\nSource `note:` field, quoted: “ported from flux-connectors C-224: quirks.rate_limit takes a fixed requests/per_seconds pair, and two shipped connectors declined to declare one for unrelated reasons (hubspot: the limit is a function of the customer's tier; discord: per-route buckets discovered from response headers). Two independent refusals is the declaration's shape being too narrow, not two lazy connectors. Nango declares retry on 160 of 957 providers”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-005-header-name-rate-limit-retry.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-005-header-name-rate-limit-retry.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 3 revision(s)\n- Legacy id `S-005`, recorded as the reference `legacy:S-005`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-005"}],"title":"A rate limit the vendor discloses at runtime can be declared by header name"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-005-header-name-rate-limit-retry.md:35`. **read**\n\n- [ ] The rate-limit declaration distinguishes at least three states: a **fixed** pair (what exists),\n      a **discovered** budget that names the response headers carrying it, and an\n      **unknown-by-tier** budget. Whether that is one enum or a struct with an optional pair is the\n      implementor's call; record the reason where the schema documents itself.\n- [ ] A discovered budget names the **actual headers** (`Retry-After`, `X-RateLimit-Remaining`,\n      `X-RateLimit-Reset`, …) rather than implying a convention. Vendors disagree about these, and\n      the disagreement is the entire difficulty; a convention would be a guess stored as data.\n- [ ] `hubspot` and `discord` both stop being exceptions: each declares what it knows, and the prose\n      in its description shrinks to what the declaration cannot carry. Two spellings of one fact is\n      the defect to avoid here.\n- [ ] What a consumer is expected to **do** with a discovered budget is **stated** in the schema's\n      own documentation and the consuming path is **named** (the invocation egress path) — a\n      declaration nothing can act on is prose with a schema. Implementing the backoff is a separate\n      story and is explicitly out of scope here.\n- [ ] Failing-first test: a connector declares a header-discovered budget and it reaches the canonical\n      document. It cannot be expressed today. Name the test.\n\n## Context\n\nLet a connector state a rate limit it genuinely knows the shape of, so the information reaches the\ncanonical document instead of surviving only in a description a machine cannot act on — and so the\nthe declared invocation path, and any later operator-only proxy, can normalize backoff from catalog\nmetadata rather than rediscovering it at runtime.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec]. **read**\n\nSource `note:` field, quoted: “ported from flux-connectors C-224: quirks.rate_limit takes a fixed requests/per_seconds pair, and two shipped connectors declined to declare one for unrelated reasons (hubspot: the limit is a function of the customer's tier; discord: per-route buckets discovered from response headers). Two independent refusals is the declaration's shape being too narrow, not two lazy connectors. Nango declares retry on 160 of 957 providers”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-005-header-name-rate-limit-retry.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-005-header-name-rate-limit-retry.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 3 revision(s)\n- Legacy id `S-005`, recorded as the reference `legacy:S-005`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-005"}],"status":"draft","title":"A rate limit the vendor discloses at runtime can be declared by header name"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/header-name-rate-limit-retry"},"payload":{"actor":"human:timo","at":1788480109627,"causation":"cmd-new-story-header-name-rate-limit-retry","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:header-name-rate-limit-retry@1#0~5632df8518021664","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"header-name-rate-limit-retry","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:catalog-adoptions"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000127","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788480109635,"causation":"cmd-story-header-name-rate-limit-retry-derived_from-epic-catalog-adoptions","change":{"change":"related","relation":"derived_from","target":"epic:catalog-adoptions"},"correlation":"protocol-artifact-relate","event_id":"story:header-name-rate-limit-retry@1#0~7b0e4ae6fd400b08","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"per-service-verification-probes","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-006-per-service-verification-probes.md:22`. **read**\n\n- [ ] A service may declare a verification probe — endpoint, expected outcome, failure\n      classification — alongside the connector-level `verify`. The resolution rule between the two is\n      stated and tested (a service probe wins; the connector probe is the default for services that\n      declare none), because a connector whose services live on different hosts has no single true\n      probe.\n- [ ] The failure classification distinguishes at least **credential invalid** (→ the connection\n      becomes `degraded` and a repair flow is offered) from **service unreachable** (→ an\n      *operational* event, never a credential verdict). These are the two answers the lifecycle needs\n      and neither is recoverable from a bare HTTP status.\n- [ ] A probe is **never an operation**: not catalogued, not addressable, not projected to models —\n      the predecessor's standing rule that an authentication or verification endpoint is not a\n      connector operation, and the accounting that proves a provider's withheld endpoints are\n      withheld deliberately.\n- [ ] At least two shipped providers declare per-service probes, and fixtures cover both\n      classifications for each. Failing-first test named.\n- [ ] The probe carries no credential value: placement comes from the catalog's auth section exactly\n      as for any request, and a probe's request plan is subject to the same subjects-before-placement\n      rule as an invocation.\n- [ ] The probe is reachable from the platform's connection lifecycle (a `verify` action on a\n      connection) and from `connectors doctor`, through one code path — not two implementations of\n      \"is this credential still good\".\n\n## Context\n\nLet a provider declare a credential-verification probe **per service**, with a failure\nclassification, so the connection lifecycle's `authorized → callable` transition and its degraded\ndetection run from declared data rather than per-provider code. Third of the three adoptions the\nprecedents analysis ordered by cost/benefit.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec, service]. **read**\n\nSource `note:` field, quoted: “research/catalog-precedents.md gap table: Nango declares a verification probe on 281 of 957 providers. Ours is a top-level per-connector `verify` — service-partitioned in the IR already (flux-connectors C-194) but never published or classified. The connection lifecycle needs authorized→callable and callable→degraded to come from declared data, not per-provider code”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-006-per-service-verification-probes.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-006-per-service-verification-probes.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-006`, recorded as the reference `legacy:S-006`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-006"}],"title":"A service declares how a credential is verified, and what a failure means"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-006-per-service-verification-probes.md:22`. **read**\n\n- [ ] A service may declare a verification probe — endpoint, expected outcome, failure\n      classification — alongside the connector-level `verify`. The resolution rule between the two is\n      stated and tested (a service probe wins; the connector probe is the default for services that\n      declare none), because a connector whose services live on different hosts has no single true\n      probe.\n- [ ] The failure classification distinguishes at least **credential invalid** (→ the connection\n      becomes `degraded` and a repair flow is offered) from **service unreachable** (→ an\n      *operational* event, never a credential verdict). These are the two answers the lifecycle needs\n      and neither is recoverable from a bare HTTP status.\n- [ ] A probe is **never an operation**: not catalogued, not addressable, not projected to models —\n      the predecessor's standing rule that an authentication or verification endpoint is not a\n      connector operation, and the accounting that proves a provider's withheld endpoints are\n      withheld deliberately.\n- [ ] At least two shipped providers declare per-service probes, and fixtures cover both\n      classifications for each. Failing-first test named.\n- [ ] The probe carries no credential value: placement comes from the catalog's auth section exactly\n      as for any request, and a probe's request plan is subject to the same subjects-before-placement\n      rule as an invocation.\n- [ ] The probe is reachable from the platform's connection lifecycle (a `verify` action on a\n      connection) and from `connectors doctor`, through one code path — not two implementations of\n      \"is this credential still good\".\n\n## Context\n\nLet a provider declare a credential-verification probe **per service**, with a failure\nclassification, so the connection lifecycle's `authorized → callable` transition and its degraded\ndetection run from declared data rather than per-provider code. Third of the three adoptions the\nprecedents analysis ordered by cost/benefit.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec, service]. **read**\n\nSource `note:` field, quoted: “research/catalog-precedents.md gap table: Nango declares a verification probe on 281 of 957 providers. Ours is a top-level per-connector `verify` — service-partitioned in the IR already (flux-connectors C-194) but never published or classified. The connection lifecycle needs authorized→callable and callable→degraded to come from declared data, not per-provider code”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-006-per-service-verification-probes.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-006-per-service-verification-probes.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-006`, recorded as the reference `legacy:S-006`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-006"}],"status":"draft","title":"A service declares how a credential is verified, and what a failure means"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/per-service-verification-probes"},"payload":{"actor":"human:timo","at":1788480109789,"causation":"cmd-new-story-per-service-verification-probes","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:per-service-verification-probes@1#0~95238c53133cd091","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"per-service-verification-probes","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:catalog-adoptions"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000132","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788480109798,"causation":"cmd-story-per-service-verification-probes-derived_from-epic-catalog-adoptions","change":{"change":"related","relation":"derived_from","target":"epic:catalog-adoptions"},"correlation":"protocol-artifact-relate","event_id":"story:per-service-verification-probes@1#0~aefcf8680cc6342a","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"m2-the-platform-skeleton-serves","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-007-m2-the-platform-skeleton-serves.md:24`. **read**\n\n- [ ] `crates/domain`, `crates/protocol`, `crates/service`, `crates/server` exist with architecture\n      §2's boundaries enforced, not merely intended: no IO and no HTTP in `domain`; `protocol` holds\n      versioned wire contracts with `deny_unknown_fields` and bounded diagnostics; `service` holds\n      use-cases over ports and is testable without a socket; `server` is composition only.\n- [ ] `connectors serve` with no config is the **personal posture**: an owner-permissioned Unix\n      socket where supported, otherwise loopback plus an automatically generated high-entropy token\n      stored in an owner-only state root that **refuses a working-tree path**. Zero manual\n      configuration remains the contract; unauthenticated localhost is not an identity mode.\n- [ ] `connectors serve --config platform.toml` in **organization/hosted posture** consumes the\n      released B10x Identity verifier/owner bundle. Receiver configuration pins issuer/trust\n      roots, exact Connectors audience, expected tenant/trust domain/deployment, and revocation\n      posture. A missing, stale, incompatible, or malformed verifier configuration refuses startup;\n      identity resolution distinguishes \"nothing presented\" from \"presented and bad\".\n- [ ] Hosted requests accept only the verifier's closed principal result. Identity browser/CLI\n      sessions, upstream-provider tokens, service client assertions, enrollment bearers, and\n      caller-written principal/tenant fields are refused as resource authority. Connectors neither\n      terminates OIDC nor mints, rotates, or persists hosted Identity credentials.\n- [ ] The closed audience scopes from Design 01 are conformance-tested as exact strings. Identity\n      service principals remain Identity-owned; connector route capability and receiver-owned\n      Grants are separate, narrowing gates. A token-carried Connection/Grant reference is never\n      admission proof.\n- [ ] Connectors-owned relational state contains stable tenant/principal projections only where\n      receiver records need them, plus integrations, connections, Grants, channels, deliveries, and\n      connector audit. No Organization, membership, login-session, upstream-token, service-principal\n      credential, or reusable service-bearer verifier table exists.\n- [ ] Tenant-in-admitted-principal is structural: no constructor, port or route handler accepts a\n      tenant beside an admitted identity, and admission compares and refuses on mismatch rather than\n      rewriting. Prove it with a compile-level check, not a scanner.\n- [ ] Connector management policy is fail-closed. Being authenticated, human, an organization\n      member, or an Identity service principal never becomes ambient operator capability.\n- [ ] **Routes fence green**: the HTTP surface is an enumerable value compared against a hand-declared\n      list with an argument per entry, and `Access` lives on the route rather than inside the handler.\n- [ ] The dependency fence classifies every workspace member (catalog / platform / network) and an\n      unclassified member fails the test run; the module size discipline is live (soft cap ~1,500\n      lines, a breach requiring a named waiver in the fence test rather than silence).\n- [ ] Bind policy is fail-closed: personal mode remains owner-permissioned/local; a reachable hosted\n      listener cannot start without the compatible Identity verifier and tenant binding.\n\n## Context\n\nStand up the platform family's four crates with real boundaries. `connectors serve` owns secure\npersonal-local authentication; organization/hosted posture consumes a pinned B10x Identity\nvalidated-envelope/verifier contract and performs its own connector audience-scope and Grant\nadmission. Everything after M2 hangs off these boundaries, so tenant-in-principal,\nidentity-verifier isolation, routes-as-data, and the closed connector-audit vocabulary are cheapest\nto make unrepresentable now.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture §9 milestone M2. Exit: personal-local authentication plus hosted conformance against the pinned B10x Identity owner bundle, with routes and dependency fences green. Connectors never owns hosted login/session/service credentials. This is a milestone container — it will spawn children; keep the exit criteria here as the definition of done for the milestone, not for one commit”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-007-m2-the-platform-skeleton-serves.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-007-m2-the-platform-skeleton-serves.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 5 revision(s)\n- Legacy id `S-007`, recorded as the reference `legacy:S-007`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-007"}],"title":"M2 — the platform skeleton serves in both postures"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-007-m2-the-platform-skeleton-serves.md:24`. **read**\n\n- [ ] `crates/domain`, `crates/protocol`, `crates/service`, `crates/server` exist with architecture\n      §2's boundaries enforced, not merely intended: no IO and no HTTP in `domain`; `protocol` holds\n      versioned wire contracts with `deny_unknown_fields` and bounded diagnostics; `service` holds\n      use-cases over ports and is testable without a socket; `server` is composition only.\n- [ ] `connectors serve` with no config is the **personal posture**: an owner-permissioned Unix\n      socket where supported, otherwise loopback plus an automatically generated high-entropy token\n      stored in an owner-only state root that **refuses a working-tree path**. Zero manual\n      configuration remains the contract; unauthenticated localhost is not an identity mode.\n- [ ] `connectors serve --config platform.toml` in **organization/hosted posture** consumes the\n      released B10x Identity verifier/owner bundle. Receiver configuration pins issuer/trust\n      roots, exact Connectors audience, expected tenant/trust domain/deployment, and revocation\n      posture. A missing, stale, incompatible, or malformed verifier configuration refuses startup;\n      identity resolution distinguishes \"nothing presented\" from \"presented and bad\".\n- [ ] Hosted requests accept only the verifier's closed principal result. Identity browser/CLI\n      sessions, upstream-provider tokens, service client assertions, enrollment bearers, and\n      caller-written principal/tenant fields are refused as resource authority. Connectors neither\n      terminates OIDC nor mints, rotates, or persists hosted Identity credentials.\n- [ ] The closed audience scopes from Design 01 are conformance-tested as exact strings. Identity\n      service principals remain Identity-owned; connector route capability and receiver-owned\n      Grants are separate, narrowing gates. A token-carried Connection/Grant reference is never\n      admission proof.\n- [ ] Connectors-owned relational state contains stable tenant/principal projections only where\n      receiver records need them, plus integrations, connections, Grants, channels, deliveries, and\n      connector audit. No Organization, membership, login-session, upstream-token, service-principal\n      credential, or reusable service-bearer verifier table exists.\n- [ ] Tenant-in-admitted-principal is structural: no constructor, port or route handler accepts a\n      tenant beside an admitted identity, and admission compares and refuses on mismatch rather than\n      rewriting. Prove it with a compile-level check, not a scanner.\n- [ ] Connector management policy is fail-closed. Being authenticated, human, an organization\n      member, or an Identity service principal never becomes ambient operator capability.\n- [ ] **Routes fence green**: the HTTP surface is an enumerable value compared against a hand-declared\n      list with an argument per entry, and `Access` lives on the route rather than inside the handler.\n- [ ] The dependency fence classifies every workspace member (catalog / platform / network) and an\n      unclassified member fails the test run; the module size discipline is live (soft cap ~1,500\n      lines, a breach requiring a named waiver in the fence test rather than silence).\n- [ ] Bind policy is fail-closed: personal mode remains owner-permissioned/local; a reachable hosted\n      listener cannot start without the compatible Identity verifier and tenant binding.\n\n## Context\n\nStand up the platform family's four crates with real boundaries. `connectors serve` owns secure\npersonal-local authentication; organization/hosted posture consumes a pinned B10x Identity\nvalidated-envelope/verifier contract and performs its own connector audience-scope and Grant\nadmission. Everything after M2 hangs off these boundaries, so tenant-in-principal,\nidentity-verifier isolation, routes-as-data, and the closed connector-audit vocabulary are cheapest\nto make unrepresentable now.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture §9 milestone M2. Exit: personal-local authentication plus hosted conformance against the pinned B10x Identity owner bundle, with routes and dependency fences green. Connectors never owns hosted login/session/service credentials. This is a milestone container — it will spawn children; keep the exit criteria here as the definition of done for the milestone, not for one commit”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-007-m2-the-platform-skeleton-serves.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-007-m2-the-platform-skeleton-serves.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 5 revision(s)\n- Legacy id `S-007`, recorded as the reference `legacy:S-007`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-007"}],"status":"draft","title":"M2 — the platform skeleton serves in both postures"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/m2-the-platform-skeleton-serves"},"payload":{"actor":"human:timo","at":1788480109956,"causation":"cmd-new-story-m2-the-platform-skeleton-serves","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:m2-the-platform-skeleton-serves@1#0~8918c4c612682586","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"m2-the-platform-skeleton-serves","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:build-order"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000137","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788480109966,"causation":"cmd-story-m2-the-platform-skeleton-serves-derived_from-epic-build-order","change":{"change":"related","relation":"derived_from","target":"epic:build-order"},"correlation":"protocol-artifact-relate","event_id":"story:m2-the-platform-skeleton-serves@1#0~0bbd2a509805bbfa","executor":null,"recorded_at":"2026-09-04T00:01:49Z"}}
{"entity":"story","version":1,"id":"m3-connect-a-provider-and-invoke-it","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-008-m3-connect-a-provider-and-invoke-it.md:23`. **read**\n\n- [ ] **Integrations** exist as a first-class noun: enabled state, the deployment's BYO OAuth\n      registration as file-shaped secrets, allowed scopes, settings defaults, and the destination\n      policy for private-host providers. An integration **configures** a provider and can never\n      extend one — a test proves surface comes only from the catalog.\n- [ ] **Connect sessions**: short-lived, server-created, single-purpose, optionally bound to a set of\n      allowed integrations, carrying reconciliation tags (end-user id, external ids), surfaced as a\n      hosted URL and headlessly. A session never carries or returns credential material to its\n      creator, and its terminal event names the connection id and nothing else.\n- [ ] **Acquisition**: OAuth authorization-code and operator/API-key entry both complete through a\n      connect session, storing owner-bound credentials with prepared, atomic multi-step mutations;\n      refresh is owned by the platform. No memory fallback, no path inside a working tree, and a\n      refusal names the address and never the value.\n- [ ] **Connection lifecycle**: `created → authorized → callable`, with `degraded → reauthorize`\n      repairing **in place**. Connection ids are stable across reauthorization — proven by a test in\n      which a Grant and a subscription referencing a Connection survive a re-auth. Multiple labelled\n      Connections per Integration are first-class, and both scopes (tenant-shared,\n      principal-owned) exist.\n- [ ] **Grants**: tenant-scoped, per-provider, admitting by selector over declared facts (risk\n      ceiling, effects subset, idempotency) plus explicit exceptions where **deny beats allow beats\n      predicate**; CAS-revisioned mutation with previewable proposals and receipts. No store bound is\n      an outage (503); an empty store is a refusal (403); a refusal never names the axis that refused.\n      Grants bind to connections, never to credentials.\n- [ ] **Declared invoke rides one path**: personal-local authentication or Identity verification →\n      admitted principal with exact Connectors audience scope → effective catalogue (sealed\n      generation) → connector Grant admission through unconstructible proof types → Connection\n      resolution → document → `RequestPlan` (data) → credential placement → egress → connector\n      audit. Permission subjects are computed **before** placement, so a query-placed secret can\n      never enter an approval prompt or an evidence record. Generic v1 has no raw proxy; S-030 owns\n      any later operator-only break-glass path.\n- [ ] Connector scopes and Grants are independently tested. Identity service principals remain\n      Identity-owned, and an Identity-carried Connection/Grant reference cannot skip receiver lookup\n      or fine-grained Grant admission.\n- [ ] Exactly **one** request-composition path exists, held by a fence: a consumer that edits a plan\n      has become a second one, and that is refused by design.\n- [ ] **Exit**: end-to-end tests for personal-local and pinned Identity authority — admit, connect a\n      real provider, grant, invoke — with every connector step audited under the closed vocabulary.\n\n## Context\n\nDeliver the middle layer and the one invocation path: a tenant configures an integration, a human\nauthorizes a vendor connection through a Connect Session, a connector operator grants authority over\ndeclared facts, and a client presents personal-local or short-lived exact-audience Identity\nauthority to invoke a declared operation. Neither the Identity credential nor the vendor credential\nchanges owners. This is the milestone at which the client contract is real.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture §9 milestone M3. Exit: admit personal-local or pinned Identity authority → connect a real provider → grant → invoke, all audited. Connectors never owns the hosted login/session. This is where the product exists; everything before it is scaffolding and everything after it is reach”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-008-m3-connect-a-provider-and-invoke-it.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-008-m3-connect-a-provider-and-invoke-it.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-16 · 6 revision(s)\n- Legacy id `S-008`, recorded as the reference `legacy:S-008`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-008"}],"title":"M3 — connect a real provider, grant it, invoke it"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-008-m3-connect-a-provider-and-invoke-it.md:23`. **read**\n\n- [ ] **Integrations** exist as a first-class noun: enabled state, the deployment's BYO OAuth\n      registration as file-shaped secrets, allowed scopes, settings defaults, and the destination\n      policy for private-host providers. An integration **configures** a provider and can never\n      extend one — a test proves surface comes only from the catalog.\n- [ ] **Connect sessions**: short-lived, server-created, single-purpose, optionally bound to a set of\n      allowed integrations, carrying reconciliation tags (end-user id, external ids), surfaced as a\n      hosted URL and headlessly. A session never carries or returns credential material to its\n      creator, and its terminal event names the connection id and nothing else.\n- [ ] **Acquisition**: OAuth authorization-code and operator/API-key entry both complete through a\n      connect session, storing owner-bound credentials with prepared, atomic multi-step mutations;\n      refresh is owned by the platform. No memory fallback, no path inside a working tree, and a\n      refusal names the address and never the value.\n- [ ] **Connection lifecycle**: `created → authorized → callable`, with `degraded → reauthorize`\n      repairing **in place**. Connection ids are stable across reauthorization — proven by a test in\n      which a Grant and a subscription referencing a Connection survive a re-auth. Multiple labelled\n      Connections per Integration are first-class, and both scopes (tenant-shared,\n      principal-owned) exist.\n- [ ] **Grants**: tenant-scoped, per-provider, admitting by selector over declared facts (risk\n      ceiling, effects subset, idempotency) plus explicit exceptions where **deny beats allow beats\n      predicate**; CAS-revisioned mutation with previewable proposals and receipts. No store bound is\n      an outage (503); an empty store is a refusal (403); a refusal never names the axis that refused.\n      Grants bind to connections, never to credentials.\n- [ ] **Declared invoke rides one path**: personal-local authentication or Identity verification →\n      admitted principal with exact Connectors audience scope → effective catalogue (sealed\n      generation) → connector Grant admission through unconstructible proof types → Connection\n      resolution → document → `RequestPlan` (data) → credential placement → egress → connector\n      audit. Permission subjects are computed **before** placement, so a query-placed secret can\n      never enter an approval prompt or an evidence record. Generic v1 has no raw proxy; S-030 owns\n      any later operator-only break-glass path.\n- [ ] Connector scopes and Grants are independently tested. Identity service principals remain\n      Identity-owned, and an Identity-carried Connection/Grant reference cannot skip receiver lookup\n      or fine-grained Grant admission.\n- [ ] Exactly **one** request-composition path exists, held by a fence: a consumer that edits a plan\n      has become a second one, and that is refused by design.\n- [ ] **Exit**: end-to-end tests for personal-local and pinned Identity authority — admit, connect a\n      real provider, grant, invoke — with every connector step audited under the closed vocabulary.\n\n## Context\n\nDeliver the middle layer and the one invocation path: a tenant configures an integration, a human\nauthorizes a vendor connection through a Connect Session, a connector operator grants authority over\ndeclared facts, and a client presents personal-local or short-lived exact-audience Identity\nauthority to invoke a declared operation. Neither the Identity credential nor the vendor credential\nchanges owners. This is the milestone at which the client contract is real.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture §9 milestone M3. Exit: admit personal-local or pinned Identity authority → connect a real provider → grant → invoke, all audited. Connectors never owns the hosted login/session. This is where the product exists; everything before it is scaffolding and everything after it is reach”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-008-m3-connect-a-provider-and-invoke-it.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-008-m3-connect-a-provider-and-invoke-it.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-16 · 6 revision(s)\n- Legacy id `S-008`, recorded as the reference `legacy:S-008`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-008"}],"status":"draft","title":"M3 — connect a real provider, grant it, invoke it"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/m3-connect-a-provider-and-invoke-it"},"payload":{"actor":"human:timo","at":1788480110124,"causation":"cmd-new-story-m3-connect-a-provider-and-invoke-it","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:m3-connect-a-provider-and-invoke-it@1#0~3cdd66559ebdea63","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"m3-connect-a-provider-and-invoke-it","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:build-order"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000142","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788480110131,"causation":"cmd-story-m3-connect-a-provider-and-invoke-it-derived_from-epic-build-order","change":{"change":"related","relation":"derived_from","target":"epic:build-order"},"correlation":"protocol-artifact-relate","event_id":"story:m3-connect-a-provider-and-invoke-it@1#0~d14962f2f9df8376","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"m4-events-reach-a-client-by-push-and-by-pull","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-009-m4-events-reach-a-client-by-push-and-by-pull.md:22`. **read**\n\n- [ ] **Channel supervisor**: owns the provider-side transports declared by the catalog's channel\n      bindings, per connection instance, with opaque host-minted ids and a restart-safe lifecycle the\n      platform owns. A channel bound to a connection survives that connection's reauthorization\n      (S-008's stable-id invariant).\n- [ ] **Webhook terminator**: one inbound endpoint; per-provider verification and attribution come\n      from declarative catalog rules ([S-012](S-012-declarative-webhook-routing-grammar.md));\n      verification runs over the **raw body before parsing**, comparison is constant-time with a\n      timestamp tolerance, and a vendor challenge/handshake is answered without waking a client.\n- [ ] **Event store**: append-only, provenance (`native` vs `polled`) on every row and honestly\n      recorded, dedup by delivery id, and the domain model's family split — operational events\n      (credential degraded, delivery failing, channel down) are never mixed into the data stream and\n      have their own subscriptions.\n- [ ] **Deliveries**: durable per-endpoint queues with the Svix envelope — `id` + `timestamp` +\n      HMAC-SHA256 over `{id}.{timestamp}.{body}` with a **dedicated signing key** — and retries with\n      backoff. Never sign with an API key; never canonicalize the payload before signing (both are\n      named anti-patterns from the researched field).\n- [ ] **Replay by id**: a client replays a delivery it names, through the client API, under its own\n      grants. The replay is distinguishable from the original (same event id, new delivery attempt),\n      is audited, and cannot be used to reach an event the principal's inbound grants do not admit.\n- [ ] **Subscriptions**: one authenticated, multiplexed WebSocket per client, gated by inbound\n      grants — every requested event must belong to the binding's **closed** declared set; an\n      ungranted subset and a cross-tenant channel id share one refusal that discloses neither.\n- [ ] **Exit**: one test in which a single provider event reaches a client both by push (signed\n      delivery) and by pull (subscription), carrying the same event id and the same provenance.\n\n## Context\n\nMake the reverse direction real: the platform owns provider-side transports so events flow without\nany client running, terminates and verifies inbound webhooks from declarative catalog rules, stores\nevents honestly with provenance, and delivers them to clients durably — with **replay-by-id** as a\nfirst-class client API rather than a support ticket.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture §9 milestone M4. Exit: a provider event reaches a client by push and by pull, with provenance. Replay-by-id is the category's most conspicuous gap (research §6 pitfall 4) and is acceptance here, not a later nicety”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-009-m4-events-reach-a-client-by-push-and-by-pull.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-009-m4-events-reach-a-client-by-push-and-by-pull.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-16 · 4 revision(s)\n- Legacy id `S-009`, recorded as the reference `legacy:S-009`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-009"}],"title":"M4 — a provider event reaches a client by push and by pull, with provenance"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-009-m4-events-reach-a-client-by-push-and-by-pull.md:22`. **read**\n\n- [ ] **Channel supervisor**: owns the provider-side transports declared by the catalog's channel\n      bindings, per connection instance, with opaque host-minted ids and a restart-safe lifecycle the\n      platform owns. A channel bound to a connection survives that connection's reauthorization\n      (S-008's stable-id invariant).\n- [ ] **Webhook terminator**: one inbound endpoint; per-provider verification and attribution come\n      from declarative catalog rules ([S-012](S-012-declarative-webhook-routing-grammar.md));\n      verification runs over the **raw body before parsing**, comparison is constant-time with a\n      timestamp tolerance, and a vendor challenge/handshake is answered without waking a client.\n- [ ] **Event store**: append-only, provenance (`native` vs `polled`) on every row and honestly\n      recorded, dedup by delivery id, and the domain model's family split — operational events\n      (credential degraded, delivery failing, channel down) are never mixed into the data stream and\n      have their own subscriptions.\n- [ ] **Deliveries**: durable per-endpoint queues with the Svix envelope — `id` + `timestamp` +\n      HMAC-SHA256 over `{id}.{timestamp}.{body}` with a **dedicated signing key** — and retries with\n      backoff. Never sign with an API key; never canonicalize the payload before signing (both are\n      named anti-patterns from the researched field).\n- [ ] **Replay by id**: a client replays a delivery it names, through the client API, under its own\n      grants. The replay is distinguishable from the original (same event id, new delivery attempt),\n      is audited, and cannot be used to reach an event the principal's inbound grants do not admit.\n- [ ] **Subscriptions**: one authenticated, multiplexed WebSocket per client, gated by inbound\n      grants — every requested event must belong to the binding's **closed** declared set; an\n      ungranted subset and a cross-tenant channel id share one refusal that discloses neither.\n- [ ] **Exit**: one test in which a single provider event reaches a client both by push (signed\n      delivery) and by pull (subscription), carrying the same event id and the same provenance.\n\n## Context\n\nMake the reverse direction real: the platform owns provider-side transports so events flow without\nany client running, terminates and verifies inbound webhooks from declarative catalog rules, stores\nevents honestly with provenance, and delivers them to clients durably — with **replay-by-id** as a\nfirst-class client API rather than a support ticket.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture §9 milestone M4. Exit: a provider event reaches a client by push and by pull, with provenance. Replay-by-id is the category's most conspicuous gap (research §6 pitfall 4) and is acceptance here, not a later nicety”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-009-m4-events-reach-a-client-by-push-and-by-pull.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-009-m4-events-reach-a-client-by-push-and-by-pull.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-16 · 4 revision(s)\n- Legacy id `S-009`, recorded as the reference `legacy:S-009`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-009"}],"status":"draft","title":"M4 — a provider event reaches a client by push and by pull, with provenance"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/m4-events-reach-a-client-by-push-and-by-pull"},"payload":{"actor":"human:timo","at":1788480110287,"causation":"cmd-new-story-m4-events-reach-a-client-by-push-and-by-pull","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:m4-events-reach-a-client-by-push-and-by-pull@1#0~cf6ddce0ffb9e49c","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"m4-events-reach-a-client-by-push-and-by-pull","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:build-order"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000147","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788480110298,"causation":"cmd-story-m4-events-reach-a-client-by-push-and-by-pull-derived_from-epic-build-order","change":{"change":"related","relation":"derived_from","target":"epic:build-order"},"correlation":"protocol-artifact-relate","event_id":"story:m4-events-reach-a-client-by-push-and-by-pull@1#0~b00fa93e63434a1f","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"m5-flux-re-points-and-the-gitlab-plugin-retires","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md:24`. **read**\n\n- [ ] flux's embedded platform client drives the whole client contract against this platform's\n      **published protocol identities**: authenticate once, discover the effective catalogue and\n      project it to tools, invoke, subscribe. This repository depends on nothing of flux — the\n      dependency arrow exists only at runtime, in one direction, over versioned contracts\n      ([dependency rules](https://github.com/beyond10x/b10x/blob/bf6859717f986dc0e2a3b8a713e087d426741d92/architecture/architecture/dependency-rules.md)).\n- [ ] flux's CLI manages a **personal-posture** instance: verified download, supervised local\n      process, zero-configuration `connectors serve`, owner-bound state — carrying forward the\n      predecessor's managed-local-install requirement without its ~31k-LOC local-management\n      ceremony; the supervision contract is redesigned lean, not copied.\n- [ ] **First-wave parity**: gitlab's frozen behavioral\n      fixtures pass through the platform over the op inventory the plugin **actually serves**\n      (descriptor grants plus event-store audit evidence), not its whole surface.\n- [ ] The gitlab plugin artifact is **deleted** in the same flux release train as its proven\n      replacement — per plugin, no batching of unproven cutovers.\n- [ ] The following cutover preconditions are met or explicitly re-scoped against the new surface,\n      each named in this story's Progress: self-serve onboarding (connect / grant / list / doctor) is\n      real, `flux app run` constructs the platform client with the same fail-closed\n      withdraw-on-refresh-failure semantics as the interactive path, and a credential migration path\n      exists (re-acquisition is acceptable; silently copying secrets out of the local store is not).\n- [ ] A client whose still-valid presented authority is unchanged survives a vendor Connection\n      reauthorization and a platform restart without repeating Identity login or personal-local\n      bootstrap. Normal Identity expiry/rotation remains Identity-owned and does not become a\n      Connectors service-token lifecycle.\n\n## Context\n\nProve the client contract against its first native client: Flux presents one current\nConnectors-audience Identity authority in hosted posture or the owner-held local bearer in personal\nposture, projects the effective catalogue into tools, invokes and subscribes — and the first native\nplugin retires onto the declared surface, deleted rather than deprecated. Connectors mints neither\nthe hosted authority nor vendor credentials. The seam keeps B10x's one-way dependency rule\nfixed; only the trust domain and release origin change.\n\nSource frontmatter: pillar Clients · areas [protocol, server, docs] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “Externally gated: Flux must record B10x adoption in its own repository before this family can schedule M5. Private predecessor records are provenance; this story restates the normative parity and cutover rules”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-24 · 9 revision(s)\n- Legacy id `S-010`, recorded as the reference `legacy:S-010`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-010"}],"title":"M5 — flux re-points at the platform and the gitlab plugin is deleted"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md:24`. **read**\n\n- [ ] flux's embedded platform client drives the whole client contract against this platform's\n      **published protocol identities**: authenticate once, discover the effective catalogue and\n      project it to tools, invoke, subscribe. This repository depends on nothing of flux — the\n      dependency arrow exists only at runtime, in one direction, over versioned contracts\n      ([dependency rules](https://github.com/beyond10x/b10x/blob/bf6859717f986dc0e2a3b8a713e087d426741d92/architecture/architecture/dependency-rules.md)).\n- [ ] flux's CLI manages a **personal-posture** instance: verified download, supervised local\n      process, zero-configuration `connectors serve`, owner-bound state — carrying forward the\n      predecessor's managed-local-install requirement without its ~31k-LOC local-management\n      ceremony; the supervision contract is redesigned lean, not copied.\n- [ ] **First-wave parity**: gitlab's frozen behavioral\n      fixtures pass through the platform over the op inventory the plugin **actually serves**\n      (descriptor grants plus event-store audit evidence), not its whole surface.\n- [ ] The gitlab plugin artifact is **deleted** in the same flux release train as its proven\n      replacement — per plugin, no batching of unproven cutovers.\n- [ ] The following cutover preconditions are met or explicitly re-scoped against the new surface,\n      each named in this story's Progress: self-serve onboarding (connect / grant / list / doctor) is\n      real, `flux app run` constructs the platform client with the same fail-closed\n      withdraw-on-refresh-failure semantics as the interactive path, and a credential migration path\n      exists (re-acquisition is acceptable; silently copying secrets out of the local store is not).\n- [ ] A client whose still-valid presented authority is unchanged survives a vendor Connection\n      reauthorization and a platform restart without repeating Identity login or personal-local\n      bootstrap. Normal Identity expiry/rotation remains Identity-owned and does not become a\n      Connectors service-token lifecycle.\n\n## Context\n\nProve the client contract against its first native client: Flux presents one current\nConnectors-audience Identity authority in hosted posture or the owner-held local bearer in personal\nposture, projects the effective catalogue into tools, invokes and subscribes — and the first native\nplugin retires onto the declared surface, deleted rather than deprecated. Connectors mints neither\nthe hosted authority nor vendor credentials. The seam keeps B10x's one-way dependency rule\nfixed; only the trust domain and release origin change.\n\nSource frontmatter: pillar Clients · areas [protocol, server, docs] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “Externally gated: Flux must record B10x adoption in its own repository before this family can schedule M5. Private predecessor records are provenance; this story restates the normative parity and cutover rules”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-24 · 9 revision(s)\n- Legacy id `S-010`, recorded as the reference `legacy:S-010`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-010"}],"status":"draft","title":"M5 — flux re-points at the platform and the gitlab plugin is deleted"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/m5-flux-re-points-and-the-gitlab-plugin-retires"},"payload":{"actor":"human:timo","at":1788480110453,"causation":"cmd-new-story-m5-flux-re-points-and-the-gitlab-plugin-retires","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:m5-flux-re-points-and-the-gitlab-plugin-retires@1#0~9bcec88d98b2b44d","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"m5-flux-re-points-and-the-gitlab-plugin-retires","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:build-order"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000152","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788480110463,"causation":"cmd-story-m5-flux-re-points-and-the-gitlab-plugin-retires-derived_from-epic-build-order","change":{"change":"related","relation":"derived_from","target":"epic:build-order"},"correlation":"protocol-artifact-relate","event_id":"story:m5-flux-re-points-and-the-gitlab-plugin-retires@1#0~240c8976529aae01","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"m5-flux-re-points-and-the-gitlab-plugin-retires","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000094","to":"proposed"},"payload":{"actor":"human:timo","at":1788480110605,"causation":"cmd-move-story-m5-flux-re-points-and-the-gitlab-plugin-retires-1788480110605","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:m5-flux-re-points-and-the-gitlab-plugin-retires@2#0~73334c424c3a2b0d","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"m5-flux-re-points-and-the-gitlab-plugin-retires","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000094","to":"active"},"payload":{"actor":"human:timo","at":1788480110746,"causation":"cmd-move-story-m5-flux-re-points-and-the-gitlab-plugin-retires-1788480110746","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:m5-flux-re-points-and-the-gitlab-plugin-retires@3#0~2e1ab2a15349e83d","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"deployment-declared-destination-aperture","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-011-deployment-declared-destination-aperture.md:24`. **read**\n\n- [ ] A deployment-declared destination list (the `[egress]` section of `platform.toml`, plus the\n      integration's destination policy for private-host providers) admits a named private destination\n      for connections whose integration requires it; everything undeclared stays refused, proven by\n      fixtures that include **a public hostname resolving to a private address** — matching happens\n      post-resolution, so DNS rebinding cannot smuggle a destination past the check.\n- [ ] The aperture is posture-scoped and each posture's behaviour is proven by a refusal test: **org**\n      admits per its declaration; **saas** refuses unconditionally; **personal** keeps the owner's\n      local rule. A posture never inherits another's aperture by default.\n- [ ] **No request field, catalog entry, connection config value or grant can name or widen a\n      destination.** The allowlist is deployment configuration only. Audit records never contain\n      credentials or raw secret-bearing URLs, but retain a stable policy-rule id and a normalized\n      destination hash so incident response can determine which aperture admitted a call.\n- [ ] Any operator-only raw proxy also requires a separately granted method/path aperture and is\n      evaluated by this same post-resolution destination policy; an arbitrary path cannot convert a\n      destination grant into arbitrary provider authority.\n- [ ] Every composition point consumes **one shared policy**: the `server` egress module is the only\n      place a vendor socket is opened (architecture §5), the invocation path and the channel\n      supervisor both go through it, and a census/fence test refuses a **third** composition point\n      appearing — the predecessor's failure mode was two dialers composing the same policy\n      independently and drifting.\n- [ ] A malformed or unparsable egress declaration admits **nothing** and refuses startup by name —\n      fail closed, consistent with the operator policy rule.\n\n## Context\n\nOpen exactly one narrow aperture in the egress rule: a deployment whose own infrastructure\nlegitimately reaches an in-cluster service declares which destinations are admitted, as configuration,\nvalue-free — and everything undeclared stays refused **after resolution**. Refusing every private\ndestination unconditionally is correct for the open internet and exactly wrong for an org-posture\ndeployment whose value is reaching its own Grafana; admitting one on a request field would hand\ndestination selection to a model.\n\nSource frontmatter: pillar Platform · areas [service, server, domain]. **read**\n\nSource `note:` field, quoted: “ported from flux-exchange X-143 and restated here as B10x authority. A deployment declares the aperture; a member, model input, Identity service principal, connector Grant, or raw-proxy request cannot widen it; matching is post-resolution”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-011-deployment-declared-destination-aperture.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-011-deployment-declared-destination-aperture.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 7 revision(s)\n- Legacy id `S-011`, recorded as the reference `legacy:S-011`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-011"}],"title":"Deployment-declared destination aperture"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-011-deployment-declared-destination-aperture.md:24`. **read**\n\n- [ ] A deployment-declared destination list (the `[egress]` section of `platform.toml`, plus the\n      integration's destination policy for private-host providers) admits a named private destination\n      for connections whose integration requires it; everything undeclared stays refused, proven by\n      fixtures that include **a public hostname resolving to a private address** — matching happens\n      post-resolution, so DNS rebinding cannot smuggle a destination past the check.\n- [ ] The aperture is posture-scoped and each posture's behaviour is proven by a refusal test: **org**\n      admits per its declaration; **saas** refuses unconditionally; **personal** keeps the owner's\n      local rule. A posture never inherits another's aperture by default.\n- [ ] **No request field, catalog entry, connection config value or grant can name or widen a\n      destination.** The allowlist is deployment configuration only. Audit records never contain\n      credentials or raw secret-bearing URLs, but retain a stable policy-rule id and a normalized\n      destination hash so incident response can determine which aperture admitted a call.\n- [ ] Any operator-only raw proxy also requires a separately granted method/path aperture and is\n      evaluated by this same post-resolution destination policy; an arbitrary path cannot convert a\n      destination grant into arbitrary provider authority.\n- [ ] Every composition point consumes **one shared policy**: the `server` egress module is the only\n      place a vendor socket is opened (architecture §5), the invocation path and the channel\n      supervisor both go through it, and a census/fence test refuses a **third** composition point\n      appearing — the predecessor's failure mode was two dialers composing the same policy\n      independently and drifting.\n- [ ] A malformed or unparsable egress declaration admits **nothing** and refuses startup by name —\n      fail closed, consistent with the operator policy rule.\n\n## Context\n\nOpen exactly one narrow aperture in the egress rule: a deployment whose own infrastructure\nlegitimately reaches an in-cluster service declares which destinations are admitted, as configuration,\nvalue-free — and everything undeclared stays refused **after resolution**. Refusing every private\ndestination unconditionally is correct for the open internet and exactly wrong for an org-posture\ndeployment whose value is reaching its own Grafana; admitting one on a request field would hand\ndestination selection to a model.\n\nSource frontmatter: pillar Platform · areas [service, server, domain]. **read**\n\nSource `note:` field, quoted: “ported from flux-exchange X-143 and restated here as B10x authority. A deployment declares the aperture; a member, model input, Identity service principal, connector Grant, or raw-proxy request cannot widen it; matching is post-resolution”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-011-deployment-declared-destination-aperture.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-011-deployment-declared-destination-aperture.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 7 revision(s)\n- Legacy id `S-011`, recorded as the reference `legacy:S-011`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-011"}],"status":"draft","title":"Deployment-declared destination aperture"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/deployment-declared-destination-aperture"},"payload":{"actor":"human:timo","at":1788480110905,"causation":"cmd-new-story-deployment-declared-destination-aperture","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:deployment-declared-destination-aperture@1#0~9b793ba057e0ce0f","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"deployment-declared-destination-aperture","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:carried-constraints"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000157","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788480110913,"causation":"cmd-story-deployment-declared-destination-aperture-derived_from-epic-carried-constraints","change":{"change":"related","relation":"derived_from","target":"epic:carried-constraints"},"correlation":"protocol-artifact-relate","event_id":"story:deployment-declared-destination-aperture@1#0~abdec0e3ad8e0a15","executor":null,"recorded_at":"2026-09-04T00:01:50Z"}}
{"entity":"story","version":1,"id":"declarative-webhook-routing-grammar","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-012-declarative-webhook-routing-grammar.md:23`. **read**\n\n- [ ] The grammar expresses, as data: the **verification matrix** (digest, encoding, signed template,\n      tolerance), the **discriminator** that labels an event, the **delivery-id selector** for dedup,\n      the **attribution rule** that resolves a payload to a connection, and the **payload map** — over\n      a **closed** event set, no wildcards.\n- [ ] Coverage is **measured, not asserted**, before generalizing: all five existing channel bindings\n      re-express with no loss (byte-comparable behaviour against their current fixtures), and the\n      Nango providers that declare a `webhook_routing_script` (54 of 957) are each classified as\n      expressible or named as a defeat, with the count recorded in the design.\n- [ ] Two known defeats of the predecessor's shape are expressible or explicitly refused with a named\n      reason:\n      **(a)** Stripe's composite `Stripe-Signature: t=…,v1=…,v0=…` — the digest is neither the whole\n      header value nor a literal prefix of it, the timestamp is a *component of the same header*, and\n      several `v1=` arrive during a secret rotation, so a verifier admits if **any** candidate\n      matches;\n      **(b)** a **body-sourced timestamp**, which is unimplementable by construction for a\n      verify-before-parse terminator (it is an input to the decision about whether the body may be\n      parsed at all) and must therefore be refused at **build** time, in the repository that owns the\n      declaration, not at load time in the consumer.\n- [ ] A verification scheme we know exists and cannot yet model is a **declarable third state**, not a\n      guess and not a lie: the predecessor had no way to say \"the vendor signs, and we cannot describe\n      how\", so the only declarations that loaded were wrong ones.\n- [ ] **No script escape hatch in v1.** A declaration attempting one is refused at load by name, and\n      the justification (Nango's 94%) is restated where the schema documents itself, so the next\n      person to want one has to defeat the grammar with a real provider first.\n- [ ] Adversarial fixtures per rule kind: wrong signature, replayed timestamp outside tolerance,\n      unknown discriminator, unattributable payload, duplicate delivery id — each refused by name and\n      counted, never absorbed. Failing-first test named per rule kind.\n\n## Context\n\nGive the webhook terminator a declarative rule grammar in the catalog — how a provider's delivery is\nverified, which connection it attributes to, which event it is, and how it deduplicates — so\nterminating inbound events scales across providers without per-provider code. Nango leaves exactly\ntwo things to scripts (webhook routing, 5.6%; post-connection, 4.3%); this is the boundary we intend\nto close declaratively, and the design has to be tested against real providers before it generalizes.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec, service]. **read**\n\nSource `note:` field, quoted: “architecture §6 and research consequence 3: design the declarative routing grammar against the five existing channel bindings and the Nango corpus BEFORE scaling inbound events. No script escape hatch in v1 — Nango's own data says 94% of 957 providers never needed one”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-012-declarative-webhook-routing-grammar.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-012-declarative-webhook-routing-grammar.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-012`, recorded as the reference `legacy:S-012`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-012"}],"title":"Webhook verification and attribution are declared as data, not as a per-provider script"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-012-declarative-webhook-routing-grammar.md:23`. **read**\n\n- [ ] The grammar expresses, as data: the **verification matrix** (digest, encoding, signed template,\n      tolerance), the **discriminator** that labels an event, the **delivery-id selector** for dedup,\n      the **attribution rule** that resolves a payload to a connection, and the **payload map** — over\n      a **closed** event set, no wildcards.\n- [ ] Coverage is **measured, not asserted**, before generalizing: all five existing channel bindings\n      re-express with no loss (byte-comparable behaviour against their current fixtures), and the\n      Nango providers that declare a `webhook_routing_script` (54 of 957) are each classified as\n      expressible or named as a defeat, with the count recorded in the design.\n- [ ] Two known defeats of the predecessor's shape are expressible or explicitly refused with a named\n      reason:\n      **(a)** Stripe's composite `Stripe-Signature: t=…,v1=…,v0=…` — the digest is neither the whole\n      header value nor a literal prefix of it, the timestamp is a *component of the same header*, and\n      several `v1=` arrive during a secret rotation, so a verifier admits if **any** candidate\n      matches;\n      **(b)** a **body-sourced timestamp**, which is unimplementable by construction for a\n      verify-before-parse terminator (it is an input to the decision about whether the body may be\n      parsed at all) and must therefore be refused at **build** time, in the repository that owns the\n      declaration, not at load time in the consumer.\n- [ ] A verification scheme we know exists and cannot yet model is a **declarable third state**, not a\n      guess and not a lie: the predecessor had no way to say \"the vendor signs, and we cannot describe\n      how\", so the only declarations that loaded were wrong ones.\n- [ ] **No script escape hatch in v1.** A declaration attempting one is refused at load by name, and\n      the justification (Nango's 94%) is restated where the schema documents itself, so the next\n      person to want one has to defeat the grammar with a real provider first.\n- [ ] Adversarial fixtures per rule kind: wrong signature, replayed timestamp outside tolerance,\n      unknown discriminator, unattributable payload, duplicate delivery id — each refused by name and\n      counted, never absorbed. Failing-first test named per rule kind.\n\n## Context\n\nGive the webhook terminator a declarative rule grammar in the catalog — how a provider's delivery is\nverified, which connection it attributes to, which event it is, and how it deduplicates — so\nterminating inbound events scales across providers without per-provider code. Nango leaves exactly\ntwo things to scripts (webhook routing, 5.6%; post-connection, 4.3%); this is the boundary we intend\nto close declaratively, and the design has to be tested against real providers before it generalizes.\n\nSource frontmatter: pillar Catalog · areas [catalog, connector-spec, service]. **read**\n\nSource `note:` field, quoted: “architecture §6 and research consequence 3: design the declarative routing grammar against the five existing channel bindings and the Nango corpus BEFORE scaling inbound events. No script escape hatch in v1 — Nango's own data says 94% of 957 providers never needed one”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-012-declarative-webhook-routing-grammar.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-012-declarative-webhook-routing-grammar.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-012`, recorded as the reference `legacy:S-012`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-012"}],"status":"draft","title":"Webhook verification and attribution are declared as data, not as a per-provider script"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/declarative-webhook-routing-grammar"},"payload":{"actor":"human:timo","at":1788480111072,"causation":"cmd-new-story-declarative-webhook-routing-grammar","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:declarative-webhook-routing-grammar@1#0~9dce88ca07841809","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"declarative-webhook-routing-grammar","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:carried-constraints"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000162","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788480111082,"causation":"cmd-story-declarative-webhook-routing-grammar-derived_from-epic-carried-constraints","change":{"change":"related","relation":"derived_from","target":"epic:carried-constraints"},"correlation":"protocol-artifact-relate","event_id":"story:declarative-webhook-routing-grammar@1#0~401f9821a1a63389","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"connect-session-oauth-custody-in-personal-posture","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-013-connect-session-oauth-custody-in-personal-posture.md:22`. **read**\n\n- [ ] A numbered design document (`docs/design/NN-*.md`, per AGENTS.md's series rule) names the\n      custody chain for each flow it admits: **loopback redirect** (`http://127.0.0.1:<port>/…`\n      registered as the deployment's redirect URI) and **device-code-style** flows, with the rule for\n      choosing between them. It states, measured against real vendor registration rules rather than\n      assumed, which providers refuse loopback, which refuse plain `http` on loopback, and which\n      offer a device flow — naming the vendors checked and the date.\n- [ ] The choice is **declared in the catalog** (the redirect-URI shapes and grant flows a provider\n      admits) rather than decided at runtime by attempting one and falling back. A fallback would let\n      the same connection succeed by different means on two machines, and the pair would be\n      unreviewable.\n- [ ] The connect-session invariants hold unchanged in personal posture: single-purpose, short-lived,\n      never returns credential material to its creator, terminal event names the connection id and\n      nothing else. The loopback listener is bound to **one** session, admits exactly one callback,\n      binds the port for no longer than the session lives, validates `state`, PKCE, exact Host and\n      expected Origin where present, and refuses CSRF, DNS-rebinding, state, or PKCE mismatch by\n      name.\n- [ ] The headless case is covered explicitly: an agent obtains the URL and a human completes it\n      ([S-014](S-014-auth-as-tool-result.md)'s consumer), **including** the case where the human's\n      browser is not on the machine running the listener. That case is either supported by a named\n      mechanism (device code, or a paste-back of the authorization response) or refused with a message\n      that says what to do instead — never a silent hang until the session expires.\n- [ ] BYO registration stays deployment configuration: no `client_id` and no secret appears in the\n      catalog, the redirect URI a user must register is derivable from their configuration alone, and\n      `connectors doctor` can print it.\n- [ ] Whatever is decided is portable: a connection authorized in personal posture and one authorized\n      in org posture are the same connection shape, because the catalog and the auth templates are\n      the same text everywhere (vision, three postures).\n\n## Context\n\nAnswer the domain model's open question 3 — how a connect session takes custody of an OAuth callback\non a machine with no public callback origin — and make the answer **declared per provider** rather\nthan discovered at runtime, so a personal-posture user can connect a real vendor through the same\nconnect-session contract every posture uses.\n\nSource frontmatter: pillar Platform · areas [domain, service, catalog]. **read**\n\nSource `note:` field, quoted: “domain model open question 3: personal posture has no public callback origin, so the custody chain is loopback redirect vs device-code-style flows — per provider, and per what each vendor's registration rules actually admit. A decision story: the deliverable is a numbered design document plus the schema consequence”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-013-connect-session-oauth-custody-in-personal-posture.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-013-connect-session-oauth-custody-in-personal-posture.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-013`, recorded as the reference `legacy:S-013`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-013"}],"title":"Decide the connect-session ↔ OAuth-callback custody chain in personal posture"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-013-connect-session-oauth-custody-in-personal-posture.md:22`. **read**\n\n- [ ] A numbered design document (`docs/design/NN-*.md`, per AGENTS.md's series rule) names the\n      custody chain for each flow it admits: **loopback redirect** (`http://127.0.0.1:<port>/…`\n      registered as the deployment's redirect URI) and **device-code-style** flows, with the rule for\n      choosing between them. It states, measured against real vendor registration rules rather than\n      assumed, which providers refuse loopback, which refuse plain `http` on loopback, and which\n      offer a device flow — naming the vendors checked and the date.\n- [ ] The choice is **declared in the catalog** (the redirect-URI shapes and grant flows a provider\n      admits) rather than decided at runtime by attempting one and falling back. A fallback would let\n      the same connection succeed by different means on two machines, and the pair would be\n      unreviewable.\n- [ ] The connect-session invariants hold unchanged in personal posture: single-purpose, short-lived,\n      never returns credential material to its creator, terminal event names the connection id and\n      nothing else. The loopback listener is bound to **one** session, admits exactly one callback,\n      binds the port for no longer than the session lives, validates `state`, PKCE, exact Host and\n      expected Origin where present, and refuses CSRF, DNS-rebinding, state, or PKCE mismatch by\n      name.\n- [ ] The headless case is covered explicitly: an agent obtains the URL and a human completes it\n      ([S-014](S-014-auth-as-tool-result.md)'s consumer), **including** the case where the human's\n      browser is not on the machine running the listener. That case is either supported by a named\n      mechanism (device code, or a paste-back of the authorization response) or refused with a message\n      that says what to do instead — never a silent hang until the session expires.\n- [ ] BYO registration stays deployment configuration: no `client_id` and no secret appears in the\n      catalog, the redirect URI a user must register is derivable from their configuration alone, and\n      `connectors doctor` can print it.\n- [ ] Whatever is decided is portable: a connection authorized in personal posture and one authorized\n      in org posture are the same connection shape, because the catalog and the auth templates are\n      the same text everywhere (vision, three postures).\n\n## Context\n\nAnswer the domain model's open question 3 — how a connect session takes custody of an OAuth callback\non a machine with no public callback origin — and make the answer **declared per provider** rather\nthan discovered at runtime, so a personal-posture user can connect a real vendor through the same\nconnect-session contract every posture uses.\n\nSource frontmatter: pillar Platform · areas [domain, service, catalog]. **read**\n\nSource `note:` field, quoted: “domain model open question 3: personal posture has no public callback origin, so the custody chain is loopback redirect vs device-code-style flows — per provider, and per what each vendor's registration rules actually admit. A decision story: the deliverable is a numbered design document plus the schema consequence”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-013-connect-session-oauth-custody-in-personal-posture.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-013-connect-session-oauth-custody-in-personal-posture.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-013`, recorded as the reference `legacy:S-013`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-013"}],"status":"draft","title":"Decide the connect-session ↔ OAuth-callback custody chain in personal posture"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/connect-session-oauth-custody-in-personal-posture"},"payload":{"actor":"human:timo","at":1788480111245,"causation":"cmd-new-story-connect-session-oauth-custody-in-personal-posture","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:connect-session-oauth-custody-in-personal-posture@1#0~ed82f01ce876b569","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"connect-session-oauth-custody-in-personal-posture","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:carried-constraints"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000167","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788480111257,"causation":"cmd-story-connect-session-oauth-custody-in-personal-posture-derived_from-epic-carried-constraints","change":{"change":"related","relation":"derived_from","target":"epic:carried-constraints"},"correlation":"protocol-artifact-relate","event_id":"story:connect-session-oauth-custody-in-personal-posture@1#0~efac31ed77e4b3a0","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"auth-as-tool-result","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-014-auth-as-tool-result.md:22`. **read**\n\n- [ ] A grant-admitted invocation against a missing or degraded connection returns a **distinct,\n      structured protocol response** under its own protocol identity, carrying: the integration it\n      needs, a connect URL backed by a freshly created connect session, that session's expiry, and how\n      to resume. It is not an HTTP status shared with authorization refusals, and it is not an error\n      string a client has to pattern-match.\n- [ ] It is **not an authorization bypass**: the response is produced only *after* grant admission\n      admits the operation. A principal whose grants do not admit the operation receives the ordinary\n      refusal and learns nothing about which connections exist or which integrations are configured —\n      no enumeration oracle, consistent with the grant rule that a refusal never names the axis that\n      refused.\n- [ ] The connect URL carries no credential and no tenant secret; the session is single-purpose\n      and bound to exactly the integration the operation needs (the domain model's\n      `allowed_integrations` binding), so handing the URL to a human cannot widen anything.\n- [ ] **Degraded is distinguished from missing**: a degraded connection yields a\n      **reauthorize-in-place** URL and the connection id is unchanged afterwards; a missing connection\n      yields a create-connection URL. A client that stored the connection id keeps it in the first\n      case.\n- [ ] The client can complete the loop with the same still-valid personal-local or\n      Connectors-audience Identity authority: after the human finishes vendor authorization, the same\n      invocation succeeds without a second Identity login or local bootstrap. Identity expiry and\n      rotation remain Identity-owned. The end-to-end test includes the wait/poll shape a client uses\n      to learn the Connect Session completed.\n- [ ] The response shape is fixture-covered in `protocol` conformance, positive and adversarial\n      (expired session, session completed for a different integration, session already consumed), so\n      a future SDK test suite shares the fixtures verbatim.\n\n## Context\n\nWhen a granted invocation needs a connection that does not exist or has degraded, answer with a\n**structured next step** — a connect URL a human can complete, backed by a real connect session —\ninstead of an error an agent can only report. This is the pattern designed for an agent that hits the\nwall mid-task, and it is a client-contract promise in the vision, not a convenience.\n\nSource frontmatter: pillar Platform · areas [protocol, service, server]. **read**\n\nSource `note:` field, quoted: “research §5 pattern 8, called out there as 'the single most agent-native pattern found' (Arcade authorize→waitForCompletion, Pipedream MCP returning a connect URL, Composio Connect Links mid-conversation). Vision, client contract: not-connected is a next step, not an error”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-014-auth-as-tool-result.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-014-auth-as-tool-result.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-14 · 2 revision(s)\n- Legacy id `S-014`, recorded as the reference `legacy:S-014`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-014"}],"title":"Not-connected is a next step: the response carries a connect URL"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-014-auth-as-tool-result.md:22`. **read**\n\n- [ ] A grant-admitted invocation against a missing or degraded connection returns a **distinct,\n      structured protocol response** under its own protocol identity, carrying: the integration it\n      needs, a connect URL backed by a freshly created connect session, that session's expiry, and how\n      to resume. It is not an HTTP status shared with authorization refusals, and it is not an error\n      string a client has to pattern-match.\n- [ ] It is **not an authorization bypass**: the response is produced only *after* grant admission\n      admits the operation. A principal whose grants do not admit the operation receives the ordinary\n      refusal and learns nothing about which connections exist or which integrations are configured —\n      no enumeration oracle, consistent with the grant rule that a refusal never names the axis that\n      refused.\n- [ ] The connect URL carries no credential and no tenant secret; the session is single-purpose\n      and bound to exactly the integration the operation needs (the domain model's\n      `allowed_integrations` binding), so handing the URL to a human cannot widen anything.\n- [ ] **Degraded is distinguished from missing**: a degraded connection yields a\n      **reauthorize-in-place** URL and the connection id is unchanged afterwards; a missing connection\n      yields a create-connection URL. A client that stored the connection id keeps it in the first\n      case.\n- [ ] The client can complete the loop with the same still-valid personal-local or\n      Connectors-audience Identity authority: after the human finishes vendor authorization, the same\n      invocation succeeds without a second Identity login or local bootstrap. Identity expiry and\n      rotation remain Identity-owned. The end-to-end test includes the wait/poll shape a client uses\n      to learn the Connect Session completed.\n- [ ] The response shape is fixture-covered in `protocol` conformance, positive and adversarial\n      (expired session, session completed for a different integration, session already consumed), so\n      a future SDK test suite shares the fixtures verbatim.\n\n## Context\n\nWhen a granted invocation needs a connection that does not exist or has degraded, answer with a\n**structured next step** — a connect URL a human can complete, backed by a real connect session —\ninstead of an error an agent can only report. This is the pattern designed for an agent that hits the\nwall mid-task, and it is a client-contract promise in the vision, not a convenience.\n\nSource frontmatter: pillar Platform · areas [protocol, service, server]. **read**\n\nSource `note:` field, quoted: “research §5 pattern 8, called out there as 'the single most agent-native pattern found' (Arcade authorize→waitForCompletion, Pipedream MCP returning a connect URL, Composio Connect Links mid-conversation). Vision, client contract: not-connected is a next step, not an error”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-014-auth-as-tool-result.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-014-auth-as-tool-result.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-14 · 2 revision(s)\n- Legacy id `S-014`, recorded as the reference `legacy:S-014`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-014"}],"status":"draft","title":"Not-connected is a next step: the response carries a connect URL"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/auth-as-tool-result"},"payload":{"actor":"human:timo","at":1788480111414,"causation":"cmd-new-story-auth-as-tool-result","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:auth-as-tool-result@1#0~2051955aac6fbf14","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"auth-as-tool-result","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:carried-constraints"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000172","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788480111421,"causation":"cmd-story-auth-as-tool-result-derived_from-epic-carried-constraints","change":{"change":"related","relation":"derived_from","target":"epic:carried-constraints"},"correlation":"protocol-artifact-relate","event_id":"story:auth-as-tool-result@1#0~31244373a2d38e26","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"retire-the-quirks-umbrella","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-015-retire-the-quirks-umbrella.md:54`. **read**\n\n- [x] `pagination`, `rate_limit` and `error_envelope` are **first-class named fields** at their\n      correct scope in `catalog/connector-document.schema.json` and in the lowering — per operation\n      where they vary by endpoint, on the service where they are a property of the whole surface.\n      The scope choice per field is decided once and recorded, not left to each provider.\n- [x] **No `quirks` key remains anywhere**: not in the schema, not in any canonical document, not in\n      the pack, not in any projection (explorer, catalogue view, effective catalogue), not in a\n      provider declaration. Every committed document is regenerated — a whole-catalog change, so the\n      regeneration is coordinator-owned.\n- [x] `grep -ri quirk` across `catalog/` and `crates/` returns **only historical references in\n      documentation** (design series, story files, CHANGELOG). A hit in a type name, a field name, a\n      serde attribute or a test name fails the story.\n- [x] The genuinely deviant declarations are handled deliberately, in one of two ways, and the reason\n      is recorded: either a rare, precisely scoped **`workarounds`** category is introduced in which\n      **each entry names the specific vendor defect it compensates for** and carries the predecessor's\n      required attribution (`grant`/`behaviour`/`attribution`/`measured` — an unattributed claim is\n      indistinguishable from a guess that aged), or nothing is introduced at all and the babelforce\n      token-endpoint measurements find a home on the auth declaration itself. **Nothing generic is\n      introduced.** If it is not in the specification, it does not become a general thing.\n- [x] The migration is a rename with proof, not a redesign: the promoted fields' contents are\n      unchanged, and a test shows that for every provider, the set of declared pagination /\n      rate-limit / error-envelope facts before and after is identical. Any *behavioural* change to\n      those fields belongs to [S-005](S-005-header-name-rate-limit-retry.md), not here.\n- [x] Determinism and the fixed point hold after regeneration: two independent builds are\n      byte-identical, `catalog check` ([S-003](S-003-the-lockfile-gets-a-verifier.md)) is\n      green on the new lock, and the build reports nothing left to write.\n- [x] Failing-first test named — today the schema refuses a top-level `pagination` on an operation\n      and requires the umbrella.\n\n## Context\n\nDelete the `quirks` key and promote what it holds to first-class named fields on the operation and\nthe service, so the schema stops calling universal API traits *deviations*. Pagination, rate limits\nand error envelopes are how HTTP APIs work; filing them under a word that means \"this vendor is\nbroken\" mis-describes ~30 shipped providers and makes the one genuinely deviant case invisible\ninside the same bag.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “owner-raised: `quirks` began as a workaround bag for the broken babelforce API and drifted into a junk drawer holding universal API traits. Every other catalog in the field declares these as plain fields (Nango paginate/retry, Airbyte paginator/error_handler). Schema evolution — must run AFTER the M1 byte-identity differential passes, same wave as S-001/S-002”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-015-retire-the-quirks-umbrella.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-015-retire-the-quirks-umbrella.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-015`, recorded as the reference `legacy:S-015`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-015"}],"title":"Retire the `quirks` umbrella — pagination, rate limits and error envelopes are ordinary facts"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-015-retire-the-quirks-umbrella.md:54`. **read**\n\n- [x] `pagination`, `rate_limit` and `error_envelope` are **first-class named fields** at their\n      correct scope in `catalog/connector-document.schema.json` and in the lowering — per operation\n      where they vary by endpoint, on the service where they are a property of the whole surface.\n      The scope choice per field is decided once and recorded, not left to each provider.\n- [x] **No `quirks` key remains anywhere**: not in the schema, not in any canonical document, not in\n      the pack, not in any projection (explorer, catalogue view, effective catalogue), not in a\n      provider declaration. Every committed document is regenerated — a whole-catalog change, so the\n      regeneration is coordinator-owned.\n- [x] `grep -ri quirk` across `catalog/` and `crates/` returns **only historical references in\n      documentation** (design series, story files, CHANGELOG). A hit in a type name, a field name, a\n      serde attribute or a test name fails the story.\n- [x] The genuinely deviant declarations are handled deliberately, in one of two ways, and the reason\n      is recorded: either a rare, precisely scoped **`workarounds`** category is introduced in which\n      **each entry names the specific vendor defect it compensates for** and carries the predecessor's\n      required attribution (`grant`/`behaviour`/`attribution`/`measured` — an unattributed claim is\n      indistinguishable from a guess that aged), or nothing is introduced at all and the babelforce\n      token-endpoint measurements find a home on the auth declaration itself. **Nothing generic is\n      introduced.** If it is not in the specification, it does not become a general thing.\n- [x] The migration is a rename with proof, not a redesign: the promoted fields' contents are\n      unchanged, and a test shows that for every provider, the set of declared pagination /\n      rate-limit / error-envelope facts before and after is identical. Any *behavioural* change to\n      those fields belongs to [S-005](S-005-header-name-rate-limit-retry.md), not here.\n- [x] Determinism and the fixed point hold after regeneration: two independent builds are\n      byte-identical, `catalog check` ([S-003](S-003-the-lockfile-gets-a-verifier.md)) is\n      green on the new lock, and the build reports nothing left to write.\n- [x] Failing-first test named — today the schema refuses a top-level `pagination` on an operation\n      and requires the umbrella.\n\n## Context\n\nDelete the `quirks` key and promote what it holds to first-class named fields on the operation and\nthe service, so the schema stops calling universal API traits *deviations*. Pagination, rate limits\nand error envelopes are how HTTP APIs work; filing them under a word that means \"this vendor is\nbroken\" mis-describes ~30 shipped providers and makes the one genuinely deviant case invisible\ninside the same bag.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “owner-raised: `quirks` began as a workaround bag for the broken babelforce API and drifted into a junk drawer holding universal API traits. Every other catalog in the field declares these as plain fields (Nango paginate/retry, Airbyte paginator/error_handler). Schema evolution — must run AFTER the M1 byte-identity differential passes, same wave as S-001/S-002”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-015-retire-the-quirks-umbrella.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-015-retire-the-quirks-umbrella.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-015`, recorded as the reference `legacy:S-015`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-015"}],"status":"draft","title":"Retire the `quirks` umbrella — pagination, rate limits and error envelopes are ordinary facts"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/retire-the-quirks-umbrella"},"payload":{"actor":"human:timo","at":1788480111596,"causation":"cmd-new-story-retire-the-quirks-umbrella","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:retire-the-quirks-umbrella@1#0~a9b71469ec537bd1","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"retire-the-quirks-umbrella","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:catalog-day-one"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000177","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788480111604,"causation":"cmd-story-retire-the-quirks-umbrella-derived_from-epic-catalog-day-one","change":{"change":"related","relation":"derived_from","target":"epic:catalog-day-one"},"correlation":"protocol-artifact-relate","event_id":"story:retire-the-quirks-umbrella@1#0~143c59a65856dd0e","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"retire-the-quirks-umbrella","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000121","to":"proposed"},"payload":{"actor":"human:timo","at":1788480111746,"causation":"cmd-move-story-retire-the-quirks-umbrella-1788480111746","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:retire-the-quirks-umbrella@2#0~14e459ad45fdcf6e","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"retire-the-quirks-umbrella","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000121","to":"active"},"payload":{"actor":"human:timo","at":1788480111891,"causation":"cmd-move-story-retire-the-quirks-umbrella-1788480111891","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:retire-the-quirks-umbrella@3#0~81d15d911de1204a","executor":null,"recorded_at":"2026-09-04T00:01:51Z"}}
{"entity":"story","version":1,"id":"sources-are-processed-by-code","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-016-sources-are-processed-by-code.md:23`. **read**\n\n- The `[[source]]` entry schema is owned by code and refused-by-name on unknown fields: `id`,\n  `kind` (`openapi-spec | provider-catalog | reference-artifact | predecessor-import`),\n  `origin` (`vendor | repository-authored`), `upstream` (an exact fetchable location for vendor\n  bytes), `references` (exact authoritative documentation for repository-authored specs), the\n  refresh method (native, a declared legacy script under `scripts/`, or `authored-review`), the\n  declared scrub, coverage, pins (`sha256`, retrieval/review date, or a pointer to the per-vendor\n  detail record), and `consumers`.\n- `catalog sources check` — validates the index schema; verifies **every** recorded checksum\n  against the bytes on disk; detects orphans in both directions (an entry whose files are\n  missing, a vendored file no entry covers); exits non-zero on any drift. It runs inside the\n  catalog invariant suite, so a drifted pin or an unindexed source fails the build.\n- `catalog sources refresh <id> | --all` — for vendor origins, fetches per the entry (invoking the\n  declared legacy `scripts/vendor-*.*` where one exists), applies the declared scrub, recomputes\n  checksums, and rewrites the pins. For repository-authored origins, enters an authored-review\n  workflow that checks source coverage against the cited references and re-pins reviewed bytes;\n  it never implies that those bytes came from the vendor.\n- `catalog sources diff <id>` — for vendor origins, fetches upstream to a scratch location and\n  reports whether the pinned bytes drifted, **mutating nothing**. For authored origins, reports\n  reference and coverage changes that require review. This is the cheap probe that cadence\n  automation (`b10x-bot`) can run without changing the authored spec.\n- The source model records the projection from each spec and its reviewed overlays to the canonical\n  connector document; a reproducibility check rebuilds our format from those inputs and refuses\n  drift. A partial spec may serve an ingest test, but cannot claim full provider reproducibility.\n- Scale is a stated requirement: check/refresh/diff over hundreds of entries with zero\n  per-entry human action.\n- The AGENTS.md \"Refreshing a source\" instruction references only these verbs plus the\n  catalog-diff review; no manual fetch/compare step survives in any document.\n\n## Context\n\n`SOURCES.toml` — the single index of everything this repository derives from (official and\nrepository-authored specs, mined reference artifacts, the predecessor import) — is a\n**machine-processed manifest**.\nA `catalog sources` verb family owns it end to end: schema validation, checksum verification,\norphan detection, drift probing, and refresh execution. No human or agent ever fetches an\nupstream or compares a checksum by hand; the whole refresh discipline collapses to\n*refresh → build → review the canonical-document diff*.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, connector-spec] · priority 5. **read**\n\nSource `note:` field, quoted: “SOURCES.toml must never be prose + a manual runbook. At 8 ingest providers hand-comparison survives; at catalog scale (Nango: 957 providers) it cannot. Code owns the index.”\n\n## Status\n\n`ready` in the source. Quoted from `docs/stories/S-016-sources-are-processed-by-code.md:5`: `status: ready`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-016-sources-are-processed-by-code.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 5 revision(s)\n- Legacy id `S-016`, recorded as the reference `legacy:S-016`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-016"}],"title":"Sources are processed by code: the index is validated, checksummed and refreshed by the tool"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-016-sources-are-processed-by-code.md:23`. **read**\n\n- The `[[source]]` entry schema is owned by code and refused-by-name on unknown fields: `id`,\n  `kind` (`openapi-spec | provider-catalog | reference-artifact | predecessor-import`),\n  `origin` (`vendor | repository-authored`), `upstream` (an exact fetchable location for vendor\n  bytes), `references` (exact authoritative documentation for repository-authored specs), the\n  refresh method (native, a declared legacy script under `scripts/`, or `authored-review`), the\n  declared scrub, coverage, pins (`sha256`, retrieval/review date, or a pointer to the per-vendor\n  detail record), and `consumers`.\n- `catalog sources check` — validates the index schema; verifies **every** recorded checksum\n  against the bytes on disk; detects orphans in both directions (an entry whose files are\n  missing, a vendored file no entry covers); exits non-zero on any drift. It runs inside the\n  catalog invariant suite, so a drifted pin or an unindexed source fails the build.\n- `catalog sources refresh <id> | --all` — for vendor origins, fetches per the entry (invoking the\n  declared legacy `scripts/vendor-*.*` where one exists), applies the declared scrub, recomputes\n  checksums, and rewrites the pins. For repository-authored origins, enters an authored-review\n  workflow that checks source coverage against the cited references and re-pins reviewed bytes;\n  it never implies that those bytes came from the vendor.\n- `catalog sources diff <id>` — for vendor origins, fetches upstream to a scratch location and\n  reports whether the pinned bytes drifted, **mutating nothing**. For authored origins, reports\n  reference and coverage changes that require review. This is the cheap probe that cadence\n  automation (`b10x-bot`) can run without changing the authored spec.\n- The source model records the projection from each spec and its reviewed overlays to the canonical\n  connector document; a reproducibility check rebuilds our format from those inputs and refuses\n  drift. A partial spec may serve an ingest test, but cannot claim full provider reproducibility.\n- Scale is a stated requirement: check/refresh/diff over hundreds of entries with zero\n  per-entry human action.\n- The AGENTS.md \"Refreshing a source\" instruction references only these verbs plus the\n  catalog-diff review; no manual fetch/compare step survives in any document.\n\n## Context\n\n`SOURCES.toml` — the single index of everything this repository derives from (official and\nrepository-authored specs, mined reference artifacts, the predecessor import) — is a\n**machine-processed manifest**.\nA `catalog sources` verb family owns it end to end: schema validation, checksum verification,\norphan detection, drift probing, and refresh execution. No human or agent ever fetches an\nupstream or compares a checksum by hand; the whole refresh discipline collapses to\n*refresh → build → review the canonical-document diff*.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, connector-spec] · priority 5. **read**\n\nSource `note:` field, quoted: “SOURCES.toml must never be prose + a manual runbook. At 8 ingest providers hand-comparison survives; at catalog scale (Nango: 957 providers) it cannot. Code owns the index.”\n\n## Status\n\n`ready` in the source. Quoted from `docs/stories/S-016-sources-are-processed-by-code.md:5`: `status: ready`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-016-sources-are-processed-by-code.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 5 revision(s)\n- Legacy id `S-016`, recorded as the reference `legacy:S-016`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-016"}],"status":"draft","title":"Sources are processed by code: the index is validated, checksummed and refreshed by the tool"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/sources-are-processed-by-code"},"payload":{"actor":"human:timo","at":1788480112050,"causation":"cmd-new-story-sources-are-processed-by-code","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:sources-are-processed-by-code@1#0~bbaaa03ff3741514","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"sources-are-processed-by-code","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:sources"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000182","target":"01MEM0000000000000052"},"payload":{"actor":"human:timo","at":1788480112061,"causation":"cmd-story-sources-are-processed-by-code-derived_from-epic-sources","change":{"change":"related","relation":"derived_from","target":"epic:sources"},"correlation":"protocol-artifact-relate","event_id":"story:sources-are-processed-by-code@1#0~f0c88685f8df08e5","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"sources-are-processed-by-code","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000130","to":"proposed"},"payload":{"actor":"human:timo","at":1788480112206,"causation":"cmd-move-story-sources-are-processed-by-code-1788480112206","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:sources-are-processed-by-code@2#0~4ba1dbf3a4689da4","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"mint-source-entries-from-the-mined-catalogs","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-017-mint-source-entries-from-the-mined-catalogs.md:23`. **read**\n\n- `mint` reads **only** locally vendored reference artifacts (each itself a `SOURCES.toml`\n  entry with its own refresh) — no live network at mint time; the mining corpus stays fresh\n  through the ordinary refresh path, hermetic-build discipline preserved.\n- A spec directory is added to the vendored reference corpus (APIs.guru's machine-readable\n  index or equivalent) so spec-URL lookup is actually answerable, with provenance like every\n  other reference artifact.\n- Output goes to **stdout only** (the scaffold rule: mint never writes or overwrites a file),\n  with `TODO` holes for every judgment field.\n- **Per-field citation**: every proposed value names which reference artifact it came from.\n- **Disagreements are printed, never silently resolved** — e.g. Nango's `token_url` vs the\n  spec's `servers` entry vs Apideck's connector metadata; the human resolves, the tool reports.\n- A vendor found in no reference artifact yields an honest empty proposal naming what was\n  searched — not a guess. Model-memory answers remain fabrication (AGENTS.md rule).\n\n## Context\n\n`catalog sources mint <vendor>` turns the vendored competitor catalogs into a lookup instead\nof a hunt: it consults the locally vendored reference artifacts — Nango `providers.yaml`,\nAirbyte declarative connectors, Apideck's connector API, and a vendored spec directory\n(APIs.guru or equivalent) — and proposes a ready-to-review `[[source]]` entry plus the\nprovider-file reference skeleton for that vendor: the spec upstream URL, the docs URL, and the\nauth endpoints as cross-checks. It **proposes references to fetch, never content** — the actual\nbytes still arrive only through `sources refresh` + the declared scrub, so the\nsourced-never-authored rule holds by construction.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, docs]. **read**\n\nSource `note:` field, quoted: “builds on S-016. The vendored reference corpus is a source-reference database: Nango's providers.yaml carries docs URLs, auth endpoints and base URLs for ~950 vendors; a spec directory (APIs.guru) carries the actual OpenAPI locations.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-017-mint-source-entries-from-the-mined-catalogs.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-017-mint-source-entries-from-the-mined-catalogs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-017`, recorded as the reference `legacy:S-017`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-017"}],"title":"Mint source entries from the mined competitor catalogs"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-017-mint-source-entries-from-the-mined-catalogs.md:23`. **read**\n\n- `mint` reads **only** locally vendored reference artifacts (each itself a `SOURCES.toml`\n  entry with its own refresh) — no live network at mint time; the mining corpus stays fresh\n  through the ordinary refresh path, hermetic-build discipline preserved.\n- A spec directory is added to the vendored reference corpus (APIs.guru's machine-readable\n  index or equivalent) so spec-URL lookup is actually answerable, with provenance like every\n  other reference artifact.\n- Output goes to **stdout only** (the scaffold rule: mint never writes or overwrites a file),\n  with `TODO` holes for every judgment field.\n- **Per-field citation**: every proposed value names which reference artifact it came from.\n- **Disagreements are printed, never silently resolved** — e.g. Nango's `token_url` vs the\n  spec's `servers` entry vs Apideck's connector metadata; the human resolves, the tool reports.\n- A vendor found in no reference artifact yields an honest empty proposal naming what was\n  searched — not a guess. Model-memory answers remain fabrication (AGENTS.md rule).\n\n## Context\n\n`catalog sources mint <vendor>` turns the vendored competitor catalogs into a lookup instead\nof a hunt: it consults the locally vendored reference artifacts — Nango `providers.yaml`,\nAirbyte declarative connectors, Apideck's connector API, and a vendored spec directory\n(APIs.guru or equivalent) — and proposes a ready-to-review `[[source]]` entry plus the\nprovider-file reference skeleton for that vendor: the spec upstream URL, the docs URL, and the\nauth endpoints as cross-checks. It **proposes references to fetch, never content** — the actual\nbytes still arrive only through `sources refresh` + the declared scrub, so the\nsourced-never-authored rule holds by construction.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, docs]. **read**\n\nSource `note:` field, quoted: “builds on S-016. The vendored reference corpus is a source-reference database: Nango's providers.yaml carries docs URLs, auth endpoints and base URLs for ~950 vendors; a spec directory (APIs.guru) carries the actual OpenAPI locations.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-017-mint-source-entries-from-the-mined-catalogs.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-017-mint-source-entries-from-the-mined-catalogs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-017`, recorded as the reference `legacy:S-017`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-017"}],"status":"draft","title":"Mint source entries from the mined competitor catalogs"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/mint-source-entries-from-the-mined-catalogs"},"payload":{"actor":"human:timo","at":1788480112365,"causation":"cmd-new-story-mint-source-entries-from-the-mined-catalogs","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:mint-source-entries-from-the-mined-catalogs@1#0~d59184aa9edf5300","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"mint-source-entries-from-the-mined-catalogs","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:sources"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000187","target":"01MEM0000000000000052"},"payload":{"actor":"human:timo","at":1788480112374,"causation":"cmd-story-mint-source-entries-from-the-mined-catalogs-derived_from-epic-sources","change":{"change":"related","relation":"derived_from","target":"epic:sources"},"correlation":"protocol-artifact-relate","event_id":"story:mint-source-entries-from-the-mined-catalogs@1#0~acb9ac3451e406b0","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"the-explorer-works-against-the-new-site-json","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:21`. **read**\n\n- [x] The dormant `web/` application, its Node manifests, tests, schemas, and brand copies are\n      removed together.\n- [x] `catalog-build` no longer constructs or writes `web/public/catalog.json`; the site-only model,\n      renderer, status projection, and surface helpers are removed from the build graph.\n- [x] JSON governance, source ownership, ignore rules, architecture fences, and local gate guidance\n      no longer name the removed projection.\n- [x] Catalog browsing is served from the hosted, authenticated Catalog protocol and presented by\n      DevCenter, while canonical documents and `catalog.pack` remain the reviewed build artifacts.\n\n## Context\n\nSource frontmatter: pillar Catalog · areas [catalog-build] · priority 7. **read**\n\nSource `note:` field, quoted: “Resolved by retiring the dormant repository explorer and its duplicate site projection after the hosted catalog contract and DevCenter became the supported browsing path”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 4 revision(s)\n- Legacy id `S-018`, recorded as the reference `legacy:S-018`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-018"}],"title":"The web explorer works against the site JSON M1 actually emits"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:21`. **read**\n\n- [x] The dormant `web/` application, its Node manifests, tests, schemas, and brand copies are\n      removed together.\n- [x] `catalog-build` no longer constructs or writes `web/public/catalog.json`; the site-only model,\n      renderer, status projection, and surface helpers are removed from the build graph.\n- [x] JSON governance, source ownership, ignore rules, architecture fences, and local gate guidance\n      no longer name the removed projection.\n- [x] Catalog browsing is served from the hosted, authenticated Catalog protocol and presented by\n      DevCenter, while canonical documents and `catalog.pack` remain the reviewed build artifacts.\n\n## Context\n\nSource frontmatter: pillar Catalog · areas [catalog-build] · priority 7. **read**\n\nSource `note:` field, quoted: “Resolved by retiring the dormant repository explorer and its duplicate site projection after the hosted catalog contract and DevCenter became the supported browsing path”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 4 revision(s)\n- Legacy id `S-018`, recorded as the reference `legacy:S-018`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-018"}],"status":"draft","title":"The web explorer works against the site JSON M1 actually emits"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-explorer-works-against-the-new-site-json"},"payload":{"actor":"human:timo","at":1788480112545,"causation":"cmd-new-story-the-explorer-works-against-the-new-site-json","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-explorer-works-against-the-new-site-json@1#0~ce4e724c2627846e","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"the-explorer-works-against-the-new-site-json","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:post-m1"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000192","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788480112554,"causation":"cmd-story-the-explorer-works-against-the-new-site-json-derived_from-epic-post-m1","change":{"change":"related","relation":"derived_from","target":"epic:post-m1"},"correlation":"protocol-artifact-relate","event_id":"story:the-explorer-works-against-the-new-site-json@1#0~bdfd46940d2c4610","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"the-explorer-works-against-the-new-site-json","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000139","to":"proposed"},"payload":{"actor":"human:timo","at":1788480112695,"causation":"cmd-move-story-the-explorer-works-against-the-new-site-json-1788480112695","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-explorer-works-against-the-new-site-json@2#0~1d1eac76872af3c3","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"the-explorer-works-against-the-new-site-json","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000139","to":"active"},"payload":{"actor":"human:timo","at":1788480112840,"causation":"cmd-move-story-the-explorer-works-against-the-new-site-json-1788480112840","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-explorer-works-against-the-new-site-json@3#0~70663257a697c46b","executor":null,"recorded_at":"2026-09-04T00:01:52Z"}}
{"entity":"story","version":1,"id":"retire-the-flux-connectors-identity","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-019-retire-the-flux-connectors-identity.md:42`. **read**\n\n- [x] The generator identity names **this** repository, the workspace version is chosen deliberately\n      (a fresh line, or a continuation of 0.26.0 — either is defensible), and the `User-Agent` product\n      token matches both. All three move in **one commit** with **one** whole-catalogue regeneration:\n      55 documents, the pack digest, `connectors.lock`, `web/public/catalog.json`.\n- [x] The reasons are replaced, not deleted: `Cargo.toml`'s load-bearing comment and `seam.rs`'s\n      generator doc say what the version means **now** (and that the differential is retired), rather\n      than losing the paragraph that explained the old pin.\n- [x] `DEFAULT_USER_AGENT`'s three assertions in `request.rs` move with the value, so the constant\n      stays derived from the manifest (`CARGO_PKG_VERSION`, `CARGO_PKG_REPOSITORY`) and cannot go\n      stale at a release.\n- [x] The **whole identity inventory is enumerated and each item decided** — renamed now, or kept with\n      a written reason. Known members, measured post-M1:\n      the pack magic `flux-connectors-catalog-pack` (`crates/catalog-reader/src/lib.rs:70` — renaming\n      it is a pack-format break, so decide it here or state why it stays);\n      the `connectors.lock` header naming `flux-connectors check` (`connector-spec/src/lock.rs:74-75`);\n      the canonical document's `$schema` URL, still\n      `https://github.com/codewandler/flux-connectors/catalog/connector-document.schema.json`\n      (`catalog-build/src/document.rs:67`);\n      `web/package.json`'s `name` and `description`;\n      and the site's `PATH_RESOLVER = 'flux-connectors:path-resolver'` injection key.\n      A rename that leaves half the strings behind has not retired anything.\n- [x] After the rewrite the tree is a fixed point: `catalog build` writes nothing on a second run,\n      `catalog check` ([S-003](S-003-the-lockfile-gets-a-verifier.md)) is green on the new lock, and\n      the catalog invariants pass unchanged.\n- [x] `grep -ri flux-connectors` over `crates/`, `catalog/`, `web/` and the manifests returns only the\n      decided exceptions plus historical references in documentation (design series, stories,\n      predecessor citations) — history stays legible, artifacts do not.\n\n## Context\n\nMove this repository's artifacts onto their own identity — in **one** reviewed change whose diff is\nthe entire catalogue. The predecessor's name and version number are stamped into every canonical\ndocument, every lockfile row, the site's JSON and the wire's `User-Agent`; each is cheap alone and\neach rewrites all 55 documents and the pack digest, so doing them separately means paying that review\nfour times.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve, web]. **read**\n\nSource `note:` field, quoted: “M1 pinned the identity deliberately: the generator string `flux-connectors <version>` is an INPUT to the migration differential, so the workspace version is held at 0.26.0 and the name kept. The differential has passed; the pin's reason is spent. One story, one full-catalogue rewrite — generator + version + user agent together, not three”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-019-retire-the-flux-connectors-identity.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-019-retire-the-flux-connectors-identity.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 5 revision(s)\n- Legacy id `S-019`, recorded as the reference `legacy:S-019`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-019"}],"title":"Retire the flux-connectors identity from the artifacts"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-019-retire-the-flux-connectors-identity.md:42`. **read**\n\n- [x] The generator identity names **this** repository, the workspace version is chosen deliberately\n      (a fresh line, or a continuation of 0.26.0 — either is defensible), and the `User-Agent` product\n      token matches both. All three move in **one commit** with **one** whole-catalogue regeneration:\n      55 documents, the pack digest, `connectors.lock`, `web/public/catalog.json`.\n- [x] The reasons are replaced, not deleted: `Cargo.toml`'s load-bearing comment and `seam.rs`'s\n      generator doc say what the version means **now** (and that the differential is retired), rather\n      than losing the paragraph that explained the old pin.\n- [x] `DEFAULT_USER_AGENT`'s three assertions in `request.rs` move with the value, so the constant\n      stays derived from the manifest (`CARGO_PKG_VERSION`, `CARGO_PKG_REPOSITORY`) and cannot go\n      stale at a release.\n- [x] The **whole identity inventory is enumerated and each item decided** — renamed now, or kept with\n      a written reason. Known members, measured post-M1:\n      the pack magic `flux-connectors-catalog-pack` (`crates/catalog-reader/src/lib.rs:70` — renaming\n      it is a pack-format break, so decide it here or state why it stays);\n      the `connectors.lock` header naming `flux-connectors check` (`connector-spec/src/lock.rs:74-75`);\n      the canonical document's `$schema` URL, still\n      `https://github.com/codewandler/flux-connectors/catalog/connector-document.schema.json`\n      (`catalog-build/src/document.rs:67`);\n      `web/package.json`'s `name` and `description`;\n      and the site's `PATH_RESOLVER = 'flux-connectors:path-resolver'` injection key.\n      A rename that leaves half the strings behind has not retired anything.\n- [x] After the rewrite the tree is a fixed point: `catalog build` writes nothing on a second run,\n      `catalog check` ([S-003](S-003-the-lockfile-gets-a-verifier.md)) is green on the new lock, and\n      the catalog invariants pass unchanged.\n- [x] `grep -ri flux-connectors` over `crates/`, `catalog/`, `web/` and the manifests returns only the\n      decided exceptions plus historical references in documentation (design series, stories,\n      predecessor citations) — history stays legible, artifacts do not.\n\n## Context\n\nMove this repository's artifacts onto their own identity — in **one** reviewed change whose diff is\nthe entire catalogue. The predecessor's name and version number are stamped into every canonical\ndocument, every lockfile row, the site's JSON and the wire's `User-Agent`; each is cheap alone and\neach rewrites all 55 documents and the pack digest, so doing them separately means paying that review\nfour times.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve, web]. **read**\n\nSource `note:` field, quoted: “M1 pinned the identity deliberately: the generator string `flux-connectors <version>` is an INPUT to the migration differential, so the workspace version is held at 0.26.0 and the name kept. The differential has passed; the pin's reason is spent. One story, one full-catalogue rewrite — generator + version + user agent together, not three”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-019-retire-the-flux-connectors-identity.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-019-retire-the-flux-connectors-identity.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 5 revision(s)\n- Legacy id `S-019`, recorded as the reference `legacy:S-019`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-019"}],"status":"draft","title":"Retire the flux-connectors identity from the artifacts"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/retire-the-flux-connectors-identity"},"payload":{"actor":"human:timo","at":1788480113015,"causation":"cmd-new-story-retire-the-flux-connectors-identity","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:retire-the-flux-connectors-identity@1#0~9788113a429fe6d0","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"retire-the-flux-connectors-identity","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:post-m1"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000197","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788480113026,"causation":"cmd-story-retire-the-flux-connectors-identity-derived_from-epic-post-m1","change":{"change":"related","relation":"derived_from","target":"epic:post-m1"},"correlation":"protocol-artifact-relate","event_id":"story:retire-the-flux-connectors-identity@1#0~071e9d198e03b649","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"retire-the-flux-connectors-identity","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000124","to":"proposed"},"payload":{"actor":"human:timo","at":1788480113171,"causation":"cmd-move-story-retire-the-flux-connectors-identity-1788480113171","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:retire-the-flux-connectors-identity@2#0~ba695f6ff725a4e1","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"retire-the-flux-connectors-identity","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000124","to":"active"},"payload":{"actor":"human:timo","at":1788480113317,"causation":"cmd-move-story-retire-the-flux-connectors-identity-1788480113317","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:retire-the-flux-connectors-identity@3#0~588e35ea6f5bfe49","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"a-ci-gate-exists","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-020-a-ci-gate-exists.md:36`. **read**\n\n- [ ] A monorepo hosted workflow triggers on pull requests and pushes to the default branch and\n      invokes the root exhaustive gate without recreating a component-private workflow.\n- [x] The **catalogue** is gated, not just the code: read-only `catalog check` first verifies the\n      committed state; `catalog build` then has to leave no tracked Git diff and `catalog diff`\n      must report everything up to date ([S-003](S-003-the-lockfile-gets-a-verifier.md)). A drifted\n      artifact fails CI instead of reaching a reviewer, which is what makes review-equals-execution\n      mechanical.\n- [x] **`cargo fetch` runs before any job that invokes `cargo metadata --locked --offline`** (the MSRV\n      and no-network fences). The workflow states why in a comment, so the step is not later removed\n      as redundant.\n- [ ] A hosted job builds and tests the workspace **on the declared MSRV toolchain**, so `rust-version` stops\n      being an unchecked number. If the workspace does not build on 1.88, the declared MSRV moves to\n      the version that does — the fence's point is that the number is *true*, not that it is low —\n      and `msrv_fence.rs`'s \"there is no CI yet at all\" paragraph is corrected to describe the\n      coverage that now exists.\n- [x] AGENTS.md gains an explicit **gate** section naming the exact landed commands; story-index and\n      link checks are already mechanical.\n- [x] Toolchains are pinned by version and third-party actions by full commit SHA (with the release\n      tag in a comment); any in-workflow commit uses the\n      Actions `GITHUB_TOKEN` (`github-actions[bot]`), never the app key and never a PAT\n      (AGENTS.md § Automation identity).\n- [ ] **Failing-first:** seed one breach per gate arm — a formatting violation, a clippy warning, a\n      hand-edited canonical document — and record that the workflow fails on each. A gate never\n      observed failing is a gate nobody has tested.\n\n## Context\n\nGive the monorepo a mechanical hosted gate that builds, tests, lints, formats, rebuilds the catalogue\nand verifies it, on the declared MSRV as well as on stable — so \"green\" is a fact about a commit\nrather than a report about somebody's laptop. The local Rust/governance/catalogue gate exists;\nthe unchecked acceptance items below name the hosted, web, and verifier work that remains.\n\nSource frontmatter: pillar Catalog · areas [ci, catalog-build] · priority 6. **read**\n\nSource `note:` field, quoted: “The monorepo local gate covers governance, Rust, and catalogue checks; hosted CI, MSRV, and failing-first evidence remain before S-020 can close”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-020-a-ci-gate-exists.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-020-a-ci-gate-exists.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 11 revision(s)\n- Legacy id `S-020`, recorded as the reference `legacy:S-020`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-020"}],"title":"A CI gate exists, and it runs what the monorepo claims it runs"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-020-a-ci-gate-exists.md:36`. **read**\n\n- [ ] A monorepo hosted workflow triggers on pull requests and pushes to the default branch and\n      invokes the root exhaustive gate without recreating a component-private workflow.\n- [x] The **catalogue** is gated, not just the code: read-only `catalog check` first verifies the\n      committed state; `catalog build` then has to leave no tracked Git diff and `catalog diff`\n      must report everything up to date ([S-003](S-003-the-lockfile-gets-a-verifier.md)). A drifted\n      artifact fails CI instead of reaching a reviewer, which is what makes review-equals-execution\n      mechanical.\n- [x] **`cargo fetch` runs before any job that invokes `cargo metadata --locked --offline`** (the MSRV\n      and no-network fences). The workflow states why in a comment, so the step is not later removed\n      as redundant.\n- [ ] A hosted job builds and tests the workspace **on the declared MSRV toolchain**, so `rust-version` stops\n      being an unchecked number. If the workspace does not build on 1.88, the declared MSRV moves to\n      the version that does — the fence's point is that the number is *true*, not that it is low —\n      and `msrv_fence.rs`'s \"there is no CI yet at all\" paragraph is corrected to describe the\n      coverage that now exists.\n- [x] AGENTS.md gains an explicit **gate** section naming the exact landed commands; story-index and\n      link checks are already mechanical.\n- [x] Toolchains are pinned by version and third-party actions by full commit SHA (with the release\n      tag in a comment); any in-workflow commit uses the\n      Actions `GITHUB_TOKEN` (`github-actions[bot]`), never the app key and never a PAT\n      (AGENTS.md § Automation identity).\n- [ ] **Failing-first:** seed one breach per gate arm — a formatting violation, a clippy warning, a\n      hand-edited canonical document — and record that the workflow fails on each. A gate never\n      observed failing is a gate nobody has tested.\n\n## Context\n\nGive the monorepo a mechanical hosted gate that builds, tests, lints, formats, rebuilds the catalogue\nand verifies it, on the declared MSRV as well as on stable — so \"green\" is a fact about a commit\nrather than a report about somebody's laptop. The local Rust/governance/catalogue gate exists;\nthe unchecked acceptance items below name the hosted, web, and verifier work that remains.\n\nSource frontmatter: pillar Catalog · areas [ci, catalog-build] · priority 6. **read**\n\nSource `note:` field, quoted: “The monorepo local gate covers governance, Rust, and catalogue checks; hosted CI, MSRV, and failing-first evidence remain before S-020 can close”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-020-a-ci-gate-exists.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-020-a-ci-gate-exists.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 11 revision(s)\n- Legacy id `S-020`, recorded as the reference `legacy:S-020`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-020"}],"status":"draft","title":"A CI gate exists, and it runs what the monorepo claims it runs"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-ci-gate-exists"},"payload":{"actor":"human:timo","at":1788480113492,"causation":"cmd-new-story-a-ci-gate-exists","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-ci-gate-exists@1#0~6fbef3920d5034db","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"a-ci-gate-exists","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:post-m1"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000202","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788480113500,"causation":"cmd-story-a-ci-gate-exists-derived_from-epic-post-m1","change":{"change":"related","relation":"derived_from","target":"epic:post-m1"},"correlation":"protocol-artifact-relate","event_id":"story:a-ci-gate-exists@1#0~1b3573e19271bb7c","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"a-ci-gate-exists","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000061","to":"proposed"},"payload":{"actor":"human:timo","at":1788480113644,"causation":"cmd-move-story-a-ci-gate-exists-1788480113644","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-ci-gate-exists@2#0~cf30e604c830bf45","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"a-ci-gate-exists","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000061","to":"active"},"payload":{"actor":"human:timo","at":1788480113790,"causation":"cmd-move-story-a-ci-gate-exists-1788480113790","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-ci-gate-exists@3#0~ac5ae5261d286325","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"coverage-regains-its-second-direction","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-021-coverage-regains-its-second-direction.md:40`. **read**\n\n- [ ] For every spec-backed provider the three numbers are **derived at test time** — declared by the\n      vendored documents, published by the catalogue, and the difference — and the difference is\n      **fully accounted for**: every operation in it is covered by a written reason. No free-floating\n      remainder, and no \"the rest are out of scope\" bucket.\n- [ ] Reasons are **authored where the connector is reviewed** (the provider declaration's existing\n      `defer` shape is the precedent), not constants in a test file — so declining an endpoint is a\n      reviewed decision in the same diff as the connector, and the reason text is prose a reader can\n      judge.\n- [ ] Counts are never hand-typed. If any number is committed, the test recomputes it from the bytes\n      on disk and fails on drift (C-81's standing lesson: hand-maintained numbers are a defect class,\n      and this repository already ships `plan.providers.len() >= 55` as the shape of the guard).\n- [ ] **An entry may not outlive the gap it explains** — a reason naming an operation that is now\n      published, or one no vendored document declares any more, fails by name. This is the half the\n      predecessor's allowlists had and the current invariant lost, and it is what makes a spec refresh\n      surface vanished operations instead of silently dropping them (AGENTS.md § Refreshing a source,\n      step 3).\n- [ ] The rule is stated **once and parameterised** over the catalogue, inside the consolidated\n      invariants file. No per-provider test file returns — the boundary AGENTS.md draws is not\n      relaxed by this story.\n- [ ] **Failing-first:** remove one published operation's selector from a provider and the test names\n      it as an unexplained gap; today nothing fails. Then add a reason and watch it pass, and delete\n      the operation from the vendored document and watch the reason fail as stale.\n- [ ] The whole-catalogue guard survives: the test still refuses to pass on a catalogue too small to\n      mean anything (the existing `spec_backed >= 7` shape).\n\n## Context\n\nRestore the coverage direction the consolidation lost — *every operation a vendored document declares\nand this catalogue does not publish has a written reason* — and restore it **as data**, iterated over\nthe whole catalogue, so it never becomes a per-provider test file again.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, providers]. **read**\n\nSource `note:` field, quoted: “M1 folded sixteen per-provider test files into one parameterised invariants file — right, and it dropped a direction: the per-vendor declared-operation count with a written reason per gap (babelforce's 389 + 5 + 3 = 397, checked both ways). Today an endpoint no patch mentions is neither invented nor a failed selector, so nothing fails and nothing explains it”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-021-coverage-regains-its-second-direction.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-021-coverage-regains-its-second-direction.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-021`, recorded as the reference `legacy:S-021`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-021"}],"title":"Coverage regains its second direction: every gap between declared and published has a reason"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-021-coverage-regains-its-second-direction.md:40`. **read**\n\n- [ ] For every spec-backed provider the three numbers are **derived at test time** — declared by the\n      vendored documents, published by the catalogue, and the difference — and the difference is\n      **fully accounted for**: every operation in it is covered by a written reason. No free-floating\n      remainder, and no \"the rest are out of scope\" bucket.\n- [ ] Reasons are **authored where the connector is reviewed** (the provider declaration's existing\n      `defer` shape is the precedent), not constants in a test file — so declining an endpoint is a\n      reviewed decision in the same diff as the connector, and the reason text is prose a reader can\n      judge.\n- [ ] Counts are never hand-typed. If any number is committed, the test recomputes it from the bytes\n      on disk and fails on drift (C-81's standing lesson: hand-maintained numbers are a defect class,\n      and this repository already ships `plan.providers.len() >= 55` as the shape of the guard).\n- [ ] **An entry may not outlive the gap it explains** — a reason naming an operation that is now\n      published, or one no vendored document declares any more, fails by name. This is the half the\n      predecessor's allowlists had and the current invariant lost, and it is what makes a spec refresh\n      surface vanished operations instead of silently dropping them (AGENTS.md § Refreshing a source,\n      step 3).\n- [ ] The rule is stated **once and parameterised** over the catalogue, inside the consolidated\n      invariants file. No per-provider test file returns — the boundary AGENTS.md draws is not\n      relaxed by this story.\n- [ ] **Failing-first:** remove one published operation's selector from a provider and the test names\n      it as an unexplained gap; today nothing fails. Then add a reason and watch it pass, and delete\n      the operation from the vendored document and watch the reason fail as stale.\n- [ ] The whole-catalogue guard survives: the test still refuses to pass on a catalogue too small to\n      mean anything (the existing `spec_backed >= 7` shape).\n\n## Context\n\nRestore the coverage direction the consolidation lost — *every operation a vendored document declares\nand this catalogue does not publish has a written reason* — and restore it **as data**, iterated over\nthe whole catalogue, so it never becomes a per-provider test file again.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, providers]. **read**\n\nSource `note:` field, quoted: “M1 folded sixteen per-provider test files into one parameterised invariants file — right, and it dropped a direction: the per-vendor declared-operation count with a written reason per gap (babelforce's 389 + 5 + 3 = 397, checked both ways). Today an endpoint no patch mentions is neither invented nor a failed selector, so nothing fails and nothing explains it”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-021-coverage-regains-its-second-direction.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-021-coverage-regains-its-second-direction.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-021`, recorded as the reference `legacy:S-021`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-021"}],"status":"draft","title":"Coverage regains its second direction: every gap between declared and published has a reason"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/coverage-regains-its-second-direction"},"payload":{"actor":"human:timo","at":1788480113966,"causation":"cmd-new-story-coverage-regains-its-second-direction","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:coverage-regains-its-second-direction@1#0~29e95c5be1f4111a","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"coverage-regains-its-second-direction","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:post-m1"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000207","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788480113974,"causation":"cmd-story-coverage-regains-its-second-direction-derived_from-epic-post-m1","change":{"change":"related","relation":"derived_from","target":"epic:post-m1"},"correlation":"protocol-artifact-relate","event_id":"story:coverage-regains-its-second-direction@1#0~a5678cbd26a1c58d","executor":null,"recorded_at":"2026-09-04T00:01:53Z"}}
{"entity":"story","version":1,"id":"orphaned-inputs-are-removed-or-re-owned","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:37`. **read**\n\n- [x] **anthropic** remains under `specs/` and covered by the OpenAPI 3.1/YAML ingest tests. Its\n      provenance says `origin = \"repository-authored\"`, pins its bytes, records the predecessor\n      authorship and exact official grounding references, and states `coverage =\n      \"partial-ingest-fixture\"`; it cannot be mistaken for vendor-published bytes.\n- [x] **anthropic's full connector** is reproducible from a sufficiently complete authored spec plus\n      reviewed overlays into the canonical connector document. Until then,\n      `providers/anthropic.toml` is named migration debt and the partial excerpt is not allowed to\n      claim provider-level coverage.\n- [x] **`specs/flux/core-v1.json`** is deleted together with every claim that something is emitted\n      from it (`web/README.md`'s `public/v1/` paragraph), or a named consumer is filed as its own\n      story before it stays. A vendored input with no consumer is a file that still validates and\n      describes nothing.\n- [x] **`migration/`** is decided against its live successor: the wave/fixture rule restated by\n      [S-010](S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md) is what actually governs\n      plugin retirement now. Either the ratchet's records move under that story's evidence with an\n      owner and a checker, or the directory goes. It does not stay as an unchecked schema plus two\n      fixtures.\n- [x] `SOURCES.toml`'s \"known defects\" header shrinks by exactly the entries closed, and the\n      predecessor-import entry's `consumers` list stops naming paths that no longer exist. The index\n      never describes a tree that is not there.\n- [x] After the sweep, orphan checking passes **in both directions** by construction: every file under\n      `specs/` is covered by a `[[source]]` entry, and every entry's files exist. Landing this before\n      S-016 means that check's first run is a green one; landing it after means S-016 ships with three\n      declared exceptions, which is the state this story exists to prevent.\n- [x] Each disposition is its own reviewable commit with the reason in the body — a deleted 157 KB\n      vendored document, a re-owned authored spec and a deleted migration ratchet are decisions,\n      not cleanup noise.\n\n## Context\n\nClose the three orphans the M1 import left behind, so every file this repository derives from has a\nconsumer, a refresh path and a provenance record — and so\n[S-016](S-016-sources-are-processed-by-code.md)'s `catalog sources check` can arrive **green** rather\nthan arriving with three permanent exceptions.\n\nSource frontmatter: pillar Catalog · areas [specs, migration, web]. **read**\n\nSource `note:` field, quoted: “three M1 classification defects: the Flux core input and migration ratchet had lost their consumers; the Anthropic YAML was a live repository-authored source spec whose origin and deliberately partial coverage were not recorded”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 7 revision(s)\n- Legacy id `S-022`, recorded as the reference `legacy:S-022`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-022"}],"title":"Orphaned inputs are removed or re-owned"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:37`. **read**\n\n- [x] **anthropic** remains under `specs/` and covered by the OpenAPI 3.1/YAML ingest tests. Its\n      provenance says `origin = \"repository-authored\"`, pins its bytes, records the predecessor\n      authorship and exact official grounding references, and states `coverage =\n      \"partial-ingest-fixture\"`; it cannot be mistaken for vendor-published bytes.\n- [x] **anthropic's full connector** is reproducible from a sufficiently complete authored spec plus\n      reviewed overlays into the canonical connector document. Until then,\n      `providers/anthropic.toml` is named migration debt and the partial excerpt is not allowed to\n      claim provider-level coverage.\n- [x] **`specs/flux/core-v1.json`** is deleted together with every claim that something is emitted\n      from it (`web/README.md`'s `public/v1/` paragraph), or a named consumer is filed as its own\n      story before it stays. A vendored input with no consumer is a file that still validates and\n      describes nothing.\n- [x] **`migration/`** is decided against its live successor: the wave/fixture rule restated by\n      [S-010](S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md) is what actually governs\n      plugin retirement now. Either the ratchet's records move under that story's evidence with an\n      owner and a checker, or the directory goes. It does not stay as an unchecked schema plus two\n      fixtures.\n- [x] `SOURCES.toml`'s \"known defects\" header shrinks by exactly the entries closed, and the\n      predecessor-import entry's `consumers` list stops naming paths that no longer exist. The index\n      never describes a tree that is not there.\n- [x] After the sweep, orphan checking passes **in both directions** by construction: every file under\n      `specs/` is covered by a `[[source]]` entry, and every entry's files exist. Landing this before\n      S-016 means that check's first run is a green one; landing it after means S-016 ships with three\n      declared exceptions, which is the state this story exists to prevent.\n- [x] Each disposition is its own reviewable commit with the reason in the body — a deleted 157 KB\n      vendored document, a re-owned authored spec and a deleted migration ratchet are decisions,\n      not cleanup noise.\n\n## Context\n\nClose the three orphans the M1 import left behind, so every file this repository derives from has a\nconsumer, a refresh path and a provenance record — and so\n[S-016](S-016-sources-are-processed-by-code.md)'s `catalog sources check` can arrive **green** rather\nthan arriving with three permanent exceptions.\n\nSource frontmatter: pillar Catalog · areas [specs, migration, web]. **read**\n\nSource `note:` field, quoted: “three M1 classification defects: the Flux core input and migration ratchet had lost their consumers; the Anthropic YAML was a live repository-authored source spec whose origin and deliberately partial coverage were not recorded”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 7 revision(s)\n- Legacy id `S-022`, recorded as the reference `legacy:S-022`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-022"}],"status":"draft","title":"Orphaned inputs are removed or re-owned"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/orphaned-inputs-are-removed-or-re-owned"},"payload":{"actor":"human:timo","at":1788480114149,"causation":"cmd-new-story-orphaned-inputs-are-removed-or-re-owned","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:orphaned-inputs-are-removed-or-re-owned@1#0~42004886d4a84097","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"orphaned-inputs-are-removed-or-re-owned","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:post-m1"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000212","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788480114159,"causation":"cmd-story-orphaned-inputs-are-removed-or-re-owned-derived_from-epic-post-m1","change":{"change":"related","relation":"derived_from","target":"epic:post-m1"},"correlation":"protocol-artifact-relate","event_id":"story:orphaned-inputs-are-removed-or-re-owned@1#0~09137e97feee0ed7","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"orphaned-inputs-are-removed-or-re-owned","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000121","to":"proposed"},"payload":{"actor":"human:timo","at":1788480114303,"causation":"cmd-move-story-orphaned-inputs-are-removed-or-re-owned-1788480114303","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:orphaned-inputs-are-removed-or-re-owned@2#0~14e459ad45fdcf6e","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"orphaned-inputs-are-removed-or-re-owned","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000121","to":"active"},"payload":{"actor":"human:timo","at":1788480114448,"causation":"cmd-move-story-orphaned-inputs-are-removed-or-re-owned-1788480114448","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:orphaned-inputs-are-removed-or-re-owned@3#0~81d15d911de1204a","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"beyond-http-facts-enter-the-document","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:20`. **read**\n\n- [x] The schema and IR type each axis independently and reject unknown driver/capability values.\n- [x] Ordinary HTTP documents lower byte-identically unless a reviewed migration says otherwise.\n- [x] Exposure to models remains distinct from operation direction and direct-byte interaction.\n- [x] Catalog build, reader, resolver, lock, site projection, and fixtures change in the single\n      coordinated schema wave.\n- [x] The substrate first-party translation has an explicit mapping/overlay fixture; no field is\n      called mechanically identical when its vocabulary differs.\n\n## Context\n\nRepresent interaction shape, closed protocol driver, placement requirements, implementation form,\nand capability predicates as independent document facts without restoring a generic runtime enum.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec, connector-resolve] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0010 delivery item 1; this is schema-wave work and never runs in parallel with another document-schema author”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-023-beyond-http-facts-enter-the-document.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-023`, recorded as the reference `legacy:S-023`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-023"}],"title":"Beyond-HTTP facts enter the connector document orthogonally"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:20`. **read**\n\n- [x] The schema and IR type each axis independently and reject unknown driver/capability values.\n- [x] Ordinary HTTP documents lower byte-identically unless a reviewed migration says otherwise.\n- [x] Exposure to models remains distinct from operation direction and direct-byte interaction.\n- [x] Catalog build, reader, resolver, lock, site projection, and fixtures change in the single\n      coordinated schema wave.\n- [x] The substrate first-party translation has an explicit mapping/overlay fixture; no field is\n      called mechanically identical when its vocabulary differs.\n\n## Context\n\nRepresent interaction shape, closed protocol driver, placement requirements, implementation form,\nand capability predicates as independent document facts without restoring a generic runtime enum.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec, connector-resolve] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0010 delivery item 1; this is schema-wave work and never runs in parallel with another document-schema author”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-023-beyond-http-facts-enter-the-document.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-023`, recorded as the reference `legacy:S-023`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-023"}],"status":"draft","title":"Beyond-HTTP facts enter the connector document orthogonally"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/beyond-http-facts-enter-the-document"},"payload":{"actor":"human:timo","at":1788480114610,"causation":"cmd-new-story-beyond-http-facts-enter-the-document","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:beyond-http-facts-enter-the-document@1#0~2aa54d52414abfcf","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"beyond-http-facts-enter-the-document","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:beyond-http"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000217","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788480114618,"causation":"cmd-story-beyond-http-facts-enter-the-document-derived_from-epic-beyond-http","change":{"change":"related","relation":"derived_from","target":"epic:beyond-http"},"correlation":"protocol-artifact-relate","event_id":"story:beyond-http-facts-enter-the-document@1#0~afcdca2fd933868c","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"beyond-http-facts-enter-the-document","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"proposed"},"payload":{"actor":"human:timo","at":1788480114818,"causation":"cmd-move-story-beyond-http-facts-enter-the-document-1788480114818","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:beyond-http-facts-enter-the-document@2#0~0710c15da85fdf8f","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"beyond-http-facts-enter-the-document","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"active"},"payload":{"actor":"human:timo","at":1788480114980,"causation":"cmd-move-story-beyond-http-facts-enter-the-document-1788480114980","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:beyond-http-facts-enter-the-document@3#0~6ea9aeb43e6711bf","executor":null,"recorded_at":"2026-09-04T00:01:54Z"}}
{"entity":"story","version":1,"id":"one-zero-io-plan-seam-selects-a-closed-driver","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-024-one-zero-io-plan-seam-selects-a-closed-driver.md:20`. **read**\n\n- [x] A typed zero-IO plan names one closed driver and its reviewed operation/channel facts.\n- [x] Grant admission and permission subjects are fixed before credential placement.\n- [x] Exactly one dispatch composition point applies egress, credential, redaction, and audit\n      policy before handing a driver its bounded plan.\n- [x] Unknown drivers and unmet capabilities refuse by name; there is no process/plugin fallback.\n- [ ] Fence tests fail if a second policy-composition or vendor-dial path appears.\n\n## Context\n\nExtend request planning beyond HTTP without letting planning perform IO or letting each driver\nrecompose grants, credentials, egress, redaction, and audit independently.\n\nSource frontmatter: pillar Platform · areas [domain, service, server, connector-resolve] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0010 delivery item 1; planning remains data and dispatch has one composition point”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-024-one-zero-io-plan-seam-selects-a-closed-driver.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-024-one-zero-io-plan-seam-selects-a-closed-driver.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 3 revision(s)\n- Legacy id `S-024`, recorded as the reference `legacy:S-024`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-024"}],"title":"One zero-IO plan seam selects a closed built-in driver"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-024-one-zero-io-plan-seam-selects-a-closed-driver.md:20`. **read**\n\n- [x] A typed zero-IO plan names one closed driver and its reviewed operation/channel facts.\n- [x] Grant admission and permission subjects are fixed before credential placement.\n- [x] Exactly one dispatch composition point applies egress, credential, redaction, and audit\n      policy before handing a driver its bounded plan.\n- [x] Unknown drivers and unmet capabilities refuse by name; there is no process/plugin fallback.\n- [ ] Fence tests fail if a second policy-composition or vendor-dial path appears.\n\n## Context\n\nExtend request planning beyond HTTP without letting planning perform IO or letting each driver\nrecompose grants, credentials, egress, redaction, and audit independently.\n\nSource frontmatter: pillar Platform · areas [domain, service, server, connector-resolve] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0010 delivery item 1; planning remains data and dispatch has one composition point”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-024-one-zero-io-plan-seam-selects-a-closed-driver.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-024-one-zero-io-plan-seam-selects-a-closed-driver.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 3 revision(s)\n- Legacy id `S-024`, recorded as the reference `legacy:S-024`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-024"}],"status":"draft","title":"One zero-IO plan seam selects a closed built-in driver"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/one-zero-io-plan-seam-selects-a-closed-driver"},"payload":{"actor":"human:timo","at":1788480115142,"causation":"cmd-new-story-one-zero-io-plan-seam-selects-a-closed-driver","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:one-zero-io-plan-seam-selects-a-closed-driver@1#0~74c8e7875f102357","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"one-zero-io-plan-seam-selects-a-closed-driver","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:beyond-http"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000222","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788480115152,"causation":"cmd-story-one-zero-io-plan-seam-selects-a-closed-driver-derived_from-epic-beyond-http","change":{"change":"related","relation":"derived_from","target":"epic:beyond-http"},"correlation":"protocol-artifact-relate","event_id":"story:one-zero-io-plan-seam-selects-a-closed-driver@1#0~a0ee42e0fde69b5c","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"one-zero-io-plan-seam-selects-a-closed-driver","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000124","to":"proposed"},"payload":{"actor":"human:timo","at":1788480115301,"causation":"cmd-move-story-one-zero-io-plan-seam-selects-a-closed-driver-1788480115301","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:one-zero-io-plan-seam-selects-a-closed-driver@2#0~ba695f6ff725a4e1","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"one-zero-io-plan-seam-selects-a-closed-driver","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000124","to":"active"},"payload":{"actor":"human:timo","at":1788480115449,"causation":"cmd-move-story-one-zero-io-plan-seam-selects-a-closed-driver-1788480115449","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:one-zero-io-plan-seam-selects-a-closed-driver@3#0~588e35ea6f5bfe49","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"satellite-federation-has-a-threat-modeled-contract","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-025-satellite-federation-has-a-threat-modeled-contract.md:20`. **read**\n\n- [x] Architecture RFCs 0001 and 0004 are accepted by ADRs 0015 and 0018 and cited by the protocol.\n- [ ] Outward-only bootstrap, deployment identity, tenant binding, monotonic signed catalog/policy\n      generations, downgrade refusal, and revocation are conformance-tested.\n- [ ] Partition and split-brain behavior is typed; mutation with stale authority refuses by default.\n- [ ] Event ordering/gap recovery composes with S-029 and byte routing does not turn control\n      federation into a generic reverse tunnel.\n- [ ] Compromise is bounded to enrolled destinations, credentials, grants, and one deployment.\n\n## Context\n\nMake satellite a contained connectors deployment role with explicit bootstrap, catalog generation,\ntenant, revocation, partition, replay, and recovery behavior.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by ADRs 0015 and 0018; implementation/conformance remains”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-025-satellite-federation-has-a-threat-modeled-contract.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-025-satellite-federation-has-a-threat-modeled-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-025`, recorded as the reference `legacy:S-025`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-025"}],"title":"Satellite federation has a threat-modeled contract"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-025-satellite-federation-has-a-threat-modeled-contract.md:20`. **read**\n\n- [x] Architecture RFCs 0001 and 0004 are accepted by ADRs 0015 and 0018 and cited by the protocol.\n- [ ] Outward-only bootstrap, deployment identity, tenant binding, monotonic signed catalog/policy\n      generations, downgrade refusal, and revocation are conformance-tested.\n- [ ] Partition and split-brain behavior is typed; mutation with stale authority refuses by default.\n- [ ] Event ordering/gap recovery composes with S-029 and byte routing does not turn control\n      federation into a generic reverse tunnel.\n- [ ] Compromise is bounded to enrolled destinations, credentials, grants, and one deployment.\n\n## Context\n\nMake satellite a contained connectors deployment role with explicit bootstrap, catalog generation,\ntenant, revocation, partition, replay, and recovery behavior.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by ADRs 0015 and 0018; implementation/conformance remains”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-025-satellite-federation-has-a-threat-modeled-contract.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-025-satellite-federation-has-a-threat-modeled-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-025`, recorded as the reference `legacy:S-025`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-025"}],"status":"draft","title":"Satellite federation has a threat-modeled contract"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/satellite-federation-has-a-threat-modeled-contract"},"payload":{"actor":"human:timo","at":1788480115609,"causation":"cmd-new-story-satellite-federation-has-a-threat-modeled-contract","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:satellite-federation-has-a-threat-modeled-contract@1#0~c72060ccd501740c","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"satellite-federation-has-a-threat-modeled-contract","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:beyond-http"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000227","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788480115618,"causation":"cmd-story-satellite-federation-has-a-threat-modeled-contract-derived_from-epic-beyond-http","change":{"change":"related","relation":"derived_from","target":"epic:beyond-http"},"correlation":"protocol-artifact-relate","event_id":"story:satellite-federation-has-a-threat-modeled-contract@1#0~225ad30f11498f49","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"one-real-non-http-driver-proves-the-model","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-026-one-real-non-http-driver-proves-the-model.md:21`. **read**\n\n- [ ] One SIP provider declares session establishment, closed call events, credentials,\n      risk/effects, driver, placement-independent shape, and capability requirements as reviewed\n      data.\n- [ ] The built-in driver consumes the common zero-IO plan and shared egress/audit composition.\n- [ ] Authentication failure, protocol refusal, reconnect, event provenance, bounded buffering,\n      call cancellation, and unsupported capability cases have fixtures.\n- [x] No caller chooses an executable, arbitrary protocol string, credential destination, or\n      placement.\n- [x] The proof records which abstraction pressure is real before any second driver is planned.\n\n## Context\n\nProve the abstraction against one real built-in protocol: SIP through the bounded `sip_v1` driver,\nwithout a generic framing language or external runtime artifact. S-032 owns the concrete slice;\nthis story retains the abstraction-level acceptance.\n\nSource frontmatter: pillar Platform · areas [catalog, domain, service, server] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “B10x sip.dial is source-grounded and development-proven; S-032 retains the stable-support matrix”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-026-one-real-non-http-driver-proves-the-model.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-026-one-real-non-http-driver-proves-the-model.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-16 · 5 revision(s)\n- Legacy id `S-026`, recorded as the reference `legacy:S-026`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-026"}],"title":"One real non-HTTP driver proves the five-axis model"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-026-one-real-non-http-driver-proves-the-model.md:21`. **read**\n\n- [ ] One SIP provider declares session establishment, closed call events, credentials,\n      risk/effects, driver, placement-independent shape, and capability requirements as reviewed\n      data.\n- [ ] The built-in driver consumes the common zero-IO plan and shared egress/audit composition.\n- [ ] Authentication failure, protocol refusal, reconnect, event provenance, bounded buffering,\n      call cancellation, and unsupported capability cases have fixtures.\n- [x] No caller chooses an executable, arbitrary protocol string, credential destination, or\n      placement.\n- [x] The proof records which abstraction pressure is real before any second driver is planned.\n\n## Context\n\nProve the abstraction against one real built-in protocol: SIP through the bounded `sip_v1` driver,\nwithout a generic framing language or external runtime artifact. S-032 owns the concrete slice;\nthis story retains the abstraction-level acceptance.\n\nSource frontmatter: pillar Platform · areas [catalog, domain, service, server] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “B10x sip.dial is source-grounded and development-proven; S-032 retains the stable-support matrix”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-026-one-real-non-http-driver-proves-the-model.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-026-one-real-non-http-driver-proves-the-model.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-16 · 5 revision(s)\n- Legacy id `S-026`, recorded as the reference `legacy:S-026`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-026"}],"status":"draft","title":"One real non-HTTP driver proves the five-axis model"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/one-real-non-http-driver-proves-the-model"},"payload":{"actor":"human:timo","at":1788480115776,"causation":"cmd-new-story-one-real-non-http-driver-proves-the-model","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:one-real-non-http-driver-proves-the-model@1#0~7d1f43930d688a06","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"one-real-non-http-driver-proves-the-model","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:beyond-http"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000232","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788480115786,"causation":"cmd-story-one-real-non-http-driver-proves-the-model-derived_from-epic-beyond-http","change":{"change":"related","relation":"derived_from","target":"epic:beyond-http"},"correlation":"protocol-artifact-relate","event_id":"story:one-real-non-http-driver-proves-the-model@1#0~629d965fcb1c76af","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"one-real-non-http-driver-proves-the-model","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000124","to":"proposed"},"payload":{"actor":"human:timo","at":1788480115936,"causation":"cmd-move-story-one-real-non-http-driver-proves-the-model-1788480115936","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:one-real-non-http-driver-proves-the-model@2#0~ba695f6ff725a4e1","executor":null,"recorded_at":"2026-09-04T00:01:55Z"}}
{"entity":"story","version":1,"id":"one-real-non-http-driver-proves-the-model","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000124","to":"active"},"payload":{"actor":"human:timo","at":1788480116086,"causation":"cmd-move-story-one-real-non-http-driver-proves-the-model-1788480116086","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:one-real-non-http-driver-proves-the-model@3#0~588e35ea6f5bfe49","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"direct-byte-session-establishment-is-operation-scoped","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-027-direct-byte-session-establishment-is-operation-scoped.md:20`. **read**\n\n- [x] Architecture RFC 0002 is accepted by ADR 0016 with issuer/verifier, audience/resource/principal binding,\n      TTL, replay, reconnect, revocation, proof-of-possession, TLS, and NAT semantics.\n- [ ] Connectors audits grant admission and authority issuance without receiving continuous bytes.\n- [ ] The serving endpoint independently enforces local ownership, limits, and capability facts.\n- [ ] A private satellite initiates RTVBP outward; an unavailable direct route is `unserved`, and\n      the federation control path cannot relay bytes.\n- [ ] One call remains on one endpoint generation until hangup or bounded drain; it is not migrated\n      mid-dialog.\n- [ ] Authorities are absent from logs, ordinary events, and client-visible long-lived state.\n- [ ] Wrong audience/resource/principal, expiry, replay, and revoked deployment all fail fixtures.\n\n## Context\n\nGovern the SIP-to-RTVBP media establishment while the selected voice and application endpoints\nexchange continuous bytes directly under a short-lived bounded authority.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0016 fixes authority; ADR 0024 selects the SIP-to-RTVBP voice journey”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-027-direct-byte-session-establishment-is-operation-scoped.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-027-direct-byte-session-establishment-is-operation-scoped.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 5 revision(s)\n- Legacy id `S-027`, recorded as the reference `legacy:S-027`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-027"}],"title":"Direct-byte session establishment is operation-scoped"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-027-direct-byte-session-establishment-is-operation-scoped.md:20`. **read**\n\n- [x] Architecture RFC 0002 is accepted by ADR 0016 with issuer/verifier, audience/resource/principal binding,\n      TTL, replay, reconnect, revocation, proof-of-possession, TLS, and NAT semantics.\n- [ ] Connectors audits grant admission and authority issuance without receiving continuous bytes.\n- [ ] The serving endpoint independently enforces local ownership, limits, and capability facts.\n- [ ] A private satellite initiates RTVBP outward; an unavailable direct route is `unserved`, and\n      the federation control path cannot relay bytes.\n- [ ] One call remains on one endpoint generation until hangup or bounded drain; it is not migrated\n      mid-dialog.\n- [ ] Authorities are absent from logs, ordinary events, and client-visible long-lived state.\n- [ ] Wrong audience/resource/principal, expiry, replay, and revoked deployment all fail fixtures.\n\n## Context\n\nGovern the SIP-to-RTVBP media establishment while the selected voice and application endpoints\nexchange continuous bytes directly under a short-lived bounded authority.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0016 fixes authority; ADR 0024 selects the SIP-to-RTVBP voice journey”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-027-direct-byte-session-establishment-is-operation-scoped.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-027-direct-byte-session-establishment-is-operation-scoped.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 5 revision(s)\n- Legacy id `S-027`, recorded as the reference `legacy:S-027`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-027"}],"status":"draft","title":"Direct-byte session establishment is operation-scoped"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/direct-byte-session-establishment-is-operation-scoped"},"payload":{"actor":"human:timo","at":1788480116246,"causation":"cmd-new-story-direct-byte-session-establishment-is-operation-scoped","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:direct-byte-session-establishment-is-operation-scoped@1#0~dc95521384e8c4ab","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"direct-byte-session-establishment-is-operation-scoped","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:beyond-http"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000237","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788480116256,"causation":"cmd-story-direct-byte-session-establishment-is-operation-scoped-derived_from-epic-beyond-http","change":{"change":"related","relation":"derived_from","target":"epic:beyond-http"},"correlation":"protocol-artifact-relate","event_id":"story:direct-byte-session-establishment-is-operation-scoped@1#0~5ff727a0d649cda2","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"direct-byte-session-establishment-is-operation-scoped","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000088","to":"proposed"},"payload":{"actor":"human:timo","at":1788480116407,"causation":"cmd-move-story-direct-byte-session-establishment-is-operation-scoped-1788480116407","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:direct-byte-session-establishment-is-operation-scoped@2#0~af12439ae99bf910","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"direct-byte-session-establishment-is-operation-scoped","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000088","to":"active"},"payload":{"actor":"human:timo","at":1788480116557,"causation":"cmd-move-story-direct-byte-session-establishment-is-operation-scoped-1788480116557","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:direct-byte-session-establishment-is-operation-scoped@3#0~e7080621c9f929fc","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"external-driver-artifacts-stay-deferred","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md:20`. **read**\n\n- [ ] At least one built-in non-HTTP driver documents concrete pressure that cannot be resolved by\n      the closed registry.\n- [ ] A separate RFC/ADR defines artifact identity, signing, provenance, review, capability manifest,\n      credential delivery, isolation, resource bounds, update/rollback, and revocation.\n- [ ] Substrate executes only generic bounded work and never interprets vendor semantics.\n- [ ] No external artifact enters a caller-selected path or becomes model-selectable.\n- [ ] Until all gates close, schema and runtime reject external implementation forms.\n\n## Context\n\nPrevent “beyond HTTP” from silently becoming an arbitrary plugin/process host before built-in\ndrivers prove a packaging boundary and a separate supply-chain decision is accepted.\n\nSource frontmatter: pillar Platform · areas [domain, service, server, docs] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “delivery item 6 is a gate, not implementation scope; unblock only after built-in pressure and a separate security ADR”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-028`, recorded as the reference `legacy:S-028`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-028"}],"title":"External driver artifacts stay deferred behind attestation"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md:20`. **read**\n\n- [ ] At least one built-in non-HTTP driver documents concrete pressure that cannot be resolved by\n      the closed registry.\n- [ ] A separate RFC/ADR defines artifact identity, signing, provenance, review, capability manifest,\n      credential delivery, isolation, resource bounds, update/rollback, and revocation.\n- [ ] Substrate executes only generic bounded work and never interprets vendor semantics.\n- [ ] No external artifact enters a caller-selected path or becomes model-selectable.\n- [ ] Until all gates close, schema and runtime reject external implementation forms.\n\n## Context\n\nPrevent “beyond HTTP” from silently becoming an arbitrary plugin/process host before built-in\ndrivers prove a packaging boundary and a separate supply-chain decision is accepted.\n\nSource frontmatter: pillar Platform · areas [domain, service, server, docs] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “delivery item 6 is a gate, not implementation scope; unblock only after built-in pressure and a separate security ADR”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 1 revision(s)\n- Legacy id `S-028`, recorded as the reference `legacy:S-028`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-028"}],"status":"draft","title":"External driver artifacts stay deferred behind attestation"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/external-driver-artifacts-stay-deferred"},"payload":{"actor":"human:timo","at":1788480116719,"causation":"cmd-new-story-external-driver-artifacts-stay-deferred","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:external-driver-artifacts-stay-deferred@1#0~11c707863cbda567","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"external-driver-artifacts-stay-deferred","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:beyond-http"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000242","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788480116726,"causation":"cmd-story-external-driver-artifacts-stay-deferred-derived_from-epic-beyond-http","change":{"change":"related","relation":"derived_from","target":"epic:beyond-http"},"correlation":"protocol-artifact-relate","event_id":"story:external-driver-artifacts-stay-deferred@1#0~63482df830782ec2","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"external-driver-artifacts-stay-deferred","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000094","to":"proposed"},"payload":{"actor":"human:timo","at":1788480116875,"causation":"cmd-move-story-external-driver-artifacts-stay-deferred-1788480116875","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:external-driver-artifacts-stay-deferred@2#0~73334c424c3a2b0d","executor":null,"recorded_at":"2026-09-04T00:01:56Z"}}
{"entity":"story","version":1,"id":"external-driver-artifacts-stay-deferred","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000094","to":"active"},"payload":{"actor":"human:timo","at":1788480117024,"causation":"cmd-move-story-external-driver-artifacts-stay-deferred-1788480117024","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:external-driver-artifacts-stay-deferred@3#0~2e1ab2a15349e83d","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"substrate-events-enter-durable-delivery-with-gap-recovery","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md:20`. **read**\n\n- [ ] One supervised Channel per `(Connection, source_scope)` partition persists a\n      source-scope/generation-scoped high-water mark and deduplicates on the exact native identity\n      `(deployment, source_scope, generation, seq)`. `source_scope` is opaque and never derived from\n      tenant data.\n- [ ] The tuple's `deployment` comes only from the Connection's authenticated, out-of-band substrate\n      peer binding. A frame/page/snapshot assertion that differs refuses before deduplication,\n      delivery, or high-water mutation, degrades the Channel, and requires an authenticated\n      operator rebind; payloads never select deployment and mismatch is not treated as a history\n      gap.\n- [ ] Native resource, transition, operation, actor, time, and source sequence survive normalization;\n      connectors adds but never rewrites platform attribution.\n- [ ] Initial durable bootstrap creates and completely consumes a stable snapshot before resuming\n      from its opaque inclusive-barrier cursor. No-cursor pull remains diagnostic only.\n- [ ] A cursor outside retention or from another source scope/generation produces one non-oracular\n      typed gap, snapshot reconciliation, and an explicit reconciliation boundary before normal\n      delivery resumes.\n- [ ] Snapshot ingestion accepts empty complete current workspace and exec sets, distinguishes\n      complete current state from bounded/truncated event provenance, and never projects operation\n      ledger rows or deletion-tombstone tables.\n- [ ] Duplicate, reconnect, retention-gap, state-loss generation, and replay fixtures are shared\n      with the released substrate contract.\n- [ ] Substrate remains independent of the connectors runtime.\n\n## Context\n\nOwn the bridge from a bounded substrate cursor to connectors' durable delivery and replay contract\nwithout pretending those guarantees are identical.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by ADR 0017 and substrate phase 3 is green; implementation remains blocked on an owner-signed substrate bundle”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 4 revision(s)\n- Legacy id `S-029`, recorded as the reference `legacy:S-029`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-029"}],"title":"Substrate events enter durable delivery with gap recovery"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md:20`. **read**\n\n- [ ] One supervised Channel per `(Connection, source_scope)` partition persists a\n      source-scope/generation-scoped high-water mark and deduplicates on the exact native identity\n      `(deployment, source_scope, generation, seq)`. `source_scope` is opaque and never derived from\n      tenant data.\n- [ ] The tuple's `deployment` comes only from the Connection's authenticated, out-of-band substrate\n      peer binding. A frame/page/snapshot assertion that differs refuses before deduplication,\n      delivery, or high-water mutation, degrades the Channel, and requires an authenticated\n      operator rebind; payloads never select deployment and mismatch is not treated as a history\n      gap.\n- [ ] Native resource, transition, operation, actor, time, and source sequence survive normalization;\n      connectors adds but never rewrites platform attribution.\n- [ ] Initial durable bootstrap creates and completely consumes a stable snapshot before resuming\n      from its opaque inclusive-barrier cursor. No-cursor pull remains diagnostic only.\n- [ ] A cursor outside retention or from another source scope/generation produces one non-oracular\n      typed gap, snapshot reconciliation, and an explicit reconciliation boundary before normal\n      delivery resumes.\n- [ ] Snapshot ingestion accepts empty complete current workspace and exec sets, distinguishes\n      complete current state from bounded/truncated event provenance, and never projects operation\n      ledger rows or deletion-tombstone tables.\n- [ ] Duplicate, reconnect, retention-gap, state-loss generation, and replay fixtures are shared\n      with the released substrate contract.\n- [ ] Substrate remains independent of the connectors runtime.\n\n## Context\n\nOwn the bridge from a bounded substrate cursor to connectors' durable delivery and replay contract\nwithout pretending those guarantees are identical.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by ADR 0017 and substrate phase 3 is green; implementation remains blocked on an owner-signed substrate bundle”\n\n## Status\n\n`blocked` in the source. Quoted from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md:5`: `status: blocked`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-15 · 4 revision(s)\n- Legacy id `S-029`, recorded as the reference `legacy:S-029`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-029"}],"status":"draft","title":"Substrate events enter durable delivery with gap recovery"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/substrate-events-enter-durable-delivery-with-gap-recovery"},"payload":{"actor":"human:timo","at":1788480117185,"causation":"cmd-new-story-substrate-events-enter-durable-delivery-with-gap-recovery","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:substrate-events-enter-durable-delivery-with-gap-recovery@1#0~5d55e647836fd837","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"substrate-events-enter-durable-delivery-with-gap-recovery","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:substrate-integration"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000247","target":"01MEM0000000000000058"},"payload":{"actor":"human:timo","at":1788480117195,"causation":"cmd-story-substrate-events-enter-durable-delivery-with-gap-recovery-derived_from-epic-substrate-integration","change":{"change":"related","relation":"derived_from","target":"epic:substrate-integration"},"correlation":"protocol-artifact-relate","event_id":"story:substrate-events-enter-durable-delivery-with-gap-recovery@1#0~132ccad97ee5ede9","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"substrate-events-enter-durable-delivery-with-gap-recovery","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000166","to":"proposed"},"payload":{"actor":"human:timo","at":1788480117346,"causation":"cmd-move-story-substrate-events-enter-durable-delivery-with-gap-recovery-1788480117346","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:substrate-events-enter-durable-delivery-with-gap-recovery@2#0~05192cc7d367ae3f","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"substrate-events-enter-durable-delivery-with-gap-recovery","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000166","to":"active"},"payload":{"actor":"human:timo","at":1788480117496,"causation":"cmd-move-story-substrate-events-enter-durable-delivery-with-gap-recovery-1788480117496","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:substrate-events-enter-durable-delivery-with-gap-recovery@3#0~8faa7b91692ad66f","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"raw-proxy-is-break-glass-not-a-model-capability","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-030-raw-proxy-is-break-glass-not-a-model-capability.md:20`. **read**\n\n- [x] Generic v1 omits raw proxy. Any later operator-only build/config gate must expose it as a\n      distinct destructive/max-effects capability unavailable to models, ordinary Identity service\n      principals, and ordinary connector Grants.\n- [ ] A separate method/path aperture and S-011 destination policy both admit the request; neither\n      request data nor a catalog grant can widen them.\n- [ ] Audit identifies break-glass authority, actor, policy rule, normalized destination hash,\n      method/path class, and result without recording credential or secret-bearing values.\n- [ ] Undeclared credential-bearing model calls are refused and direct users to add a reviewed\n      catalog operation.\n- [ ] Delete and money-movement refusal fixtures prove the old `risk=high` classification cannot\n      reappear.\n\n## Context\n\nEnsure arbitrary credential-bearing provider requests cannot bypass reviewed operation facts or be\nmistaken for ordinary granted catalog execution.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/01-domain-model.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by omitting raw proxy from generic v1; implementation fences remain”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-030-raw-proxy-is-break-glass-not-a-model-capability.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-030-raw-proxy-is-break-glass-not-a-model-capability.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-14 · 3 revision(s)\n- Legacy id `S-030`, recorded as the reference `legacy:S-030`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-030"}],"title":"Raw proxy is break-glass authority, not a model capability"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-030-raw-proxy-is-break-glass-not-a-model-capability.md:20`. **read**\n\n- [x] Generic v1 omits raw proxy. Any later operator-only build/config gate must expose it as a\n      distinct destructive/max-effects capability unavailable to models, ordinary Identity service\n      principals, and ordinary connector Grants.\n- [ ] A separate method/path aperture and S-011 destination policy both admit the request; neither\n      request data nor a catalog grant can widen them.\n- [ ] Audit identifies break-glass authority, actor, policy rule, normalized destination hash,\n      method/path class, and result without recording credential or secret-bearing values.\n- [ ] Undeclared credential-bearing model calls are refused and direct users to add a reviewed\n      catalog operation.\n- [ ] Delete and money-movement refusal fixtures prove the old `risk=high` classification cannot\n      reappear.\n\n## Context\n\nEnsure arbitrary credential-bearing provider requests cannot bypass reviewed operation facts or be\nmistaken for ordinary granted catalog execution.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server] · design `docs/design/01-domain-model.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by omitting raw proxy from generic v1; implementation fences remain”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-030-raw-proxy-is-break-glass-not-a-model-capability.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-030-raw-proxy-is-break-glass-not-a-model-capability.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-14 · 3 revision(s)\n- Legacy id `S-030`, recorded as the reference `legacy:S-030`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-030"}],"status":"draft","title":"Raw proxy is break-glass authority, not a model capability"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/raw-proxy-is-break-glass-not-a-model-capability"},"payload":{"actor":"human:timo","at":1788480117659,"causation":"cmd-new-story-raw-proxy-is-break-glass-not-a-model-capability","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:raw-proxy-is-break-glass-not-a-model-capability@1#0~227d9b585359c982","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"raw-proxy-is-break-glass-not-a-model-capability","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:carried-constraints"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000252","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788480117669,"causation":"cmd-story-raw-proxy-is-break-glass-not-a-model-capability-derived_from-epic-carried-constraints","change":{"change":"related","relation":"derived_from","target":"epic:carried-constraints"},"correlation":"protocol-artifact-relate","event_id":"story:raw-proxy-is-break-glass-not-a-model-capability@1#0~7cec87519edd7bbb","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"contract-bundles-are-versioned-and-pinned","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-031-contract-bundles-are-versioned-and-pinned.md:20`. **read**\n\n- [x] Architecture RFC 0005 is accepted by ADR 0019 and connectors owns explicit catalog and platform bundles.\n- [ ] Each manifest records protocol version, source commit, generator version, hashes, and signing\n      identity; consumers pin version and digest.\n- [ ] Request, response, event, and channel-frame unknown-field/evolution rules are distinct and\n      conformance-tested.\n- [ ] A clean-room consumer passes the same vectors without repository source access.\n- [ ] Release CI uses immutable action pins and emits signed evidence.\n\n## Context\n\nPublish schemas and conformance vectors as reproducible owner-issued releases so substrate, agent,\ncloud, Flux, and autodev can pin a contract without copying from `main` or a sibling checkout.\n\nSource frontmatter: pillar Platform · areas [catalog, protocol, ci, docs] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by ADR 0019; bundle/release implementation remains”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-031-contract-bundles-are-versioned-and-pinned.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-031-contract-bundles-are-versioned-and-pinned.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-031`, recorded as the reference `legacy:S-031`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-031"}],"title":"Connector contract bundles are versioned, signed, and pinned"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-031-contract-bundles-are-versioned-and-pinned.md:20`. **read**\n\n- [x] Architecture RFC 0005 is accepted by ADR 0019 and connectors owns explicit catalog and platform bundles.\n- [ ] Each manifest records protocol version, source commit, generator version, hashes, and signing\n      identity; consumers pin version and digest.\n- [ ] Request, response, event, and channel-frame unknown-field/evolution rules are distinct and\n      conformance-tested.\n- [ ] A clean-room consumer passes the same vectors without repository source access.\n- [ ] Release CI uses immutable action pins and emits signed evidence.\n\n## Context\n\nPublish schemas and conformance vectors as reproducible owner-issued releases so substrate, agent,\ncloud, Flux, and autodev can pin a contract without copying from `main` or a sibling checkout.\n\nSource frontmatter: pillar Platform · areas [catalog, protocol, ci, docs] · design `docs/design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “architecture closed by ADR 0019; bundle/release implementation remains”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-031-contract-bundles-are-versioned-and-pinned.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-031-contract-bundles-are-versioned-and-pinned.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-031`, recorded as the reference `legacy:S-031`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-031"}],"status":"draft","title":"Connector contract bundles are versioned, signed, and pinned"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/contract-bundles-are-versioned-and-pinned"},"payload":{"actor":"human:timo","at":1788480117831,"causation":"cmd-new-story-contract-bundles-are-versioned-and-pinned","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:contract-bundles-are-versioned-and-pinned@1#0~625341131fd1a4af","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"contract-bundles-are-versioned-and-pinned","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:contract-release"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000257","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788480117841,"causation":"cmd-story-contract-bundles-are-versioned-and-pinned-derived_from-epic-contract-release","change":{"change":"related","relation":"derived_from","target":"epic:contract-release"},"correlation":"protocol-artifact-relate","event_id":"story:contract-bundles-are-versioned-and-pinned@1#0~58fbdb04d9e75050","executor":null,"recorded_at":"2026-09-04T00:01:57Z"}}
{"entity":"story","version":1,"id":"sip-driver-terminates-one-governed-call","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-032-sip-driver-terminates-one-governed-call.md:20`. **read**\n\n- [x] S-024 and the platform-family ports exist; the driver implements the neutral\n      `TelephonySession` port and only `server` selects it after admission.\n- [x] The closed catalog vocabulary represents `sip_v1` without accepting arbitrary driver names;\n      canonical-document and consumer round-trip tests preserve it exactly.\n- [x] The repository-authored B10x native-capability source with registered provenance\n      produces a reviewed provider declaration and generated canonical catalog member\n      whose interaction shape is `session_establishment`, driver is `sip_v1`, implementation is\n      `built_in`, and capability/risk/idempotency facts are exact. The first exposure is deliberately\n      limited to the alias-only `sip.dial` member after the driver and development route proof.\n- [x] The source, provenance, provider declaration, canonical document, lock row, pack, and web\n      projection land atomically; the effective catalog exposes the member only where the selected\n      deployment can actually dispatch `sip_v1`.\n- [x] `driver-sip` is the only named network-capable driver and calls `sipx` bind only from a\n      non-serializable, proof-bearing `AdmittedSipPlan`; fence tests reject every other path.\n- [ ] Configured, DNS-resolved, SIP-learned, and SDP-learned targets plus local listener/media ports\n      are checked against the admitted destination/bind apertures before transmission.\n- [ ] A symmetric-RTP source cannot become an egress peer before media-peer admission. Loopback may\n      characterize the pinned behavior, but stable/exposed support waits for an enforceable hook,\n      supported disable switch, or independently measured network aperture.\n- [x] The workspace explicitly moves to Rust 1.88 with its MSRV lane green, and `sipx`\n      `v1.0.0-rc.23` resolves to `004ac534b8b222060ad2d2308763efe6e1dedc10` in release evidence.\n- [ ] Loopback fixtures cover registration, inbound and outbound dialogs, DTMF, bounded duplex\n      audio, hangup, cancellation, authentication failure, reconnect, and whole-task termination.\n- [ ] Driver, implementation, credential, placement, target aperture, tenant, and profile are\n      server-selected; caller attempts to widen any one refuse before secret access.\n- [ ] SIP credentials and SRTP material are structurally absent from protocol DTOs, events, audit,\n      logs, fixtures, and the `TelephonySession` port.\n- [x] The crate implements no proxy, registrar service, PBX, arbitrary SIP URI proxy, TURN, video,\n      IVR, RTVBP, or Agent behavior.\n- [x] Stable support remains blocked while the selected `sipx` API is prerelease unless an explicit\n      compatibility and upgrade exception is accepted with conformance evidence.\n\n## Context\n\nImplement the closed built-in `sip_v1` driver with exact `sipx` dependencies and prove one bounded\nSIP call without importing RTVBP or requiring a live carrier.\n\nSource frontmatter: pillar Platform · areas [catalog, domain, service, server, driver-sip] · design `docs/design/05-native-sip-and-rtvbp.md`. **read**\n\nSource `note:` field, quoted: “Source-grounded catalog and dev-cluster outbound proof exist; full conformance and stable network aperture remain”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-032-sip-driver-terminates-one-governed-call.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-032-sip-driver-terminates-one-governed-call.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-14 · last touched 2026-08-16 · 7 revision(s)\n- Legacy id `S-032`, recorded as the reference `legacy:S-032`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-032"}],"title":"The SIP driver terminates one governed call"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-032-sip-driver-terminates-one-governed-call.md:20`. **read**\n\n- [x] S-024 and the platform-family ports exist; the driver implements the neutral\n      `TelephonySession` port and only `server` selects it after admission.\n- [x] The closed catalog vocabulary represents `sip_v1` without accepting arbitrary driver names;\n      canonical-document and consumer round-trip tests preserve it exactly.\n- [x] The repository-authored B10x native-capability source with registered provenance\n      produces a reviewed provider declaration and generated canonical catalog member\n      whose interaction shape is `session_establishment`, driver is `sip_v1`, implementation is\n      `built_in`, and capability/risk/idempotency facts are exact. The first exposure is deliberately\n      limited to the alias-only `sip.dial` member after the driver and development route proof.\n- [x] The source, provenance, provider declaration, canonical document, lock row, pack, and web\n      projection land atomically; the effective catalog exposes the member only where the selected\n      deployment can actually dispatch `sip_v1`.\n- [x] `driver-sip` is the only named network-capable driver and calls `sipx` bind only from a\n      non-serializable, proof-bearing `AdmittedSipPlan`; fence tests reject every other path.\n- [ ] Configured, DNS-resolved, SIP-learned, and SDP-learned targets plus local listener/media ports\n      are checked against the admitted destination/bind apertures before transmission.\n- [ ] A symmetric-RTP source cannot become an egress peer before media-peer admission. Loopback may\n      characterize the pinned behavior, but stable/exposed support waits for an enforceable hook,\n      supported disable switch, or independently measured network aperture.\n- [x] The workspace explicitly moves to Rust 1.88 with its MSRV lane green, and `sipx`\n      `v1.0.0-rc.23` resolves to `004ac534b8b222060ad2d2308763efe6e1dedc10` in release evidence.\n- [ ] Loopback fixtures cover registration, inbound and outbound dialogs, DTMF, bounded duplex\n      audio, hangup, cancellation, authentication failure, reconnect, and whole-task termination.\n- [ ] Driver, implementation, credential, placement, target aperture, tenant, and profile are\n      server-selected; caller attempts to widen any one refuse before secret access.\n- [ ] SIP credentials and SRTP material are structurally absent from protocol DTOs, events, audit,\n      logs, fixtures, and the `TelephonySession` port.\n- [x] The crate implements no proxy, registrar service, PBX, arbitrary SIP URI proxy, TURN, video,\n      IVR, RTVBP, or Agent behavior.\n- [x] Stable support remains blocked while the selected `sipx` API is prerelease unless an explicit\n      compatibility and upgrade exception is accepted with conformance evidence.\n\n## Context\n\nImplement the closed built-in `sip_v1` driver with exact `sipx` dependencies and prove one bounded\nSIP call without importing RTVBP or requiring a live carrier.\n\nSource frontmatter: pillar Platform · areas [catalog, domain, service, server, driver-sip] · design `docs/design/05-native-sip-and-rtvbp.md`. **read**\n\nSource `note:` field, quoted: “Source-grounded catalog and dev-cluster outbound proof exist; full conformance and stable network aperture remain”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-032-sip-driver-terminates-one-governed-call.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-032-sip-driver-terminates-one-governed-call.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-14 · last touched 2026-08-16 · 7 revision(s)\n- Legacy id `S-032`, recorded as the reference `legacy:S-032`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-032"}],"status":"draft","title":"The SIP driver terminates one governed call"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/sip-driver-terminates-one-governed-call"},"payload":{"actor":"human:timo","at":1788480118044,"causation":"cmd-new-story-sip-driver-terminates-one-governed-call","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:sip-driver-terminates-one-governed-call@1#0~604d4ba5fbad7318","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"sip-driver-terminates-one-governed-call","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:native-voice"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000262","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788480118055,"causation":"cmd-story-sip-driver-terminates-one-governed-call-derived_from-epic-native-voice","change":{"change":"related","relation":"derived_from","target":"epic:native-voice"},"correlation":"protocol-artifact-relate","event_id":"story:sip-driver-terminates-one-governed-call@1#0~b1042c5a78dd032d","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"sip-driver-terminates-one-governed-call","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000169","to":"proposed"},"payload":{"actor":"human:timo","at":1788480118205,"causation":"cmd-move-story-sip-driver-terminates-one-governed-call-1788480118205","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:sip-driver-terminates-one-governed-call@2#0~c5a8d02863208fe2","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"sip-driver-terminates-one-governed-call","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000169","to":"active"},"payload":{"actor":"human:timo","at":1788480118356,"causation":"cmd-move-story-sip-driver-terminates-one-governed-call-1788480118356","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:sip-driver-terminates-one-governed-call@3#0~3ac5b842fd312526","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"neutral-rtvbp-bridges-the-call-to-an-application-channel","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-033-neutral-rtvbp-bridges-the-call-to-an-application-channel.md:21`. **read**\n\n- [ ] RTVBP Rust SDK `sdk/rust/v0.1.0` resolves to\n      `dc0a60f7425b4899885f372152028457791b1e72`, its released crate asset matches SHA-256\n      `7d1d675e359016a5c8711bc0a29783ad9ce57a2f80f47ab5c77bc0152935ff9b`, and licenses are\n      recorded; the B10x binding manifest and fixtures are locally owner-released with\n      conformance evidence rather than compiled into the upstream SDK.\n- [ ] The released `VoiceSession` contract contains no RTVBP type, and both endpoint adapters prove\n      their method/event/media mapping against the same generic vectors.\n- [x] Generic code boots and passes its lifecycle with `babelforce.v1` absent; attempting that\n      product profile or omitting explicit profile negotiation at the generic endpoint refuses by\n      name rather than selecting RTVBP's legacy default.\n- [ ] Memory and bounded WebSocket fixtures prove initialize, control/media channels, DTMF,\n      duplex L16 mono audio, keepalive, close, cancellation, and whole-task termination.\n- [x] The proof-bound authority binds endpoint, tenant, actor, Connection, Grant, operation,\n      profile, proof key, and lease; wrong audience, expiry, replay, and revocation refuse.\n- [x] Media/frame/queue/request bounds are explicit; overflow records observable bounded loss, and\n      barge-in has one causal interruption/clear path.\n- [ ] Characterization measures the SDK's internal control/transport queues. An outer bounded queue\n      alone cannot support a stable/exposed bounded-memory claim.\n- [ ] A private-satellite fixture connects outward directly, refuses an unreachable route as\n      `session_unserved`, and proves federation never carries RTVBP or media bytes.\n- [ ] One composed model-free loopback call crosses SIP → RTVBP → fake neutral application media,\n      including hangup, cancellation, loss, interruption, and one-generation drain.\n- [ ] A signed Connectors owner bundle and clean-room consumer proof precede any stable or hosted\n      support claim.\n- [ ] The `RUSTSEC-2025-0141` maintenance warning is closed by replacing RTVBP's\n      `webrtc 0.14.0` → `dtls 0.13.0` → `bincode 1.3.3` closure or by pinning and reviewing a\n      maintained fork before stable or exposed support.\n\n## Context\n\nImplement the Connectors-owned RTVBP binding from generic `VoiceSession` semantics over\n`TelephonySession` and prove a directly authorized, bounded session against a fake application peer\nwithout a model or Babelforce product semantics.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server, rtvbp-voice-endpoint, voice-runtime] · design `docs/design/05-native-sip-and-rtvbp.md`. **read**\n\nSource `note:` field, quoted: “Supervised authenticated SIP/RTVBP composition exists; complete lifecycle, placement, and release evidence remain”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-033-neutral-rtvbp-bridges-the-call-to-an-application-channel.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-033-neutral-rtvbp-bridges-the-call-to-an-application-channel.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-14 · last touched 2026-08-15 · 8 revision(s)\n- Legacy id `S-033`, recorded as the reference `legacy:S-033`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-033"}],"title":"Neutral RTVBP bridges the call to an application channel"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-033-neutral-rtvbp-bridges-the-call-to-an-application-channel.md:21`. **read**\n\n- [ ] RTVBP Rust SDK `sdk/rust/v0.1.0` resolves to\n      `dc0a60f7425b4899885f372152028457791b1e72`, its released crate asset matches SHA-256\n      `7d1d675e359016a5c8711bc0a29783ad9ce57a2f80f47ab5c77bc0152935ff9b`, and licenses are\n      recorded; the B10x binding manifest and fixtures are locally owner-released with\n      conformance evidence rather than compiled into the upstream SDK.\n- [ ] The released `VoiceSession` contract contains no RTVBP type, and both endpoint adapters prove\n      their method/event/media mapping against the same generic vectors.\n- [x] Generic code boots and passes its lifecycle with `babelforce.v1` absent; attempting that\n      product profile or omitting explicit profile negotiation at the generic endpoint refuses by\n      name rather than selecting RTVBP's legacy default.\n- [ ] Memory and bounded WebSocket fixtures prove initialize, control/media channels, DTMF,\n      duplex L16 mono audio, keepalive, close, cancellation, and whole-task termination.\n- [x] The proof-bound authority binds endpoint, tenant, actor, Connection, Grant, operation,\n      profile, proof key, and lease; wrong audience, expiry, replay, and revocation refuse.\n- [x] Media/frame/queue/request bounds are explicit; overflow records observable bounded loss, and\n      barge-in has one causal interruption/clear path.\n- [ ] Characterization measures the SDK's internal control/transport queues. An outer bounded queue\n      alone cannot support a stable/exposed bounded-memory claim.\n- [ ] A private-satellite fixture connects outward directly, refuses an unreachable route as\n      `session_unserved`, and proves federation never carries RTVBP or media bytes.\n- [ ] One composed model-free loopback call crosses SIP → RTVBP → fake neutral application media,\n      including hangup, cancellation, loss, interruption, and one-generation drain.\n- [ ] A signed Connectors owner bundle and clean-room consumer proof precede any stable or hosted\n      support claim.\n- [ ] The `RUSTSEC-2025-0141` maintenance warning is closed by replacing RTVBP's\n      `webrtc 0.14.0` → `dtls 0.13.0` → `bincode 1.3.3` closure or by pinning and reviewing a\n      maintained fork before stable or exposed support.\n\n## Context\n\nImplement the Connectors-owned RTVBP binding from generic `VoiceSession` semantics over\n`TelephonySession` and prove a directly authorized, bounded session against a fake application peer\nwithout a model or Babelforce product semantics.\n\nSource frontmatter: pillar Platform · areas [domain, protocol, service, server, rtvbp-voice-endpoint, voice-runtime] · design `docs/design/05-native-sip-and-rtvbp.md`. **read**\n\nSource `note:` field, quoted: “Supervised authenticated SIP/RTVBP composition exists; complete lifecycle, placement, and release evidence remain”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-033-neutral-rtvbp-bridges-the-call-to-an-application-channel.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-033-neutral-rtvbp-bridges-the-call-to-an-application-channel.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-14 · last touched 2026-08-15 · 8 revision(s)\n- Legacy id `S-033`, recorded as the reference `legacy:S-033`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-033"}],"status":"draft","title":"Neutral RTVBP bridges the call to an application channel"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/neutral-rtvbp-bridges-the-call-to-an-application-channel"},"payload":{"actor":"human:timo","at":1788480118557,"causation":"cmd-new-story-neutral-rtvbp-bridges-the-call-to-an-application-channel","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:neutral-rtvbp-bridges-the-call-to-an-application-channel@1#0~6e01fa9657524539","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"neutral-rtvbp-bridges-the-call-to-an-application-channel","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:native-voice"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000267","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788480118566,"causation":"cmd-story-neutral-rtvbp-bridges-the-call-to-an-application-channel-derived_from-epic-native-voice","change":{"change":"related","relation":"derived_from","target":"epic:native-voice"},"correlation":"protocol-artifact-relate","event_id":"story:neutral-rtvbp-bridges-the-call-to-an-application-channel@1#0~1bb08e5566c6db0c","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"neutral-rtvbp-bridges-the-call-to-an-application-channel","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000130","to":"proposed"},"payload":{"actor":"human:timo","at":1788480118717,"causation":"cmd-move-story-neutral-rtvbp-bridges-the-call-to-an-application-channel-1788480118717","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:neutral-rtvbp-bridges-the-call-to-an-application-channel@2#0~4ba1dbf3a4689da4","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"neutral-rtvbp-bridges-the-call-to-an-application-channel","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000130","to":"active"},"payload":{"actor":"human:timo","at":1788480118870,"causation":"cmd-move-story-neutral-rtvbp-bridges-the-call-to-an-application-channel-1788480118870","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:neutral-rtvbp-bridges-the-call-to-an-application-channel@3#0~2b08abc63630e648","executor":null,"recorded_at":"2026-09-04T00:01:58Z"}}
{"entity":"story","version":1,"id":"production-credential-custody-closes","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-034-production-credential-custody-closes.md:21`. **read**\n\n- [ ] Personal release custody uses an accepted OS-keychain set and explicitly refuses headless\n      operation when none is available; no file or environment fallback is shipped.\n- [ ] External-provider binding fixes session lifetime, user presence, revocation, and unavailable\n      behavior without a generic value-reading Connector operation.\n- [ ] The management contract implements value-free acquire/status/rotate/revoke/delete, atomic\n      credential-generation and Connection metadata changes, and no read/export/list-value method.\n- [ ] Target-sealed satellite completion fixes the envelope algorithm, authenticated target-key\n      binding, one-use replay/expiry behavior, partition handling, and interrupted-commit recovery.\n- [ ] Production managed storage proves tenant isolation, KMS/seal custody, HA, backup/restore,\n      root-token elimination, break-glass recovery, rotation/revocation SLOs, and incident response.\n- [ ] End-to-end sentinels prove credential and derived forms absent from responses, logs, audit,\n      argv, environment, relational state, manifests, crashes, backups, and central relay plaintext.\n- [ ] Every maintained repository runs the checksum-pinned full-history secret scan; any historical\n      baseline is exact-fingerprint-only, provenance-reviewed, and paired with rotation evidence\n      whenever material may have been live.\n- [ ] Owner-signed bundles, a clean-room consumer, and cross-foundation Identity/Cloud/Substrate\n      conformance satisfy ADR 0019 before a stable or production claim.\n\n## Context\n\nComplete the Connector-owned portion of architecture's phase-8 production credential and secret\nclosure without turning a development FileStore, one-node Vault, or design fixture into release\nevidence.\n\nSource frontmatter: pillar Platform · areas [connector-secrets, service, protocol, ci, docs] · design `docs/design/07-credential-custody-topologies.md`. **read**\n\nSource `note:` field, quoted: “ADR 0032 accepts only the central managed-store development subset; every production exit remains gated”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-034-production-credential-custody-closes.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-034-production-credential-custody-closes.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-15 · last touched 2026-08-15 · 1 revision(s)\n- Legacy id `S-034`, recorded as the reference `legacy:S-034`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-034"}],"title":"Production credential custody closes with owner evidence"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-034-production-credential-custody-closes.md:21`. **read**\n\n- [ ] Personal release custody uses an accepted OS-keychain set and explicitly refuses headless\n      operation when none is available; no file or environment fallback is shipped.\n- [ ] External-provider binding fixes session lifetime, user presence, revocation, and unavailable\n      behavior without a generic value-reading Connector operation.\n- [ ] The management contract implements value-free acquire/status/rotate/revoke/delete, atomic\n      credential-generation and Connection metadata changes, and no read/export/list-value method.\n- [ ] Target-sealed satellite completion fixes the envelope algorithm, authenticated target-key\n      binding, one-use replay/expiry behavior, partition handling, and interrupted-commit recovery.\n- [ ] Production managed storage proves tenant isolation, KMS/seal custody, HA, backup/restore,\n      root-token elimination, break-glass recovery, rotation/revocation SLOs, and incident response.\n- [ ] End-to-end sentinels prove credential and derived forms absent from responses, logs, audit,\n      argv, environment, relational state, manifests, crashes, backups, and central relay plaintext.\n- [ ] Every maintained repository runs the checksum-pinned full-history secret scan; any historical\n      baseline is exact-fingerprint-only, provenance-reviewed, and paired with rotation evidence\n      whenever material may have been live.\n- [ ] Owner-signed bundles, a clean-room consumer, and cross-foundation Identity/Cloud/Substrate\n      conformance satisfy ADR 0019 before a stable or production claim.\n\n## Context\n\nComplete the Connector-owned portion of architecture's phase-8 production credential and secret\nclosure without turning a development FileStore, one-node Vault, or design fixture into release\nevidence.\n\nSource frontmatter: pillar Platform · areas [connector-secrets, service, protocol, ci, docs] · design `docs/design/07-credential-custody-topologies.md`. **read**\n\nSource `note:` field, quoted: “ADR 0032 accepts only the central managed-store development subset; every production exit remains gated”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-034-production-credential-custody-closes.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-034-production-credential-custody-closes.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-15 · last touched 2026-08-15 · 1 revision(s)\n- Legacy id `S-034`, recorded as the reference `legacy:S-034`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-034"}],"status":"draft","title":"Production credential custody closes with owner evidence"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/production-credential-custody-closes"},"payload":{"actor":"human:timo","at":1788480119080,"causation":"cmd-new-story-production-credential-custody-closes","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:production-credential-custody-closes@1#0~755bf2fd1cbbc0b1","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"production-credential-custody-closes","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:credential-production"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000272","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788480119088,"causation":"cmd-story-production-credential-custody-closes-derived_from-epic-credential-production","change":{"change":"related","relation":"derived_from","target":"epic:credential-production"},"correlation":"protocol-artifact-relate","event_id":"story:production-credential-custody-closes@1#0~28441c308f4ee592","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"the-cli-runs-without-being-configured-by-hand","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-035-the-cli-runs-without-being-configured-by-hand.md:109`. **read**\n\n- [x] `connectors init` writes a configuration the daemon reads back, at `0600`, refusing to\n      overwrite silently, and refusing to leave an invalid file on disk.\n- [x] Every personal-local subcommand runs without `--config` and `--state-root`.\n- [x] `-o json` and `-o yaml` emit failures on stdout in that format; `text`/`compact` use stderr.\n- [x] A Connector refusal exits non-zero and forwards the Connector's own error code.\n- [x] Results render as their payload, not their transport envelope; `-o compact` is one record\n      per line.\n- [x] `connectors doctor` names a state root too deep to bind a Connect Session endpoint, and exits\n      non-zero.\n- [x] `operation invoke` accepts input from an argument, a file, or stdin.\n- [x] `connectors providers` reports, per catalogued provider, its authority, credential\n      requirement, required configuration fields, verify probe and operation count — so coverage is\n      a measured fact rather than a claim.\n- [ ] A one-shot command composes the runtime in-process when no daemon holds the state lock, so\n      `connectors operation search` answers with nothing running. The in-process path must enter at\n      the same `BackendRegistry` entry point, so there is exactly one authority path.\n- [ ] `ConnectSessionStatus` carries a non-secret failure code. Today `state: Failed` is all a\n      caller can see, so `integration-slack` logs its reason with `eprintln!` and a CLI can only\n      print \"failed\". Needs `ConnectSessionTerminal::Failed` to carry a code, which touches every\n      caller in `integration-slack` and `integration-jira`.\n\n## Context\n\nMake `connectors` a program someone can install and use on their own machine, with no deployment,\nno Identity and no PostgreSQL — the first of the six stories that turn the local posture from a test\nharness for the hosted service into a product. This story is the entry surface: writing a\nconfiguration, finding out why something does not work, and getting results a script can read.\n\nSource frontmatter: pillar Platform · areas [cli, config, protocol] · priority 1. **read**\n\nSource `note:` field, quoted: “init/doctor/providers/output and correct exit codes have landed, with the operator surface split into connectors-console; the in-process one-shot and the Connect Session failure reason remain”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-035-the-cli-runs-without-being-configured-by-hand.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-035-the-cli-runs-without-being-configured-by-hand.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-20 · 1 revision(s)\n- Legacy id `S-035`, recorded as the reference `legacy:S-035`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-035"}],"title":"The CLI runs without being configured by hand"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-035-the-cli-runs-without-being-configured-by-hand.md:109`. **read**\n\n- [x] `connectors init` writes a configuration the daemon reads back, at `0600`, refusing to\n      overwrite silently, and refusing to leave an invalid file on disk.\n- [x] Every personal-local subcommand runs without `--config` and `--state-root`.\n- [x] `-o json` and `-o yaml` emit failures on stdout in that format; `text`/`compact` use stderr.\n- [x] A Connector refusal exits non-zero and forwards the Connector's own error code.\n- [x] Results render as their payload, not their transport envelope; `-o compact` is one record\n      per line.\n- [x] `connectors doctor` names a state root too deep to bind a Connect Session endpoint, and exits\n      non-zero.\n- [x] `operation invoke` accepts input from an argument, a file, or stdin.\n- [x] `connectors providers` reports, per catalogued provider, its authority, credential\n      requirement, required configuration fields, verify probe and operation count — so coverage is\n      a measured fact rather than a claim.\n- [ ] A one-shot command composes the runtime in-process when no daemon holds the state lock, so\n      `connectors operation search` answers with nothing running. The in-process path must enter at\n      the same `BackendRegistry` entry point, so there is exactly one authority path.\n- [ ] `ConnectSessionStatus` carries a non-secret failure code. Today `state: Failed` is all a\n      caller can see, so `integration-slack` logs its reason with `eprintln!` and a CLI can only\n      print \"failed\". Needs `ConnectSessionTerminal::Failed` to carry a code, which touches every\n      caller in `integration-slack` and `integration-jira`.\n\n## Context\n\nMake `connectors` a program someone can install and use on their own machine, with no deployment,\nno Identity and no PostgreSQL — the first of the six stories that turn the local posture from a test\nharness for the hosted service into a product. This story is the entry surface: writing a\nconfiguration, finding out why something does not work, and getting results a script can read.\n\nSource frontmatter: pillar Platform · areas [cli, config, protocol] · priority 1. **read**\n\nSource `note:` field, quoted: “init/doctor/providers/output and correct exit codes have landed, with the operator surface split into connectors-console; the in-process one-shot and the Connect Session failure reason remain”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-035-the-cli-runs-without-being-configured-by-hand.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-035-the-cli-runs-without-being-configured-by-hand.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-20 · 1 revision(s)\n- Legacy id `S-035`, recorded as the reference `legacy:S-035`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-035"}],"status":"draft","title":"The CLI runs without being configured by hand"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-cli-runs-without-being-configured-by-hand"},"payload":{"actor":"human:timo","at":1788480119346,"causation":"cmd-new-story-the-cli-runs-without-being-configured-by-hand","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-cli-runs-without-being-configured-by-hand@1#0~d008fc1b75cc3f5f","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"the-cli-runs-without-being-configured-by-hand","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:local-product"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000277","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788480119355,"causation":"cmd-story-the-cli-runs-without-being-configured-by-hand-derived_from-epic-local-product","change":{"change":"related","relation":"derived_from","target":"epic:local-product"},"correlation":"protocol-artifact-relate","event_id":"story:the-cli-runs-without-being-configured-by-hand@1#0~0eaf58c319b8942d","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"the-cli-runs-without-being-configured-by-hand","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000184","to":"proposed"},"payload":{"actor":"human:timo","at":1788480119508,"causation":"cmd-move-story-the-cli-runs-without-being-configured-by-hand-1788480119508","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-cli-runs-without-being-configured-by-hand@2#0~51f1757cc2254e8b","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"the-cli-runs-without-being-configured-by-hand","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000184","to":"active"},"payload":{"actor":"human:timo","at":1788480119661,"causation":"cmd-move-story-the-cli-runs-without-being-configured-by-hand-1788480119661","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-cli-runs-without-being-configured-by-hand@3#0~3aafeceb4d938ea3","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"the-os-keyring-becomes-the-local-store","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-036-the-os-keyring-becomes-the-local-store.md:61`. **read**\n\n- [x] Values in the OS keyring on Linux, in preference to the file store.\n- [x] The bound backend is published in readiness and named by `doctor`.\n- [x] A live round trip against this machine's keyring, including trailing-whitespace preservation\n      and the `NotFound`-versus-`Unreachable` distinction.\n- [x] `connectors auth status` reports connectedness without reading a value.\n- [x] Design 07 amended with the accepted topology and its exact scope.\n- [ ] The prepared two-phase store, which Slack uses, still writes the unencrypted file.\n- [ ] macOS Keychain and Windows Credential Manager, behind the same port.\n\n## Context\n\nA credential on a workstation should be sealed by the desktop session, not by file permissions\nalone.\n\nSource frontmatter: pillar Platform · areas [connector-secrets, runtime, cli] · priority 2 · design `../design/07-credential-custody-topologies.md`. **read**\n\nSource `note:` field, quoted: “values are in the OS keyring on Linux and `connectors auth status` reports connectedness without reading one; the prepared two-phase store and macOS/Windows remain on the file backend”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-036-the-os-keyring-becomes-the-local-store.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-036-the-os-keyring-becomes-the-local-store.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-20 · 1 revision(s)\n- Legacy id `S-036`, recorded as the reference `legacy:S-036`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-036"}],"title":"The OS keyring becomes the local credential store"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-036-the-os-keyring-becomes-the-local-store.md:61`. **read**\n\n- [x] Values in the OS keyring on Linux, in preference to the file store.\n- [x] The bound backend is published in readiness and named by `doctor`.\n- [x] A live round trip against this machine's keyring, including trailing-whitespace preservation\n      and the `NotFound`-versus-`Unreachable` distinction.\n- [x] `connectors auth status` reports connectedness without reading a value.\n- [x] Design 07 amended with the accepted topology and its exact scope.\n- [ ] The prepared two-phase store, which Slack uses, still writes the unencrypted file.\n- [ ] macOS Keychain and Windows Credential Manager, behind the same port.\n\n## Context\n\nA credential on a workstation should be sealed by the desktop session, not by file permissions\nalone.\n\nSource frontmatter: pillar Platform · areas [connector-secrets, runtime, cli] · priority 2 · design `../design/07-credential-custody-topologies.md`. **read**\n\nSource `note:` field, quoted: “values are in the OS keyring on Linux and `connectors auth status` reports connectedness without reading one; the prepared two-phase store and macOS/Windows remain on the file backend”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-036-the-os-keyring-becomes-the-local-store.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-036-the-os-keyring-becomes-the-local-store.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-20 · 1 revision(s)\n- Legacy id `S-036`, recorded as the reference `legacy:S-036`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-036"}],"status":"draft","title":"The OS keyring becomes the local credential store"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-os-keyring-becomes-the-local-store"},"payload":{"actor":"human:timo","at":1788480119901,"causation":"cmd-new-story-the-os-keyring-becomes-the-local-store","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-os-keyring-becomes-the-local-store@1#0~d607528431b72118","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"the-os-keyring-becomes-the-local-store","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:local-product"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000282","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788480119910,"causation":"cmd-story-the-os-keyring-becomes-the-local-store-derived_from-epic-local-product","change":{"change":"related","relation":"derived_from","target":"epic:local-product"},"correlation":"protocol-artifact-relate","event_id":"story:the-os-keyring-becomes-the-local-store@1#0~cc66fb533daeabc5","executor":null,"recorded_at":"2026-09-04T00:01:59Z"}}
{"entity":"story","version":1,"id":"the-os-keyring-becomes-the-local-store","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000196","to":"proposed"},"payload":{"actor":"human:timo","at":1788480120065,"causation":"cmd-move-story-the-os-keyring-becomes-the-local-store-1788480120065","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-os-keyring-becomes-the-local-store@2#0~d06cd722eaa082a4","executor":null,"recorded_at":"2026-09-04T00:02:00Z"}}
{"entity":"story","version":1,"id":"the-os-keyring-becomes-the-local-store","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000196","to":"active"},"payload":{"actor":"human:timo","at":1788480120222,"causation":"cmd-move-story-the-os-keyring-becomes-the-local-store-1788480120222","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-os-keyring-becomes-the-local-store@3#0~6d64ae8bda934348","executor":null,"recorded_at":"2026-09-04T00:02:00Z"}}
{"entity":"story","version":1,"id":"state-becomes-a-port","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-041-state-becomes-a-port.md:85`. **read**\n\n- [x] A `StateStore` port with a shared key grammar and error vocabulary.\n- [x] An in-memory backend, a SQLite backend (in-memory and file), and PostgreSQL behind it.\n- [x] One conformance suite, run by every backend, pinning byte-exactness, atomic bounded append,\n      bounded reads, delete idempotence and key validation on **every** operation including reads.\n- [x] The suite passes against a live PostgreSQL, so backend equivalence is measured rather than\n      claimed.\n- [x] The five Integrations drop their private state shapes and take `Arc<dyn StateStore>` on the\n      hosted seam, and `hosted-vault`'s prepared-transaction journal with them.\n- [ ] `integration-jira` composes locally, which today it cannot. It no longer *requires* a\n      database — its constructor takes the port — but no personal-posture constructor wires it.\n- [ ] `slack` and `gitlab` collapse their owner-only file branch into the port as well.\n\n## Context\n\nMake where an Integration's durable state lives a **port**, the way secrets and egress already are,\nso a deployment chooses a backend instead of the code choosing a branch — and give it a SQLite\nimplementation that is in-memory for tests and a file for a workstation.\n\nSource frontmatter: pillar Platform · areas [service, state, testing] · priority 2 · design `../design/12-one-owner-for-every-outside-connection.md`. **read**\n\nSource `note:` field, quoted: “the port, its three backends and the shared conformance suite have landed, and every hosted state seam now takes Arc<dyn StateStore> — serve-hosted runs on SQLite with no database server. What remains is the personal posture: slack and gitlab still keep an owner-only file branch beside the port, and jira has no personal constructor at all.”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-041-state-becomes-a-port.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-041-state-becomes-a-port.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-21 · 2 revision(s)\n- Legacy id `S-041`, recorded as the reference `legacy:S-041`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-041"}],"title":"State becomes a port, with a SQLite backend"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-041-state-becomes-a-port.md:85`. **read**\n\n- [x] A `StateStore` port with a shared key grammar and error vocabulary.\n- [x] An in-memory backend, a SQLite backend (in-memory and file), and PostgreSQL behind it.\n- [x] One conformance suite, run by every backend, pinning byte-exactness, atomic bounded append,\n      bounded reads, delete idempotence and key validation on **every** operation including reads.\n- [x] The suite passes against a live PostgreSQL, so backend equivalence is measured rather than\n      claimed.\n- [x] The five Integrations drop their private state shapes and take `Arc<dyn StateStore>` on the\n      hosted seam, and `hosted-vault`'s prepared-transaction journal with them.\n- [ ] `integration-jira` composes locally, which today it cannot. It no longer *requires* a\n      database — its constructor takes the port — but no personal-posture constructor wires it.\n- [ ] `slack` and `gitlab` collapse their owner-only file branch into the port as well.\n\n## Context\n\nMake where an Integration's durable state lives a **port**, the way secrets and egress already are,\nso a deployment chooses a backend instead of the code choosing a branch — and give it a SQLite\nimplementation that is in-memory for tests and a file for a workstation.\n\nSource frontmatter: pillar Platform · areas [service, state, testing] · priority 2 · design `../design/12-one-owner-for-every-outside-connection.md`. **read**\n\nSource `note:` field, quoted: “the port, its three backends and the shared conformance suite have landed, and every hosted state seam now takes Arc<dyn StateStore> — serve-hosted runs on SQLite with no database server. What remains is the personal posture: slack and gitlab still keep an owner-only file branch beside the port, and jira has no personal constructor at all.”\n\n## Status\n\n`in-progress` in the source. Quoted from `docs/stories/S-041-state-becomes-a-port.md:5`: `status: in-progress`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-041-state-becomes-a-port.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-21 · 2 revision(s)\n- Legacy id `S-041`, recorded as the reference `legacy:S-041`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-041"}],"status":"draft","title":"State becomes a port, with a SQLite backend"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/state-becomes-a-port"},"payload":{"actor":"human:timo","at":1788480120474,"causation":"cmd-new-story-state-becomes-a-port","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:state-becomes-a-port@1#0~af08c05dae1cfea8","executor":null,"recorded_at":"2026-09-04T00:02:00Z"}}
{"entity":"story","version":1,"id":"state-becomes-a-port","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:local-product"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000287","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788480120484,"causation":"cmd-story-state-becomes-a-port-derived_from-epic-local-product","change":{"change":"related","relation":"derived_from","target":"epic:local-product"},"correlation":"protocol-artifact-relate","event_id":"story:state-becomes-a-port@1#0~be8dbd3c6b9d3150","executor":null,"recorded_at":"2026-09-04T00:02:00Z"}}
{"entity":"story","version":1,"id":"state-becomes-a-port","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000181","to":"proposed"},"payload":{"actor":"human:timo","at":1788480120640,"causation":"cmd-move-story-state-becomes-a-port-1788480120640","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:state-becomes-a-port@2#0~1594434014892190","executor":null,"recorded_at":"2026-09-04T00:02:00Z"}}
{"entity":"story","version":1,"id":"state-becomes-a-port","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000181","to":"active"},"payload":{"actor":"human:timo","at":1788480120796,"causation":"cmd-move-story-state-becomes-a-port-1788480120796","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:state-becomes-a-port@3#0~f40fbd81f6be9e7c","executor":null,"recorded_at":"2026-09-04T00:02:00Z"}}
{"entity":"story","version":1,"id":"one-composed-local-placement","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-042-one-composed-local-placement.md:61`. **read**\n\n- [ ] `compose` and `bind` are separate, and `connectors serve` is the only caller of `bind`.\n- [ ] A one-shot CLI command answers with no daemon running.\n- [ ] Zwirn's local placement is a call to `compose`, not a spawned process plus reimplemented flows.\n- [ ] `attach_managed_slack`, `attach_managed_kubernetes` and `submit_connect_credential` are deleted\n      from `products/zwirn/crates/agent-app/src/connectors.rs`.\n- [ ] A declared instance still materialises at open without a human typing a token, so a restart\n      costs no hands. This property came from the concurrent session's Slack work and must survive.\n- [ ] An ADR records the build-dependency edge.\n\n## Context\n\nOne function that composes a local Connector placement from configuration, called by everything that\nneeds one — a one-shot CLI command, `connectors serve`, and Zwirn's local placement — so there is\nexactly one composition path and exactly one owner of auth, secrets, token state and dispatch.\n\nTimo, 2026-08-20: *\"since connectors-cli AND zwirn both need a 'local' variant of those seams, we\nshould put the main composed thing in a shared place inside connectors. Then cli just calls the\nconstructor with config, and zwirn does the same thing.\"*\n\nSource frontmatter: pillar Platform · areas [runtime, cli, zwirn] · priority 3 · design `../design/12-one-owner-for-every-outside-connection.md`. **read**\n\nSource `note:` field, quoted: “depends on S-041; splits compose from bind so the CLI one-shot, `connectors serve` and Zwirn's local placement share one entry point”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-042-one-composed-local-placement.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-042-one-composed-local-placement.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-20 · 1 revision(s)\n- Legacy id `S-042`, recorded as the reference `legacy:S-042`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-042"}],"title":"One composed local placement, called by both the CLI and Zwirn"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-042-one-composed-local-placement.md:61`. **read**\n\n- [ ] `compose` and `bind` are separate, and `connectors serve` is the only caller of `bind`.\n- [ ] A one-shot CLI command answers with no daemon running.\n- [ ] Zwirn's local placement is a call to `compose`, not a spawned process plus reimplemented flows.\n- [ ] `attach_managed_slack`, `attach_managed_kubernetes` and `submit_connect_credential` are deleted\n      from `products/zwirn/crates/agent-app/src/connectors.rs`.\n- [ ] A declared instance still materialises at open without a human typing a token, so a restart\n      costs no hands. This property came from the concurrent session's Slack work and must survive.\n- [ ] An ADR records the build-dependency edge.\n\n## Context\n\nOne function that composes a local Connector placement from configuration, called by everything that\nneeds one — a one-shot CLI command, `connectors serve`, and Zwirn's local placement — so there is\nexactly one composition path and exactly one owner of auth, secrets, token state and dispatch.\n\nTimo, 2026-08-20: *\"since connectors-cli AND zwirn both need a 'local' variant of those seams, we\nshould put the main composed thing in a shared place inside connectors. Then cli just calls the\nconstructor with config, and zwirn does the same thing.\"*\n\nSource frontmatter: pillar Platform · areas [runtime, cli, zwirn] · priority 3 · design `../design/12-one-owner-for-every-outside-connection.md`. **read**\n\nSource `note:` field, quoted: “depends on S-041; splits compose from bind so the CLI one-shot, `connectors serve` and Zwirn's local placement share one entry point”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-042-one-composed-local-placement.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-042-one-composed-local-placement.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-20 · last touched 2026-08-20 · 1 revision(s)\n- Legacy id `S-042`, recorded as the reference `legacy:S-042`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-042"}],"status":"draft","title":"One composed local placement, called by both the CLI and Zwirn"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/one-composed-local-placement"},"payload":{"actor":"human:timo","at":1788480121037,"causation":"cmd-new-story-one-composed-local-placement","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:one-composed-local-placement@1#0~3caf9628beb05719","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"one-composed-local-placement","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:local-product"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000292","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788480121046,"causation":"cmd-story-one-composed-local-placement-derived_from-epic-local-product","change":{"change":"related","relation":"derived_from","target":"epic:local-product"},"correlation":"protocol-artifact-relate","event_id":"story:one-composed-local-placement@1#0~f7b88d6b8a6b9582","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"admission-proofs-become-unforgeable","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-043-admission-proofs-become-unforgeable.md:23`. **read**\n\n- `from_grant_decision` no longer exists; grepping for it finds only history.\n- `AdmittedOperation` has no public constructor; construction sites outside `crates/domain` use\n  the named local-owner path, and its rustdoc states what it asserts and where it must not be used\n  (hosted request handling).\n- The workspace gate is green with no test weakened.\n\n## Context\n\nRemove `AdmittedOperation::from_grant_decision` — a public constructor over caller-supplied\nstrings — and make `AdmittedOperation` reachable only through two honestly named, sealed paths:\na `GrantDecision` proof (hosted; constructor lands with S-044, a module-private placeholder seals\nthe type now) and a local-owner admission for personal placements speaking over the owner's own\nsocket. Every current caller moves to the local-owner path explicitly; behavior does not change.\n\nSource frontmatter: pillar Platform · areas [domain, service, integrations] · priority 1 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantDecision sealed in domain::evaluator (S-044 owns the production constructor); all 22 call sites moved to for_local_owner; the hosted-registry backends that reach it on read-only invocations are flagged for S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-043-admission-proofs-become-unforgeable.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-043-admission-proofs-become-unforgeable.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-043`, recorded as the reference `legacy:S-043`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-043"}],"title":"Admission proofs become unforgeable"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-043-admission-proofs-become-unforgeable.md:23`. **read**\n\n- `from_grant_decision` no longer exists; grepping for it finds only history.\n- `AdmittedOperation` has no public constructor; construction sites outside `crates/domain` use\n  the named local-owner path, and its rustdoc states what it asserts and where it must not be used\n  (hosted request handling).\n- The workspace gate is green with no test weakened.\n\n## Context\n\nRemove `AdmittedOperation::from_grant_decision` — a public constructor over caller-supplied\nstrings — and make `AdmittedOperation` reachable only through two honestly named, sealed paths:\na `GrantDecision` proof (hosted; constructor lands with S-044, a module-private placeholder seals\nthe type now) and a local-owner admission for personal placements speaking over the owner's own\nsocket. Every current caller moves to the local-owner path explicitly; behavior does not change.\n\nSource frontmatter: pillar Platform · areas [domain, service, integrations] · priority 1 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantDecision sealed in domain::evaluator (S-044 owns the production constructor); all 22 call sites moved to for_local_owner; the hosted-registry backends that reach it on read-only invocations are flagged for S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-043-admission-proofs-become-unforgeable.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-043-admission-proofs-become-unforgeable.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-043`, recorded as the reference `legacy:S-043`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-043"}],"status":"draft","title":"Admission proofs become unforgeable"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/admission-proofs-become-unforgeable"},"payload":{"actor":"human:timo","at":1788480121245,"causation":"cmd-new-story-admission-proofs-become-unforgeable","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:admission-proofs-become-unforgeable@1#0~8b383439d2eca07a","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"admission-proofs-become-unforgeable","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000297","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480121253,"causation":"cmd-story-admission-proofs-become-unforgeable-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:admission-proofs-become-unforgeable@1#0~3ac28d183cc91da5","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"admission-proofs-become-unforgeable","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"proposed"},"payload":{"actor":"human:timo","at":1788480121409,"causation":"cmd-move-story-admission-proofs-become-unforgeable-1788480121409","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:admission-proofs-become-unforgeable@2#0~60376ba333507a6a","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"admission-proofs-become-unforgeable","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"active"},"payload":{"actor":"human:timo","at":1788480121567,"causation":"cmd-move-story-admission-proofs-become-unforgeable-1788480121567","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:admission-proofs-become-unforgeable@3#0~ce5da4ae628174de","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"a-grant-decision-is-evaluated-not-asserted","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:24`. **read**\n\n- No store bound → 503; empty store or nothing admitting → 403; no refusal names the axis.\n- Deny beats allow beats predicate, proven by tests including an allow+deny collision.\n- A `GrantDecision` cannot be constructed from any other crate (compile-fail test or doc-tested\n  seal), and an expired decision refuses at use.\n- SQLite and hosted-PostgreSQL store backends pass one shared conformance exercise.\n\n## Context\n\nImplement the `GrantEvaluator` over the S-041 state port: revisioned tenant Grants, selector\nsemantics (risk ceiling, effects subset, idempotency class), explicit exceptions with\ndeny > allow > predicate, inbound events as closed sets. Success is a `GrantDecision` proof —\nprivate fields, module-sealed, bound to issuer, tenant, sub, act, operation ref, Connection ref,\ncatalog generation, description lease, grant revision, canonical input digest, decision, expiry\nand a one-time id.\n\nSource frontmatter: pillar Platform · areas [domain, service, state] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantEvaluator + revisioned Grant records land in domain over the S-041 port; the cfg(test) decision builder is deleted, so the evaluator is the only constructor; grant_conformance runs on memory, SQLite (memory+file) and #[ignore]d live PostgreSQL; hosted wiring stays with S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-044`, recorded as the reference `legacy:S-044`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-044"}],"title":"A Grant decision is evaluated, not asserted"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:24`. **read**\n\n- No store bound → 503; empty store or nothing admitting → 403; no refusal names the axis.\n- Deny beats allow beats predicate, proven by tests including an allow+deny collision.\n- A `GrantDecision` cannot be constructed from any other crate (compile-fail test or doc-tested\n  seal), and an expired decision refuses at use.\n- SQLite and hosted-PostgreSQL store backends pass one shared conformance exercise.\n\n## Context\n\nImplement the `GrantEvaluator` over the S-041 state port: revisioned tenant Grants, selector\nsemantics (risk ceiling, effects subset, idempotency class), explicit exceptions with\ndeny > allow > predicate, inbound events as closed sets. Success is a `GrantDecision` proof —\nprivate fields, module-sealed, bound to issuer, tenant, sub, act, operation ref, Connection ref,\ncatalog generation, description lease, grant revision, canonical input digest, decision, expiry\nand a one-time id.\n\nSource frontmatter: pillar Platform · areas [domain, service, state] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantEvaluator + revisioned Grant records land in domain over the S-041 port; the cfg(test) decision builder is deleted, so the evaluator is the only constructor; grant_conformance runs on memory, SQLite (memory+file) and #[ignore]d live PostgreSQL; hosted wiring stays with S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-044`, recorded as the reference `legacy:S-044`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-044"}],"status":"draft","title":"A Grant decision is evaluated, not asserted"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-grant-decision-is-evaluated-not-asserted"},"payload":{"actor":"human:timo","at":1788480121768,"causation":"cmd-new-story-a-grant-decision-is-evaluated-not-asserted","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-grant-decision-is-evaluated-not-asserted@1#0~ab1e7947ef995f64","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"a-grant-decision-is-evaluated-not-asserted","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000302","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480121781,"causation":"cmd-story-a-grant-decision-is-evaluated-not-asserted-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:a-grant-decision-is-evaluated-not-asserted@1#0~644e379e480d4786","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"a-grant-decision-is-evaluated-not-asserted","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"proposed"},"payload":{"actor":"human:timo","at":1788480121938,"causation":"cmd-move-story-a-grant-decision-is-evaluated-not-asserted-1788480121938","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-grant-decision-is-evaluated-not-asserted@2#0~60376ba333507a6a","executor":null,"recorded_at":"2026-09-04T00:02:01Z"}}
{"entity":"story","version":1,"id":"a-grant-decision-is-evaluated-not-asserted","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"active"},"payload":{"actor":"human:timo","at":1788480122095,"causation":"cmd-move-story-a-grant-decision-is-evaluated-not-asserted-1788480122095","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-grant-decision-is-evaluated-not-asserted@3#0~ce5da4ae628174de","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"an-approval-is-redeemed-exactly-once","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:23`. **read**\n\n- Redemption is atomic under concurrent presentation: exactly one of N concurrent identical\n  requests proceeds (test with real concurrency, not sequential simulation).\n- A mismatched operation, Connection, digest or expired record refuses without naming the axis.\n- Attempted audit exists for every dispatch attempt, including refusals and crashes between\n  redemption and terminal write (proven by a kill-point test or documented crash-recovery scan).\n\n## Context\n\nImplement the approval verifier and redemption store: an externally issued approval record\n(issuer, subject, operation, Connection, canonical input digest, expiry) is verified against the\ninvocation and atomically redeemed — the attempted-audit row and the redemption land in one\ntransaction before dispatch; the terminal outcome row follows after. A second presentation of the\nsame reference refuses and audits as replay.\n\nSource frontmatter: pillar Platform · areas [domain, service, state, audit] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “ApprovalGate in domain::approval — axis-free verification; the one-time claim is a bounded append to approval.redemption.<sha256(reference)> whose payload IS the attempted-audit row; replay is its own journal kind; ApprovalGate::recover is the documented startup crash scan. SQLite concurrency/replay/crash evidence in state-sqlite/tests/approval_gate.rs; S-046 wires the hosted route”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-045`, recorded as the reference `legacy:S-045`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-045"}],"title":"An approval is redeemed exactly once"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:23`. **read**\n\n- Redemption is atomic under concurrent presentation: exactly one of N concurrent identical\n  requests proceeds (test with real concurrency, not sequential simulation).\n- A mismatched operation, Connection, digest or expired record refuses without naming the axis.\n- Attempted audit exists for every dispatch attempt, including refusals and crashes between\n  redemption and terminal write (proven by a kill-point test or documented crash-recovery scan).\n\n## Context\n\nImplement the approval verifier and redemption store: an externally issued approval record\n(issuer, subject, operation, Connection, canonical input digest, expiry) is verified against the\ninvocation and atomically redeemed — the attempted-audit row and the redemption land in one\ntransaction before dispatch; the terminal outcome row follows after. A second presentation of the\nsame reference refuses and audits as replay.\n\nSource frontmatter: pillar Platform · areas [domain, service, state, audit] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “ApprovalGate in domain::approval — axis-free verification; the one-time claim is a bounded append to approval.redemption.<sha256(reference)> whose payload IS the attempted-audit row; replay is its own journal kind; ApprovalGate::recover is the documented startup crash scan. SQLite concurrency/replay/crash evidence in state-sqlite/tests/approval_gate.rs; S-046 wires the hosted route”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-045`, recorded as the reference `legacy:S-045`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-045"}],"status":"draft","title":"An approval is redeemed exactly once"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/an-approval-is-redeemed-exactly-once"},"payload":{"actor":"human:timo","at":1788480122297,"causation":"cmd-new-story-an-approval-is-redeemed-exactly-once","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:an-approval-is-redeemed-exactly-once@1#0~2de2e76373dbbefd","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"an-approval-is-redeemed-exactly-once","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000307","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480122306,"causation":"cmd-story-an-approval-is-redeemed-exactly-once-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:an-approval-is-redeemed-exactly-once@1#0~bf3f6a724b72e287","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"an-approval-is-redeemed-exactly-once","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000073","to":"proposed"},"payload":{"actor":"human:timo","at":1788480122463,"causation":"cmd-move-story-an-approval-is-redeemed-exactly-once-1788480122463","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:an-approval-is-redeemed-exactly-once@2#0~531175becbb86966","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"an-approval-is-redeemed-exactly-once","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000073","to":"active"},"payload":{"actor":"human:timo","at":1788480122620,"causation":"cmd-move-story-an-approval-is-redeemed-exactly-once-1788480122620","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:an-approval-is-redeemed-exactly-once@3#0~482018a8eb7992b2","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"the-hosted-route-enforces-instead-of-refusing","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:21`. **read**\n\n- A caller with a valid route-family token but no admitting Grant is refused (403) on a mutation.\n- A granted mutation with a demanded approval refuses without one, dispatches with one, and\n  refuses the same approval's second use.\n- The pre-existing read-only path is unchanged for callers without Grants covering effects.\n- End-to-end test drives the full chain over the hosted HTTP surface.\n\n## Context\n\nReplace the two hosted fences (approval-reference refusal; ReadOnly+NotRequired re-describe gate)\nwith the real gates: every Invoke passes the GrantEvaluator, and descriptions demanding approval\nadditionally pass the approval gate. Effect-bearing hosted invocation becomes reachable — only\nthrough proofs.\n\nSource frontmatter: pillar Platform · areas [server, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “server::hosted::HostedAuthority (GrantEvaluator + ApprovalGate over the one bound S-041 store) replaces both fences; described ReadOnly+NotRequired keeps the pre-existing policy path when no Grant admits; every enforcement 403 is one byte-identical axis-free body, replay distinct only in the journal; issued approval records live at approval.issued.<sha256(reference)> via the issue_approval bootstrap; the description carries no declared risk/effects/idempotency yet, so evaluation claims worst-case facts and exact allow exceptions are the admission shape until S-047 threads the proofs and facts onward”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-046`, recorded as the reference `legacy:S-046`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-046"}],"title":"The hosted route enforces instead of refusing"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:21`. **read**\n\n- A caller with a valid route-family token but no admitting Grant is refused (403) on a mutation.\n- A granted mutation with a demanded approval refuses without one, dispatches with one, and\n  refuses the same approval's second use.\n- The pre-existing read-only path is unchanged for callers without Grants covering effects.\n- End-to-end test drives the full chain over the hosted HTTP surface.\n\n## Context\n\nReplace the two hosted fences (approval-reference refusal; ReadOnly+NotRequired re-describe gate)\nwith the real gates: every Invoke passes the GrantEvaluator, and descriptions demanding approval\nadditionally pass the approval gate. Effect-bearing hosted invocation becomes reachable — only\nthrough proofs.\n\nSource frontmatter: pillar Platform · areas [server, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “server::hosted::HostedAuthority (GrantEvaluator + ApprovalGate over the one bound S-041 store) replaces both fences; described ReadOnly+NotRequired keeps the pre-existing policy path when no Grant admits; every enforcement 403 is one byte-identical axis-free body, replay distinct only in the journal; issued approval records live at approval.issued.<sha256(reference)> via the issue_approval bootstrap; the description carries no declared risk/effects/idempotency yet, so evaluation claims worst-case facts and exact allow exceptions are the admission shape until S-047 threads the proofs and facts onward”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-046`, recorded as the reference `legacy:S-046`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-046"}],"status":"draft","title":"The hosted route enforces instead of refusing"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-hosted-route-enforces-instead-of-refusing"},"payload":{"actor":"human:timo","at":1788480122820,"causation":"cmd-new-story-the-hosted-route-enforces-instead-of-refusing","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-hosted-route-enforces-instead-of-refusing@1#0~ea88b92ac1d8a722","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"the-hosted-route-enforces-instead-of-refusing","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000312","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480122828,"causation":"cmd-story-the-hosted-route-enforces-instead-of-refusing-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:the-hosted-route-enforces-instead-of-refusing@1#0~20578b0fafe98bad","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"the-hosted-route-enforces-instead-of-refusing","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000208","to":"proposed"},"payload":{"actor":"human:timo","at":1788480122992,"causation":"cmd-move-story-the-hosted-route-enforces-instead-of-refusing-1788480122992","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-hosted-route-enforces-instead-of-refusing@2#0~4454932168dcf416","executor":null,"recorded_at":"2026-09-04T00:02:02Z"}}
{"entity":"story","version":1,"id":"the-hosted-route-enforces-instead-of-refusing","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000208","to":"active"},"payload":{"actor":"human:timo","at":1788480123149,"causation":"cmd-move-story-the-hosted-route-enforces-instead-of-refusing-1788480123149","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-hosted-route-enforces-instead-of-refusing@3#0~39780ed358f90d62","executor":null,"recorded_at":"2026-09-04T00:02:03Z"}}
{"entity":"story","version":1,"id":"effect-backends-trust-only-proofs","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-047-effect-backends-trust-only-proofs.md:21`. **read**\n\n- No integration reads `approval_evidence_ref` for admission; the field reaches backends only as\n  audit correlation.\n- One parameterised invariant in `catalog_invariants.rs` (house rule: one rule, all providers)\n  fails if a future effect operation bypasses the proof chain.\n- The workspace gate is green.\n\n## Context\n\nDelete the per-integration approval-presence checks (gitlab, kubernetes, slack, sip, jira,\nmonitoring) — the proof chain upstream is the authority — and add the catalogue-wide invariant\ntest that no effect-bearing backend code path is reachable without an `AdmittedOperation` built\nfrom proofs.\n\nSource frontmatter: pillar Platform · areas [integrations, testing] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “every Integration's local approval-presence check is deleted (kubernetes hosted+local, monitoring, jira, gitlab, sip, slack, b10x) — the S-043..S-046 proof chain is the only admission authority; InvokeAdmission::Proven now carries the AdmittedOperation and hosted dispatch consumes it by value; catalog_invariants rule 15 scans every integration crate for approval_evidence_ref reads with an empty named audit-correlation allowlist; Slack's event-authorized companion reply (ReplyClaimStore) went with its check — an event that should authorize a reply must be issued as an approval record; ApprovalGate::recover is wired into HostedRuntime startup; GrantEffect::ALL makes the worst-case facts exhaustive by construction”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-047-effect-backends-trust-only-proofs.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-047-effect-backends-trust-only-proofs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-047`, recorded as the reference `legacy:S-047`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-047"}],"title":"Effect backends trust only proofs"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-047-effect-backends-trust-only-proofs.md:21`. **read**\n\n- No integration reads `approval_evidence_ref` for admission; the field reaches backends only as\n  audit correlation.\n- One parameterised invariant in `catalog_invariants.rs` (house rule: one rule, all providers)\n  fails if a future effect operation bypasses the proof chain.\n- The workspace gate is green.\n\n## Context\n\nDelete the per-integration approval-presence checks (gitlab, kubernetes, slack, sip, jira,\nmonitoring) — the proof chain upstream is the authority — and add the catalogue-wide invariant\ntest that no effect-bearing backend code path is reachable without an `AdmittedOperation` built\nfrom proofs.\n\nSource frontmatter: pillar Platform · areas [integrations, testing] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “every Integration's local approval-presence check is deleted (kubernetes hosted+local, monitoring, jira, gitlab, sip, slack, b10x) — the S-043..S-046 proof chain is the only admission authority; InvokeAdmission::Proven now carries the AdmittedOperation and hosted dispatch consumes it by value; catalog_invariants rule 15 scans every integration crate for approval_evidence_ref reads with an empty named audit-correlation allowlist; Slack's event-authorized companion reply (ReplyClaimStore) went with its check — an event that should authorize a reply must be issued as an approval record; ApprovalGate::recover is wired into HostedRuntime startup; GrantEffect::ALL makes the worst-case facts exhaustive by construction”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-047-effect-backends-trust-only-proofs.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-047-effect-backends-trust-only-proofs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-047`, recorded as the reference `legacy:S-047`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-047"}],"status":"draft","title":"Effect backends trust only proofs"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/effect-backends-trust-only-proofs"},"payload":{"actor":"human:timo","at":1788480123352,"causation":"cmd-new-story-effect-backends-trust-only-proofs","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:effect-backends-trust-only-proofs@1#0~ccb75941e42a591b","executor":null,"recorded_at":"2026-09-04T00:02:03Z"}}
{"entity":"story","version":1,"id":"effect-backends-trust-only-proofs","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000317","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480123360,"causation":"cmd-story-effect-backends-trust-only-proofs-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:effect-backends-trust-only-proofs@1#0~3e944d5a9f3107ac","executor":null,"recorded_at":"2026-09-04T00:02:03Z"}}
{"entity":"story","version":1,"id":"effect-backends-trust-only-proofs","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000103","to":"proposed"},"payload":{"actor":"human:timo","at":1788480123519,"causation":"cmd-move-story-effect-backends-trust-only-proofs-1788480123519","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:effect-backends-trust-only-proofs@2#0~2a5bc5f52c4eb9b6","executor":null,"recorded_at":"2026-09-04T00:02:03Z"}}
{"entity":"story","version":1,"id":"effect-backends-trust-only-proofs","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000103","to":"active"},"payload":{"actor":"human:timo","at":1788480123678,"causation":"cmd-move-story-effect-backends-trust-only-proofs-1788480123678","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:effect-backends-trust-only-proofs@3#0~bba00d1598fe3782","executor":null,"recorded_at":"2026-09-04T00:02:03Z"}}
{"entity":"story","version":1,"id":"a-companion-reply-is-claimed-exactly-once-locally","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:24`. **read**\n\n- On the local placement, two presentations of the same companion event produce exactly one\n  outward Slack reply; the second refuses and is journaled, proven by a real-concurrency test.\n- Catalog invariant rule 15 stays green without weakening its empty-by-default posture.\n- The SIP `approval_evidence_ref` config field, which no code consumes since S-047, is retired\n  from `connectors-config` (`personal.rs`), the examples, and the connectors-cli README — or its\n  retention is justified in the config docs.\n\n## Context\n\nS-047 deleted the Slack backend's `ReplyClaimStore` with the approval-presence check it rode on,\nso on the local placement nothing enforces one reply per companion mention any more: a retried\ninvoke can post the same Slack reply twice (S-047 review, 2026-08-23). Restore the exactly-once\nclaim as an honest local mechanism — a durable one-time claim keyed on the triggering event,\nmade at the dispatch seam rather than by the Integration re-reading `approval_evidence_ref`\n(which catalog invariant rule 15 now forbids; its named allowlist\n`APPROVAL_EVIDENCE_AUDIT_CORRELATION_USES` is the sanctioned escape hatch if correlation by that\nfield turns out to be the right shape).\n\nSource frontmatter: pillar Platform · areas [integrations, state, config] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 3 revision(s)\n- Legacy id `S-048`, recorded as the reference `legacy:S-048`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-048"}],"title":"A companion reply is claimed exactly once, locally too"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:24`. **read**\n\n- On the local placement, two presentations of the same companion event produce exactly one\n  outward Slack reply; the second refuses and is journaled, proven by a real-concurrency test.\n- Catalog invariant rule 15 stays green without weakening its empty-by-default posture.\n- The SIP `approval_evidence_ref` config field, which no code consumes since S-047, is retired\n  from `connectors-config` (`personal.rs`), the examples, and the connectors-cli README — or its\n  retention is justified in the config docs.\n\n## Context\n\nS-047 deleted the Slack backend's `ReplyClaimStore` with the approval-presence check it rode on,\nso on the local placement nothing enforces one reply per companion mention any more: a retried\ninvoke can post the same Slack reply twice (S-047 review, 2026-08-23). Restore the exactly-once\nclaim as an honest local mechanism — a durable one-time claim keyed on the triggering event,\nmade at the dispatch seam rather than by the Integration re-reading `approval_evidence_ref`\n(which catalog invariant rule 15 now forbids; its named allowlist\n`APPROVAL_EVIDENCE_AUDIT_CORRELATION_USES` is the sanctioned escape hatch if correlation by that\nfield turns out to be the right shape).\n\nSource frontmatter: pillar Platform · areas [integrations, state, config] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 3 revision(s)\n- Legacy id `S-048`, recorded as the reference `legacy:S-048`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-048"}],"status":"draft","title":"A companion reply is claimed exactly once, locally too"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-companion-reply-is-claimed-exactly-once-locally"},"payload":{"actor":"human:timo","at":1788480123859,"causation":"cmd-new-story-a-companion-reply-is-claimed-exactly-once-locally","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-companion-reply-is-claimed-exactly-once-locally@1#0~67e44acd7dbde7bd","executor":null,"recorded_at":"2026-09-04T00:02:03Z"}}
{"entity":"story","version":1,"id":"a-companion-reply-is-claimed-exactly-once-locally","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000322","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480123868,"causation":"cmd-story-a-companion-reply-is-claimed-exactly-once-locally-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:a-companion-reply-is-claimed-exactly-once-locally@1#0~c6752a15b803ec9c","executor":null,"recorded_at":"2026-09-04T00:02:03Z"}}
{"entity":"story","version":1,"id":"a-companion-reply-is-claimed-exactly-once-locally","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"proposed"},"payload":{"actor":"human:timo","at":1788480124028,"causation":"cmd-move-story-a-companion-reply-is-claimed-exactly-once-locally-1788480124028","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-companion-reply-is-claimed-exactly-once-locally@2#0~60376ba333507a6a","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"a-companion-reply-is-claimed-exactly-once-locally","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"active"},"payload":{"actor":"human:timo","at":1788480124189,"causation":"cmd-move-story-a-companion-reply-is-claimed-exactly-once-locally-1788480124189","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-companion-reply-is-claimed-exactly-once-locally@3#0~ce5da4ae628174de","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"a-session-signal-carries-authority","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-049-a-session-signal-carries-authority.md:22`. **read**\n\n- An effect-bearing session signal is refused unless a Grant admits it; refusals are axis-free\n  and journaled, matching the invoke path's semantics.\n- The catalogue invariant family covers the session-signal seam so a future signal route cannot\n  bypass admission silently.\n- Hosted single-replica assumption for `ApprovalGate::recover` is either stated in design 13 or\n  replaced with a fencing mechanism (S-047 review open question rides along here with the\n  session-lifecycle work).\n\n## Context\n\n`SessionSignal` dispatches effect-bearing input (a keypress cannot be undone) behind scope and\noperator-group policy only — no `GrantEvaluator`, no proof (S-046 and S-047 reviews, 2026-08-23).\nIt escaped the enforced-authority epic because it carries no operation ref, description, or\nConnection: nothing a `GrantRequest` can name. Give session signals a describable authority —\neither a described session-signal operation per provider or a session-scoped admission derived\nfrom the Grant that admitted the session's creation — and bring the dispatch behind it.\n\nSource frontmatter: pillar Platform · areas [server, domain, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-049-a-session-signal-carries-authority.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-049-a-session-signal-carries-authority.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 4 revision(s)\n- Legacy id `S-049`, recorded as the reference `legacy:S-049`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-049"}],"title":"A session signal carries authority"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-049-a-session-signal-carries-authority.md:22`. **read**\n\n- An effect-bearing session signal is refused unless a Grant admits it; refusals are axis-free\n  and journaled, matching the invoke path's semantics.\n- The catalogue invariant family covers the session-signal seam so a future signal route cannot\n  bypass admission silently.\n- Hosted single-replica assumption for `ApprovalGate::recover` is either stated in design 13 or\n  replaced with a fencing mechanism (S-047 review open question rides along here with the\n  session-lifecycle work).\n\n## Context\n\n`SessionSignal` dispatches effect-bearing input (a keypress cannot be undone) behind scope and\noperator-group policy only — no `GrantEvaluator`, no proof (S-046 and S-047 reviews, 2026-08-23).\nIt escaped the enforced-authority epic because it carries no operation ref, description, or\nConnection: nothing a `GrantRequest` can name. Give session signals a describable authority —\neither a described session-signal operation per provider or a session-scoped admission derived\nfrom the Grant that admitted the session's creation — and bring the dispatch behind it.\n\nSource frontmatter: pillar Platform · areas [server, domain, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-049-a-session-signal-carries-authority.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-049-a-session-signal-carries-authority.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 4 revision(s)\n- Legacy id `S-049`, recorded as the reference `legacy:S-049`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-049"}],"status":"draft","title":"A session signal carries authority"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-session-signal-carries-authority"},"payload":{"actor":"human:timo","at":1788480124395,"causation":"cmd-new-story-a-session-signal-carries-authority","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-session-signal-carries-authority@1#0~c96f330d623d38c8","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"a-session-signal-carries-authority","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000327","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480124403,"causation":"cmd-story-a-session-signal-carries-authority-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:a-session-signal-carries-authority@1#0~8418b91c1b3316dd","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"a-session-signal-carries-authority","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"proposed"},"payload":{"actor":"human:timo","at":1788480124563,"causation":"cmd-move-story-a-session-signal-carries-authority-1788480124563","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-session-signal-carries-authority@2#0~0710c15da85fdf8f","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"a-session-signal-carries-authority","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"active"},"payload":{"actor":"human:timo","at":1788480124723,"causation":"cmd-move-story-a-session-signal-carries-authority-1788480124723","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-session-signal-carries-authority@3#0~6ea9aeb43e6711bf","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"the-claims-journal-survives-a-full-life","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-050-the-claims-journal-survives-a-full-life.md:22`. **read**\n\n- A claims journal at capacity with expired entries accepts a new claim after pruning; a journal\n  at capacity with only live entries still refuses, and the refusal names the condition.\n- The replay-refusal journal row is asserted under an injected append failure — either the refusal\n  still journals through a fallback, or the degraded posture is declared where the semantics live.\n- Pruning never weakens exactly-once: a pruned claim's event is past its approval window, proven\n  by a test that replays a pruned event and is refused on window grounds.\n\n## Context\n\nThe local event-reply claims journal introduced by S-048 refuses all further event-authorized\nreplies once full — 10k claims / 4 MiB, no pruning (S-048 implementation notes, 2026-08-23), and\nthe replay-refusal journal append is best-effort, so a refusal under a failing store is enforced\nbut unjournaled (S-048 review minor). Give the journal a bounded life a deployment can live with:\nexpired-claim pruning (an event's approval window bounds how long its claim matters), an explicit\nposture when the store is unavailable, and a journaled refusal that is proven, not hoped for.\n\nSource frontmatter: pillar Platform · areas [state, integrations] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-050-the-claims-journal-survives-a-full-life.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-050-the-claims-journal-survives-a-full-life.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 1 revision(s)\n- Legacy id `S-050`, recorded as the reference `legacy:S-050`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-050"}],"title":"The claims journal survives a full life"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-050-the-claims-journal-survives-a-full-life.md:22`. **read**\n\n- A claims journal at capacity with expired entries accepts a new claim after pruning; a journal\n  at capacity with only live entries still refuses, and the refusal names the condition.\n- The replay-refusal journal row is asserted under an injected append failure — either the refusal\n  still journals through a fallback, or the degraded posture is declared where the semantics live.\n- Pruning never weakens exactly-once: a pruned claim's event is past its approval window, proven\n  by a test that replays a pruned event and is refused on window grounds.\n\n## Context\n\nThe local event-reply claims journal introduced by S-048 refuses all further event-authorized\nreplies once full — 10k claims / 4 MiB, no pruning (S-048 implementation notes, 2026-08-23), and\nthe replay-refusal journal append is best-effort, so a refusal under a failing store is enforced\nbut unjournaled (S-048 review minor). Give the journal a bounded life a deployment can live with:\nexpired-claim pruning (an event's approval window bounds how long its claim matters), an explicit\nposture when the store is unavailable, and a journaled refusal that is proven, not hoped for.\n\nSource frontmatter: pillar Platform · areas [state, integrations] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-050-the-claims-journal-survives-a-full-life.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-050-the-claims-journal-survives-a-full-life.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 1 revision(s)\n- Legacy id `S-050`, recorded as the reference `legacy:S-050`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-050"}],"status":"draft","title":"The claims journal survives a full life"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-claims-journal-survives-a-full-life"},"payload":{"actor":"human:timo","at":1788480124905,"causation":"cmd-new-story-the-claims-journal-survives-a-full-life","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-claims-journal-survives-a-full-life@1#0~9abb08f0ba47ba5e","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"the-claims-journal-survives-a-full-life","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000332","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788480124914,"causation":"cmd-story-the-claims-journal-survives-a-full-life-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:the-claims-journal-survives-a-full-life@1#0~74d87e82dbcc9183","executor":null,"recorded_at":"2026-09-04T00:02:04Z"}}
{"entity":"story","version":1,"id":"the-hosted-server-speaks-mcp-through-the-admission-seam","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:28`. **read**\n\n- `tools/list` returns exactly the three meta-tools; a read-group principal's `tool_search`\n  lists the k8s entries its seam results support, a group-less principal's lists none.\n- An op-backed `tool_invoke` provably performs Describe then Invoke through the decided seam\n  with a matching lease; datasource-backed tools route through the datasource seam; refusal\n  paths surface as `isError` + `structuredContent.code` (`not_granted` without\n  `connectors.invoke`, `approval_required` with evidence passthrough proven, stale-authority\n  retried once); 401/-32700/-32602/413 behave per design 14.\n- A catalog-invariant sibling of rule 16 forbids `.backend`/`ConnectorBackend`/\n  `dispatch_admitted`/`admit_` tokens in the MCP modules' production lines, and rule 16's doc\n  comment names `/mcp` as a funneled entry.\n- `bash scripts/gate.sh` exits 0 (module fence: `hosted.rs` stays under 1500 — the\n  connect/oauth handler move to `hosted/connect.rs` is the sanctioned valve if needed).\n\n## Context\n\nAdd `POST {base_path}/mcp` to the hosted router: stateless JSON-RPC 2.0 MCP (revisions\n2025-03-26/2025-06-18; no SSE, no session id, no new dependency) with methods `initialize`,\n`notifications/*` (202), `ping`, `tools/list` (exactly `tool_search`, `tool_describe`,\n`tool_invoke`), `tools/call`. Split the private `operation()`/`datasource()` handlers into\nverify + `*_decided` halves inside `hosted.rs`; the MCP modules (`hosted/mcp.rs`,\n`hosted/mcp/toolset.rs`) verify the bearer once and reach backends only through the decided\nseams. Toolset v1 per design 14 (`k8s_namespace_list`, `k8s_deployment_list`,\n`k8s_deployment_status`, `k8s_pod_status`, `k8s_pod_logs`), role-filtered by the caller's own\nseam results; `description_ref` hidden with server-side re-describe and one stale-authority\nretry. Include `k8s_pod_logs` in the table even though S-054 lands the operation — the\nrequirement mechanism hides it until the op exists, which also makes the two stories\norder-independent.\n\nSource frontmatter: pillar Platform · areas [server, testing] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-053`, recorded as the reference `legacy:S-053`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-053"}],"title":"The hosted server speaks MCP through the admission seam"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:28`. **read**\n\n- `tools/list` returns exactly the three meta-tools; a read-group principal's `tool_search`\n  lists the k8s entries its seam results support, a group-less principal's lists none.\n- An op-backed `tool_invoke` provably performs Describe then Invoke through the decided seam\n  with a matching lease; datasource-backed tools route through the datasource seam; refusal\n  paths surface as `isError` + `structuredContent.code` (`not_granted` without\n  `connectors.invoke`, `approval_required` with evidence passthrough proven, stale-authority\n  retried once); 401/-32700/-32602/413 behave per design 14.\n- A catalog-invariant sibling of rule 16 forbids `.backend`/`ConnectorBackend`/\n  `dispatch_admitted`/`admit_` tokens in the MCP modules' production lines, and rule 16's doc\n  comment names `/mcp` as a funneled entry.\n- `bash scripts/gate.sh` exits 0 (module fence: `hosted.rs` stays under 1500 — the\n  connect/oauth handler move to `hosted/connect.rs` is the sanctioned valve if needed).\n\n## Context\n\nAdd `POST {base_path}/mcp` to the hosted router: stateless JSON-RPC 2.0 MCP (revisions\n2025-03-26/2025-06-18; no SSE, no session id, no new dependency) with methods `initialize`,\n`notifications/*` (202), `ping`, `tools/list` (exactly `tool_search`, `tool_describe`,\n`tool_invoke`), `tools/call`. Split the private `operation()`/`datasource()` handlers into\nverify + `*_decided` halves inside `hosted.rs`; the MCP modules (`hosted/mcp.rs`,\n`hosted/mcp/toolset.rs`) verify the bearer once and reach backends only through the decided\nseams. Toolset v1 per design 14 (`k8s_namespace_list`, `k8s_deployment_list`,\n`k8s_deployment_status`, `k8s_pod_status`, `k8s_pod_logs`), role-filtered by the caller's own\nseam results; `description_ref` hidden with server-side re-describe and one stale-authority\nretry. Include `k8s_pod_logs` in the table even though S-054 lands the operation — the\nrequirement mechanism hides it until the op exists, which also makes the two stories\norder-independent.\n\nSource frontmatter: pillar Platform · areas [server, testing] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-053`, recorded as the reference `legacy:S-053`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-053"}],"status":"draft","title":"The hosted server speaks MCP through the admission seam"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-hosted-server-speaks-mcp-through-the-admission-seam"},"payload":{"actor":"human:timo","at":1788480125491,"causation":"cmd-new-story-the-hosted-server-speaks-mcp-through-the-admission-seam","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-hosted-server-speaks-mcp-through-the-admission-seam@1#0~75508076e9d839f7","executor":null,"recorded_at":"2026-09-04T00:02:05Z"}}
{"entity":"story","version":1,"id":"the-hosted-server-speaks-mcp-through-the-admission-seam","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:mcp-entry"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000337","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788480125502,"causation":"cmd-story-the-hosted-server-speaks-mcp-through-the-admission-seam-derived_from-epic-mcp-entry","change":{"change":"related","relation":"derived_from","target":"epic:mcp-entry"},"correlation":"protocol-artifact-relate","event_id":"story:the-hosted-server-speaks-mcp-through-the-admission-seam@1#0~08063dac45749033","executor":null,"recorded_at":"2026-09-04T00:02:05Z"}}
{"entity":"story","version":1,"id":"the-hosted-server-speaks-mcp-through-the-admission-seam","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000223","to":"proposed"},"payload":{"actor":"human:timo","at":1788480125663,"causation":"cmd-move-story-the-hosted-server-speaks-mcp-through-the-admission-seam-1788480125663","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-hosted-server-speaks-mcp-through-the-admission-seam@2#0~dcdfb08b8ae8a619","executor":null,"recorded_at":"2026-09-04T00:02:05Z"}}
{"entity":"story","version":1,"id":"the-hosted-server-speaks-mcp-through-the-admission-seam","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000223","to":"active"},"payload":{"actor":"human:timo","at":1788480125825,"causation":"cmd-move-story-the-hosted-server-speaks-mcp-through-the-admission-seam-1788480125825","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-hosted-server-speaks-mcp-through-the-admission-seam@3#0~93742650189eae11","executor":null,"recorded_at":"2026-09-04T00:02:05Z"}}
{"entity":"story","version":1,"id":"pod-logs-are-a-hosted-read-only-kubernetes-operation","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:25`. **read**\n\n- Search lists `kubernetes.pod.logs` for read-group principals and hides it otherwise;\n  describe returns input/output schemas and a lease; invoke passes container/tail_lines/\n  since_seconds through to the reader, proven against a fake.\n- A non-admitted namespace refuses `not_granted`; a stale lease refuses `stale_authority`;\n  an oversized log body yields front-trimmed output with `truncated: true` inside a\n  validating envelope; upstream 403 maps to `not_granted`; input caps are enforced.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nAdd `kubernetes.pod.logs` (effect read_only, approval not_required) to the hosted Kubernetes\nintegration per design 14: input `{namespace, pod, container?, tail_lines 1..=1000 default\n200, since_seconds 1..=86400?}`, backed by the pod log subresource with `limitBytes` 128 KiB,\noutput `{namespace, pod, container?, tail_lines, text, truncated}` with serialize-then-trim-\noldest-lines so the envelope never trips `result_too_large`. A default `DeploymentReader`\ntrait method refuses `unavailable` so the local placement and test fakes compile unchanged.\nGate admission on the same `namespace_access.read_groups` as deployment status via one arm in\n`admits_kubernetes_operation`; search advertises the operation only under read access, with\n`log`/`logs`/`pod` terms.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-054`, recorded as the reference `legacy:S-054`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-054"}],"title":"Pod logs are a hosted read-only Kubernetes operation"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:25`. **read**\n\n- Search lists `kubernetes.pod.logs` for read-group principals and hides it otherwise;\n  describe returns input/output schemas and a lease; invoke passes container/tail_lines/\n  since_seconds through to the reader, proven against a fake.\n- A non-admitted namespace refuses `not_granted`; a stale lease refuses `stale_authority`;\n  an oversized log body yields front-trimmed output with `truncated: true` inside a\n  validating envelope; upstream 403 maps to `not_granted`; input caps are enforced.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nAdd `kubernetes.pod.logs` (effect read_only, approval not_required) to the hosted Kubernetes\nintegration per design 14: input `{namespace, pod, container?, tail_lines 1..=1000 default\n200, since_seconds 1..=86400?}`, backed by the pod log subresource with `limitBytes` 128 KiB,\noutput `{namespace, pod, container?, tail_lines, text, truncated}` with serialize-then-trim-\noldest-lines so the envelope never trips `result_too_large`. A default `DeploymentReader`\ntrait method refuses `unavailable` so the local placement and test fakes compile unchanged.\nGate admission on the same `namespace_access.read_groups` as deployment status via one arm in\n`admits_kubernetes_operation`; search advertises the operation only under read access, with\n`log`/`logs`/`pod` terms.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-054`, recorded as the reference `legacy:S-054`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-054"}],"status":"draft","title":"Pod logs are a hosted read-only Kubernetes operation"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/pod-logs-are-a-hosted-read-only-kubernetes-operation"},"payload":{"actor":"human:timo","at":1788480126008,"causation":"cmd-new-story-pod-logs-are-a-hosted-read-only-kubernetes-operation","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:pod-logs-are-a-hosted-read-only-kubernetes-operation@1#0~8421b8fbca39c8bb","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"pod-logs-are-a-hosted-read-only-kubernetes-operation","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:mcp-entry"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000342","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788480126016,"causation":"cmd-story-pod-logs-are-a-hosted-read-only-kubernetes-operation-derived_from-epic-mcp-entry","change":{"change":"related","relation":"derived_from","target":"epic:mcp-entry"},"correlation":"protocol-artifact-relate","event_id":"story:pod-logs-are-a-hosted-read-only-kubernetes-operation@1#0~79fb3379233396a3","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"pod-logs-are-a-hosted-read-only-kubernetes-operation","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000172","to":"proposed"},"payload":{"actor":"human:timo","at":1788480126176,"causation":"cmd-move-story-pod-logs-are-a-hosted-read-only-kubernetes-operation-1788480126176","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:pod-logs-are-a-hosted-read-only-kubernetes-operation@2#0~d749d744e919c1ee","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"pod-logs-are-a-hosted-read-only-kubernetes-operation","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000172","to":"active"},"payload":{"actor":"human:timo","at":1788480126342,"causation":"cmd-move-story-pod-logs-are-a-hosted-read-only-kubernetes-operation-1788480126342","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:pod-logs-are-a-hosted-read-only-kubernetes-operation@3#0~22ff5ef3f0124eca","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"a-self-service-scope-subset-needs-no-operator","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:25`. **read**\n\n- In beyond10x/identity: a member principal mints `connectors.catalog.read connectors.invoke`\n  (and the full self-service four); `connectors.catalog.read connectors.events.read` still\n  demands `operator`; operator behaviour is unchanged — all proven in the vocabulary test.\n- Identity's `bash scripts/gate.sh` exits 0; the identity commit is referenced here on close.\n\n## Context\n\nThis story's change lands in the separate beyond10x/identity repository (from `a664b99b`); it\nis tracked here so the board shows the end-to-end dependency. Identity's\n`admitted_connector_scope` compares the whole canonical scope string against four single-scope\nliterals, so any multi-scope request demands the `operator` group — an `sre`/`dev` principal\ncannot hold `connectors.catalog.read connectors.invoke` in one token, making the MCP endpoint\noperator-only in practice. Admit any subset of the four self-service scopes\n(`connectors.catalog.read`, `connectors.connections.self`, `connectors.events.self`,\n`connectors.invoke`) for any authenticated principal; any other member in the set still\ndemands `operator`.\n\nSource frontmatter: pillar Platform · areas [identity] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-055`, recorded as the reference `legacy:S-055`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-055"}],"title":"A self-service scope subset needs no operator"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:25`. **read**\n\n- In beyond10x/identity: a member principal mints `connectors.catalog.read connectors.invoke`\n  (and the full self-service four); `connectors.catalog.read connectors.events.read` still\n  demands `operator`; operator behaviour is unchanged — all proven in the vocabulary test.\n- Identity's `bash scripts/gate.sh` exits 0; the identity commit is referenced here on close.\n\n## Context\n\nThis story's change lands in the separate beyond10x/identity repository (from `a664b99b`); it\nis tracked here so the board shows the end-to-end dependency. Identity's\n`admitted_connector_scope` compares the whole canonical scope string against four single-scope\nliterals, so any multi-scope request demands the `operator` group — an `sre`/`dev` principal\ncannot hold `connectors.catalog.read connectors.invoke` in one token, making the MCP endpoint\noperator-only in practice. Admit any subset of the four self-service scopes\n(`connectors.catalog.read`, `connectors.connections.self`, `connectors.events.self`,\n`connectors.invoke`) for any authenticated principal; any other member in the set still\ndemands `operator`.\n\nSource frontmatter: pillar Platform · areas [identity] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-055`, recorded as the reference `legacy:S-055`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-055"}],"status":"draft","title":"A self-service scope subset needs no operator"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-self-service-scope-subset-needs-no-operator"},"payload":{"actor":"human:timo","at":1788480126548,"causation":"cmd-new-story-a-self-service-scope-subset-needs-no-operator","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-self-service-scope-subset-needs-no-operator@1#0~d2b5e2bc16bedcb8","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"a-self-service-scope-subset-needs-no-operator","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:mcp-entry"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000347","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788480126560,"causation":"cmd-story-a-self-service-scope-subset-needs-no-operator-derived_from-epic-mcp-entry","change":{"change":"related","relation":"derived_from","target":"epic:mcp-entry"},"correlation":"protocol-artifact-relate","event_id":"story:a-self-service-scope-subset-needs-no-operator@1#0~0a872492c08990a2","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"a-self-service-scope-subset-needs-no-operator","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"proposed"},"payload":{"actor":"human:timo","at":1788480126721,"causation":"cmd-move-story-a-self-service-scope-subset-needs-no-operator-1788480126721","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-self-service-scope-subset-needs-no-operator@2#0~0710c15da85fdf8f","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"a-self-service-scope-subset-needs-no-operator","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"active"},"payload":{"actor":"human:timo","at":1788480126884,"causation":"cmd-move-story-a-self-service-scope-subset-needs-no-operator-1788480126884","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-self-service-scope-subset-needs-no-operator@3#0~6ea9aeb43e6711bf","executor":null,"recorded_at":"2026-09-04T00:02:06Z"}}
{"entity":"story","version":1,"id":"the-cli-bridges-mcp-over-stdio","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:22`. **read**\n\n- A stdio MCP client completes initialize/tools-list/tools-call against a hosted deployment\n  through the bridge, across a token expiry, without manual re-auth.\n- The thin-CLI architecture fence stays green with a dated, bounded cap raise covering parser and\n  dispatch declarations only.\n\n## Context\n\nInteractive MCP clients hold a static bearer, but identity access tokens live 300 seconds, so\nthe hosted `/mcp` endpoint alone serves scripted callers well and interactive ones poorly.\nAdd a `connectors mcp` subcommand: a stdio MCP server that proxies to the hosted `/mcp`\nendpoint with automatic per-scope token refresh from the login session established by\n`connectors login`. The behaviour belongs in a `connectors-client` module; the subcommand itself\nstays a thin frontend.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-056`, recorded as the reference `legacy:S-056`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-056"}],"title":"The CLI bridges MCP over stdio"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:22`. **read**\n\n- A stdio MCP client completes initialize/tools-list/tools-call against a hosted deployment\n  through the bridge, across a token expiry, without manual re-auth.\n- The thin-CLI architecture fence stays green with a dated, bounded cap raise covering parser and\n  dispatch declarations only.\n\n## Context\n\nInteractive MCP clients hold a static bearer, but identity access tokens live 300 seconds, so\nthe hosted `/mcp` endpoint alone serves scripted callers well and interactive ones poorly.\nAdd a `connectors mcp` subcommand: a stdio MCP server that proxies to the hosted `/mcp`\nendpoint with automatic per-scope token refresh from the login session established by\n`connectors login`. The behaviour belongs in a `connectors-client` module; the subcommand itself\nstays a thin frontend.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-056`, recorded as the reference `legacy:S-056`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-056"}],"status":"draft","title":"The CLI bridges MCP over stdio"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-cli-bridges-mcp-over-stdio"},"payload":{"actor":"human:timo","at":1788480127089,"causation":"cmd-new-story-the-cli-bridges-mcp-over-stdio","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-cli-bridges-mcp-over-stdio@1#0~b41c2106bc466131","executor":null,"recorded_at":"2026-09-04T00:02:07Z"}}
{"entity":"story","version":1,"id":"the-cli-bridges-mcp-over-stdio","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:mcp-entry"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000352","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788480127098,"causation":"cmd-story-the-cli-bridges-mcp-over-stdio-derived_from-epic-mcp-entry","change":{"change":"related","relation":"derived_from","target":"epic:mcp-entry"},"correlation":"protocol-artifact-relate","event_id":"story:the-cli-bridges-mcp-over-stdio@1#0~8afbef1c4b4d2e70","executor":null,"recorded_at":"2026-09-04T00:02:07Z"}}
{"entity":"story","version":1,"id":"the-cli-bridges-mcp-over-stdio","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000217","to":"proposed"},"payload":{"actor":"human:timo","at":1788480127261,"causation":"cmd-move-story-the-cli-bridges-mcp-over-stdio-1788480127261","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-cli-bridges-mcp-over-stdio@2#0~3a550283a612fff2","executor":null,"recorded_at":"2026-09-04T00:02:07Z"}}
{"entity":"story","version":1,"id":"the-cli-bridges-mcp-over-stdio","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000217","to":"active"},"payload":{"actor":"human:timo","at":1788480127428,"causation":"cmd-move-story-the-cli-bridges-mcp-over-stdio-1788480127428","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-cli-bridges-mcp-over-stdio@3#0~c6486cb341557e76","executor":null,"recorded_at":"2026-09-04T00:02:07Z"}}
{"entity":"story","version":1,"id":"mysql-and-postgresql-become-connectors","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:24`. **read**\n\n- Providers `mysql` and `postgresql` exist with permanent ids/authorities per the\n  adding-a-connector discipline; their read-only operations carry honest risk/effects/\n  idempotency and JSON Schemas, and `catalog build && diff && check` stays clean.\n- A bounded query against a real database (containerized in tests) returns rows inside the\n  result cap with honest truncation, and a write statement (INSERT/UPDATE/DDL) is refused\n  before reaching the database, proven by tests.\n- No credential value appears in config, catalog, logs, or errors — the custody path is a\n  reference; the brand and secret fences stay green.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe platform governs HTTP vendors and Kubernetes but not the databases the product actually\nruns on. Give MySQL and PostgreSQL permanent provider identities and a read-only operation\nsurface first — bounded query (row and byte capped), list-schemas/tables, describe-table —\nexecuted by a SQL protocol driver in the mold of the existing non-HTTP drivers, behind the\nsame admission seam as every operation. Credentials arrive through platform custody\n(connection secret references, Vault), never from the caller; connection configs name a\nreference, not a value. Effect posture: everything in this story is `read_only` and rides the\nread path; mutations are a later grant-gated story.\n\nSource frontmatter: pillar Platform · areas [catalog, integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-058`, recorded as the reference `legacy:S-058`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-058"}],"title":"MySQL and PostgreSQL become connectors"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:24`. **read**\n\n- Providers `mysql` and `postgresql` exist with permanent ids/authorities per the\n  adding-a-connector discipline; their read-only operations carry honest risk/effects/\n  idempotency and JSON Schemas, and `catalog build && diff && check` stays clean.\n- A bounded query against a real database (containerized in tests) returns rows inside the\n  result cap with honest truncation, and a write statement (INSERT/UPDATE/DDL) is refused\n  before reaching the database, proven by tests.\n- No credential value appears in config, catalog, logs, or errors — the custody path is a\n  reference; the brand and secret fences stay green.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe platform governs HTTP vendors and Kubernetes but not the databases the product actually\nruns on. Give MySQL and PostgreSQL permanent provider identities and a read-only operation\nsurface first — bounded query (row and byte capped), list-schemas/tables, describe-table —\nexecuted by a SQL protocol driver in the mold of the existing non-HTTP drivers, behind the\nsame admission seam as every operation. Credentials arrive through platform custody\n(connection secret references, Vault), never from the caller; connection configs name a\nreference, not a value. Effect posture: everything in this story is `read_only` and rides the\nread path; mutations are a later grant-gated story.\n\nSource frontmatter: pillar Platform · areas [catalog, integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-058`, recorded as the reference `legacy:S-058`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-058"}],"status":"draft","title":"MySQL and PostgreSQL become connectors"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/mysql-and-postgresql-become-connectors"},"payload":{"actor":"human:timo","at":1788480127786,"causation":"cmd-new-story-mysql-and-postgresql-become-connectors","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:mysql-and-postgresql-become-connectors@1#0~8eba8a0dbd074ec3","executor":null,"recorded_at":"2026-09-04T00:02:07Z"}}
{"entity":"story","version":1,"id":"mysql-and-postgresql-become-connectors","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000357","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480127797,"causation":"cmd-story-mysql-and-postgresql-become-connectors-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:mysql-and-postgresql-become-connectors@1#0~17d716548450ab7b","executor":null,"recorded_at":"2026-09-04T00:02:07Z"}}
{"entity":"story","version":1,"id":"mysql-and-postgresql-become-connectors","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000151","to":"proposed"},"payload":{"actor":"human:timo","at":1788480127962,"causation":"cmd-move-story-mysql-and-postgresql-become-connectors-1788480127962","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:mysql-and-postgresql-become-connectors@2#0~26eeabe1c5ef5981","executor":null,"recorded_at":"2026-09-04T00:02:07Z"}}
{"entity":"story","version":1,"id":"mysql-and-postgresql-become-connectors","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000151","to":"active"},"payload":{"actor":"human:timo","at":1788480128129,"causation":"cmd-move-story-mysql-and-postgresql-become-connectors-1788480128129","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:mysql-and-postgresql-become-connectors@3#0~438705f98c5491e9","executor":null,"recorded_at":"2026-09-04T00:02:08Z"}}
{"entity":"story","version":1,"id":"kubernetes-discovers-database-endpoints","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:23`. **read**\n\n- A datasource (bindings per admitted namespace; read verbs list/get) returns the derived\n  endpoint descriptors for both engines, proven against fakes shaped like the real\n  `database.{mysql,postgresql}.sql.crossplane.io` resources; secret references are named,\n  secret values never read.\n- The RBAC the deployment needs (get/list on those CRDs) is stated in the story's close note\n  for the chart, and admission stays behind the same namespace `read_groups`.\n- The catalog invariant family covers the new seam; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe admitted namespace carries the endpoint inventory as Crossplane resources (measured on the\ndev cluster's `latest`: 24 MySQL + 23 PostgreSQL database resources, 35 connection secrets).\nThe hosted Kubernetes integration becomes an endpoint discoverer: a read-only datasource over\nthe admitted namespaces that lists database managed resources and derives endpoint descriptors\n— engine, host, port, database, connection-secret reference — never credential bytes. The\ndescriptors are what S-058's connections consume, so a discovered database is usable without\nanyone writing configuration.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-059`, recorded as the reference `legacy:S-059`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-059"}],"title":"Kubernetes discovers database endpoints"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:23`. **read**\n\n- A datasource (bindings per admitted namespace; read verbs list/get) returns the derived\n  endpoint descriptors for both engines, proven against fakes shaped like the real\n  `database.{mysql,postgresql}.sql.crossplane.io` resources; secret references are named,\n  secret values never read.\n- The RBAC the deployment needs (get/list on those CRDs) is stated in the story's close note\n  for the chart, and admission stays behind the same namespace `read_groups`.\n- The catalog invariant family covers the new seam; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe admitted namespace carries the endpoint inventory as Crossplane resources (measured on the\ndev cluster's `latest`: 24 MySQL + 23 PostgreSQL database resources, 35 connection secrets).\nThe hosted Kubernetes integration becomes an endpoint discoverer: a read-only datasource over\nthe admitted namespaces that lists database managed resources and derives endpoint descriptors\n— engine, host, port, database, connection-secret reference — never credential bytes. The\ndescriptors are what S-058's connections consume, so a discovered database is usable without\nanyone writing configuration.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-059`, recorded as the reference `legacy:S-059`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-059"}],"status":"draft","title":"Kubernetes discovers database endpoints"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/kubernetes-discovers-database-endpoints"},"payload":{"actor":"human:timo","at":1788480128313,"causation":"cmd-new-story-kubernetes-discovers-database-endpoints","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:kubernetes-discovers-database-endpoints@1#0~50897986d1ded409","executor":null,"recorded_at":"2026-09-04T00:02:08Z"}}
{"entity":"story","version":1,"id":"kubernetes-discovers-database-endpoints","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000362","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480128323,"causation":"cmd-story-kubernetes-discovers-database-endpoints-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:kubernetes-discovers-database-endpoints@1#0~1116ee6881d9c943","executor":null,"recorded_at":"2026-09-04T00:02:08Z"}}
{"entity":"story","version":1,"id":"kubernetes-discovers-database-endpoints","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000133","to":"proposed"},"payload":{"actor":"human:timo","at":1788480128486,"causation":"cmd-move-story-kubernetes-discovers-database-endpoints-1788480128486","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:kubernetes-discovers-database-endpoints@2#0~2a885557fa90b2d1","executor":null,"recorded_at":"2026-09-04T00:02:08Z"}}
{"entity":"story","version":1,"id":"kubernetes-discovers-database-endpoints","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000133","to":"active"},"payload":{"actor":"human:timo","at":1788480128651,"causation":"cmd-move-story-kubernetes-discovers-database-endpoints-1788480128651","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:kubernetes-discovers-database-endpoints@3#0~00fded41a90286f9","executor":null,"recorded_at":"2026-09-04T00:02:08Z"}}
{"entity":"story","version":1,"id":"monitoring-joins-the-mcp-toolset","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:23`. **read**\n\n- `tool_search` for a monitoring-read-group principal lists the monitoring tools with input\n  schemas that surface the configured targets (connection choices) honestly; a group-less\n  principal sees none; router tests cover a happy invoke and a refusal per design 14's\n  patterns.\n- The rule-17 seam guard still binds the toolset; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe hosted deployment already reaches the central Grafana on the infra cluster with fifteen\nconfigured Prometheus/Loki/Alertmanager targets, but the MCP toolset projects only Kubernetes.\nAdd the monitoring tools to the static toolset — grafana dashboards list/get, datasources\nlist, prometheus_query_range, loki_query_range, alertmanager_alerts — with the same\nrequirement-driven role filtering (an entry appears only when the caller's own seam results\nsupport it) and the same lease hiding. All six underlying operations are catalogued read-only\nand ride the read path; the toolset adds zero policy.\n\nSource frontmatter: pillar Platform · areas [server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-060`, recorded as the reference `legacy:S-060`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-060"}],"title":"Monitoring joins the MCP toolset"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:23`. **read**\n\n- `tool_search` for a monitoring-read-group principal lists the monitoring tools with input\n  schemas that surface the configured targets (connection choices) honestly; a group-less\n  principal sees none; router tests cover a happy invoke and a refusal per design 14's\n  patterns.\n- The rule-17 seam guard still binds the toolset; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe hosted deployment already reaches the central Grafana on the infra cluster with fifteen\nconfigured Prometheus/Loki/Alertmanager targets, but the MCP toolset projects only Kubernetes.\nAdd the monitoring tools to the static toolset — grafana dashboards list/get, datasources\nlist, prometheus_query_range, loki_query_range, alertmanager_alerts — with the same\nrequirement-driven role filtering (an entry appears only when the caller's own seam results\nsupport it) and the same lease hiding. All six underlying operations are catalogued read-only\nand ride the read path; the toolset adds zero policy.\n\nSource frontmatter: pillar Platform · areas [server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-060`, recorded as the reference `legacy:S-060`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-060"}],"status":"draft","title":"Monitoring joins the MCP toolset"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/monitoring-joins-the-mcp-toolset"},"payload":{"actor":"human:timo","at":1788480128859,"causation":"cmd-new-story-monitoring-joins-the-mcp-toolset","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:monitoring-joins-the-mcp-toolset@1#0~514c3ec2fdaa279f","executor":null,"recorded_at":"2026-09-04T00:02:08Z"}}
{"entity":"story","version":1,"id":"monitoring-joins-the-mcp-toolset","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000367","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480128867,"causation":"cmd-story-monitoring-joins-the-mcp-toolset-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:monitoring-joins-the-mcp-toolset@1#0~de94be5ce8a3a5ce","executor":null,"recorded_at":"2026-09-04T00:02:08Z"}}
{"entity":"story","version":1,"id":"monitoring-joins-the-mcp-toolset","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000154","to":"proposed"},"payload":{"actor":"human:timo","at":1788480129037,"causation":"cmd-move-story-monitoring-joins-the-mcp-toolset-1788480129037","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-joins-the-mcp-toolset@2#0~4966d998cfeadb0e","executor":null,"recorded_at":"2026-09-04T00:02:09Z"}}
{"entity":"story","version":1,"id":"monitoring-joins-the-mcp-toolset","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000154","to":"active"},"payload":{"actor":"human:timo","at":1788480129202,"causation":"cmd-move-story-monitoring-joins-the-mcp-toolset-1788480129202","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-joins-the-mcp-toolset@3#0~104f3b924e4acd6a","executor":null,"recorded_at":"2026-09-04T00:02:09Z"}}
{"entity":"story","version":1,"id":"a-logged-in-person-needs-no-configuration","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:22`. **read**\n\n- On a machine with no prior state: `connectors login <connectors-base>` completes the browser\n  flow, and a following catalog/search/invoke command against the selected hosted deployment\n  succeeds with no endpoint or token flags; tokens refresh across the\n  300-second expiry without re-login, proven by an integration test with a fake identity.\n- The thin-CLI architecture fence stays green — the behaviour lands behind connectors-client\n  per its prescription.\n\n## Context\n\nToday a person needs curl and token plumbing to use the hosted plane. Give the local Connectors\nCLI a zero-configuration hosted mode: `connectors login <connectors-base>` reads that deployment's\npublic bootstrap document, drives the neutral Identity loopback flow, and places the opaque login\nsession in the OS keyring. Subsequent hosted CLI requests and the stdio MCP bridge exchange that\nsession for short-lived, exact-scope tokens transparently. Identity remains relying-party neutral:\nConnectors publishes the Identity origin it trusts, never the other way around.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-061`, recorded as the reference `legacy:S-061`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-061"}],"title":"A logged-in person needs no configuration"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:22`. **read**\n\n- On a machine with no prior state: `connectors login <connectors-base>` completes the browser\n  flow, and a following catalog/search/invoke command against the selected hosted deployment\n  succeeds with no endpoint or token flags; tokens refresh across the\n  300-second expiry without re-login, proven by an integration test with a fake identity.\n- The thin-CLI architecture fence stays green — the behaviour lands behind connectors-client\n  per its prescription.\n\n## Context\n\nToday a person needs curl and token plumbing to use the hosted plane. Give the local Connectors\nCLI a zero-configuration hosted mode: `connectors login <connectors-base>` reads that deployment's\npublic bootstrap document, drives the neutral Identity loopback flow, and places the opaque login\nsession in the OS keyring. Subsequent hosted CLI requests and the stdio MCP bridge exchange that\nsession for short-lived, exact-scope tokens transparently. Identity remains relying-party neutral:\nConnectors publishes the Identity origin it trusts, never the other way around.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-061`, recorded as the reference `legacy:S-061`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-061"}],"status":"draft","title":"A logged-in person needs no configuration"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-logged-in-person-needs-no-configuration"},"payload":{"actor":"human:timo","at":1788480129410,"causation":"cmd-new-story-a-logged-in-person-needs-no-configuration","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-logged-in-person-needs-no-configuration@1#0~69a9a42ebead2e77","executor":null,"recorded_at":"2026-09-04T00:02:09Z"}}
{"entity":"story","version":1,"id":"a-logged-in-person-needs-no-configuration","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000372","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480129420,"causation":"cmd-story-a-logged-in-person-needs-no-configuration-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:a-logged-in-person-needs-no-configuration@1#0~4ded7b665bfbfdac","executor":null,"recorded_at":"2026-09-04T00:02:09Z"}}
{"entity":"story","version":1,"id":"a-logged-in-person-needs-no-configuration","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"proposed"},"payload":{"actor":"human:timo","at":1788480129614,"causation":"cmd-move-story-a-logged-in-person-needs-no-configuration-1788480129614","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-logged-in-person-needs-no-configuration@2#0~0710c15da85fdf8f","executor":null,"recorded_at":"2026-09-04T00:02:09Z"}}
{"entity":"story","version":1,"id":"a-logged-in-person-needs-no-configuration","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"active"},"payload":{"actor":"human:timo","at":1788480129780,"causation":"cmd-move-story-a-logged-in-person-needs-no-configuration-1788480129780","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-logged-in-person-needs-no-configuration@3#0~6ea9aeb43e6711bf","executor":null,"recorded_at":"2026-09-04T00:02:09Z"}}
{"entity":"story","version":1,"id":"discovery-reads-the-cluster-scoped-database-resources","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:29`. **read**\n\n- The reader lists the cluster-scoped collection and follows `spec.providerConfigRef` to the\n  `ProviderConfig`, emitting `{engine, name, provider_config, secret_ref{name,namespace}, ready}`;\n  secret VALUES are still never read — endpoint resolution stays with the SQL driver's\n  credential custody at connect time.\n- Namespace association is derived honestly (the secret reference's namespace), and the\n  datasource description stops promising \"namespace-scoped\" facts the resources do not declare.\n- A wrong-scope regression is impossible to reintroduce silently: a 404 from a group that IS\n  served (its discovery document lists `databases`) surfaces as an error, not an empty list.\n- e2e on dev: the list read returns 24 + 23 records with correct engines and ready flags.\n\n## Context\n\n`kubernetes.databases` returns zero records on the dev cluster while 24 MySQL and 23 PostgreSQL\n`Database` resources exist (e2e 2026-08-24, release rev 14). Two mismatches with the real\nprovider-sql shape, found live:\n\n- The reader lists `/apis/{group}/v1alpha1/namespaces/{ns}/databases`\n  (`crates/integration-kubernetes/src/hosted.rs:608-615`), but the CRDs are **cluster-scoped**\n  (`kubectl api-resources`: NAMESPACED=false). The API answers 404, which the\n  404-means-no-Crossplane mapping silently turns into an empty inventory — the wrong path is\n  indistinguishable from an absent provider.\n- The live `Database` spec carries **no endpoint facts** — only `providerConfigRef` and\n  `deletionPolicy`; host/port live behind the referenced `ProviderConfig`'s\n  `credentials.connectionSecretRef` (name + namespace). The descriptor model\n  `{host, port, database, secret_ref}` cannot be filled from the Database resource alone.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-062`, recorded as the reference `legacy:S-062`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-062"}],"title":"Discovery reads the cluster-scoped database resources"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:29`. **read**\n\n- The reader lists the cluster-scoped collection and follows `spec.providerConfigRef` to the\n  `ProviderConfig`, emitting `{engine, name, provider_config, secret_ref{name,namespace}, ready}`;\n  secret VALUES are still never read — endpoint resolution stays with the SQL driver's\n  credential custody at connect time.\n- Namespace association is derived honestly (the secret reference's namespace), and the\n  datasource description stops promising \"namespace-scoped\" facts the resources do not declare.\n- A wrong-scope regression is impossible to reintroduce silently: a 404 from a group that IS\n  served (its discovery document lists `databases`) surfaces as an error, not an empty list.\n- e2e on dev: the list read returns 24 + 23 records with correct engines and ready flags.\n\n## Context\n\n`kubernetes.databases` returns zero records on the dev cluster while 24 MySQL and 23 PostgreSQL\n`Database` resources exist (e2e 2026-08-24, release rev 14). Two mismatches with the real\nprovider-sql shape, found live:\n\n- The reader lists `/apis/{group}/v1alpha1/namespaces/{ns}/databases`\n  (`crates/integration-kubernetes/src/hosted.rs:608-615`), but the CRDs are **cluster-scoped**\n  (`kubectl api-resources`: NAMESPACED=false). The API answers 404, which the\n  404-means-no-Crossplane mapping silently turns into an empty inventory — the wrong path is\n  indistinguishable from an absent provider.\n- The live `Database` spec carries **no endpoint facts** — only `providerConfigRef` and\n  `deletionPolicy`; host/port live behind the referenced `ProviderConfig`'s\n  `credentials.connectionSecretRef` (name + namespace). The descriptor model\n  `{host, port, database, secret_ref}` cannot be filled from the Database resource alone.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-062`, recorded as the reference `legacy:S-062`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-062"}],"status":"draft","title":"Discovery reads the cluster-scoped database resources"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/discovery-reads-the-cluster-scoped-database-resources"},"payload":{"actor":"human:timo","at":1788480129996,"causation":"cmd-new-story-discovery-reads-the-cluster-scoped-database-resources","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:discovery-reads-the-cluster-scoped-database-resources@1#0~ca74a50233f6dbc1","executor":null,"recorded_at":"2026-09-04T00:02:09Z"}}
{"entity":"story","version":1,"id":"discovery-reads-the-cluster-scoped-database-resources","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000377","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480130005,"causation":"cmd-story-discovery-reads-the-cluster-scoped-database-resources-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:discovery-reads-the-cluster-scoped-database-resources@1#0~47942d9c990f32f1","executor":null,"recorded_at":"2026-09-04T00:02:10Z"}}
{"entity":"story","version":1,"id":"discovery-reads-the-cluster-scoped-database-resources","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000115","to":"proposed"},"payload":{"actor":"human:timo","at":1788480130174,"causation":"cmd-move-story-discovery-reads-the-cluster-scoped-database-resources-1788480130174","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:discovery-reads-the-cluster-scoped-database-resources@2#0~fe1e7a9802390899","executor":null,"recorded_at":"2026-09-04T00:02:10Z"}}
{"entity":"story","version":1,"id":"discovery-reads-the-cluster-scoped-database-resources","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000115","to":"active"},"payload":{"actor":"human:timo","at":1788480130341,"causation":"cmd-move-story-discovery-reads-the-cluster-scoped-database-resources-1788480130341","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:discovery-reads-the-cluster-scoped-database-resources@3#0~fd94195065decc91","executor":null,"recorded_at":"2026-09-04T00:02:10Z"}}
{"entity":"story","version":1,"id":"the-deployment-list-pages-instead-of-refusing","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:27`. **read**\n\n- [x] List records carry the minimal projection only — `name`, `desired_replicas`,\n      `ready_replicas`, `rollout_state` — and nothing K8s-internal; object identity\n      (`namespace`, `uid`, `resource_version`, generations, replica breakdowns) stays with the\n      detail record.\n- [x] `k8s_deployment_list {namespace}` — no paging args — succeeds against a fake namespace of\n      100+ deployments whose raw objects are large enough to have tripped the live refusal;\n      `result_too_large` does not occur.\n- [x] The aggregation is hard-bounded (500 records / 40 seam pages) and a cut listing says so\n      with `truncated: true`.\n- [x] The tool description names the contract: minimal summaries, whole namespace in one call,\n      the truncation bound, and `k8s_deployment_status` for one deployment's depth.\n\n## Context\n\n`tool_invoke k8s_deployment_list {namespace: \"latest\"}` refuses with `result_too_large`\n(e2e 2026-08-24): the namespace holds too many deployments for one response envelope, and the\ntool exposes no way to page or bound the result. A bounded refusal is correct fail-closed\nbehaviour, but a list tool that cannot list a busy namespace is not usable.\n\n**Amended 2026-08-24 (Timo):** minimal records over a paging surface. List-shaped Kubernetes\ndatasource results must not carry raw or verbose object data — a list record is a name plus the\nsmallest rollout summary, and depth belongs to the get/describe path. If slim records fit, the\nMCP tool carries no `limit`/`cursor` arguments at all; upstream paging stays an internal,\nbounded implementation detail, and a hard total cap with an explicit truncation marker replaces\nan unbounded walk.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-063`, recorded as the reference `legacy:S-063`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-063"}],"title":"The deployment list pages instead of refusing"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:27`. **read**\n\n- [x] List records carry the minimal projection only — `name`, `desired_replicas`,\n      `ready_replicas`, `rollout_state` — and nothing K8s-internal; object identity\n      (`namespace`, `uid`, `resource_version`, generations, replica breakdowns) stays with the\n      detail record.\n- [x] `k8s_deployment_list {namespace}` — no paging args — succeeds against a fake namespace of\n      100+ deployments whose raw objects are large enough to have tripped the live refusal;\n      `result_too_large` does not occur.\n- [x] The aggregation is hard-bounded (500 records / 40 seam pages) and a cut listing says so\n      with `truncated: true`.\n- [x] The tool description names the contract: minimal summaries, whole namespace in one call,\n      the truncation bound, and `k8s_deployment_status` for one deployment's depth.\n\n## Context\n\n`tool_invoke k8s_deployment_list {namespace: \"latest\"}` refuses with `result_too_large`\n(e2e 2026-08-24): the namespace holds too many deployments for one response envelope, and the\ntool exposes no way to page or bound the result. A bounded refusal is correct fail-closed\nbehaviour, but a list tool that cannot list a busy namespace is not usable.\n\n**Amended 2026-08-24 (Timo):** minimal records over a paging surface. List-shaped Kubernetes\ndatasource results must not carry raw or verbose object data — a list record is a name plus the\nsmallest rollout summary, and depth belongs to the get/describe path. If slim records fit, the\nMCP tool carries no `limit`/`cursor` arguments at all; upstream paging stays an internal,\nbounded implementation detail, and a hard total cap with an explicit truncation marker replaces\nan unbounded walk.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-063`, recorded as the reference `legacy:S-063`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-063"}],"status":"draft","title":"The deployment list pages instead of refusing"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-deployment-list-pages-instead-of-refusing"},"payload":{"actor":"human:timo","at":1788480130576,"causation":"cmd-new-story-the-deployment-list-pages-instead-of-refusing","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-deployment-list-pages-instead-of-refusing@1#0~2db1f83f125a432c","executor":null,"recorded_at":"2026-09-04T00:02:10Z"}}
{"entity":"story","version":1,"id":"the-deployment-list-pages-instead-of-refusing","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:mcp-entry"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000382","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788480130584,"causation":"cmd-story-the-deployment-list-pages-instead-of-refusing-derived_from-epic-mcp-entry","change":{"change":"related","relation":"derived_from","target":"epic:mcp-entry"},"correlation":"protocol-artifact-relate","event_id":"story:the-deployment-list-pages-instead-of-refusing@1#0~9e71f4637c7f045f","executor":null,"recorded_at":"2026-09-04T00:02:10Z"}}
{"entity":"story","version":1,"id":"the-deployment-list-pages-instead-of-refusing","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000238","to":"proposed"},"payload":{"actor":"human:timo","at":1788480130753,"causation":"cmd-move-story-the-deployment-list-pages-instead-of-refusing-1788480130753","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-deployment-list-pages-instead-of-refusing@2#0~0d08cda0fc09671d","executor":null,"recorded_at":"2026-09-04T00:02:10Z"}}
{"entity":"story","version":1,"id":"the-deployment-list-pages-instead-of-refusing","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000238","to":"active"},"payload":{"actor":"human:timo","at":1788480130922,"causation":"cmd-move-story-the-deployment-list-pages-instead-of-refusing-1788480130922","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-deployment-list-pages-instead-of-refusing@3#0~6cd5d0e238f5ef8d","executor":null,"recorded_at":"2026-09-04T00:02:10Z"}}
{"entity":"story","version":1,"id":"monitoring-tool-schemas-tell-the-truth","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:27`. **read**\n\n- [x] Each monitoring tool's schema states exactly the fields its operation accepts, required\n      matches what refusal enforces, cursors are optional, and numeric-or-string timestamps are\n      either accepted or documented.\n- [x] The e2e sequence `grafana_dashboards_list {target}` and `prometheus_query_range` with epoch\n      ints passes or refuses with a message naming the expected encoding.\n\n## Context\n\nLive e2e (2026-08-24) against the deployed MCP toolset found the monitoring tool input schemas\nmisleading in three ways:\n\n- `grafana_dashboards_list` requires `[\"namespace\", \"limit\", \"continue\"]` — a Kubernetes\n  workload-list shape, not a Grafana dashboards shape; `continue` (a resume cursor) being\n  REQUIRED forces callers to invent an empty string.\n- `target` is enum-projected but not in `required`, while the tools refuse without it\n  (`invalid_input`), so the schema admits calls that can never succeed.\n- `prometheus_query_range` demands string `start`/`end`/`step`; integer epoch seconds — the\n  natural client encoding — refuse as `invalid_input` with no hint. Either accept both or say\n  \"string\" is unix-seconds-or-RFC3339 in the description.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-064`, recorded as the reference `legacy:S-064`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-064"}],"title":"Monitoring tool schemas tell the truth"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:27`. **read**\n\n- [x] Each monitoring tool's schema states exactly the fields its operation accepts, required\n      matches what refusal enforces, cursors are optional, and numeric-or-string timestamps are\n      either accepted or documented.\n- [x] The e2e sequence `grafana_dashboards_list {target}` and `prometheus_query_range` with epoch\n      ints passes or refuses with a message naming the expected encoding.\n\n## Context\n\nLive e2e (2026-08-24) against the deployed MCP toolset found the monitoring tool input schemas\nmisleading in three ways:\n\n- `grafana_dashboards_list` requires `[\"namespace\", \"limit\", \"continue\"]` — a Kubernetes\n  workload-list shape, not a Grafana dashboards shape; `continue` (a resume cursor) being\n  REQUIRED forces callers to invent an empty string.\n- `target` is enum-projected but not in `required`, while the tools refuse without it\n  (`invalid_input`), so the schema admits calls that can never succeed.\n- `prometheus_query_range` demands string `start`/`end`/`step`; integer epoch seconds — the\n  natural client encoding — refuse as `invalid_input` with no hint. Either accept both or say\n  \"string\" is unix-seconds-or-RFC3339 in the description.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-064`, recorded as the reference `legacy:S-064`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-064"}],"status":"draft","title":"Monitoring tool schemas tell the truth"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/monitoring-tool-schemas-tell-the-truth"},"payload":{"actor":"human:timo","at":1788480131135,"causation":"cmd-new-story-monitoring-tool-schemas-tell-the-truth","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:monitoring-tool-schemas-tell-the-truth@1#0~d9181f9dd78e52e0","executor":null,"recorded_at":"2026-09-04T00:02:11Z"}}
{"entity":"story","version":1,"id":"monitoring-tool-schemas-tell-the-truth","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000387","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480131146,"causation":"cmd-story-monitoring-tool-schemas-tell-the-truth-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:monitoring-tool-schemas-tell-the-truth@1#0~40d4057880e5fbb4","executor":null,"recorded_at":"2026-09-04T00:02:11Z"}}
{"entity":"story","version":1,"id":"monitoring-tool-schemas-tell-the-truth","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000163","to":"proposed"},"payload":{"actor":"human:timo","at":1788480131317,"causation":"cmd-move-story-monitoring-tool-schemas-tell-the-truth-1788480131317","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-tool-schemas-tell-the-truth@2#0~d6c10a9c36bc4e1c","executor":null,"recorded_at":"2026-09-04T00:02:11Z"}}
{"entity":"story","version":1,"id":"monitoring-tool-schemas-tell-the-truth","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000163","to":"active"},"payload":{"actor":"human:timo","at":1788480131487,"causation":"cmd-move-story-monitoring-tool-schemas-tell-the-truth-1788480131487","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-tool-schemas-tell-the-truth@3#0~81efe589edc6c0f0","executor":null,"recorded_at":"2026-09-04T00:02:11Z"}}
{"entity":"story","version":1,"id":"monitoring-refusals-name-the-upstream","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:24`. **read**\n\n- [x] A refused monitoring dispatch logs one structured server-side line naming the operation,\n      the route (direct/mediated), and the upstream HTTP status (never the body, which may carry\n      data) — enough to distinguish 401 vs 403 vs 404 vs transport failure from `kubectl logs`.\n- [x] The refusal's `structuredContent`/message distinguishes at least \"upstream refused\n      (status class)\" from \"upstream unreachable\" from \"credential custody failed\", without\n      leaking upstream body content.\n- [ ] With that in place, the live grafana-parent and alertmanager failures are diagnosed on the\n      dev deployment and their concrete cause is recorded in this story (fix here if it is a\n      document/path bug; a provisioning follow-up if it is credential rights).\n\n## Context\n\nTwo monitoring surfaces refuse `unavailable — monitoring connector runtime is unavailable` on\nthe dev deployment (e2e 2026-08-24, rev 15) while their siblings work: the parent Grafana\ntools (`grafana_dashboards_list` on Grafana 13.2.0, whose app-platform API answers the\norigin), and `alertmanager_alerts` — while `prometheus_query_range` and `loki_query_range`\nride the same credential and the same datasource proxy successfully. Diagnosis is impossible\nfrom outside because the backend maps every transport error, non-2xx and non-JSON body to the\nsame context-free refusal (`integration-monitoring/src/backend.rs:110-114` and the\n`operation_unavailable()` call sites), and nothing is logged server-side.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-065`, recorded as the reference `legacy:S-065`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-065"}],"title":"Monitoring refusals name the upstream"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:24`. **read**\n\n- [x] A refused monitoring dispatch logs one structured server-side line naming the operation,\n      the route (direct/mediated), and the upstream HTTP status (never the body, which may carry\n      data) — enough to distinguish 401 vs 403 vs 404 vs transport failure from `kubectl logs`.\n- [x] The refusal's `structuredContent`/message distinguishes at least \"upstream refused\n      (status class)\" from \"upstream unreachable\" from \"credential custody failed\", without\n      leaking upstream body content.\n- [ ] With that in place, the live grafana-parent and alertmanager failures are diagnosed on the\n      dev deployment and their concrete cause is recorded in this story (fix here if it is a\n      document/path bug; a provisioning follow-up if it is credential rights).\n\n## Context\n\nTwo monitoring surfaces refuse `unavailable — monitoring connector runtime is unavailable` on\nthe dev deployment (e2e 2026-08-24, rev 15) while their siblings work: the parent Grafana\ntools (`grafana_dashboards_list` on Grafana 13.2.0, whose app-platform API answers the\norigin), and `alertmanager_alerts` — while `prometheus_query_range` and `loki_query_range`\nride the same credential and the same datasource proxy successfully. Diagnosis is impossible\nfrom outside because the backend maps every transport error, non-2xx and non-JSON body to the\nsame context-free refusal (`integration-monitoring/src/backend.rs:110-114` and the\n`operation_unavailable()` call sites), and nothing is logged server-side.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-065`, recorded as the reference `legacy:S-065`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-065"}],"status":"draft","title":"Monitoring refusals name the upstream"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/monitoring-refusals-name-the-upstream"},"payload":{"actor":"human:timo","at":1788480131704,"causation":"cmd-new-story-monitoring-refusals-name-the-upstream","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:monitoring-refusals-name-the-upstream@1#0~6adf0e87215af3a9","executor":null,"recorded_at":"2026-09-04T00:02:11Z"}}
{"entity":"story","version":1,"id":"monitoring-refusals-name-the-upstream","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000392","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480131713,"causation":"cmd-story-monitoring-refusals-name-the-upstream-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:monitoring-refusals-name-the-upstream@1#0~28f00da9ce074092","executor":null,"recorded_at":"2026-09-04T00:02:11Z"}}
{"entity":"story","version":1,"id":"monitoring-refusals-name-the-upstream","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000163","to":"proposed"},"payload":{"actor":"human:timo","at":1788480131885,"causation":"cmd-move-story-monitoring-refusals-name-the-upstream-1788480131885","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-refusals-name-the-upstream@2#0~d6c10a9c36bc4e1c","executor":null,"recorded_at":"2026-09-04T00:02:11Z"}}
{"entity":"story","version":1,"id":"monitoring-refusals-name-the-upstream","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000163","to":"active"},"payload":{"actor":"human:timo","at":1788480132060,"causation":"cmd-move-story-monitoring-refusals-name-the-upstream-1788480132060","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-refusals-name-the-upstream@3#0~81efe589edc6c0f0","executor":null,"recorded_at":"2026-09-04T00:02:12Z"}}
{"entity":"story","version":1,"id":"the-transport-refusal-names-its-class","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-066-the-transport-refusal-names-its-class.md:22`. **read**\n\n- [x] The transport arm of the refusal log carries an error class derived from the transport\n  error (timeout / connect / tls / body-read / other) — never the error's Display string,\n  which can embed the URL.\n- [x] The transport class distinguishes an oversized upstream body from unreachability; an\n  executor cap breach refuses as \"response too large\", never \"unreachable\".\n- [x] `grafana_dashboards_list` dispatches with a bounded page `limit` and follows `continue`\n  server-side up to a hard budget, so the 4.6 MB namespace lists instead of refusing.\n- [x] The alertmanager document resolves the v2 API sub-path; `alertmanager_alerts` returns live\n  alerts on dev through the mediated route. (The document half is test-proven through the real\n  resolve path; the live-dev half needs the next deploy to pick the rebuilt catalog pack up.)\n- [x] The alertmanager 403 is IN scope — diagnosis below proved it a document defect, not\n  provisioning.\n\n## Context\n\nS-065's live diagnosis (rev 16) pinned `grafana_dashboards_list` to an upstream-transport\nfailure that is specific to that operation's resolved request — `grafana-datasources-list`\nsucceeds on the same direct route, credential and origin every reconcile tick. The final\ndiscriminator (timeout vs connect vs TLS vs body-read) is discarded at the transport mapping\n(`integration-monitoring`, the `.map_err(|_| UpstreamFailure::Transport)` sites), so the last\nstep of the diagnosis is blind.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-066-the-transport-refusal-names-its-class.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-066-the-transport-refusal-names-its-class.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 4 revision(s)\n- Legacy id `S-066`, recorded as the reference `legacy:S-066`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-066"}],"title":"The transport refusal names its class"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-066-the-transport-refusal-names-its-class.md:22`. **read**\n\n- [x] The transport arm of the refusal log carries an error class derived from the transport\n  error (timeout / connect / tls / body-read / other) — never the error's Display string,\n  which can embed the URL.\n- [x] The transport class distinguishes an oversized upstream body from unreachability; an\n  executor cap breach refuses as \"response too large\", never \"unreachable\".\n- [x] `grafana_dashboards_list` dispatches with a bounded page `limit` and follows `continue`\n  server-side up to a hard budget, so the 4.6 MB namespace lists instead of refusing.\n- [x] The alertmanager document resolves the v2 API sub-path; `alertmanager_alerts` returns live\n  alerts on dev through the mediated route. (The document half is test-proven through the real\n  resolve path; the live-dev half needs the next deploy to pick the rebuilt catalog pack up.)\n- [x] The alertmanager 403 is IN scope — diagnosis below proved it a document defect, not\n  provisioning.\n\n## Context\n\nS-065's live diagnosis (rev 16) pinned `grafana_dashboards_list` to an upstream-transport\nfailure that is specific to that operation's resolved request — `grafana-datasources-list`\nsucceeds on the same direct route, credential and origin every reconcile tick. The final\ndiscriminator (timeout vs connect vs TLS vs body-read) is discarded at the transport mapping\n(`integration-monitoring`, the `.map_err(|_| UpstreamFailure::Transport)` sites), so the last\nstep of the diagnosis is blind.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-066-the-transport-refusal-names-its-class.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-066-the-transport-refusal-names-its-class.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 4 revision(s)\n- Legacy id `S-066`, recorded as the reference `legacy:S-066`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-066"}],"status":"draft","title":"The transport refusal names its class"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-transport-refusal-names-its-class"},"payload":{"actor":"human:timo","at":1788480132295,"causation":"cmd-new-story-the-transport-refusal-names-its-class","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-transport-refusal-names-its-class@1#0~d5b0daa7d8c5126b","executor":null,"recorded_at":"2026-09-04T00:02:12Z"}}
{"entity":"story","version":1,"id":"the-transport-refusal-names-its-class","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:endpoint-plane"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000397","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788480132303,"causation":"cmd-story-the-transport-refusal-names-its-class-derived_from-epic-endpoint-plane","change":{"change":"related","relation":"derived_from","target":"epic:endpoint-plane"},"correlation":"protocol-artifact-relate","event_id":"story:the-transport-refusal-names-its-class@1#0~9b17a04a0b354d4f","executor":null,"recorded_at":"2026-09-04T00:02:12Z"}}
{"entity":"story","version":1,"id":"the-transport-refusal-names-its-class","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000268","to":"proposed"},"payload":{"actor":"human:timo","at":1788480132473,"causation":"cmd-move-story-the-transport-refusal-names-its-class-1788480132473","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-transport-refusal-names-its-class@2#0~f1bb801e383a9d30","executor":null,"recorded_at":"2026-09-04T00:02:12Z"}}
{"entity":"story","version":1,"id":"the-transport-refusal-names-its-class","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000268","to":"active"},"payload":{"actor":"human:timo","at":1788480132643,"causation":"cmd-move-story-the-transport-refusal-names-its-class-1788480132643","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-transport-refusal-names-its-class@3#0~abd8c1aec189701c","executor":null,"recorded_at":"2026-09-04T00:02:12Z"}}
{"entity":"story","version":1,"id":"the-server-serves-its-own-openapi-contract","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:20`. **read**\n\n- `GET {base}/openapi.json` serves a committed OpenAPI 3.1 document, unauthenticated,\n  `application/json`, immutable per build (ETag or content hash header).\n- The document covers: `/operations`, `/connections`, `/catalog`, `/events`,\n  `/datasources` (envelope request/response schemas incl. the closed method sets and\n  refusal codes), `/mcp` (JSON-RPC shape, the three meta-tools), the health probes, and\n  the bearer security scheme naming the identity audience and scopes.\n- Drift is impossible silently: a test suite validates every example in the document\n  against the real `protocol` serde types — each request example must deserialize\n  (deny_unknown_fields), each refusal example must match a real error shape, and a\n  deliberately wrong example must fail the suite.\n- No new dependency; the document is authored as a file and served verbatim from a new\n  `crates/server/src/hosted/docs.rs`, with only route registration touching `hosted.rs`.\n- The document is b10x-branded; no b10x string at the surface (check-brand stays\n  clean).\n\n## Context\n\nConsumers of the hosted API — the CLI, the harness, external teams — have no\nmachine-readable contract to generate against or validate with. The server knows its\nsurface precisely (five strict envelope endpoints, the MCP transport, health probes), and\nthe repo already pins contracts as committed artifacts; the OpenAPI document joins that\nfamily.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-067`, recorded as the reference `legacy:S-067`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-067"}],"title":"The server serves its own OpenAPI contract"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:20`. **read**\n\n- `GET {base}/openapi.json` serves a committed OpenAPI 3.1 document, unauthenticated,\n  `application/json`, immutable per build (ETag or content hash header).\n- The document covers: `/operations`, `/connections`, `/catalog`, `/events`,\n  `/datasources` (envelope request/response schemas incl. the closed method sets and\n  refusal codes), `/mcp` (JSON-RPC shape, the three meta-tools), the health probes, and\n  the bearer security scheme naming the identity audience and scopes.\n- Drift is impossible silently: a test suite validates every example in the document\n  against the real `protocol` serde types — each request example must deserialize\n  (deny_unknown_fields), each refusal example must match a real error shape, and a\n  deliberately wrong example must fail the suite.\n- No new dependency; the document is authored as a file and served verbatim from a new\n  `crates/server/src/hosted/docs.rs`, with only route registration touching `hosted.rs`.\n- The document is b10x-branded; no b10x string at the surface (check-brand stays\n  clean).\n\n## Context\n\nConsumers of the hosted API — the CLI, the harness, external teams — have no\nmachine-readable contract to generate against or validate with. The server knows its\nsurface precisely (five strict envelope endpoints, the MCP transport, health probes), and\nthe repo already pins contracts as committed artifacts; the OpenAPI document joins that\nfamily.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-067`, recorded as the reference `legacy:S-067`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-067"}],"status":"draft","title":"The server serves its own OpenAPI contract"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-server-serves-its-own-openapi-contract"},"payload":{"actor":"human:timo","at":1788480132836,"causation":"cmd-new-story-the-server-serves-its-own-openapi-contract","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-server-serves-its-own-openapi-contract@1#0~27f305477d6c34bc","executor":null,"recorded_at":"2026-09-04T00:02:12Z"}}
{"entity":"story","version":1,"id":"the-server-serves-its-own-openapi-contract","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:public-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000402","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788480132845,"causation":"cmd-story-the-server-serves-its-own-openapi-contract-derived_from-epic-public-surface","change":{"change":"related","relation":"derived_from","target":"epic:public-surface"},"correlation":"protocol-artifact-relate","event_id":"story:the-server-serves-its-own-openapi-contract@1#0~2c735376d6d02b5d","executor":null,"recorded_at":"2026-09-04T00:02:12Z"}}
{"entity":"story","version":1,"id":"the-server-serves-its-own-openapi-contract","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000268","to":"proposed"},"payload":{"actor":"human:timo","at":1788480133016,"causation":"cmd-move-story-the-server-serves-its-own-openapi-contract-1788480133016","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:the-server-serves-its-own-openapi-contract@2#0~f1bb801e383a9d30","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"the-server-serves-its-own-openapi-contract","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000268","to":"active"},"payload":{"actor":"human:timo","at":1788480133186,"causation":"cmd-move-story-the-server-serves-its-own-openapi-contract-1788480133186","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:the-server-serves-its-own-openapi-contract@3#0~abd8c1aec189701c","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:20`. **read**\n\n- `GET {base}/docs` serves one self-contained HTML page, unauthenticated: zero external\n  requests (no CDN scripts, no fonts, no images fetched elsewhere) — the connect\n  completion page precedent.\n- Content sections: authentication (identity login → access token mint with audience and\n  scopes), the five envelope endpoints with request/response examples lifted from the\n  S-067 document (single source of truth — examples are extracted, not duplicated), the\n  MCP endpoint with an initialize/tools-call example, datasources incl. the read verbs,\n  and the refusal code table.\n- The page links `openapi.json` and renders its version; a drift test asserts every\n  example shown on the page exists in the OpenAPI document.\n- Static and tenant-free: the handler reads no state, takes no auth, and cannot render\n  request-derived content beyond the base path.\n- b10x-branded; check-brand stays clean.\n\n## Context\n\nA person handed the connectors URL has nothing to read. `GET {base}/docs` renders the\npublic documentation: how to log in and mint a token, every endpoint with a working curl\nexample, the MCP entry point, datasources, and the refusal codes — one self-contained\nHTML page, no external assets.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-068`, recorded as the reference `legacy:S-068`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-068"}],"title":"A public documentation page renders the contract"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:20`. **read**\n\n- `GET {base}/docs` serves one self-contained HTML page, unauthenticated: zero external\n  requests (no CDN scripts, no fonts, no images fetched elsewhere) — the connect\n  completion page precedent.\n- Content sections: authentication (identity login → access token mint with audience and\n  scopes), the five envelope endpoints with request/response examples lifted from the\n  S-067 document (single source of truth — examples are extracted, not duplicated), the\n  MCP endpoint with an initialize/tools-call example, datasources incl. the read verbs,\n  and the refusal code table.\n- The page links `openapi.json` and renders its version; a drift test asserts every\n  example shown on the page exists in the OpenAPI document.\n- Static and tenant-free: the handler reads no state, takes no auth, and cannot render\n  request-derived content beyond the base path.\n- b10x-branded; check-brand stays clean.\n\n## Context\n\nA person handed the connectors URL has nothing to read. `GET {base}/docs` renders the\npublic documentation: how to log in and mint a token, every endpoint with a working curl\nexample, the MCP entry point, datasources, and the refusal codes — one self-contained\nHTML page, no external assets.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-068`, recorded as the reference `legacy:S-068`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-068"}],"status":"draft","title":"A public documentation page renders the contract"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-public-documentation-page-renders-the-contract"},"payload":{"actor":"human:timo","at":1788480133377,"causation":"cmd-new-story-a-public-documentation-page-renders-the-contract","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-public-documentation-page-renders-the-contract@1#0~8e9bf1d6eb364a2b","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:public-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000407","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788480133386,"causation":"cmd-story-a-public-documentation-page-renders-the-contract-derived_from-epic-public-surface","change":{"change":"related","relation":"derived_from","target":"epic:public-surface"},"correlation":"protocol-artifact-relate","event_id":"story:a-public-documentation-page-renders-the-contract@1#0~9903af2667d7a240","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000073","to":"proposed"},"payload":{"actor":"human:timo","at":1788480133558,"causation":"cmd-move-story-a-public-documentation-page-renders-the-contract-1788480133558","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-public-documentation-page-renders-the-contract@2#0~531175becbb86966","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000073","to":"active"},"payload":{"actor":"human:timo","at":1788480133729,"causation":"cmd-move-story-a-public-documentation-page-renders-the-contract-1788480133729","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-public-documentation-page-renders-the-contract@3#0~482018a8eb7992b2","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"one-oauth-implementation-not-three","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-069-one-oauth-implementation-not-three.md:21`. **read**\n\n- [x] A new `crates/connector-oauth` carries: state generation with a single-use TTL map, S256\n      challenge derivation, authorize-URL construction, bounded token exchange with per-field\n      validation, and refresh with a configurable skew under double-checked locking.\n- [x] `integration-gitlab` migrates first and is the extraction source — it is the only current\n      PKCE implementation (`crates/integration-gitlab/src/backend.rs:678-687`, `:739-750`, `:752`,\n      `:806-855`). Its behaviour is unchanged, proven by its existing tests passing untouched.\n- [x] `integration-jira` migrates second. Its refresh path is the best in the repo\n      (`crates/integration-jira/src/backend/auth.rs:479-518` skew + double-checked lock,\n      `:416-451` crash recovery) and the extracted crate must not regress it — if the shared shape\n      cannot express it, the shared shape is wrong.\n- [x] `integration-slack` migrates third, keeping its egress-gate routing\n      (`crates/integration-slack/src/backend/api_runtime.rs:43-179`) rather than a bare client.\n      **Partially, on purpose** — only the pending-state table moved; see the note below. Two of\n      three is the honest result, not a shortfall hidden behind a tick.\n- [x] **Behaviour-preserving.** Jira and Slack stay non-PKCE; neither provider declares\n      `public_client`, so adding PKCE here would be an undeclared change to a live integration.\n- [x] One commit per integration, gate green between each, so a regression bisects to one vendor.\n      `f7149ab` GitLab, `ce04be1` Jira, `d9fc657` Slack. The first draft landed all three as one\n      commit; independent review caught it and the branch was split before it reached `main`.\n- [x] The prepared-transaction commit path stays the only way a token pair is persisted; no point\n      write appears in the extracted crate.\n\n## Context\n\nReplace three hand-rolled OAuth implementations with one, so the next connector that needs a\nbrowser flow inherits PKCE, single-use state, bounded exchange and crash-safe refresh instead of\ncopying whichever neighbour it read first.\n\nSource frontmatter: pillar Platform · areas [service, integration-gitlab, integration-jira, integration-slack] · priority 1. **read**\n\nSource `note:` field, quoted: “GitLab, Jira and Slack each hand-rolled authorization-code OAuth; only GitLab does PKCE. Extract one crate from the GitLab implementation and migrate the other two onto it, behaviour-preserving. Prerequisite Timo chose for the Claude connector work; also unblocks S-013's loopback callback.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-069-one-oauth-implementation-not-three.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-069-one-oauth-implementation-not-three.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-069`, recorded as the reference `legacy:S-069`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-069"}],"title":"One OAuth implementation, not three"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-069-one-oauth-implementation-not-three.md:21`. **read**\n\n- [x] A new `crates/connector-oauth` carries: state generation with a single-use TTL map, S256\n      challenge derivation, authorize-URL construction, bounded token exchange with per-field\n      validation, and refresh with a configurable skew under double-checked locking.\n- [x] `integration-gitlab` migrates first and is the extraction source — it is the only current\n      PKCE implementation (`crates/integration-gitlab/src/backend.rs:678-687`, `:739-750`, `:752`,\n      `:806-855`). Its behaviour is unchanged, proven by its existing tests passing untouched.\n- [x] `integration-jira` migrates second. Its refresh path is the best in the repo\n      (`crates/integration-jira/src/backend/auth.rs:479-518` skew + double-checked lock,\n      `:416-451` crash recovery) and the extracted crate must not regress it — if the shared shape\n      cannot express it, the shared shape is wrong.\n- [x] `integration-slack` migrates third, keeping its egress-gate routing\n      (`crates/integration-slack/src/backend/api_runtime.rs:43-179`) rather than a bare client.\n      **Partially, on purpose** — only the pending-state table moved; see the note below. Two of\n      three is the honest result, not a shortfall hidden behind a tick.\n- [x] **Behaviour-preserving.** Jira and Slack stay non-PKCE; neither provider declares\n      `public_client`, so adding PKCE here would be an undeclared change to a live integration.\n- [x] One commit per integration, gate green between each, so a regression bisects to one vendor.\n      `f7149ab` GitLab, `ce04be1` Jira, `d9fc657` Slack. The first draft landed all three as one\n      commit; independent review caught it and the branch was split before it reached `main`.\n- [x] The prepared-transaction commit path stays the only way a token pair is persisted; no point\n      write appears in the extracted crate.\n\n## Context\n\nReplace three hand-rolled OAuth implementations with one, so the next connector that needs a\nbrowser flow inherits PKCE, single-use state, bounded exchange and crash-safe refresh instead of\ncopying whichever neighbour it read first.\n\nSource frontmatter: pillar Platform · areas [service, integration-gitlab, integration-jira, integration-slack] · priority 1. **read**\n\nSource `note:` field, quoted: “GitLab, Jira and Slack each hand-rolled authorization-code OAuth; only GitLab does PKCE. Extract one crate from the GitLab implementation and migrate the other two onto it, behaviour-preserving. Prerequisite Timo chose for the Claude connector work; also unblocks S-013's loopback callback.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-069-one-oauth-implementation-not-three.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-069-one-oauth-implementation-not-three.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-069`, recorded as the reference `legacy:S-069`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-069"}],"status":"draft","title":"One OAuth implementation, not three"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/one-oauth-implementation-not-three"},"payload":{"actor":"human:timo","at":1788480133950,"causation":"cmd-new-story-one-oauth-implementation-not-three","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:one-oauth-implementation-not-three@1#0~bdc008d28e2bb153","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"one-oauth-implementation-not-three","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:oauth-consolidation"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000412","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788480133958,"causation":"cmd-story-one-oauth-implementation-not-three-derived_from-epic-oauth-consolidation","change":{"change":"related","relation":"derived_from","target":"epic:oauth-consolidation"},"correlation":"protocol-artifact-relate","event_id":"story:one-oauth-implementation-not-three@1#0~f3f008d62d884860","executor":null,"recorded_at":"2026-09-04T00:02:13Z"}}
{"entity":"story","version":1,"id":"one-oauth-implementation-not-three","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000184","to":"proposed"},"payload":{"actor":"human:timo","at":1788480134133,"causation":"cmd-move-story-one-oauth-implementation-not-three-1788480134133","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:one-oauth-implementation-not-three@2#0~51f1757cc2254e8b","executor":null,"recorded_at":"2026-09-04T00:02:14Z"}}
{"entity":"story","version":1,"id":"one-oauth-implementation-not-three","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000184","to":"active"},"payload":{"actor":"human:timo","at":1788480134306,"causation":"cmd-move-story-one-oauth-implementation-not-three-1788480134306","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:one-oauth-implementation-not-three@3#0~3aafeceb4d938ea3","executor":null,"recorded_at":"2026-09-04T00:02:14Z"}}
{"entity":"story","version":1,"id":"a-provider-can-hold-a-credential-it-cannot-spend","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:21`. **read**\n\n- [x] `custody_only = true` is a provider-level declaration field, schema'd in\n      `crates/connector-spec/schema/provider-toml.schema.json` and represented in the IR.\n- [x] The loader **refuses**, with a named reason each, a `custody_only` provider that declares\n      `[spec]`, `[[operations]]`, `[[services]]`, `base_url`, `verify`, `[[channels]]`,\n      `[[events]]`, `[[discoveries]]`, `[[graphs]]`, `[patch]`, `const_headers`, `default_auth`, or\n      an `auth.oauth2` block. Each is a refusal, not a silent allowance, so the kind cannot smuggle\n      a half-declared ordinary provider past review. Asked of the **declared key**, so\n      `base_url = \"\"` and `operations = []` are refused too.\n- [x] The loader **requires** at least one `[[auth]]` on a `custody_only` provider — a provider that\n      holds nothing has no reason to exist. `auth = []` is refused for the same reason.\n- [x] The existing refusal at `crates/connector-spec/src/provider/validation.rs:31` is unchanged\n      for every other provider, and its golden `tests/golden/nothing-to-generate.error` still\n      matches byte for byte.\n- [x] The invariant is parameterised over every provider setting the flag, in the one consolidated\n      file `crates/catalog-build/tests/main/catalog_invariants.rs` —\n      `a_custody_only_provider_publishes_no_surface_and_every_other_provider_does`, asserting both\n      directions. **No per-provider test file** — AGENTS.md states the rule once and parameterises\n      it.\n- [x] `catalog build` emits a custody-only entry; `catalog diff` is clean twice; `catalog check`\n      passes. The catalog document publishes `custody_only` and carries no service at all, rather\n      than one implicit service with an empty base URL.\n\n## Context\n\nLet a declaration say \"this provider owns a credential and no request surface\", so a credential\nwhose *use* belongs to another component can still have an owner, an address and a lifecycle here —\nwithout any path by which connectors could spend it.\n\nSource frontmatter: pillar Catalog · areas [connector-spec, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “design 16: the custody_only declaration kind. Today the loader refuses a provider with neither [spec] nor [[operations]], so custody-without-use cannot be expressed. The two workarounds available are both dishonest; this is the schema consequence.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-070`, recorded as the reference `legacy:S-070`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-070"}],"title":"A provider can hold a credential it cannot spend"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:21`. **read**\n\n- [x] `custody_only = true` is a provider-level declaration field, schema'd in\n      `crates/connector-spec/schema/provider-toml.schema.json` and represented in the IR.\n- [x] The loader **refuses**, with a named reason each, a `custody_only` provider that declares\n      `[spec]`, `[[operations]]`, `[[services]]`, `base_url`, `verify`, `[[channels]]`,\n      `[[events]]`, `[[discoveries]]`, `[[graphs]]`, `[patch]`, `const_headers`, `default_auth`, or\n      an `auth.oauth2` block. Each is a refusal, not a silent allowance, so the kind cannot smuggle\n      a half-declared ordinary provider past review. Asked of the **declared key**, so\n      `base_url = \"\"` and `operations = []` are refused too.\n- [x] The loader **requires** at least one `[[auth]]` on a `custody_only` provider — a provider that\n      holds nothing has no reason to exist. `auth = []` is refused for the same reason.\n- [x] The existing refusal at `crates/connector-spec/src/provider/validation.rs:31` is unchanged\n      for every other provider, and its golden `tests/golden/nothing-to-generate.error` still\n      matches byte for byte.\n- [x] The invariant is parameterised over every provider setting the flag, in the one consolidated\n      file `crates/catalog-build/tests/main/catalog_invariants.rs` —\n      `a_custody_only_provider_publishes_no_surface_and_every_other_provider_does`, asserting both\n      directions. **No per-provider test file** — AGENTS.md states the rule once and parameterises\n      it.\n- [x] `catalog build` emits a custody-only entry; `catalog diff` is clean twice; `catalog check`\n      passes. The catalog document publishes `custody_only` and carries no service at all, rather\n      than one implicit service with an empty base URL.\n\n## Context\n\nLet a declaration say \"this provider owns a credential and no request surface\", so a credential\nwhose *use* belongs to another component can still have an owner, an address and a lifecycle here —\nwithout any path by which connectors could spend it.\n\nSource frontmatter: pillar Catalog · areas [connector-spec, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “design 16: the custody_only declaration kind. Today the loader refuses a provider with neither [spec] nor [[operations]], so custody-without-use cannot be expressed. The two workarounds available are both dishonest; this is the schema consequence.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-070`, recorded as the reference `legacy:S-070`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-070"}],"status":"draft","title":"A provider can hold a credential it cannot spend"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-provider-can-hold-a-credential-it-cannot-spend"},"payload":{"actor":"human:timo","at":1788480134512,"causation":"cmd-new-story-a-provider-can-hold-a-credential-it-cannot-spend","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-provider-can-hold-a-credential-it-cannot-spend@1#0~e59864865baf0ce7","executor":null,"recorded_at":"2026-09-04T00:02:14Z"}}
{"entity":"story","version":1,"id":"a-provider-can-hold-a-credential-it-cannot-spend","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000417","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788480134521,"causation":"cmd-story-a-provider-can-hold-a-credential-it-cannot-spend-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:a-provider-can-hold-a-credential-it-cannot-spend@1#0~cb96b4bcaaa1f00f","executor":null,"recorded_at":"2026-09-04T00:02:14Z"}}
{"entity":"story","version":1,"id":"a-provider-can-hold-a-credential-it-cannot-spend","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000073","to":"proposed"},"payload":{"actor":"human:timo","at":1788480134696,"causation":"cmd-move-story-a-provider-can-hold-a-credential-it-cannot-spend-1788480134696","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:a-provider-can-hold-a-credential-it-cannot-spend@2#0~531175becbb86966","executor":null,"recorded_at":"2026-09-04T00:02:14Z"}}
{"entity":"story","version":1,"id":"a-provider-can-hold-a-credential-it-cannot-spend","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000073","to":"active"},"payload":{"actor":"human:timo","at":1788480134872,"causation":"cmd-move-story-a-provider-can-hold-a-credential-it-cannot-spend-1788480134872","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:a-provider-can-hold-a-credential-it-cannot-spend@3#0~482018a8eb7992b2","executor":null,"recorded_at":"2026-09-04T00:02:14Z"}}
{"entity":"story","version":1,"id":"claude-code-joins-the-catalog","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-071-claude-code-joins-the-catalog.md:20`. **read**\n\n- [x] `providers/claude-code.toml` declares `id = \"claude-code\"`,\n      `authority = \"com.anthropic.claude-code\"`, `custody_only = true`, and exactly one credential\n      with `entry = \"connect_session\"`.\n- [x] The authority `com.anthropic.claude-code` differs from `anthropic`'s `com.anthropic.api`, and\n      a catalogue invariant asserts no two providers share an authority — the authority leads every\n      credential path this provider will ever own and is never repointed (AGENTS.md, adding a\n      connector, step 3).\n- [x] The declaration carries **no** `client_id`, no secret, no credential-shaped example, and no\n      reference to `claude.ai/oauth` or `platform.claude.com/v1/oauth`. We do not drive the vendor's\n      OAuth; `claude setup-token` does, on the person's own machine.\n- [x] `providers/anthropic.toml` is untouched. Its invariant at\n      `crates/catalog-build/tests/main/catalog_invariants.rs:1226` — auth exactly\n      `[\"anthropic.api_key\", \"anthropic.admin_key\"]`, no Claude Code authority in the API connector\n      — still passes unmodified. A separate id is what keeps that assertion honest.\n- [x] A parameterised invariant asserts `claude-code` exposes zero operations and zero services, so\n      nothing in connectors can spend the credential.\n- [x] Build and commit as one unit: `catalog build` → `catalog diff` clean twice → `catalog check`.\n\n## Context\n\nGive a person's Claude Code subscription credential an owner, a per-user address and a revocation\npath, without connectors ever being able to spend it.\n\nSource frontmatter: pillar Catalog · areas [providers, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “New provider id claude-code, authority com.anthropic.claude-code, custody_only. The credential is a claude setup-token output pasted into a Connect Session. Blocked on S-070; the authority is permanent once chosen.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-071-claude-code-joins-the-catalog.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-071-claude-code-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-09-01 · 4 revision(s)\n- Legacy id `S-071`, recorded as the reference `legacy:S-071`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-071"}],"title":"Claude Code joins the catalog as a custody-only provider"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-071-claude-code-joins-the-catalog.md:20`. **read**\n\n- [x] `providers/claude-code.toml` declares `id = \"claude-code\"`,\n      `authority = \"com.anthropic.claude-code\"`, `custody_only = true`, and exactly one credential\n      with `entry = \"connect_session\"`.\n- [x] The authority `com.anthropic.claude-code` differs from `anthropic`'s `com.anthropic.api`, and\n      a catalogue invariant asserts no two providers share an authority — the authority leads every\n      credential path this provider will ever own and is never repointed (AGENTS.md, adding a\n      connector, step 3).\n- [x] The declaration carries **no** `client_id`, no secret, no credential-shaped example, and no\n      reference to `claude.ai/oauth` or `platform.claude.com/v1/oauth`. We do not drive the vendor's\n      OAuth; `claude setup-token` does, on the person's own machine.\n- [x] `providers/anthropic.toml` is untouched. Its invariant at\n      `crates/catalog-build/tests/main/catalog_invariants.rs:1226` — auth exactly\n      `[\"anthropic.api_key\", \"anthropic.admin_key\"]`, no Claude Code authority in the API connector\n      — still passes unmodified. A separate id is what keeps that assertion honest.\n- [x] A parameterised invariant asserts `claude-code` exposes zero operations and zero services, so\n      nothing in connectors can spend the credential.\n- [x] Build and commit as one unit: `catalog build` → `catalog diff` clean twice → `catalog check`.\n\n## Context\n\nGive a person's Claude Code subscription credential an owner, a per-user address and a revocation\npath, without connectors ever being able to spend it.\n\nSource frontmatter: pillar Catalog · areas [providers, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “New provider id claude-code, authority com.anthropic.claude-code, custody_only. The credential is a claude setup-token output pasted into a Connect Session. Blocked on S-070; the authority is permanent once chosen.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-071-claude-code-joins-the-catalog.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-071-claude-code-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-09-01 · 4 revision(s)\n- Legacy id `S-071`, recorded as the reference `legacy:S-071`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-071"}],"status":"draft","title":"Claude Code joins the catalog as a custody-only provider"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/claude-code-joins-the-catalog"},"payload":{"actor":"human:timo","at":1788480135083,"causation":"cmd-new-story-claude-code-joins-the-catalog","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:claude-code-joins-the-catalog@1#0~49410b19c606d4fe","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"claude-code-joins-the-catalog","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000422","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788480135092,"causation":"cmd-story-claude-code-joins-the-catalog-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:claude-code-joins-the-catalog@1#0~cb25cbf9fd1f8353","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"claude-code-joins-the-catalog","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000100","to":"proposed"},"payload":{"actor":"human:timo","at":1788480135269,"causation":"cmd-move-story-claude-code-joins-the-catalog-1788480135269","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:claude-code-joins-the-catalog@2#0~9c05457f55c7ec1f","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"claude-code-joins-the-catalog","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000100","to":"active"},"payload":{"actor":"human:timo","at":1788480135444,"causation":"cmd-move-story-claude-code-joins-the-catalog-1788480135444","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:claude-code-joins-the-catalog@3#0~f2bc0f8df682bb4f","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"the-anthropic-api-key-arrives-through-a-connect-session","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-072-the-anthropic-api-key-arrives-through-a-connect-session.md:20`. **read**\n\n- [ ] `anthropic.api_key` gains `entry = \"connect_session\"` in `providers/anthropic.toml`.\n- [ ] The provider's `id`, `authority`, auth method **names**, services and operations are\n      unchanged, so `catalog_invariants.rs:1226` passes untouched.\n- [ ] The declared `verify = \"anthropic-models-list\"` is what a completed connection runs as its\n      validity ping. On success the connection records a value-free fingerprint and a\n      `last_verified_at`; on failure the session refuses with the upstream's reason named.\n- [ ] No identity is claimed or displayed. Anthropic exposes no profile or email on any credential,\n      and `/v1/organizations/me` needs an Admin key and returns organization fields only — the UI\n      says \"verified\", never who.\n- [ ] The Connect Session invariants hold: single-purpose, short-lived, never returns credential\n      material to its creator, terminal event names the connection id and nothing else.\n\n## Context\n\nLet a person supply their Anthropic API key through the same short-lived, single-purpose session\nevery other connector uses, rather than by writing it into a file by hand.\n\nSource frontmatter: pillar Catalog · areas [providers, service] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “anthropic.api_key gains entry = connect_session so it can be pasted into a single-purpose session instead of a hand-edited file. verify = anthropic-models-list already exists and becomes the validity ping.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-072-the-anthropic-api-key-arrives-through-a-connect-session.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-072-the-anthropic-api-key-arrives-through-a-connect-session.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 1 revision(s)\n- Legacy id `S-072`, recorded as the reference `legacy:S-072`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-072"}],"title":"The Anthropic API key arrives through a Connect Session"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-072-the-anthropic-api-key-arrives-through-a-connect-session.md:20`. **read**\n\n- [ ] `anthropic.api_key` gains `entry = \"connect_session\"` in `providers/anthropic.toml`.\n- [ ] The provider's `id`, `authority`, auth method **names**, services and operations are\n      unchanged, so `catalog_invariants.rs:1226` passes untouched.\n- [ ] The declared `verify = \"anthropic-models-list\"` is what a completed connection runs as its\n      validity ping. On success the connection records a value-free fingerprint and a\n      `last_verified_at`; on failure the session refuses with the upstream's reason named.\n- [ ] No identity is claimed or displayed. Anthropic exposes no profile or email on any credential,\n      and `/v1/organizations/me` needs an Admin key and returns organization fields only — the UI\n      says \"verified\", never who.\n- [ ] The Connect Session invariants hold: single-purpose, short-lived, never returns credential\n      material to its creator, terminal event names the connection id and nothing else.\n\n## Context\n\nLet a person supply their Anthropic API key through the same short-lived, single-purpose session\nevery other connector uses, rather than by writing it into a file by hand.\n\nSource frontmatter: pillar Catalog · areas [providers, service] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “anthropic.api_key gains entry = connect_session so it can be pasted into a single-purpose session instead of a hand-edited file. verify = anthropic-models-list already exists and becomes the validity ping.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-072-the-anthropic-api-key-arrives-through-a-connect-session.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-072-the-anthropic-api-key-arrives-through-a-connect-session.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 1 revision(s)\n- Legacy id `S-072`, recorded as the reference `legacy:S-072`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-072"}],"status":"draft","title":"The Anthropic API key arrives through a Connect Session"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-anthropic-api-key-arrives-through-a-connect-session"},"payload":{"actor":"human:timo","at":1788480135653,"causation":"cmd-new-story-the-anthropic-api-key-arrives-through-a-connect-session","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-anthropic-api-key-arrives-through-a-connect-session@1#0~237764c2830a474a","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"the-anthropic-api-key-arrives-through-a-connect-session","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000427","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788480135663,"causation":"cmd-story-the-anthropic-api-key-arrives-through-a-connect-session-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:the-anthropic-api-key-arrives-through-a-connect-session@1#0~35ca5c9e5ff3847e","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"the-hosted-posture-connects-a-catalogued-provider","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-073-the-hosted-posture-connects-a-catalogued-provider.md:21`. **read**\n\n- [ ] `CatalogBackend` is composed into `HostedRuntime`\n      (`crates/connectors-runtime/src/composition.rs:438-742`), backed by `HostedVaultStore` /\n      `PreparedVaultStore` and `PostgresState`. It is generic over the catalogue, not special-cased\n      per provider.\n- [ ] Per-user addressing: one fresh `instance_id` per Connection, keyed to the owner's subject,\n      addressed by `CredentialRef::for_instance(tenant, authority, instance_id, service, leaf)` —\n      the Jira shape at `crates/integration-jira/src/backend/auth.rs:452-465`.\n- [ ] The address is produced by `integration_catalog::credential_address`\n      (`crates/integration-catalog/src/lib.rs:798-818`) and by nothing else. A second copy of the\n      addressing rule is what made `auth status` report every named instance as not-connected\n      (`crates/connectors-console/src/enrol.rs:70-72`).\n- [ ] `connect_session_access` returns `SelfService` for catalogued paste credentials: any admitted\n      person connects their own credential without an operator.\n- [ ] `setup_profiles` advertises the available flows so a UI can render them without hardcoding a\n      provider list.\n- [ ] Values commit through a prepared transaction — reserve → prepare → pending row → commit →\n      reclaim — with crash recovery. **No point write.** A killed process mid-connect leaves no\n      half-written credential.\n- [ ] Non-credential response fields land in Connection metadata, never the credential store.\n- [ ] **Two people, two connections.** A test asserts that a second signed-in person sees and\n      resolves only their own credential. This is the per-user claim; it is a test, not a\n      click-through.\n- [ ] Provider credentials appear in no Operation, Connection or Event contract, and nothing\n      credential-shaped reaches logs, audit records, argv, environment or crash artifacts — proven\n      with the repository's sentinel convention.\n\n## Context\n\nMake a catalogued provider connectable in a deployment by a signed-in person, under their own\nidentity, so \"connect Anthropic\" in the platform UI reaches the same credential custody the CLI\nalready reaches on a laptop.\n\nSource frontmatter: pillar Platform · areas [connectors-runtime, service, server, hosted-vault] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “CatalogBackend is composed only into PersonalRuntime (composition.rs:344). HostedRuntime wires curated backends only, so all 57 catalogued API-key providers are unconnectable in a deployment. The largest piece of the Connect Claude work, and it is generic, not Anthropic-specific.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-073-the-hosted-posture-connects-a-catalogued-provider.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-073-the-hosted-posture-connects-a-catalogued-provider.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 2 revision(s)\n- Legacy id `S-073`, recorded as the reference `legacy:S-073`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-073"}],"title":"The hosted posture connects a catalogued provider"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-073-the-hosted-posture-connects-a-catalogued-provider.md:21`. **read**\n\n- [ ] `CatalogBackend` is composed into `HostedRuntime`\n      (`crates/connectors-runtime/src/composition.rs:438-742`), backed by `HostedVaultStore` /\n      `PreparedVaultStore` and `PostgresState`. It is generic over the catalogue, not special-cased\n      per provider.\n- [ ] Per-user addressing: one fresh `instance_id` per Connection, keyed to the owner's subject,\n      addressed by `CredentialRef::for_instance(tenant, authority, instance_id, service, leaf)` —\n      the Jira shape at `crates/integration-jira/src/backend/auth.rs:452-465`.\n- [ ] The address is produced by `integration_catalog::credential_address`\n      (`crates/integration-catalog/src/lib.rs:798-818`) and by nothing else. A second copy of the\n      addressing rule is what made `auth status` report every named instance as not-connected\n      (`crates/connectors-console/src/enrol.rs:70-72`).\n- [ ] `connect_session_access` returns `SelfService` for catalogued paste credentials: any admitted\n      person connects their own credential without an operator.\n- [ ] `setup_profiles` advertises the available flows so a UI can render them without hardcoding a\n      provider list.\n- [ ] Values commit through a prepared transaction — reserve → prepare → pending row → commit →\n      reclaim — with crash recovery. **No point write.** A killed process mid-connect leaves no\n      half-written credential.\n- [ ] Non-credential response fields land in Connection metadata, never the credential store.\n- [ ] **Two people, two connections.** A test asserts that a second signed-in person sees and\n      resolves only their own credential. This is the per-user claim; it is a test, not a\n      click-through.\n- [ ] Provider credentials appear in no Operation, Connection or Event contract, and nothing\n      credential-shaped reaches logs, audit records, argv, environment or crash artifacts — proven\n      with the repository's sentinel convention.\n\n## Context\n\nMake a catalogued provider connectable in a deployment by a signed-in person, under their own\nidentity, so \"connect Anthropic\" in the platform UI reaches the same credential custody the CLI\nalready reaches on a laptop.\n\nSource frontmatter: pillar Platform · areas [connectors-runtime, service, server, hosted-vault] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “CatalogBackend is composed only into PersonalRuntime (composition.rs:344). HostedRuntime wires curated backends only, so all 57 catalogued API-key providers are unconnectable in a deployment. The largest piece of the Connect Claude work, and it is generic, not Anthropic-specific.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-073-the-hosted-posture-connects-a-catalogued-provider.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-073-the-hosted-posture-connects-a-catalogued-provider.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 2 revision(s)\n- Legacy id `S-073`, recorded as the reference `legacy:S-073`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-073"}],"status":"draft","title":"The hosted posture connects a catalogued provider"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-hosted-posture-connects-a-catalogued-provider"},"payload":{"actor":"human:timo","at":1788480135874,"causation":"cmd-new-story-the-hosted-posture-connects-a-catalogued-provider","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-hosted-posture-connects-a-catalogued-provider@1#0~02795e542e50eba5","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"the-hosted-posture-connects-a-catalogued-provider","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000432","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788480135886,"causation":"cmd-story-the-hosted-posture-connects-a-catalogued-provider-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:the-hosted-posture-connects-a-catalogued-provider@1#0~f17c6e0b85dc9a1c","executor":null,"recorded_at":"2026-09-04T00:02:15Z"}}
{"entity":"story","version":1,"id":"the-cli-drives-a-hosted-connection","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-074-the-cli-drives-a-hosted-connection.md:20`. **read**\n\n- [ ] A hosted target is reachable from the `connectors` verbs. `HostedClient`\n      (`crates/connectors-client/src/lib.rs:459-543`) already speaks operation, connection and\n      event; today no verb constructs it.\n- [ ] `connectors connect anthropic` and `connectors connect claude-code` work against a deployment\n      and against a local state root, by the same code path.\n- [ ] Credential input keeps the existing discipline: hidden `rpassword` prompt, or\n      `--credential-file` for scripting. **No stdin path and no argument** — `connect` deliberately\n      has neither, because an argument lands in `ps` output and shell history\n      (`crates/connectors-console/src/enrol.rs:59-64`).\n- [ ] `connectors auth status` reports both providers' presence from `SecretStore::exists` over\n      declared credentials, without enumerating the store, and remains structurally value-free.\n- [ ] Behaviour lives in `connectors-console`; the CLI crate gains clap arms and nothing else.\n- [ ] `CLI_TOTAL_LINE_LIMIT` at `crates/catalog-build/tests/main/architecture_fence.rs:24` is raised\n      with a dated justification comment in the style of the three prior raises, stating what moved\n      and why it is packaging rather than behaviour. The pinned dependency list\n      (`architecture_fence.rs:299-321`) does not grow.\n\n## Context\n\nLet the same `connectors connect` a person runs on their laptop drive a deployment, so the web UI\nis a convenience rather than the only way to connect and the flow is testable without a browser.\n\nSource frontmatter: pillar Clients · areas [connectors-cli, connectors-console, connectors-client] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “HostedClient exists but no connectors verb reaches it. Also: the CLI is at exactly its 856-line architecture-fence cap, so any new arm needs the cap raised with a dated justification.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-074-the-cli-drives-a-hosted-connection.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-074-the-cli-drives-a-hosted-connection.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 1 revision(s)\n- Legacy id `S-074`, recorded as the reference `legacy:S-074`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-074"}],"title":"The CLI drives a hosted connection"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-074-the-cli-drives-a-hosted-connection.md:20`. **read**\n\n- [ ] A hosted target is reachable from the `connectors` verbs. `HostedClient`\n      (`crates/connectors-client/src/lib.rs:459-543`) already speaks operation, connection and\n      event; today no verb constructs it.\n- [ ] `connectors connect anthropic` and `connectors connect claude-code` work against a deployment\n      and against a local state root, by the same code path.\n- [ ] Credential input keeps the existing discipline: hidden `rpassword` prompt, or\n      `--credential-file` for scripting. **No stdin path and no argument** — `connect` deliberately\n      has neither, because an argument lands in `ps` output and shell history\n      (`crates/connectors-console/src/enrol.rs:59-64`).\n- [ ] `connectors auth status` reports both providers' presence from `SecretStore::exists` over\n      declared credentials, without enumerating the store, and remains structurally value-free.\n- [ ] Behaviour lives in `connectors-console`; the CLI crate gains clap arms and nothing else.\n- [ ] `CLI_TOTAL_LINE_LIMIT` at `crates/catalog-build/tests/main/architecture_fence.rs:24` is raised\n      with a dated justification comment in the style of the three prior raises, stating what moved\n      and why it is packaging rather than behaviour. The pinned dependency list\n      (`architecture_fence.rs:299-321`) does not grow.\n\n## Context\n\nLet the same `connectors connect` a person runs on their laptop drive a deployment, so the web UI\nis a convenience rather than the only way to connect and the flow is testable without a browser.\n\nSource frontmatter: pillar Clients · areas [connectors-cli, connectors-console, connectors-client] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “HostedClient exists but no connectors verb reaches it. Also: the CLI is at exactly its 856-line architecture-fence cap, so any new arm needs the cap raised with a dated justification.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-074-the-cli-drives-a-hosted-connection.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-074-the-cli-drives-a-hosted-connection.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 1 revision(s)\n- Legacy id `S-074`, recorded as the reference `legacy:S-074`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-074"}],"status":"draft","title":"The CLI drives a hosted connection"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-cli-drives-a-hosted-connection"},"payload":{"actor":"human:timo","at":1788480136097,"causation":"cmd-new-story-the-cli-drives-a-hosted-connection","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-cli-drives-a-hosted-connection@1#0~56867971744303ea","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"the-cli-drives-a-hosted-connection","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000437","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788480136106,"causation":"cmd-story-the-cli-drives-a-hosted-connection-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:the-cli-drives-a-hosted-connection@1#0~e3411ac9c9faffc1","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"deployment-catalogs-are-external-packs","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-075-deployment-catalogs-are-external-packs.md:21`. **read**\n\n- [ ] The hosted runtime accepts zero or more external catalog packs by immutable OCI digest. A\n      mutable tag, local directory traversal, unknown schema version, duplicate provider authority\n      or conflicting operation identity is a named startup refusal.\n- [ ] A pack carries its schema version, content digest, provenance index and generated catalog\n      bytes. The runtime verifies all of them before any item enters discovery or invocation.\n- [ ] Merge order is deterministic and cannot replace or widen a core provider, operation risk,\n      effect, authentication requirement or exposure decision. Extensions use an explicit namespace\n      and may only narrow model visibility unless a reviewed deployment policy grants more.\n- [ ] A private downstream build owns every deployment-specific provider declaration, specification,\n      source-refresh script and generated pack. The generic repository and its OCI artifacts contain\n      none of those identifiers or bytes.\n- [ ] The current deployment-specific surface migrates without changing its stable provider,\n      authority and operation identities. Golden discovery and invocation vectors agree before and\n      after extraction.\n- [ ] The Helm composition can mount or init-copy the pinned pack without baking it into the\n      Connectors image, and reports the accepted pack digests through safe readiness/telemetry.\n- [ ] CI reads the confidential-marker policy from a protected secret and fails both source and\n      generated-artifact builds when a deployment identifier re-enters the generic repository.\n\n## Context\n\nKeep the generic Connectors source and release artifacts deployment-neutral while allowing a private\ndeployment to add proprietary providers, specifications and curated operation mappings as an\nindependently built, immutable catalog pack.\n\nSource frontmatter: pillar Catalog · areas [catalog-reader, catalog-build, connectors-config, deployment] · design `../design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “The core repository currently embeds a deployment-specific provider and generated bundle. Move ownership only after an external pack has the same deterministic and fail-closed guarantees.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-075-deployment-catalogs-are-external-packs.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-075-deployment-catalogs-are-external-packs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-075`, recorded as the reference `legacy:S-075`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-075"}],"title":"Deployment catalogs are external immutable packs"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-075-deployment-catalogs-are-external-packs.md:21`. **read**\n\n- [ ] The hosted runtime accepts zero or more external catalog packs by immutable OCI digest. A\n      mutable tag, local directory traversal, unknown schema version, duplicate provider authority\n      or conflicting operation identity is a named startup refusal.\n- [ ] A pack carries its schema version, content digest, provenance index and generated catalog\n      bytes. The runtime verifies all of them before any item enters discovery or invocation.\n- [ ] Merge order is deterministic and cannot replace or widen a core provider, operation risk,\n      effect, authentication requirement or exposure decision. Extensions use an explicit namespace\n      and may only narrow model visibility unless a reviewed deployment policy grants more.\n- [ ] A private downstream build owns every deployment-specific provider declaration, specification,\n      source-refresh script and generated pack. The generic repository and its OCI artifacts contain\n      none of those identifiers or bytes.\n- [ ] The current deployment-specific surface migrates without changing its stable provider,\n      authority and operation identities. Golden discovery and invocation vectors agree before and\n      after extraction.\n- [ ] The Helm composition can mount or init-copy the pinned pack without baking it into the\n      Connectors image, and reports the accepted pack digests through safe readiness/telemetry.\n- [ ] CI reads the confidential-marker policy from a protected secret and fails both source and\n      generated-artifact builds when a deployment identifier re-enters the generic repository.\n\n## Context\n\nKeep the generic Connectors source and release artifacts deployment-neutral while allowing a private\ndeployment to add proprietary providers, specifications and curated operation mappings as an\nindependently built, immutable catalog pack.\n\nSource frontmatter: pillar Catalog · areas [catalog-reader, catalog-build, connectors-config, deployment] · design `../design/02-architecture.md`. **read**\n\nSource `note:` field, quoted: “The core repository currently embeds a deployment-specific provider and generated bundle. Move ownership only after an external pack has the same deterministic and fail-closed guarantees.”\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-075-deployment-catalogs-are-external-packs.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-075-deployment-catalogs-are-external-packs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-075`, recorded as the reference `legacy:S-075`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-075"}],"status":"draft","title":"Deployment catalogs are external immutable packs"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/deployment-catalogs-are-external-packs"},"payload":{"actor":"human:timo","at":1788480136314,"causation":"cmd-new-story-deployment-catalogs-are-external-packs","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:deployment-catalogs-are-external-packs@1#0~8681d2760aa986ac","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"deployment-catalogs-are-external-packs","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:deployment-packs"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000442","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788480136325,"causation":"cmd-story-deployment-catalogs-are-external-packs-derived_from-epic-deployment-packs","change":{"change":"related","relation":"derived_from","target":"epic:deployment-packs"},"correlation":"protocol-artifact-relate","event_id":"story:deployment-catalogs-are-external-packs@1#0~5f7c5158120e4467","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"an-agent-attempt-can-lease-a-user-subscription","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:20`. **read**\n\n- [x] Connector-owned custody stores one credential per verified tenant/subject under the permanent\n      Claude Code authority and exposes presence, replacement and idempotent disconnect.\n- [x] Lease creation requires `connectors.credentials.lease` and binds a cryptorandom capability to\n      an exact attempt id, at most one hour, 1–1,024 uses and an in-process 10,000-lease cap.\n- [x] Redemption accepts the lease bearer rather than Identity, compares it in constant time, and\n      refuses wrong attempt, expiry, exhaustion, unknown id and wrong bearer without distinction.\n- [x] Restart, disconnect and credential replacement revoke live leases. Replacement and redemption\n      serialize, so an old capability cannot reach the new credential generation.\n- [x] Hosted routes are opt-in, require Vault custody, bound every body, and return `no-store` plus\n      `no-cache` on successful confidential responses.\n- [x] The official client keeps lease and credential values private, redacts `Debug`, zeroizes on\n      drop, enforces HTTPS, and refuses a cacheable confidential response.\n- [x] The embedded OpenAPI document covers all five operations and marks returned secrets as\n      sensitive without misclassifying response-only fields as write-only.\n- [x] Tests prove connect → lease → wrong-attempt refusal → exact redemption → spent refusal,\n      rotation revocation, disconnect revocation, redaction, and cache-policy refusal.\n\n## Context\n\nLet a user-bound agent attempt run through the user's Claude Code subscription without copying the\ndurable credential into Agent Platform, Devcenter, configuration, logs, or an agent record.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime, identity-http] · design `../design/17-attempt-bounded-subscription-credential-leases.md`. **read**\n\nSource `note:` field, quoted: “A least-privilege Identity exchange creates an expiring finite-use capability for one exact Harness attempt; Connectors keeps durable custody and the provider value reaches only the bearer boundary.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-076`, recorded as the reference `legacy:S-076`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-076"}],"title":"An agent attempt can lease a user's subscription"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:20`. **read**\n\n- [x] Connector-owned custody stores one credential per verified tenant/subject under the permanent\n      Claude Code authority and exposes presence, replacement and idempotent disconnect.\n- [x] Lease creation requires `connectors.credentials.lease` and binds a cryptorandom capability to\n      an exact attempt id, at most one hour, 1–1,024 uses and an in-process 10,000-lease cap.\n- [x] Redemption accepts the lease bearer rather than Identity, compares it in constant time, and\n      refuses wrong attempt, expiry, exhaustion, unknown id and wrong bearer without distinction.\n- [x] Restart, disconnect and credential replacement revoke live leases. Replacement and redemption\n      serialize, so an old capability cannot reach the new credential generation.\n- [x] Hosted routes are opt-in, require Vault custody, bound every body, and return `no-store` plus\n      `no-cache` on successful confidential responses.\n- [x] The official client keeps lease and credential values private, redacts `Debug`, zeroizes on\n      drop, enforces HTTPS, and refuses a cacheable confidential response.\n- [x] The embedded OpenAPI document covers all five operations and marks returned secrets as\n      sensitive without misclassifying response-only fields as write-only.\n- [x] Tests prove connect → lease → wrong-attempt refusal → exact redemption → spent refusal,\n      rotation revocation, disconnect revocation, redaction, and cache-policy refusal.\n\n## Context\n\nLet a user-bound agent attempt run through the user's Claude Code subscription without copying the\ndurable credential into Agent Platform, Devcenter, configuration, logs, or an agent record.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime, identity-http] · design `../design/17-attempt-bounded-subscription-credential-leases.md`. **read**\n\nSource `note:` field, quoted: “A least-privilege Identity exchange creates an expiring finite-use capability for one exact Harness attempt; Connectors keeps durable custody and the provider value reaches only the bearer boundary.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-076`, recorded as the reference `legacy:S-076`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-076"}],"status":"draft","title":"An agent attempt can lease a user's subscription"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/an-agent-attempt-can-lease-a-user-subscription"},"payload":{"actor":"human:timo","at":1788480136532,"causation":"cmd-new-story-an-agent-attempt-can-lease-a-user-subscription","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:an-agent-attempt-can-lease-a-user-subscription@1#0~bc8c9c8e9ac83584","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"an-agent-attempt-can-lease-a-user-subscription","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000447","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788480136543,"causation":"cmd-story-an-agent-attempt-can-lease-a-user-subscription-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:an-agent-attempt-can-lease-a-user-subscription@1#0~a9112ac0003680b8","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"an-agent-attempt-can-lease-a-user-subscription","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000091","to":"proposed"},"payload":{"actor":"human:timo","at":1788480136721,"causation":"cmd-move-story-an-agent-attempt-can-lease-a-user-subscription-1788480136721","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:an-agent-attempt-can-lease-a-user-subscription@2#0~b7774a8295922c72","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"an-agent-attempt-can-lease-a-user-subscription","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000091","to":"active"},"payload":{"actor":"human:timo","at":1788480136899,"causation":"cmd-move-story-an-agent-attempt-can-lease-a-user-subscription-1788480136899","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:an-agent-attempt-can-lease-a-user-subscription@3#0~c48ea9b6340156f6","executor":null,"recorded_at":"2026-09-04T00:02:16Z"}}
{"entity":"story","version":1,"id":"claude-subscriptions-connect-with-pkce-and-refresh","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:20`. **read**\n\n- [x] A bounded, expiring, single-use start operation creates server-held PKCE state and returns\n      only an authorization URL plus an opaque flow id.\n- [x] Completion is bound to the verified tenant and subject, consumes the pending flow before the\n      provider exchange, verifies the state returned in the provider's `code#state` manual result,\n      exchanges only the code component, and stores neither code nor verifier after the attempt.\n- [x] The provider exchange uses the exact public-client authorization-code JSON contract and\n      validates bounded access, refresh, expiry, and inference-scope fields before custody.\n- [x] Custody stores a versioned refresh-capable record, returns presence only, and exports only a\n      current access credential through an exact attempt lease redemption.\n- [x] Redemption refreshes under serialization before expiry, handles refresh-token rotation, and\n      replaces custody without exposing either token in diagnostics or API responses.\n- [x] Hosted routes and the official Rust client expose start and complete with non-cacheable\n      responses, bounded bodies, exact Identity scope checks, and credential-safe types.\n- [x] The embedded OpenAPI artifact and tests cover start, completion, replay refusal, provider\n      response validation, refresh, and lease redemption.\n\n## Context\n\nLet an authenticated person connect their Claude subscription through the provider's public-client\nPKCE flow instead of obtaining and pasting a durable credential themselves, while keeping every\nprovider token in Connector-owned custody.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime]. **read**\n\nSource `note:` field, quoted: “Connectors owns the public-client PKCE verifier, provider exchange, refresh-capable custody, and refresh-on-redemption; Identity remains provider-agnostic.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-077`, recorded as the reference `legacy:S-077`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-077"}],"title":"Claude subscriptions connect with PKCE and refresh"},"args":{"command":"create-entity","data":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:20`. **read**\n\n- [x] A bounded, expiring, single-use start operation creates server-held PKCE state and returns\n      only an authorization URL plus an opaque flow id.\n- [x] Completion is bound to the verified tenant and subject, consumes the pending flow before the\n      provider exchange, verifies the state returned in the provider's `code#state` manual result,\n      exchanges only the code component, and stores neither code nor verifier after the attempt.\n- [x] The provider exchange uses the exact public-client authorization-code JSON contract and\n      validates bounded access, refresh, expiry, and inference-scope fields before custody.\n- [x] Custody stores a versioned refresh-capable record, returns presence only, and exports only a\n      current access credential through an exact attempt lease redemption.\n- [x] Redemption refreshes under serialization before expiry, handles refresh-token rotation, and\n      replaces custody without exposing either token in diagnostics or API responses.\n- [x] Hosted routes and the official Rust client expose start and complete with non-cacheable\n      responses, bounded bodies, exact Identity scope checks, and credential-safe types.\n- [x] The embedded OpenAPI artifact and tests cover start, completion, replay refusal, provider\n      response validation, refresh, and lease redemption.\n\n## Context\n\nLet an authenticated person connect their Claude subscription through the provider's public-client\nPKCE flow instead of obtaining and pasting a durable credential themselves, while keeping every\nprovider token in Connector-owned custody.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime]. **read**\n\nSource `note:` field, quoted: “Connectors owns the public-client PKCE verifier, provider exchange, refresh-capable custody, and refresh-on-redemption; Identity remains provider-agnostic.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-077`, recorded as the reference `legacy:S-077`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n","refs":[{"provider":"legacy","reference":"S-077"}],"status":"draft","title":"Claude subscriptions connect with PKCE and refresh"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/claude-subscriptions-connect-with-pkce-and-refresh"},"payload":{"actor":"human:timo","at":1788480137107,"causation":"cmd-new-story-claude-subscriptions-connect-with-pkce-and-refresh","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:claude-subscriptions-connect-with-pkce-and-refresh@1#0~8e5989a9b1447e00","executor":null,"recorded_at":"2026-09-04T00:02:17Z"}}
{"entity":"story","version":1,"id":"claude-subscriptions-connect-with-pkce-and-refresh","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000452","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788480137116,"causation":"cmd-story-claude-subscriptions-connect-with-pkce-and-refresh-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:claude-subscriptions-connect-with-pkce-and-refresh@1#0~1443dcedeafa90de","executor":null,"recorded_at":"2026-09-04T00:02:17Z"}}
{"entity":"story","version":1,"id":"claude-subscriptions-connect-with-pkce-and-refresh","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000106","to":"proposed"},"payload":{"actor":"human:timo","at":1788480137295,"causation":"cmd-move-story-claude-subscriptions-connect-with-pkce-and-refresh-1788480137295","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:claude-subscriptions-connect-with-pkce-and-refresh@2#0~7a1f801a74903869","executor":null,"recorded_at":"2026-09-04T00:02:17Z"}}
{"entity":"story","version":1,"id":"claude-subscriptions-connect-with-pkce-and-refresh","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000106","to":"active"},"payload":{"actor":"human:timo","at":1788480137477,"causation":"cmd-move-story-claude-subscriptions-connect-with-pkce-and-refresh-1788480137477","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:claude-subscriptions-connect-with-pkce-and-refresh@3#0~b09d340a59dab461","executor":null,"recorded_at":"2026-09-04T00:02:17Z"}}
{"entity":"story","version":1,"id":"one-connection-config-shape","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-051-one-connection-config-shape.md:19`. **read**\n\n- connectors-config exposes a single connection config shape consumed by both call sites, or a\n  doc comment on each type states the deliberate distinction and a test pins their divergence\n  as intentional.\n- Existing personal config files parse unchanged; no serialized field name moves.\n\n## Context\n\nSince S-048 retired `approval_evidence_ref`, `ConnectionConfig` and `B10xConnectionConfig`\nin connectors-config are structurally identical (S-048 implementation notes, 2026-08-23). Two\nnames for one shape invite drift: a field added to one silently diverges the other. Merge them\ninto one connection config shape — or document why the b10x-provider connection is a\ndistinct type on purpose.\n\nSource frontmatter: pillar Platform · areas [config, integrations]. **read**\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-051-one-connection-config-shape.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-051-one-connection-config-shape.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 1 revision(s)\n- Legacy id `S-051`, recorded as the reference `legacy:S-051`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-051-one-connection-config-shape.md:19`. **read**\n\n- connectors-config exposes a single connection config shape consumed by both call sites, or a\n  doc comment on each type states the deliberate distinction and a test pins their divergence\n  as intentional.\n- Existing personal config files parse unchanged; no serialized field name moves.\n\n## Context\n\nSince S-048 retired `approval_evidence_ref`, `ConnectionConfig` and `B10xConnectionConfig`\nin connectors-config are structurally identical (S-048 implementation notes, 2026-08-23). Two\nnames for one shape invite drift: a field added to one silently diverges the other. Merge them\ninto one connection config shape — or document why the b10x-provider connection is a\ndistinct type on purpose.\n\nSource frontmatter: pillar Platform · areas [config, integrations]. **read**\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-051-one-connection-config-shape.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-051-one-connection-config-shape.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 1 revision(s)\n- Legacy id `S-051`, recorded as the reference `legacy:S-051`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000196"},"payload":{"actor":"human:timo","at":1788480152720,"causation":"cmd-protocol-artifact-body-story-one-connection-config-shape-1788480152720","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:one-connection-config-shape@2#0~992a24d2002c5bb1","executor":null,"recorded_at":"2026-09-04T00:02:32Z"}}
{"entity":"story","version":1,"id":"the-platform-connector-is-named-platform","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-052-the-platform-connector-is-named-platform.md:25`. **read**\n\n- No non-allowlisted `b10x` remains in crate names, type/function/variant names, or prose;\n  `scripts/check-brand.sh`'s allowlist shrinks accordingly and stays negative-tested.\n- An existing personal config carrying the old section name parses unchanged, proven by a test.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nD5 is resolved: the platform's name is \"platform\" (extraction plan resolution note, 2026-08-23).\nThe brand sweep left ~154 code-level occurrences blocked on that decision. Rename the code-level\nfamily: crate `integration-b10x` becomes `integration-platform`, and\n`B10xConnectionConfig`, `B10xIntegrationConfig`, `B10xBackend`,\n`ConnectionInitiator::B10x`, `b10x_only()` and companions follow, with prose\n(\"B10x Identity/Integration/Provider/Session Authority\", \"Authorize B10x\" consent\npages) moving to platform naming. Explicitly kept, each on the brand-fence allowlist: provider id\n`b10x` and authority `io.b10x` (public identity, irreversible), `urn:b10x:*`,\n`x-b10x-*` headers, `b10x.*.vN` contract ids, hash domains, and serialized state\n(keyring service attribute). The serde-visible `[b10x]` config section moves only behind a\nserde alias or a documented migration — an existing personal config must keep parsing.\n\nSource frontmatter: pillar Platform · areas [integrations, config, server]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-052-the-platform-connector-is-named-platform.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-052-the-platform-connector-is-named-platform.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-052`, recorded as the reference `legacy:S-052`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-052-the-platform-connector-is-named-platform.md:25`. **read**\n\n- No non-allowlisted `b10x` remains in crate names, type/function/variant names, or prose;\n  `scripts/check-brand.sh`'s allowlist shrinks accordingly and stays negative-tested.\n- An existing personal config carrying the old section name parses unchanged, proven by a test.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nD5 is resolved: the platform's name is \"platform\" (extraction plan resolution note, 2026-08-23).\nThe brand sweep left ~154 code-level occurrences blocked on that decision. Rename the code-level\nfamily: crate `integration-b10x` becomes `integration-platform`, and\n`B10xConnectionConfig`, `B10xIntegrationConfig`, `B10xBackend`,\n`ConnectionInitiator::B10x`, `b10x_only()` and companions follow, with prose\n(\"B10x Identity/Integration/Provider/Session Authority\", \"Authorize B10x\" consent\npages) moving to platform naming. Explicitly kept, each on the brand-fence allowlist: provider id\n`b10x` and authority `io.b10x` (public identity, irreversible), `urn:b10x:*`,\n`x-b10x-*` headers, `b10x.*.vN` contract ids, hash domains, and serialized state\n(keyring service attribute). The serde-visible `[b10x]` config section moves only behind a\nserde alias or a documented migration — an existing personal config must keep parsing.\n\nSource frontmatter: pillar Platform · areas [integrations, config, server]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-052-the-platform-connector-is-named-platform.md:5`: `status: done`. **read**\n\nThis artifact stands at `active`, not `implemented`. The store refuses that rung without\nevidence, and no run in this migration observed this story's acceptance:\n\n> implemented is on the ladder and not yet earned: reaching implemented needs at least 1\n> test_result record(s). no test_result record is held for this artifact\n\nRecord a `test_result` against it and move it; a migration does not get to assert one.\n\n## Provenance\n\nMigrated from `docs/stories/S-052-the-platform-connector-is-named-platform.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-052`, recorded as the reference `legacy:S-052`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000295"},"payload":{"actor":"human:timo","at":1788480152875,"causation":"cmd-protocol-artifact-body-story-the-platform-connector-is-named-platform-1788480152875","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:the-platform-connector-is-named-platform@4#0~3135616b5cfd60ff","executor":null,"recorded_at":"2026-09-04T00:02:32Z"}}
{"entity":"story","version":1,"id":"kubernetes-joins-the-catalog","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-057-kubernetes-joins-the-catalog.md:25`. **read**\n\n- `providers/kubernetes.toml` (repository-authored spec with provenance) catalogs the current\n  operation surface; the generic `CatalogBackend` executes it in-cluster behind the same\n  admission outcomes the hand-written backend produced, proven by the existing hosted tests\n  running against the declarative path.\n- The generic executor's new primitives (file-sourced bearer, namespace group policy, bounded\n  text response) are declared, not hard-coded to Kubernetes.\n- The deleted Rust is actually deleted; `integration-kubernetes` retains only what no\n  declaration can express, each retention justified in its module doc.\n\n## Context\n\nKubernetes is the last hand-coded HTTP surface: every other REST provider is declared in\n`providers/*.toml` and executed by the one generic adapter (`integration-catalog` — \"One adapter\nfor every declared provider\"). The Kubernetes integration stays Rust only because the generic\nexecutor lacks three primitives: a file-sourced bearer that is re-read per call (the in-cluster\nServiceAccount token), per-namespace group policy (`namespace_access.read_groups` is finer than\ntenant Grants), and bounded plain-text responses (pod logs). Teach the generic executor those\nprimitives, declare the Kubernetes API surface as a repository-authored spec + provider TOML\n(deployment status, rollout restart, pod logs — grounded in the official API reference, per the\nauthored-spec discipline), and delete the corresponding hand-written paths from\n`integration-kubernetes`. The workloads datasource (pods+events join) may remain code; it is a\nprojection, not an HTTP call.\n\nSource frontmatter: pillar Platform · areas [integrations, catalog, server]. **read**\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-057-kubernetes-joins-the-catalog.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-057-kubernetes-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 1 revision(s)\n- Legacy id `S-057`, recorded as the reference `legacy:S-057`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-057-kubernetes-joins-the-catalog.md:25`. **read**\n\n- `providers/kubernetes.toml` (repository-authored spec with provenance) catalogs the current\n  operation surface; the generic `CatalogBackend` executes it in-cluster behind the same\n  admission outcomes the hand-written backend produced, proven by the existing hosted tests\n  running against the declarative path.\n- The generic executor's new primitives (file-sourced bearer, namespace group policy, bounded\n  text response) are declared, not hard-coded to Kubernetes.\n- The deleted Rust is actually deleted; `integration-kubernetes` retains only what no\n  declaration can express, each retention justified in its module doc.\n\n## Context\n\nKubernetes is the last hand-coded HTTP surface: every other REST provider is declared in\n`providers/*.toml` and executed by the one generic adapter (`integration-catalog` — \"One adapter\nfor every declared provider\"). The Kubernetes integration stays Rust only because the generic\nexecutor lacks three primitives: a file-sourced bearer that is re-read per call (the in-cluster\nServiceAccount token), per-namespace group policy (`namespace_access.read_groups` is finer than\ntenant Grants), and bounded plain-text responses (pod logs). Teach the generic executor those\nprimitives, declare the Kubernetes API surface as a repository-authored spec + provider TOML\n(deployment status, rollout restart, pod logs — grounded in the official API reference, per the\nauthored-spec discipline), and delete the corresponding hand-written paths from\n`integration-kubernetes`. The workloads datasource (pods+events join) may remain code; it is a\nprojection, not an HTTP call.\n\nSource frontmatter: pillar Platform · areas [integrations, catalog, server]. **read**\n\n## Status\n\n`backlog` in the source. Quoted from `docs/stories/S-057-kubernetes-joins-the-catalog.md:5`: `status: backlog`. **read**\n\n## Provenance\n\nMigrated from `docs/stories/S-057-kubernetes-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 1 revision(s)\n- Legacy id `S-057`, recorded as the reference `legacy:S-057`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788480153016,"causation":"cmd-protocol-artifact-body-story-kubernetes-joins-the-catalog-1788480153016","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:kubernetes-joins-the-catalog@2#0~9835222264bfa6d8","executor":null,"recorded_at":"2026-09-04T00:02:33Z"}}
{"entity":"dependency-blocker","version":1,"id":"s015-not-picked-up","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"## What would clear this\n\nUnblocked when S-015 is picked up.\n\nQuoted from `docs/stories/S-002-effects-are-read-never-derived.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","title":"S-015 has not been picked up, so the grant-admission half cannot land"},"args":{"command":"create-entity","data":{"body":"## What would clear this\n\nUnblocked when S-015 is picked up.\n\nQuoted from `docs/stories/S-002-effects-are-read-never-derived.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","status":"open","title":"S-015 has not been picked up, so the grant-admission half cannot land"},"entity_type":"aep.dependency-blocker/v1","locator":"ep://planning/store/dependency-blocker/s015-not-picked-up"},"payload":{"actor":"human:timo","at":1788480173453,"causation":"cmd-new-dependency-blocker-s015-not-picked-up","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"dependency-blocker:s015-not-picked-up@1#0~0617e63522b4f2ac","executor":null,"recorded_at":"2026-09-04T00:02:53Z"}}
{"entity":"dependency-blocker","version":1,"id":"s015-not-picked-up","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:effects-are-read-never-derived"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000457","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788480173469,"causation":"cmd-dependency-blocker-s015-not-picked-up-blocks-story-effects-are-read-never-derived","change":{"change":"related","relation":"blocks","target":"story:effects-are-read-never-derived"},"correlation":"protocol-artifact-relate","event_id":"dependency-blocker:s015-not-picked-up@1#0~870557d8c4e0fff2","executor":null,"recorded_at":"2026-09-04T00:02:53Z"}}
{"entity":"external-blocker","version":1,"id":"flux-has-not-recorded-adoption","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"## What would clear this\n\nExternally gated: Flux must record B10x adoption in its own repository before this family can schedule M5.\n\nQuoted from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","title":"Flux has not recorded b10x adoption in its own repository"},"args":{"command":"create-entity","data":{"body":"## What would clear this\n\nExternally gated: Flux must record B10x adoption in its own repository before this family can schedule M5.\n\nQuoted from `docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","status":"open","title":"Flux has not recorded b10x adoption in its own repository"},"entity_type":"aep.external-blocker/v1","locator":"ep://planning/store/external-blocker/flux-has-not-recorded-adoption"},"payload":{"actor":"human:timo","at":1788480173650,"causation":"cmd-new-external-blocker-flux-has-not-recorded-adoption","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"external-blocker:flux-has-not-recorded-adoption@1#0~7cd29229f87e5dd3","executor":null,"recorded_at":"2026-09-04T00:02:53Z"}}
{"entity":"external-blocker","version":1,"id":"flux-has-not-recorded-adoption","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:m5-flux-re-points-and-the-gitlab-plugin-retires"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000462","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788480173658,"causation":"cmd-external-blocker-flux-has-not-recorded-adoption-blocks-story-m5-flux-re-points-and-the-gitlab-plugin-retires","change":{"change":"related","relation":"blocks","target":"story:m5-flux-re-points-and-the-gitlab-plugin-retires"},"correlation":"protocol-artifact-relate","event_id":"external-blocker:flux-has-not-recorded-adoption@1#0~4114a88d4e1471de","executor":null,"recorded_at":"2026-09-04T00:02:53Z"}}
{"entity":"decision-blocker","version":1,"id":"external-driver-security-adr","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"## What would clear this\n\nDelivery item 6 is a gate, not implementation scope; unblock only after built-in pressure and a separate security ADR.\n\nQuoted from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","title":"No security ADR has decided external driver artifacts"},"args":{"command":"create-entity","data":{"body":"## What would clear this\n\nDelivery item 6 is a gate, not implementation scope; unblock only after built-in pressure and a separate security ADR.\n\nQuoted from `docs/stories/S-028-external-driver-artifacts-stay-deferred.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","status":"open","title":"No security ADR has decided external driver artifacts"},"entity_type":"aep.decision-blocker/v1","locator":"ep://planning/store/decision-blocker/external-driver-security-adr"},"payload":{"actor":"human:timo","at":1788480173839,"causation":"cmd-new-decision-blocker-external-driver-security-adr","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"decision-blocker:external-driver-security-adr@1#0~f18ab9cbb4d64564","executor":null,"recorded_at":"2026-09-04T00:02:53Z"}}
{"entity":"decision-blocker","version":1,"id":"external-driver-security-adr","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:external-driver-artifacts-stay-deferred"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000467","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788480173848,"causation":"cmd-decision-blocker-external-driver-security-adr-blocks-story-external-driver-artifacts-stay-deferred","change":{"change":"related","relation":"blocks","target":"story:external-driver-artifacts-stay-deferred"},"correlation":"protocol-artifact-relate","event_id":"decision-blocker:external-driver-security-adr@1#0~ed35301ed738b34f","executor":null,"recorded_at":"2026-09-04T00:02:53Z"}}
{"entity":"dependency-blocker","version":1,"id":"substrate-bundle-unsigned","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"## What would clear this\n\nArchitecture closed by ADR 0017 and substrate phase 3 is green; implementation remains blocked on an owner-signed substrate bundle.\n\nQuoted from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","title":"No owner-signed substrate bundle exists"},"args":{"command":"create-entity","data":{"body":"## What would clear this\n\nArchitecture closed by ADR 0017 and substrate phase 3 is green; implementation remains blocked on an owner-signed substrate bundle.\n\nQuoted from `docs/stories/S-029-substrate-events-enter-durable-delivery-with-gap-recovery.md`, the source's own `note:` field. **read**\n\n## Provenance\n\nMigrated 2026-09-04 by the `aep-planning:story-migration` skill. The source recorded this as\n`status: blocked`. A status field cannot tell an item parked on an external gate from one somebody\nis working on today, so the block is this artifact and the story it blocks stands at `active`.\n","status":"open","title":"No owner-signed substrate bundle exists"},"entity_type":"aep.dependency-blocker/v1","locator":"ep://planning/store/dependency-blocker/substrate-bundle-unsigned"},"payload":{"actor":"human:timo","at":1788480174033,"causation":"cmd-new-dependency-blocker-substrate-bundle-unsigned","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"dependency-blocker:substrate-bundle-unsigned@1#0~71fe182e57918718","executor":null,"recorded_at":"2026-09-04T00:02:54Z"}}
{"entity":"dependency-blocker","version":1,"id":"substrate-bundle-unsigned","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:substrate-events-enter-durable-delivery-with-gap-recovery"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000472","target":"01MEM0000000000000262"},"payload":{"actor":"human:timo","at":1788480174043,"causation":"cmd-dependency-blocker-substrate-bundle-unsigned-blocks-story-substrate-events-enter-durable-delivery-with-gap-recovery","change":{"change":"related","relation":"blocks","target":"story:substrate-events-enter-durable-delivery-with-gap-recovery"},"correlation":"protocol-artifact-relate","event_id":"dependency-blocker:substrate-bundle-unsigned@1#0~37317f18c5eeb298","executor":null,"recorded_at":"2026-09-04T00:02:54Z"}}
{"entity":"story","version":1,"id":"the-document-carries-the-callers-contract","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-resolve"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-resolve"}]},"command":"update-entity","target":"01MEM0000000000000286"},"payload":{"actor":"human:timo","at":1788480212454,"causation":"cmd-protocol-artifact-scope-story-the-document-carries-the-callers-contract-1788480212454","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-document-carries-the-callers-contract@4#0~62408b243e0083c9","executor":null,"recorded_at":"2026-09-04T00:03:32Z"}}
{"entity":"story","version":1,"id":"effects-are-read-never-derived","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/domain"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/domain"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788480212580,"causation":"cmd-protocol-artifact-scope-story-effects-are-read-never-derived-1788480212580","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:effects-are-read-never-derived@4#0~c961294fcc58df76","executor":null,"recorded_at":"2026-09-04T00:03:32Z"}}
{"entity":"story","version":1,"id":"the-lockfile-gets-a-verifier","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"command":"update-entity","target":"01MEM0000000000000301"},"payload":{"actor":"human:timo","at":1788480212699,"causation":"cmd-protocol-artifact-scope-story-the-lockfile-gets-a-verifier-1788480212699","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-lockfile-gets-a-verifier@4#0~e60a7e82bb420d92","executor":null,"recorded_at":"2026-09-04T00:03:32Z"}}
{"entity":"story","version":1,"id":"m5-flux-re-points-and-the-gitlab-plugin-retires","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"docs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"docs"}]},"command":"update-entity","target":"01MEM0000000000000184"},"payload":{"actor":"human:timo","at":1788480212819,"causation":"cmd-protocol-artifact-scope-story-m5-flux-re-points-and-the-gitlab-plugin-retires-1788480212819","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:m5-flux-re-points-and-the-gitlab-plugin-retires@4#0~fbe1511584b62ae2","executor":null,"recorded_at":"2026-09-04T00:03:32Z"}}
{"entity":"story","version":1,"id":"retire-the-quirks-umbrella","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"command":"update-entity","target":"01MEM0000000000000244"},"payload":{"actor":"human:timo","at":1788480212938,"causation":"cmd-protocol-artifact-scope-story-retire-the-quirks-umbrella-1788480212938","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:retire-the-quirks-umbrella@4#0~b2b19689e007e2d8","executor":null,"recorded_at":"2026-09-04T00:03:32Z"}}
{"entity":"story","version":1,"id":"the-explorer-works-against-the-new-site-json","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build"}]},"command":"update-entity","target":"01MEM0000000000000289"},"payload":{"actor":"human:timo","at":1788480213056,"causation":"cmd-protocol-artifact-scope-story-the-explorer-works-against-the-new-site-json-1788480213056","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-explorer-works-against-the-new-site-json@4#0~88766106b1402d9f","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"retire-the-flux-connectors-identity","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-resolve"},{"confidence":"cited","path":"web"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-resolve"},{"confidence":"cited","path":"web"}]},"command":"update-entity","target":"01MEM0000000000000241"},"payload":{"actor":"human:timo","at":1788480213176,"causation":"cmd-protocol-artifact-scope-story-retire-the-flux-connectors-identity-1788480213176","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:retire-the-flux-connectors-identity@4#0~788efe9bbf5425b8","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"a-ci-gate-exists","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build"}]},"command":"update-entity","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788480213297,"causation":"cmd-protocol-artifact-scope-story-a-ci-gate-exists-1788480213297","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:a-ci-gate-exists@4#0~d5a153544d0dbbec","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"orphaned-inputs-are-removed-or-re-owned","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"specs"},{"confidence":"cited","path":"web"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"specs"},{"confidence":"cited","path":"web"}]},"command":"update-entity","target":"01MEM0000000000000220"},"payload":{"actor":"human:timo","at":1788480213417,"causation":"cmd-protocol-artifact-scope-story-orphaned-inputs-are-removed-or-re-owned-1788480213417","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:orphaned-inputs-are-removed-or-re-owned@4#0~14567b5e908d08cf","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"beyond-http-facts-enter-the-document","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-resolve"},{"confidence":"cited","path":"crates/connector-spec"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-resolve"},{"confidence":"cited","path":"crates/connector-spec"}]},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788480213540,"causation":"cmd-protocol-artifact-scope-story-beyond-http-facts-enter-the-document-1788480213540","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:beyond-http-facts-enter-the-document@4#0~3d8acac6bbdded8f","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"one-zero-io-plan-seam-selects-a-closed-driver","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/connector-resolve"},{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connector-resolve"},{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000217"},"payload":{"actor":"human:timo","at":1788480213673,"causation":"cmd-protocol-artifact-scope-story-one-zero-io-plan-seam-selects-a-closed-driver-1788480213673","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:one-zero-io-plan-seam-selects-a-closed-driver@4#0~099427d12952d709","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"one-real-non-http-driver-proves-the-model","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000214"},"payload":{"actor":"human:timo","at":1788480213796,"causation":"cmd-protocol-artifact-scope-story-one-real-non-http-driver-proves-the-model-1788480213796","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:one-real-non-http-driver-proves-the-model@4#0~faa9a17fde275e93","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"direct-byte-session-establishment-is-operation-scoped","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000142"},"payload":{"actor":"human:timo","at":1788480213916,"causation":"cmd-protocol-artifact-scope-story-direct-byte-session-establishment-is-operation-scoped-1788480213916","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:direct-byte-session-establishment-is-operation-scoped@4#0~1c980728bbfc5e66","executor":null,"recorded_at":"2026-09-04T00:03:33Z"}}
{"entity":"story","version":1,"id":"external-driver-artifacts-stay-deferred","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"},{"confidence":"cited","path":"docs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"},{"confidence":"cited","path":"docs"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788480214036,"causation":"cmd-protocol-artifact-scope-story-external-driver-artifacts-stay-deferred-1788480214036","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:external-driver-artifacts-stay-deferred@4#0~63afa3e09eb17137","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"substrate-events-enter-durable-delivery-with-gap-recovery","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000265"},"payload":{"actor":"human:timo","at":1788480214156,"causation":"cmd-protocol-artifact-scope-story-substrate-events-enter-durable-delivery-with-gap-recovery-1788480214156","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:substrate-events-enter-durable-delivery-with-gap-recovery@4#0~34f7a8a2ae6eadb0","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"sip-driver-terminates-one-governed-call","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/driver-sip"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/driver-sip"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000256"},"payload":{"actor":"human:timo","at":1788480214274,"causation":"cmd-protocol-artifact-scope-story-sip-driver-terminates-one-governed-call-1788480214274","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:sip-driver-terminates-one-governed-call@4#0~43a7037a24c31fcd","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"neutral-rtvbp-bridges-the-call-to-an-application-channel","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/rtvbp-voice-endpoint"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"},{"confidence":"cited","path":"crates/voice-runtime"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/rtvbp-voice-endpoint"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"},{"confidence":"cited","path":"crates/voice-runtime"}]},"command":"update-entity","target":"01MEM0000000000000202"},"payload":{"actor":"human:timo","at":1788480214393,"causation":"cmd-protocol-artifact-scope-story-neutral-rtvbp-bridges-the-call-to-an-application-channel-1788480214393","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:neutral-rtvbp-bridges-the-call-to-an-application-channel@4#0~df0c885ef6b39e07","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"the-cli-runs-without-being-configured-by-hand","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/protocol"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/protocol"}]},"command":"update-entity","target":"01MEM0000000000000280"},"payload":{"actor":"human:timo","at":1788480214512,"causation":"cmd-protocol-artifact-scope-story-the-cli-runs-without-being-configured-by-hand-1788480214512","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-cli-runs-without-being-configured-by-hand@4#0~325cd4f9cf9e3f58","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"the-os-keyring-becomes-the-local-store","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/connector-secrets"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connector-secrets"}]},"command":"update-entity","target":"01MEM0000000000000304"},"payload":{"actor":"human:timo","at":1788480214632,"causation":"cmd-protocol-artifact-scope-story-the-os-keyring-becomes-the-local-store-1788480214632","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-os-keyring-becomes-the-local-store@4#0~f3384eb1617f3186","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"state-becomes-a-port","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000262"},"payload":{"actor":"human:timo","at":1788480214751,"causation":"cmd-protocol-artifact-scope-story-state-becomes-a-port-1788480214751","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:state-becomes-a-port@4#0~3c5825b39e980dd9","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"admission-proofs-become-unforgeable","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788480214870,"causation":"cmd-protocol-artifact-scope-story-admission-proofs-become-unforgeable-1788480214870","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:admission-proofs-become-unforgeable@4#0~1b9a3847af3be67a","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"a-grant-decision-is-evaluated-not-asserted","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788480214991,"causation":"cmd-protocol-artifact-scope-story-a-grant-decision-is-evaluated-not-asserted-1788480214991","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:a-grant-decision-is-evaluated-not-asserted@4#0~d583d29b52bf1936","executor":null,"recorded_at":"2026-09-04T00:03:34Z"}}
{"entity":"story","version":1,"id":"an-approval-is-redeemed-exactly-once","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788480215110,"causation":"cmd-protocol-artifact-scope-story-an-approval-is-redeemed-exactly-once-1788480215110","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:an-approval-is-redeemed-exactly-once@4#0~0f877b5da9c8f87f","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"the-hosted-route-enforces-instead-of-refusing","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000295"},"payload":{"actor":"human:timo","at":1788480215230,"causation":"cmd-protocol-artifact-scope-story-the-hosted-route-enforces-instead-of-refusing-1788480215230","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-hosted-route-enforces-instead-of-refusing@4#0~783eae1e1a2f2997","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"a-session-signal-carries-authority","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/domain"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788480215350,"causation":"cmd-protocol-artifact-scope-story-a-session-signal-carries-authority-1788480215350","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:a-session-signal-carries-authority@4#0~efa2e460191256a7","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"the-platform-connector-is-named-platform","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000307"},"payload":{"actor":"human:timo","at":1788480215470,"causation":"cmd-protocol-artifact-scope-story-the-platform-connector-is-named-platform-1788480215470","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-platform-connector-is-named-platform@5#0~4d72e7395cb4fa71","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"the-hosted-server-speaks-mcp-through-the-admission-seam","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000298"},"payload":{"actor":"human:timo","at":1788480215590,"causation":"cmd-protocol-artifact-scope-story-the-hosted-server-speaks-mcp-through-the-admission-seam-1788480215590","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-hosted-server-speaks-mcp-through-the-admission-seam@4#0~c3adc1c91b43229a","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"pod-logs-are-a-hosted-read-only-kubernetes-operation","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000229"},"payload":{"actor":"human:timo","at":1788480215710,"causation":"cmd-protocol-artifact-scope-story-pod-logs-are-a-hosted-read-only-kubernetes-operation-1788480215710","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:pod-logs-are-a-hosted-read-only-kubernetes-operation@4#0~ec3c98f2cf37d01a","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"mysql-and-postgresql-become-connectors","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog"},{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000199"},"payload":{"actor":"human:timo","at":1788480215829,"causation":"cmd-protocol-artifact-scope-story-mysql-and-postgresql-become-connectors-1788480215829","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:mysql-and-postgresql-become-connectors@4#0~f78eb1aa73eb8336","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"kubernetes-discovers-database-endpoints","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000169"},"payload":{"actor":"human:timo","at":1788480215953,"causation":"cmd-protocol-artifact-scope-story-kubernetes-discovers-database-endpoints-1788480215953","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:kubernetes-discovers-database-endpoints@4#0~ec171cc82ee708db","executor":null,"recorded_at":"2026-09-04T00:03:35Z"}}
{"entity":"story","version":1,"id":"monitoring-joins-the-mcp-toolset","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000190"},"payload":{"actor":"human:timo","at":1788480216074,"causation":"cmd-protocol-artifact-scope-story-monitoring-joins-the-mcp-toolset-1788480216074","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:monitoring-joins-the-mcp-toolset@4#0~ba003c33d04a068b","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"discovery-reads-the-cluster-scoped-database-resources","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788480216194,"causation":"cmd-protocol-artifact-scope-story-discovery-reads-the-cluster-scoped-database-resources-1788480216194","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:discovery-reads-the-cluster-scoped-database-resources@4#0~df15e8d78dbf504d","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"the-deployment-list-pages-instead-of-refusing","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000283"},"payload":{"actor":"human:timo","at":1788480216313,"causation":"cmd-protocol-artifact-scope-story-the-deployment-list-pages-instead-of-refusing-1788480216313","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-deployment-list-pages-instead-of-refusing@4#0~4a4c6ac049ac791e","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"monitoring-tool-schemas-tell-the-truth","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"}]},"command":"update-entity","target":"01MEM0000000000000196"},"payload":{"actor":"human:timo","at":1788480216435,"causation":"cmd-protocol-artifact-scope-story-monitoring-tool-schemas-tell-the-truth-1788480216435","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:monitoring-tool-schemas-tell-the-truth@4#0~cb609233da25315d","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"the-server-serves-its-own-openapi-contract","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"docs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"docs"}]},"command":"update-entity","target":"01MEM0000000000000310"},"payload":{"actor":"human:timo","at":1788480216557,"causation":"cmd-protocol-artifact-scope-story-the-server-serves-its-own-openapi-contract-1788480216557","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-server-serves-its-own-openapi-contract@4#0~c7f24d6b74d69794","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"docs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"docs"}]},"command":"update-entity","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788480216677,"causation":"cmd-protocol-artifact-scope-story-a-public-documentation-page-renders-the-contract-1788480216677","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:a-public-documentation-page-renders-the-contract@4#0~7ef5f194a869deac","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"one-oauth-implementation-not-three","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab"},{"confidence":"cited","path":"crates/integration-jira"},{"confidence":"cited","path":"crates/integration-slack"},{"confidence":"cited","path":"crates/service"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab"},{"confidence":"cited","path":"crates/integration-jira"},{"confidence":"cited","path":"crates/integration-slack"},{"confidence":"cited","path":"crates/service"}]},"command":"update-entity","target":"01MEM0000000000000211"},"payload":{"actor":"human:timo","at":1788480216796,"causation":"cmd-protocol-artifact-scope-story-one-oauth-implementation-not-three-1788480216796","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:one-oauth-implementation-not-three@4#0~4d6ca8b2ac61bd2e","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"a-provider-can-hold-a-credential-it-cannot-spend","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"command":"update-entity","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788480216916,"causation":"cmd-protocol-artifact-scope-story-a-provider-can-hold-a-credential-it-cannot-spend-1788480216916","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:a-provider-can-hold-a-credential-it-cannot-spend@4#0~05e22259de65901f","executor":null,"recorded_at":"2026-09-04T00:03:36Z"}}
{"entity":"story","version":1,"id":"claude-code-joins-the-catalog","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"providers"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"providers"}]},"command":"update-entity","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788480217037,"causation":"cmd-protocol-artifact-scope-story-claude-code-joins-the-catalog-1788480217037","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:claude-code-joins-the-catalog@4#0~14b0b4af636e24f7","executor":null,"recorded_at":"2026-09-04T00:03:37Z"}}
{"entity":"story","version":1,"id":"an-agent-attempt-can-lease-a-user-subscription","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-client"},{"confidence":"cited","path":"crates/connectors-runtime"},{"confidence":"cited","path":"crates/identity-http"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/subscription-custody"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-client"},{"confidence":"cited","path":"crates/connectors-runtime"},{"confidence":"cited","path":"crates/identity-http"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/subscription-custody"}]},"command":"update-entity","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788480217157,"causation":"cmd-protocol-artifact-scope-story-an-agent-attempt-can-lease-a-user-subscription-1788480217157","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:an-agent-attempt-can-lease-a-user-subscription@4#0~8da4e6e9b9261fe2","executor":null,"recorded_at":"2026-09-04T00:03:37Z"}}
{"entity":"story","version":1,"id":"claude-subscriptions-connect-with-pkce-and-refresh","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-client"},{"confidence":"cited","path":"crates/connectors-runtime"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/subscription-custody"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-client"},{"confidence":"cited","path":"crates/connectors-runtime"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/subscription-custody"}]},"command":"update-entity","target":"01MEM0000000000000118"},"payload":{"actor":"human:timo","at":1788480217276,"causation":"cmd-protocol-artifact-scope-story-claude-subscriptions-connect-with-pkce-and-refresh-1788480217276","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:claude-subscriptions-connect-with-pkce-and-refresh@4#0~b98dcd5954f76084","executor":null,"recorded_at":"2026-09-04T00:03:37Z"}}
{"entity":"story","version":1,"id":"the-document-carries-the-callers-contract","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000286","to":"implemented"},"payload":{"actor":"human:timo","at":1788482225593,"causation":"cmd-move-story-the-document-carries-the-callers-contract-1788482225593","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-document-carries-the-callers-contract@5#0~16f892a9e833f8c0","executor":null,"recorded_at":"2026-09-04T00:37:05Z"}}
{"entity":"story","version":1,"id":"the-document-carries-the-callers-contract","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-001-the-document-carries-the-callers-contract.md:35`. **read**\n\n- [x] `catalog/connector-document.schema.json` gains all three, **additively**: a caller-facing\n      symbol per parameter, the projection description, and the contract `input_schema` in its\n      **lowered, caller-typed** form — computed at build time by `catalog-build` (which has the\n      lowering, `src/contract.rs`) and stored as data. Every committed canonical document was\n      regenerated whole-catalogue. → `schema_version` stays 1, not a bump: additive per the\n      schema's own rule and C-537; the resolution is design 04 §2.\n- [x] A consumer constructs the **complete** caller-facing contract — name, description,\n      `input_schema`, `expose` — from the document's data alone.\n      → `catalog::Operation::{contract_description, input_schema, expose}`;\n      `consumer_api.rs::the_caller_contract_is_document_data_alone` names the consumer path and\n      proves the absence (no source-form parser in the crate's dependency closure).\n- [x] The symbol is written by the lowering, and the second reproduction of the allocator retires:\n      `crates/connector-resolve` reads the document field (its copy survives only as the\n      pre-S-001-document fallback, exercised by no build here). Disagreement is refused at build:\n      the fixed-point invariant catches any committed symbol the allocator would not produce, and\n      `the_contract_and_the_params_state_the_same_symbols` names the operation whose two symbol\n      statements drift.\n- [x] The const-pinned trap is closed: the allocator reserves a symbol for every body parameter,\n      `const`-pinned ones included, and the document states the shifted result.\n      → `contract.rs::a_const_pinned_body_field_reserves_the_symbol_a_later_field_must_shift_past`\n      (build side) and `connector-resolve/document.rs`'s reader-side twin.\n- [x] The `format = \"origin\"` blind spot is closed.\n      → `catalog_invariants.rs::every_format_origin_field_lowers_to_the_origin_slot`, with the\n      cannot-go-blind counter (gitlab is the shipped case).\n- [x] **`CredentialRequirement` stops being derived.** The document publishes\n      `credential_requirement` (C-206's tokens), computed at build where `Operation::auth`'s\n      `Option` still distinguishes declared-empty from never-declared;\n      `table.rs::credential_requirement` reads it and the default-resolving derivation is gone.\n      → failing-first pair: `table.rs::tests::the_document_tells_apart_the_pair_the_derivation_could_not`.\n- [x] **`Acquisition::Minted` is reachable** — the carry arm, not the deferral: the document\n      gains the optional `produces_credential { credential, secret }` join (zero byte cost on\n      every shipped document; none declares one) and `table.rs` constructs the variant from it,\n      refusing conflicting provenance by name.\n      → `table.rs::tests::a_minting_join_in_the_document_reaches_acquisition_minted`.\n- [x] Determinism is unbroken: the fixed-point and two-plans invariants are green over the\n      regenerated tree, and the one-time differential against the predecessor ran against its\n      C-552 regenerated documents (the shared fields, which its pack predates): **835/835\n      operations, 1518/1518 symbols** — `symbol`, `contract.description`, `contract.input_schema`\n      all byte-equal to the engine-derived values. Effects excluded (S-002).\n\n## Context\n\nClose the gap C-538 measured in the predecessor and C-552 was filed to fix: three things a caller or\na model receives are recovered by **parsing emitted source**, because the canonical document does not\ncarry them. Widen `catalog/connector-document.schema.json` and the lowering so a consumer builds the\ncomplete caller-facing contract from document data alone — the precondition for architecture §5's\nabsolute rule, *no runtime parsing of any source form, ever; the plan is derived from document data\nonly.*\n\nThe three fields, as C-552 measured them:\n\n1. **the caller-facing symbol** — the document publishes a parameter's IR `name` (`time.start`,\n   `$top`) and its `wire` name, but the contract a caller sees advertises the allocated symbol\n   (`time_start`, `_top`, `response_2`). The predecessor reproduced the allocator in a second crate,\n   held honest only by a differential gate comparing against the emitted declaration;\n2. **the error-envelope-extended description** — the projected description appends the\n   error-envelope paragraph that the document's one-line summary lacks (measured on\n   `airtable-record-get`);\n3. **the contract `input_schema`** — lowered from the engine's types, where the document carries the\n   vendor's raw JSON Schema (`int64 integer` vs `number`).\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve] · design `../design/04-the-callers-contract.md`. **read**\n\nSource `note:` field, quoted: “ported from flux-connectors C-552 (measured by C-538). Architecture §2 day-one change 1, first half: the caller-facing symbol, the error-envelope-extended description and the contract input_schema. Per-operation effects — C-552's fourth field — are S-002, because they have a second consumer (grant admission) and a mistake of their own to close”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-001-the-document-carries-the-callers-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-001-the-document-carries-the-callers-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 6 revision(s)\n- Legacy id `S-001`, recorded as the reference `legacy:S-001`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-001-the-document-carries-the-callers-contract.md:35`. **read**\n\n- [x] `catalog/connector-document.schema.json` gains all three, **additively**: a caller-facing\n      symbol per parameter, the projection description, and the contract `input_schema` in its\n      **lowered, caller-typed** form — computed at build time by `catalog-build` (which has the\n      lowering, `src/contract.rs`) and stored as data. Every committed canonical document was\n      regenerated whole-catalogue. → `schema_version` stays 1, not a bump: additive per the\n      schema's own rule and C-537; the resolution is design 04 §2.\n- [x] A consumer constructs the **complete** caller-facing contract — name, description,\n      `input_schema`, `expose` — from the document's data alone.\n      → `catalog::Operation::{contract_description, input_schema, expose}`;\n      `consumer_api.rs::the_caller_contract_is_document_data_alone` names the consumer path and\n      proves the absence (no source-form parser in the crate's dependency closure).\n- [x] The symbol is written by the lowering, and the second reproduction of the allocator retires:\n      `crates/connector-resolve` reads the document field (its copy survives only as the\n      pre-S-001-document fallback, exercised by no build here). Disagreement is refused at build:\n      the fixed-point invariant catches any committed symbol the allocator would not produce, and\n      `the_contract_and_the_params_state_the_same_symbols` names the operation whose two symbol\n      statements drift.\n- [x] The const-pinned trap is closed: the allocator reserves a symbol for every body parameter,\n      `const`-pinned ones included, and the document states the shifted result.\n      → `contract.rs::a_const_pinned_body_field_reserves_the_symbol_a_later_field_must_shift_past`\n      (build side) and `connector-resolve/document.rs`'s reader-side twin.\n- [x] The `format = \"origin\"` blind spot is closed.\n      → `catalog_invariants.rs::every_format_origin_field_lowers_to_the_origin_slot`, with the\n      cannot-go-blind counter (gitlab is the shipped case).\n- [x] **`CredentialRequirement` stops being derived.** The document publishes\n      `credential_requirement` (C-206's tokens), computed at build where `Operation::auth`'s\n      `Option` still distinguishes declared-empty from never-declared;\n      `table.rs::credential_requirement` reads it and the default-resolving derivation is gone.\n      → failing-first pair: `table.rs::tests::the_document_tells_apart_the_pair_the_derivation_could_not`.\n- [x] **`Acquisition::Minted` is reachable** — the carry arm, not the deferral: the document\n      gains the optional `produces_credential { credential, secret }` join (zero byte cost on\n      every shipped document; none declares one) and `table.rs` constructs the variant from it,\n      refusing conflicting provenance by name.\n      → `table.rs::tests::a_minting_join_in_the_document_reaches_acquisition_minted`.\n- [x] Determinism is unbroken: the fixed-point and two-plans invariants are green over the\n      regenerated tree, and the one-time differential against the predecessor ran against its\n      C-552 regenerated documents (the shared fields, which its pack predates): **835/835\n      operations, 1518/1518 symbols** — `symbol`, `contract.description`, `contract.input_schema`\n      all byte-equal to the engine-derived values. Effects excluded (S-002).\n\n## Context\n\nClose the gap C-538 measured in the predecessor and C-552 was filed to fix: three things a caller or\na model receives are recovered by **parsing emitted source**, because the canonical document does not\ncarry them. Widen `catalog/connector-document.schema.json` and the lowering so a consumer builds the\ncomplete caller-facing contract from document data alone — the precondition for architecture §5's\nabsolute rule, *no runtime parsing of any source form, ever; the plan is derived from document data\nonly.*\n\nThe three fields, as C-552 measured them:\n\n1. **the caller-facing symbol** — the document publishes a parameter's IR `name` (`time.start`,\n   `$top`) and its `wire` name, but the contract a caller sees advertises the allocated symbol\n   (`time_start`, `_top`, `response_2`). The predecessor reproduced the allocator in a second crate,\n   held honest only by a differential gate comparing against the emitted declaration;\n2. **the error-envelope-extended description** — the projected description appends the\n   error-envelope paragraph that the document's one-line summary lacks (measured on\n   `airtable-record-get`);\n3. **the contract `input_schema`** — lowered from the engine's types, where the document carries the\n   vendor's raw JSON Schema (`int64 integer` vs `number`).\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve] · design `../design/04-the-callers-contract.md`. **read**\n\nSource `note:` field, quoted: “ported from flux-connectors C-552 (measured by C-538). Architecture §2 day-one change 1, first half: the caller-facing symbol, the error-envelope-extended description and the contract input_schema. Per-operation effects — C-552's fourth field — are S-002, because they have a second consumer (grant admission) and a mistake of their own to close”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-001-the-document-carries-the-callers-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-001-the-document-carries-the-callers-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 6 revision(s)\n- Legacy id `S-001`, recorded as the reference `legacy:S-001`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000286"},"payload":{"actor":"human:timo","at":1788482225722,"causation":"cmd-protocol-artifact-body-section-story-the-document-carries-the-callers-contract-1788482225722","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-document-carries-the-callers-contract@6#0~720111db6f44b773","executor":null,"recorded_at":"2026-09-04T00:37:05Z"}}
{"entity":"story","version":1,"id":"the-lockfile-gets-a-verifier","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000301","to":"implemented"},"payload":{"actor":"human:timo","at":1788482225916,"causation":"cmd-move-story-the-lockfile-gets-a-verifier-1788482225916","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-lockfile-gets-a-verifier@5#0~7c9d30211b9e31ba","executor":null,"recorded_at":"2026-09-04T00:37:05Z"}}
{"entity":"story","version":1,"id":"the-lockfile-gets-a-verifier","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:31`. **read**\n\n- [x] `catalog check` recomputes every hash in `connectors.lock` whose bytes are identified by the\n      current v1 format — each provider's\n      declaration bytes, each vendored spec document, each committed artifact by repository-relative\n      path — and **exits non-zero** on any mismatch, naming the provider and which input moved. A\n      populated predecessor-reserved hash without addressable bytes is refused as unverifiable.\n      The ignored site projection is regenerated build output, not reviewed state, so it is not\n      counted as a verified committed artifact.\n- [x] `check` performs **no network IO**, so CI runs it offline and hermetically. Upstream drift\n      (has the vendor's document moved?) is a different question with a different answer and is not\n      folded in here.\n- [x] Four drift classes are distinct, named failures, each proven by a seeded case: a mutated\n      artifact; an edited provider declaration (**including a comment-only edit** — the hash domain\n      is file bytes, and a comment can change what an operator believes the connector does); a\n      re-vendored spec whose bytes changed; and a lock row that no longer matches its artifact.\n- [x] Coverage is checked in both directions: a provider with no lock row fails, and a lock row with\n      no provider fails. A verifier that only checks the rows it finds cannot see a deletion.\n- [x] Failing-first test: a mutated committed artifact makes `check` exit non-zero and name it; a\n      clean tree exits zero and prints the counts it verified (providers, artifacts) rather than a\n      bare \"ok\".\n- [x] `check` is in the repository's gate, and the gate's definition says so — a verifier nothing\n      runs is the same as no verifier.\n\n## Context\n\nGive `connectors.lock` the verifier it never had, so vision principle 1 — *review equals execution;\nevery derived form is byte-identical to the committed canonical documents* — is enforced by a command\nCI runs rather than asserted in a CHANGELOG. Drift is not preventable; it is **detectable**, and that\nis the whole design.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “the predecessor's lockfile has a writer (C-189) and no verifier (C-14, never built), so provenance is computed, committed, and never checked. Architecture §2 day-one change 2. Review-equals-execution is a claim until something recomputes it in CI”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-003-the-lockfile-gets-a-verifier.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-003`, recorded as the reference `legacy:S-003`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:31`. **read**\n\n- [x] `catalog check` recomputes every hash in `connectors.lock` whose bytes are identified by the\n      current v1 format — each provider's\n      declaration bytes, each vendored spec document, each committed artifact by repository-relative\n      path — and **exits non-zero** on any mismatch, naming the provider and which input moved. A\n      populated predecessor-reserved hash without addressable bytes is refused as unverifiable.\n      The ignored site projection is regenerated build output, not reviewed state, so it is not\n      counted as a verified committed artifact.\n- [x] `check` performs **no network IO**, so CI runs it offline and hermetically. Upstream drift\n      (has the vendor's document moved?) is a different question with a different answer and is not\n      folded in here.\n- [x] Four drift classes are distinct, named failures, each proven by a seeded case: a mutated\n      artifact; an edited provider declaration (**including a comment-only edit** — the hash domain\n      is file bytes, and a comment can change what an operator believes the connector does); a\n      re-vendored spec whose bytes changed; and a lock row that no longer matches its artifact.\n- [x] Coverage is checked in both directions: a provider with no lock row fails, and a lock row with\n      no provider fails. A verifier that only checks the rows it finds cannot see a deletion.\n- [x] Failing-first test: a mutated committed artifact makes `check` exit non-zero and name it; a\n      clean tree exits zero and prints the counts it verified (providers, artifacts) rather than a\n      bare \"ok\".\n- [x] `check` is in the repository's gate, and the gate's definition says so — a verifier nothing\n      runs is the same as no verifier.\n\n## Context\n\nGive `connectors.lock` the verifier it never had, so vision principle 1 — *review equals execution;\nevery derived form is byte-identical to the committed canonical documents* — is enforced by a command\nCI runs rather than asserted in a CHANGELOG. Drift is not preventable; it is **detectable**, and that\nis the whole design.\n\nSource frontmatter: pillar Catalog · areas [catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “the predecessor's lockfile has a writer (C-189) and no verifier (C-14, never built), so provenance is computed, committed, and never checked. Architecture §2 day-one change 2. Review-equals-execution is a claim until something recomputes it in CI”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-003-the-lockfile-gets-a-verifier.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-003-the-lockfile-gets-a-verifier.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-003`, recorded as the reference `legacy:S-003`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000301"},"payload":{"actor":"human:timo","at":1788482226041,"causation":"cmd-protocol-artifact-body-section-story-the-lockfile-gets-a-verifier-1788482226041","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-lockfile-gets-a-verifier@6#0~2f21386c1d1ec313","executor":null,"recorded_at":"2026-09-04T00:37:06Z"}}
{"entity":"story","version":1,"id":"retire-the-quirks-umbrella","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000244","to":"implemented"},"payload":{"actor":"human:timo","at":1788482226228,"causation":"cmd-move-story-retire-the-quirks-umbrella-1788482226228","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:retire-the-quirks-umbrella@5#0~9c4bd661644417aa","executor":null,"recorded_at":"2026-09-04T00:37:06Z"}}
{"entity":"story","version":1,"id":"retire-the-quirks-umbrella","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-015-retire-the-quirks-umbrella.md:54`. **read**\n\n- [x] `pagination`, `rate_limit` and `error_envelope` are **first-class named fields** at their\n      correct scope in `catalog/connector-document.schema.json` and in the lowering — per operation\n      where they vary by endpoint, on the service where they are a property of the whole surface.\n      The scope choice per field is decided once and recorded, not left to each provider.\n- [x] **No `quirks` key remains anywhere**: not in the schema, not in any canonical document, not in\n      the pack, not in any projection (explorer, catalogue view, effective catalogue), not in a\n      provider declaration. Every committed document is regenerated — a whole-catalog change, so the\n      regeneration is coordinator-owned.\n- [x] `grep -ri quirk` across `catalog/` and `crates/` returns **only historical references in\n      documentation** (design series, story files, CHANGELOG). A hit in a type name, a field name, a\n      serde attribute or a test name fails the story.\n- [x] The genuinely deviant declarations are handled deliberately, in one of two ways, and the reason\n      is recorded: either a rare, precisely scoped **`workarounds`** category is introduced in which\n      **each entry names the specific vendor defect it compensates for** and carries the predecessor's\n      required attribution (`grant`/`behaviour`/`attribution`/`measured` — an unattributed claim is\n      indistinguishable from a guess that aged), or nothing is introduced at all and the babelforce\n      token-endpoint measurements find a home on the auth declaration itself. **Nothing generic is\n      introduced.** If it is not in the specification, it does not become a general thing.\n- [x] The migration is a rename with proof, not a redesign: the promoted fields' contents are\n      unchanged, and a test shows that for every provider, the set of declared pagination /\n      rate-limit / error-envelope facts before and after is identical. Any *behavioural* change to\n      those fields belongs to [S-005](../../../docs/stories/S-005-header-name-rate-limit-retry.md), not here.\n- [x] Determinism and the fixed point hold after regeneration: two independent builds are\n      byte-identical, `catalog check` ([S-003](../../../docs/stories/S-003-the-lockfile-gets-a-verifier.md)) is\n      green on the new lock, and the build reports nothing left to write.\n- [x] Failing-first test named — today the schema refuses a top-level `pagination` on an operation\n      and requires the umbrella.\n\n## Context\n\nDelete the `quirks` key and promote what it holds to first-class named fields on the operation and\nthe service, so the schema stops calling universal API traits *deviations*. Pagination, rate limits\nand error envelopes are how HTTP APIs work; filing them under a word that means \"this vendor is\nbroken\" mis-describes ~30 shipped providers and makes the one genuinely deviant case invisible\ninside the same bag.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “owner-raised: `quirks` began as a workaround bag for the broken babelforce API and drifted into a junk drawer holding universal API traits. Every other catalog in the field declares these as plain fields (Nango paginate/retry, Airbyte paginator/error_handler). Schema evolution — must run AFTER the M1 byte-identity differential passes, same wave as S-001/S-002”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-015-retire-the-quirks-umbrella.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-015-retire-the-quirks-umbrella.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-015`, recorded as the reference `legacy:S-015`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-015-retire-the-quirks-umbrella.md:54`. **read**\n\n- [x] `pagination`, `rate_limit` and `error_envelope` are **first-class named fields** at their\n      correct scope in `catalog/connector-document.schema.json` and in the lowering — per operation\n      where they vary by endpoint, on the service where they are a property of the whole surface.\n      The scope choice per field is decided once and recorded, not left to each provider.\n- [x] **No `quirks` key remains anywhere**: not in the schema, not in any canonical document, not in\n      the pack, not in any projection (explorer, catalogue view, effective catalogue), not in a\n      provider declaration. Every committed document is regenerated — a whole-catalog change, so the\n      regeneration is coordinator-owned.\n- [x] `grep -ri quirk` across `catalog/` and `crates/` returns **only historical references in\n      documentation** (design series, story files, CHANGELOG). A hit in a type name, a field name, a\n      serde attribute or a test name fails the story.\n- [x] The genuinely deviant declarations are handled deliberately, in one of two ways, and the reason\n      is recorded: either a rare, precisely scoped **`workarounds`** category is introduced in which\n      **each entry names the specific vendor defect it compensates for** and carries the predecessor's\n      required attribution (`grant`/`behaviour`/`attribution`/`measured` — an unattributed claim is\n      indistinguishable from a guess that aged), or nothing is introduced at all and the babelforce\n      token-endpoint measurements find a home on the auth declaration itself. **Nothing generic is\n      introduced.** If it is not in the specification, it does not become a general thing.\n- [x] The migration is a rename with proof, not a redesign: the promoted fields' contents are\n      unchanged, and a test shows that for every provider, the set of declared pagination /\n      rate-limit / error-envelope facts before and after is identical. Any *behavioural* change to\n      those fields belongs to [S-005](../../../docs/stories/S-005-header-name-rate-limit-retry.md), not here.\n- [x] Determinism and the fixed point hold after regeneration: two independent builds are\n      byte-identical, `catalog check` ([S-003](../../../docs/stories/S-003-the-lockfile-gets-a-verifier.md)) is\n      green on the new lock, and the build reports nothing left to write.\n- [x] Failing-first test named — today the schema refuses a top-level `pagination` on an operation\n      and requires the umbrella.\n\n## Context\n\nDelete the `quirks` key and promote what it holds to first-class named fields on the operation and\nthe service, so the schema stops calling universal API traits *deviations*. Pagination, rate limits\nand error envelopes are how HTTP APIs work; filing them under a word that means \"this vendor is\nbroken\" mis-describes ~30 shipped providers and makes the one genuinely deviant case invisible\ninside the same bag.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec]. **read**\n\nSource `note:` field, quoted: “owner-raised: `quirks` began as a workaround bag for the broken babelforce API and drifted into a junk drawer holding universal API traits. Every other catalog in the field declares these as plain fields (Nango paginate/retry, Airbyte paginator/error_handler). Schema evolution — must run AFTER the M1 byte-identity differential passes, same wave as S-001/S-002”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-015-retire-the-quirks-umbrella.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-015-retire-the-quirks-umbrella.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 4 revision(s)\n- Legacy id `S-015`, recorded as the reference `legacy:S-015`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000244"},"payload":{"actor":"human:timo","at":1788482226352,"causation":"cmd-protocol-artifact-body-section-story-retire-the-quirks-umbrella-1788482226352","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:retire-the-quirks-umbrella@6#0~1bf6fa054f6458fb","executor":null,"recorded_at":"2026-09-04T00:37:06Z"}}
{"entity":"story","version":1,"id":"the-explorer-works-against-the-new-site-json","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000289","to":"implemented"},"payload":{"actor":"human:timo","at":1788482226549,"causation":"cmd-move-story-the-explorer-works-against-the-new-site-json-1788482226549","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-explorer-works-against-the-new-site-json@5#0~a3531a322758cf9f","executor":null,"recorded_at":"2026-09-04T00:37:06Z"}}
{"entity":"story","version":1,"id":"the-explorer-works-against-the-new-site-json","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:21`. **read**\n\n- [x] The dormant `web/` application, its Node manifests, tests, schemas, and brand copies are\n      removed together.\n- [x] `catalog-build` no longer constructs or writes `web/public/catalog.json`; the site-only model,\n      renderer, status projection, and surface helpers are removed from the build graph.\n- [x] JSON governance, source ownership, ignore rules, architecture fences, and local gate guidance\n      no longer name the removed projection.\n- [x] Catalog browsing is served from the hosted, authenticated Catalog protocol and presented by\n      DevCenter, while canonical documents and `catalog.pack` remain the reviewed build artifacts.\n\n## Context\n\nSource frontmatter: pillar Catalog · areas [catalog-build] · priority 7. **read**\n\nSource `note:` field, quoted: “Resolved by retiring the dormant repository explorer and its duplicate site projection after the hosted catalog contract and DevCenter became the supported browsing path”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 4 revision(s)\n- Legacy id `S-018`, recorded as the reference `legacy:S-018`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:21`. **read**\n\n- [x] The dormant `web/` application, its Node manifests, tests, schemas, and brand copies are\n      removed together.\n- [x] `catalog-build` no longer constructs or writes `web/public/catalog.json`; the site-only model,\n      renderer, status projection, and surface helpers are removed from the build graph.\n- [x] JSON governance, source ownership, ignore rules, architecture fences, and local gate guidance\n      no longer name the removed projection.\n- [x] Catalog browsing is served from the hosted, authenticated Catalog protocol and presented by\n      DevCenter, while canonical documents and `catalog.pack` remain the reviewed build artifacts.\n\n## Context\n\nSource frontmatter: pillar Catalog · areas [catalog-build] · priority 7. **read**\n\nSource `note:` field, quoted: “Resolved by retiring the dormant repository explorer and its duplicate site projection after the hosted catalog contract and DevCenter became the supported browsing path”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-018-the-explorer-works-against-the-new-site-json.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 4 revision(s)\n- Legacy id `S-018`, recorded as the reference `legacy:S-018`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000289"},"payload":{"actor":"human:timo","at":1788482226985,"causation":"cmd-protocol-artifact-body-section-story-the-explorer-works-against-the-new-site-json-1788482226985","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-explorer-works-against-the-new-site-json@6#0~f9d614d229b881c2","executor":null,"recorded_at":"2026-09-04T00:37:06Z"}}
{"entity":"story","version":1,"id":"retire-the-flux-connectors-identity","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000241","to":"implemented"},"payload":{"actor":"human:timo","at":1788482227296,"causation":"cmd-move-story-retire-the-flux-connectors-identity-1788482227296","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:retire-the-flux-connectors-identity@5#0~eabc6b4349ccbf4b","executor":null,"recorded_at":"2026-09-04T00:37:07Z"}}
{"entity":"story","version":1,"id":"retire-the-flux-connectors-identity","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-019-retire-the-flux-connectors-identity.md:42`. **read**\n\n- [x] The generator identity names **this** repository, the workspace version is chosen deliberately\n      (a fresh line, or a continuation of 0.26.0 — either is defensible), and the `User-Agent` product\n      token matches both. All three move in **one commit** with **one** whole-catalogue regeneration:\n      55 documents, the pack digest, `connectors.lock`, `web/public/catalog.json`.\n- [x] The reasons are replaced, not deleted: `Cargo.toml`'s load-bearing comment and `seam.rs`'s\n      generator doc say what the version means **now** (and that the differential is retired), rather\n      than losing the paragraph that explained the old pin.\n- [x] `DEFAULT_USER_AGENT`'s three assertions in `request.rs` move with the value, so the constant\n      stays derived from the manifest (`CARGO_PKG_VERSION`, `CARGO_PKG_REPOSITORY`) and cannot go\n      stale at a release.\n- [x] The **whole identity inventory is enumerated and each item decided** — renamed now, or kept with\n      a written reason. Known members, measured post-M1:\n      the pack magic `flux-connectors-catalog-pack` (`crates/catalog-reader/src/lib.rs:70` — renaming\n      it is a pack-format break, so decide it here or state why it stays);\n      the `connectors.lock` header naming `flux-connectors check` (`connector-spec/src/lock.rs:74-75`);\n      the canonical document's `$schema` URL, still\n      `https://github.com/codewandler/flux-connectors/catalog/connector-document.schema.json`\n      (`catalog-build/src/document.rs:67`);\n      `web/package.json`'s `name` and `description`;\n      and the site's `PATH_RESOLVER = 'flux-connectors:path-resolver'` injection key.\n      A rename that leaves half the strings behind has not retired anything.\n- [x] After the rewrite the tree is a fixed point: `catalog build` writes nothing on a second run,\n      `catalog check` ([S-003](../../../docs/stories/S-003-the-lockfile-gets-a-verifier.md)) is green on the new lock, and\n      the catalog invariants pass unchanged.\n- [x] `grep -ri flux-connectors` over `crates/`, `catalog/`, `web/` and the manifests returns only the\n      decided exceptions plus historical references in documentation (design series, stories,\n      predecessor citations) — history stays legible, artifacts do not.\n\n## Context\n\nMove this repository's artifacts onto their own identity — in **one** reviewed change whose diff is\nthe entire catalogue. The predecessor's name and version number are stamped into every canonical\ndocument, every lockfile row, the site's JSON and the wire's `User-Agent`; each is cheap alone and\neach rewrites all 55 documents and the pack digest, so doing them separately means paying that review\nfour times.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve, web]. **read**\n\nSource `note:` field, quoted: “M1 pinned the identity deliberately: the generator string `flux-connectors <version>` is an INPUT to the migration differential, so the workspace version is held at 0.26.0 and the name kept. The differential has passed; the pin's reason is spent. One story, one full-catalogue rewrite — generator + version + user agent together, not three”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-019-retire-the-flux-connectors-identity.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-019-retire-the-flux-connectors-identity.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 5 revision(s)\n- Legacy id `S-019`, recorded as the reference `legacy:S-019`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-019-retire-the-flux-connectors-identity.md:42`. **read**\n\n- [x] The generator identity names **this** repository, the workspace version is chosen deliberately\n      (a fresh line, or a continuation of 0.26.0 — either is defensible), and the `User-Agent` product\n      token matches both. All three move in **one commit** with **one** whole-catalogue regeneration:\n      55 documents, the pack digest, `connectors.lock`, `web/public/catalog.json`.\n- [x] The reasons are replaced, not deleted: `Cargo.toml`'s load-bearing comment and `seam.rs`'s\n      generator doc say what the version means **now** (and that the differential is retired), rather\n      than losing the paragraph that explained the old pin.\n- [x] `DEFAULT_USER_AGENT`'s three assertions in `request.rs` move with the value, so the constant\n      stays derived from the manifest (`CARGO_PKG_VERSION`, `CARGO_PKG_REPOSITORY`) and cannot go\n      stale at a release.\n- [x] The **whole identity inventory is enumerated and each item decided** — renamed now, or kept with\n      a written reason. Known members, measured post-M1:\n      the pack magic `flux-connectors-catalog-pack` (`crates/catalog-reader/src/lib.rs:70` — renaming\n      it is a pack-format break, so decide it here or state why it stays);\n      the `connectors.lock` header naming `flux-connectors check` (`connector-spec/src/lock.rs:74-75`);\n      the canonical document's `$schema` URL, still\n      `https://github.com/codewandler/flux-connectors/catalog/connector-document.schema.json`\n      (`catalog-build/src/document.rs:67`);\n      `web/package.json`'s `name` and `description`;\n      and the site's `PATH_RESOLVER = 'flux-connectors:path-resolver'` injection key.\n      A rename that leaves half the strings behind has not retired anything.\n- [x] After the rewrite the tree is a fixed point: `catalog build` writes nothing on a second run,\n      `catalog check` ([S-003](../../../docs/stories/S-003-the-lockfile-gets-a-verifier.md)) is green on the new lock, and\n      the catalog invariants pass unchanged.\n- [x] `grep -ri flux-connectors` over `crates/`, `catalog/`, `web/` and the manifests returns only the\n      decided exceptions plus historical references in documentation (design series, stories,\n      predecessor citations) — history stays legible, artifacts do not.\n\n## Context\n\nMove this repository's artifacts onto their own identity — in **one** reviewed change whose diff is\nthe entire catalogue. The predecessor's name and version number are stamped into every canonical\ndocument, every lockfile row, the site's JSON and the wire's `User-Agent`; each is cheap alone and\neach rewrites all 55 documents and the pack digest, so doing them separately means paying that review\nfour times.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-resolve, web]. **read**\n\nSource `note:` field, quoted: “M1 pinned the identity deliberately: the generator string `flux-connectors <version>` is an INPUT to the migration differential, so the workspace version is held at 0.26.0 and the name kept. The differential has passed; the pin's reason is spent. One story, one full-catalogue rewrite — generator + version + user agent together, not three”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-019-retire-the-flux-connectors-identity.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-019-retire-the-flux-connectors-identity.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-09-02 · 5 revision(s)\n- Legacy id `S-019`, recorded as the reference `legacy:S-019`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000241"},"payload":{"actor":"human:timo","at":1788482227515,"causation":"cmd-protocol-artifact-body-section-story-retire-the-flux-connectors-identity-1788482227515","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:retire-the-flux-connectors-identity@6#0~cb54892df14da0ad","executor":null,"recorded_at":"2026-09-04T00:37:07Z"}}
{"entity":"story","version":1,"id":"orphaned-inputs-are-removed-or-re-owned","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000220","to":"implemented"},"payload":{"actor":"human:timo","at":1788482227800,"causation":"cmd-move-story-orphaned-inputs-are-removed-or-re-owned-1788482227800","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:orphaned-inputs-are-removed-or-re-owned@5#0~9161a666e8a801c0","executor":null,"recorded_at":"2026-09-04T00:37:07Z"}}
{"entity":"story","version":1,"id":"orphaned-inputs-are-removed-or-re-owned","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:37`. **read**\n\n- [x] **anthropic** remains under `specs/` and covered by the OpenAPI 3.1/YAML ingest tests. Its\n      provenance says `origin = \"repository-authored\"`, pins its bytes, records the predecessor\n      authorship and exact official grounding references, and states `coverage =\n      \"partial-ingest-fixture\"`; it cannot be mistaken for vendor-published bytes.\n- [x] **anthropic's full connector** is reproducible from a sufficiently complete authored spec plus\n      reviewed overlays into the canonical connector document. Until then,\n      `providers/anthropic.toml` is named migration debt and the partial excerpt is not allowed to\n      claim provider-level coverage.\n- [x] **`specs/flux/core-v1.json`** is deleted together with every claim that something is emitted\n      from it (`web/README.md`'s `public/v1/` paragraph), or a named consumer is filed as its own\n      story before it stays. A vendored input with no consumer is a file that still validates and\n      describes nothing.\n- [x] **`migration/`** is decided against its live successor: the wave/fixture rule restated by\n      [S-010](../../../docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md) is what actually governs\n      plugin retirement now. Either the ratchet's records move under that story's evidence with an\n      owner and a checker, or the directory goes. It does not stay as an unchecked schema plus two\n      fixtures.\n- [x] `SOURCES.toml`'s \"known defects\" header shrinks by exactly the entries closed, and the\n      predecessor-import entry's `consumers` list stops naming paths that no longer exist. The index\n      never describes a tree that is not there.\n- [x] After the sweep, orphan checking passes **in both directions** by construction: every file under\n      `specs/` is covered by a `[[source]]` entry, and every entry's files exist. Landing this before\n      S-016 means that check's first run is a green one; landing it after means S-016 ships with three\n      declared exceptions, which is the state this story exists to prevent.\n- [x] Each disposition is its own reviewable commit with the reason in the body — a deleted 157 KB\n      vendored document, a re-owned authored spec and a deleted migration ratchet are decisions,\n      not cleanup noise.\n\n## Context\n\nClose the three orphans the M1 import left behind, so every file this repository derives from has a\nconsumer, a refresh path and a provenance record — and so\n[S-016](../../../docs/stories/S-016-sources-are-processed-by-code.md)'s `catalog sources check` can arrive **green** rather\nthan arriving with three permanent exceptions.\n\nSource frontmatter: pillar Catalog · areas [specs, migration, web]. **read**\n\nSource `note:` field, quoted: “three M1 classification defects: the Flux core input and migration ratchet had lost their consumers; the Anthropic YAML was a live repository-authored source spec whose origin and deliberately partial coverage were not recorded”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 7 revision(s)\n- Legacy id `S-022`, recorded as the reference `legacy:S-022`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:37`. **read**\n\n- [x] **anthropic** remains under `specs/` and covered by the OpenAPI 3.1/YAML ingest tests. Its\n      provenance says `origin = \"repository-authored\"`, pins its bytes, records the predecessor\n      authorship and exact official grounding references, and states `coverage =\n      \"partial-ingest-fixture\"`; it cannot be mistaken for vendor-published bytes.\n- [x] **anthropic's full connector** is reproducible from a sufficiently complete authored spec plus\n      reviewed overlays into the canonical connector document. Until then,\n      `providers/anthropic.toml` is named migration debt and the partial excerpt is not allowed to\n      claim provider-level coverage.\n- [x] **`specs/flux/core-v1.json`** is deleted together with every claim that something is emitted\n      from it (`web/README.md`'s `public/v1/` paragraph), or a named consumer is filed as its own\n      story before it stays. A vendored input with no consumer is a file that still validates and\n      describes nothing.\n- [x] **`migration/`** is decided against its live successor: the wave/fixture rule restated by\n      [S-010](../../../docs/stories/S-010-m5-flux-re-points-and-the-gitlab-plugin-retires.md) is what actually governs\n      plugin retirement now. Either the ratchet's records move under that story's evidence with an\n      owner and a checker, or the directory goes. It does not stay as an unchecked schema plus two\n      fixtures.\n- [x] `SOURCES.toml`'s \"known defects\" header shrinks by exactly the entries closed, and the\n      predecessor-import entry's `consumers` list stops naming paths that no longer exist. The index\n      never describes a tree that is not there.\n- [x] After the sweep, orphan checking passes **in both directions** by construction: every file under\n      `specs/` is covered by a `[[source]]` entry, and every entry's files exist. Landing this before\n      S-016 means that check's first run is a green one; landing it after means S-016 ships with three\n      declared exceptions, which is the state this story exists to prevent.\n- [x] Each disposition is its own reviewable commit with the reason in the body — a deleted 157 KB\n      vendored document, a re-owned authored spec and a deleted migration ratchet are decisions,\n      not cleanup noise.\n\n## Context\n\nClose the three orphans the M1 import left behind, so every file this repository derives from has a\nconsumer, a refresh path and a provenance record — and so\n[S-016](../../../docs/stories/S-016-sources-are-processed-by-code.md)'s `catalog sources check` can arrive **green** rather\nthan arriving with three permanent exceptions.\n\nSource frontmatter: pillar Catalog · areas [specs, migration, web]. **read**\n\nSource `note:` field, quoted: “three M1 classification defects: the Flux core input and migration ratchet had lost their consumers; the Anthropic YAML was a live repository-authored source spec whose origin and deliberately partial coverage were not recorded”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-022-orphaned-inputs-are-removed-or-re-owned.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 7 revision(s)\n- Legacy id `S-022`, recorded as the reference `legacy:S-022`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000220"},"payload":{"actor":"human:timo","at":1788482227992,"causation":"cmd-protocol-artifact-body-section-story-orphaned-inputs-are-removed-or-re-owned-1788482227992","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:orphaned-inputs-are-removed-or-re-owned@6#0~1e2558ba848189e2","executor":null,"recorded_at":"2026-09-04T00:37:07Z"}}
{"entity":"story","version":1,"id":"beyond-http-facts-enter-the-document","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000112","to":"implemented"},"payload":{"actor":"human:timo","at":1788482228183,"causation":"cmd-move-story-beyond-http-facts-enter-the-document-1788482228183","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:beyond-http-facts-enter-the-document@5#0~6a68013cae915b8c","executor":null,"recorded_at":"2026-09-04T00:37:08Z"}}
{"entity":"story","version":1,"id":"beyond-http-facts-enter-the-document","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:20`. **read**\n\n- [x] The schema and IR type each axis independently and reject unknown driver/capability values.\n- [x] Ordinary HTTP documents lower byte-identically unless a reviewed migration says otherwise.\n- [x] Exposure to models remains distinct from operation direction and direct-byte interaction.\n- [x] Catalog build, reader, resolver, lock, site projection, and fixtures change in the single\n      coordinated schema wave.\n- [x] The substrate first-party translation has an explicit mapping/overlay fixture; no field is\n      called mechanically identical when its vocabulary differs.\n\n## Context\n\nRepresent interaction shape, closed protocol driver, placement requirements, implementation form,\nand capability predicates as independent document facts without restoring a generic runtime enum.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec, connector-resolve] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0010 delivery item 1; this is schema-wave work and never runs in parallel with another document-schema author”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-023-beyond-http-facts-enter-the-document.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-023`, recorded as the reference `legacy:S-023`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:20`. **read**\n\n- [x] The schema and IR type each axis independently and reject unknown driver/capability values.\n- [x] Ordinary HTTP documents lower byte-identically unless a reviewed migration says otherwise.\n- [x] Exposure to models remains distinct from operation direction and direct-byte interaction.\n- [x] Catalog build, reader, resolver, lock, site projection, and fixtures change in the single\n      coordinated schema wave.\n- [x] The substrate first-party translation has an explicit mapping/overlay fixture; no field is\n      called mechanically identical when its vocabulary differs.\n\n## Context\n\nRepresent interaction shape, closed protocol driver, placement requirements, implementation form,\nand capability predicates as independent document facts without restoring a generic runtime enum.\n\nSource frontmatter: pillar Catalog · areas [catalog, catalog-build, connector-spec, connector-resolve] · design `docs/design/03-beyond-http.md`. **read**\n\nSource `note:` field, quoted: “ADR 0010 delivery item 1; this is schema-wave work and never runs in parallel with another document-schema author”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-023-beyond-http-facts-enter-the-document.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-023-beyond-http-facts-enter-the-document.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-13 · last touched 2026-08-13 · 2 revision(s)\n- Legacy id `S-023`, recorded as the reference `legacy:S-023`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788482228306,"causation":"cmd-protocol-artifact-body-section-story-beyond-http-facts-enter-the-document-1788482228306","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:beyond-http-facts-enter-the-document@6#0~55c1ebb249f02c8f","executor":null,"recorded_at":"2026-09-04T00:37:08Z"}}
{"entity":"story","version":1,"id":"admission-proofs-become-unforgeable","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000097","to":"implemented"},"payload":{"actor":"human:timo","at":1788482228499,"causation":"cmd-move-story-admission-proofs-become-unforgeable-1788482228499","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:admission-proofs-become-unforgeable@5#0~3f48d5a88ac59314","executor":null,"recorded_at":"2026-09-04T00:37:08Z"}}
{"entity":"story","version":1,"id":"admission-proofs-become-unforgeable","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-043-admission-proofs-become-unforgeable.md:23`. **read**\n\n- `from_grant_decision` no longer exists; grepping for it finds only history.\n- `AdmittedOperation` has no public constructor; construction sites outside `crates/domain` use\n  the named local-owner path, and its rustdoc states what it asserts and where it must not be used\n  (hosted request handling).\n- The workspace gate is green with no test weakened.\n\n## Context\n\nRemove `AdmittedOperation::from_grant_decision` — a public constructor over caller-supplied\nstrings — and make `AdmittedOperation` reachable only through two honestly named, sealed paths:\na `GrantDecision` proof (hosted; constructor lands with S-044, a module-private placeholder seals\nthe type now) and a local-owner admission for personal placements speaking over the owner's own\nsocket. Every current caller moves to the local-owner path explicitly; behavior does not change.\n\nSource frontmatter: pillar Platform · areas [domain, service, integrations] · priority 1 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantDecision sealed in domain::evaluator (S-044 owns the production constructor); all 22 call sites moved to for_local_owner; the hosted-registry backends that reach it on read-only invocations are flagged for S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-043-admission-proofs-become-unforgeable.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-043-admission-proofs-become-unforgeable.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-043`, recorded as the reference `legacy:S-043`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-043-admission-proofs-become-unforgeable.md:23`. **read**\n\n- `from_grant_decision` no longer exists; grepping for it finds only history.\n- `AdmittedOperation` has no public constructor; construction sites outside `crates/domain` use\n  the named local-owner path, and its rustdoc states what it asserts and where it must not be used\n  (hosted request handling).\n- The workspace gate is green with no test weakened.\n\n## Context\n\nRemove `AdmittedOperation::from_grant_decision` — a public constructor over caller-supplied\nstrings — and make `AdmittedOperation` reachable only through two honestly named, sealed paths:\na `GrantDecision` proof (hosted; constructor lands with S-044, a module-private placeholder seals\nthe type now) and a local-owner admission for personal placements speaking over the owner's own\nsocket. Every current caller moves to the local-owner path explicitly; behavior does not change.\n\nSource frontmatter: pillar Platform · areas [domain, service, integrations] · priority 1 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantDecision sealed in domain::evaluator (S-044 owns the production constructor); all 22 call sites moved to for_local_owner; the hosted-registry backends that reach it on read-only invocations are flagged for S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-043-admission-proofs-become-unforgeable.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-043-admission-proofs-become-unforgeable.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-043`, recorded as the reference `legacy:S-043`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788482228621,"causation":"cmd-protocol-artifact-body-section-story-admission-proofs-become-unforgeable-1788482228621","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:admission-proofs-become-unforgeable@6#0~b3fdfd479ddc9bca","executor":null,"recorded_at":"2026-09-04T00:37:08Z"}}
{"entity":"story","version":1,"id":"a-grant-decision-is-evaluated-not-asserted","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000079","to":"implemented"},"payload":{"actor":"human:timo","at":1788482228810,"causation":"cmd-move-story-a-grant-decision-is-evaluated-not-asserted-1788482228810","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:a-grant-decision-is-evaluated-not-asserted@5#0~b0a144a2e1e613d4","executor":null,"recorded_at":"2026-09-04T00:37:08Z"}}
{"entity":"story","version":1,"id":"a-grant-decision-is-evaluated-not-asserted","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:24`. **read**\n\n- No store bound → 503; empty store or nothing admitting → 403; no refusal names the axis.\n- Deny beats allow beats predicate, proven by tests including an allow+deny collision.\n- A `GrantDecision` cannot be constructed from any other crate (compile-fail test or doc-tested\n  seal), and an expired decision refuses at use.\n- SQLite and hosted-PostgreSQL store backends pass one shared conformance exercise.\n\n## Context\n\nImplement the `GrantEvaluator` over the S-041 state port: revisioned tenant Grants, selector\nsemantics (risk ceiling, effects subset, idempotency class), explicit exceptions with\ndeny > allow > predicate, inbound events as closed sets. Success is a `GrantDecision` proof —\nprivate fields, module-sealed, bound to issuer, tenant, sub, act, operation ref, Connection ref,\ncatalog generation, description lease, grant revision, canonical input digest, decision, expiry\nand a one-time id.\n\nSource frontmatter: pillar Platform · areas [domain, service, state] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantEvaluator + revisioned Grant records land in domain over the S-041 port; the cfg(test) decision builder is deleted, so the evaluator is the only constructor; grant_conformance runs on memory, SQLite (memory+file) and #[ignore]d live PostgreSQL; hosted wiring stays with S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-044`, recorded as the reference `legacy:S-044`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:24`. **read**\n\n- No store bound → 503; empty store or nothing admitting → 403; no refusal names the axis.\n- Deny beats allow beats predicate, proven by tests including an allow+deny collision.\n- A `GrantDecision` cannot be constructed from any other crate (compile-fail test or doc-tested\n  seal), and an expired decision refuses at use.\n- SQLite and hosted-PostgreSQL store backends pass one shared conformance exercise.\n\n## Context\n\nImplement the `GrantEvaluator` over the S-041 state port: revisioned tenant Grants, selector\nsemantics (risk ceiling, effects subset, idempotency class), explicit exceptions with\ndeny > allow > predicate, inbound events as closed sets. Success is a `GrantDecision` proof —\nprivate fields, module-sealed, bound to issuer, tenant, sub, act, operation ref, Connection ref,\ncatalog generation, description lease, grant revision, canonical input digest, decision, expiry\nand a one-time id.\n\nSource frontmatter: pillar Platform · areas [domain, service, state] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “GrantEvaluator + revisioned Grant records land in domain over the S-041 port; the cfg(test) decision builder is deleted, so the evaluator is the only constructor; grant_conformance runs on memory, SQLite (memory+file) and #[ignore]d live PostgreSQL; hosted wiring stays with S-046”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-044-a-grant-decision-is-evaluated-not-asserted.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-044`, recorded as the reference `legacy:S-044`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788482228935,"causation":"cmd-protocol-artifact-body-section-story-a-grant-decision-is-evaluated-not-asserted-1788482228935","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:a-grant-decision-is-evaluated-not-asserted@6#0~4489cd6fb7d592f6","executor":null,"recorded_at":"2026-09-04T00:37:08Z"}}
{"entity":"story","version":1,"id":"an-approval-is-redeemed-exactly-once","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000106","to":"implemented"},"payload":{"actor":"human:timo","at":1788482229124,"causation":"cmd-move-story-an-approval-is-redeemed-exactly-once-1788482229124","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:an-approval-is-redeemed-exactly-once@5#0~e0d4051b5c8ea6cb","executor":null,"recorded_at":"2026-09-04T00:37:09Z"}}
{"entity":"story","version":1,"id":"an-approval-is-redeemed-exactly-once","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:23`. **read**\n\n- Redemption is atomic under concurrent presentation: exactly one of N concurrent identical\n  requests proceeds (test with real concurrency, not sequential simulation).\n- A mismatched operation, Connection, digest or expired record refuses without naming the axis.\n- Attempted audit exists for every dispatch attempt, including refusals and crashes between\n  redemption and terminal write (proven by a kill-point test or documented crash-recovery scan).\n\n## Context\n\nImplement the approval verifier and redemption store: an externally issued approval record\n(issuer, subject, operation, Connection, canonical input digest, expiry) is verified against the\ninvocation and atomically redeemed — the attempted-audit row and the redemption land in one\ntransaction before dispatch; the terminal outcome row follows after. A second presentation of the\nsame reference refuses and audits as replay.\n\nSource frontmatter: pillar Platform · areas [domain, service, state, audit] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “ApprovalGate in domain::approval — axis-free verification; the one-time claim is a bounded append to approval.redemption.<sha256(reference)> whose payload IS the attempted-audit row; replay is its own journal kind; ApprovalGate::recover is the documented startup crash scan. SQLite concurrency/replay/crash evidence in state-sqlite/tests/approval_gate.rs; S-046 wires the hosted route”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-045`, recorded as the reference `legacy:S-045`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:23`. **read**\n\n- Redemption is atomic under concurrent presentation: exactly one of N concurrent identical\n  requests proceeds (test with real concurrency, not sequential simulation).\n- A mismatched operation, Connection, digest or expired record refuses without naming the axis.\n- Attempted audit exists for every dispatch attempt, including refusals and crashes between\n  redemption and terminal write (proven by a kill-point test or documented crash-recovery scan).\n\n## Context\n\nImplement the approval verifier and redemption store: an externally issued approval record\n(issuer, subject, operation, Connection, canonical input digest, expiry) is verified against the\ninvocation and atomically redeemed — the attempted-audit row and the redemption land in one\ntransaction before dispatch; the terminal outcome row follows after. A second presentation of the\nsame reference refuses and audits as replay.\n\nSource frontmatter: pillar Platform · areas [domain, service, state, audit] · priority 2 · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “ApprovalGate in domain::approval — axis-free verification; the one-time claim is a bounded append to approval.redemption.<sha256(reference)> whose payload IS the attempted-audit row; replay is its own journal kind; ApprovalGate::recover is the documented startup crash scan. SQLite concurrency/replay/crash evidence in state-sqlite/tests/approval_gate.rs; S-046 wires the hosted route”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-045-an-approval-is-redeemed-exactly-once.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-045`, recorded as the reference `legacy:S-045`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788482229249,"causation":"cmd-protocol-artifact-body-section-story-an-approval-is-redeemed-exactly-once-1788482229249","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:an-approval-is-redeemed-exactly-once@6#0~598b0b862f1fb566","executor":null,"recorded_at":"2026-09-04T00:37:09Z"}}
{"entity":"story","version":1,"id":"the-hosted-route-enforces-instead-of-refusing","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000295","to":"implemented"},"payload":{"actor":"human:timo","at":1788482229439,"causation":"cmd-move-story-the-hosted-route-enforces-instead-of-refusing-1788482229439","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-hosted-route-enforces-instead-of-refusing@5#0~b048ff40198c2530","executor":null,"recorded_at":"2026-09-04T00:37:09Z"}}
{"entity":"story","version":1,"id":"the-hosted-route-enforces-instead-of-refusing","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:21`. **read**\n\n- A caller with a valid route-family token but no admitting Grant is refused (403) on a mutation.\n- A granted mutation with a demanded approval refuses without one, dispatches with one, and\n  refuses the same approval's second use.\n- The pre-existing read-only path is unchanged for callers without Grants covering effects.\n- End-to-end test drives the full chain over the hosted HTTP surface.\n\n## Context\n\nReplace the two hosted fences (approval-reference refusal; ReadOnly+NotRequired re-describe gate)\nwith the real gates: every Invoke passes the GrantEvaluator, and descriptions demanding approval\nadditionally pass the approval gate. Effect-bearing hosted invocation becomes reachable — only\nthrough proofs.\n\nSource frontmatter: pillar Platform · areas [server, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “server::hosted::HostedAuthority (GrantEvaluator + ApprovalGate over the one bound S-041 store) replaces both fences; described ReadOnly+NotRequired keeps the pre-existing policy path when no Grant admits; every enforcement 403 is one byte-identical axis-free body, replay distinct only in the journal; issued approval records live at approval.issued.<sha256(reference)> via the issue_approval bootstrap; the description carries no declared risk/effects/idempotency yet, so evaluation claims worst-case facts and exact allow exceptions are the admission shape until S-047 threads the proofs and facts onward”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-046`, recorded as the reference `legacy:S-046`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:21`. **read**\n\n- A caller with a valid route-family token but no admitting Grant is refused (403) on a mutation.\n- A granted mutation with a demanded approval refuses without one, dispatches with one, and\n  refuses the same approval's second use.\n- The pre-existing read-only path is unchanged for callers without Grants covering effects.\n- End-to-end test drives the full chain over the hosted HTTP surface.\n\n## Context\n\nReplace the two hosted fences (approval-reference refusal; ReadOnly+NotRequired re-describe gate)\nwith the real gates: every Invoke passes the GrantEvaluator, and descriptions demanding approval\nadditionally pass the approval gate. Effect-bearing hosted invocation becomes reachable — only\nthrough proofs.\n\nSource frontmatter: pillar Platform · areas [server, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “server::hosted::HostedAuthority (GrantEvaluator + ApprovalGate over the one bound S-041 store) replaces both fences; described ReadOnly+NotRequired keeps the pre-existing policy path when no Grant admits; every enforcement 403 is one byte-identical axis-free body, replay distinct only in the journal; issued approval records live at approval.issued.<sha256(reference)> via the issue_approval bootstrap; the description carries no declared risk/effects/idempotency yet, so evaluation claims worst-case facts and exact allow exceptions are the admission shape until S-047 threads the proofs and facts onward”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-046-the-hosted-route-enforces-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-046`, recorded as the reference `legacy:S-046`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000295"},"payload":{"actor":"human:timo","at":1788482229562,"causation":"cmd-protocol-artifact-body-section-story-the-hosted-route-enforces-instead-of-refusing-1788482229562","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-hosted-route-enforces-instead-of-refusing@6#0~a36ecbb295a4b65e","executor":null,"recorded_at":"2026-09-04T00:37:09Z"}}
{"entity":"story","version":1,"id":"effect-backends-trust-only-proofs","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000148","to":"implemented"},"payload":{"actor":"human:timo","at":1788482229754,"causation":"cmd-move-story-effect-backends-trust-only-proofs-1788482229754","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:effect-backends-trust-only-proofs@4#0~66389cd4cdc4b83d","executor":null,"recorded_at":"2026-09-04T00:37:09Z"}}
{"entity":"story","version":1,"id":"effect-backends-trust-only-proofs","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-047-effect-backends-trust-only-proofs.md:21`. **read**\n\n- No integration reads `approval_evidence_ref` for admission; the field reaches backends only as\n  audit correlation.\n- One parameterised invariant in `catalog_invariants.rs` (house rule: one rule, all providers)\n  fails if a future effect operation bypasses the proof chain.\n- The workspace gate is green.\n\n## Context\n\nDelete the per-integration approval-presence checks (gitlab, kubernetes, slack, sip, jira,\nmonitoring) — the proof chain upstream is the authority — and add the catalogue-wide invariant\ntest that no effect-bearing backend code path is reachable without an `AdmittedOperation` built\nfrom proofs.\n\nSource frontmatter: pillar Platform · areas [integrations, testing] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “every Integration's local approval-presence check is deleted (kubernetes hosted+local, monitoring, jira, gitlab, sip, slack, b10x) — the S-043..S-046 proof chain is the only admission authority; InvokeAdmission::Proven now carries the AdmittedOperation and hosted dispatch consumes it by value; catalog_invariants rule 15 scans every integration crate for approval_evidence_ref reads with an empty named audit-correlation allowlist; Slack's event-authorized companion reply (ReplyClaimStore) went with its check — an event that should authorize a reply must be issued as an approval record; ApprovalGate::recover is wired into HostedRuntime startup; GrantEffect::ALL makes the worst-case facts exhaustive by construction”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-047-effect-backends-trust-only-proofs.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-047-effect-backends-trust-only-proofs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-047`, recorded as the reference `legacy:S-047`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-047-effect-backends-trust-only-proofs.md:21`. **read**\n\n- No integration reads `approval_evidence_ref` for admission; the field reaches backends only as\n  audit correlation.\n- One parameterised invariant in `catalog_invariants.rs` (house rule: one rule, all providers)\n  fails if a future effect operation bypasses the proof chain.\n- The workspace gate is green.\n\n## Context\n\nDelete the per-integration approval-presence checks (gitlab, kubernetes, slack, sip, jira,\nmonitoring) — the proof chain upstream is the authority — and add the catalogue-wide invariant\ntest that no effect-bearing backend code path is reachable without an `AdmittedOperation` built\nfrom proofs.\n\nSource frontmatter: pillar Platform · areas [integrations, testing] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\nSource `note:` field, quoted: “every Integration's local approval-presence check is deleted (kubernetes hosted+local, monitoring, jira, gitlab, sip, slack, b10x) — the S-043..S-046 proof chain is the only admission authority; InvokeAdmission::Proven now carries the AdmittedOperation and hosted dispatch consumes it by value; catalog_invariants rule 15 scans every integration crate for approval_evidence_ref reads with an empty named audit-correlation allowlist; Slack's event-authorized companion reply (ReplyClaimStore) went with its check — an event that should authorize a reply must be issued as an approval record; ApprovalGate::recover is wired into HostedRuntime startup; GrantEffect::ALL makes the worst-case facts exhaustive by construction”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-047-effect-backends-trust-only-proofs.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-047-effect-backends-trust-only-proofs.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 2 revision(s)\n- Legacy id `S-047`, recorded as the reference `legacy:S-047`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788482229881,"causation":"cmd-protocol-artifact-body-section-story-effect-backends-trust-only-proofs-1788482229881","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:effect-backends-trust-only-proofs@5#0~c163b40034eefb3f","executor":null,"recorded_at":"2026-09-04T00:37:09Z"}}
{"entity":"story","version":1,"id":"a-companion-reply-is-claimed-exactly-once-locally","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000076","to":"implemented"},"payload":{"actor":"human:timo","at":1788482230074,"causation":"cmd-move-story-a-companion-reply-is-claimed-exactly-once-locally-1788482230074","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:a-companion-reply-is-claimed-exactly-once-locally@4#0~a5fc84a03df7a8db","executor":null,"recorded_at":"2026-09-04T00:37:10Z"}}
{"entity":"story","version":1,"id":"a-companion-reply-is-claimed-exactly-once-locally","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:24`. **read**\n\n- On the local placement, two presentations of the same companion event produce exactly one\n  outward Slack reply; the second refuses and is journaled, proven by a real-concurrency test.\n- Catalog invariant rule 15 stays green without weakening its empty-by-default posture.\n- The SIP `approval_evidence_ref` config field, which no code consumes since S-047, is retired\n  from `connectors-config` (`personal.rs`), the examples, and the connectors-cli README — or its\n  retention is justified in the config docs.\n\n## Context\n\nS-047 deleted the Slack backend's `ReplyClaimStore` with the approval-presence check it rode on,\nso on the local placement nothing enforces one reply per companion mention any more: a retried\ninvoke can post the same Slack reply twice (S-047 review, 2026-08-23). Restore the exactly-once\nclaim as an honest local mechanism — a durable one-time claim keyed on the triggering event,\nmade at the dispatch seam rather than by the Integration re-reading `approval_evidence_ref`\n(which catalog invariant rule 15 now forbids; its named allowlist\n`APPROVAL_EVIDENCE_AUDIT_CORRELATION_USES` is the sanctioned escape hatch if correlation by that\nfield turns out to be the right shape).\n\nSource frontmatter: pillar Platform · areas [integrations, state, config] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 3 revision(s)\n- Legacy id `S-048`, recorded as the reference `legacy:S-048`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:24`. **read**\n\n- On the local placement, two presentations of the same companion event produce exactly one\n  outward Slack reply; the second refuses and is journaled, proven by a real-concurrency test.\n- Catalog invariant rule 15 stays green without weakening its empty-by-default posture.\n- The SIP `approval_evidence_ref` config field, which no code consumes since S-047, is retired\n  from `connectors-config` (`personal.rs`), the examples, and the connectors-cli README — or its\n  retention is justified in the config docs.\n\n## Context\n\nS-047 deleted the Slack backend's `ReplyClaimStore` with the approval-presence check it rode on,\nso on the local placement nothing enforces one reply per companion mention any more: a retried\ninvoke can post the same Slack reply twice (S-047 review, 2026-08-23). Restore the exactly-once\nclaim as an honest local mechanism — a durable one-time claim keyed on the triggering event,\nmade at the dispatch seam rather than by the Integration re-reading `approval_evidence_ref`\n(which catalog invariant rule 15 now forbids; its named allowlist\n`APPROVAL_EVIDENCE_AUDIT_CORRELATION_USES` is the sanctioned escape hatch if correlation by that\nfield turns out to be the right shape).\n\nSource frontmatter: pillar Platform · areas [integrations, state, config] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-048-a-companion-reply-is-claimed-exactly-once-locally.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 3 revision(s)\n- Legacy id `S-048`, recorded as the reference `legacy:S-048`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000076"},"payload":{"actor":"human:timo","at":1788482230247,"causation":"cmd-protocol-artifact-body-section-story-a-companion-reply-is-claimed-exactly-once-locally-1788482230247","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:a-companion-reply-is-claimed-exactly-once-locally@5#0~c8eb2075bb30b9c3","executor":null,"recorded_at":"2026-09-04T00:37:10Z"}}
{"entity":"story","version":1,"id":"a-session-signal-carries-authority","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000094","to":"implemented"},"payload":{"actor":"human:timo","at":1788482230436,"causation":"cmd-move-story-a-session-signal-carries-authority-1788482230436","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:a-session-signal-carries-authority@5#0~ee1589e4e18d04bf","executor":null,"recorded_at":"2026-09-04T00:37:10Z"}}
{"entity":"story","version":1,"id":"a-session-signal-carries-authority","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-049-a-session-signal-carries-authority.md:22`. **read**\n\n- An effect-bearing session signal is refused unless a Grant admits it; refusals are axis-free\n  and journaled, matching the invoke path's semantics.\n- The catalogue invariant family covers the session-signal seam so a future signal route cannot\n  bypass admission silently.\n- Hosted single-replica assumption for `ApprovalGate::recover` is either stated in design 13 or\n  replaced with a fencing mechanism (S-047 review open question rides along here with the\n  session-lifecycle work).\n\n## Context\n\n`SessionSignal` dispatches effect-bearing input (a keypress cannot be undone) behind scope and\noperator-group policy only — no `GrantEvaluator`, no proof (S-046 and S-047 reviews, 2026-08-23).\nIt escaped the enforced-authority epic because it carries no operation ref, description, or\nConnection: nothing a `GrantRequest` can name. Give session signals a describable authority —\neither a described session-signal operation per provider or a session-scoped admission derived\nfrom the Grant that admitted the session's creation — and bring the dispatch behind it.\n\nSource frontmatter: pillar Platform · areas [server, domain, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-049-a-session-signal-carries-authority.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-049-a-session-signal-carries-authority.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 4 revision(s)\n- Legacy id `S-049`, recorded as the reference `legacy:S-049`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-049-a-session-signal-carries-authority.md:22`. **read**\n\n- An effect-bearing session signal is refused unless a Grant admits it; refusals are axis-free\n  and journaled, matching the invoke path's semantics.\n- The catalogue invariant family covers the session-signal seam so a future signal route cannot\n  bypass admission silently.\n- Hosted single-replica assumption for `ApprovalGate::recover` is either stated in design 13 or\n  replaced with a fencing mechanism (S-047 review open question rides along here with the\n  session-lifecycle work).\n\n## Context\n\n`SessionSignal` dispatches effect-bearing input (a keypress cannot be undone) behind scope and\noperator-group policy only — no `GrantEvaluator`, no proof (S-046 and S-047 reviews, 2026-08-23).\nIt escaped the enforced-authority epic because it carries no operation ref, description, or\nConnection: nothing a `GrantRequest` can name. Give session signals a describable authority —\neither a described session-signal operation per provider or a session-scoped admission derived\nfrom the Grant that admitted the session's creation — and bring the dispatch behind it.\n\nSource frontmatter: pillar Platform · areas [server, domain, service] · design `../design/13-grant-evaluation-and-approval-redemption.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-049-a-session-signal-carries-authority.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-049-a-session-signal-carries-authority.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-23 · 4 revision(s)\n- Legacy id `S-049`, recorded as the reference `legacy:S-049`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788482230667,"causation":"cmd-protocol-artifact-body-section-story-a-session-signal-carries-authority-1788482230667","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:a-session-signal-carries-authority@6#0~7ada1417cbaa35ae","executor":null,"recorded_at":"2026-09-04T00:37:10Z"}}
{"entity":"story","version":1,"id":"the-platform-connector-is-named-platform","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000307","to":"implemented"},"payload":{"actor":"human:timo","at":1788482230964,"causation":"cmd-move-story-the-platform-connector-is-named-platform-1788482230964","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-platform-connector-is-named-platform@6#0~3e99df97ee8414e0","executor":null,"recorded_at":"2026-09-04T00:37:10Z"}}
{"entity":"story","version":1,"id":"the-platform-connector-is-named-platform","revision":7,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-052-the-platform-connector-is-named-platform.md:25`. **read**\n\n- No non-allowlisted `b10x` remains in crate names, type/function/variant names, or prose;\n  `scripts/check-brand.sh`'s allowlist shrinks accordingly and stays negative-tested.\n- An existing personal config carrying the old section name parses unchanged, proven by a test.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nD5 is resolved: the platform's name is \"platform\" (extraction plan resolution note, 2026-08-23).\nThe brand sweep left ~154 code-level occurrences blocked on that decision. Rename the code-level\nfamily: crate `integration-b10x` becomes `integration-platform`, and\n`B10xConnectionConfig`, `B10xIntegrationConfig`, `B10xBackend`,\n`ConnectionInitiator::B10x`, `b10x_only()` and companions follow, with prose\n(\"B10x Identity/Integration/Provider/Session Authority\", \"Authorize B10x\" consent\npages) moving to platform naming. Explicitly kept, each on the brand-fence allowlist: provider id\n`b10x` and authority `io.b10x` (public identity, irreversible), `urn:b10x:*`,\n`x-b10x-*` headers, `b10x.*.vN` contract ids, hash domains, and serialized state\n(keyring service attribute). The serde-visible `[b10x]` config section moves only behind a\nserde alias or a documented migration — an existing personal config must keep parsing.\n\nSource frontmatter: pillar Platform · areas [integrations, config, server]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-052-the-platform-connector-is-named-platform.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-052-the-platform-connector-is-named-platform.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-052`, recorded as the reference `legacy:S-052`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-052-the-platform-connector-is-named-platform.md:25`. **read**\n\n- No non-allowlisted `b10x` remains in crate names, type/function/variant names, or prose;\n  `scripts/check-brand.sh`'s allowlist shrinks accordingly and stays negative-tested.\n- An existing personal config carrying the old section name parses unchanged, proven by a test.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nD5 is resolved: the platform's name is \"platform\" (extraction plan resolution note, 2026-08-23).\nThe brand sweep left ~154 code-level occurrences blocked on that decision. Rename the code-level\nfamily: crate `integration-b10x` becomes `integration-platform`, and\n`B10xConnectionConfig`, `B10xIntegrationConfig`, `B10xBackend`,\n`ConnectionInitiator::B10x`, `b10x_only()` and companions follow, with prose\n(\"B10x Identity/Integration/Provider/Session Authority\", \"Authorize B10x\" consent\npages) moving to platform naming. Explicitly kept, each on the brand-fence allowlist: provider id\n`b10x` and authority `io.b10x` (public identity, irreversible), `urn:b10x:*`,\n`x-b10x-*` headers, `b10x.*.vN` contract ids, hash domains, and serialized state\n(keyring service attribute). The serde-visible `[b10x]` config section moves only behind a\nserde alias or a documented migration — an existing personal config must keep parsing.\n\nSource frontmatter: pillar Platform · areas [integrations, config, server]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-052-the-platform-connector-is-named-platform.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-052-the-platform-connector-is-named-platform.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-23 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-052`, recorded as the reference `legacy:S-052`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000307"},"payload":{"actor":"human:timo","at":1788482231164,"causation":"cmd-protocol-artifact-body-section-story-the-platform-connector-is-named-platform-1788482231164","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-platform-connector-is-named-platform@7#0~6ccf128b84f64fae","executor":null,"recorded_at":"2026-09-04T00:37:11Z"}}
{"entity":"story","version":1,"id":"the-hosted-server-speaks-mcp-through-the-admission-seam","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000298","to":"implemented"},"payload":{"actor":"human:timo","at":1788482231406,"causation":"cmd-move-story-the-hosted-server-speaks-mcp-through-the-admission-seam-1788482231406","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-hosted-server-speaks-mcp-through-the-admission-seam@5#0~9eb0b439457df8c1","executor":null,"recorded_at":"2026-09-04T00:37:11Z"}}
{"entity":"story","version":1,"id":"the-hosted-server-speaks-mcp-through-the-admission-seam","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:28`. **read**\n\n- `tools/list` returns exactly the three meta-tools; a read-group principal's `tool_search`\n  lists the k8s entries its seam results support, a group-less principal's lists none.\n- An op-backed `tool_invoke` provably performs Describe then Invoke through the decided seam\n  with a matching lease; datasource-backed tools route through the datasource seam; refusal\n  paths surface as `isError` + `structuredContent.code` (`not_granted` without\n  `connectors.invoke`, `approval_required` with evidence passthrough proven, stale-authority\n  retried once); 401/-32700/-32602/413 behave per design 14.\n- A catalog-invariant sibling of rule 16 forbids `.backend`/`ConnectorBackend`/\n  `dispatch_admitted`/`admit_` tokens in the MCP modules' production lines, and rule 16's doc\n  comment names `/mcp` as a funneled entry.\n- `bash scripts/gate.sh` exits 0 (module fence: `hosted.rs` stays under 1500 — the\n  connect/oauth handler move to `hosted/connect.rs` is the sanctioned valve if needed).\n\n## Context\n\nAdd `POST {base_path}/mcp` to the hosted router: stateless JSON-RPC 2.0 MCP (revisions\n2025-03-26/2025-06-18; no SSE, no session id, no new dependency) with methods `initialize`,\n`notifications/*` (202), `ping`, `tools/list` (exactly `tool_search`, `tool_describe`,\n`tool_invoke`), `tools/call`. Split the private `operation()`/`datasource()` handlers into\nverify + `*_decided` halves inside `hosted.rs`; the MCP modules (`hosted/mcp.rs`,\n`hosted/mcp/toolset.rs`) verify the bearer once and reach backends only through the decided\nseams. Toolset v1 per design 14 (`k8s_namespace_list`, `k8s_deployment_list`,\n`k8s_deployment_status`, `k8s_pod_status`, `k8s_pod_logs`), role-filtered by the caller's own\nseam results; `description_ref` hidden with server-side re-describe and one stale-authority\nretry. Include `k8s_pod_logs` in the table even though S-054 lands the operation — the\nrequirement mechanism hides it until the op exists, which also makes the two stories\norder-independent.\n\nSource frontmatter: pillar Platform · areas [server, testing] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-053`, recorded as the reference `legacy:S-053`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:28`. **read**\n\n- `tools/list` returns exactly the three meta-tools; a read-group principal's `tool_search`\n  lists the k8s entries its seam results support, a group-less principal's lists none.\n- An op-backed `tool_invoke` provably performs Describe then Invoke through the decided seam\n  with a matching lease; datasource-backed tools route through the datasource seam; refusal\n  paths surface as `isError` + `structuredContent.code` (`not_granted` without\n  `connectors.invoke`, `approval_required` with evidence passthrough proven, stale-authority\n  retried once); 401/-32700/-32602/413 behave per design 14.\n- A catalog-invariant sibling of rule 16 forbids `.backend`/`ConnectorBackend`/\n  `dispatch_admitted`/`admit_` tokens in the MCP modules' production lines, and rule 16's doc\n  comment names `/mcp` as a funneled entry.\n- `bash scripts/gate.sh` exits 0 (module fence: `hosted.rs` stays under 1500 — the\n  connect/oauth handler move to `hosted/connect.rs` is the sanctioned valve if needed).\n\n## Context\n\nAdd `POST {base_path}/mcp` to the hosted router: stateless JSON-RPC 2.0 MCP (revisions\n2025-03-26/2025-06-18; no SSE, no session id, no new dependency) with methods `initialize`,\n`notifications/*` (202), `ping`, `tools/list` (exactly `tool_search`, `tool_describe`,\n`tool_invoke`), `tools/call`. Split the private `operation()`/`datasource()` handlers into\nverify + `*_decided` halves inside `hosted.rs`; the MCP modules (`hosted/mcp.rs`,\n`hosted/mcp/toolset.rs`) verify the bearer once and reach backends only through the decided\nseams. Toolset v1 per design 14 (`k8s_namespace_list`, `k8s_deployment_list`,\n`k8s_deployment_status`, `k8s_pod_status`, `k8s_pod_logs`), role-filtered by the caller's own\nseam results; `description_ref` hidden with server-side re-describe and one stale-authority\nretry. Include `k8s_pod_logs` in the table even though S-054 lands the operation — the\nrequirement mechanism hides it until the op exists, which also makes the two stories\norder-independent.\n\nSource frontmatter: pillar Platform · areas [server, testing] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-053-the-hosted-server-speaks-mcp-through-the-admission-seam.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-053`, recorded as the reference `legacy:S-053`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000298"},"payload":{"actor":"human:timo","at":1788482231568,"causation":"cmd-protocol-artifact-body-section-story-the-hosted-server-speaks-mcp-through-the-admission-seam-1788482231568","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-hosted-server-speaks-mcp-through-the-admission-seam@6#0~7784a893a2a59af0","executor":null,"recorded_at":"2026-09-04T00:37:11Z"}}
{"entity":"story","version":1,"id":"pod-logs-are-a-hosted-read-only-kubernetes-operation","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000229","to":"implemented"},"payload":{"actor":"human:timo","at":1788482231803,"causation":"cmd-move-story-pod-logs-are-a-hosted-read-only-kubernetes-operation-1788482231803","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:pod-logs-are-a-hosted-read-only-kubernetes-operation@5#0~ceda94068f24bb15","executor":null,"recorded_at":"2026-09-04T00:37:11Z"}}
{"entity":"story","version":1,"id":"pod-logs-are-a-hosted-read-only-kubernetes-operation","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:25`. **read**\n\n- Search lists `kubernetes.pod.logs` for read-group principals and hides it otherwise;\n  describe returns input/output schemas and a lease; invoke passes container/tail_lines/\n  since_seconds through to the reader, proven against a fake.\n- A non-admitted namespace refuses `not_granted`; a stale lease refuses `stale_authority`;\n  an oversized log body yields front-trimmed output with `truncated: true` inside a\n  validating envelope; upstream 403 maps to `not_granted`; input caps are enforced.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nAdd `kubernetes.pod.logs` (effect read_only, approval not_required) to the hosted Kubernetes\nintegration per design 14: input `{namespace, pod, container?, tail_lines 1..=1000 default\n200, since_seconds 1..=86400?}`, backed by the pod log subresource with `limitBytes` 128 KiB,\noutput `{namespace, pod, container?, tail_lines, text, truncated}` with serialize-then-trim-\noldest-lines so the envelope never trips `result_too_large`. A default `DeploymentReader`\ntrait method refuses `unavailable` so the local placement and test fakes compile unchanged.\nGate admission on the same `namespace_access.read_groups` as deployment status via one arm in\n`admits_kubernetes_operation`; search advertises the operation only under read access, with\n`log`/`logs`/`pod` terms.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-054`, recorded as the reference `legacy:S-054`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:25`. **read**\n\n- Search lists `kubernetes.pod.logs` for read-group principals and hides it otherwise;\n  describe returns input/output schemas and a lease; invoke passes container/tail_lines/\n  since_seconds through to the reader, proven against a fake.\n- A non-admitted namespace refuses `not_granted`; a stale lease refuses `stale_authority`;\n  an oversized log body yields front-trimmed output with `truncated: true` inside a\n  validating envelope; upstream 403 maps to `not_granted`; input caps are enforced.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nAdd `kubernetes.pod.logs` (effect read_only, approval not_required) to the hosted Kubernetes\nintegration per design 14: input `{namespace, pod, container?, tail_lines 1..=1000 default\n200, since_seconds 1..=86400?}`, backed by the pod log subresource with `limitBytes` 128 KiB,\noutput `{namespace, pod, container?, tail_lines, text, truncated}` with serialize-then-trim-\noldest-lines so the envelope never trips `result_too_large`. A default `DeploymentReader`\ntrait method refuses `unavailable` so the local placement and test fakes compile unchanged.\nGate admission on the same `namespace_access.read_groups` as deployment status via one arm in\n`admits_kubernetes_operation`; search advertises the operation only under read access, with\n`log`/`logs`/`pod` terms.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-054-pod-logs-are-a-hosted-read-only-kubernetes-operation.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-054`, recorded as the reference `legacy:S-054`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000229"},"payload":{"actor":"human:timo","at":1788482231966,"causation":"cmd-protocol-artifact-body-section-story-pod-logs-are-a-hosted-read-only-kubernetes-operation-1788482231966","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:pod-logs-are-a-hosted-read-only-kubernetes-operation@6#0~3e1825d07f1f7fb8","executor":null,"recorded_at":"2026-09-04T00:37:11Z"}}
{"entity":"story","version":1,"id":"a-self-service-scope-subset-needs-no-operator","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000091","to":"implemented"},"payload":{"actor":"human:timo","at":1788482232200,"causation":"cmd-move-story-a-self-service-scope-subset-needs-no-operator-1788482232200","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:a-self-service-scope-subset-needs-no-operator@4#0~570f3222b08427b6","executor":null,"recorded_at":"2026-09-04T00:37:12Z"}}
{"entity":"story","version":1,"id":"a-self-service-scope-subset-needs-no-operator","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:25`. **read**\n\n- In beyond10x/identity: a member principal mints `connectors.catalog.read connectors.invoke`\n  (and the full self-service four); `connectors.catalog.read connectors.events.read` still\n  demands `operator`; operator behaviour is unchanged — all proven in the vocabulary test.\n- Identity's `bash scripts/gate.sh` exits 0; the identity commit is referenced here on close.\n\n## Context\n\nThis story's change lands in the separate beyond10x/identity repository (from `a664b99b`); it\nis tracked here so the board shows the end-to-end dependency. Identity's\n`admitted_connector_scope` compares the whole canonical scope string against four single-scope\nliterals, so any multi-scope request demands the `operator` group — an `sre`/`dev` principal\ncannot hold `connectors.catalog.read connectors.invoke` in one token, making the MCP endpoint\noperator-only in practice. Admit any subset of the four self-service scopes\n(`connectors.catalog.read`, `connectors.connections.self`, `connectors.events.self`,\n`connectors.invoke`) for any authenticated principal; any other member in the set still\ndemands `operator`.\n\nSource frontmatter: pillar Platform · areas [identity] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-055`, recorded as the reference `legacy:S-055`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:25`. **read**\n\n- In beyond10x/identity: a member principal mints `connectors.catalog.read connectors.invoke`\n  (and the full self-service four); `connectors.catalog.read connectors.events.read` still\n  demands `operator`; operator behaviour is unchanged — all proven in the vocabulary test.\n- Identity's `bash scripts/gate.sh` exits 0; the identity commit is referenced here on close.\n\n## Context\n\nThis story's change lands in the separate beyond10x/identity repository (from `a664b99b`); it\nis tracked here so the board shows the end-to-end dependency. Identity's\n`admitted_connector_scope` compares the whole canonical scope string against four single-scope\nliterals, so any multi-scope request demands the `operator` group — an `sre`/`dev` principal\ncannot hold `connectors.catalog.read connectors.invoke` in one token, making the MCP endpoint\noperator-only in practice. Admit any subset of the four self-service scopes\n(`connectors.catalog.read`, `connectors.connections.self`, `connectors.events.self`,\n`connectors.invoke`) for any authenticated principal; any other member in the set still\ndemands `operator`.\n\nSource frontmatter: pillar Platform · areas [identity] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-055-a-self-service-scope-subset-needs-no-operator.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-055`, recorded as the reference `legacy:S-055`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788482232367,"causation":"cmd-protocol-artifact-body-section-story-a-self-service-scope-subset-needs-no-operator-1788482232367","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:a-self-service-scope-subset-needs-no-operator@5#0~4baf743f205bfc5e","executor":null,"recorded_at":"2026-09-04T00:37:12Z"}}
{"entity":"story","version":1,"id":"the-cli-bridges-mcp-over-stdio","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000274","to":"implemented"},"payload":{"actor":"human:timo","at":1788482232600,"causation":"cmd-move-story-the-cli-bridges-mcp-over-stdio-1788482232600","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-cli-bridges-mcp-over-stdio@4#0~f6058bb21a6a994f","executor":null,"recorded_at":"2026-09-04T00:37:12Z"}}
{"entity":"story","version":1,"id":"the-cli-bridges-mcp-over-stdio","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:22`. **read**\n\n- A stdio MCP client completes initialize/tools-list/tools-call against a hosted deployment\n  through the bridge, across a token expiry, without manual re-auth.\n- The thin-CLI architecture fence stays green with a dated, bounded cap raise covering parser and\n  dispatch declarations only.\n\n## Context\n\nInteractive MCP clients hold a static bearer, but identity access tokens live 300 seconds, so\nthe hosted `/mcp` endpoint alone serves scripted callers well and interactive ones poorly.\nAdd a `connectors mcp` subcommand: a stdio MCP server that proxies to the hosted `/mcp`\nendpoint with automatic per-scope token refresh from the login session established by\n`connectors login`. The behaviour belongs in a `connectors-client` module; the subcommand itself\nstays a thin frontend.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-056`, recorded as the reference `legacy:S-056`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:22`. **read**\n\n- A stdio MCP client completes initialize/tools-list/tools-call against a hosted deployment\n  through the bridge, across a token expiry, without manual re-auth.\n- The thin-CLI architecture fence stays green with a dated, bounded cap raise covering parser and\n  dispatch declarations only.\n\n## Context\n\nInteractive MCP clients hold a static bearer, but identity access tokens live 300 seconds, so\nthe hosted `/mcp` endpoint alone serves scripted callers well and interactive ones poorly.\nAdd a `connectors mcp` subcommand: a stdio MCP server that proxies to the hosted `/mcp`\nendpoint with automatic per-scope token refresh from the login session established by\n`connectors login`. The behaviour belongs in a `connectors-client` module; the subcommand itself\nstays a thin frontend.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-056-the-cli-bridges-mcp-over-stdio.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-056`, recorded as the reference `legacy:S-056`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000274"},"payload":{"actor":"human:timo","at":1788482232801,"causation":"cmd-protocol-artifact-body-section-story-the-cli-bridges-mcp-over-stdio-1788482232801","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-cli-bridges-mcp-over-stdio@5#0~b27698502bb7934c","executor":null,"recorded_at":"2026-09-04T00:37:12Z"}}
{"entity":"story","version":1,"id":"mysql-and-postgresql-become-connectors","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000199","to":"implemented"},"payload":{"actor":"human:timo","at":1788482233022,"causation":"cmd-move-story-mysql-and-postgresql-become-connectors-1788482233022","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:mysql-and-postgresql-become-connectors@5#0~86bcb3a51a6f65f7","executor":null,"recorded_at":"2026-09-04T00:37:13Z"}}
{"entity":"story","version":1,"id":"mysql-and-postgresql-become-connectors","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:24`. **read**\n\n- Providers `mysql` and `postgresql` exist with permanent ids/authorities per the\n  adding-a-connector discipline; their read-only operations carry honest risk/effects/\n  idempotency and JSON Schemas, and `catalog build && diff && check` stays clean.\n- A bounded query against a real database (containerized in tests) returns rows inside the\n  result cap with honest truncation, and a write statement (INSERT/UPDATE/DDL) is refused\n  before reaching the database, proven by tests.\n- No credential value appears in config, catalog, logs, or errors — the custody path is a\n  reference; the brand and secret fences stay green.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe platform governs HTTP vendors and Kubernetes but not the databases the product actually\nruns on. Give MySQL and PostgreSQL permanent provider identities and a read-only operation\nsurface first — bounded query (row and byte capped), list-schemas/tables, describe-table —\nexecuted by a SQL protocol driver in the mold of the existing non-HTTP drivers, behind the\nsame admission seam as every operation. Credentials arrive through platform custody\n(connection secret references, Vault), never from the caller; connection configs name a\nreference, not a value. Effect posture: everything in this story is `read_only` and rides the\nread path; mutations are a later grant-gated story.\n\nSource frontmatter: pillar Platform · areas [catalog, integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-058`, recorded as the reference `legacy:S-058`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:24`. **read**\n\n- Providers `mysql` and `postgresql` exist with permanent ids/authorities per the\n  adding-a-connector discipline; their read-only operations carry honest risk/effects/\n  idempotency and JSON Schemas, and `catalog build && diff && check` stays clean.\n- A bounded query against a real database (containerized in tests) returns rows inside the\n  result cap with honest truncation, and a write statement (INSERT/UPDATE/DDL) is refused\n  before reaching the database, proven by tests.\n- No credential value appears in config, catalog, logs, or errors — the custody path is a\n  reference; the brand and secret fences stay green.\n- `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe platform governs HTTP vendors and Kubernetes but not the databases the product actually\nruns on. Give MySQL and PostgreSQL permanent provider identities and a read-only operation\nsurface first — bounded query (row and byte capped), list-schemas/tables, describe-table —\nexecuted by a SQL protocol driver in the mold of the existing non-HTTP drivers, behind the\nsame admission seam as every operation. Credentials arrive through platform custody\n(connection secret references, Vault), never from the caller; connection configs name a\nreference, not a value. Effect posture: everything in this story is `read_only` and rides the\nread path; mutations are a later grant-gated story.\n\nSource frontmatter: pillar Platform · areas [catalog, integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-058-mysql-and-postgresql-become-connectors.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-058`, recorded as the reference `legacy:S-058`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000199"},"payload":{"actor":"human:timo","at":1788482233148,"causation":"cmd-protocol-artifact-body-section-story-mysql-and-postgresql-become-connectors-1788482233148","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:mysql-and-postgresql-become-connectors@6#0~804353a593d00ef7","executor":null,"recorded_at":"2026-09-04T00:37:13Z"}}
{"entity":"story","version":1,"id":"kubernetes-discovers-database-endpoints","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000169","to":"implemented"},"payload":{"actor":"human:timo","at":1788482233337,"causation":"cmd-move-story-kubernetes-discovers-database-endpoints-1788482233337","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:kubernetes-discovers-database-endpoints@5#0~fc723f579530a846","executor":null,"recorded_at":"2026-09-04T00:37:13Z"}}
{"entity":"story","version":1,"id":"kubernetes-discovers-database-endpoints","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:23`. **read**\n\n- A datasource (bindings per admitted namespace; read verbs list/get) returns the derived\n  endpoint descriptors for both engines, proven against fakes shaped like the real\n  `database.{mysql,postgresql}.sql.crossplane.io` resources; secret references are named,\n  secret values never read.\n- The RBAC the deployment needs (get/list on those CRDs) is stated in the story's close note\n  for the chart, and admission stays behind the same namespace `read_groups`.\n- The catalog invariant family covers the new seam; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe admitted namespace carries the endpoint inventory as Crossplane resources (measured on the\ndev cluster's `latest`: 24 MySQL + 23 PostgreSQL database resources, 35 connection secrets).\nThe hosted Kubernetes integration becomes an endpoint discoverer: a read-only datasource over\nthe admitted namespaces that lists database managed resources and derives endpoint descriptors\n— engine, host, port, database, connection-secret reference — never credential bytes. The\ndescriptors are what S-058's connections consume, so a discovered database is usable without\nanyone writing configuration.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-059`, recorded as the reference `legacy:S-059`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:23`. **read**\n\n- A datasource (bindings per admitted namespace; read verbs list/get) returns the derived\n  endpoint descriptors for both engines, proven against fakes shaped like the real\n  `database.{mysql,postgresql}.sql.crossplane.io` resources; secret references are named,\n  secret values never read.\n- The RBAC the deployment needs (get/list on those CRDs) is stated in the story's close note\n  for the chart, and admission stays behind the same namespace `read_groups`.\n- The catalog invariant family covers the new seam; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe admitted namespace carries the endpoint inventory as Crossplane resources (measured on the\ndev cluster's `latest`: 24 MySQL + 23 PostgreSQL database resources, 35 connection secrets).\nThe hosted Kubernetes integration becomes an endpoint discoverer: a read-only datasource over\nthe admitted namespaces that lists database managed resources and derives endpoint descriptors\n— engine, host, port, database, connection-secret reference — never credential bytes. The\ndescriptors are what S-058's connections consume, so a discovered database is usable without\nanyone writing configuration.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-059-kubernetes-discovers-database-endpoints.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-059`, recorded as the reference `legacy:S-059`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000169"},"payload":{"actor":"human:timo","at":1788482233460,"causation":"cmd-protocol-artifact-body-section-story-kubernetes-discovers-database-endpoints-1788482233460","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:kubernetes-discovers-database-endpoints@6#0~a859ed895ac2b440","executor":null,"recorded_at":"2026-09-04T00:37:13Z"}}
{"entity":"story","version":1,"id":"monitoring-joins-the-mcp-toolset","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000190","to":"implemented"},"payload":{"actor":"human:timo","at":1788482233649,"causation":"cmd-move-story-monitoring-joins-the-mcp-toolset-1788482233649","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-joins-the-mcp-toolset@5#0~c9a8be29424fa102","executor":null,"recorded_at":"2026-09-04T00:37:13Z"}}
{"entity":"story","version":1,"id":"monitoring-joins-the-mcp-toolset","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:23`. **read**\n\n- `tool_search` for a monitoring-read-group principal lists the monitoring tools with input\n  schemas that surface the configured targets (connection choices) honestly; a group-less\n  principal sees none; router tests cover a happy invoke and a refusal per design 14's\n  patterns.\n- The rule-17 seam guard still binds the toolset; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe hosted deployment already reaches the central Grafana on the infra cluster with fifteen\nconfigured Prometheus/Loki/Alertmanager targets, but the MCP toolset projects only Kubernetes.\nAdd the monitoring tools to the static toolset — grafana dashboards list/get, datasources\nlist, prometheus_query_range, loki_query_range, alertmanager_alerts — with the same\nrequirement-driven role filtering (an entry appears only when the caller's own seam results\nsupport it) and the same lease hiding. All six underlying operations are catalogued read-only\nand ride the read path; the toolset adds zero policy.\n\nSource frontmatter: pillar Platform · areas [server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-060`, recorded as the reference `legacy:S-060`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:23`. **read**\n\n- `tool_search` for a monitoring-read-group principal lists the monitoring tools with input\n  schemas that surface the configured targets (connection choices) honestly; a group-less\n  principal sees none; router tests cover a happy invoke and a refusal per design 14's\n  patterns.\n- The rule-17 seam guard still binds the toolset; `bash scripts/gate.sh` exits 0.\n\n## Context\n\nThe hosted deployment already reaches the central Grafana on the infra cluster with fifteen\nconfigured Prometheus/Loki/Alertmanager targets, but the MCP toolset projects only Kubernetes.\nAdd the monitoring tools to the static toolset — grafana dashboards list/get, datasources\nlist, prometheus_query_range, loki_query_range, alertmanager_alerts — with the same\nrequirement-driven role filtering (an entry appears only when the caller's own seam results\nsupport it) and the same lease hiding. All six underlying operations are catalogued read-only\nand ride the read path; the toolset adds zero policy.\n\nSource frontmatter: pillar Platform · areas [server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-060-monitoring-joins-the-mcp-toolset.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-060`, recorded as the reference `legacy:S-060`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000190"},"payload":{"actor":"human:timo","at":1788482233772,"causation":"cmd-protocol-artifact-body-section-story-monitoring-joins-the-mcp-toolset-1788482233772","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:monitoring-joins-the-mcp-toolset@6#0~c7827b427d3262df","executor":null,"recorded_at":"2026-09-04T00:37:13Z"}}
{"entity":"story","version":1,"id":"a-logged-in-person-needs-no-configuration","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000082","to":"implemented"},"payload":{"actor":"human:timo","at":1788482233968,"causation":"cmd-move-story-a-logged-in-person-needs-no-configuration-1788482233968","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:a-logged-in-person-needs-no-configuration@4#0~00c21f2edb75fe7a","executor":null,"recorded_at":"2026-09-04T00:37:13Z"}}
{"entity":"story","version":1,"id":"a-logged-in-person-needs-no-configuration","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:22`. **read**\n\n- On a machine with no prior state: `connectors login <connectors-base>` completes the browser\n  flow, and a following catalog/search/invoke command against the selected hosted deployment\n  succeeds with no endpoint or token flags; tokens refresh across the\n  300-second expiry without re-login, proven by an integration test with a fake identity.\n- The thin-CLI architecture fence stays green — the behaviour lands behind connectors-client\n  per its prescription.\n\n## Context\n\nToday a person needs curl and token plumbing to use the hosted plane. Give the local Connectors\nCLI a zero-configuration hosted mode: `connectors login <connectors-base>` reads that deployment's\npublic bootstrap document, drives the neutral Identity loopback flow, and places the opaque login\nsession in the OS keyring. Subsequent hosted CLI requests and the stdio MCP bridge exchange that\nsession for short-lived, exact-scope tokens transparently. Identity remains relying-party neutral:\nConnectors publishes the Identity origin it trusts, never the other way around.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-061`, recorded as the reference `legacy:S-061`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:22`. **read**\n\n- On a machine with no prior state: `connectors login <connectors-base>` completes the browser\n  flow, and a following catalog/search/invoke command against the selected hosted deployment\n  succeeds with no endpoint or token flags; tokens refresh across the\n  300-second expiry without re-login, proven by an integration test with a fake identity.\n- The thin-CLI architecture fence stays green — the behaviour lands behind connectors-client\n  per its prescription.\n\n## Context\n\nToday a person needs curl and token plumbing to use the hosted plane. Give the local Connectors\nCLI a zero-configuration hosted mode: `connectors login <connectors-base>` reads that deployment's\npublic bootstrap document, drives the neutral Identity loopback flow, and places the opaque login\nsession in the OS keyring. Subsequent hosted CLI requests and the stdio MCP bridge exchange that\nsession for short-lived, exact-scope tokens transparently. Identity remains relying-party neutral:\nConnectors publishes the Identity origin it trusts, never the other way around.\n\nSource frontmatter: pillar Platform · areas [config, integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-061-a-logged-in-person-needs-no-configuration.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-09-02 · 2 revision(s)\n- Legacy id `S-061`, recorded as the reference `legacy:S-061`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788482234094,"causation":"cmd-protocol-artifact-body-section-story-a-logged-in-person-needs-no-configuration-1788482234094","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:a-logged-in-person-needs-no-configuration@5#0~e5e4f7e53c1c19b7","executor":null,"recorded_at":"2026-09-04T00:37:14Z"}}
{"entity":"story","version":1,"id":"discovery-reads-the-cluster-scoped-database-resources","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000145","to":"implemented"},"payload":{"actor":"human:timo","at":1788482234286,"causation":"cmd-move-story-discovery-reads-the-cluster-scoped-database-resources-1788482234286","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:discovery-reads-the-cluster-scoped-database-resources@5#0~25973108d884c884","executor":null,"recorded_at":"2026-09-04T00:37:14Z"}}
{"entity":"story","version":1,"id":"discovery-reads-the-cluster-scoped-database-resources","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:29`. **read**\n\n- The reader lists the cluster-scoped collection and follows `spec.providerConfigRef` to the\n  `ProviderConfig`, emitting `{engine, name, provider_config, secret_ref{name,namespace}, ready}`;\n  secret VALUES are still never read — endpoint resolution stays with the SQL driver's\n  credential custody at connect time.\n- Namespace association is derived honestly (the secret reference's namespace), and the\n  datasource description stops promising \"namespace-scoped\" facts the resources do not declare.\n- A wrong-scope regression is impossible to reintroduce silently: a 404 from a group that IS\n  served (its discovery document lists `databases`) surfaces as an error, not an empty list.\n- e2e on dev: the list read returns 24 + 23 records with correct engines and ready flags.\n\n## Context\n\n`kubernetes.databases` returns zero records on the dev cluster while 24 MySQL and 23 PostgreSQL\n`Database` resources exist (e2e 2026-08-24, release rev 14). Two mismatches with the real\nprovider-sql shape, found live:\n\n- The reader lists `/apis/{group}/v1alpha1/namespaces/{ns}/databases`\n  (`crates/integration-kubernetes/src/hosted.rs:608-615`), but the CRDs are **cluster-scoped**\n  (`kubectl api-resources`: NAMESPACED=false). The API answers 404, which the\n  404-means-no-Crossplane mapping silently turns into an empty inventory — the wrong path is\n  indistinguishable from an absent provider.\n- The live `Database` spec carries **no endpoint facts** — only `providerConfigRef` and\n  `deletionPolicy`; host/port live behind the referenced `ProviderConfig`'s\n  `credentials.connectionSecretRef` (name + namespace). The descriptor model\n  `{host, port, database, secret_ref}` cannot be filled from the Database resource alone.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-062`, recorded as the reference `legacy:S-062`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:29`. **read**\n\n- The reader lists the cluster-scoped collection and follows `spec.providerConfigRef` to the\n  `ProviderConfig`, emitting `{engine, name, provider_config, secret_ref{name,namespace}, ready}`;\n  secret VALUES are still never read — endpoint resolution stays with the SQL driver's\n  credential custody at connect time.\n- Namespace association is derived honestly (the secret reference's namespace), and the\n  datasource description stops promising \"namespace-scoped\" facts the resources do not declare.\n- A wrong-scope regression is impossible to reintroduce silently: a 404 from a group that IS\n  served (its discovery document lists `databases`) surfaces as an error, not an empty list.\n- e2e on dev: the list read returns 24 + 23 records with correct engines and ready flags.\n\n## Context\n\n`kubernetes.databases` returns zero records on the dev cluster while 24 MySQL and 23 PostgreSQL\n`Database` resources exist (e2e 2026-08-24, release rev 14). Two mismatches with the real\nprovider-sql shape, found live:\n\n- The reader lists `/apis/{group}/v1alpha1/namespaces/{ns}/databases`\n  (`crates/integration-kubernetes/src/hosted.rs:608-615`), but the CRDs are **cluster-scoped**\n  (`kubectl api-resources`: NAMESPACED=false). The API answers 404, which the\n  404-means-no-Crossplane mapping silently turns into an empty inventory — the wrong path is\n  indistinguishable from an absent provider.\n- The live `Database` spec carries **no endpoint facts** — only `providerConfigRef` and\n  `deletionPolicy`; host/port live behind the referenced `ProviderConfig`'s\n  `credentials.connectionSecretRef` (name + namespace). The descriptor model\n  `{host, port, database, secret_ref}` cannot be filled from the Database resource alone.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-062-discovery-reads-the-cluster-scoped-database-resources.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-062`, recorded as the reference `legacy:S-062`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788482234409,"causation":"cmd-protocol-artifact-body-section-story-discovery-reads-the-cluster-scoped-database-resources-1788482234409","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:discovery-reads-the-cluster-scoped-database-resources@6#0~eaf63a37892583a2","executor":null,"recorded_at":"2026-09-04T00:37:14Z"}}
{"entity":"story","version":1,"id":"the-deployment-list-pages-instead-of-refusing","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000283","to":"implemented"},"payload":{"actor":"human:timo","at":1788482234704,"causation":"cmd-move-story-the-deployment-list-pages-instead-of-refusing-1788482234704","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-deployment-list-pages-instead-of-refusing@5#0~ae4a5341919aa329","executor":null,"recorded_at":"2026-09-04T00:37:14Z"}}
{"entity":"story","version":1,"id":"the-deployment-list-pages-instead-of-refusing","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:27`. **read**\n\n- [x] List records carry the minimal projection only — `name`, `desired_replicas`,\n      `ready_replicas`, `rollout_state` — and nothing K8s-internal; object identity\n      (`namespace`, `uid`, `resource_version`, generations, replica breakdowns) stays with the\n      detail record.\n- [x] `k8s_deployment_list {namespace}` — no paging args — succeeds against a fake namespace of\n      100+ deployments whose raw objects are large enough to have tripped the live refusal;\n      `result_too_large` does not occur.\n- [x] The aggregation is hard-bounded (500 records / 40 seam pages) and a cut listing says so\n      with `truncated: true`.\n- [x] The tool description names the contract: minimal summaries, whole namespace in one call,\n      the truncation bound, and `k8s_deployment_status` for one deployment's depth.\n\n## Context\n\n`tool_invoke k8s_deployment_list {namespace: \"latest\"}` refuses with `result_too_large`\n(e2e 2026-08-24): the namespace holds too many deployments for one response envelope, and the\ntool exposes no way to page or bound the result. A bounded refusal is correct fail-closed\nbehaviour, but a list tool that cannot list a busy namespace is not usable.\n\n**Amended 2026-08-24 (Timo):** minimal records over a paging surface. List-shaped Kubernetes\ndatasource results must not carry raw or verbose object data — a list record is a name plus the\nsmallest rollout summary, and depth belongs to the get/describe path. If slim records fit, the\nMCP tool carries no `limit`/`cursor` arguments at all; upstream paging stays an internal,\nbounded implementation detail, and a hard total cap with an explicit truncation marker replaces\nan unbounded walk.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-063`, recorded as the reference `legacy:S-063`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:27`. **read**\n\n- [x] List records carry the minimal projection only — `name`, `desired_replicas`,\n      `ready_replicas`, `rollout_state` — and nothing K8s-internal; object identity\n      (`namespace`, `uid`, `resource_version`, generations, replica breakdowns) stays with the\n      detail record.\n- [x] `k8s_deployment_list {namespace}` — no paging args — succeeds against a fake namespace of\n      100+ deployments whose raw objects are large enough to have tripped the live refusal;\n      `result_too_large` does not occur.\n- [x] The aggregation is hard-bounded (500 records / 40 seam pages) and a cut listing says so\n      with `truncated: true`.\n- [x] The tool description names the contract: minimal summaries, whole namespace in one call,\n      the truncation bound, and `k8s_deployment_status` for one deployment's depth.\n\n## Context\n\n`tool_invoke k8s_deployment_list {namespace: \"latest\"}` refuses with `result_too_large`\n(e2e 2026-08-24): the namespace holds too many deployments for one response envelope, and the\ntool exposes no way to page or bound the result. A bounded refusal is correct fail-closed\nbehaviour, but a list tool that cannot list a busy namespace is not usable.\n\n**Amended 2026-08-24 (Timo):** minimal records over a paging surface. List-shaped Kubernetes\ndatasource results must not carry raw or verbose object data — a list record is a name plus the\nsmallest rollout summary, and depth belongs to the get/describe path. If slim records fit, the\nMCP tool carries no `limit`/`cursor` arguments at all; upstream paging stays an internal,\nbounded implementation detail, and a hard total cap with an explicit truncation marker replaces\nan unbounded walk.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/14-mcp-transport-for-the-hosted-connectors-server.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-063-the-deployment-list-pages-instead-of-refusing.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-063`, recorded as the reference `legacy:S-063`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000283"},"payload":{"actor":"human:timo","at":1788482234929,"causation":"cmd-protocol-artifact-body-section-story-the-deployment-list-pages-instead-of-refusing-1788482234929","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-deployment-list-pages-instead-of-refusing@6#0~a8173dfa5939eae2","executor":null,"recorded_at":"2026-09-04T00:37:14Z"}}
{"entity":"story","version":1,"id":"monitoring-tool-schemas-tell-the-truth","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000196","to":"implemented"},"payload":{"actor":"human:timo","at":1788482235170,"causation":"cmd-move-story-monitoring-tool-schemas-tell-the-truth-1788482235170","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-tool-schemas-tell-the-truth@5#0~f3e8147e5223ec28","executor":null,"recorded_at":"2026-09-04T00:37:15Z"}}
{"entity":"story","version":1,"id":"monitoring-tool-schemas-tell-the-truth","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:27`. **read**\n\n- [x] Each monitoring tool's schema states exactly the fields its operation accepts, required\n      matches what refusal enforces, cursors are optional, and numeric-or-string timestamps are\n      either accepted or documented.\n- [x] The e2e sequence `grafana_dashboards_list {target}` and `prometheus_query_range` with epoch\n      ints passes or refuses with a message naming the expected encoding.\n\n## Context\n\nLive e2e (2026-08-24) against the deployed MCP toolset found the monitoring tool input schemas\nmisleading in three ways:\n\n- `grafana_dashboards_list` requires `[\"namespace\", \"limit\", \"continue\"]` — a Kubernetes\n  workload-list shape, not a Grafana dashboards shape; `continue` (a resume cursor) being\n  REQUIRED forces callers to invent an empty string.\n- `target` is enum-projected but not in `required`, while the tools refuse without it\n  (`invalid_input`), so the schema admits calls that can never succeed.\n- `prometheus_query_range` demands string `start`/`end`/`step`; integer epoch seconds — the\n  natural client encoding — refuse as `invalid_input` with no hint. Either accept both or say\n  \"string\" is unix-seconds-or-RFC3339 in the description.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-064`, recorded as the reference `legacy:S-064`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:27`. **read**\n\n- [x] Each monitoring tool's schema states exactly the fields its operation accepts, required\n      matches what refusal enforces, cursors are optional, and numeric-or-string timestamps are\n      either accepted or documented.\n- [x] The e2e sequence `grafana_dashboards_list {target}` and `prometheus_query_range` with epoch\n      ints passes or refuses with a message naming the expected encoding.\n\n## Context\n\nLive e2e (2026-08-24) against the deployed MCP toolset found the monitoring tool input schemas\nmisleading in three ways:\n\n- `grafana_dashboards_list` requires `[\"namespace\", \"limit\", \"continue\"]` — a Kubernetes\n  workload-list shape, not a Grafana dashboards shape; `continue` (a resume cursor) being\n  REQUIRED forces callers to invent an empty string.\n- `target` is enum-projected but not in `required`, while the tools refuse without it\n  (`invalid_input`), so the schema admits calls that can never succeed.\n- `prometheus_query_range` demands string `start`/`end`/`step`; integer epoch seconds — the\n  natural client encoding — refuse as `invalid_input` with no hint. Either accept both or say\n  \"string\" is unix-seconds-or-RFC3339 in the description.\n\nSource frontmatter: pillar Platform · areas [integrations, server] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-064-monitoring-tool-schemas-tell-the-truth.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-064`, recorded as the reference `legacy:S-064`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000196"},"payload":{"actor":"human:timo","at":1788482235348,"causation":"cmd-protocol-artifact-body-section-story-monitoring-tool-schemas-tell-the-truth-1788482235348","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:monitoring-tool-schemas-tell-the-truth@6#0~52406a15f60fba0d","executor":null,"recorded_at":"2026-09-04T00:37:15Z"}}
{"entity":"story","version":1,"id":"monitoring-refusals-name-the-upstream","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000193","to":"implemented"},"payload":{"actor":"human:timo","at":1788482235576,"causation":"cmd-move-story-monitoring-refusals-name-the-upstream-1788482235576","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:monitoring-refusals-name-the-upstream@4#0~03999189cef5d951","executor":null,"recorded_at":"2026-09-04T00:37:15Z"}}
{"entity":"story","version":1,"id":"monitoring-refusals-name-the-upstream","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:24`. **read**\n\n- [x] A refused monitoring dispatch logs one structured server-side line naming the operation,\n      the route (direct/mediated), and the upstream HTTP status (never the body, which may carry\n      data) — enough to distinguish 401 vs 403 vs 404 vs transport failure from `kubectl logs`.\n- [x] The refusal's `structuredContent`/message distinguishes at least \"upstream refused\n      (status class)\" from \"upstream unreachable\" from \"credential custody failed\", without\n      leaking upstream body content.\n- [ ] With that in place, the live grafana-parent and alertmanager failures are diagnosed on the\n      dev deployment and their concrete cause is recorded in this story (fix here if it is a\n      document/path bug; a provisioning follow-up if it is credential rights).\n\n## Context\n\nTwo monitoring surfaces refuse `unavailable — monitoring connector runtime is unavailable` on\nthe dev deployment (e2e 2026-08-24, rev 15) while their siblings work: the parent Grafana\ntools (`grafana_dashboards_list` on Grafana 13.2.0, whose app-platform API answers the\norigin), and `alertmanager_alerts` — while `prometheus_query_range` and `loki_query_range`\nride the same credential and the same datasource proxy successfully. Diagnosis is impossible\nfrom outside because the backend maps every transport error, non-2xx and non-JSON body to the\nsame context-free refusal (`integration-monitoring/src/backend.rs:110-114` and the\n`operation_unavailable()` call sites), and nothing is logged server-side.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-065`, recorded as the reference `legacy:S-065`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:24`. **read**\n\n- [x] A refused monitoring dispatch logs one structured server-side line naming the operation,\n      the route (direct/mediated), and the upstream HTTP status (never the body, which may carry\n      data) — enough to distinguish 401 vs 403 vs 404 vs transport failure from `kubectl logs`.\n- [x] The refusal's `structuredContent`/message distinguishes at least \"upstream refused\n      (status class)\" from \"upstream unreachable\" from \"credential custody failed\", without\n      leaking upstream body content.\n- [ ] With that in place, the live grafana-parent and alertmanager failures are diagnosed on the\n      dev deployment and their concrete cause is recorded in this story (fix here if it is a\n      document/path bug; a provisioning follow-up if it is credential rights).\n\n## Context\n\nTwo monitoring surfaces refuse `unavailable — monitoring connector runtime is unavailable` on\nthe dev deployment (e2e 2026-08-24, rev 15) while their siblings work: the parent Grafana\ntools (`grafana_dashboards_list` on Grafana 13.2.0, whose app-platform API answers the\norigin), and `alertmanager_alerts` — while `prometheus_query_range` and `loki_query_range`\nride the same credential and the same datasource proxy successfully. Diagnosis is impossible\nfrom outside because the backend maps every transport error, non-2xx and non-JSON body to the\nsame context-free refusal (`integration-monitoring/src/backend.rs:110-114` and the\n`operation_unavailable()` call sites), and nothing is logged server-side.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-065-monitoring-refusals-name-the-upstream.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-065`, recorded as the reference `legacy:S-065`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000193"},"payload":{"actor":"human:timo","at":1788482235741,"causation":"cmd-protocol-artifact-body-section-story-monitoring-refusals-name-the-upstream-1788482235741","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:monitoring-refusals-name-the-upstream@5#0~c7f40e58b5351d63","executor":null,"recorded_at":"2026-09-04T00:37:15Z"}}
{"entity":"story","version":1,"id":"the-transport-refusal-names-its-class","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000313","to":"implemented"},"payload":{"actor":"human:timo","at":1788482235955,"causation":"cmd-move-story-the-transport-refusal-names-its-class-1788482235955","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-transport-refusal-names-its-class@4#0~67bd9a807ddb266b","executor":null,"recorded_at":"2026-09-04T00:37:15Z"}}
{"entity":"story","version":1,"id":"the-transport-refusal-names-its-class","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-066-the-transport-refusal-names-its-class.md:22`. **read**\n\n- [x] The transport arm of the refusal log carries an error class derived from the transport\n  error (timeout / connect / tls / body-read / other) — never the error's Display string,\n  which can embed the URL.\n- [x] The transport class distinguishes an oversized upstream body from unreachability; an\n  executor cap breach refuses as \"response too large\", never \"unreachable\".\n- [x] `grafana_dashboards_list` dispatches with a bounded page `limit` and follows `continue`\n  server-side up to a hard budget, so the 4.6 MB namespace lists instead of refusing.\n- [x] The alertmanager document resolves the v2 API sub-path; `alertmanager_alerts` returns live\n  alerts on dev through the mediated route. (The document half is test-proven through the real\n  resolve path; the live-dev half needs the next deploy to pick the rebuilt catalog pack up.)\n- [x] The alertmanager 403 is IN scope — diagnosis below proved it a document defect, not\n  provisioning.\n\n## Context\n\nS-065's live diagnosis (rev 16) pinned `grafana_dashboards_list` to an upstream-transport\nfailure that is specific to that operation's resolved request — `grafana-datasources-list`\nsucceeds on the same direct route, credential and origin every reconcile tick. The final\ndiscriminator (timeout vs connect vs TLS vs body-read) is discarded at the transport mapping\n(`integration-monitoring`, the `.map_err(|_| UpstreamFailure::Transport)` sites), so the last\nstep of the diagnosis is blind.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-066-the-transport-refusal-names-its-class.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-066-the-transport-refusal-names-its-class.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 4 revision(s)\n- Legacy id `S-066`, recorded as the reference `legacy:S-066`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-066-the-transport-refusal-names-its-class.md:22`. **read**\n\n- [x] The transport arm of the refusal log carries an error class derived from the transport\n  error (timeout / connect / tls / body-read / other) — never the error's Display string,\n  which can embed the URL.\n- [x] The transport class distinguishes an oversized upstream body from unreachability; an\n  executor cap breach refuses as \"response too large\", never \"unreachable\".\n- [x] `grafana_dashboards_list` dispatches with a bounded page `limit` and follows `continue`\n  server-side up to a hard budget, so the 4.6 MB namespace lists instead of refusing.\n- [x] The alertmanager document resolves the v2 API sub-path; `alertmanager_alerts` returns live\n  alerts on dev through the mediated route. (The document half is test-proven through the real\n  resolve path; the live-dev half needs the next deploy to pick the rebuilt catalog pack up.)\n- [x] The alertmanager 403 is IN scope — diagnosis below proved it a document defect, not\n  provisioning.\n\n## Context\n\nS-065's live diagnosis (rev 16) pinned `grafana_dashboards_list` to an upstream-transport\nfailure that is specific to that operation's resolved request — `grafana-datasources-list`\nsucceeds on the same direct route, credential and origin every reconcile tick. The final\ndiscriminator (timeout vs connect vs TLS vs body-read) is discarded at the transport mapping\n(`integration-monitoring`, the `.map_err(|_| UpstreamFailure::Transport)` sites), so the last\nstep of the diagnosis is blind.\n\nSource frontmatter: pillar Platform · areas [integrations] · design `../design/15-a-zero-configuration-endpoint-plane.md`. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-066-the-transport-refusal-names-its-class.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-066-the-transport-refusal-names-its-class.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 4 revision(s)\n- Legacy id `S-066`, recorded as the reference `legacy:S-066`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000313"},"payload":{"actor":"human:timo","at":1788482236103,"causation":"cmd-protocol-artifact-body-section-story-the-transport-refusal-names-its-class-1788482236103","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-transport-refusal-names-its-class@5#0~e72ce5df2eb6e2bf","executor":null,"recorded_at":"2026-09-04T00:37:16Z"}}
{"entity":"story","version":1,"id":"the-server-serves-its-own-openapi-contract","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000310","to":"implemented"},"payload":{"actor":"human:timo","at":1788482236319,"causation":"cmd-move-story-the-server-serves-its-own-openapi-contract-1788482236319","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:the-server-serves-its-own-openapi-contract@5#0~153cfbe921bf1af0","executor":null,"recorded_at":"2026-09-04T00:37:16Z"}}
{"entity":"story","version":1,"id":"the-server-serves-its-own-openapi-contract","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:20`. **read**\n\n- `GET {base}/openapi.json` serves a committed OpenAPI 3.1 document, unauthenticated,\n  `application/json`, immutable per build (ETag or content hash header).\n- The document covers: `/operations`, `/connections`, `/catalog`, `/events`,\n  `/datasources` (envelope request/response schemas incl. the closed method sets and\n  refusal codes), `/mcp` (JSON-RPC shape, the three meta-tools), the health probes, and\n  the bearer security scheme naming the identity audience and scopes.\n- Drift is impossible silently: a test suite validates every example in the document\n  against the real `protocol` serde types — each request example must deserialize\n  (deny_unknown_fields), each refusal example must match a real error shape, and a\n  deliberately wrong example must fail the suite.\n- No new dependency; the document is authored as a file and served verbatim from a new\n  `crates/server/src/hosted/docs.rs`, with only route registration touching `hosted.rs`.\n- The document is b10x-branded; no b10x string at the surface (check-brand stays\n  clean).\n\n## Context\n\nConsumers of the hosted API — the CLI, the harness, external teams — have no\nmachine-readable contract to generate against or validate with. The server knows its\nsurface precisely (five strict envelope endpoints, the MCP transport, health probes), and\nthe repo already pins contracts as committed artifacts; the OpenAPI document joins that\nfamily.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-067`, recorded as the reference `legacy:S-067`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:20`. **read**\n\n- `GET {base}/openapi.json` serves a committed OpenAPI 3.1 document, unauthenticated,\n  `application/json`, immutable per build (ETag or content hash header).\n- The document covers: `/operations`, `/connections`, `/catalog`, `/events`,\n  `/datasources` (envelope request/response schemas incl. the closed method sets and\n  refusal codes), `/mcp` (JSON-RPC shape, the three meta-tools), the health probes, and\n  the bearer security scheme naming the identity audience and scopes.\n- Drift is impossible silently: a test suite validates every example in the document\n  against the real `protocol` serde types — each request example must deserialize\n  (deny_unknown_fields), each refusal example must match a real error shape, and a\n  deliberately wrong example must fail the suite.\n- No new dependency; the document is authored as a file and served verbatim from a new\n  `crates/server/src/hosted/docs.rs`, with only route registration touching `hosted.rs`.\n- The document is b10x-branded; no b10x string at the surface (check-brand stays\n  clean).\n\n## Context\n\nConsumers of the hosted API — the CLI, the harness, external teams — have no\nmachine-readable contract to generate against or validate with. The server knows its\nsurface precisely (five strict envelope endpoints, the MCP transport, health probes), and\nthe repo already pins contracts as committed artifacts; the OpenAPI document joins that\nfamily.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-067-the-server-serves-its-own-openapi-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 3 revision(s)\n- Legacy id `S-067`, recorded as the reference `legacy:S-067`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000310"},"payload":{"actor":"human:timo","at":1788482236462,"causation":"cmd-protocol-artifact-body-section-story-the-server-serves-its-own-openapi-contract-1788482236462","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:the-server-serves-its-own-openapi-contract@6#0~5af0f8d8a226edbf","executor":null,"recorded_at":"2026-09-04T00:37:16Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000088","to":"implemented"},"payload":{"actor":"human:timo","at":1788482236658,"causation":"cmd-move-story-a-public-documentation-page-renders-the-contract-1788482236658","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:a-public-documentation-page-renders-the-contract@5#0~edd5ce8162201854","executor":null,"recorded_at":"2026-09-04T00:37:16Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:20`. **read**\n\n- `GET {base}/docs` serves one self-contained HTML page, unauthenticated: zero external\n  requests (no CDN scripts, no fonts, no images fetched elsewhere) — the connect\n  completion page precedent.\n- Content sections: authentication (identity login → access token mint with audience and\n  scopes), the five envelope endpoints with request/response examples lifted from the\n  S-067 document (single source of truth — examples are extracted, not duplicated), the\n  MCP endpoint with an initialize/tools-call example, datasources incl. the read verbs,\n  and the refusal code table.\n- The page links `openapi.json` and renders its version; a drift test asserts every\n  example shown on the page exists in the OpenAPI document.\n- Static and tenant-free: the handler reads no state, takes no auth, and cannot render\n  request-derived content beyond the base path.\n- b10x-branded; check-brand stays clean.\n\n## Context\n\nA person handed the connectors URL has nothing to read. `GET {base}/docs` renders the\npublic documentation: how to log in and mint a token, every endpoint with a working curl\nexample, the MCP entry point, datasources, and the refusal codes — one self-contained\nHTML page, no external assets.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-068`, recorded as the reference `legacy:S-068`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:20`. **read**\n\n- `GET {base}/docs` serves one self-contained HTML page, unauthenticated: zero external\n  requests (no CDN scripts, no fonts, no images fetched elsewhere) — the connect\n  completion page precedent.\n- Content sections: authentication (identity login → access token mint with audience and\n  scopes), the five envelope endpoints with request/response examples lifted from the\n  S-067 document (single source of truth — examples are extracted, not duplicated), the\n  MCP endpoint with an initialize/tools-call example, datasources incl. the read verbs,\n  and the refusal code table.\n- The page links `openapi.json` and renders its version; a drift test asserts every\n  example shown on the page exists in the OpenAPI document.\n- Static and tenant-free: the handler reads no state, takes no auth, and cannot render\n  request-derived content beyond the base path.\n- b10x-branded; check-brand stays clean.\n\n## Context\n\nA person handed the connectors URL has nothing to read. `GET {base}/docs` renders the\npublic documentation: how to log in and mint a token, every endpoint with a working curl\nexample, the MCP entry point, datasources, and the refusal codes — one self-contained\nHTML page, no external assets.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-068`, recorded as the reference `legacy:S-068`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788482236787,"causation":"cmd-protocol-artifact-body-section-story-a-public-documentation-page-renders-the-contract-1788482236787","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:a-public-documentation-page-renders-the-contract@6#0~10c4bd25f1204ecb","executor":null,"recorded_at":"2026-09-04T00:37:16Z"}}
{"entity":"story","version":1,"id":"one-oauth-implementation-not-three","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000211","to":"implemented"},"payload":{"actor":"human:timo","at":1788482236986,"causation":"cmd-move-story-one-oauth-implementation-not-three-1788482236986","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:one-oauth-implementation-not-three@5#0~32ebdefb138b0b44","executor":null,"recorded_at":"2026-09-04T00:37:16Z"}}
{"entity":"story","version":1,"id":"one-oauth-implementation-not-three","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-069-one-oauth-implementation-not-three.md:21`. **read**\n\n- [x] A new `crates/connector-oauth` carries: state generation with a single-use TTL map, S256\n      challenge derivation, authorize-URL construction, bounded token exchange with per-field\n      validation, and refresh with a configurable skew under double-checked locking.\n- [x] `integration-gitlab` migrates first and is the extraction source — it is the only current\n      PKCE implementation (`crates/integration-gitlab/src/backend.rs:678-687`, `:739-750`, `:752`,\n      `:806-855`). Its behaviour is unchanged, proven by its existing tests passing untouched.\n- [x] `integration-jira` migrates second. Its refresh path is the best in the repo\n      (`crates/integration-jira/src/backend/auth.rs:479-518` skew + double-checked lock,\n      `:416-451` crash recovery) and the extracted crate must not regress it — if the shared shape\n      cannot express it, the shared shape is wrong.\n- [x] `integration-slack` migrates third, keeping its egress-gate routing\n      (`crates/integration-slack/src/backend/api_runtime.rs:43-179`) rather than a bare client.\n      **Partially, on purpose** — only the pending-state table moved; see the note below. Two of\n      three is the honest result, not a shortfall hidden behind a tick.\n- [x] **Behaviour-preserving.** Jira and Slack stay non-PKCE; neither provider declares\n      `public_client`, so adding PKCE here would be an undeclared change to a live integration.\n- [x] One commit per integration, gate green between each, so a regression bisects to one vendor.\n      `f7149ab` GitLab, `ce04be1` Jira, `d9fc657` Slack. The first draft landed all three as one\n      commit; independent review caught it and the branch was split before it reached `main`.\n- [x] The prepared-transaction commit path stays the only way a token pair is persisted; no point\n      write appears in the extracted crate.\n\n## Context\n\nReplace three hand-rolled OAuth implementations with one, so the next connector that needs a\nbrowser flow inherits PKCE, single-use state, bounded exchange and crash-safe refresh instead of\ncopying whichever neighbour it read first.\n\nSource frontmatter: pillar Platform · areas [service, integration-gitlab, integration-jira, integration-slack] · priority 1. **read**\n\nSource `note:` field, quoted: “GitLab, Jira and Slack each hand-rolled authorization-code OAuth; only GitLab does PKCE. Extract one crate from the GitLab implementation and migrate the other two onto it, behaviour-preserving. Prerequisite Timo chose for the Claude connector work; also unblocks S-013's loopback callback.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-069-one-oauth-implementation-not-three.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-069-one-oauth-implementation-not-three.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-069`, recorded as the reference `legacy:S-069`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-069-one-oauth-implementation-not-three.md:21`. **read**\n\n- [x] A new `crates/connector-oauth` carries: state generation with a single-use TTL map, S256\n      challenge derivation, authorize-URL construction, bounded token exchange with per-field\n      validation, and refresh with a configurable skew under double-checked locking.\n- [x] `integration-gitlab` migrates first and is the extraction source — it is the only current\n      PKCE implementation (`crates/integration-gitlab/src/backend.rs:678-687`, `:739-750`, `:752`,\n      `:806-855`). Its behaviour is unchanged, proven by its existing tests passing untouched.\n- [x] `integration-jira` migrates second. Its refresh path is the best in the repo\n      (`crates/integration-jira/src/backend/auth.rs:479-518` skew + double-checked lock,\n      `:416-451` crash recovery) and the extracted crate must not regress it — if the shared shape\n      cannot express it, the shared shape is wrong.\n- [x] `integration-slack` migrates third, keeping its egress-gate routing\n      (`crates/integration-slack/src/backend/api_runtime.rs:43-179`) rather than a bare client.\n      **Partially, on purpose** — only the pending-state table moved; see the note below. Two of\n      three is the honest result, not a shortfall hidden behind a tick.\n- [x] **Behaviour-preserving.** Jira and Slack stay non-PKCE; neither provider declares\n      `public_client`, so adding PKCE here would be an undeclared change to a live integration.\n- [x] One commit per integration, gate green between each, so a regression bisects to one vendor.\n      `f7149ab` GitLab, `ce04be1` Jira, `d9fc657` Slack. The first draft landed all three as one\n      commit; independent review caught it and the branch was split before it reached `main`.\n- [x] The prepared-transaction commit path stays the only way a token pair is persisted; no point\n      write appears in the extracted crate.\n\n## Context\n\nReplace three hand-rolled OAuth implementations with one, so the next connector that needs a\nbrowser flow inherits PKCE, single-use state, bounded exchange and crash-safe refresh instead of\ncopying whichever neighbour it read first.\n\nSource frontmatter: pillar Platform · areas [service, integration-gitlab, integration-jira, integration-slack] · priority 1. **read**\n\nSource `note:` field, quoted: “GitLab, Jira and Slack each hand-rolled authorization-code OAuth; only GitLab does PKCE. Extract one crate from the GitLab implementation and migrate the other two onto it, behaviour-preserving. Prerequisite Timo chose for the Claude connector work; also unblocks S-013's loopback callback.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-069-one-oauth-implementation-not-three.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-069-one-oauth-implementation-not-three.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-069`, recorded as the reference `legacy:S-069`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000211"},"payload":{"actor":"human:timo","at":1788482237152,"causation":"cmd-protocol-artifact-body-section-story-one-oauth-implementation-not-three-1788482237152","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:one-oauth-implementation-not-three@6#0~6736a70f6493209c","executor":null,"recorded_at":"2026-09-04T00:37:17Z"}}
{"entity":"story","version":1,"id":"a-provider-can-hold-a-credential-it-cannot-spend","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000085","to":"implemented"},"payload":{"actor":"human:timo","at":1788482237633,"causation":"cmd-move-story-a-provider-can-hold-a-credential-it-cannot-spend-1788482237633","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:a-provider-can-hold-a-credential-it-cannot-spend@5#0~2a9899b32581974d","executor":null,"recorded_at":"2026-09-04T00:37:17Z"}}
{"entity":"story","version":1,"id":"a-provider-can-hold-a-credential-it-cannot-spend","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:21`. **read**\n\n- [x] `custody_only = true` is a provider-level declaration field, schema'd in\n      `crates/connector-spec/schema/provider-toml.schema.json` and represented in the IR.\n- [x] The loader **refuses**, with a named reason each, a `custody_only` provider that declares\n      `[spec]`, `[[operations]]`, `[[services]]`, `base_url`, `verify`, `[[channels]]`,\n      `[[events]]`, `[[discoveries]]`, `[[graphs]]`, `[patch]`, `const_headers`, `default_auth`, or\n      an `auth.oauth2` block. Each is a refusal, not a silent allowance, so the kind cannot smuggle\n      a half-declared ordinary provider past review. Asked of the **declared key**, so\n      `base_url = \"\"` and `operations = []` are refused too.\n- [x] The loader **requires** at least one `[[auth]]` on a `custody_only` provider — a provider that\n      holds nothing has no reason to exist. `auth = []` is refused for the same reason.\n- [x] The existing refusal at `crates/connector-spec/src/provider/validation.rs:31` is unchanged\n      for every other provider, and its golden `tests/golden/nothing-to-generate.error` still\n      matches byte for byte.\n- [x] The invariant is parameterised over every provider setting the flag, in the one consolidated\n      file `crates/catalog-build/tests/main/catalog_invariants.rs` —\n      `a_custody_only_provider_publishes_no_surface_and_every_other_provider_does`, asserting both\n      directions. **No per-provider test file** — AGENTS.md states the rule once and parameterises\n      it.\n- [x] `catalog build` emits a custody-only entry; `catalog diff` is clean twice; `catalog check`\n      passes. The catalog document publishes `custody_only` and carries no service at all, rather\n      than one implicit service with an empty base URL.\n\n## Context\n\nLet a declaration say \"this provider owns a credential and no request surface\", so a credential\nwhose *use* belongs to another component can still have an owner, an address and a lifecycle here —\nwithout any path by which connectors could spend it.\n\nSource frontmatter: pillar Catalog · areas [connector-spec, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “design 16: the custody_only declaration kind. Today the loader refuses a provider with neither [spec] nor [[operations]], so custody-without-use cannot be expressed. The two workarounds available are both dishonest; this is the schema consequence.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-070`, recorded as the reference `legacy:S-070`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:21`. **read**\n\n- [x] `custody_only = true` is a provider-level declaration field, schema'd in\n      `crates/connector-spec/schema/provider-toml.schema.json` and represented in the IR.\n- [x] The loader **refuses**, with a named reason each, a `custody_only` provider that declares\n      `[spec]`, `[[operations]]`, `[[services]]`, `base_url`, `verify`, `[[channels]]`,\n      `[[events]]`, `[[discoveries]]`, `[[graphs]]`, `[patch]`, `const_headers`, `default_auth`, or\n      an `auth.oauth2` block. Each is a refusal, not a silent allowance, so the kind cannot smuggle\n      a half-declared ordinary provider past review. Asked of the **declared key**, so\n      `base_url = \"\"` and `operations = []` are refused too.\n- [x] The loader **requires** at least one `[[auth]]` on a `custody_only` provider — a provider that\n      holds nothing has no reason to exist. `auth = []` is refused for the same reason.\n- [x] The existing refusal at `crates/connector-spec/src/provider/validation.rs:31` is unchanged\n      for every other provider, and its golden `tests/golden/nothing-to-generate.error` still\n      matches byte for byte.\n- [x] The invariant is parameterised over every provider setting the flag, in the one consolidated\n      file `crates/catalog-build/tests/main/catalog_invariants.rs` —\n      `a_custody_only_provider_publishes_no_surface_and_every_other_provider_does`, asserting both\n      directions. **No per-provider test file** — AGENTS.md states the rule once and parameterises\n      it.\n- [x] `catalog build` emits a custody-only entry; `catalog diff` is clean twice; `catalog check`\n      passes. The catalog document publishes `custody_only` and carries no service at all, rather\n      than one implicit service with an empty base URL.\n\n## Context\n\nLet a declaration say \"this provider owns a credential and no request surface\", so a credential\nwhose *use* belongs to another component can still have an owner, an address and a lifecycle here —\nwithout any path by which connectors could spend it.\n\nSource frontmatter: pillar Catalog · areas [connector-spec, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “design 16: the custody_only declaration kind. Today the loader refuses a provider with neither [spec] nor [[operations]], so custody-without-use cannot be expressed. The two workarounds available are both dishonest; this is the schema consequence.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-070-a-provider-can-hold-a-credential-it-cannot-spend.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-08-25 · 3 revision(s)\n- Legacy id `S-070`, recorded as the reference `legacy:S-070`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788482237770,"causation":"cmd-protocol-artifact-body-section-story-a-provider-can-hold-a-credential-it-cannot-spend-1788482237770","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:a-provider-can-hold-a-credential-it-cannot-spend@6#0~2e8596b8134a1d4b","executor":null,"recorded_at":"2026-09-04T00:37:17Z"}}
{"entity":"story","version":1,"id":"claude-code-joins-the-catalog","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000115","to":"implemented"},"payload":{"actor":"human:timo","at":1788482237982,"causation":"cmd-move-story-claude-code-joins-the-catalog-1788482237982","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:claude-code-joins-the-catalog@5#0~7f4f09b70243321b","executor":null,"recorded_at":"2026-09-04T00:37:17Z"}}
{"entity":"story","version":1,"id":"claude-code-joins-the-catalog","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-071-claude-code-joins-the-catalog.md:20`. **read**\n\n- [x] `providers/claude-code.toml` declares `id = \"claude-code\"`,\n      `authority = \"com.anthropic.claude-code\"`, `custody_only = true`, and exactly one credential\n      with `entry = \"connect_session\"`.\n- [x] The authority `com.anthropic.claude-code` differs from `anthropic`'s `com.anthropic.api`, and\n      a catalogue invariant asserts no two providers share an authority — the authority leads every\n      credential path this provider will ever own and is never repointed (AGENTS.md, adding a\n      connector, step 3).\n- [x] The declaration carries **no** `client_id`, no secret, no credential-shaped example, and no\n      reference to `claude.ai/oauth` or `platform.claude.com/v1/oauth`. We do not drive the vendor's\n      OAuth; `claude setup-token` does, on the person's own machine.\n- [x] `providers/anthropic.toml` is untouched. Its invariant at\n      `crates/catalog-build/tests/main/catalog_invariants.rs:1226` — auth exactly\n      `[\"anthropic.api_key\", \"anthropic.admin_key\"]`, no Claude Code authority in the API connector\n      — still passes unmodified. A separate id is what keeps that assertion honest.\n- [x] A parameterised invariant asserts `claude-code` exposes zero operations and zero services, so\n      nothing in connectors can spend the credential.\n- [x] Build and commit as one unit: `catalog build` → `catalog diff` clean twice → `catalog check`.\n\n## Context\n\nGive a person's Claude Code subscription credential an owner, a per-user address and a revocation\npath, without connectors ever being able to spend it.\n\nSource frontmatter: pillar Catalog · areas [providers, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “New provider id claude-code, authority com.anthropic.claude-code, custody_only. The credential is a claude setup-token output pasted into a Connect Session. Blocked on S-070; the authority is permanent once chosen.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-071-claude-code-joins-the-catalog.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-071-claude-code-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-09-01 · 4 revision(s)\n- Legacy id `S-071`, recorded as the reference `legacy:S-071`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-071-claude-code-joins-the-catalog.md:20`. **read**\n\n- [x] `providers/claude-code.toml` declares `id = \"claude-code\"`,\n      `authority = \"com.anthropic.claude-code\"`, `custody_only = true`, and exactly one credential\n      with `entry = \"connect_session\"`.\n- [x] The authority `com.anthropic.claude-code` differs from `anthropic`'s `com.anthropic.api`, and\n      a catalogue invariant asserts no two providers share an authority — the authority leads every\n      credential path this provider will ever own and is never repointed (AGENTS.md, adding a\n      connector, step 3).\n- [x] The declaration carries **no** `client_id`, no secret, no credential-shaped example, and no\n      reference to `claude.ai/oauth` or `platform.claude.com/v1/oauth`. We do not drive the vendor's\n      OAuth; `claude setup-token` does, on the person's own machine.\n- [x] `providers/anthropic.toml` is untouched. Its invariant at\n      `crates/catalog-build/tests/main/catalog_invariants.rs:1226` — auth exactly\n      `[\"anthropic.api_key\", \"anthropic.admin_key\"]`, no Claude Code authority in the API connector\n      — still passes unmodified. A separate id is what keeps that assertion honest.\n- [x] A parameterised invariant asserts `claude-code` exposes zero operations and zero services, so\n      nothing in connectors can spend the credential.\n- [x] Build and commit as one unit: `catalog build` → `catalog diff` clean twice → `catalog check`.\n\n## Context\n\nGive a person's Claude Code subscription credential an owner, a per-user address and a revocation\npath, without connectors ever being able to spend it.\n\nSource frontmatter: pillar Catalog · areas [providers, catalog-build] · design `../design/16-subscription-credential-custody.md`. **read**\n\nSource `note:` field, quoted: “New provider id claude-code, authority com.anthropic.claude-code, custody_only. The credential is a claude setup-token output pasted into a Connect Session. Blocked on S-070; the authority is permanent once chosen.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-071-claude-code-joins-the-catalog.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-071-claude-code-joins-the-catalog.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-25 · last touched 2026-09-01 · 4 revision(s)\n- Legacy id `S-071`, recorded as the reference `legacy:S-071`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788482238113,"causation":"cmd-protocol-artifact-body-section-story-claude-code-joins-the-catalog-1788482238113","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:claude-code-joins-the-catalog@6#0~b056dd7e744e5437","executor":null,"recorded_at":"2026-09-04T00:37:18Z"}}
{"entity":"story","version":1,"id":"an-agent-attempt-can-lease-a-user-subscription","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000103","to":"implemented"},"payload":{"actor":"human:timo","at":1788482238306,"causation":"cmd-move-story-an-agent-attempt-can-lease-a-user-subscription-1788482238306","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:an-agent-attempt-can-lease-a-user-subscription@5#0~926370397705ff2a","executor":null,"recorded_at":"2026-09-04T00:37:18Z"}}
{"entity":"story","version":1,"id":"an-agent-attempt-can-lease-a-user-subscription","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:20`. **read**\n\n- [x] Connector-owned custody stores one credential per verified tenant/subject under the permanent\n      Claude Code authority and exposes presence, replacement and idempotent disconnect.\n- [x] Lease creation requires `connectors.credentials.lease` and binds a cryptorandom capability to\n      an exact attempt id, at most one hour, 1–1,024 uses and an in-process 10,000-lease cap.\n- [x] Redemption accepts the lease bearer rather than Identity, compares it in constant time, and\n      refuses wrong attempt, expiry, exhaustion, unknown id and wrong bearer without distinction.\n- [x] Restart, disconnect and credential replacement revoke live leases. Replacement and redemption\n      serialize, so an old capability cannot reach the new credential generation.\n- [x] Hosted routes are opt-in, require Vault custody, bound every body, and return `no-store` plus\n      `no-cache` on successful confidential responses.\n- [x] The official client keeps lease and credential values private, redacts `Debug`, zeroizes on\n      drop, enforces HTTPS, and refuses a cacheable confidential response.\n- [x] The embedded OpenAPI document covers all five operations and marks returned secrets as\n      sensitive without misclassifying response-only fields as write-only.\n- [x] Tests prove connect → lease → wrong-attempt refusal → exact redemption → spent refusal,\n      rotation revocation, disconnect revocation, redaction, and cache-policy refusal.\n\n## Context\n\nLet a user-bound agent attempt run through the user's Claude Code subscription without copying the\ndurable credential into Agent Platform, Devcenter, configuration, logs, or an agent record.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime, identity-http] · design `../design/17-attempt-bounded-subscription-credential-leases.md`. **read**\n\nSource `note:` field, quoted: “A least-privilege Identity exchange creates an expiring finite-use capability for one exact Harness attempt; Connectors keeps durable custody and the provider value reaches only the bearer boundary.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-076`, recorded as the reference `legacy:S-076`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:20`. **read**\n\n- [x] Connector-owned custody stores one credential per verified tenant/subject under the permanent\n      Claude Code authority and exposes presence, replacement and idempotent disconnect.\n- [x] Lease creation requires `connectors.credentials.lease` and binds a cryptorandom capability to\n      an exact attempt id, at most one hour, 1–1,024 uses and an in-process 10,000-lease cap.\n- [x] Redemption accepts the lease bearer rather than Identity, compares it in constant time, and\n      refuses wrong attempt, expiry, exhaustion, unknown id and wrong bearer without distinction.\n- [x] Restart, disconnect and credential replacement revoke live leases. Replacement and redemption\n      serialize, so an old capability cannot reach the new credential generation.\n- [x] Hosted routes are opt-in, require Vault custody, bound every body, and return `no-store` plus\n      `no-cache` on successful confidential responses.\n- [x] The official client keeps lease and credential values private, redacts `Debug`, zeroizes on\n      drop, enforces HTTPS, and refuses a cacheable confidential response.\n- [x] The embedded OpenAPI document covers all five operations and marks returned secrets as\n      sensitive without misclassifying response-only fields as write-only.\n- [x] Tests prove connect → lease → wrong-attempt refusal → exact redemption → spent refusal,\n      rotation revocation, disconnect revocation, redaction, and cache-policy refusal.\n\n## Context\n\nLet a user-bound agent attempt run through the user's Claude Code subscription without copying the\ndurable credential into Agent Platform, Devcenter, configuration, logs, or an agent record.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime, identity-http] · design `../design/17-attempt-bounded-subscription-credential-leases.md`. **read**\n\nSource `note:` field, quoted: “A least-privilege Identity exchange creates an expiring finite-use capability for one exact Harness attempt; Connectors keeps durable custody and the provider value reaches only the bearer boundary.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-076-an-agent-attempt-can-lease-a-user-subscription.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-076`, recorded as the reference `legacy:S-076`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788482238430,"causation":"cmd-protocol-artifact-body-section-story-an-agent-attempt-can-lease-a-user-subscription-1788482238430","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:an-agent-attempt-can-lease-a-user-subscription@6#0~223d0a1dc8a57b34","executor":null,"recorded_at":"2026-09-04T00:37:18Z"}}
{"entity":"story","version":1,"id":"claude-subscriptions-connect-with-pkce-and-refresh","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"asserted\":{\"test_result\":1}}","target":"01MEM0000000000000118","to":"implemented"},"payload":{"actor":"human:timo","at":1788482238619,"causation":"cmd-move-story-claude-subscriptions-connect-with-pkce-and-refresh-1788482238619","change":{"change":"moved","decided_on":{"asserted":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:claude-subscriptions-connect-with-pkce-and-refresh@5#0~f1c0c23ad470c362","executor":null,"recorded_at":"2026-09-04T00:37:18Z"}}
{"entity":"story","version":1,"id":"claude-subscriptions-connect-with-pkce-and-refresh","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:20`. **read**\n\n- [x] A bounded, expiring, single-use start operation creates server-held PKCE state and returns\n      only an authorization URL plus an opaque flow id.\n- [x] Completion is bound to the verified tenant and subject, consumes the pending flow before the\n      provider exchange, verifies the state returned in the provider's `code#state` manual result,\n      exchanges only the code component, and stores neither code nor verifier after the attempt.\n- [x] The provider exchange uses the exact public-client authorization-code JSON contract and\n      validates bounded access, refresh, expiry, and inference-scope fields before custody.\n- [x] Custody stores a versioned refresh-capable record, returns presence only, and exports only a\n      current access credential through an exact attempt lease redemption.\n- [x] Redemption refreshes under serialization before expiry, handles refresh-token rotation, and\n      replaces custody without exposing either token in diagnostics or API responses.\n- [x] Hosted routes and the official Rust client expose start and complete with non-cacheable\n      responses, bounded bodies, exact Identity scope checks, and credential-safe types.\n- [x] The embedded OpenAPI artifact and tests cover start, completion, replay refusal, provider\n      response validation, refresh, and lease redemption.\n\n## Context\n\nLet an authenticated person connect their Claude subscription through the provider's public-client\nPKCE flow instead of obtaining and pasting a durable credential themselves, while keeping every\nprovider token in Connector-owned custody.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime]. **read**\n\nSource `note:` field, quoted: “Connectors owns the public-client PKCE verifier, provider exchange, refresh-capable custody, and refresh-on-redemption; Identity remains provider-agnostic.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-077`, recorded as the reference `legacy:S-077`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:20`. **read**\n\n- [x] A bounded, expiring, single-use start operation creates server-held PKCE state and returns\n      only an authorization URL plus an opaque flow id.\n- [x] Completion is bound to the verified tenant and subject, consumes the pending flow before the\n      provider exchange, verifies the state returned in the provider's `code#state` manual result,\n      exchanges only the code component, and stores neither code nor verifier after the attempt.\n- [x] The provider exchange uses the exact public-client authorization-code JSON contract and\n      validates bounded access, refresh, expiry, and inference-scope fields before custody.\n- [x] Custody stores a versioned refresh-capable record, returns presence only, and exports only a\n      current access credential through an exact attempt lease redemption.\n- [x] Redemption refreshes under serialization before expiry, handles refresh-token rotation, and\n      replaces custody without exposing either token in diagnostics or API responses.\n- [x] Hosted routes and the official Rust client expose start and complete with non-cacheable\n      responses, bounded bodies, exact Identity scope checks, and credential-safe types.\n- [x] The embedded OpenAPI artifact and tests cover start, completion, replay refusal, provider\n      response validation, refresh, and lease redemption.\n\n## Context\n\nLet an authenticated person connect their Claude subscription through the provider's public-client\nPKCE flow instead of obtaining and pasting a durable credential themselves, while keeping every\nprovider token in Connector-owned custody.\n\nSource frontmatter: pillar Platform · areas [subscription-custody, server, connectors-client, connectors-runtime]. **read**\n\nSource `note:` field, quoted: “Connectors owns the public-client PKCE verifier, provider exchange, refresh-capable custody, and refresh-on-redemption; Identity remains provider-agnostic.”\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-077-claude-subscriptions-connect-with-pkce-and-refresh.md`, which is not deleted and now names this artifact.\n\n- First written 2026-09-01 · last touched 2026-09-01 · 2 revision(s)\n- Legacy id `S-077`, recorded as the reference `legacy:S-077`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n"},"command":"update-entity","target":"01MEM0000000000000118"},"payload":{"actor":"human:timo","at":1788482238746,"causation":"cmd-protocol-artifact-body-section-story-claude-subscriptions-connect-with-pkce-and-refresh-1788482238746","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:claude-subscriptions-connect-with-pkce-and-refresh@6#0~69beae7427f67600","executor":null,"recorded_at":"2026-09-04T00:37:18Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":6,"type":"aep.relation.create/v1","from_state":"implemented","to_state":"implemented","changed":{"relations":[{"derived_from":"epic:public-surface"},{"depends_on":"story:the-server-serves-its-own-openapi-contract"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000088","target":"01MEM0000000000000310"},"payload":{"actor":"human:timo","at":1788482460779,"causation":"cmd-story-a-public-documentation-page-renders-the-contract-depends_on-story-the-server-serves-its-own-openapi-contract","change":{"change":"related","relation":"depends_on","target":"story:the-server-serves-its-own-openapi-contract"},"correlation":"protocol-artifact-relate","event_id":"story:a-public-documentation-page-renders-the-contract@6#0~da469123b06f99ee","executor":null,"recorded_at":"2026-09-04T00:41:00Z"}}
{"entity":"story","version":1,"id":"sources-are-processed-by-code","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build"},{"confidence":"cited","path":"crates/connector-spec"}]},"command":"update-entity","target":"01MEM0000000000000259"},"payload":{"actor":"human:timo","at":1788482469215,"causation":"cmd-protocol-artifact-scope-story-sources-are-processed-by-code-1788482469215","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:sources-are-processed-by-code@3#0~626276cfa0b72104","executor":null,"recorded_at":"2026-09-04T00:41:09Z"}}
{"entity":"story","version":1,"id":"a-public-documentation-page-renders-the-contract","revision":7,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:20`. **read**\n\n- `GET {base}/docs` serves one self-contained HTML page, unauthenticated: zero external\n  requests (no CDN scripts, no fonts, no images fetched elsewhere) — the connect\n  completion page precedent.\n- Content sections: authentication (identity login → access token mint with audience and\n  scopes), the five envelope endpoints with request/response examples lifted from the\n  S-067 document (single source of truth — examples are extracted, not duplicated), the\n  MCP endpoint with an initialize/tools-call example, datasources incl. the read verbs,\n  and the refusal code table.\n- The page links `openapi.json` and renders its version; a drift test asserts every\n  example shown on the page exists in the OpenAPI document.\n- Static and tenant-free: the handler reads no state, takes no auth, and cannot render\n  request-derived content beyond the base path.\n- b10x-branded; check-brand stays clean.\n\n## Context\n\nA person handed the connectors URL has nothing to read. `GET {base}/docs` renders the\npublic documentation: how to log in and mint a token, every endpoint with a working curl\nexample, the MCP entry point, datasources, and the refusal codes — one self-contained\nHTML page, no external assets.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-068`, recorded as the reference `legacy:S-068`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n\nSource frontmatter also declares `depends_on: [S-067]`, migrated as the `depends_on` edge to\n`story:the-server-serves-its-own-openapi-contract`. **read**\n"},"args":{"changes":{"body":"## Acceptance\n\nVerbatim from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:20`. **read**\n\n- `GET {base}/docs` serves one self-contained HTML page, unauthenticated: zero external\n  requests (no CDN scripts, no fonts, no images fetched elsewhere) — the connect\n  completion page precedent.\n- Content sections: authentication (identity login → access token mint with audience and\n  scopes), the five envelope endpoints with request/response examples lifted from the\n  S-067 document (single source of truth — examples are extracted, not duplicated), the\n  MCP endpoint with an initialize/tools-call example, datasources incl. the read verbs,\n  and the refusal code table.\n- The page links `openapi.json` and renders its version; a drift test asserts every\n  example shown on the page exists in the OpenAPI document.\n- Static and tenant-free: the handler reads no state, takes no auth, and cannot render\n  request-derived content beyond the base path.\n- b10x-branded; check-brand stays clean.\n\n## Context\n\nA person handed the connectors URL has nothing to read. `GET {base}/docs` renders the\npublic documentation: how to log in and mint a token, every endpoint with a working curl\nexample, the MCP entry point, datasources, and the refusal codes — one self-contained\nHTML page, no external assets.\n\nSource frontmatter: pillar Platform · areas [server, docs]. **read**\n\n## Status\n\n`done` in the source. Quoted from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md:5`: `status: done`. **read**\n\nThis artifact reached `implemented` with `aep artifact move --evidence test_result=1`. The journal\nrecords that move as resting on an **assertion**, not on a run this migration observed. The flag is\nwhat the CLI provides for evidence that lives outside the store.\n\nWhat was asserted, and where it came from:\n\n- The source records `status: done` at the line quoted above. **read**\n- `bash scripts/gate.sh` was green at commit `a48030b` on 2026-09-04 — exit 0, 136 `test result: ok`\n  lines across 11 workspaces. **read**, from `~/.cache/connectors-gate/gate2.log`\n\nNo per-story run was attributed to this story. The gate is a repository-wide fact, and reading it as\nproof of one story's acceptance would be an inference this record does not make.\n\n## Provenance\n\nMigrated from `docs/stories/S-068-a-public-documentation-page-renders-the-contract.md`, which is not deleted and now names this artifact.\n\n- First written 2026-08-24 · last touched 2026-08-24 · 2 revision(s)\n- Legacy id `S-068`, recorded as the reference `legacy:S-068`\n- Migrated 2026-09-04 by the `aep-planning:story-migration` skill\n\nSource frontmatter also declares `depends_on: [S-067]`, migrated as the `depends_on` edge to\n`story:the-server-serves-its-own-openapi-contract`. **read**\n"},"command":"update-entity","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788482473224,"causation":"cmd-protocol-artifact-body-append-story-a-public-documentation-page-renders-the-contract-1788482473224","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:a-public-documentation-page-renders-the-contract@7#0~907ccdf748471030","executor":null,"recorded_at":"2026-09-04T00:41:13Z"}}
{"entity":"nosuchkind","version":1,"id":"probe","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# A title\n","refs":[{"provider":"legacy","reference":"S-074"}],"title":"A title"},"args":{"command":"create-entity","data":{"body":"# A title\n","refs":[{"provider":"legacy","reference":"S-074"}],"status":"draft","title":"A title"},"entity_type":"aep.nosuchkind/v1","locator":"ep://planning/store/nosuchkind/probe"},"payload":{"actor":"human:timo","at":1788482562307,"causation":"cmd-new-nosuchkind-probe","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"nosuchkind:probe@1#0~13a63c903a6e4a00","executor":null,"recorded_at":"2026-09-04T00:42:42Z"}}
{"entity":"nosuchkind","version":1,"id":"probe","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:sources"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000479","target":"01MEM0000000000000061"},"payload":{"actor":"human:timo","at":1788482562320,"causation":"cmd-nosuchkind-probe-derived_from-epic-sources","change":{"change":"related","relation":"derived_from","target":"epic:sources"},"correlation":"protocol-artifact-relate","event_id":"nosuchkind:probe@1#0~9b631d3316fff614","executor":null,"recorded_at":"2026-09-04T00:42:42Z"}}
{"entity":"nosuchkind","version":1,"id":"probe","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# A title\n","refs":[{"provider":"legacy","reference":"S-074"}],"title":"A title"},"args":{"command":"create-entity","data":{"body":"# A title\n","refs":[{"provider":"legacy","reference":"S-074"}],"status":"draft","title":"A title"},"entity_type":"aep.nosuchkind/v1","locator":"ep://planning/store/nosuchkind/probe"},"payload":{"actor":"human:timo","at":1788482591986,"causation":"cmd-new-nosuchkind-probe","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"nosuchkind:probe@1#0~13a63c903a6e4a00","executor":null,"recorded_at":"2026-09-04T00:43:11Z"}}
{"entity":"nosuchkind","version":1,"id":"probe","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:sources"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000479","target":"01MEM0000000000000061"},"payload":{"actor":"human:timo","at":1788482591995,"causation":"cmd-nosuchkind-probe-derived_from-epic-sources","change":{"change":"related","relation":"derived_from","target":"epic:sources"},"correlation":"protocol-artifact-relate","event_id":"nosuchkind:probe@1#0~9b631d3316fff614","executor":null,"recorded_at":"2026-09-04T00:43:11Z"}}
{"entity":"nosuchkind","version":1,"id":"probe","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"archived","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000073","to":"archived"},"payload":{"actor":"human:timo","at":1788482592258,"causation":"cmd-move-nosuchkind-probe-1788482592258","change":{"change":"moved","decided_on":{},"from":"draft","to":"archived"},"correlation":"protocol-artifact-move","event_id":"nosuchkind:probe@2#0~b36b7a45906b661e","executor":null,"recorded_at":"2026-09-04T00:43:12Z"}}
{"entity":"nosuchkind","version":1,"id":"probe","revision":3,"type":"aep.entity.update/v1","from_state":"archived","to_state":"archived","changed":{"title":"Probe artifact created in error, kept as a record"},"args":{"changes":{"title":"Probe artifact created in error, kept as a record"},"command":"update-entity","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788483038233,"causation":"cmd-protocol-artifact-set-nosuchkind-probe-1788483038233","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"nosuchkind:probe@3#0~20b15531a1f5350e","executor":null,"recorded_at":"2026-09-04T00:50:38Z"}}
{"entity":"nosuchkind","version":1,"id":"probe","revision":4,"type":"aep.entity.update/v1","from_state":"archived","to_state":"archived","changed":{"body":"## What this is\n\nAn artifact created by mistake on 2026-09-04, kept because the store's journal is tamper-evident and\na deleted document is a defect `aep artifact validate` reports.\n\nWhile demonstrating an argument-order bug in the migration driver, the agent assumed\n`aep artifact new` would refuse an undeclared kind and used one as a supposedly inert probe. It is\nnot refused: a kind that declares no lifecycle permits every status and every move, so the probe\nbecame a real document. Removing it with `rm` was then caught:\n\n> nosuchkind:probe was deleted: its log ends at event nosuchkind:probe@1#0~9b631d3316fff614 and the\n> store holds no document — nothing is physically deleted through a command, so this was `rm`.\n\nThe document was never in version control, so it was recreated and retired through its ladder\ninstead. It is `archived` and appears in no active view.\n\n## Why it is not deleted\n\nNothing in the planning store is deleted. The lesson is worth more than the tidiness: the planning\nskill says never to create an artifact in order to look at one, and this is what happens when that\nrule is broken.\n"},"args":{"changes":{"body":"## What this is\n\nAn artifact created by mistake on 2026-09-04, kept because the store's journal is tamper-evident and\na deleted document is a defect `aep artifact validate` reports.\n\nWhile demonstrating an argument-order bug in the migration driver, the agent assumed\n`aep artifact new` would refuse an undeclared kind and used one as a supposedly inert probe. It is\nnot refused: a kind that declares no lifecycle permits every status and every move, so the probe\nbecame a real document. Removing it with `rm` was then caught:\n\n> nosuchkind:probe was deleted: its log ends at event nosuchkind:probe@1#0~9b631d3316fff614 and the\n> store holds no document — nothing is physically deleted through a command, so this was `rm`.\n\nThe document was never in version control, so it was recreated and retired through its ladder\ninstead. It is `archived` and appears in no active view.\n\n## Why it is not deleted\n\nNothing in the planning store is deleted. The lesson is worth more than the tidiness: the planning\nskill says never to create an artifact in order to look at one, and this is what happens when that\nrule is broken.\n"},"command":"update-entity","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788483038378,"causation":"cmd-protocol-artifact-body-nosuchkind-probe-1788483038378","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"nosuchkind:probe@4#0~f82f58db880cf619","executor":null,"recorded_at":"2026-09-04T00:50:38Z"}}
{"entity":"story","version":1,"id":"personal-local-workload-read","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `aep artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","title":"Personal-local Kubernetes serves a callable workload inventory"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `aep artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","status":"draft","title":"Personal-local Kubernetes serves a callable workload inventory"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/personal-local-workload-read"},"payload":{"actor":"human:timo","at":1788481843289,"causation":"cmd-new-story-personal-local-workload-read","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:personal-local-workload-read@1#0~20dcd9a4ca826b8e","executor":null,"recorded_at":"2026-09-04T00:30:43Z"}}
{"entity":"story","version":1,"id":"personal-local-workload-read","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"## Context\n\nPersonal-local Kubernetes publishes two operations and one datasource, and only the operations can\nbe called from this machine.\n\n| surface | personal-local | hosted |\n|---|---|---|\n| `kubernetes.deployment.status` | yes | yes |\n| `kubernetes.deployment.rollout-restart` | yes | yes |\n| `kubernetes.pod.logs` | no | yes |\n| `kubernetes.workloads` datasource | **served, not callable** | yes (MCP) |\n| `kubernetes.databases` datasource | no | yes |\n\n`KubernetesLocalBackend::capabilities` returns `datasources: true` and `owns_datasource` delegates\nto `WorkloadSurface::owns` (`crates/integration-kubernetes/src/local.rs`), so the daemon answers\n`b10x.connector-datasource.v0alpha1` for `kubernetes.workloads`. The `connectors` CLI has no\n`datasource` verb at all — its command tree is login, logout, mcp, admin, init, auth, providers,\ndoctor, serve, serve-hosted, connect, connection, event, operation, completions — so nothing an\noperator can run reaches it. The hosted placement reaches the same projection through MCP.\n\nThe gap that leaves: the only way to learn what runs in an activated cluster is to already know a\ndeployment's namespace and name and ask `kubernetes.deployment.status` for it one at a time. There\nis no answer to \"what is deployed here\", \"what image is this running\", or \"which namespaces are\nadmitted\" — which is the first question anything reading a cluster asks.\n\nFound on 2026-09-04 while proving that every authorized cluster serves reads (two EKS contexts\nactivated, both answered `kubernetes.deployment.status` for `kube-system/coredns` with distinct\ngenerations).\n\n## Acceptance\n\nAn operator with an activated Kubernetes Connection can list, from the `connectors` CLI, the\nnamespaces that Connection admits and, per namespace, each deployment with its container images,\ndesired and ready replica counts — without naming a deployment first. The reads go through the same\nadmission the workload operations already pass, are refused for a Connection that was never\nactivated, and every authorized Connection answers rather than one of them.\n\n## Scope\n\n- `crates/integration-kubernetes/src/local.rs` — the personal-local dispatch, whichever surface the\n  decision below picks.\n- `crates/integration-kubernetes/src/local_workloads.rs` — the projection already exists; what is\n  missing is a caller.\n- `crates/connectors-cli/src/lib.rs` — a verb, if the decision is to expose the datasource protocol\n  rather than to add operations.\n\n## Notes\n\nTwo shapes, and the choice is the first thing to settle:\n\n1. **A `connectors datasource` verb** — search, describe, bindings, read — mirroring `operation`.\n   It makes the whole `b10x.connector-datasource.v0alpha1` protocol reachable, so the hosted\n   `kubernetes.databases` projection would light up on personal-local for free the day it is\n   composed there. It is also a new public CLI surface with its own lease and paging vocabulary.\n2. **Catalogued operations** (`kubernetes.workload.list`, `kubernetes.namespace.list`) beside the\n   two that exist. Smaller, reuses the description-lease path a caller already knows, and needs no\n   new protocol verb — but it publishes a second, narrower answer to a question the datasource\n   already answers, and the two would drift.\n\nNot implemented here by instruction; this story records the gap and the fork.\n"},"args":{"changes":{"body":"## Context\n\nPersonal-local Kubernetes publishes two operations and one datasource, and only the operations can\nbe called from this machine.\n\n| surface | personal-local | hosted |\n|---|---|---|\n| `kubernetes.deployment.status` | yes | yes |\n| `kubernetes.deployment.rollout-restart` | yes | yes |\n| `kubernetes.pod.logs` | no | yes |\n| `kubernetes.workloads` datasource | **served, not callable** | yes (MCP) |\n| `kubernetes.databases` datasource | no | yes |\n\n`KubernetesLocalBackend::capabilities` returns `datasources: true` and `owns_datasource` delegates\nto `WorkloadSurface::owns` (`crates/integration-kubernetes/src/local.rs`), so the daemon answers\n`b10x.connector-datasource.v0alpha1` for `kubernetes.workloads`. The `connectors` CLI has no\n`datasource` verb at all — its command tree is login, logout, mcp, admin, init, auth, providers,\ndoctor, serve, serve-hosted, connect, connection, event, operation, completions — so nothing an\noperator can run reaches it. The hosted placement reaches the same projection through MCP.\n\nThe gap that leaves: the only way to learn what runs in an activated cluster is to already know a\ndeployment's namespace and name and ask `kubernetes.deployment.status` for it one at a time. There\nis no answer to \"what is deployed here\", \"what image is this running\", or \"which namespaces are\nadmitted\" — which is the first question anything reading a cluster asks.\n\nFound on 2026-09-04 while proving that every authorized cluster serves reads (two EKS contexts\nactivated, both answered `kubernetes.deployment.status` for `kube-system/coredns` with distinct\ngenerations).\n\n## Acceptance\n\nAn operator with an activated Kubernetes Connection can list, from the `connectors` CLI, the\nnamespaces that Connection admits and, per namespace, each deployment with its container images,\ndesired and ready replica counts — without naming a deployment first. The reads go through the same\nadmission the workload operations already pass, are refused for a Connection that was never\nactivated, and every authorized Connection answers rather than one of them.\n\n## Scope\n\n- `crates/integration-kubernetes/src/local.rs` — the personal-local dispatch, whichever surface the\n  decision below picks.\n- `crates/integration-kubernetes/src/local_workloads.rs` — the projection already exists; what is\n  missing is a caller.\n- `crates/connectors-cli/src/lib.rs` — a verb, if the decision is to expose the datasource protocol\n  rather than to add operations.\n\n## Notes\n\nTwo shapes, and the choice is the first thing to settle:\n\n1. **A `connectors datasource` verb** — search, describe, bindings, read — mirroring `operation`.\n   It makes the whole `b10x.connector-datasource.v0alpha1` protocol reachable, so the hosted\n   `kubernetes.databases` projection would light up on personal-local for free the day it is\n   composed there. It is also a new public CLI surface with its own lease and paging vocabulary.\n2. **Catalogued operations** (`kubernetes.workload.list`, `kubernetes.namespace.list`) beside the\n   two that exist. Smaller, reuses the description-lease path a caller already knows, and needs no\n   new protocol verb — but it publishes a second, narrower answer to a question the datasource\n   already answers, and the two would drift.\n\nNot implemented here by instruction; this story records the gap and the fork.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788481862767,"causation":"cmd-protocol-artifact-body-story-personal-local-workload-read-1788481862767","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:personal-local-workload-read@2#0~0f5d24f097e8f862","executor":null,"recorded_at":"2026-09-04T00:31:02Z"}}
{"entity":"story","version":1,"id":"one-placement-several-credentials","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: one placement, several credentials, a Connection per identity\n\n## Goal\nA personal-local `[[catalog]]` placement holds one credential and answers as one identity\n(`CatalogIntegrationConfig.credential`, `crates/connectors-config/src/personal.rs`; the Connection\nreference is derived from provider + instance). A Slack app is two identities at once: its bot\ntoken posts as the app, the operator's user token (issued to the same app) reads what the operator\ncan read. Today that needs two placements with two instance names (`timo-ai` for the bot token,\n`timo-ai-user` for the user token), and nothing records that they are one app — the keyring already\nholds five credential slots per instance, so the storage knows, the placement model does not.\n\n## Shape\n- A placement may declare `credentials = [\"slack.bot_token\", \"slack.user_token\"]` (or keep the\n  single `credential`); each declared credential yields its own Connection,\n  `connection:<provider>:<instance>` for the first and `connection:<provider>:<instance>/<leaf>` for\n  the others, all labelled from the one placement and reported together by `connection list` with\n  the credential each answers as.\n- `connectors connect <provider> --instance <i> --as <credential>` adds a credential to an existing\n  placement instead of refusing or creating a second one; `auth status` lists which of the\n  placement's Connections are callable.\n- Operation discovery lists a Connection only for the mechanisms its credential satisfies, so a\n  read-only user Connection never advertises a write the bot alone may do.\n- Documentation: `docs/guides/connect-slack.md` gains the two-identity setup.\n\n## Acceptance\n- One `[[catalog]]` entry with two credentials produces two callable Slack Connections; reads on the\n  user one succeed on a public channel the bot is not in; `slack-chat-post-message` is offered on\n  the bot one only.\n- An adopter's configuration that names one credential is unchanged in behaviour.\n","title":"One placement, several credentials, a Connection per identity"},"args":{"command":"create-entity","data":{"body":"# Story: one placement, several credentials, a Connection per identity\n\n## Goal\nA personal-local `[[catalog]]` placement holds one credential and answers as one identity\n(`CatalogIntegrationConfig.credential`, `crates/connectors-config/src/personal.rs`; the Connection\nreference is derived from provider + instance). A Slack app is two identities at once: its bot\ntoken posts as the app, the operator's user token (issued to the same app) reads what the operator\ncan read. Today that needs two placements with two instance names (`timo-ai` for the bot token,\n`timo-ai-user` for the user token), and nothing records that they are one app — the keyring already\nholds five credential slots per instance, so the storage knows, the placement model does not.\n\n## Shape\n- A placement may declare `credentials = [\"slack.bot_token\", \"slack.user_token\"]` (or keep the\n  single `credential`); each declared credential yields its own Connection,\n  `connection:<provider>:<instance>` for the first and `connection:<provider>:<instance>/<leaf>` for\n  the others, all labelled from the one placement and reported together by `connection list` with\n  the credential each answers as.\n- `connectors connect <provider> --instance <i> --as <credential>` adds a credential to an existing\n  placement instead of refusing or creating a second one; `auth status` lists which of the\n  placement's Connections are callable.\n- Operation discovery lists a Connection only for the mechanisms its credential satisfies, so a\n  read-only user Connection never advertises a write the bot alone may do.\n- Documentation: `docs/guides/connect-slack.md` gains the two-identity setup.\n\n## Acceptance\n- One `[[catalog]]` entry with two credentials produces two callable Slack Connections; reads on the\n  user one succeed on a public channel the bot is not in; `slack-chat-post-message` is offered on\n  the bot one only.\n- An adopter's configuration that names one credential is unchanged in behaviour.\n","status":"draft","title":"One placement, several credentials, a Connection per identity"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/one-placement-several-credentials"},"payload":{"actor":"agent:org-brain","at":1788483414222,"causation":"cmd-new-story-one-placement-several-credentials","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:one-placement-several-credentials@1#0~283111a02460f3ac","executor":null,"recorded_at":"2026-09-04T00:56:54Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000169","to":"proposed"},"payload":{"actor":"human:timo","at":1788484317945,"causation":"cmd-move-story-informative-command-readability-1788484317945","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:informative-command-readability@3#0~c5a8d02863208fe2","executor":null,"recorded_at":"2026-09-04T01:11:57Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000169","to":"active"},"payload":{"actor":"human:timo","at":1788484318134,"causation":"cmd-move-story-informative-command-readability-1788484318134","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:informative-command-readability@4#0~3ac5b842fd312526","executor":null,"recorded_at":"2026-09-04T01:11:58Z"}}
{"entity":"story","version":1,"id":"one-shot-operations-without-a-daemon","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: one-shot operations without a daemon\n\n## Goal\n`connectors operation describe|invoke --config <file> --state-root <dir>` is a client to the personal\ndaemon's control socket (`crates/connectors-cli/src/lib.rs`, `LocalClient::new(state_root.join(\n\"connectors.sock\"))`); with no daemon it fails `connector-unreachable: local Connector transport\nfailed: No such file or directory`. The runtime that owns the keyring handles, the egress rules, the\ndescription leases and the audit trail is composed only by `connectors serve` (`PersonalRuntime::bind`).\nA caller that is itself a short-lived program — a sync round started by a timer, a CI job, an agent\nverb such as `brain acquire` — therefore needs a long-running process beside it before it can read\none page, and `doctor`'s \"one-shot commands work\" reads as if it did not.\n\n## Shape\nEither of two, decided in review:\n- **Ephemeral runtime.** When the socket is absent and `--config`/`--state-root` are given, the\n  `operation` and `connection` verbs compose the personal runtime in-process for the duration of\n  the command (same code path as `serve`, no socket published, leases valid within the process),\n  then release the state root. Events still need `serve`; `doctor` says so.\n- **`connectors serve --until-idle <secs>`.** A verb spawns the daemon when the socket is absent and\n  it exits after a quiet period; concurrent callers share it. Fewer code paths, one more process.\n\nIn both, credentials never leave the connectors process; a caller still sees only the protocol.\n`doctor`'s wording changes to name which verbs need the daemon.\n\n## Acceptance\n- With no daemon, `operation describe` and `invoke` with `--config`/`--state-root` succeed against\n  a fixture provider; the state root holds no socket afterwards.\n- With a daemon running, behaviour is unchanged (the socket is used).\n- `event receive` without a daemon is refused naming `connectors serve`.\n- `connectors doctor` names the verbs that need the daemon.\n","title":"One-shot operations without a daemon"},"args":{"command":"create-entity","data":{"body":"# Story: one-shot operations without a daemon\n\n## Goal\n`connectors operation describe|invoke --config <file> --state-root <dir>` is a client to the personal\ndaemon's control socket (`crates/connectors-cli/src/lib.rs`, `LocalClient::new(state_root.join(\n\"connectors.sock\"))`); with no daemon it fails `connector-unreachable: local Connector transport\nfailed: No such file or directory`. The runtime that owns the keyring handles, the egress rules, the\ndescription leases and the audit trail is composed only by `connectors serve` (`PersonalRuntime::bind`).\nA caller that is itself a short-lived program — a sync round started by a timer, a CI job, an agent\nverb such as `brain acquire` — therefore needs a long-running process beside it before it can read\none page, and `doctor`'s \"one-shot commands work\" reads as if it did not.\n\n## Shape\nEither of two, decided in review:\n- **Ephemeral runtime.** When the socket is absent and `--config`/`--state-root` are given, the\n  `operation` and `connection` verbs compose the personal runtime in-process for the duration of\n  the command (same code path as `serve`, no socket published, leases valid within the process),\n  then release the state root. Events still need `serve`; `doctor` says so.\n- **`connectors serve --until-idle <secs>`.** A verb spawns the daemon when the socket is absent and\n  it exits after a quiet period; concurrent callers share it. Fewer code paths, one more process.\n\nIn both, credentials never leave the connectors process; a caller still sees only the protocol.\n`doctor`'s wording changes to name which verbs need the daemon.\n\n## Acceptance\n- With no daemon, `operation describe` and `invoke` with `--config`/`--state-root` succeed against\n  a fixture provider; the state root holds no socket afterwards.\n- With a daemon running, behaviour is unchanged (the socket is used).\n- `event receive` without a daemon is refused naming `connectors serve`.\n- `connectors doctor` names the verbs that need the daemon.\n","status":"draft","title":"One-shot operations without a daemon"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/one-shot-operations-without-a-daemon"},"payload":{"actor":"agent:org-brain","at":1788484565693,"causation":"cmd-new-story-one-shot-operations-without-a-daemon","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:one-shot-operations-without-a-daemon@1#0~38648da4394349d4","executor":null,"recorded_at":"2026-09-04T01:16:05Z"}}
{"entity":"story","version":1,"id":"console-clippy-findings","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Context\n\n`cargo clippy -p connectors-console --all-targets --locked -- -D warnings` exits 101 on five\nfindings, none of them in code any current story touches:\n\n| file | lint | count |\n|---|---|---|\n| `crates/connectors-console/src/enrol.rs` | `write_with_newline` | 3 |\n| `crates/connectors-console/src/enrol.rs` | `items_after_test_module` | 1 |\n| `crates/connectors-console/src/envelope.rs` | `unused_imports` | 1 |\n\nFound by the `adp:implementor` agent on the CLI output readability unit, 2026-09-04, and reported\nas `pre-existing` rather than introduced. It verified a fix applies clean and leaves the package\ngreen (clippy exit 0, 55 tests passed), then reverted it because the files were not its to change.\nThe patch is at\n`~/.cache/connectors-wave/informative-command-readability/scratch/pre-existing-clippy.patch`; it is\noutside the repository and will not survive a cache clear.\n\nThese went unseen because `crates/connectors-console` was in no gate lane. That is being fixed\nseparately, in the wave that found it, which is what makes these five visible from now on.\n\n## Acceptance\n\n`cargo clippy -p connectors-console --all-targets --locked -- -D warnings` exits 0, and the package's\ntests still pass.\n\n## Notes\n\nThe `write_with_newline` fix rewrites roughly 150 lines of `enrol.rs`, so it is a reformat rather\nthan a two-line change. That size is the reason it was not folded into the unit that found it.\n","summary":"Three write_with_newline, one items_after_test_module and one unused_imports, invisible until the package gained a gate lane.","title":"connectors-console fails clippy on five pre-existing findings"},"args":{"command":"create-entity","data":{"body":"## Context\n\n`cargo clippy -p connectors-console --all-targets --locked -- -D warnings` exits 101 on five\nfindings, none of them in code any current story touches:\n\n| file | lint | count |\n|---|---|---|\n| `crates/connectors-console/src/enrol.rs` | `write_with_newline` | 3 |\n| `crates/connectors-console/src/enrol.rs` | `items_after_test_module` | 1 |\n| `crates/connectors-console/src/envelope.rs` | `unused_imports` | 1 |\n\nFound by the `adp:implementor` agent on the CLI output readability unit, 2026-09-04, and reported\nas `pre-existing` rather than introduced. It verified a fix applies clean and leaves the package\ngreen (clippy exit 0, 55 tests passed), then reverted it because the files were not its to change.\nThe patch is at\n`~/.cache/connectors-wave/informative-command-readability/scratch/pre-existing-clippy.patch`; it is\noutside the repository and will not survive a cache clear.\n\nThese went unseen because `crates/connectors-console` was in no gate lane. That is being fixed\nseparately, in the wave that found it, which is what makes these five visible from now on.\n\n## Acceptance\n\n`cargo clippy -p connectors-console --all-targets --locked -- -D warnings` exits 0, and the package's\ntests still pass.\n\n## Notes\n\nThe `write_with_newline` fix rewrites roughly 150 lines of `enrol.rs`, so it is a reformat rather\nthan a two-line change. That size is the reason it was not folded into the unit that found it.\n","status":"draft","summary":"Three write_with_newline, one items_after_test_module and one unused_imports, invisible until the package gained a gate lane.","title":"connectors-console fails clippy on five pre-existing findings"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/console-clippy-findings"},"payload":{"actor":"human:timo","at":1788485702682,"causation":"cmd-new-story-console-clippy-findings","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:console-clippy-findings@1#0~8393dab432c8675c","executor":null,"recorded_at":"2026-09-04T01:35:02Z"}}
{"entity":"story","version":1,"id":"console-clippy-findings","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/enrol.rs"},{"confidence":"cited","path":"crates/connectors-console/src/envelope.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/enrol.rs"},{"confidence":"cited","path":"crates/connectors-console/src/envelope.rs"}]},"command":"update-entity","target":"01MEM0000000000000130"},"payload":{"actor":"human:timo","at":1788485702806,"causation":"cmd-protocol-artifact-scope-story-console-clippy-findings-1788485702806","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:console-clippy-findings@2#0~53f3794dbb44b25b","executor":null,"recorded_at":"2026-09-04T01:35:02Z"}}
{"entity":"story","version":1,"id":"explicit-target-never-implicit","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: a command names its target; nothing infers it from a stored login\n\n## Defect\n`connectors operation …`, `connectors connection …` and `connectors event …` decide where they go\nby the absence of two flags: when neither `--config` nor `--state-root` is given and\n`~/.local/state/b10x/connectors/identity-sessions.json` holds a login, the request goes to that\nhosted deployment (`crates/connectors-cli/src/lib.rs`, the guard\n`if config_path.is_none() && state_root.is_none() { AuthenticatedHostedClient::active() … }` at\nthe three call sites, ~L729, ~L789, ~L871). Otherwise it goes to the personal daemon's socket. The\nsame command line therefore reaches two different systems depending on a file the operator did\nnot name, and `connectors login` run once changes the target of every later `operation invoke`\non the machine — including the payloads a script passes as input. Measured 2026-09-04 with\n`strace -e trace=connect`: `operation search --query slack` with no flags connected to\n`devcenter.dev.babelforce.com:443`; with `--config`/`--state-root` to the local socket; the daemon\nwas never consulted on the first route. No other verb has this fallback: `mcp`, `admin`,\n`serve-hosted`, `login`, `logout` are hosted by name; `connect`, `auth`, `providers`, `doctor` are\nlocal by construction. The pattern exists in connectors only; `zwirn` and the platform agent use\nthe hosted client explicitly.\n\n## Shape\n- A target is always explicit: `--target local|hosted` on the three verbs, or a named context\n  (`connectors context use <name>`, stored per machine, printed by `doctor` and in every result\n  envelope as `target:`). No target and both a login and a local state root present → refused\n  naming both, never a guess.\n- The default, when nothing is declared, is **local**. A hosted target is chosen, not fallen into.\n- `--config`/`--state-root` stop implying the target; they only say which local configuration and\n  which socket, and are refused together with `--target hosted`.\n- `connectors login` prints which contexts now exist and that no command changed target.\n- `doctor` gains a `target` check: which target the next `operation` would reach and why.\n\n## Acceptance\n- With a login present and no flags, `operation search` reaches the local socket (or refuses when\n  there is none) and never a remote; `strace -e trace=connect` shows no network `connect()`.\n- `--target hosted` reaches the login's deployment; without a login it is refused by name.\n- Every result envelope names the target it came from.\n- The old implicit route is gone: a test asserts the guard no longer exists.\n","title":"A command names its target; nothing infers it from a stored login"},"args":{"command":"create-entity","data":{"body":"# Story: a command names its target; nothing infers it from a stored login\n\n## Defect\n`connectors operation …`, `connectors connection …` and `connectors event …` decide where they go\nby the absence of two flags: when neither `--config` nor `--state-root` is given and\n`~/.local/state/b10x/connectors/identity-sessions.json` holds a login, the request goes to that\nhosted deployment (`crates/connectors-cli/src/lib.rs`, the guard\n`if config_path.is_none() && state_root.is_none() { AuthenticatedHostedClient::active() … }` at\nthe three call sites, ~L729, ~L789, ~L871). Otherwise it goes to the personal daemon's socket. The\nsame command line therefore reaches two different systems depending on a file the operator did\nnot name, and `connectors login` run once changes the target of every later `operation invoke`\non the machine — including the payloads a script passes as input. Measured 2026-09-04 with\n`strace -e trace=connect`: `operation search --query slack` with no flags connected to\n`devcenter.dev.babelforce.com:443`; with `--config`/`--state-root` to the local socket; the daemon\nwas never consulted on the first route. No other verb has this fallback: `mcp`, `admin`,\n`serve-hosted`, `login`, `logout` are hosted by name; `connect`, `auth`, `providers`, `doctor` are\nlocal by construction. The pattern exists in connectors only; `zwirn` and the platform agent use\nthe hosted client explicitly.\n\n## Shape\n- A target is always explicit: `--target local|hosted` on the three verbs, or a named context\n  (`connectors context use <name>`, stored per machine, printed by `doctor` and in every result\n  envelope as `target:`). No target and both a login and a local state root present → refused\n  naming both, never a guess.\n- The default, when nothing is declared, is **local**. A hosted target is chosen, not fallen into.\n- `--config`/`--state-root` stop implying the target; they only say which local configuration and\n  which socket, and are refused together with `--target hosted`.\n- `connectors login` prints which contexts now exist and that no command changed target.\n- `doctor` gains a `target` check: which target the next `operation` would reach and why.\n\n## Acceptance\n- With a login present and no flags, `operation search` reaches the local socket (or refuses when\n  there is none) and never a remote; `strace -e trace=connect` shows no network `connect()`.\n- `--target hosted` reaches the login's deployment; without a login it is refused by name.\n- Every result envelope names the target it came from.\n- The old implicit route is gone: a test asserts the guard no longer exists.\n","status":"draft","title":"A command names its target; nothing infers it from a stored login"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/explicit-target-never-implicit"},"payload":{"actor":"agent:org-brain","at":1788485780026,"causation":"cmd-new-story-explicit-target-never-implicit","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:explicit-target-never-implicit@1#0~b2a1b5dfa6031741","executor":null,"recorded_at":"2026-09-04T01:36:20Z"}}
{"entity":"story","version":1,"id":"rate-limit-in-the-protocol","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: a provider's rate limit is a protocol fact, not a sentence\n\n## Defect\n`OperationErrorCode` has no rate-limit variant and the response envelope has no `retry_after`;\na 429 from Slack surfaces as `unavailable` with the text \"the provider refused the request with\nHTTP 429 — the provider is rate-limiting this credential\". A caller that must pace itself (the\norg-brain ledger walked 530 pages in one run and hit 51 such refusals) has to match the wording.\n\n## Shape\n- `OperationErrorCode::RateLimited` with `retry_after_seconds: Option<u64>` taken from the\n  provider's `Retry-After` header when present.\n- Catalogue operations may declare their provider's rate tier so `describe` exposes a suggested\n  interval; the personal runtime may pace requests per connection to that interval.\n\n## Acceptance\n- A 429 with `Retry-After: 30` is returned as `rate_limited` with `retry_after_seconds: 30`.\n- `slack-conversations-history` `describe` names the interval Slack's tier implies.\n","title":"A provider's rate limit is a protocol fact, not a sentence"},"args":{"command":"create-entity","data":{"body":"# Story: a provider's rate limit is a protocol fact, not a sentence\n\n## Defect\n`OperationErrorCode` has no rate-limit variant and the response envelope has no `retry_after`;\na 429 from Slack surfaces as `unavailable` with the text \"the provider refused the request with\nHTTP 429 — the provider is rate-limiting this credential\". A caller that must pace itself (the\norg-brain ledger walked 530 pages in one run and hit 51 such refusals) has to match the wording.\n\n## Shape\n- `OperationErrorCode::RateLimited` with `retry_after_seconds: Option<u64>` taken from the\n  provider's `Retry-After` header when present.\n- Catalogue operations may declare their provider's rate tier so `describe` exposes a suggested\n  interval; the personal runtime may pace requests per connection to that interval.\n\n## Acceptance\n- A 429 with `Retry-After: 30` is returned as `rate_limited` with `retry_after_seconds: 30`.\n- `slack-conversations-history` `describe` names the interval Slack's tier implies.\n","status":"draft","title":"A provider's rate limit is a protocol fact, not a sentence"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/rate-limit-in-the-protocol"},"payload":{"actor":"agent:org-brain","at":1788485780163,"causation":"cmd-new-story-rate-limit-in-the-protocol","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:rate-limit-in-the-protocol@1#0~540d916c62fc218d","executor":null,"recorded_at":"2026-09-04T01:36:20Z"}}
{"entity":"review-result","version":1,"id":"adversary-informative-readability-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary, pass 1 — informative-command-readability\n\n```\nunit: informative-command-readability\nverdict: red\ncases: executed 55->62, red 6\norigin: introduced 9, pre-existing 1, undecided 0\nwrote-outside-worktree: .../scratch/{probe/,mutant/,full-mutant/,emit.err}\nneeds-coordinator: no\n```\n\nWorktree `wt-d41dd2700a2e`, `impl/informative-command-readability` @ `7daeb22`, base `7dc4c07`.\nAdded `crates/connectors-console/tests/adversary_readability.rs`, 302 lines, 6 red cases plus one\ngreen helper. No implementation file was edited.\n\n**The findings block below is the adversary's, with one mechanical change: each `message` is written\nas a YAML block scalar.** As returned, four messages began with a backtick, which YAML forbids at\nthe start of a plain scalar, and `aep artifact new` refused the record. No wording was altered.\n\n## Suite\n\n```\nrunning 55 tests\ntest result: ok. 55 passed; 0 failed\n     Running tests/adversary_readability.rs\nrunning 7 tests\ntest result: FAILED. 1 passed; 6 failed\nfailures:\n    a_record_whose_cells_are_all_empty_is_rendered_as_a_blank_line\n    a_wide_character_cell_leaves_the_column_after_it_ragged\n    an_unranked_table_lets_a_cell_sit_where_the_severity_marker_sits\n    compact_no_longer_puts_one_record_on_every_line\n    doctor_spreads_one_check_over_several_unmarked_lines_when_the_configuration_is_malformed\n    providers_starts_its_last_column_past_the_width_of_any_terminal\nexit status 101\n```\n\n## Attacked and could not break\n\n- `-o json` / `-o yaml` bytes: 10 values rendered through base and HEAD side by side, **0 byte\n  differences**. The unit's central claim holds.\n- Severity through a pipe: no escape sequence, no TTY detection, no colour; markers are ASCII.\n- `compact` dropping a field: no value found at any nesting depth whose field vanishes.\n- `payload` unwrapping, mixed and degenerate arrays, ragged records, `unwrap_single_array` with two\n  arrays, and the index arithmetic in the column move all held.\n\n## Fixes the adversary named, and did not apply\n\n1. fold a newline in `cell`/`inline` to a visible escape, as empty is folded to `(none)`\n2. cap or elide columns in `providers`, or give the table a width budget\n3. update `Format::Compact`'s doc, or prefix the summary line so a consumer can skip it\n4. measure width with a display-width function\n5. always emit the marker column, blank when unranked\n8. rank `not-callable` `Warn`\n\n## Findings\n\n```findings\n- file: crates/connectors-console/src/output.rs\n  line: 389\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    a newline inside a cell is written verbatim, so `connectors doctor` over a malformed TOML config renders one check as six lines with the toml caret diagram sitting in the marker column.\n- file: crates/connectors-console/src/output.rs\n  line: 322\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    moving the widest column last cannot stop `connectors providers` wrapping, because the eleven columns before it already start the last column at terminal column 196 and the widest row is 314.\n- file: crates/connectors-console/src/output.rs\n  line: 45\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    `Format::Compact` still documents \"One record per line, for `grep` and `wc -l`\" while `providers` now emits a leading summary line that no reader or script can tell from a record, moving `wc -l` from 65 to 66.\n- file: crates/connectors-console/src/output.rs\n  line: 311\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    column widths are counted in `char`s rather than terminal columns, so one wide character in a cell displaces every column after it by one on that row alone.\n- file: crates/connectors-console/src/output.rs\n  line: 335\n  category: acceptance\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    a table with no ranked record omits the marker column entirely, so a cell beginning `x ` occupies the exact position the reader has been taught means a failed row.\n- file: crates/connectors-console/src/output.rs\n  line: 371\n  category: boundary\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    `write_row` trims the finished line, so a record whose cells are all empty strings renders as a blank line with neither keys nor the `-` sentinel; no command was found that emits one.\n- file: crates/connectors-console/src/output.rs\n  line: 322\n  category: mutant\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    four mutants survive all 55 tests - deleting the whole widest-column-move block, dropping `line.trim_end()`, making `strictly_widest` ignore ties, and replacing the `-` for an absent key with an empty cell.\n- file: crates/connectors-console/src/output.rs\n  line: 490\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    `not-callable` ranks as a failure while the same row's `absent` credentials rank as a warning, so a fresh install marks every configured provider `x` under the module's own reasoning for the opposite.\n- file: crates/connectors-console/src/output.rs\n  line: 853\n  category: judgement\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    the severity-word scan reads only this package's own `src/` for same-line literals, not the wire vocabulary the doc claims it guards (`AdminCredentialState::Present` ranks Unknown), though no marker column reaches that word today.\n- file: crates/connectors-cli/src/lib.rs\n  line: 459\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: pre-existing\n  message: >-\n    `emit` returns Err(BrokenPipe) rather than treating a closed pipe as success, and only `completions` maps BrokenPipe to `Ok(())`, so `connectors providers | head -1` exits non-zero once the output exceeds the pipe buffer.\n```\n","title":"Adversary, pass 1 — informative-command-readability"},"args":{"command":"create-entity","data":{"body":"# Adversary, pass 1 — informative-command-readability\n\n```\nunit: informative-command-readability\nverdict: red\ncases: executed 55->62, red 6\norigin: introduced 9, pre-existing 1, undecided 0\nwrote-outside-worktree: .../scratch/{probe/,mutant/,full-mutant/,emit.err}\nneeds-coordinator: no\n```\n\nWorktree `wt-d41dd2700a2e`, `impl/informative-command-readability` @ `7daeb22`, base `7dc4c07`.\nAdded `crates/connectors-console/tests/adversary_readability.rs`, 302 lines, 6 red cases plus one\ngreen helper. No implementation file was edited.\n\n**The findings block below is the adversary's, with one mechanical change: each `message` is written\nas a YAML block scalar.** As returned, four messages began with a backtick, which YAML forbids at\nthe start of a plain scalar, and `aep artifact new` refused the record. No wording was altered.\n\n## Suite\n\n```\nrunning 55 tests\ntest result: ok. 55 passed; 0 failed\n     Running tests/adversary_readability.rs\nrunning 7 tests\ntest result: FAILED. 1 passed; 6 failed\nfailures:\n    a_record_whose_cells_are_all_empty_is_rendered_as_a_blank_line\n    a_wide_character_cell_leaves_the_column_after_it_ragged\n    an_unranked_table_lets_a_cell_sit_where_the_severity_marker_sits\n    compact_no_longer_puts_one_record_on_every_line\n    doctor_spreads_one_check_over_several_unmarked_lines_when_the_configuration_is_malformed\n    providers_starts_its_last_column_past_the_width_of_any_terminal\nexit status 101\n```\n\n## Attacked and could not break\n\n- `-o json` / `-o yaml` bytes: 10 values rendered through base and HEAD side by side, **0 byte\n  differences**. The unit's central claim holds.\n- Severity through a pipe: no escape sequence, no TTY detection, no colour; markers are ASCII.\n- `compact` dropping a field: no value found at any nesting depth whose field vanishes.\n- `payload` unwrapping, mixed and degenerate arrays, ragged records, `unwrap_single_array` with two\n  arrays, and the index arithmetic in the column move all held.\n\n## Fixes the adversary named, and did not apply\n\n1. fold a newline in `cell`/`inline` to a visible escape, as empty is folded to `(none)`\n2. cap or elide columns in `providers`, or give the table a width budget\n3. update `Format::Compact`'s doc, or prefix the summary line so a consumer can skip it\n4. measure width with a display-width function\n5. always emit the marker column, blank when unranked\n8. rank `not-callable` `Warn`\n\n## Findings\n\n```findings\n- file: crates/connectors-console/src/output.rs\n  line: 389\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    a newline inside a cell is written verbatim, so `connectors doctor` over a malformed TOML config renders one check as six lines with the toml caret diagram sitting in the marker column.\n- file: crates/connectors-console/src/output.rs\n  line: 322\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    moving the widest column last cannot stop `connectors providers` wrapping, because the eleven columns before it already start the last column at terminal column 196 and the widest row is 314.\n- file: crates/connectors-console/src/output.rs\n  line: 45\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    `Format::Compact` still documents \"One record per line, for `grep` and `wc -l`\" while `providers` now emits a leading summary line that no reader or script can tell from a record, moving `wc -l` from 65 to 66.\n- file: crates/connectors-console/src/output.rs\n  line: 311\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    column widths are counted in `char`s rather than terminal columns, so one wide character in a cell displaces every column after it by one on that row alone.\n- file: crates/connectors-console/src/output.rs\n  line: 335\n  category: acceptance\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    a table with no ranked record omits the marker column entirely, so a cell beginning `x ` occupies the exact position the reader has been taught means a failed row.\n- file: crates/connectors-console/src/output.rs\n  line: 371\n  category: boundary\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    `write_row` trims the finished line, so a record whose cells are all empty strings renders as a blank line with neither keys nor the `-` sentinel; no command was found that emits one.\n- file: crates/connectors-console/src/output.rs\n  line: 322\n  category: mutant\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    four mutants survive all 55 tests - deleting the whole widest-column-move block, dropping `line.trim_end()`, making `strictly_widest` ignore ties, and replacing the `-` for an absent key with an empty cell.\n- file: crates/connectors-console/src/output.rs\n  line: 490\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    `not-callable` ranks as a failure while the same row's `absent` credentials rank as a warning, so a fresh install marks every configured provider `x` under the module's own reasoning for the opposite.\n- file: crates/connectors-console/src/output.rs\n  line: 853\n  category: judgement\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    the severity-word scan reads only this package's own `src/` for same-line literals, not the wire vocabulary the doc claims it guards (`AdminCredentialState::Present` ranks Unknown), though no marker column reaches that word today.\n- file: crates/connectors-cli/src/lib.rs\n  line: 459\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: pre-existing\n  message: >-\n    `emit` returns Err(BrokenPipe) rather than treating a closed pipe as success, and only `completions` maps BrokenPipe to `Ok(())`, so `connectors providers | head -1` exits non-zero once the output exceeds the pipe buffer.\n```\n","status":"active","title":"Adversary, pass 1 — informative-command-readability"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-informative-readability-pass-1"},"payload":{"actor":"human:timo","at":1788486990607,"causation":"cmd-new-review-result-adversary-informative-readability-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-informative-readability-pass-1@1#0~b43c9264fdeb799a","executor":null,"recorded_at":"2026-09-04T01:56:30Z"}}
{"entity":"review-result","version":1,"id":"adversary-informative-readability-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:informative-command-readability"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000487","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788486990771,"causation":"cmd-review-result-adversary-informative-readability-pass-1-reviews-story-informative-command-readability","change":{"change":"related","relation":"reviews","target":"story:informative-command-readability"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-informative-readability-pass-1@1#0~ca8675f7f8ae6e03","executor":null,"recorded_at":"2026-09-04T01:56:30Z"}}
{"entity":"story","version":1,"id":"emit-treats-a-closed-pipe-as-failure","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Context\n\n`connectors providers | head -1` exits non-zero once the output exceeds the 64 KiB pipe buffer.\n\nObserved at runtime by the `adp:adversary` agent on 2026-09-04, not inferred:\n`emit(Format::Text, providers::run(\"\"))` into a reader that exits immediately returns\n`Err(Io(Os { code: 32, kind: BrokenPipe }))`. It neither panics nor succeeds.\n`MainError::Output` at `crates/connectors-cli/src/lib.rs:385` then falls through to `emit_error`\nand `ExitCode::FAILURE` at `:444`. The only place a closed pipe is mapped to `Ok(())` is\n`Command::Completions` at `:459`, added the same day for the completions script.\n\nMarked `pre-existing`: `emit` and the CLI's error handling are byte-identical to the base commit\nthe readability unit forked from.\n\n## Acceptance\n\nA reader that closes the pipe early — `connectors providers | head -1` above the pipe buffer —\nleaves `connectors` exiting 0, in every output format, and a genuine write failure still exits\nnon-zero.\n\n## Notes\n\nReachability moved in the opposite direction this week. The CLI output readability work shrank\n`providers` text output from roughly 40 KiB to roughly 14 KiB, which is below the pipe buffer, so\nthe defect became harder to hit rather than easier. It is still wrong: `head`, `less` and a reader\nthat stops early are ordinary, and the output grows with the catalogue.\n","summary":"emit returns Err(BrokenPipe) and only the completions subcommand maps it to success, so piping into head fails above the pipe buffer.","title":"A reader that closes the pipe early makes connectors exit non-zero"},"args":{"command":"create-entity","data":{"body":"## Context\n\n`connectors providers | head -1` exits non-zero once the output exceeds the 64 KiB pipe buffer.\n\nObserved at runtime by the `adp:adversary` agent on 2026-09-04, not inferred:\n`emit(Format::Text, providers::run(\"\"))` into a reader that exits immediately returns\n`Err(Io(Os { code: 32, kind: BrokenPipe }))`. It neither panics nor succeeds.\n`MainError::Output` at `crates/connectors-cli/src/lib.rs:385` then falls through to `emit_error`\nand `ExitCode::FAILURE` at `:444`. The only place a closed pipe is mapped to `Ok(())` is\n`Command::Completions` at `:459`, added the same day for the completions script.\n\nMarked `pre-existing`: `emit` and the CLI's error handling are byte-identical to the base commit\nthe readability unit forked from.\n\n## Acceptance\n\nA reader that closes the pipe early — `connectors providers | head -1` above the pipe buffer —\nleaves `connectors` exiting 0, in every output format, and a genuine write failure still exits\nnon-zero.\n\n## Notes\n\nReachability moved in the opposite direction this week. The CLI output readability work shrank\n`providers` text output from roughly 40 KiB to roughly 14 KiB, which is below the pipe buffer, so\nthe defect became harder to hit rather than easier. It is still wrong: `head`, `less` and a reader\nthat stops early are ordinary, and the output grows with the catalogue.\n","status":"draft","summary":"emit returns Err(BrokenPipe) and only the completions subcommand maps it to success, so piping into head fails above the pipe buffer.","title":"A reader that closes the pipe early makes connectors exit non-zero"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/emit-treats-a-closed-pipe-as-failure"},"payload":{"actor":"human:timo","at":1788487029786,"causation":"cmd-new-story-emit-treats-a-closed-pipe-as-failure","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:emit-treats-a-closed-pipe-as-failure@1#0~862d7d45c7e5f7fc","executor":null,"recorded_at":"2026-09-04T01:57:09Z"}}
{"entity":"story","version":1,"id":"emit-treats-a-closed-pipe-as-failure","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"}]},"command":"update-entity","target":"01MEM0000000000000163"},"payload":{"actor":"human:timo","at":1788487029910,"causation":"cmd-protocol-artifact-scope-story-emit-treats-a-closed-pipe-as-failure-1788487029910","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:emit-treats-a-closed-pipe-as-failure@2#0~8af9d9afa145fd24","executor":null,"recorded_at":"2026-09-04T01:57:09Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"escalated","review":"review-result:adversary-informative-readability-pass-1","source":"review-result:adversary-informative-readability-pass-1","target":"01MEM0000000000000178"},"payload":{"actor":"human:timo","at":1788487030000,"causation":"cmd-evidence-story-informative-command-readability-review_outcome-1788487030036","change":{"change":"evidence","kind":"review_outcome","outcome":"escalated","review":"review-result:adversary-informative-readability-pass-1","source":"review-result:adversary-informative-readability-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:informative-command-readability@4#0~7737b874615581f6","executor":null,"recorded_at":"2026-09-04T01:57:10Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"no-op","review":"review-result:adversary-informative-readability-pass-1","source":"review-result:adversary-informative-readability-pass-1","target":"01MEM0000000000000178"},"payload":{"actor":"human:timo","at":1788487047000,"causation":"cmd-evidence-story-informative-command-readability-review_outcome-1788487047769","change":{"change":"evidence","kind":"review_outcome","outcome":"no-op","review":"review-result:adversary-informative-readability-pass-1","source":"review-result:adversary-informative-readability-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:informative-command-readability@4#0~008e5863254e18d9","executor":null,"recorded_at":"2026-09-04T01:57:27Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-informative-readability-pass-1","source":"review-result:adversary-informative-readability-pass-1","target":"01MEM0000000000000178"},"payload":{"actor":"human:timo","at":1788488617000,"causation":"cmd-evidence-story-informative-command-readability-review_outcome-1788488617995","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-informative-readability-pass-1","source":"review-result:adversary-informative-readability-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:informative-command-readability@4#0~963456d5d085a05c","executor":null,"recorded_at":"2026-09-04T02:23:37Z"}}
{"entity":"review-result","version":1,"id":"adversary-informative-readability-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary, pass 2 — informative-command-readability\n\n```\nunit: informative-command-readability\nverdict: red\ncases: executed 72->77, red 4\norigin: introduced 6, pre-existing 2, undecided 0\nwrote-outside-worktree: .../scratch/{adv2probe/,adv2probe-base/,adv2-*,x.json,y.json,z.json}\nneeds-coordinator: yes\n```\n\nAttacking `06e1e22`, base `7dc4c07c`. Added\n`crates/connectors-console/tests/adversary_readability_pass2.rs`, 4 red cases plus one helper. No\nimplementation file was edited; both mutation probes ran against copies in scratch.\n\n**The header's own arithmetic disagrees with the block below.** It says 6 introduced and 2\npre-existing, 8 in total; the findings block carries 9 rows, 7 introduced and 2 pre-existing.\nRecorded as returned, with the discrepancy named rather than silently corrected.\n\n**Each `message` is written as a YAML block scalar.** Several begin with a backtick, which YAML\nforbids at the start of a plain scalar. No wording was altered.\n\n## Suite\n\n```\nrunning 65 tests   test result: ok.     65 passed; 0 failed   (lib)\nrunning  7 tests   test result: ok.      7 passed; 0 failed   (pass 1 cases)\nrunning  5 tests   test result: FAILED.  1 passed; 4 failed   (pass 2 cases)\nEXIT=101\n```\n\n## Attacked and could not break\n\n- `-o json` / `-o yaml` byte identity: six comparisons across three fixtures (control characters,\n  nulls, empty keys, nested arrays) against a base-code probe. No difference.\n- Severity through a pipe: no escape sequence, no `is_terminal`, no `NO_COLOR` branch.\n- Report-level field colliding with a record key in `compact`: all seven shapes this crate builds\n  enumerated. No collision.\n- `fit_to_budget` termination and arithmetic: no underflow, no infinite loop, no panic at one\n  column, zero leading columns, or a pad wider than the budget.\n- `elide` never over-runs its allocation for any width >= 1, wide characters included.\n- `doctor` and `auth status` both fit the budget with room to spare, at 36 and 62 columns. Only\n  `providers` reaches it.\n\n## Deviation the adversary reported against itself\n\nThree probe files were first written to `/tmp`, against the standing rule. They were moved into the\nassigned scratch root and nothing remains in `/tmp`.\n\n## Findings\n\n```findings\n- file: crates/connectors-console/src/output.rs\n  line: 181\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: pre-existing\n  message: >-\n    compact answers an empty listing with one line that parses as a record, which is the exact thing the Format::Compact doc this correction wrote says a line must never be.\n- file: crates/connectors-console/src/output.rs\n  line: 416\n  category: boundary\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    fit_to_budget lets the last column begin at display column 120 of the 120-column budget, so on the terminal the module documents itself for not one character of connectors providers' last column is on the line.\n- file: crates/connectors-console/src/output.rs\n  line: 213\n  category: acceptance\n  severity: warning\n  verdict: CONFIRMED\n  origin: pre-existing\n  message: >-\n    when the single array a report carries holds scalars, compact writes each element bare and drops the array's own key, so `connect slack -o compact` loses the name `events` that the acceptance says no format may drop.\n- file: crates/connectors-console/src/output.rs\n  line: 508\n  category: boundary\n  severity: warning\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    elide cannot honour a width of zero and always emits its one-column mark, so a column the budget squeezed to zero puts the header and every row one column out of step - reachable only through an empty JSON key, which nothing here was shown to produce.\n- file: crates/connectors-console/src/output.rs\n  line: 411\n  category: judgement\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    the phase-one floor is the column name width, so required_config_fields keeps 22 of the 94 available columns for one-character cells while provider is cut to 8 (18 of 65 catalogued ids truncated) and four pairs of distinct authorities render identically.\n- file: crates/connectors-console/src/output.rs\n  line: 471\n  category: contract-drift\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    display_width's doc asserts control characters cannot reach it because one_line folded them, but column names are taken raw from fields.keys() and never pass through one_line.\n- file: crates/connectors-console/src/output.rs\n  line: 623\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    ConnectionState::Revoked, this repository's own protocol word for a connection that cannot work, ranks Unknown and shows `?` rather than the `x` the renderer defines for exactly that meaning.\n- file: crates/connectors-console/src/output.rs\n  line: 472\n  category: property\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    an emoji in a Grafana-chosen target label displaces every later column on that row by one, measured at 14 against 13 on a scratch copy.\n- file: crates/connectors-console/src/output.rs\n  line: 592\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    boolean ready:false marks 13 of 65 catalogued providers with `x`, the glyph defined as \"this cannot work\", for providers that merely declare no verify probe - the same reasoning the correction used to downgrade not-callable to warn.\n```\n","title":"Adversary, pass 2 — informative-command-readability"},"args":{"command":"create-entity","data":{"body":"# Adversary, pass 2 — informative-command-readability\n\n```\nunit: informative-command-readability\nverdict: red\ncases: executed 72->77, red 4\norigin: introduced 6, pre-existing 2, undecided 0\nwrote-outside-worktree: .../scratch/{adv2probe/,adv2probe-base/,adv2-*,x.json,y.json,z.json}\nneeds-coordinator: yes\n```\n\nAttacking `06e1e22`, base `7dc4c07c`. Added\n`crates/connectors-console/tests/adversary_readability_pass2.rs`, 4 red cases plus one helper. No\nimplementation file was edited; both mutation probes ran against copies in scratch.\n\n**The header's own arithmetic disagrees with the block below.** It says 6 introduced and 2\npre-existing, 8 in total; the findings block carries 9 rows, 7 introduced and 2 pre-existing.\nRecorded as returned, with the discrepancy named rather than silently corrected.\n\n**Each `message` is written as a YAML block scalar.** Several begin with a backtick, which YAML\nforbids at the start of a plain scalar. No wording was altered.\n\n## Suite\n\n```\nrunning 65 tests   test result: ok.     65 passed; 0 failed   (lib)\nrunning  7 tests   test result: ok.      7 passed; 0 failed   (pass 1 cases)\nrunning  5 tests   test result: FAILED.  1 passed; 4 failed   (pass 2 cases)\nEXIT=101\n```\n\n## Attacked and could not break\n\n- `-o json` / `-o yaml` byte identity: six comparisons across three fixtures (control characters,\n  nulls, empty keys, nested arrays) against a base-code probe. No difference.\n- Severity through a pipe: no escape sequence, no `is_terminal`, no `NO_COLOR` branch.\n- Report-level field colliding with a record key in `compact`: all seven shapes this crate builds\n  enumerated. No collision.\n- `fit_to_budget` termination and arithmetic: no underflow, no infinite loop, no panic at one\n  column, zero leading columns, or a pad wider than the budget.\n- `elide` never over-runs its allocation for any width >= 1, wide characters included.\n- `doctor` and `auth status` both fit the budget with room to spare, at 36 and 62 columns. Only\n  `providers` reaches it.\n\n## Deviation the adversary reported against itself\n\nThree probe files were first written to `/tmp`, against the standing rule. They were moved into the\nassigned scratch root and nothing remains in `/tmp`.\n\n## Findings\n\n```findings\n- file: crates/connectors-console/src/output.rs\n  line: 181\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: pre-existing\n  message: >-\n    compact answers an empty listing with one line that parses as a record, which is the exact thing the Format::Compact doc this correction wrote says a line must never be.\n- file: crates/connectors-console/src/output.rs\n  line: 416\n  category: boundary\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    fit_to_budget lets the last column begin at display column 120 of the 120-column budget, so on the terminal the module documents itself for not one character of connectors providers' last column is on the line.\n- file: crates/connectors-console/src/output.rs\n  line: 213\n  category: acceptance\n  severity: warning\n  verdict: CONFIRMED\n  origin: pre-existing\n  message: >-\n    when the single array a report carries holds scalars, compact writes each element bare and drops the array's own key, so `connect slack -o compact` loses the name `events` that the acceptance says no format may drop.\n- file: crates/connectors-console/src/output.rs\n  line: 508\n  category: boundary\n  severity: warning\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    elide cannot honour a width of zero and always emits its one-column mark, so a column the budget squeezed to zero puts the header and every row one column out of step - reachable only through an empty JSON key, which nothing here was shown to produce.\n- file: crates/connectors-console/src/output.rs\n  line: 411\n  category: judgement\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    the phase-one floor is the column name width, so required_config_fields keeps 22 of the 94 available columns for one-character cells while provider is cut to 8 (18 of 65 catalogued ids truncated) and four pairs of distinct authorities render identically.\n- file: crates/connectors-console/src/output.rs\n  line: 471\n  category: contract-drift\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: >-\n    display_width's doc asserts control characters cannot reach it because one_line folded them, but column names are taken raw from fields.keys() and never pass through one_line.\n- file: crates/connectors-console/src/output.rs\n  line: 623\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    ConnectionState::Revoked, this repository's own protocol word for a connection that cannot work, ranks Unknown and shows `?` rather than the `x` the renderer defines for exactly that meaning.\n- file: crates/connectors-console/src/output.rs\n  line: 472\n  category: property\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    an emoji in a Grafana-chosen target label displaces every later column on that row by one, measured at 14 against 13 on a scratch copy.\n- file: crates/connectors-console/src/output.rs\n  line: 592\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    boolean ready:false marks 13 of 65 catalogued providers with `x`, the glyph defined as \"this cannot work\", for providers that merely declare no verify probe - the same reasoning the correction used to downgrade not-callable to warn.\n```\n","status":"active","title":"Adversary, pass 2 — informative-command-readability"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-informative-readability-pass-2"},"payload":{"actor":"human:timo","at":1788489769522,"causation":"cmd-new-review-result-adversary-informative-readability-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-informative-readability-pass-2@1#0~b943e7ecb9bac44a","executor":null,"recorded_at":"2026-09-04T02:42:49Z"}}
{"entity":"review-result","version":1,"id":"adversary-informative-readability-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:informative-command-readability"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000495","target":"01MEM0000000000000178"},"payload":{"actor":"human:timo","at":1788489769541,"causation":"cmd-review-result-adversary-informative-readability-pass-2-reviews-story-informative-command-readability","change":{"change":"related","relation":"reviews","target":"story:informative-command-readability"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-informative-readability-pass-2@1#0~795645f912117f68","executor":null,"recorded_at":"2026-09-04T02:42:49Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/connectors-gate/wave-gate2.log","source":"bash scripts/gate.sh, sharded, 12 workspaces + --final on wave/cli-output, every step exit 0","target":"01MEM0000000000000181"},"payload":{"actor":"human:timo","at":1788491464000,"causation":"cmd-evidence-story-informative-command-readability-test_result-1788491465509","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/connectors-gate/wave-gate2.log","source":"bash scripts/gate.sh, sharded, 12 workspaces + --final on wave/cli-output, every step exit 0"},"correlation":"protocol-artifact-evidence","event_id":"story:informative-command-readability@4#0~97ea59e5ca97b64e","executor":null,"recorded_at":"2026-09-04T03:11:04Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-informative-readability-pass-2","source":"review-result:adversary-informative-readability-pass-2","target":"01MEM0000000000000181"},"payload":{"actor":"human:timo","at":1788491465000,"causation":"cmd-evidence-story-informative-command-readability-review_outcome-1788491465705","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-informative-readability-pass-2","source":"review-result:adversary-informative-readability-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:informative-command-readability@4#0~f87698b5ef5849e8","executor":null,"recorded_at":"2026-09-04T03:11:05Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":4}}","target":"01MEM0000000000000181","to":"implemented"},"payload":{"actor":"human:timo","at":1788491465959,"causation":"cmd-move-story-informative-command-readability-1788491465959","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":4,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:informative-command-readability@5#0~379b1afe9899b8f0","executor":null,"recorded_at":"2026-09-04T03:11:05Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"## Context\n\nEvery informative subcommand builds a `serde_json::Value` and hands it to `output::emit`\n(`crates/connectors-cli/src/lib.rs`, 13 call sites). In `text` — the default — that value is walked\nby one generic JSON pretty-printer, `output.rs:168`, which knows nothing about what it is printing:\n`key: value` per scalar, two spaces per nesting level, a bare `-` line per array element.\n\nMeasured on this machine against `connectors 0.5.7`:\n\n| command | lines | records |\n|---|---|---|\n| `connectors doctor` | 26 | 6 checks |\n| `connectors providers` | 927 | 65 providers |\n| `connectors providers --query jira` | 22 | 1 provider |\n| `connectors auth status` | 133 | 7 providers |\n\n`doctor` spends four lines per check — a bare `-`, then `check:`, `status:`, `detail:` — and the\none `warn` it found here renders identically to the five `ok` lines above it. The module that\nproduces the report already ranks the three states and says why (`doctor.rs:11`: \"`fail` is *this\ncannot work*; `warn` is *this works and you should know*\"), and the renderer discards that rank at\nthe last inch. `providers` averages 14 lines per provider, so the catalogue does not fit on a\nscreen and cannot be scanned.\n\n`-o compact` is closer to readable — `doctor` is 6 lines there — but it is not the default and it\nloses data: `compact` unwraps the single array field (`output.rs:126`, `output.rs:137`) and\ndiscards the scalar siblings beside it, so `healthy: true` is absent from every compact `doctor`\nrun. Verified: `connectors -o compact doctor | grep -c healthy` prints 0.\n\n## Acceptance\n\n`connectors doctor`, `providers` and `auth status` each render in `text` as one aligned row per\nrecord with the severity or readiness of a row distinguishable without reading its text, no output\nformat drops a field the value carries, and the bytes of `-o json` and `-o yaml` are unchanged.\n\n## Scope\n\nConfirmed by the implementor across three rounds; both lines were `cited` and both held.\n\n| path | confidence | what the wave learned |\n|---|---|---|\n| `crates/connectors-console/src/output.rs` | cited, confirmed | carried every one of the 19 findings both adversary passes raised. Split during integration into `output.rs` (761 lines) and `output_tests.rs` (799), because the module-size fence refuses 1500 |\n| `crates/connectors-console/src/doctor.rs` | cited, confirmed | touched in round 1 only; the final round needed no change here |\n\nSurfaces the story did not name and the wave added:\n\n| path | why |\n|---|---|\n| `crates/connectors-console/src/output_tests.rs` | new; the renderer's tests, split out at integration |\n| `crates/connectors-console/tests/adversary_readability.rs` | new; adversary pass 1, 7 cases |\n| `crates/connectors-console/tests/adversary_readability_pass2.rs` | new; adversary pass 2, 5 cases |\n| `crates/connectors-console/Cargo.lock` | its path pins still read 0.4.1 against manifests at 0.5.8, so `--locked` refused to start |\n| `scripts/gate.sh`, `AGENTS.md` | coordinator's, not the unit's: the package was in no gate lane |\n\n## Notes\n\n- **The renderer is shared, so this is one change and every informative command gets it.** Thirteen\n  `output::emit` call sites, one `text` function. A per-command renderer would be thirteen places\n  for the format to drift.\n- **`json` and `yaml` are contracts and do not move.** `output.rs:1` states why a structured format\n  carries its failures on stdout; a caller parsing those bytes is not the reader this story is for.\n- **Colour is a decision, not an assumption.** If severity is carried by colour it needs TTY\n  detection and `NO_COLOR`; a symbol or a leading status column needs neither and works in a pipe.\n  Prefer the form that survives redirection, and treat colour as an addition on top of it.\n- **The compact data loss is a defect, not a preference.** It is written here because it lives in\n  the same function as the readability work, and it can be split into its own story if that is\n  wanted.\n"},"args":{"changes":{"body":"## Context\n\nEvery informative subcommand builds a `serde_json::Value` and hands it to `output::emit`\n(`crates/connectors-cli/src/lib.rs`, 13 call sites). In `text` — the default — that value is walked\nby one generic JSON pretty-printer, `output.rs:168`, which knows nothing about what it is printing:\n`key: value` per scalar, two spaces per nesting level, a bare `-` line per array element.\n\nMeasured on this machine against `connectors 0.5.7`:\n\n| command | lines | records |\n|---|---|---|\n| `connectors doctor` | 26 | 6 checks |\n| `connectors providers` | 927 | 65 providers |\n| `connectors providers --query jira` | 22 | 1 provider |\n| `connectors auth status` | 133 | 7 providers |\n\n`doctor` spends four lines per check — a bare `-`, then `check:`, `status:`, `detail:` — and the\none `warn` it found here renders identically to the five `ok` lines above it. The module that\nproduces the report already ranks the three states and says why (`doctor.rs:11`: \"`fail` is *this\ncannot work*; `warn` is *this works and you should know*\"), and the renderer discards that rank at\nthe last inch. `providers` averages 14 lines per provider, so the catalogue does not fit on a\nscreen and cannot be scanned.\n\n`-o compact` is closer to readable — `doctor` is 6 lines there — but it is not the default and it\nloses data: `compact` unwraps the single array field (`output.rs:126`, `output.rs:137`) and\ndiscards the scalar siblings beside it, so `healthy: true` is absent from every compact `doctor`\nrun. Verified: `connectors -o compact doctor | grep -c healthy` prints 0.\n\n## Acceptance\n\n`connectors doctor`, `providers` and `auth status` each render in `text` as one aligned row per\nrecord with the severity or readiness of a row distinguishable without reading its text, no output\nformat drops a field the value carries, and the bytes of `-o json` and `-o yaml` are unchanged.\n\n## Scope\n\nConfirmed by the implementor across three rounds; both lines were `cited` and both held.\n\n| path | confidence | what the wave learned |\n|---|---|---|\n| `crates/connectors-console/src/output.rs` | cited, confirmed | carried every one of the 19 findings both adversary passes raised. Split during integration into `output.rs` (761 lines) and `output_tests.rs` (799), because the module-size fence refuses 1500 |\n| `crates/connectors-console/src/doctor.rs` | cited, confirmed | touched in round 1 only; the final round needed no change here |\n\nSurfaces the story did not name and the wave added:\n\n| path | why |\n|---|---|\n| `crates/connectors-console/src/output_tests.rs` | new; the renderer's tests, split out at integration |\n| `crates/connectors-console/tests/adversary_readability.rs` | new; adversary pass 1, 7 cases |\n| `crates/connectors-console/tests/adversary_readability_pass2.rs` | new; adversary pass 2, 5 cases |\n| `crates/connectors-console/Cargo.lock` | its path pins still read 0.4.1 against manifests at 0.5.8, so `--locked` refused to start |\n| `scripts/gate.sh`, `AGENTS.md` | coordinator's, not the unit's: the package was in no gate lane |\n\n## Notes\n\n- **The renderer is shared, so this is one change and every informative command gets it.** Thirteen\n  `output::emit` call sites, one `text` function. A per-command renderer would be thirteen places\n  for the format to drift.\n- **`json` and `yaml` are contracts and do not move.** `output.rs:1` states why a structured format\n  carries its failures on stdout; a caller parsing those bytes is not the reader this story is for.\n- **Colour is a decision, not an assumption.** If severity is carried by colour it needs TTY\n  detection and `NO_COLOR`; a symbol or a leading status column needs neither and works in a pipe.\n  Prefer the form that survives redirection, and treat colour as an addition on top of it.\n- **The compact data loss is a defect, not a preference.** It is written here because it lives in\n  the same function as the readability work, and it can be split into its own story if that is\n  wanted.\n"},"command":"update-entity","target":"01MEM0000000000000181"},"payload":{"actor":"human:timo","at":1788491466207,"causation":"cmd-protocol-artifact-body-section-story-informative-command-readability-1788491466207","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:informative-command-readability@6#0~b2a2fb031f122df5","executor":null,"recorded_at":"2026-09-04T03:11:06Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":7,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output_tests.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output_tests.rs"}]},"command":"update-entity","target":"01MEM0000000000000181"},"payload":{"actor":"human:timo","at":1788491466476,"causation":"cmd-protocol-artifact-scope-story-informative-command-readability-1788491466476","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:informative-command-readability@7#0~96e31246fb64cb96","executor":null,"recorded_at":"2026-09-04T03:11:06Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":8,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output_tests.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_readability.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output_tests.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_readability.rs"}]},"command":"update-entity","target":"01MEM0000000000000181"},"payload":{"actor":"human:timo","at":1788491466721,"causation":"cmd-protocol-artifact-scope-story-informative-command-readability-1788491466721","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:informative-command-readability@8#0~f384b63ccfa701c1","executor":null,"recorded_at":"2026-09-04T03:11:06Z"}}
{"entity":"story","version":1,"id":"informative-command-readability","revision":9,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output_tests.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_readability.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_readability_pass2.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-console/src/doctor.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output_tests.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_readability.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_readability_pass2.rs"}]},"command":"update-entity","target":"01MEM0000000000000181"},"payload":{"actor":"human:timo","at":1788491466971,"causation":"cmd-protocol-artifact-scope-story-informative-command-readability-1788491466971","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:informative-command-readability@9#0~44e6fd7b4036ad56","executor":null,"recorded_at":"2026-09-04T03:11:06Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Context\n\nA live delegated GitLab OAuth connection remains discoverable after its two-hour access token expires, but the first credential-backed datasource read is refused even though the client secret and refresh token remain present. Refresh-token rotation must either commit the replacement credential set durably or return a precise reconnect-required state without misrepresenting the failure as repository authorization.\n\n## Acceptance\n\n- A deterministic integration test expires a GitLab OAuth access token, performs the refresh grant, and proves the replacement access and refresh tokens are durably committed before a repository datasource read succeeds.\n- Concurrent reads cause at most one refresh exchange and observe the committed credential generation.\n- A refresh failure is distinguishable from a missing repository grant at the product boundary without exposing credential or provider response bytes.\n- The released hosted Connectors runtime passes the repository gate and the live Devcenter repository read succeeds after deployment.\n\n## Scope\n\n- `crates/integration-gitlab/src/backend.rs`\n- `crates/integration-gitlab/src/backend_tests.rs`\n- Connector protocol/product refusal mapping if required by the demonstrated failure.\n- Release metadata and live deployment evidence.\n","summary":"Rotate and durably commit GitLab OAuth credentials before expired user connections lose repository access.","title":"Keep delegated GitLab reads alive across OAuth refresh"},"args":{"command":"create-entity","data":{"body":"## Context\n\nA live delegated GitLab OAuth connection remains discoverable after its two-hour access token expires, but the first credential-backed datasource read is refused even though the client secret and refresh token remain present. Refresh-token rotation must either commit the replacement credential set durably or return a precise reconnect-required state without misrepresenting the failure as repository authorization.\n\n## Acceptance\n\n- A deterministic integration test expires a GitLab OAuth access token, performs the refresh grant, and proves the replacement access and refresh tokens are durably committed before a repository datasource read succeeds.\n- Concurrent reads cause at most one refresh exchange and observe the committed credential generation.\n- A refresh failure is distinguishable from a missing repository grant at the product boundary without exposing credential or provider response bytes.\n- The released hosted Connectors runtime passes the repository gate and the live Devcenter repository read succeeds after deployment.\n\n## Scope\n\n- `crates/integration-gitlab/src/backend.rs`\n- `crates/integration-gitlab/src/backend_tests.rs`\n- Connector protocol/product refusal mapping if required by the demonstrated failure.\n- Release metadata and live deployment evidence.\n","status":"draft","summary":"Rotate and durably commit GitLab OAuth credentials before expired user connections lose repository access.","title":"Keep delegated GitLab reads alive across OAuth refresh"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/durable-gitlab-oauth-refresh"},"payload":{"actor":"human:timo","at":1788493409384,"causation":"cmd-new-story-durable-gitlab-oauth-refresh","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:durable-gitlab-oauth-refresh@1#0~224d78efcd85db78","executor":null,"recorded_at":"2026-09-04T03:43:29Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:credential-production"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000515","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788493409401,"causation":"cmd-story-durable-gitlab-oauth-refresh-decomposes-epic-credential-production","change":{"change":"related","relation":"decomposes","target":"epic:credential-production"},"correlation":"protocol-artifact-relate","event_id":"story:durable-gitlab-oauth-refresh@1#0~5141bf3eae4ef578","executor":null,"recorded_at":"2026-09-04T03:43:29Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788493409531,"causation":"cmd-protocol-artifact-scope-story-durable-gitlab-oauth-refresh-1788493409531","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:durable-gitlab-oauth-refresh@2#0~685200cff13e0665","executor":null,"recorded_at":"2026-09-04T03:43:29Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000160","to":"proposed"},"payload":{"actor":"human:timo","at":1788493409728,"causation":"cmd-move-story-durable-gitlab-oauth-refresh-1788493409728","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:durable-gitlab-oauth-refresh@3#0~d8053775dbdfe409","executor":null,"recorded_at":"2026-09-04T03:43:29Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000160","to":"active"},"payload":{"actor":"human:timo","at":1788493409933,"causation":"cmd-move-story-durable-gitlab-oauth-refresh-1788493409933","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:durable-gitlab-oauth-refresh@4#0~767bf21938cb2f81","executor":null,"recorded_at":"2026-09-04T03:43:29Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"Cargo.lock"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"catalog"},{"confidence":"cited","path":"connectors.lock"},{"confidence":"cited","path":"crates/catalog-reader/catalog.pack"},{"confidence":"cited","path":"crates/connectors-console/Cargo.lock"},{"confidence":"cited","path":"crates/connectors-console/Cargo.toml"},{"confidence":"cited","path":"crates/connectors-console/src/auth.rs"},{"confidence":"cited","path":"crates/connectors-runtime/Cargo.lock"},{"confidence":"cited","path":"crates/connectors-runtime/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"Cargo.lock"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"catalog"},{"confidence":"cited","path":"connectors.lock"},{"confidence":"cited","path":"crates/catalog-reader/catalog.pack"},{"confidence":"cited","path":"crates/connectors-console/Cargo.lock"},{"confidence":"cited","path":"crates/connectors-console/Cargo.toml"},{"confidence":"cited","path":"crates/connectors-console/src/auth.rs"},{"confidence":"cited","path":"crates/connectors-runtime/Cargo.lock"},{"confidence":"cited","path":"crates/connectors-runtime/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788494018628,"causation":"cmd-protocol-artifact-scope-story-durable-gitlab-oauth-refresh-1788494018628","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:durable-gitlab-oauth-refresh@5#0~7037ec3c2d7bd6c7","executor":null,"recorded_at":"2026-09-04T03:53:38Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/integration-gitlab/src/backend_tests.rs","source":"cargo test --manifest-path crates/integration-gitlab/Cargo.toml --locked","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788494018000,"causation":"cmd-evidence-story-durable-gitlab-oauth-refresh-test_result-1788494018775","change":{"change":"evidence","kind":"test_result","reference":"crates/integration-gitlab/src/backend_tests.rs","source":"cargo test --manifest-path crates/integration-gitlab/Cargo.toml --locked"},"correlation":"protocol-artifact-evidence","event_id":"story:durable-gitlab-oauth-refresh@5#0~a5bb57ff83f0ba38","executor":null,"recorded_at":"2026-09-04T03:53:38Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/connectors-console/src/auth.rs","source":"cargo test --manifest-path crates/connectors-console/Cargo.toml --locked --lib","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788494018000,"causation":"cmd-evidence-story-durable-gitlab-oauth-refresh-test_result-1788494018919","change":{"change":"evidence","kind":"test_result","reference":"crates/connectors-console/src/auth.rs","source":"cargo test --manifest-path crates/connectors-console/Cargo.toml --locked --lib"},"correlation":"protocol-artifact-evidence","event_id":"story:durable-gitlab-oauth-refresh@5#0~49902e773b741b8f","executor":null,"recorded_at":"2026-09-04T03:53:38Z"}}
{"entity":"story","version":1,"id":"durable-gitlab-oauth-refresh","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh --final","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788494019000,"causation":"cmd-evidence-story-durable-gitlab-oauth-refresh-test_result-1788494019064","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh --final"},"correlation":"protocol-artifact-evidence","event_id":"story:durable-gitlab-oauth-refresh@5#0~52df71a2c8da9007","executor":null,"recorded_at":"2026-09-04T03:53:39Z"}}
{"entity":"story","version":1,"id":"personal-gitlab-schedules-are-discoverable-and-governed","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Defect\n\nAn end-to-end check of the personal-local GitLab path on 2026-09-04 showed that a healthy,\ncallable connection is not an operable schedule-management surface:\n\n- `connectors doctor -o json` reported the personal posture healthy and `gitlab-user-get` plus\n  `gitlab-project-list` both succeeded through the local socket, while `connectors connection list\n  --query gitlab` returned no connections even though `operation search --query gitlab` named the\n  admitted catalog-backed GitLab connection; the two discovery surfaces therefore disagree about\n  the same live connection.\n- `connectors operation search --query 'pipeline schedule'` returned no operation because\n  `providers/gitlab.toml` declares pipeline inspection but no pipeline-schedule list, create,\n  update, or delete operations, so an operator cannot configure the internal scheduled trigger\n  needed for periodic Atlas reconciliation through Connectors.\n- `connectors operation search --limit 50` was accepted by clap and then surfaced\n  `connector-unreachable: Connector request was invalid: InvalidInput: search bounds are invalid`;\n  the actual maximum is the undocumented `MAX_SEARCH_RESULTS = 25` in\n  `crates/protocol/src/operation.rs`, while `crates/connectors-cli/src/lib.rs` declares only a\n  default and no range, making caller input look like transport failure.\n\nThe separate implicit hosted-target defect is already governed by\n`story:explicit-target-never-implicit`; this story is informed by it and does not duplicate its\ntarget-selection scope.\n\n## Scope\n\n- Make a catalog-backed personal-local connection appear consistently in `connection list` and\n  `operation search`, with tests proving the same opaque connection reference is reported by both.\n- Add source-grounded GitLab pipeline-schedule list, create, update, and delete operations to the\n  existing provider declaration, generated catalog artifacts, and consolidated catalog invariant\n  coverage; keep the read useful to operators and curate model exposure separately from\n  callability.\n- Classify schedule mutations with truthful effects, risk, idempotency, OAuth/PAT scope, and\n  approval requirements, and preserve the personal aperture: mutations remain absent or refused\n  unless that configured GitLab placement explicitly enables writes.\n- Put protocol search bounds into clap validation and help for every affected CLI search command,\n  and render an out-of-range value as local invalid input before any socket or network request.\n- Document and test the shortest personal-local workflow for finding the connection, describing\n  the schedule operations, and invoking them after explicit write enablement.\n\n## Out of scope\n\n- Creating or changing a production GitLab pipeline schedule as part of implementation.\n- Enabling writes in an operator's personal configuration.\n- Reworking hosted-versus-local target selection, which belongs to\n  `story:explicit-target-never-implicit`.\n\n## Acceptance\n\nWith a healthy personal-local GitLab connection, the default local CLI reports one consistent connection across connection and operation discovery, documents and rejects search limits above the protocol maximum before transport, exposes source-grounded pipeline-schedule list/create/update/delete operations, and proves schedule mutations remain refused until writes are explicitly enabled and approval requirements are satisfied.\n","summary":"Close the CLI discovery and validation gaps, then add write-gated GitLab pipeline-schedule operations.","title":"Personal GitLab schedules are discoverable and governed"},"args":{"command":"create-entity","data":{"body":"## Defect\n\nAn end-to-end check of the personal-local GitLab path on 2026-09-04 showed that a healthy,\ncallable connection is not an operable schedule-management surface:\n\n- `connectors doctor -o json` reported the personal posture healthy and `gitlab-user-get` plus\n  `gitlab-project-list` both succeeded through the local socket, while `connectors connection list\n  --query gitlab` returned no connections even though `operation search --query gitlab` named the\n  admitted catalog-backed GitLab connection; the two discovery surfaces therefore disagree about\n  the same live connection.\n- `connectors operation search --query 'pipeline schedule'` returned no operation because\n  `providers/gitlab.toml` declares pipeline inspection but no pipeline-schedule list, create,\n  update, or delete operations, so an operator cannot configure the internal scheduled trigger\n  needed for periodic Atlas reconciliation through Connectors.\n- `connectors operation search --limit 50` was accepted by clap and then surfaced\n  `connector-unreachable: Connector request was invalid: InvalidInput: search bounds are invalid`;\n  the actual maximum is the undocumented `MAX_SEARCH_RESULTS = 25` in\n  `crates/protocol/src/operation.rs`, while `crates/connectors-cli/src/lib.rs` declares only a\n  default and no range, making caller input look like transport failure.\n\nThe separate implicit hosted-target defect is already governed by\n`story:explicit-target-never-implicit`; this story is informed by it and does not duplicate its\ntarget-selection scope.\n\n## Scope\n\n- Make a catalog-backed personal-local connection appear consistently in `connection list` and\n  `operation search`, with tests proving the same opaque connection reference is reported by both.\n- Add source-grounded GitLab pipeline-schedule list, create, update, and delete operations to the\n  existing provider declaration, generated catalog artifacts, and consolidated catalog invariant\n  coverage; keep the read useful to operators and curate model exposure separately from\n  callability.\n- Classify schedule mutations with truthful effects, risk, idempotency, OAuth/PAT scope, and\n  approval requirements, and preserve the personal aperture: mutations remain absent or refused\n  unless that configured GitLab placement explicitly enables writes.\n- Put protocol search bounds into clap validation and help for every affected CLI search command,\n  and render an out-of-range value as local invalid input before any socket or network request.\n- Document and test the shortest personal-local workflow for finding the connection, describing\n  the schedule operations, and invoking them after explicit write enablement.\n\n## Out of scope\n\n- Creating or changing a production GitLab pipeline schedule as part of implementation.\n- Enabling writes in an operator's personal configuration.\n- Reworking hosted-versus-local target selection, which belongs to\n  `story:explicit-target-never-implicit`.\n\n## Acceptance\n\nWith a healthy personal-local GitLab connection, the default local CLI reports one consistent connection across connection and operation discovery, documents and rejects search limits above the protocol maximum before transport, exposes source-grounded pipeline-schedule list/create/update/delete operations, and proves schedule mutations remain refused until writes are explicitly enabled and approval requirements are satisfied.\n","status":"draft","summary":"Close the CLI discovery and validation gaps, then add write-gated GitLab pipeline-schedule operations.","title":"Personal GitLab schedules are discoverable and governed"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/personal-gitlab-schedules-are-discoverable-and-governed"},"payload":{"actor":"human:timo","at":1788510697368,"causation":"cmd-new-story-personal-gitlab-schedules-are-discoverable-and-governed","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:personal-gitlab-schedules-are-discoverable-and-governed@1#0~fd03c963365d6cb9","executor":null,"recorded_at":"2026-09-04T08:31:37Z"}}
{"entity":"story","version":1,"id":"personal-gitlab-schedules-are-discoverable-and-governed","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:local-product"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000520","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788510697384,"causation":"cmd-story-personal-gitlab-schedules-are-discoverable-and-governed-derived_from-epic-local-product","change":{"change":"related","relation":"derived_from","target":"epic:local-product"},"correlation":"protocol-artifact-relate","event_id":"story:personal-gitlab-schedules-are-discoverable-and-governed@1#0~74010a932093d39a","executor":null,"recorded_at":"2026-09-04T08:31:37Z"}}
{"entity":"story","version":1,"id":"personal-gitlab-schedules-are-discoverable-and-governed","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:local-product"},{"informed_by":"story:explicit-target-never-implicit"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000520","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788510697393,"causation":"cmd-story-personal-gitlab-schedules-are-discoverable-and-governed-informed_by-story-explicit-target-never-implicit","change":{"change":"related","relation":"informed_by","target":"story:explicit-target-never-implicit"},"correlation":"protocol-artifact-relate","event_id":"story:personal-gitlab-schedules-are-discoverable-and-governed@1#0~99dac81b1906fb6e","executor":null,"recorded_at":"2026-09-04T08:31:37Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Outcome\n\nEvery GitLab repository returned by the paginated project datasource remains admissible for project-bound reads, even when it is not among the first 100 recently active memberships.\n\n## Context\n\n`gitlab.projects` pages through the provider collection, but project-bound binding discovery and binding redemption each inspect only the first provider page. Workspace can therefore list a repository and then receive `NotGranted` while opening it.\n\n## Acceptance\n\n- A deterministic integration test places the selected repository after the first 100 projects and proves branch binding discovery succeeds.\n- Redeeming that binding walks bounded provider pages and resolves the same project without widening the user's GitLab authority.\n- Pagination is bounded, malformed continuation headers fail closed, and default-branch selection remains provider-declared.\n- The released Connectors runtime restores the authenticated Devcenter project-open journey.\n\n## Scope\n\n- `crates/integration-gitlab/src/backend.rs`\n- `crates/integration-gitlab/src/backend_tests.rs`\n- Connectors release metadata and Devcenter deployment evidence.\n","summary":"Keep every listed GitLab repository admissible across project-bound datasource checks.","title":"Page GitLab project admission"},"args":{"command":"create-entity","data":{"body":"## Outcome\n\nEvery GitLab repository returned by the paginated project datasource remains admissible for project-bound reads, even when it is not among the first 100 recently active memberships.\n\n## Context\n\n`gitlab.projects` pages through the provider collection, but project-bound binding discovery and binding redemption each inspect only the first provider page. Workspace can therefore list a repository and then receive `NotGranted` while opening it.\n\n## Acceptance\n\n- A deterministic integration test places the selected repository after the first 100 projects and proves branch binding discovery succeeds.\n- Redeeming that binding walks bounded provider pages and resolves the same project without widening the user's GitLab authority.\n- Pagination is bounded, malformed continuation headers fail closed, and default-branch selection remains provider-declared.\n- The released Connectors runtime restores the authenticated Devcenter project-open journey.\n\n## Scope\n\n- `crates/integration-gitlab/src/backend.rs`\n- `crates/integration-gitlab/src/backend_tests.rs`\n- Connectors release metadata and Devcenter deployment evidence.\n","status":"draft","summary":"Keep every listed GitLab repository admissible across project-bound datasource checks.","title":"Page GitLab project admission"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/page-gitlab-project-admission"},"payload":{"actor":"human:timo","at":1788530345023,"causation":"cmd-new-story-page-gitlab-project-admission","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:page-gitlab-project-admission@1#0~24377433400c95d1","executor":null,"recorded_at":"2026-09-04T13:59:05Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:enforced-authority"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000527","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788530345035,"causation":"cmd-story-page-gitlab-project-admission-derived_from-epic-enforced-authority","change":{"change":"related","relation":"derived_from","target":"epic:enforced-authority"},"correlation":"protocol-artifact-relate","event_id":"story:page-gitlab-project-admission@1#0~2e4130b181180737","executor":null,"recorded_at":"2026-09-04T13:59:05Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"command":"update-entity","target":"01MEM0000000000000253"},"payload":{"actor":"human:timo","at":1788530345300,"causation":"cmd-protocol-artifact-scope-story-page-gitlab-project-admission-1788530345300","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:page-gitlab-project-admission@2#0~7fe8756fa1779c4d","executor":null,"recorded_at":"2026-09-04T13:59:05Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000253","to":"proposed"},"payload":{"actor":"human:timo","at":1788530345642,"causation":"cmd-move-story-page-gitlab-project-admission-1788530345642","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:page-gitlab-project-admission@3#0~608e0ce084a4b83a","executor":null,"recorded_at":"2026-09-04T13:59:05Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000253","to":"active"},"payload":{"actor":"human:timo","at":1788530345654,"causation":"cmd-move-story-page-gitlab-project-admission-1788530345654","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:page-gitlab-project-admission@4#0~ee7a33f9337dfa2e","executor":null,"recorded_at":"2026-09-04T13:59:05Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Outcome\n\nAn OAuth-backed Claude Code subscription renews in Connector custody before an Agent attempt receives its leased access token.\n\n## Context\n\nThe initial OAuth exchange works and reconnect temporarily restores Agent execution, but the provider refresh response can omit `scope`. The decoder currently requires that field before the existing carry-forward logic can preserve the already verified scope set.\n\n## Acceptance\n\n- A refresh response without `scope` is accepted only when a previous verified OAuth record supplies the required `user:inference` scope.\n- A first authorization response without scopes remains refused.\n- Refresh-token rotation remains in Connector custody and a new task can redeem a refreshed access token without re-authentication.\n\n## Scope\n\n- `crates/subscription-custody/src/lib.rs`\n- Connectors release metadata and live model-backed Devcenter evidence.\n","summary":"Accept scope-omitting Claude refresh responses only by carrying forward previously verified scopes.","title":"Preserve Claude refresh scope"},"args":{"command":"create-entity","data":{"body":"## Outcome\n\nAn OAuth-backed Claude Code subscription renews in Connector custody before an Agent attempt receives its leased access token.\n\n## Context\n\nThe initial OAuth exchange works and reconnect temporarily restores Agent execution, but the provider refresh response can omit `scope`. The decoder currently requires that field before the existing carry-forward logic can preserve the already verified scope set.\n\n## Acceptance\n\n- A refresh response without `scope` is accepted only when a previous verified OAuth record supplies the required `user:inference` scope.\n- A first authorization response without scopes remains refused.\n- Refresh-token rotation remains in Connector custody and a new task can redeem a refreshed access token without re-authentication.\n\n## Scope\n\n- `crates/subscription-custody/src/lib.rs`\n- Connectors release metadata and live model-backed Devcenter evidence.\n","status":"draft","summary":"Accept scope-omitting Claude refresh responses only by carrying forward previously verified scopes.","title":"Preserve Claude refresh scope"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/preserve-claude-refresh-scope"},"payload":{"actor":"human:timo","at":1788530503642,"causation":"cmd-new-story-preserve-claude-refresh-scope","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:preserve-claude-refresh-scope@1#0~53b0e67e76d042bb","executor":null,"recorded_at":"2026-09-04T14:01:43Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:subscription-custody"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000532","target":"01MEM0000000000000064"},"payload":{"actor":"human:timo","at":1788530503663,"causation":"cmd-story-preserve-claude-refresh-scope-derived_from-epic-subscription-custody","change":{"change":"related","relation":"derived_from","target":"epic:subscription-custody"},"correlation":"protocol-artifact-relate","event_id":"story:preserve-claude-refresh-scope@1#0~dbee44a738e940ed","executor":null,"recorded_at":"2026-09-04T14:01:43Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/subscription-custody/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/subscription-custody/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000268"},"payload":{"actor":"human:timo","at":1788530503942,"causation":"cmd-protocol-artifact-scope-story-preserve-claude-refresh-scope-1788530503942","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:preserve-claude-refresh-scope@2#0~621eaa9623a1bc2f","executor":null,"recorded_at":"2026-09-04T14:01:43Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000268","to":"proposed"},"payload":{"actor":"human:timo","at":1788530504273,"causation":"cmd-move-story-preserve-claude-refresh-scope-1788530504273","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:preserve-claude-refresh-scope@3#0~f1bb801e383a9d30","executor":null,"recorded_at":"2026-09-04T14:01:44Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000268","to":"active"},"payload":{"actor":"human:timo","at":1788530504286,"causation":"cmd-move-story-preserve-claude-refresh-scope-1788530504286","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:preserve-claude-refresh-scope@4#0~abd8c1aec189701c","executor":null,"recorded_at":"2026-09-04T14:01:44Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"}]},"command":"update-entity","target":"01MEM0000000000000253"},"payload":{"actor":"human:timo","at":1788530742490,"causation":"cmd-protocol-artifact-scope-story-page-gitlab-project-admission-1788530742490","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:page-gitlab-project-admission@5#0~3e49315fb8bb1575","executor":null,"recorded_at":"2026-09-04T14:05:42Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `aep artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","title":"Activate a hosted Slack organization bot operationally"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `aep artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","status":"draft","title":"Activate a hosted Slack organization bot operationally"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/activate-hosted-slack-org-bot"},"payload":{"actor":"human:timo","at":1788531221823,"causation":"cmd-new-story-activate-hosted-slack-org-bot","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:activate-hosted-slack-org-bot@1#0~156a55d14ea82836","executor":null,"recorded_at":"2026-09-04T14:13:41Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"Allow an operator to install the existing Slack app and bot tokens through the hosted administrative credential seam. The runtime must materialize the organization connection only when both credentials are present and valid, while personal Slack OAuth remains explicitly unavailable unless its separate client registration is configured.\n"},"args":{"changes":{"body":"Allow an operator to install the existing Slack app and bot tokens through the hosted administrative credential seam. The runtime must materialize the organization connection only when both credentials are present and valid, while personal Slack OAuth remains explicitly unavailable unless its separate client registration is configured.\n"},"command":"update-entity","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788531222022,"causation":"cmd-protocol-artifact-body-story-activate-hosted-slack-org-bot-1788531222022","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:activate-hosted-slack-org-bot@2#0~a535bae7cf1785f6","executor":null,"recorded_at":"2026-09-04T14:13:42Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-config/src/hosted.rs"},{"confidence":"cited","path":"crates/connectors-runtime/src/composition.rs"},{"confidence":"cited","path":"crates/integration-slack/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-config/src/hosted.rs"},{"confidence":"cited","path":"crates/connectors-runtime/src/composition.rs"},{"confidence":"cited","path":"crates/integration-slack/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788531234818,"causation":"cmd-protocol-artifact-scope-story-activate-hosted-slack-org-bot-1788531234818","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:activate-hosted-slack-org-bot@3#0~dec861831fa7953d","executor":null,"recorded_at":"2026-09-04T14:13:54Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":3,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:credential-production"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000106","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788531234961,"causation":"cmd-story-activate-hosted-slack-org-bot-derived_from-epic-credential-production","change":{"change":"related","relation":"derived_from","target":"epic:credential-production"},"correlation":"protocol-artifact-relate","event_id":"story:activate-hosted-slack-org-bot@3#0~58ca93373f574b4c","executor":null,"recorded_at":"2026-09-04T14:13:54Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000106","to":"proposed"},"payload":{"actor":"human:timo","at":1788531235264,"causation":"cmd-move-story-activate-hosted-slack-org-bot-1788531235264","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:activate-hosted-slack-org-bot@4#0~7a1f801a74903869","executor":null,"recorded_at":"2026-09-04T14:13:55Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000106","to":"active"},"payload":{"actor":"human:timo","at":1788531235279,"causation":"cmd-move-story-activate-hosted-slack-org-bot-1788531235279","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:activate-hosted-slack-org-bot@5#0~b09d340a59dab461","executor":null,"recorded_at":"2026-09-04T14:13:55Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-config/src/hosted.rs"},{"confidence":"cited","path":"crates/connectors-runtime/src/composition.rs"},{"confidence":"cited","path":"crates/integration-slack/src/backend.rs"},{"confidence":"cited","path":"crates/integration-slack/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-config/src/hosted.rs"},{"confidence":"cited","path":"crates/connectors-runtime/src/composition.rs"},{"confidence":"cited","path":"crates/integration-slack/src/backend.rs"},{"confidence":"cited","path":"crates/integration-slack/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788531333473,"causation":"cmd-protocol-artifact-scope-story-activate-hosted-slack-org-bot-1788531333473","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:activate-hosted-slack-org-bot@6#0~90774e28744034f8","executor":null,"recorded_at":"2026-09-04T14:15:33Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/project_scan.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/integration-gitlab/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/project_scan.rs"}]},"command":"update-entity","target":"01MEM0000000000000256"},"payload":{"actor":"human:timo","at":1788531628830,"causation":"cmd-protocol-artifact-scope-story-page-gitlab-project-admission-1788531628830","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:page-gitlab-project-admission@6#0~5359d244efd2628d","executor":null,"recorded_at":"2026-09-04T14:20:28Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"## Outcome\n\nA hosted Slack organization bot can be activated from value-free deployment policy plus app and bot credentials installed through the administrative API.\n\n## Context\n\nThe runtime already knows how to materialize the tenant-wide organization bot from `app_token` and `bot_token`, but the hosted administration surface exposed only a personal OAuth client secret. Existing Socket Mode credentials therefore had no supported operational installation path. Personal user OAuth and organization-bot credentials are separate capabilities and must remain visibly separate.\n\n## Acceptance\n\n- Hosted Slack configuration may omit the OAuth client pair when only bot or companion operation is configured.\n- The administrative surface accepts optional `app_token` and `bot_token` requirements at their exact tenant-wide credential addresses.\n- A personal OAuth client secret is required and advertised only when the paired OAuth client ID and redirect URI are configured.\n- The runtime creates the organization connection only after both Slack credentials exist and pass the existing workspace-bound verification.\n- No credential value enters configuration, source, logs, planning artifacts, or deployment values.\n"},"args":{"changes":{"body":"## Outcome\n\nA hosted Slack organization bot can be activated from value-free deployment policy plus app and bot credentials installed through the administrative API.\n\n## Context\n\nThe runtime already knows how to materialize the tenant-wide organization bot from `app_token` and `bot_token`, but the hosted administration surface exposed only a personal OAuth client secret. Existing Socket Mode credentials therefore had no supported operational installation path. Personal user OAuth and organization-bot credentials are separate capabilities and must remain visibly separate.\n\n## Acceptance\n\n- Hosted Slack configuration may omit the OAuth client pair when only bot or companion operation is configured.\n- The administrative surface accepts optional `app_token` and `bot_token` requirements at their exact tenant-wide credential addresses.\n- A personal OAuth client secret is required and advertised only when the paired OAuth client ID and redirect URI are configured.\n- The runtime creates the organization connection only after both Slack credentials exist and pass the existing workspace-bound verification.\n- No credential value enters configuration, source, logs, planning artifacts, or deployment values.\n"},"command":"update-entity","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788531628986,"causation":"cmd-protocol-artifact-body-story-activate-hosted-slack-org-bot-1788531628986","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:activate-hosted-slack-org-bot@7#0~3078abd0e8755db6","executor":null,"recorded_at":"2026-09-04T14:20:28Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/integration-gitlab/src/backend_tests.rs","source":"cargo test --manifest-path crates/integration-gitlab/Cargo.toml --locked","target":"01MEM0000000000000256"},"payload":{"actor":"human:timo","at":1788531629000,"causation":"cmd-evidence-story-page-gitlab-project-admission-test_result-1788531629131","change":{"change":"evidence","kind":"test_result","reference":"crates/integration-gitlab/src/backend_tests.rs","source":"cargo test --manifest-path crates/integration-gitlab/Cargo.toml --locked"},"correlation":"protocol-artifact-evidence","event_id":"story:page-gitlab-project-admission@6#0~b17d020a9a1351a8","executor":null,"recorded_at":"2026-09-04T14:20:29Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/subscription-custody/src/lib.rs","source":"cargo test --manifest-path crates/subscription-custody/Cargo.toml --locked","target":"01MEM0000000000000271"},"payload":{"actor":"human:timo","at":1788531629000,"causation":"cmd-evidence-story-preserve-claude-refresh-scope-test_result-1788531629283","change":{"change":"evidence","kind":"test_result","reference":"crates/subscription-custody/src/lib.rs","source":"cargo test --manifest-path crates/subscription-custody/Cargo.toml --locked"},"correlation":"protocol-artifact-evidence","event_id":"story:preserve-claude-refresh-scope@4#0~2fbc90a0b9a482a4","executor":null,"recorded_at":"2026-09-04T14:20:29Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/integration-slack/src/lib.rs","source":"cargo test and clippy for integration-slack; connectors-runtime gate","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788531629000,"causation":"cmd-evidence-story-activate-hosted-slack-org-bot-test_result-1788531629436","change":{"change":"evidence","kind":"test_result","reference":"crates/integration-slack/src/lib.rs","source":"cargo test and clippy for integration-slack; connectors-runtime gate"},"correlation":"protocol-artifact-evidence","event_id":"story:activate-hosted-slack-org-bot@7#0~e269d04c0e2f0a74","executor":null,"recorded_at":"2026-09-04T14:20:29Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":7,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000256","to":"implemented"},"payload":{"actor":"human:timo","at":1788531629661,"causation":"cmd-move-story-page-gitlab-project-admission-1788531629661","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:page-gitlab-project-admission@7#0~c0be3e937b02cd86","executor":null,"recorded_at":"2026-09-04T14:20:29Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000271","to":"implemented"},"payload":{"actor":"human:timo","at":1788531629905,"causation":"cmd-move-story-preserve-claude-refresh-scope-1788531629905","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:preserve-claude-refresh-scope@5#0~ffa30e059cf63173","executor":null,"recorded_at":"2026-09-04T14:20:29Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000106","to":"implemented"},"payload":{"actor":"human:timo","at":1788531630135,"causation":"cmd-move-story-activate-hosted-slack-org-bot-1788531630135","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:activate-hosted-slack-org-bot@8#0~9d1bd86ada7b24fe","executor":null,"recorded_at":"2026-09-04T14:20:30Z"}}
{"entity":"story","version":1,"id":"page-gitlab-project-admission","revision":8,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/integration-gitlab/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/project_scan.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/integration-gitlab/Cargo.toml"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/backend_tests.rs"},{"confidence":"cited","path":"crates/integration-gitlab/src/project_scan.rs"}]},"command":"update-entity","target":"01MEM0000000000000256"},"payload":{"actor":"human:timo","at":1788531647924,"causation":"cmd-protocol-artifact-scope-story-page-gitlab-project-admission-1788531647924","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:page-gitlab-project-admission@8#0~4ef4e678b8bed26c","executor":null,"recorded_at":"2026-09-04T14:20:47Z"}}
{"entity":"story","version":1,"id":"preserve-claude-refresh-scope","revision":6,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/subscription-custody/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/subscription-custody/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000271"},"payload":{"actor":"human:timo","at":1788531648080,"causation":"cmd-protocol-artifact-scope-story-preserve-claude-refresh-scope-1788531648080","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:preserve-claude-refresh-scope@6#0~9a0eb3136aadb394","executor":null,"recorded_at":"2026-09-04T14:20:48Z"}}
{"entity":"story","version":1,"id":"activate-hosted-slack-org-bot","revision":9,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/connectors-config/src/hosted.rs"},{"confidence":"cited","path":"crates/connectors-runtime/src/composition.rs"},{"confidence":"cited","path":"crates/integration-slack/src/backend.rs"},{"confidence":"cited","path":"crates/integration-slack/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/connectors-config/src/hosted.rs"},{"confidence":"cited","path":"crates/connectors-runtime/src/composition.rs"},{"confidence":"cited","path":"crates/integration-slack/src/backend.rs"},{"confidence":"cited","path":"crates/integration-slack/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788531648232,"causation":"cmd-protocol-artifact-scope-story-activate-hosted-slack-org-bot-1788531648232","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:activate-hosted-slack-org-bot@9#0~ef5cf8fe859f2c13","executor":null,"recorded_at":"2026-09-04T14:20:48Z"}}
{"entity":"epic","version":1,"id":"cli-surface","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: the CLI surface is specified, not assembled\n\n## Outcome\n\nThe `connectors` command tree is a projection of an ESS specification rather than a hand-written\nparser with no contract beside it. Two consequences follow, and the epic is finished when both hold:\n\n1. A command cannot be added in the wrong place, given no target, or dropped, without the gate\n   saying so.\n2. A second frontend — web, TUI, MCP — reads the same declaration the command tree was generated\n   from, so it costs a projection rather than a rewrite.\n\n## Why now\n\nTwo defects share one cause, which is that nothing declares the surface.\n\n**No categorization.** 16 top-level commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at one level despite being five different activities. clap renders exactly one\n`Commands:` block per level (`clap_builder-4.6.6/src/output/help_template.rs:403-416`), so the\ngrouping has to be real nesting; there is no help-only shortcut.\n\n**No declared target.** `operation`, `connection` and `event` route by the absence of two flags\n(`crates/connectors-cli/src/lib.rs:729,789,871`). The same command line reaches a hosted deployment\nor the local socket depending on a file the operator did not name. Carried by\n`story:explicit-target-never-implicit`.\n\n## Shape\n\nESS gains a command-line surface — a third `reached_by` and a `cli:` block — and a fourth synthesis\ntarget beside `rust`, `go` and `web`, which already exist\n(`ess generate synthesize --target rust|go|web`). `connectors` is its first consumer: it draws its\ndomains from `crates/protocol/src/`, declares its groups, generates its parser and completions, and\nholds the committed tree byte-identical to a fresh generation.\n\n## Not in this epic\n\n- Building a second frontend on `--target web`. The epic makes it cheap; it does not spend it.\n- Renaming any `operation`, `connection` or `event` subcommand. Only the top level moves.\n- The `zwirn` pin bump and its ADR (`zwirn/crates/agent-app/src/main.rs:124` forwards argv verbatim).\n\n## Provenance\n\nFiled 2026-09-04 from a plan reviewed with the operator. Plan file:\n`~/.claude/plans/lazy-painting-turtle.md`.\n","title":"The CLI surface is specified, not assembled"},"args":{"command":"create-entity","data":{"body":"# Epic: the CLI surface is specified, not assembled\n\n## Outcome\n\nThe `connectors` command tree is a projection of an ESS specification rather than a hand-written\nparser with no contract beside it. Two consequences follow, and the epic is finished when both hold:\n\n1. A command cannot be added in the wrong place, given no target, or dropped, without the gate\n   saying so.\n2. A second frontend — web, TUI, MCP — reads the same declaration the command tree was generated\n   from, so it costs a projection rather than a rewrite.\n\n## Why now\n\nTwo defects share one cause, which is that nothing declares the surface.\n\n**No categorization.** 16 top-level commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at one level despite being five different activities. clap renders exactly one\n`Commands:` block per level (`clap_builder-4.6.6/src/output/help_template.rs:403-416`), so the\ngrouping has to be real nesting; there is no help-only shortcut.\n\n**No declared target.** `operation`, `connection` and `event` route by the absence of two flags\n(`crates/connectors-cli/src/lib.rs:729,789,871`). The same command line reaches a hosted deployment\nor the local socket depending on a file the operator did not name. Carried by\n`story:explicit-target-never-implicit`.\n\n## Shape\n\nESS gains a command-line surface — a third `reached_by` and a `cli:` block — and a fourth synthesis\ntarget beside `rust`, `go` and `web`, which already exist\n(`ess generate synthesize --target rust|go|web`). `connectors` is its first consumer: it draws its\ndomains from `crates/protocol/src/`, declares its groups, generates its parser and completions, and\nholds the committed tree byte-identical to a fresh generation.\n\n## Not in this epic\n\n- Building a second frontend on `--target web`. The epic makes it cheap; it does not spend it.\n- Renaming any `operation`, `connection` or `event` subcommand. Only the top level moves.\n- The `zwirn` pin bump and its ADR (`zwirn/crates/agent-app/src/main.rs:124` forwards argv verbatim).\n\n## Provenance\n\nFiled 2026-09-04 from a plan reviewed with the operator. Plan file:\n`~/.claude/plans/lazy-painting-turtle.md`.\n","status":"draft","title":"The CLI surface is specified, not assembled"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/cli-surface"},"payload":{"actor":"human:timo","at":1788512566353,"causation":"cmd-new-epic-cli-surface","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:cli-surface@1#0~87240faf41ad0bdc","executor":null,"recorded_at":"2026-09-04T09:02:46Z"}}
{"entity":"story","version":1,"id":"ess-command-line-reach","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: a component can say its callers are people at a terminal\n\n## Defect\n\n`ess/1` has two answers to *where are this component's callers*: `in_process` and `network`\n(`crates/specify/ess-domain/src/component.rs:142-156` at tag `0.13.5`). Neither describes a binary a\nperson types at. A CLI's callers are deployed with it, so `in_process` is nearly right and says the\nsurface never leaves the process — but the surface does leave it, as a grammar rather than a call,\nand nothing in the model can say so. The consequence is that a command-line surface has no contract\nESS can project, which is why `connectors` has 16 top-level commands and nothing that declares them.\n\n## Shape\n\n- A third `Reach` variant, `command_line`, stating the same kind of fact the other two state — who\n  issues the commands — and naming no wire, port, path or verb. The `Reach` doc comment's own rule\n  holds: which contract follows is derived, never chosen.\n- `Reach::ALL` and `Reach::as_str` (`:160-167`) gain it; the doc comment saying \"the two words\"\n  becomes three.\n- `RawComponentSpec` gains one optional `cli:` block: a `binary:` name, an ungrouped `commands:`\n  list, and `groups:` of `{name, summary, commands}`. It holds what cannot be derived — which\n  activity a command belongs to. Paths inside a group derive from `naming.wire`, as OpenAPI paths\n  already do.\n\n## Refusals\n\nFive, and they are the reason to model this rather than write a document beside the parser:\n\n| refused | why |\n|---|---|\n| `cli:` on a component that is not `reached_by: command_line` | it describes a surface the component does not have |\n| a command in `cli:` that is not in `accepts.commands` | the unknown-target refusal `relations:` already gives |\n| an accepted command in no group and not in `commands:` | the surface would be incomplete and nothing else would say so |\n| an accepted command in two groups | a command has one path; two answers is an undecided question written twice |\n| `reached_by: command_line` with no `cli:` block | nothing to project |\n\n## Format decision\n\n`ess/1`, not `ess/2`, on the precedent `reached_by` set: it serialises out when unset, so every\nexisting document digests exactly as before (`Reach::is_in_process` doc comment). `ess/AGENTS.md`\nrequires this be decided explicitly and requires an old-reader compatibility test before any\npersisted field, so both are part of this story:\n\n- a test proving a document using neither addition produces byte-identical IR;\n- the forward-incompatibility written down: an older `ess` binary refuses a document that uses\n  either, the same consequence `reached_by` had.\n\n## Acceptance\n\n- `ess validate` accepts a component declaring `reached_by: command_line` with a `cli:` block.\n- Each of the five refusals has a test that names it.\n- The old-reader test passes: IR bytes unchanged for documents without the new fields.\n- `task check` exits 0.\n\n## Where the work lands\n\n`github.com/beyond10x/ess`, not this repository. Filed here because\n`epic:cli-surface` spans both and `connectors` is the first consumer.\n","title":"A component can say its callers are people at a terminal"},"args":{"command":"create-entity","data":{"body":"# Story: a component can say its callers are people at a terminal\n\n## Defect\n\n`ess/1` has two answers to *where are this component's callers*: `in_process` and `network`\n(`crates/specify/ess-domain/src/component.rs:142-156` at tag `0.13.5`). Neither describes a binary a\nperson types at. A CLI's callers are deployed with it, so `in_process` is nearly right and says the\nsurface never leaves the process — but the surface does leave it, as a grammar rather than a call,\nand nothing in the model can say so. The consequence is that a command-line surface has no contract\nESS can project, which is why `connectors` has 16 top-level commands and nothing that declares them.\n\n## Shape\n\n- A third `Reach` variant, `command_line`, stating the same kind of fact the other two state — who\n  issues the commands — and naming no wire, port, path or verb. The `Reach` doc comment's own rule\n  holds: which contract follows is derived, never chosen.\n- `Reach::ALL` and `Reach::as_str` (`:160-167`) gain it; the doc comment saying \"the two words\"\n  becomes three.\n- `RawComponentSpec` gains one optional `cli:` block: a `binary:` name, an ungrouped `commands:`\n  list, and `groups:` of `{name, summary, commands}`. It holds what cannot be derived — which\n  activity a command belongs to. Paths inside a group derive from `naming.wire`, as OpenAPI paths\n  already do.\n\n## Refusals\n\nFive, and they are the reason to model this rather than write a document beside the parser:\n\n| refused | why |\n|---|---|\n| `cli:` on a component that is not `reached_by: command_line` | it describes a surface the component does not have |\n| a command in `cli:` that is not in `accepts.commands` | the unknown-target refusal `relations:` already gives |\n| an accepted command in no group and not in `commands:` | the surface would be incomplete and nothing else would say so |\n| an accepted command in two groups | a command has one path; two answers is an undecided question written twice |\n| `reached_by: command_line` with no `cli:` block | nothing to project |\n\n## Format decision\n\n`ess/1`, not `ess/2`, on the precedent `reached_by` set: it serialises out when unset, so every\nexisting document digests exactly as before (`Reach::is_in_process` doc comment). `ess/AGENTS.md`\nrequires this be decided explicitly and requires an old-reader compatibility test before any\npersisted field, so both are part of this story:\n\n- a test proving a document using neither addition produces byte-identical IR;\n- the forward-incompatibility written down: an older `ess` binary refuses a document that uses\n  either, the same consequence `reached_by` had.\n\n## Acceptance\n\n- `ess validate` accepts a component declaring `reached_by: command_line` with a `cli:` block.\n- Each of the five refusals has a test that names it.\n- The old-reader test passes: IR bytes unchanged for documents without the new fields.\n- `task check` exits 0.\n\n## Where the work lands\n\n`github.com/beyond10x/ess`, not this repository. Filed here because\n`epic:cli-surface` spans both and `connectors` is the first consumer.\n","status":"draft","title":"A component can say its callers are people at a terminal"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/ess-command-line-reach"},"payload":{"actor":"human:timo","at":1788512596681,"causation":"cmd-new-story-ess-command-line-reach","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:ess-command-line-reach@1#0~6645ab92fffa11ca","executor":null,"recorded_at":"2026-09-04T09:03:16Z"}}
{"entity":"story","version":1,"id":"ess-command-line-reach","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000523","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788512596693,"causation":"cmd-story-ess-command-line-reach-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:ess-command-line-reach@1#0~9cd9b0af4957fcb8","executor":null,"recorded_at":"2026-09-04T09:03:16Z"}}
{"entity":"story","version":1,"id":"ess-clap-target","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: ESS synthesizes a clap parser and its completions\n\n## Defect\n\n`ess generate synthesize --target rust|go|web` already turns one IR into three implementations, and\n`web` is a whole frontend (`crates/generate/ess-synth/src/web/` — `catalog.rs`, `page.rs`,\n`layout.rs`, `bridge.rs`, `refusal.rs`). A command-line surface has no target, so a CLI is the one\nfrontend that stays hand-written, drifts from the specification, and cannot be reproduced from it.\n\n## Shape\n\n- `crates/generate/ess-synth/src/clap/`, beside `rust/`, `go/` and `web/`, in the same module shape\n  those three share: `layout.rs`, `name.rs`, `items.rs`, `obligation.rs`, `refusal.rs`, `port.rs`.\n- `clap` joins the `--target` value list.\n\nWhat it emits, from what:\n\n| artifact | from |\n|---|---|\n| the clap derive tree: root `Parser`, one `Subcommand` enum per group | `cli:` groups, `naming.wire` |\n| one typed arg per command input field | command `input:` |\n| a `completions` subcommand wired to `clap_complete` for every shell | the `binary:` declaration |\n| exit conditions, error variants included | command `outcomes:`, `errors:` |\n| a `Handler` trait, one method per command | the obligation model `rust/obligation.rs` already uses |\n\nThe generated half is the grammar. Handler bodies stay hand-written and are reported as obligations,\nwhich is the existing synth contract rather than a new one.\n\n## Why it is worth more than a document\n\n`--target web` reads the same IR. Once `cli:` declares the groups, a web frontend gets its navigation\nfrom the declaration that produced the command tree, and a third frontend costs a projection rather\nthan a rewrite. That is the property a hand-written parser with a contract document beside it does\nnot have.\n\n## Acceptance\n\n- `ess generate synthesize --target clap --out <dir>` run twice produces identical bytes.\n- The generated tree compiles and its `--help` shows one entry per declared group.\n- Completions are generated, not hand-maintained.\n- Every command with no handler is reported as an obligation, by name.\n- `task check` exits 0.\n\n## Depends on\n\n`story:ess-command-line-reach` — there is nothing to project until `cli:` exists.\n\n## Where the work lands\n\n`github.com/beyond10x/ess`, not this repository.\n","title":"ESS synthesizes a clap parser and its completions"},"args":{"command":"create-entity","data":{"body":"# Story: ESS synthesizes a clap parser and its completions\n\n## Defect\n\n`ess generate synthesize --target rust|go|web` already turns one IR into three implementations, and\n`web` is a whole frontend (`crates/generate/ess-synth/src/web/` — `catalog.rs`, `page.rs`,\n`layout.rs`, `bridge.rs`, `refusal.rs`). A command-line surface has no target, so a CLI is the one\nfrontend that stays hand-written, drifts from the specification, and cannot be reproduced from it.\n\n## Shape\n\n- `crates/generate/ess-synth/src/clap/`, beside `rust/`, `go/` and `web/`, in the same module shape\n  those three share: `layout.rs`, `name.rs`, `items.rs`, `obligation.rs`, `refusal.rs`, `port.rs`.\n- `clap` joins the `--target` value list.\n\nWhat it emits, from what:\n\n| artifact | from |\n|---|---|\n| the clap derive tree: root `Parser`, one `Subcommand` enum per group | `cli:` groups, `naming.wire` |\n| one typed arg per command input field | command `input:` |\n| a `completions` subcommand wired to `clap_complete` for every shell | the `binary:` declaration |\n| exit conditions, error variants included | command `outcomes:`, `errors:` |\n| a `Handler` trait, one method per command | the obligation model `rust/obligation.rs` already uses |\n\nThe generated half is the grammar. Handler bodies stay hand-written and are reported as obligations,\nwhich is the existing synth contract rather than a new one.\n\n## Why it is worth more than a document\n\n`--target web` reads the same IR. Once `cli:` declares the groups, a web frontend gets its navigation\nfrom the declaration that produced the command tree, and a third frontend costs a projection rather\nthan a rewrite. That is the property a hand-written parser with a contract document beside it does\nnot have.\n\n## Acceptance\n\n- `ess generate synthesize --target clap --out <dir>` run twice produces identical bytes.\n- The generated tree compiles and its `--help` shows one entry per declared group.\n- Completions are generated, not hand-maintained.\n- Every command with no handler is reported as an obligation, by name.\n- `task check` exits 0.\n\n## Depends on\n\n`story:ess-command-line-reach` — there is nothing to project until `cli:` exists.\n\n## Where the work lands\n\n`github.com/beyond10x/ess`, not this repository.\n","status":"draft","title":"ESS synthesizes a clap parser and its completions"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/ess-clap-target"},"payload":{"actor":"human:timo","at":1788512609612,"causation":"cmd-new-story-ess-clap-target","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:ess-clap-target@1#0~1499fe201e6ddc84","executor":null,"recorded_at":"2026-09-04T09:03:29Z"}}
{"entity":"story","version":1,"id":"ess-clap-target","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000528","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788512609624,"causation":"cmd-story-ess-clap-target-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:ess-clap-target@1#0~9d9b01f8594d80fb","executor":null,"recorded_at":"2026-09-04T09:03:29Z"}}
{"entity":"story","version":1,"id":"ess-clap-target","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"},{"depends_on":"story:ess-command-line-reach"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000528","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788512609633,"causation":"cmd-story-ess-clap-target-depends_on-story-ess-command-line-reach","change":{"change":"related","relation":"depends_on","target":"story:ess-command-line-reach"},"correlation":"protocol-artifact-relate","event_id":"story:ess-clap-target@1#0~0fb0a25d11bd6e66","executor":null,"recorded_at":"2026-09-04T09:03:29Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: the connectors domains get a typed home\n\n## Defect\n\nThis repository has 44 crates, a 450-line domain model and no ESS specification —\n`find -name system.yaml` returns six across `beyond10x` and none here. The nouns exist twice\ninstead: as prose in `docs/design/01-domain-model.md`, and as Rust enums in `crates/protocol/src/`.\nNothing checks that the two agree, and nothing can project either.\n\n## The states are citable, not invented\n\n| entity | states | source |\n|---|---|---|\n| `Connection` | Created, Authorized, Callable, Degraded, Revoked | `crates/protocol/src/connection.rs:112-117` |\n| `Channel` | Starting, Connected, Reconnecting, Stopped | `connection.rs:147-151` |\n| `DiscoveryObservation` | Observed, Unsupported, Materialized, Withdrawn | `connection.rs:212-216` |\n| `ConnectionCandidate` | Detected, Activated | `connection.rs:221-223` |\n| `ConnectSession` | Pending, Completed, Expired, Failed | `connection.rs:259-263` |\n| `Session` (operation) | Establishing, Established, Terminating, Terminated, OutcomeUnknown | `operation.rs:194-199` |\n| `SipDial` | read at model time | `sip.rs:122` |\n| `Provider`, `Operation`, `Catalog`, `Integration`, `Credential`, `Grant`, `Invocation`, `Proxy`, `Event`, `Webhook`, `Delivery`, `Subscription`, `Audit` | no state enum, no drawn lifecycle | `docs/design/01-domain-model.md:109-411` |\n\n## The constraint that sizes this\n\nESS refuses a transition no command outcome causes (`missing_causation`), so a list of states is not\na lifecycle. Only `Connection` has named transitions anywhere:\n`created ──authorize──▶ authorized ──verify──▶ callable`, plus `reauthorize` and `revoke`\n(`docs/design/01-domain-model.md:198-201`).\n\nSo, per entity:\n\n- **Transitions stated** → model them, cite the line.\n- **States but no transitions** → read the transitions from the code that performs the state change,\n  or give the entity one state and an `UNMAPPED:` marker naming what would settle it.\n- **Neither** → a single-state lifecycle, which is what the specification can honestly say today.\n\nNo guessed `owns` or `references`, no guessed cardinality. Every one of those is an `UNMAPPED:`\nmarker and is named again in the report, because one-to-many and one-to-one project different\nschemas and imply different stories.\n\n## Shape\n\n`ess/system/system.yaml`, `ess/system/components.yaml`, `ess/system/domains/*.yaml`, laid out like\n`devcenter/ess/system/`. `ess validate` after each entity, not at the end.\n\n`connectors.target.Target` is drafted here, because `story:explicit-target-never-implicit`\nintroduces the noun and guardrail 7 of the `aep-planning:planning` skill says a story introducing a\nnoun models it first.\n\n## Acceptance\n\n- `ess validate --path ess/system` exits 0.\n- Every entity in the table above is present, or its absence is stated with a reason.\n- Every state in the specification cites a `path:line` in `crates/protocol/src/` or\n  `docs/design/01-domain-model.md`.\n- Every `UNMAPPED:` marker names what would settle it, and the count is reported.\n\n## Depends on\n\n`story:ess-clap-target` — the specification is written against the ESS version that has the\ncommand-line construct, and validating it needs that binary installed.\n","title":"The connectors domains get a typed home"},"args":{"command":"create-entity","data":{"body":"# Story: the connectors domains get a typed home\n\n## Defect\n\nThis repository has 44 crates, a 450-line domain model and no ESS specification —\n`find -name system.yaml` returns six across `beyond10x` and none here. The nouns exist twice\ninstead: as prose in `docs/design/01-domain-model.md`, and as Rust enums in `crates/protocol/src/`.\nNothing checks that the two agree, and nothing can project either.\n\n## The states are citable, not invented\n\n| entity | states | source |\n|---|---|---|\n| `Connection` | Created, Authorized, Callable, Degraded, Revoked | `crates/protocol/src/connection.rs:112-117` |\n| `Channel` | Starting, Connected, Reconnecting, Stopped | `connection.rs:147-151` |\n| `DiscoveryObservation` | Observed, Unsupported, Materialized, Withdrawn | `connection.rs:212-216` |\n| `ConnectionCandidate` | Detected, Activated | `connection.rs:221-223` |\n| `ConnectSession` | Pending, Completed, Expired, Failed | `connection.rs:259-263` |\n| `Session` (operation) | Establishing, Established, Terminating, Terminated, OutcomeUnknown | `operation.rs:194-199` |\n| `SipDial` | read at model time | `sip.rs:122` |\n| `Provider`, `Operation`, `Catalog`, `Integration`, `Credential`, `Grant`, `Invocation`, `Proxy`, `Event`, `Webhook`, `Delivery`, `Subscription`, `Audit` | no state enum, no drawn lifecycle | `docs/design/01-domain-model.md:109-411` |\n\n## The constraint that sizes this\n\nESS refuses a transition no command outcome causes (`missing_causation`), so a list of states is not\na lifecycle. Only `Connection` has named transitions anywhere:\n`created ──authorize──▶ authorized ──verify──▶ callable`, plus `reauthorize` and `revoke`\n(`docs/design/01-domain-model.md:198-201`).\n\nSo, per entity:\n\n- **Transitions stated** → model them, cite the line.\n- **States but no transitions** → read the transitions from the code that performs the state change,\n  or give the entity one state and an `UNMAPPED:` marker naming what would settle it.\n- **Neither** → a single-state lifecycle, which is what the specification can honestly say today.\n\nNo guessed `owns` or `references`, no guessed cardinality. Every one of those is an `UNMAPPED:`\nmarker and is named again in the report, because one-to-many and one-to-one project different\nschemas and imply different stories.\n\n## Shape\n\n`ess/system/system.yaml`, `ess/system/components.yaml`, `ess/system/domains/*.yaml`, laid out like\n`devcenter/ess/system/`. `ess validate` after each entity, not at the end.\n\n`connectors.target.Target` is drafted here, because `story:explicit-target-never-implicit`\nintroduces the noun and guardrail 7 of the `aep-planning:planning` skill says a story introducing a\nnoun models it first.\n\n## Acceptance\n\n- `ess validate --path ess/system` exits 0.\n- Every entity in the table above is present, or its absence is stated with a reason.\n- Every state in the specification cites a `path:line` in `crates/protocol/src/` or\n  `docs/design/01-domain-model.md`.\n- Every `UNMAPPED:` marker names what would settle it, and the count is reported.\n\n## Depends on\n\n`story:ess-clap-target` — the specification is written against the ESS version that has the\ncommand-line construct, and validating it needs that binary installed.\n","status":"draft","title":"The connectors domains get a typed home"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/connectors-ess-domain"},"payload":{"actor":"human:timo","at":1788512627669,"causation":"cmd-new-story-connectors-ess-domain","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:connectors-ess-domain@1#0~7c81032a90a04b15","executor":null,"recorded_at":"2026-09-04T09:03:47Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000535","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788512627680,"causation":"cmd-story-connectors-ess-domain-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:connectors-ess-domain@1#0~64190b0cb2e040f5","executor":null,"recorded_at":"2026-09-04T09:03:47Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"},{"depends_on":"story:ess-clap-target"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000535","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788512627688,"causation":"cmd-story-connectors-ess-domain-depends_on-story-ess-clap-target","change":{"change":"related","relation":"depends_on","target":"story:ess-clap-target"},"correlation":"protocol-artifact-relate","event_id":"story:connectors-ess-domain@1#0~1773a8bd27700244","executor":null,"recorded_at":"2026-09-04T09:03:47Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: the command tree is generated, and the gate holds it there\n\n## Defect\n\n`crates/connectors-cli/src/lib.rs` declares 16 top-level commands and 14 subcommands by hand. No\nartifact says what the surface is, so nothing catches a command added in the wrong place, a command\ndropped, or a command whose target nobody declared. The completions script is already generated from\nthe parser (`lib.rs:170-179`), which proves the parser can be a source — but the parser itself is\nthe source of record, and it answers to nothing.\n\n## Shape\n\n- `ess generate synthesize --target clap --out crates/connectors-cli/src/generated/`, committed.\n- `crates/connectors-cli/src/lib.rs` gains `pub fn command() -> clap::Command { Cli::command() }`,\n  reusing what the `Completions` arm already builds.\n- `crates/connectors-cli/tests/cli_surface.rs`, dev-deps `serde_norway` + `serde`\n  (`Cargo.toml:132`). `tests/` is excluded from the CLI line fence and `[dev-dependencies]` is not\n  fenced (`crates/catalog-build/tests/main/architecture_fence.rs:592-606`, `:559-570`), so this\n  costs no fence headroom.\n- `scripts/gate.sh --final` gains `ess validate --path ess/system` and the regeneration check.\n- `docs/design/19-the-cli-surface.md`: why these groups, the target rule, and that this repository is\n  the first consumer of the ESS command-line construct. The series runs to 18.\n\n## What the test asserts\n\n1. The committed generated tree is byte-identical to a fresh `ess generate synthesize`. A stale\n   contract fails the gate.\n2. Every command in `connectors_cli::command()` is in the specification, and every command in the\n   specification is in the tree. Both directions, so neither file can drift alone.\n3. `operation`, `connection` and `event` carry `--target`. Until\n   `story:explicit-target-never-implicit` lands they are a named allowlist of exactly three\n   exceptions, and the test fails when the list is any other size — a countdown that reaches zero\n   when that story is done.\n\n## Adoption is incremental\n\nThe generated tree lands first as a checked parallel artifact. The hand-written enums are replaced\none group at a time, with the gate green between each. A single cutover of all 16 commands is the\nversion of this that fails at 2 a.m.\n\n## Acceptance\n\n- `cargo test -p connectors-cli --locked` fails when a command is added to the parser and not the\n  specification, and when the committed tree is stale. Both proven by a deliberate edit, reverted.\n- `bash scripts/gate.sh --workspace crates/connectors-cli` exits 0.\n- `connectors setup completions fish` is generated rather than hand-maintained.\n\n## Depends on\n\n`story:connectors-ess-domain`.\n","title":"The command tree is generated, and the gate holds it there"},"args":{"command":"create-entity","data":{"body":"# Story: the command tree is generated, and the gate holds it there\n\n## Defect\n\n`crates/connectors-cli/src/lib.rs` declares 16 top-level commands and 14 subcommands by hand. No\nartifact says what the surface is, so nothing catches a command added in the wrong place, a command\ndropped, or a command whose target nobody declared. The completions script is already generated from\nthe parser (`lib.rs:170-179`), which proves the parser can be a source — but the parser itself is\nthe source of record, and it answers to nothing.\n\n## Shape\n\n- `ess generate synthesize --target clap --out crates/connectors-cli/src/generated/`, committed.\n- `crates/connectors-cli/src/lib.rs` gains `pub fn command() -> clap::Command { Cli::command() }`,\n  reusing what the `Completions` arm already builds.\n- `crates/connectors-cli/tests/cli_surface.rs`, dev-deps `serde_norway` + `serde`\n  (`Cargo.toml:132`). `tests/` is excluded from the CLI line fence and `[dev-dependencies]` is not\n  fenced (`crates/catalog-build/tests/main/architecture_fence.rs:592-606`, `:559-570`), so this\n  costs no fence headroom.\n- `scripts/gate.sh --final` gains `ess validate --path ess/system` and the regeneration check.\n- `docs/design/19-the-cli-surface.md`: why these groups, the target rule, and that this repository is\n  the first consumer of the ESS command-line construct. The series runs to 18.\n\n## What the test asserts\n\n1. The committed generated tree is byte-identical to a fresh `ess generate synthesize`. A stale\n   contract fails the gate.\n2. Every command in `connectors_cli::command()` is in the specification, and every command in the\n   specification is in the tree. Both directions, so neither file can drift alone.\n3. `operation`, `connection` and `event` carry `--target`. Until\n   `story:explicit-target-never-implicit` lands they are a named allowlist of exactly three\n   exceptions, and the test fails when the list is any other size — a countdown that reaches zero\n   when that story is done.\n\n## Adoption is incremental\n\nThe generated tree lands first as a checked parallel artifact. The hand-written enums are replaced\none group at a time, with the gate green between each. A single cutover of all 16 commands is the\nversion of this that fails at 2 a.m.\n\n## Acceptance\n\n- `cargo test -p connectors-cli --locked` fails when a command is added to the parser and not the\n  specification, and when the committed tree is stale. Both proven by a deliberate edit, reverted.\n- `bash scripts/gate.sh --workspace crates/connectors-cli` exits 0.\n- `connectors setup completions fish` is generated rather than hand-maintained.\n\n## Depends on\n\n`story:connectors-ess-domain`.\n","status":"draft","title":"The command tree is generated, and the gate holds it there"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/cli-surface-contract"},"payload":{"actor":"human:timo","at":1788512642516,"causation":"cmd-new-story-cli-surface-contract","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:cli-surface-contract@1#0~109ab73dee09ec94","executor":null,"recorded_at":"2026-09-04T09:04:02Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000542","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788512642526,"causation":"cmd-story-cli-surface-contract-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:cli-surface-contract@1#0~f757f0387d2e67f3","executor":null,"recorded_at":"2026-09-04T09:04:02Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"},{"depends_on":"story:connectors-ess-domain"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000542","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788512642535,"causation":"cmd-story-cli-surface-contract-depends_on-story-connectors-ess-domain","change":{"change":"related","relation":"depends_on","target":"story:connectors-ess-domain"},"correlation":"protocol-artifact-relate","event_id":"story:cli-surface-contract@1#0~790c048db46a860a","executor":null,"recorded_at":"2026-09-04T09:04:02Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: the top level reads as categories, not as sixteen commands\n\n## Defect\n\n`connectors --help` lists 16 commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at the same level despite being five different activities, so the help output does\nnot tell an operator which of them they want.\n\nThere is no help-only fix. clap 4.6.6 renders exactly one `Commands:` section per level:\n`subcommand_help_heading` renames that section and does not partition it\n(`clap_builder-4.6.6/src/output/help_template.rs:403-416`), and `flatten_help` emits one heading per\nleaf with its arguments rather than a grouped index (`:878-936`). The grouping has to be real\nnesting.\n\n## Shape\n\nTop level goes 16 to 8. The three everyday verbs keep their paths, because they are the point of the\ntool and lengthening them costs every invocation.\n\n| group | members | was |\n|---|---|---|\n| `setup` | `init`, `connect`, `completions` | `connectors init`, `connect`, `completions` |\n| `inspect` | `doctor`, `providers`, `auth` | `connectors doctor`, `providers`, `auth status` |\n| `session` | `login`, `logout` | `connectors login`, `logout` |\n| `serve` | `local`, `hosted`, `mcp` | `connectors serve`, `serve-hosted`, `mcp` |\n\n`operation`, `connection`, `event` and `admin` stay at the top level.\n\n`auth` collapses: `AuthCommand` (`lib.rs:182-191`) has exactly one variant, so `connectors inspect\nauth` replaces `connectors auth status` rather than becoming a third level.\n\nFour subcommand enums hold the moved variants verbatim — the variants move, they are not copied.\n\n## Compatibility\n\nOld paths keep working for one release. In `run_from` (`lib.rs:430`) a const table maps a legacy\nfirst token to its group pair before clap is handed the argv, and one stderr line names the new\npath. It rewrites `argv[1]` and never inspects a flag: clap remains the only parser, and the repeated\nrule that a Rust CLI uses clap derive is not bent by it.\n\nThe table is the same one the contract test reads, so the shim cannot drift from the specification.\n\n## Fence\n\n`CLI_TOTAL_LINE_LIMIT` is 1006 and `crates/connectors-cli/src/lib.rs` is at 998\n(`crates/catalog-build/tests/main/architecture_fence.rs:33`). Raise it with a dated reason comment,\nin the form the `966 → 1006` completions bump set, to what the build reports rather than to an\nestimate.\n\n## Cross-repository\n\n`zwirn` forwards argv verbatim into `connectors_cli::run_from`\n(`zwirn/crates/agent-app/src/main.rs:124`) and pins this repository at rev `1e0eb9f`\n(`zwirn/Cargo.toml:25`), so `zwirn connectors …` changes only when that pin moves. Renaming something\nanother repository verifies is a coordinated migration with an ADR (`AGENTS.md:7-8`); the ADR and\nthe pin bump are a follow-up, named in the report, not a blocker for this story.\n\n## Acceptance\n\n- `connectors --help` lists 8 commands.\n- `connectors doctor` produces the same output as `connectors inspect doctor`, plus one stderr line\n  naming the new path.\n- The contract test passes, so the specification and the tree agree after the move.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-surface-contract`.\n","title":"The top level reads as categories, not as sixteen commands"},"args":{"command":"create-entity","data":{"body":"# Story: the top level reads as categories, not as sixteen commands\n\n## Defect\n\n`connectors --help` lists 16 commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at the same level despite being five different activities, so the help output does\nnot tell an operator which of them they want.\n\nThere is no help-only fix. clap 4.6.6 renders exactly one `Commands:` section per level:\n`subcommand_help_heading` renames that section and does not partition it\n(`clap_builder-4.6.6/src/output/help_template.rs:403-416`), and `flatten_help` emits one heading per\nleaf with its arguments rather than a grouped index (`:878-936`). The grouping has to be real\nnesting.\n\n## Shape\n\nTop level goes 16 to 8. The three everyday verbs keep their paths, because they are the point of the\ntool and lengthening them costs every invocation.\n\n| group | members | was |\n|---|---|---|\n| `setup` | `init`, `connect`, `completions` | `connectors init`, `connect`, `completions` |\n| `inspect` | `doctor`, `providers`, `auth` | `connectors doctor`, `providers`, `auth status` |\n| `session` | `login`, `logout` | `connectors login`, `logout` |\n| `serve` | `local`, `hosted`, `mcp` | `connectors serve`, `serve-hosted`, `mcp` |\n\n`operation`, `connection`, `event` and `admin` stay at the top level.\n\n`auth` collapses: `AuthCommand` (`lib.rs:182-191`) has exactly one variant, so `connectors inspect\nauth` replaces `connectors auth status` rather than becoming a third level.\n\nFour subcommand enums hold the moved variants verbatim — the variants move, they are not copied.\n\n## Compatibility\n\nOld paths keep working for one release. In `run_from` (`lib.rs:430`) a const table maps a legacy\nfirst token to its group pair before clap is handed the argv, and one stderr line names the new\npath. It rewrites `argv[1]` and never inspects a flag: clap remains the only parser, and the repeated\nrule that a Rust CLI uses clap derive is not bent by it.\n\nThe table is the same one the contract test reads, so the shim cannot drift from the specification.\n\n## Fence\n\n`CLI_TOTAL_LINE_LIMIT` is 1006 and `crates/connectors-cli/src/lib.rs` is at 998\n(`crates/catalog-build/tests/main/architecture_fence.rs:33`). Raise it with a dated reason comment,\nin the form the `966 → 1006` completions bump set, to what the build reports rather than to an\nestimate.\n\n## Cross-repository\n\n`zwirn` forwards argv verbatim into `connectors_cli::run_from`\n(`zwirn/crates/agent-app/src/main.rs:124`) and pins this repository at rev `1e0eb9f`\n(`zwirn/Cargo.toml:25`), so `zwirn connectors …` changes only when that pin moves. Renaming something\nanother repository verifies is a coordinated migration with an ADR (`AGENTS.md:7-8`); the ADR and\nthe pin bump are a follow-up, named in the report, not a blocker for this story.\n\n## Acceptance\n\n- `connectors --help` lists 8 commands.\n- `connectors doctor` produces the same output as `connectors inspect doctor`, plus one stderr line\n  naming the new path.\n- The contract test passes, so the specification and the tree agree after the move.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-surface-contract`.\n","status":"draft","title":"The top level reads as categories, not as sixteen commands"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/cli-first-level-groups"},"payload":{"actor":"human:timo","at":1788512660384,"causation":"cmd-new-story-cli-first-level-groups","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:cli-first-level-groups@1#0~32e17b08a688d933","executor":null,"recorded_at":"2026-09-04T09:04:20Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000549","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788512660401,"causation":"cmd-story-cli-first-level-groups-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:cli-first-level-groups@1#0~816ef0fd816f1ec0","executor":null,"recorded_at":"2026-09-04T09:04:20Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"},{"depends_on":"story:cli-surface-contract"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000549","target":"01MEM0000000000000136"},"payload":{"actor":"human:timo","at":1788512660411,"causation":"cmd-story-cli-first-level-groups-depends_on-story-cli-surface-contract","change":{"change":"related","relation":"depends_on","target":"story:cli-surface-contract"},"correlation":"protocol-artifact-relate","event_id":"story:cli-first-level-groups@1#0~5ab319852beac108","executor":null,"recorded_at":"2026-09-04T09:04:20Z"}}
{"entity":"story","version":1,"id":"explicit-target-never-implicit","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000190","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788512664664,"causation":"cmd-story-explicit-target-never-implicit-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:explicit-target-never-implicit@1#0~7c3b087c5b434546","executor":null,"recorded_at":"2026-09-04T09:04:24Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"ess/system/system.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"ess/system/system.yaml"}]},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788512672425,"causation":"cmd-protocol-artifact-scope-story-connectors-ess-domain-1788512672425","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:connectors-ess-domain@2#0~8aa9d8e5272761b1","executor":null,"recorded_at":"2026-09-04T09:04:32Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788512672568,"causation":"cmd-protocol-artifact-scope-story-connectors-ess-domain-1788512672568","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:connectors-ess-domain@3#0~da39cdaa7e382868","executor":null,"recorded_at":"2026-09-04T09:04:32Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788512672704,"causation":"cmd-protocol-artifact-scope-story-connectors-ess-domain-1788512672704","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:connectors-ess-domain@4#0~3e3c82581a1119c7","executor":null,"recorded_at":"2026-09-04T09:04:32Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"}]},"command":"update-entity","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788512672841,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788512672841","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@2#0~a7dba42432e7d31a","executor":null,"recorded_at":"2026-09-04T09:04:32Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"}]},"command":"update-entity","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788512672979,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788512672979","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@3#0~cfdeb804ed99802f","executor":null,"recorded_at":"2026-09-04T09:04:32Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"}]},"command":"update-entity","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788512673117,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788512673117","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@4#0~025d7b7fe0819b40","executor":null,"recorded_at":"2026-09-04T09:04:33Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788512673255,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788512673255","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@5#0~7de78b783d764ed9","executor":null,"recorded_at":"2026-09-04T09:04:33Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788512673391,"causation":"cmd-protocol-artifact-scope-story-cli-first-level-groups-1788512673391","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-first-level-groups@2#0~c8a278064189413a","executor":null,"recorded_at":"2026-09-04T09:04:33Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788512673530,"causation":"cmd-protocol-artifact-scope-story-cli-first-level-groups-1788512673530","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-first-level-groups@3#0~024a48355867205c","executor":null,"recorded_at":"2026-09-04T09:04:33Z"}}
{"entity":"story","version":1,"id":"explicit-target-never-implicit","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000190"},"payload":{"actor":"human:timo","at":1788512673667,"causation":"cmd-protocol-artifact-scope-story-explicit-target-never-implicit-1788512673667","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:explicit-target-never-implicit@2#0~a668c538e8bebf25","executor":null,"recorded_at":"2026-09-04T09:04:33Z"}}
{"entity":"story","version":1,"id":"ess-command-line-reach","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000187","to":"proposed"},"payload":{"actor":"human:timo","at":1788516562028,"causation":"cmd-move-story-ess-command-line-reach-1788516562028","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:ess-command-line-reach@2#0~73498f666891bea2","executor":null,"recorded_at":"2026-09-04T10:09:22Z"}}
{"entity":"story","version":1,"id":"ess-command-line-reach","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000187","to":"active"},"payload":{"actor":"human:timo","at":1788516562375,"causation":"cmd-move-story-ess-command-line-reach-1788516562375","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:ess-command-line-reach@3#0~e274432061c135e6","executor":null,"recorded_at":"2026-09-04T10:09:22Z"}}
{"entity":"story","version":1,"id":"ess-command-line-reach","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/releases/tag/0.14.0","source":"ESS 0.14.0 release run 33860008863: gate green, tag 0.14.0 published 2026-09-04T10:01:53Z; verified by running the released binary","target":"01MEM0000000000000187"},"payload":{"actor":"human:timo","at":1788516113000,"causation":"cmd-evidence-story-ess-command-line-reach-test_result-1788516562637","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/releases/tag/0.14.0","source":"ESS 0.14.0 release run 33860008863: gate green, tag 0.14.0 published 2026-09-04T10:01:53Z; verified by running the released binary"},"correlation":"protocol-artifact-evidence","event_id":"story:ess-command-line-reach@3#0~88f7a952cf5db5e1","executor":null,"recorded_at":"2026-09-04T10:01:53Z"}}
{"entity":"story","version":1,"id":"ess-command-line-reach","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000187","to":"implemented"},"payload":{"actor":"human:timo","at":1788516562967,"causation":"cmd-move-story-ess-command-line-reach-1788516562967","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:ess-command-line-reach@4#0~c03b993489cb07c3","executor":null,"recorded_at":"2026-09-04T10:09:22Z"}}
{"entity":"story","version":1,"id":"ess-clap-target","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000184","to":"proposed"},"payload":{"actor":"human:timo","at":1788516563291,"causation":"cmd-move-story-ess-clap-target-1788516563291","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:ess-clap-target@2#0~51f1757cc2254e8b","executor":null,"recorded_at":"2026-09-04T10:09:23Z"}}
{"entity":"story","version":1,"id":"ess-clap-target","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000184","to":"active"},"payload":{"actor":"human:timo","at":1788516563713,"causation":"cmd-move-story-ess-clap-target-1788516563713","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:ess-clap-target@3#0~3aafeceb4d938ea3","executor":null,"recorded_at":"2026-09-04T10:09:23Z"}}
{"entity":"story","version":1,"id":"ess-clap-target","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/releases/tag/0.14.0","source":"ESS 0.14.0 release run 33860008863: gate green, tag 0.14.0 published 2026-09-04T10:01:53Z; verified by running the released binary","target":"01MEM0000000000000184"},"payload":{"actor":"human:timo","at":1788516113000,"causation":"cmd-evidence-story-ess-clap-target-test_result-1788516563959","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/releases/tag/0.14.0","source":"ESS 0.14.0 release run 33860008863: gate green, tag 0.14.0 published 2026-09-04T10:01:53Z; verified by running the released binary"},"correlation":"protocol-artifact-evidence","event_id":"story:ess-clap-target@3#0~a2f42052de15bc14","executor":null,"recorded_at":"2026-09-04T10:01:53Z"}}
{"entity":"story","version":1,"id":"ess-clap-target","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000184","to":"implemented"},"payload":{"actor":"human:timo","at":1788516564316,"causation":"cmd-move-story-ess-clap-target-1788516564316","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:ess-clap-target@4#0~04a1b7ea6ad43508","executor":null,"recorded_at":"2026-09-04T10:09:24Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":5,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000145","to":"proposed"},"payload":{"actor":"human:timo","at":1788516938580,"causation":"cmd-move-story-connectors-ess-domain-1788516938580","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:connectors-ess-domain@5#0~f2acbc5213bbefc0","executor":null,"recorded_at":"2026-09-04T10:15:38Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":6,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000145","to":"active"},"payload":{"actor":"human:timo","at":1788516938782,"causation":"cmd-move-story-connectors-ess-domain-1788516938782","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:connectors-ess-domain@6#0~a9323774028e1c2c","executor":null,"recorded_at":"2026-09-04T10:15:38Z"}}
{"entity":"review-result","version":1,"id":"adversary-ess-domain-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 1 — `story:connectors-ess-domain`\n\nWorktree `wt-2a8ad5b88c79`, branch `impl/connectors-ess-domain`, base `7c66c54` plus the untracked\n`ess/` tree. `adp:adversary`. 191,533 tokens, 82 tool uses, 1,166 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 57->62, red 5\norigin: introduced 10, pre-existing 1, undecided 0\nwrote-outside-worktree: 6 paths (all under the assigned scratch root)\nneeds-coordinator: no\n```\n\n## What the attack was\n\nThe specification's central claim is that every state, transition and field is read from this\nrepository and cited by `path:line`, and that everything unreadable carries an `UNMAPPED:` marker\nnaming what would settle it. The adversary attacked the claim by **opening the citations**, and\nwrote a fence — `crates/catalog-build/tests/main/ess_citation_fence.rs`, 337 lines, 5 cases — that\nopens them mechanically.\n\nThe load-bearing observation: `ess validate` and `bash scripts/gate.sh --final` both stay green\nwith all eleven findings standing. **Nothing in either opens a cited line.** A citation is a claim\nno existing check reads, which is why five of the findings are only reachable by a test that was\nwritten to read them.\n\n## What held\n\n- 49 top-level enums under `crates/protocol/src/`; exactly 7 named `*State`, all 7 modelled.\n- Every enum-variant citation matches its cited range exactly, across 8 enums.\n- 48 distinct `docs/design/01-domain-model.md` ranges opened line by line; all correct but one.\n- All 36 `UNMAPPED:` markers carry a settle clause — 0 without, matching the unit's claim.\n- `ess validate` twice: identical. `ess specify compile --format json` twice: 116,158 bytes,\n  byte-identical.\n- The `Establishing` and `OutcomeUnknown` markers are honest: neither variant is ever assigned.\n\n## Findings\n\nOne byte-level change from what the adversary returned, and it is recorded rather than silent: the\nlast finding's `message` value began with a `\"`, so YAML read it as a quoted scalar and the store\nrefused the whole block — `did not find expected key at line 77 column 50`. The value is now single\nquoted and the original opening words are preserved inside it. Every other line is as returned.\n\nThis is a defect in the adversary's output shape, not in its finding.\n\n```findings\n- file: ess/system/domains/connection.yaml\n  line: 612\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: connectors.connection.CandidateNotActivatable carries neither a citation nor an UNMAPPED marker, and no such refusal exists — crates/integration-kubernetes/src/local.rs:286-296 returns the existing Connection for an already-activated candidate.\n- file: ess/system/domains/runtime.yaml\n  line: 288\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: connectors.runtime.SessionStateConflict carries neither a citation nor an UNMAPPED marker, and crates/integration-sip/src/backend/sessions.rs:45-71 returns Ok(status) for an already-terminated session rather than refusing.\n- file: ess/system/domains/catalog.yaml\n  line: 53\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the ProviderSummary field citation stops at crates/protocol/src/catalog.rs:85 while the struct runs to :95, dropping configurable and setup_profiles with no UNMAPPED marker.\n- file: ess/system/domains/connection.yaml\n  line: 148\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the Channel UNMAPPED marker's reason, \"ChannelSummary carries no connection_ref at all\", is contradicted by crates/protocol/src/event.rs:76 in a file the same document already cites.\n- file: ess/system/domains/connection.yaml\n  line: 178\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the ConnectSession transitions were read from one registry, and crates/integration-catalog/src/hosted.rs performs the same moves at :383 and :758 without ever being opened.\n- file: ess/system/domains/connection.yaml\n  line: 303\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: reobserve is declared Withdrawn->Observed but the cited site (integration-monitoring/src/backend.rs:1562) leaves connection_ref set and the derivation at :1659-1660 reads it first, so a materialized observation reobserves into Materialized.\n- file: crates/integration-catalog/src/hosted.rs\n  line: 758\n  category: concurrency\n  severity: warning\n  verdict: INFEASIBLE\n  origin: pre-existing\n  message: Completed is written after two awaits without re-checking Pending, so a concurrent expire_sessions can produce Expired->Completed; I read the window but could not build the interleaving.\n- file: ess/system/domains/connection.yaml\n  line: 107\n  category: judgement\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the Connection entity drops auth_profile (crates/protocol/src/connection.rs:199) with no UNMAPPED marker while route gets one, and auth_profile is the input of a command this document declares.\n- file: ess/system/domains/connection.yaml\n  line: 556\n  category: judgement\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the not-materializable wrong_state outcome cites a connection_not_found refusal (local.rs:640-643) while the actual wrong-state refusal, integration-monitoring/src/backend.rs:1184-1189, is in an implementation the document never opened.\n- file: ess/system/domains/deployment.yaml\n  line: 95\n  category: judgement\n  severity: note\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the quotation \"every cache is bound to the credential generation that produced the evidence\" is cited to docs/design/01-domain-model.md:283 but spans 282-283.\n- file: ess/system/domains/connection.yaml\n  line: 181\n  category: judgement\n  severity: note\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: 'the claim \"which the code enforces at line 204\" does not cover fail_pending (connect_session.rs:225-236), the path the same comment cites for Failed at :233, whose own guard is line 228.'\n```\n","title":"Adversary pass 1 against the connectors ESS domain"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 1 — `story:connectors-ess-domain`\n\nWorktree `wt-2a8ad5b88c79`, branch `impl/connectors-ess-domain`, base `7c66c54` plus the untracked\n`ess/` tree. `adp:adversary`. 191,533 tokens, 82 tool uses, 1,166 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 57->62, red 5\norigin: introduced 10, pre-existing 1, undecided 0\nwrote-outside-worktree: 6 paths (all under the assigned scratch root)\nneeds-coordinator: no\n```\n\n## What the attack was\n\nThe specification's central claim is that every state, transition and field is read from this\nrepository and cited by `path:line`, and that everything unreadable carries an `UNMAPPED:` marker\nnaming what would settle it. The adversary attacked the claim by **opening the citations**, and\nwrote a fence — `crates/catalog-build/tests/main/ess_citation_fence.rs`, 337 lines, 5 cases — that\nopens them mechanically.\n\nThe load-bearing observation: `ess validate` and `bash scripts/gate.sh --final` both stay green\nwith all eleven findings standing. **Nothing in either opens a cited line.** A citation is a claim\nno existing check reads, which is why five of the findings are only reachable by a test that was\nwritten to read them.\n\n## What held\n\n- 49 top-level enums under `crates/protocol/src/`; exactly 7 named `*State`, all 7 modelled.\n- Every enum-variant citation matches its cited range exactly, across 8 enums.\n- 48 distinct `docs/design/01-domain-model.md` ranges opened line by line; all correct but one.\n- All 36 `UNMAPPED:` markers carry a settle clause — 0 without, matching the unit's claim.\n- `ess validate` twice: identical. `ess specify compile --format json` twice: 116,158 bytes,\n  byte-identical.\n- The `Establishing` and `OutcomeUnknown` markers are honest: neither variant is ever assigned.\n\n## Findings\n\nOne byte-level change from what the adversary returned, and it is recorded rather than silent: the\nlast finding's `message` value began with a `\"`, so YAML read it as a quoted scalar and the store\nrefused the whole block — `did not find expected key at line 77 column 50`. The value is now single\nquoted and the original opening words are preserved inside it. Every other line is as returned.\n\nThis is a defect in the adversary's output shape, not in its finding.\n\n```findings\n- file: ess/system/domains/connection.yaml\n  line: 612\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: connectors.connection.CandidateNotActivatable carries neither a citation nor an UNMAPPED marker, and no such refusal exists — crates/integration-kubernetes/src/local.rs:286-296 returns the existing Connection for an already-activated candidate.\n- file: ess/system/domains/runtime.yaml\n  line: 288\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: connectors.runtime.SessionStateConflict carries neither a citation nor an UNMAPPED marker, and crates/integration-sip/src/backend/sessions.rs:45-71 returns Ok(status) for an already-terminated session rather than refusing.\n- file: ess/system/domains/catalog.yaml\n  line: 53\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the ProviderSummary field citation stops at crates/protocol/src/catalog.rs:85 while the struct runs to :95, dropping configurable and setup_profiles with no UNMAPPED marker.\n- file: ess/system/domains/connection.yaml\n  line: 148\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the Channel UNMAPPED marker's reason, \"ChannelSummary carries no connection_ref at all\", is contradicted by crates/protocol/src/event.rs:76 in a file the same document already cites.\n- file: ess/system/domains/connection.yaml\n  line: 178\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the ConnectSession transitions were read from one registry, and crates/integration-catalog/src/hosted.rs performs the same moves at :383 and :758 without ever being opened.\n- file: ess/system/domains/connection.yaml\n  line: 303\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: reobserve is declared Withdrawn->Observed but the cited site (integration-monitoring/src/backend.rs:1562) leaves connection_ref set and the derivation at :1659-1660 reads it first, so a materialized observation reobserves into Materialized.\n- file: crates/integration-catalog/src/hosted.rs\n  line: 758\n  category: concurrency\n  severity: warning\n  verdict: INFEASIBLE\n  origin: pre-existing\n  message: Completed is written after two awaits without re-checking Pending, so a concurrent expire_sessions can produce Expired->Completed; I read the window but could not build the interleaving.\n- file: ess/system/domains/connection.yaml\n  line: 107\n  category: judgement\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the Connection entity drops auth_profile (crates/protocol/src/connection.rs:199) with no UNMAPPED marker while route gets one, and auth_profile is the input of a command this document declares.\n- file: ess/system/domains/connection.yaml\n  line: 556\n  category: judgement\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the not-materializable wrong_state outcome cites a connection_not_found refusal (local.rs:640-643) while the actual wrong-state refusal, integration-monitoring/src/backend.rs:1184-1189, is in an implementation the document never opened.\n- file: ess/system/domains/deployment.yaml\n  line: 95\n  category: judgement\n  severity: note\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the quotation \"every cache is bound to the credential generation that produced the evidence\" is cited to docs/design/01-domain-model.md:283 but spans 282-283.\n- file: ess/system/domains/connection.yaml\n  line: 181\n  category: judgement\n  severity: note\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: 'the claim \"which the code enforces at line 204\" does not cover fail_pending (connect_session.rs:225-236), the path the same comment cites for Failed at :233, whose own guard is line 228.'\n```\n","status":"active","title":"Adversary pass 1 against the connectors ESS domain"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-ess-domain-pass-1"},"payload":{"actor":"human:timo","at":1788519540639,"causation":"cmd-new-review-result-adversary-ess-domain-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-ess-domain-pass-1@1#0~f8d82aa3b1262bc6","executor":null,"recorded_at":"2026-09-04T10:59:00Z"}}
{"entity":"review-result","version":1,"id":"adversary-ess-domain-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:connectors-ess-domain"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000558","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788519540649,"causation":"cmd-review-result-adversary-ess-domain-pass-1-reviews-story-connectors-ess-domain","change":{"change":"related","relation":"reviews","target":"story:connectors-ess-domain"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-ess-domain-pass-1@1#0~601bb7204c52111c","executor":null,"recorded_at":"2026-09-04T10:59:00Z"}}
{"entity":"story","version":1,"id":"hosted-session-completes-out-of-terminal-state","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: a hosted connect session can complete out of a terminal state\n\n## What was measured\n\n`crates/integration-catalog/src/hosted.rs:758` writes `current.state =\nConnectSessionState::Completed` with no re-check that the session is still `Pending`. The `Pending`\nfilter is at `:731`; the lock is then released, `verify_credential` (`:748`) and `commit_connection`\n(`:751`) are awaited, and the write happens at `:758`.\n\n`expire_sessions` (`:379-387`) runs at the head of both `hosted_completion_page` (`:708`) and\n`complete_hosted_session` (`:723`), and writes `Expired` at `:383`. A concurrent request inside the\nawait window can therefore write `Expired`, and the later write produces `Expired -> Completed` —\nout of a state the domain model treats as terminal\n(`ess/system/domains/connection.yaml`, `ConnectSession`).\n\nThe Slack path takes a `hosted_completion_lock` (`crates/integration-slack/src/backend.rs:757`);\nthis path takes none.\n\n## What reaches it\n\n**Not established.** Read, not executed. The adversary that found it reported\n`verdict: INFEASIBLE` — it read the window and could not construct the interleaving. Two hosted\ncompletion requests for one session, overlapping inside the two awaits, is the shape; whether any\ncaller produces that is unmeasured.\n\nThis distinction is the finding's size. A window read out of the code is a hypothesis about a race;\nit is not evidence that anybody has run one, and the fix's priority turns on which it is.\n\n## Shape\n\nRe-read and re-check the session's state after the awaits and before the write, or take the\ncompletion lock the Slack path already takes. Either makes the terminal state terminal.\n\n## Acceptance\n\n- A test drives two overlapping completions of one hosted session and shows the second does not\n  move a session out of `Expired`.\n- The state written at `hosted.rs:758` is guarded by a check performed after `commit_connection`\n  returns.\n\n## Provenance\n\n`review-result:adversary-ess-domain-pass-1`, finding 7, `origin: pre-existing`. Filed rather than\nrouted to the implementor, because it is a defect in the repository the ESS specification exposed by\nmodelling the lifecycle, not a defect the specification introduced.\n","title":"A hosted connect session can complete out of a terminal state"},"args":{"command":"create-entity","data":{"body":"# Story: a hosted connect session can complete out of a terminal state\n\n## What was measured\n\n`crates/integration-catalog/src/hosted.rs:758` writes `current.state =\nConnectSessionState::Completed` with no re-check that the session is still `Pending`. The `Pending`\nfilter is at `:731`; the lock is then released, `verify_credential` (`:748`) and `commit_connection`\n(`:751`) are awaited, and the write happens at `:758`.\n\n`expire_sessions` (`:379-387`) runs at the head of both `hosted_completion_page` (`:708`) and\n`complete_hosted_session` (`:723`), and writes `Expired` at `:383`. A concurrent request inside the\nawait window can therefore write `Expired`, and the later write produces `Expired -> Completed` —\nout of a state the domain model treats as terminal\n(`ess/system/domains/connection.yaml`, `ConnectSession`).\n\nThe Slack path takes a `hosted_completion_lock` (`crates/integration-slack/src/backend.rs:757`);\nthis path takes none.\n\n## What reaches it\n\n**Not established.** Read, not executed. The adversary that found it reported\n`verdict: INFEASIBLE` — it read the window and could not construct the interleaving. Two hosted\ncompletion requests for one session, overlapping inside the two awaits, is the shape; whether any\ncaller produces that is unmeasured.\n\nThis distinction is the finding's size. A window read out of the code is a hypothesis about a race;\nit is not evidence that anybody has run one, and the fix's priority turns on which it is.\n\n## Shape\n\nRe-read and re-check the session's state after the awaits and before the write, or take the\ncompletion lock the Slack path already takes. Either makes the terminal state terminal.\n\n## Acceptance\n\n- A test drives two overlapping completions of one hosted session and shows the second does not\n  move a session out of `Expired`.\n- The state written at `hosted.rs:758` is guarded by a check performed after `commit_connection`\n  returns.\n\n## Provenance\n\n`review-result:adversary-ess-domain-pass-1`, finding 7, `origin: pre-existing`. Filed rather than\nrouted to the implementor, because it is a defect in the repository the ESS specification exposed by\nmodelling the lifecycle, not a defect the specification introduced.\n","status":"draft","title":"A hosted connect session can complete out of a terminal state"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/hosted-session-completes-out-of-terminal-state"},"payload":{"actor":"human:timo","at":1788519560622,"causation":"cmd-new-story-hosted-session-completes-out-of-terminal-state","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:hosted-session-completes-out-of-terminal-state@1#0~709c13404c1bf20f","executor":null,"recorded_at":"2026-09-04T10:59:20Z"}}
{"entity":"story","version":1,"id":"hosted-session-completes-out-of-terminal-state","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"informed_by":"review-result:adversary-ess-domain-pass-1"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000563","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788519560637,"causation":"cmd-story-hosted-session-completes-out-of-terminal-state-informed_by-review-result-adversary-ess-domain-pass-1","change":{"change":"related","relation":"informed_by","target":"review-result:adversary-ess-domain-pass-1"},"correlation":"protocol-artifact-relate","event_id":"story:hosted-session-completes-out-of-terminal-state@1#0~f5f11c14e47929df","executor":null,"recorded_at":"2026-09-04T10:59:20Z"}}
{"entity":"story","version":1,"id":"hosted-session-completes-out-of-terminal-state","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/integration-catalog/src/hosted.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/integration-catalog/src/hosted.rs"}]},"command":"update-entity","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788519560775,"causation":"cmd-protocol-artifact-scope-story-hosted-session-completes-out-of-terminal-state-1788519560775","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:hosted-session-completes-out-of-terminal-state@2#0~3251cec2b73bb7a8","executor":null,"recorded_at":"2026-09-04T10:59:20Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"no-op","review":"review-result:adversary-ess-domain-pass-1","source":"review-result:adversary-ess-domain-pass-1","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788519560000,"causation":"cmd-evidence-story-connectors-ess-domain-review_outcome-1788519560913","change":{"change":"evidence","kind":"review_outcome","outcome":"no-op","review":"review-result:adversary-ess-domain-pass-1","source":"review-result:adversary-ess-domain-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:connectors-ess-domain@6#0~e9c82bf6fd703bf4","executor":null,"recorded_at":"2026-09-04T10:59:20Z"}}
{"entity":"story","version":1,"id":"reobserve-returns-a-withdrawn-observation-to-materialized","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: a withdrawn observation that was materialized comes back as materialized\n\n## What was measured\n\n`crates/integration-monitoring/src/backend.rs:1558-1565` re-activates an observation the refresh\npass has seen again. It sets `active = true` and updates the evidence, and it does **not** clear\n`connection_ref`.\n\n`observation_summary` (`:1656-1665`) derives the projected state in this order: `!active` →\n`Withdrawn`, then `connection_ref.is_some()` → `Materialized`, then `target_provider.is_some()` →\n`Observed`. `materialize` is the only thing that sets `connection_ref` (`:1244`).\n\nSo an observation that was materialized, then withdrawn — `degrade_hosted_targets` at `:1359-1364`\nsets `active = false` on every observation — and then seen again by the refresh pass comes back as\n**`Materialized`**, not `Observed`.\n\nMeasured by an adversary pass against the ESS specification of this domain, which had declared the\ntransition as `reobserve: Withdrawn -> Observed`. The specification now carries the observed\nbehaviour as `rematerialize: Withdrawn -> Materialized`\n(`ess/system/domains/connection.yaml`), because a specification that states a transition the tree\ndoes not perform is worse than one that states an awkward truth.\n\n## The open question\n\nIs `Withdrawn -> Materialized` the behaviour anyone wants? A withdrawn observation's Connection was\nbuilt against evidence that has since changed. Coming back as `Materialized` asserts the old\nConnection still describes it, and nothing re-checks that.\n\n## What reaches it\n\n**Not established.** No test covers *refresh a materialized observation after it was withdrawn*.\n`cargo test -p integration-monitoring --locked` is 16/16 green both with and without the one-line\nfix, which is exactly the problem: the suite does not distinguish the two behaviours.\n\n## Why it was not simply fixed\n\nA one-line fix exists and was measured — `observation.connection_ref = None;` in that branch — but\nit has an uncovered surface. Clearing the reference leaves the child in `state.children`\n(`:1191-1195` returns it) and `child_is_current` (`:1684-1690`) never reads `connection_ref`, so a\nlater `materialize` may mint a duplicate child. That is a behaviour decision on a shipping adapter,\nand it was refused inside a wave whose unit writes YAML.\n\n## Shape\n\nDecide which of the two the adapter should do, then make the suite tell them apart:\n\n- **Clear it** — a re-seen observation is re-derived from evidence, so a stale Connection reference\n  should not survive. Requires deciding what happens to the orphaned child in `state.children`.\n- **Keep it** — a Connection built once stays until something withdraws it explicitly. Then\n  `rematerialize` is correct and permanent, and the story closes as a no-op with a test.\n\n## Acceptance\n\n- A test drives materialize → withdraw → refresh, and asserts the resulting projected state.\n- Whichever branch is chosen, `crates/catalog-build/tests/main/ess_citation_fence.rs`'s\n  `the_reobserve_site_leaves_a_connection_ref_and_the_specification_says_so` is updated in the same\n  change, and `ess/system/domains/connection.yaml` agrees with the adapter.\n\n## Provenance\n\n`review-result:adversary-ess-domain-pass-1`, finding 6. The finding was `origin: introduced` against\nthe specification and is fixed there; this story carries the behaviour question it exposed, which is\nolder than the specification.\n","title":"A withdrawn observation that was materialized comes back as materialized"},"args":{"command":"create-entity","data":{"body":"# Story: a withdrawn observation that was materialized comes back as materialized\n\n## What was measured\n\n`crates/integration-monitoring/src/backend.rs:1558-1565` re-activates an observation the refresh\npass has seen again. It sets `active = true` and updates the evidence, and it does **not** clear\n`connection_ref`.\n\n`observation_summary` (`:1656-1665`) derives the projected state in this order: `!active` →\n`Withdrawn`, then `connection_ref.is_some()` → `Materialized`, then `target_provider.is_some()` →\n`Observed`. `materialize` is the only thing that sets `connection_ref` (`:1244`).\n\nSo an observation that was materialized, then withdrawn — `degrade_hosted_targets` at `:1359-1364`\nsets `active = false` on every observation — and then seen again by the refresh pass comes back as\n**`Materialized`**, not `Observed`.\n\nMeasured by an adversary pass against the ESS specification of this domain, which had declared the\ntransition as `reobserve: Withdrawn -> Observed`. The specification now carries the observed\nbehaviour as `rematerialize: Withdrawn -> Materialized`\n(`ess/system/domains/connection.yaml`), because a specification that states a transition the tree\ndoes not perform is worse than one that states an awkward truth.\n\n## The open question\n\nIs `Withdrawn -> Materialized` the behaviour anyone wants? A withdrawn observation's Connection was\nbuilt against evidence that has since changed. Coming back as `Materialized` asserts the old\nConnection still describes it, and nothing re-checks that.\n\n## What reaches it\n\n**Not established.** No test covers *refresh a materialized observation after it was withdrawn*.\n`cargo test -p integration-monitoring --locked` is 16/16 green both with and without the one-line\nfix, which is exactly the problem: the suite does not distinguish the two behaviours.\n\n## Why it was not simply fixed\n\nA one-line fix exists and was measured — `observation.connection_ref = None;` in that branch — but\nit has an uncovered surface. Clearing the reference leaves the child in `state.children`\n(`:1191-1195` returns it) and `child_is_current` (`:1684-1690`) never reads `connection_ref`, so a\nlater `materialize` may mint a duplicate child. That is a behaviour decision on a shipping adapter,\nand it was refused inside a wave whose unit writes YAML.\n\n## Shape\n\nDecide which of the two the adapter should do, then make the suite tell them apart:\n\n- **Clear it** — a re-seen observation is re-derived from evidence, so a stale Connection reference\n  should not survive. Requires deciding what happens to the orphaned child in `state.children`.\n- **Keep it** — a Connection built once stays until something withdraws it explicitly. Then\n  `rematerialize` is correct and permanent, and the story closes as a no-op with a test.\n\n## Acceptance\n\n- A test drives materialize → withdraw → refresh, and asserts the resulting projected state.\n- Whichever branch is chosen, `crates/catalog-build/tests/main/ess_citation_fence.rs`'s\n  `the_reobserve_site_leaves_a_connection_ref_and_the_specification_says_so` is updated in the same\n  change, and `ess/system/domains/connection.yaml` agrees with the adapter.\n\n## Provenance\n\n`review-result:adversary-ess-domain-pass-1`, finding 6. The finding was `origin: introduced` against\nthe specification and is fixed there; this story carries the behaviour question it exposed, which is\nolder than the specification.\n","status":"draft","title":"A withdrawn observation that was materialized comes back as materialized"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/reobserve-returns-a-withdrawn-observation-to-materialized"},"payload":{"actor":"human:timo","at":1788520324334,"causation":"cmd-new-story-reobserve-returns-a-withdrawn-observation-to-materialized","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:reobserve-returns-a-withdrawn-observation-to-materialized@1#0~3d0f12608f582c3c","executor":null,"recorded_at":"2026-09-04T11:12:04Z"}}
{"entity":"story","version":1,"id":"reobserve-returns-a-withdrawn-observation-to-materialized","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"informed_by":"review-result:adversary-ess-domain-pass-1"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000568","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788520324354,"causation":"cmd-story-reobserve-returns-a-withdrawn-observation-to-materialized-informed_by-review-result-adversary-ess-domain-pass-1","change":{"change":"related","relation":"informed_by","target":"review-result:adversary-ess-domain-pass-1"},"correlation":"protocol-artifact-relate","event_id":"story:reobserve-returns-a-withdrawn-observation-to-materialized@1#0~e91542cd081def44","executor":null,"recorded_at":"2026-09-04T11:12:04Z"}}
{"entity":"story","version":1,"id":"reobserve-returns-a-withdrawn-observation-to-materialized","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/integration-monitoring/src/backend.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/integration-monitoring/src/backend.rs"}]},"command":"update-entity","target":"01MEM0000000000000298"},"payload":{"actor":"human:timo","at":1788520324502,"causation":"cmd-protocol-artifact-scope-story-reobserve-returns-a-withdrawn-observation-to-materialized-1788520324502","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:reobserve-returns-a-withdrawn-observation-to-materialized@2#0~6942e11b49efaa0d","executor":null,"recorded_at":"2026-09-04T11:12:04Z"}}
{"entity":"story","version":1,"id":"reobserve-returns-a-withdrawn-observation-to-materialized","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/integration-monitoring/src/backend.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/integration-monitoring/src/backend.rs"}]},"command":"update-entity","target":"01MEM0000000000000298"},"payload":{"actor":"human:timo","at":1788520324652,"causation":"cmd-protocol-artifact-scope-story-reobserve-returns-a-withdrawn-observation-to-materialized-1788520324652","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:reobserve-returns-a-withdrawn-observation-to-materialized@3#0~e763c4e6cef88e27","executor":null,"recorded_at":"2026-09-04T11:12:04Z"}}
{"entity":"review-result","version":1,"id":"adversary-ess-domain-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 2 — `story:connectors-ess-domain`\n\nWorktree `wt-2a8ad5b88c79`, branch `impl/connectors-ess-domain`, working tree over `7c66c54`.\n`adp:adversary`. 183,169 tokens, 71 tool uses, 917 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 62->67, red 5\norigin: introduced 8, pre-existing 0, undecided 0\nwrote-outside-worktree: 2 paths (both under the assigned scratch root)\nneeds-coordinator: no\n```\n\n## What the attack was\n\nPass 1 attacked citation drift and its fence now checks the mechanical part. Pass 2 was told not to\nre-run it, and attacked the layer above: **claims the specification makes about the tree that no\ncheck reads.** It wrote a second fence — `crates/catalog-build/tests/main/ess_claim_fence.rs`,\n5 cases — where every assertion is an implication: *the document still says X* implies *the tree\nstill does X*. Correcting either side turns it green, and neither side goes green by accident.\n\nThe 62 pre-existing cases, including all five of pass 1's, still pass.\n\n## The one that would have escaped the wave\n\n`naming.wire` is taken verbatim as the address, and `story:cli-surface-contract` — the next unit of\nthis same wave — generates a committed, byte-gated clap tree from these values. Four of the six are\nkebab-case while both request enums are `#[serde(tag = \"method\", rename_all = \"snake_case\")]`, so\nthey name methods every decoder in the tree refuses. The frozen vectors agree:\n`contracts/connector-connection/v0alpha1/vectors.json:10,20,28` carry `candidate_search`,\n`candidate_activate`, `connect_session_create`.\n\nThe two single-word commands, `materialize` and `invoke`, match exactly — which is what shows the\nintent was the method tag and not a separate CLI word.\n\n## What held\n\n- All **226** `path:line` citations in `ess/system/` swept for missing files, past-EOF, inverted and\n  blank-line endpoints. Four trailing overshoots found, all below the finding threshold.\n- The cross-domain claim: `catalog.rs:101` is verbatim \"Stable auth-profile id, exactly as\n  `ConnectSessionCreateRequest.auth_profile` accepts it\". Holds.\n- The `wrong_state:` audit: exactly one remains, and `connect_session.rs:204` does refuse from every\n  non-`Pending` state. Both halves hold.\n- The `Connection` lifecycle against the only drawn one: five transitions and their `from:` sets\n  match `docs/design/01-domain-model.md:198-201` exactly.\n- Every new type and error citation from correction round 1 points where it says.\n- `ess validate --path ess/system` exit 0; the 40 `UNMAPPED:` count is exact.\n\n## Findings, verbatim\n\n```findings\n- file: ess/system/domains/connection.yaml\n  line: 492\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: four naming.wire values are kebab-case while both request enums are rename_all snake_case, so connect-session-create, candidate-activate, session-terminate and session-reconcile name methods every decoder in the tree refuses, and story:cli-surface-contract generates a committed clap tree from these values.\n- file: ess/system/domains/connection.yaml\n  line: 608\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: MaterializeObservation says its declared refusal is the one refusal the command performs, and fn materialize at crates/integration-monitoring/src/backend.rs:1177 refuses six times, the first being NotFound on an unknown observation_ref that the sibling ActivateCandidate models as CandidateNotFound.\n- file: ess/system/domains/runtime.yaml\n  line: 258\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: TerminateSession says not-found is the only refusal the path performs, and fn session_terminate also returns OperationErrorCode::Unavailable from the audit append at crates/integration-sip/src/backend/sessions.rs:65 with no outcome naming it.\n- file: ess/system/domains/connection.yaml\n  line: 209\n  category: acceptance\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the new UNMAPPED marker says Failed to Completed is reachable on the hosted path, and nothing under crates/integration-catalog/src ever assigns ConnectSessionState::Failed over a map that is built empty and never deserialized, so it names a transition the tree cannot perform.\n- file: ess/system/domains/connection.yaml\n  line: 208\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the same marker says the hosted path writes Completed with no state guard at all and asks to be settled by taking connect_session.rs:204's guard, which crates/integration-catalog/src/hosted.rs:731 already is, so the marker cannot be closed as written; the real defect is the unguarded re-lookup at :754-758 after the awaits at :748-753 drop the lock.\n- file: crates/catalog-build/tests/main/ess_citation_fence.rs\n  line: 20\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: cargo fmt -p catalog-build --check reports three hunks in the unit's new fence file and a mod reordering in tests/main.rs, which scripts/gate.sh does not run and therefore never catches.\n- file: crates/catalog-build/tests/main/ess_citation_fence.rs\n  line: 184\n  category: mutant\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: every_connect_session_state_write_is_cited greps only \"state = ConnectSessionState::\" so struct-literal and indirect writes are invisible to it, but no file in the tree escapes it today, so showing the gap needs a constructed fixture.\n- file: ess/system/domains/runtime.yaml\n  line: 282\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: ReconcileSession.terminated declares moves Session.settle while the handler it cites at crates/integration-sip/src/backend/mod.rs:636-648 only calls session_status, so the command observes the move rather than performing it.\n```\n","title":"Adversary pass 2 against the connectors ESS domain"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 2 — `story:connectors-ess-domain`\n\nWorktree `wt-2a8ad5b88c79`, branch `impl/connectors-ess-domain`, working tree over `7c66c54`.\n`adp:adversary`. 183,169 tokens, 71 tool uses, 917 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 62->67, red 5\norigin: introduced 8, pre-existing 0, undecided 0\nwrote-outside-worktree: 2 paths (both under the assigned scratch root)\nneeds-coordinator: no\n```\n\n## What the attack was\n\nPass 1 attacked citation drift and its fence now checks the mechanical part. Pass 2 was told not to\nre-run it, and attacked the layer above: **claims the specification makes about the tree that no\ncheck reads.** It wrote a second fence — `crates/catalog-build/tests/main/ess_claim_fence.rs`,\n5 cases — where every assertion is an implication: *the document still says X* implies *the tree\nstill does X*. Correcting either side turns it green, and neither side goes green by accident.\n\nThe 62 pre-existing cases, including all five of pass 1's, still pass.\n\n## The one that would have escaped the wave\n\n`naming.wire` is taken verbatim as the address, and `story:cli-surface-contract` — the next unit of\nthis same wave — generates a committed, byte-gated clap tree from these values. Four of the six are\nkebab-case while both request enums are `#[serde(tag = \"method\", rename_all = \"snake_case\")]`, so\nthey name methods every decoder in the tree refuses. The frozen vectors agree:\n`contracts/connector-connection/v0alpha1/vectors.json:10,20,28` carry `candidate_search`,\n`candidate_activate`, `connect_session_create`.\n\nThe two single-word commands, `materialize` and `invoke`, match exactly — which is what shows the\nintent was the method tag and not a separate CLI word.\n\n## What held\n\n- All **226** `path:line` citations in `ess/system/` swept for missing files, past-EOF, inverted and\n  blank-line endpoints. Four trailing overshoots found, all below the finding threshold.\n- The cross-domain claim: `catalog.rs:101` is verbatim \"Stable auth-profile id, exactly as\n  `ConnectSessionCreateRequest.auth_profile` accepts it\". Holds.\n- The `wrong_state:` audit: exactly one remains, and `connect_session.rs:204` does refuse from every\n  non-`Pending` state. Both halves hold.\n- The `Connection` lifecycle against the only drawn one: five transitions and their `from:` sets\n  match `docs/design/01-domain-model.md:198-201` exactly.\n- Every new type and error citation from correction round 1 points where it says.\n- `ess validate --path ess/system` exit 0; the 40 `UNMAPPED:` count is exact.\n\n## Findings, verbatim\n\n```findings\n- file: ess/system/domains/connection.yaml\n  line: 492\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: four naming.wire values are kebab-case while both request enums are rename_all snake_case, so connect-session-create, candidate-activate, session-terminate and session-reconcile name methods every decoder in the tree refuses, and story:cli-surface-contract generates a committed clap tree from these values.\n- file: ess/system/domains/connection.yaml\n  line: 608\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: MaterializeObservation says its declared refusal is the one refusal the command performs, and fn materialize at crates/integration-monitoring/src/backend.rs:1177 refuses six times, the first being NotFound on an unknown observation_ref that the sibling ActivateCandidate models as CandidateNotFound.\n- file: ess/system/domains/runtime.yaml\n  line: 258\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: TerminateSession says not-found is the only refusal the path performs, and fn session_terminate also returns OperationErrorCode::Unavailable from the audit append at crates/integration-sip/src/backend/sessions.rs:65 with no outcome naming it.\n- file: ess/system/domains/connection.yaml\n  line: 209\n  category: acceptance\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the new UNMAPPED marker says Failed to Completed is reachable on the hosted path, and nothing under crates/integration-catalog/src ever assigns ConnectSessionState::Failed over a map that is built empty and never deserialized, so it names a transition the tree cannot perform.\n- file: ess/system/domains/connection.yaml\n  line: 208\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the same marker says the hosted path writes Completed with no state guard at all and asks to be settled by taking connect_session.rs:204's guard, which crates/integration-catalog/src/hosted.rs:731 already is, so the marker cannot be closed as written; the real defect is the unguarded re-lookup at :754-758 after the awaits at :748-753 drop the lock.\n- file: crates/catalog-build/tests/main/ess_citation_fence.rs\n  line: 20\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: cargo fmt -p catalog-build --check reports three hunks in the unit's new fence file and a mod reordering in tests/main.rs, which scripts/gate.sh does not run and therefore never catches.\n- file: crates/catalog-build/tests/main/ess_citation_fence.rs\n  line: 184\n  category: mutant\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: every_connect_session_state_write_is_cited greps only \"state = ConnectSessionState::\" so struct-literal and indirect writes are invisible to it, but no file in the tree escapes it today, so showing the gap needs a constructed fixture.\n- file: ess/system/domains/runtime.yaml\n  line: 282\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: ReconcileSession.terminated declares moves Session.settle while the handler it cites at crates/integration-sip/src/backend/mod.rs:636-648 only calls session_status, so the command observes the move rather than performing it.\n```\n","status":"active","title":"Adversary pass 2 against the connectors ESS domain"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-ess-domain-pass-2"},"payload":{"actor":"human:timo","at":1788521316359,"causation":"cmd-new-review-result-adversary-ess-domain-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-ess-domain-pass-2@1#0~18cf404fee3d56b9","executor":null,"recorded_at":"2026-09-04T11:28:36Z"}}
{"entity":"review-result","version":1,"id":"adversary-ess-domain-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:connectors-ess-domain"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000573","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788521316373,"causation":"cmd-review-result-adversary-ess-domain-pass-2-reviews-story-connectors-ess-domain","change":{"change":"related","relation":"reviews","target":"story:connectors-ess-domain"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-ess-domain-pass-2@1#0~44126a7d14df277a","executor":null,"recorded_at":"2026-09-04T11:28:36Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-ess-domain-pass-2","source":"review-result:adversary-ess-domain-pass-2","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788521849000,"causation":"cmd-evidence-story-connectors-ess-domain-review_outcome-1788521849987","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-ess-domain-pass-2","source":"review-result:adversary-ess-domain-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:connectors-ess-domain@6#0~81e778efe25dc1b1","executor":null,"recorded_at":"2026-09-04T11:37:29Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788522579122,"causation":"cmd-protocol-artifact-scope-story-connectors-ess-domain-1788522579122","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:connectors-ess-domain@7#0~d0a300e853dd80f8","executor":null,"recorded_at":"2026-09-04T11:49:39Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788522579276,"causation":"cmd-protocol-artifact-scope-story-connectors-ess-domain-1788522579276","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:connectors-ess-domain@8#0~afbb47afb1c1812b","executor":null,"recorded_at":"2026-09-04T11:49:39Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"ess/system/domains"},{"confidence":"cited","path":"ess/system/system.yaml"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788522579418,"causation":"cmd-protocol-artifact-scope-story-connectors-ess-domain-1788522579418","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:connectors-ess-domain@9#0~220d425c7c24c2af","executor":null,"recorded_at":"2026-09-04T11:49:39Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":9,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"bash scripts/gate.sh, sharded one workspace per step over merge commit e49e5af: 13 steps, every exit 0, 670s, disk floor 74G. Includes cargo test -p catalog-build 67/67 with both ESS fences.","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788534720000,"causation":"cmd-evidence-story-connectors-ess-domain-test_result-1788522579564","change":{"change":"evidence","kind":"test_result","source":"bash scripts/gate.sh, sharded one workspace per step over merge commit e49e5af: 13 steps, every exit 0, 670s, disk floor 74G. Includes cargo test -p catalog-build 67/67 with both ESS fences."},"correlation":"protocol-artifact-evidence","event_id":"story:connectors-ess-domain@9#0~9ba2c4419d9e77ae","executor":null,"recorded_at":"2026-09-04T15:12:00Z"}}
{"entity":"story","version":1,"id":"connectors-ess-domain","revision":10,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":2}}","target":"01MEM0000000000000151","to":"implemented"},"payload":{"actor":"human:timo","at":1788522579779,"causation":"cmd-move-story-connectors-ess-domain-1788522579779","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:connectors-ess-domain@10#0~338c450e9bba90a9","executor":null,"recorded_at":"2026-09-04T11:49:39Z"}}
{"entity":"review-result","version":1,"id":"adversary-cli-contract-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 1 — `story:cli-surface-contract`\n\nWorktree `wt-8854cc8bb2ed`, uncommitted work over base `7f85149`. `adp:adversary`.\n138,739 tokens, 49 tool uses, 832 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 11->20, red 8\norigin: introduced 12, pre-existing 0, undecided 0\nwrote-outside-worktree: 6 paths (all under the assigned scratch root)\nneeds-coordinator: yes\n```\n\n## What the attack was\n\nThe coordinator's brief named the suspicion: `connectors-cli` accepts no commands, so the generated\ntree carries none, and a tree with no commands cannot drift from a parser with 16. The adversary\nwas told to construct the drift the contract claims to catch and see whether anything went red.\n\nIt wrote `crates/connectors-cli/tests/cli_surface_drift.rs`, 9 cases, restating the five assertions\nof `cli_surface.rs` over a parser and a tree passed as arguments — because those assertions live\ninside `#[test]` fns of another binary and cannot be called. Eight went red.\n\n**The contract compares first-level words only.** `connectors connection harvest`, added to the\nparser and to nothing else, raises no refusal. Neither does `connectors event invoke`, an operation\nverb under the event group. The story's `## Acceptance` says the test \"fails when a command is added\nto the parser and not the specification\", and its `## Defect` says nothing catches \"a command added\nin the wrong place\". The parser already carries 14 subcommands under those 5 groups.\n\nThe generator was ruled out as the variable: regeneration against the unmodified specification is\nbyte-identical twice, and a one-sentence `summary:` edit moves only the `.about(...)` line.\n\n## Findings, verbatim\n\n```findings\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 163\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the contract compares first-level words only, so a subcommand added under a declared group or added under the wrong group raises no refusal, which is what the story's Acceptance and Defect sections both say it catches\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 302\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: a stale committed tree is not caught by the crate suite because the only tree assertions are group-name presence and a word count, and a one-word summary edit in the specification moves nothing else\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 260\n  category: mutant\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the target countdown reads only the leaves' arguments, so a --target declared on the connection group itself is invisible, and that is where the next story's flag naturally goes\n- file: docs/design/19-the-cli-surface.md\n  line: 98\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the accepted one-group-at-a-time cutover cannot keep the gate green because the emitted group carries no subcommand and cli_surface.rs refuses a declared group whose parser word carries none\n- file: ess/system/components.yaml\n  line: 86\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the citation architecture_fence.rs:303 points at a ConnectorBackend assertion inside focused_adapters_are_focused, not at product_cli_is_a_thin_frontend which spans lines 310 to 380\n- file: ess/generated/clap/crates/connectors-cli/Cargo.toml\n  line: 7\n  category: boundary\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the emitted manifest sits inside the root workspace directory and is in neither members nor exclude, so every cargo command against it is refused before it is read\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 301\n  category: mutant\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: a tree that swaps completions for any other word keeps the count at seven and passes, but I could show no automated path that produces such a tree\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 53\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the allowlist header claims every entry is a read or a process-lifecycle verb, and init and connect are neither by their own reason strings in the same list\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 243\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the countdown's size assertion compares a literal to a constant twelve lines above it in the same file, so it measures the test file rather than the parser\n- file: ess/generated/clap/crates/connectors-cli/src/main.rs\n  line: 22\n  category: judgement\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: the emitted binary writes the completion script straight to stdout and panics under a pipe that closes early, which is the exact failure the hand-written lib.rs buffers to avoid, and the emitter is ess rather than this repository\n- file: scripts/gate.sh\n  line: 75\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the byte-identity diff runs only under --final, which only the release workflow invokes, so a stale generated tree reaches main and is first refused at a release cut\n- file: ess/generated/clap/crates/connectors-cli/src/tree.rs\n  line: 15\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the emitted binary's about text is the component summary rather than the product's and the emitted tree carries no global output flag, both of which a cutover would ship into connectors --help unnoticed\n```\n","title":"Adversary pass 1 against the CLI surface contract"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 1 — `story:cli-surface-contract`\n\nWorktree `wt-8854cc8bb2ed`, uncommitted work over base `7f85149`. `adp:adversary`.\n138,739 tokens, 49 tool uses, 832 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 11->20, red 8\norigin: introduced 12, pre-existing 0, undecided 0\nwrote-outside-worktree: 6 paths (all under the assigned scratch root)\nneeds-coordinator: yes\n```\n\n## What the attack was\n\nThe coordinator's brief named the suspicion: `connectors-cli` accepts no commands, so the generated\ntree carries none, and a tree with no commands cannot drift from a parser with 16. The adversary\nwas told to construct the drift the contract claims to catch and see whether anything went red.\n\nIt wrote `crates/connectors-cli/tests/cli_surface_drift.rs`, 9 cases, restating the five assertions\nof `cli_surface.rs` over a parser and a tree passed as arguments — because those assertions live\ninside `#[test]` fns of another binary and cannot be called. Eight went red.\n\n**The contract compares first-level words only.** `connectors connection harvest`, added to the\nparser and to nothing else, raises no refusal. Neither does `connectors event invoke`, an operation\nverb under the event group. The story's `## Acceptance` says the test \"fails when a command is added\nto the parser and not the specification\", and its `## Defect` says nothing catches \"a command added\nin the wrong place\". The parser already carries 14 subcommands under those 5 groups.\n\nThe generator was ruled out as the variable: regeneration against the unmodified specification is\nbyte-identical twice, and a one-sentence `summary:` edit moves only the `.about(...)` line.\n\n## Findings, verbatim\n\n```findings\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 163\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the contract compares first-level words only, so a subcommand added under a declared group or added under the wrong group raises no refusal, which is what the story's Acceptance and Defect sections both say it catches\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 302\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: a stale committed tree is not caught by the crate suite because the only tree assertions are group-name presence and a word count, and a one-word summary edit in the specification moves nothing else\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 260\n  category: mutant\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the target countdown reads only the leaves' arguments, so a --target declared on the connection group itself is invisible, and that is where the next story's flag naturally goes\n- file: docs/design/19-the-cli-surface.md\n  line: 98\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the accepted one-group-at-a-time cutover cannot keep the gate green because the emitted group carries no subcommand and cli_surface.rs refuses a declared group whose parser word carries none\n- file: ess/system/components.yaml\n  line: 86\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the citation architecture_fence.rs:303 points at a ConnectorBackend assertion inside focused_adapters_are_focused, not at product_cli_is_a_thin_frontend which spans lines 310 to 380\n- file: ess/generated/clap/crates/connectors-cli/Cargo.toml\n  line: 7\n  category: boundary\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the emitted manifest sits inside the root workspace directory and is in neither members nor exclude, so every cargo command against it is refused before it is read\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 301\n  category: mutant\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: a tree that swaps completions for any other word keeps the count at seven and passes, but I could show no automated path that produces such a tree\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 53\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the allowlist header claims every entry is a read or a process-lifecycle verb, and init and connect are neither by their own reason strings in the same list\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 243\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the countdown's size assertion compares a literal to a constant twelve lines above it in the same file, so it measures the test file rather than the parser\n- file: ess/generated/clap/crates/connectors-cli/src/main.rs\n  line: 22\n  category: judgement\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: the emitted binary writes the completion script straight to stdout and panics under a pipe that closes early, which is the exact failure the hand-written lib.rs buffers to avoid, and the emitter is ess rather than this repository\n- file: scripts/gate.sh\n  line: 75\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the byte-identity diff runs only under --final, which only the release workflow invokes, so a stale generated tree reaches main and is first refused at a release cut\n- file: ess/generated/clap/crates/connectors-cli/src/tree.rs\n  line: 15\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the emitted binary's about text is the component summary rather than the product's and the emitted tree carries no global output flag, both of which a cutover would ship into connectors --help unnoticed\n```\n","status":"active","title":"Adversary pass 1 against the CLI surface contract"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-cli-contract-pass-1"},"payload":{"actor":"human:timo","at":1788528354651,"causation":"cmd-new-review-result-adversary-cli-contract-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-cli-contract-pass-1@1#0~90d2df40568e210f","executor":null,"recorded_at":"2026-09-04T13:25:54Z"}}
{"entity":"review-result","version":1,"id":"adversary-cli-contract-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:cli-surface-contract"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000578","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788528354662,"causation":"cmd-review-result-adversary-cli-contract-pass-1-reviews-story-cli-surface-contract","change":{"change":"related","relation":"reviews","target":"story:cli-surface-contract"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-cli-contract-pass-1@1#0~de2808aff868e8de","executor":null,"recorded_at":"2026-09-04T13:25:54Z"}}
{"entity":"review-result","version":1,"id":"adversary-cli-contract-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 2 — `story:cli-surface-contract`\n\nWorktree `wt-8854cc8bb2ed`, uncommitted over `7f85149`. `adp:adversary`.\n164,828 tokens, 57 tool uses, 1,033 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 23->29, red 6\norigin: introduced 11, pre-existing 0, undecided 0\nwrote-outside-worktree: 1 path (an empty scratch directory)\nneeds-coordinator: no\n```\n\n## What the attack was\n\nPass 1 found the contract compared first-level words only. The correction rewrote every comparison\nin the file. Pass 2 was told not to re-run that ground and to attack what the correction\nintroduced — chiefly the 26-entry exception list, now the contract's escape hatch, and the drift\nsuite's copied constants.\n\n**The contract held. Its documentation did not.** Eight of eleven findings are prose that still\ndescribes the pre-correction contract: the design document names a constant that does not exist,\ndescribes the exception list as first-level words when 17 of its 26 entries are two- and three-word\npaths, and says the test asserts a length it no longer asserts. The drift suite cites a 33-line\nrange for constants that are not in it and quotes four sentences that occur nowhere in the file\nthey are attributed to.\n\nTwo of those four quotations tell a reader the live contract still has the count assertion and the\nleaves-only `carries_target` that pass 1 removed.\n\n## The one property finding\n\n`cli_surface.rs:322` documents the `--target` candidate set as \"read from the tree\", and it is a\nthree-element literal filtered by `is_file()`. `crates/protocol/src` declares **five** request\nenums a deployment answers — `catalog`, `connection`, `datasource`, `event`, `operation`. A filter\ncan only shrink a literal, so a group named after `catalog` or `datasource` would never enter\n`TARGET_EXCEPTIONS` and the countdown would reach zero without it. No such group exists today.\n\n## What held\n\n- The 26-entry list is **complete and exact**, forced in both directions. The adversary enumerated\n  the parser by hand: 32 paths = 5 declared groups + `completions` + 26 exceptions.\n- Every `Grouping` entry is the only kind with children; every `Forwarded` entry names a command\n  `TARGET.md` lists as accepted by `connectors-service`; no `Read` entry names a declared command.\n  No misclassification.\n- Every `path:line` in `ess/system/components.yaml` checked for meaning rather than bounds. All\n  exact.\n- The new workspace `exclude` misses nothing: `production_sources` is only called under `crates/`,\n  `json_governance` has no JSON to see, the other fences read the root manifest only.\n- The flipped cutover case asserts what the amended design document now says.\n\n## Findings, verbatim\n\n```findings\n- file: docs/design/19-the-cli-surface.md\n  line: 65\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the accepted design document names a constant `UNSPECIFIED_WORDS` that no file of this repository declares; the contract's constant is `UNSPECIFIED_PATHS`\n- file: docs/design/19-the-cli-surface.md\n  line: 65\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the section bounding the escape hatch describes the exception list as first-level words that the parser gains, and 17 of its 26 entries are two- and three-word paths, so the accepted document still describes the contract the correction replaced\n- file: docs/design/19-the-cli-surface.md\n  line: 69\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page says the test asserts `TARGET_EXCEPTIONS` has exactly three members, and the contract makes no assertion about its length at all\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 550\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: four sentences the drift suite attributes to `cli_surface.rs` occur nowhere in it, and two of them tell the reader the live contract has a count assertion and a leaves-only `carries_target` that it no longer has\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 13\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the header cites `cli_surface.rs:62-94` as where its two constants were copied from and that range declares neither, and the five ranges it calls the four assertions hold no assertion\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 322\n  category: property\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the countdown's candidate set is documented as read from the tree and is a three-element literal filtered only by `is_file()`, while `crates/protocol` declares five request enums a deployment answers, so the set can never grow and a `catalog` or `datasource` group would reach zero without entering it\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 42\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the control test is described as refusing a copy that has stopped being one, and `contract_refusals` reads only the path column, so the `Unspecified` kind and the reason string can diverge between the two files with nothing red\n- file: crates/catalog-build/tests/main/architecture_fence.rs\n  line: 33\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the comment justifying the 1006 to 1014 raise enumerates nine lines for an eight-line change, counting four lines of doc where `pub fn command()` carries three\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 315\n  category: boundary\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: one descendant carrying `--target` retires the whole group from the countdown, so a partial rollout would empty the list while a group's reads still infer the target, but the story puts the flag on the groups and nothing was found that reaches the partial state\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 579\n  category: mutant\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: the generated-tree comparison is a flat word sequence and cannot see structure, so a tree nesting one declared group under another would pass, but while the component accepts no command the emitted tree can only be flat and nothing reaches it\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 659\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the case says the cited claims require that the emitted crate can be built and measures `cargo metadata --no-deps --offline`, which reads the manifest and compiles nothing\n```\n","title":"Adversary pass 2 against the CLI surface contract"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 2 — `story:cli-surface-contract`\n\nWorktree `wt-8854cc8bb2ed`, uncommitted over `7f85149`. `adp:adversary`.\n164,828 tokens, 57 tool uses, 1,033 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 23->29, red 6\norigin: introduced 11, pre-existing 0, undecided 0\nwrote-outside-worktree: 1 path (an empty scratch directory)\nneeds-coordinator: no\n```\n\n## What the attack was\n\nPass 1 found the contract compared first-level words only. The correction rewrote every comparison\nin the file. Pass 2 was told not to re-run that ground and to attack what the correction\nintroduced — chiefly the 26-entry exception list, now the contract's escape hatch, and the drift\nsuite's copied constants.\n\n**The contract held. Its documentation did not.** Eight of eleven findings are prose that still\ndescribes the pre-correction contract: the design document names a constant that does not exist,\ndescribes the exception list as first-level words when 17 of its 26 entries are two- and three-word\npaths, and says the test asserts a length it no longer asserts. The drift suite cites a 33-line\nrange for constants that are not in it and quotes four sentences that occur nowhere in the file\nthey are attributed to.\n\nTwo of those four quotations tell a reader the live contract still has the count assertion and the\nleaves-only `carries_target` that pass 1 removed.\n\n## The one property finding\n\n`cli_surface.rs:322` documents the `--target` candidate set as \"read from the tree\", and it is a\nthree-element literal filtered by `is_file()`. `crates/protocol/src` declares **five** request\nenums a deployment answers — `catalog`, `connection`, `datasource`, `event`, `operation`. A filter\ncan only shrink a literal, so a group named after `catalog` or `datasource` would never enter\n`TARGET_EXCEPTIONS` and the countdown would reach zero without it. No such group exists today.\n\n## What held\n\n- The 26-entry list is **complete and exact**, forced in both directions. The adversary enumerated\n  the parser by hand: 32 paths = 5 declared groups + `completions` + 26 exceptions.\n- Every `Grouping` entry is the only kind with children; every `Forwarded` entry names a command\n  `TARGET.md` lists as accepted by `connectors-service`; no `Read` entry names a declared command.\n  No misclassification.\n- Every `path:line` in `ess/system/components.yaml` checked for meaning rather than bounds. All\n  exact.\n- The new workspace `exclude` misses nothing: `production_sources` is only called under `crates/`,\n  `json_governance` has no JSON to see, the other fences read the root manifest only.\n- The flipped cutover case asserts what the amended design document now says.\n\n## Findings, verbatim\n\n```findings\n- file: docs/design/19-the-cli-surface.md\n  line: 65\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the accepted design document names a constant `UNSPECIFIED_WORDS` that no file of this repository declares; the contract's constant is `UNSPECIFIED_PATHS`\n- file: docs/design/19-the-cli-surface.md\n  line: 65\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the section bounding the escape hatch describes the exception list as first-level words that the parser gains, and 17 of its 26 entries are two- and three-word paths, so the accepted document still describes the contract the correction replaced\n- file: docs/design/19-the-cli-surface.md\n  line: 69\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page says the test asserts `TARGET_EXCEPTIONS` has exactly three members, and the contract makes no assertion about its length at all\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 550\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: four sentences the drift suite attributes to `cli_surface.rs` occur nowhere in it, and two of them tell the reader the live contract has a count assertion and a leaves-only `carries_target` that it no longer has\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 13\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the header cites `cli_surface.rs:62-94` as where its two constants were copied from and that range declares neither, and the five ranges it calls the four assertions hold no assertion\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 322\n  category: property\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the countdown's candidate set is documented as read from the tree and is a three-element literal filtered only by `is_file()`, while `crates/protocol` declares five request enums a deployment answers, so the set can never grow and a `catalog` or `datasource` group would reach zero without entering it\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 42\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the control test is described as refusing a copy that has stopped being one, and `contract_refusals` reads only the path column, so the `Unspecified` kind and the reason string can diverge between the two files with nothing red\n- file: crates/catalog-build/tests/main/architecture_fence.rs\n  line: 33\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the comment justifying the 1006 to 1014 raise enumerates nine lines for an eight-line change, counting four lines of doc where `pub fn command()` carries three\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 315\n  category: boundary\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: one descendant carrying `--target` retires the whole group from the countdown, so a partial rollout would empty the list while a group's reads still infer the target, but the story puts the flag on the groups and nothing was found that reaches the partial state\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 579\n  category: mutant\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: the generated-tree comparison is a flat word sequence and cannot see structure, so a tree nesting one declared group under another would pass, but while the component accepts no command the emitted tree can only be flat and nothing reaches it\n- file: crates/connectors-cli/tests/cli_surface_drift.rs\n  line: 659\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the case says the cited claims require that the emitted crate can be built and measures `cargo metadata --no-deps --offline`, which reads the manifest and compiles nothing\n```\n","status":"active","title":"Adversary pass 2 against the CLI surface contract"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-cli-contract-pass-2"},"payload":{"actor":"human:timo","at":1788530291869,"causation":"cmd-new-review-result-adversary-cli-contract-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-cli-contract-pass-2@1#0~73698b3a4908576c","executor":null,"recorded_at":"2026-09-04T13:58:11Z"}}
{"entity":"review-result","version":1,"id":"adversary-cli-contract-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:cli-surface-contract"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000583","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788530291882,"causation":"cmd-review-result-adversary-cli-contract-pass-2-reviews-story-cli-surface-contract","change":{"change":"related","relation":"reviews","target":"story:cli-surface-contract"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-cli-contract-pass-2@1#0~125ae2f882a85b27","executor":null,"recorded_at":"2026-09-04T13:58:11Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-cli-contract-pass-1","source":"review-result:adversary-cli-contract-pass-1","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530887000,"causation":"cmd-evidence-story-cli-surface-contract-review_outcome-1788530887273","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-cli-contract-pass-1","source":"review-result:adversary-cli-contract-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-surface-contract@5#0~4ec99235ff992289","executor":null,"recorded_at":"2026-09-04T14:08:07Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-cli-contract-pass-2","source":"review-result:adversary-cli-contract-pass-2","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530887000,"causation":"cmd-evidence-story-cli-surface-contract-review_outcome-1788530887536","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-cli-contract-pass-2","source":"review-result:adversary-cli-contract-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-surface-contract@5#0~c49632e529dda9bf","executor":null,"recorded_at":"2026-09-04T14:08:07Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530888004,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788530888004","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@6#0~1712576872294979","executor":null,"recorded_at":"2026-09-04T14:08:08Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":7,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530888477,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788530888477","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@7#0~90605ccfdce411f0","executor":null,"recorded_at":"2026-09-04T14:08:08Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":8,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530888801,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788530888801","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@8#0~c8855056d6f9ec9a","executor":null,"recorded_at":"2026-09-04T14:08:08Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":9,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530889250,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788530889250","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@9#0~c959e6ca1559de37","executor":null,"recorded_at":"2026-09-04T14:08:09Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":10,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530889589,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788530889589","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@10#0~542282fd1765ecef","executor":null,"recorded_at":"2026-09-04T14:08:09Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":11,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530889986,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788530889986","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@11#0~950dacdd0832417f","executor":null,"recorded_at":"2026-09-04T14:08:09Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":12,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/architecture_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/src/generated"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_pass_two.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/generated/clap"},{"confidence":"cited","path":"ess/system/components.yaml"},{"confidence":"cited","path":"scripts/gate.sh"}]},"command":"update-entity","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788530890414,"causation":"cmd-protocol-artifact-scope-story-cli-surface-contract-1788530890414","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-contract@12#0~967eb7f37ddcb2a9","executor":null,"recorded_at":"2026-09-04T14:08:10Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":13,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":2}}","target":"01MEM0000000000000151","to":"proposed"},"payload":{"actor":"human:timo","at":1788530996087,"causation":"cmd-move-story-cli-surface-contract-1788530996087","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:cli-surface-contract@13#0~c54585b56aa23756","executor":null,"recorded_at":"2026-09-04T14:09:56Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":14,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":2}}","target":"01MEM0000000000000151","to":"active"},"payload":{"actor":"human:timo","at":1788530996796,"causation":"cmd-move-story-cli-surface-contract-1788530996796","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:cli-surface-contract@14#0~d24dcf27342a4aa2","executor":null,"recorded_at":"2026-09-04T14:09:56Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":14,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"bash scripts/gate.sh sharded over merge commit 8d14cfc: 13 steps, every exit 0, 1032s. Includes crates/connectors-cli 30/30 with three contract suites, catalog-build 67/67, root workspace 1124/1124.","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788537900000,"causation":"cmd-evidence-story-cli-surface-contract-test_result-1788532046114","change":{"change":"evidence","kind":"test_result","source":"bash scripts/gate.sh sharded over merge commit 8d14cfc: 13 steps, every exit 0, 1032s. Includes crates/connectors-cli 30/30 with three contract suites, catalog-build 67/67, root workspace 1124/1124."},"correlation":"protocol-artifact-evidence","event_id":"story:cli-surface-contract@14#0~4337305039d91711","executor":null,"recorded_at":"2026-09-04T16:05:00Z"}}
{"entity":"story","version":1,"id":"cli-surface-contract","revision":15,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":2}}","target":"01MEM0000000000000151","to":"implemented"},"payload":{"actor":"human:timo","at":1788532046333,"causation":"cmd-move-story-cli-surface-contract-1788532046333","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:cli-surface-contract@15#0~338c450e9bba90a9","executor":null,"recorded_at":"2026-09-04T14:27:26Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: the CLI surface fences assert what they claim to\n\n## Defect\n\nAn independent review of 2026-09-04's whole change set found the fences guarding the CLI surface\nare green for the wrong reasons. Verified directly:\n\n```\ncrates/catalog-build/tests/main/ess_claim_fence.rs:228  !declared.contains(claim) || sites.len() == 1,\ncrates/catalog-build/tests/main/ess_claim_fence.rs:261  !declared.contains(claim) || sites.len() == 1,\ncrates/catalog-build/tests/main/ess_claim_fence.rs:321  !declared.contains(claim) || !writers.is_empty(),\ncrates/catalog-build/tests/main/ess_claim_fence.rs:367  !declared.contains(claim) || guards.is_empty(),\n```\n\nZero of those four sentences remain in `ess/system`. **Four of the five tests pass by their guard\nclause.** The reviewer deleted `MaterializeObservation`'s three refusal outcomes — the exact defect\nthe fence exists for — and it stayed green. Only reinstating the literal old sentence makes it fail.\n\nA test that cannot fail is worse than no test: the gate reports it green.\n\n## The four blockers\n\n| # | where | what was measured |\n|---|---|---|\n| 1 | `ess_claim_fence.rs:228,261,321,367` | four of five assertions are `!declared.contains(claim) \\|\\| …`, and no claim string survives in the specification |\n| 2 | `cli_surface.rs:98`, `:78`, `:434-435` | `connection prune` added to the parser is absorbed by three edits — one `Unmodelled` entry with an arbitrary reason, its copy in the drift suite, and two numbers in the design document. 25/25 green, `ess/system` and `ess/generated` byte-identical. **The failure message instructs exactly this.** No check ties a `Forwarded` or `Read` reason to anything |\n| 3 | `crates/connectors-cli/src/lib.rs:71,164`, `scripts/gate.sh:69-71` | editing `Admin`'s doc comment alone leaves 25/25 green — no fence reads `get_about`. `#[command(alias = \"conn\")]` on `Connection` is green, `connectors conn --help` works, and the fish completion lists it zero times. The gate comment claims the parser's `--help` lines are compared; only the generated tree's are |\n| 4 | `components.yaml:39-60` | 20 `publishes: events`; 16 carry neither a citation nor an `UNMAPPED:` marker, and no file under `crates/` mentions any of 13 sampled names. `ess_citation_fence.rs`'s `entries_of_block` is only ever called for `errors`. ESS-COMMAND-007 forces an emit or error per outcome, so they are schema-forced and unmarked |\n\n## Also found\n\n- `cli_surface_drift.rs:287` still carries the literal-filter `deployment_protocol_modules` the\n  contract replaced, while its header claims a faithful restatement of every assertion.\n- `output.rs:28` and `CHANGELOG.md:29-31` claim a row fits a terminal at a 120-column budget. 66 of\n  71 `connectors providers` lines exceed 120 columns; the longest is 237. The code is as designed —\n  the claim is false as worded.\n- `release.yml:105-108` pins `ess` at `--tag 0.16.0`; 0.18.0 is released and installed.\n- `release.yml:32-33` says eleven workspaces and 39 GB; `gate.sh:8-9` says twelve and 41 GB. Twelve\n  is right.\n- `cli_surface_drift.rs:726` says the emitted manifest is in neither `members` nor `exclude`;\n  `Cargo.toml:76` excludes it, and without that line `cargo metadata` exits 101.\n- The generated files say to regenerate with `cargo xtask synth` and `ess synthesize`. Neither is\n  the command `scripts/gate.sh` runs.\n\n## Shape\n\nA guard clause that makes an assertion conditional on a string still being present is the failure\nmode, not an instance. Every fence assertion is rewritten so it holds against the specification as\nit is, and a claim that disappears is a **failure**, not a pass. Where a fence needs a sentence to\nexist, it asserts the sentence exists.\n\nThe exception list stops being a list a failure message invites you to extend: an entry needs\nsomething checkable behind it, and the message stops naming the escape hatch first.\n\n## Acceptance\n\n- Each of the reviewer's four constructions is reproduced as a test and each goes red before the\n  fix: deleting `MaterializeObservation`'s refusal outcomes, adding `connection prune`, editing a\n  doc comment, adding `alias = \"conn\"`.\n- No assertion in any fence is conditional on a claim string being present.\n- `bash scripts/gate.sh` exits 0, every step read as its own status.\n\n## Provenance\n\nIndependent review, `fable` model, over `e162cbf..wave/cli-contract` — 37 commits, 366 files,\n+46531 −4087. Its verdict was *not mergeable as a gate*. The merge to `main` (`7b2d680`) had already\nhappened when the review returned; nothing is pushed.\n","title":"The CLI surface fences assert what they claim to"},"args":{"command":"create-entity","data":{"body":"# Story: the CLI surface fences assert what they claim to\n\n## Defect\n\nAn independent review of 2026-09-04's whole change set found the fences guarding the CLI surface\nare green for the wrong reasons. Verified directly:\n\n```\ncrates/catalog-build/tests/main/ess_claim_fence.rs:228  !declared.contains(claim) || sites.len() == 1,\ncrates/catalog-build/tests/main/ess_claim_fence.rs:261  !declared.contains(claim) || sites.len() == 1,\ncrates/catalog-build/tests/main/ess_claim_fence.rs:321  !declared.contains(claim) || !writers.is_empty(),\ncrates/catalog-build/tests/main/ess_claim_fence.rs:367  !declared.contains(claim) || guards.is_empty(),\n```\n\nZero of those four sentences remain in `ess/system`. **Four of the five tests pass by their guard\nclause.** The reviewer deleted `MaterializeObservation`'s three refusal outcomes — the exact defect\nthe fence exists for — and it stayed green. Only reinstating the literal old sentence makes it fail.\n\nA test that cannot fail is worse than no test: the gate reports it green.\n\n## The four blockers\n\n| # | where | what was measured |\n|---|---|---|\n| 1 | `ess_claim_fence.rs:228,261,321,367` | four of five assertions are `!declared.contains(claim) \\|\\| …`, and no claim string survives in the specification |\n| 2 | `cli_surface.rs:98`, `:78`, `:434-435` | `connection prune` added to the parser is absorbed by three edits — one `Unmodelled` entry with an arbitrary reason, its copy in the drift suite, and two numbers in the design document. 25/25 green, `ess/system` and `ess/generated` byte-identical. **The failure message instructs exactly this.** No check ties a `Forwarded` or `Read` reason to anything |\n| 3 | `crates/connectors-cli/src/lib.rs:71,164`, `scripts/gate.sh:69-71` | editing `Admin`'s doc comment alone leaves 25/25 green — no fence reads `get_about`. `#[command(alias = \"conn\")]` on `Connection` is green, `connectors conn --help` works, and the fish completion lists it zero times. The gate comment claims the parser's `--help` lines are compared; only the generated tree's are |\n| 4 | `components.yaml:39-60` | 20 `publishes: events`; 16 carry neither a citation nor an `UNMAPPED:` marker, and no file under `crates/` mentions any of 13 sampled names. `ess_citation_fence.rs`'s `entries_of_block` is only ever called for `errors`. ESS-COMMAND-007 forces an emit or error per outcome, so they are schema-forced and unmarked |\n\n## Also found\n\n- `cli_surface_drift.rs:287` still carries the literal-filter `deployment_protocol_modules` the\n  contract replaced, while its header claims a faithful restatement of every assertion.\n- `output.rs:28` and `CHANGELOG.md:29-31` claim a row fits a terminal at a 120-column budget. 66 of\n  71 `connectors providers` lines exceed 120 columns; the longest is 237. The code is as designed —\n  the claim is false as worded.\n- `release.yml:105-108` pins `ess` at `--tag 0.16.0`; 0.18.0 is released and installed.\n- `release.yml:32-33` says eleven workspaces and 39 GB; `gate.sh:8-9` says twelve and 41 GB. Twelve\n  is right.\n- `cli_surface_drift.rs:726` says the emitted manifest is in neither `members` nor `exclude`;\n  `Cargo.toml:76` excludes it, and without that line `cargo metadata` exits 101.\n- The generated files say to regenerate with `cargo xtask synth` and `ess synthesize`. Neither is\n  the command `scripts/gate.sh` runs.\n\n## Shape\n\nA guard clause that makes an assertion conditional on a string still being present is the failure\nmode, not an instance. Every fence assertion is rewritten so it holds against the specification as\nit is, and a claim that disappears is a **failure**, not a pass. Where a fence needs a sentence to\nexist, it asserts the sentence exists.\n\nThe exception list stops being a list a failure message invites you to extend: an entry needs\nsomething checkable behind it, and the message stops naming the escape hatch first.\n\n## Acceptance\n\n- Each of the reviewer's four constructions is reproduced as a test and each goes red before the\n  fix: deleting `MaterializeObservation`'s refusal outcomes, adding `connection prune`, editing a\n  doc comment, adding `alias = \"conn\"`.\n- No assertion in any fence is conditional on a claim string being present.\n- `bash scripts/gate.sh` exits 0, every step read as its own status.\n\n## Provenance\n\nIndependent review, `fable` model, over `e162cbf..wave/cli-contract` — 37 commits, 366 files,\n+46531 −4087. Its verdict was *not mergeable as a gate*. The merge to `main` (`7b2d680`) had already\nhappened when the review returned; nothing is pushed.\n","status":"draft","title":"The CLI surface fences assert what they claim to"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/cli-surface-fences-assert-what-they-claim"},"payload":{"actor":"human:timo","at":1788532709710,"causation":"cmd-new-story-cli-surface-fences-assert-what-they-claim","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:cli-surface-fences-assert-what-they-claim@1#0~bc325a9a0cefc214","executor":null,"recorded_at":"2026-09-04T14:38:29Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000588","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788532710393,"causation":"cmd-story-cli-surface-fences-assert-what-they-claim-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:cli-surface-fences-assert-what-they-claim@1#0~cb2de17dfa426c35","executor":null,"recorded_at":"2026-09-04T14:38:30Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788532710682,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788532710682","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@2#0~96f900873edbbbca","executor":null,"recorded_at":"2026-09-04T14:38:30Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788532711017,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788532711017","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@3#0~b93aec061cf75d78","executor":null,"recorded_at":"2026-09-04T14:38:31Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788532711304,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788532711304","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@4#0~cdd9ed0b2c267dfa","executor":null,"recorded_at":"2026-09-04T14:38:31Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788532711652,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788532711652","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@5#0~d74f160e12ebd78c","executor":null,"recorded_at":"2026-09-04T14:38:31Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788532712063,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788532712063","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@6#0~8f00bcd1d231529d","executor":null,"recorded_at":"2026-09-04T14:38:32Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":7,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788532712597,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788532712597","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@7#0~16a58a008d15152c","executor":null,"recorded_at":"2026-09-04T14:38:32Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":8,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788532713585,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788532713585","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@8#0~b1696864864cb89c","executor":null,"recorded_at":"2026-09-04T14:38:33Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":9,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000154","to":"proposed"},"payload":{"actor":"human:timo","at":1788532721825,"causation":"cmd-move-story-cli-surface-fences-assert-what-they-claim-1788532721825","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:cli-surface-fences-assert-what-they-claim@9#0~4966d998cfeadb0e","executor":null,"recorded_at":"2026-09-04T14:38:41Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":10,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000154","to":"active"},"payload":{"actor":"human:timo","at":1788532722039,"causation":"cmd-move-story-cli-surface-fences-assert-what-they-claim-1788532722039","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:cli-surface-fences-assert-what-they-claim@10#0~104f3b924e4acd6a","executor":null,"recorded_at":"2026-09-04T14:38:42Z"}}
{"entity":"review-result","version":1,"id":"adversary-fences-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 1 — `story:cli-surface-fences-assert-what-they-claim`\n\nWorktree `wt-d6c9d0f84db6`, uncommitted over `7b2d680`. `adp:adversary`.\n214,285 tokens, 85 tool uses, 1,246 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 1252->1261, red 6\norigin: introduced 7, pre-existing 0, undecided 0\nwrote-outside-worktree: 7 paths (all under the assigned scratch root)\nneeds-coordinator: no\n```\n\n## Method\n\nThe brief told the adversary how the previous two passes over this code were beaten — by reading\nassertions and judging them sound — and prescribed construction instead. It carried\ncharacter-for-character copies of the contract's `Unspecified`, `UNSPECIFIED_PATHS`,\n`exception_list_refusals`, `parser_paths` and six more helpers into its probe, so the kind cases run\nthe shipped code rather than a paraphrase, with a control test proving the copies are still copies.\n\n## What it found\n\n**The escape hatch is narrower than the unit claimed, and the narrowing is in a different place.**\nRelabelling each of the 19 non-`Lifecycle` entries to `Lifecycle` with a neutral reason is refused\nfor **2** of them — the two `Grouping` ones, caught by the subcommand rule. The other 17 pass. The\n`Read | Lifecycle | Flow | Unmodelled` arm refuses only when the entry's own reason string\nvolunteers a `connectors.<domain>.<Command>` token, and that string is written by whoever wants the\nentry.\n\nThe unit's own refused `Forwarded` construction for `connection materialize` raises `[]` once the\nkind column alone is changed.\n\n**A third line-shift citation break in one day.** The diff inserted about 192 lines above\n`ess_claim_fence.rs:156`, and two documents cite that line as what enforces the `naming.wire` rule.\nIt was correct at base — `git show 7b2d680:… | sed -n 156p` is that function's signature. The unit's\nown `every_citation_this_unit_wrote_resolves` passes it because 156 is inside the file, and says in\nits own comment that it does not check the line means what the sentence says.\n\n**A contradiction between the code and both documents.** `cli_surface.rs:961` lowercases the\nqualified name's last segment; both documents say the typed word is that segment \"verbatim and\nun-cased\" and list the eleven CamelCase words it produces. The set holds `supervisechannel` and none\nof the eleven, so the `Unmodelled` contradiction is inert for every wire-less command.\n\n## What held\n\n- Every assertion in the five fence files swept for the guard-clause class: **none remain**. The\n  base's four are gone, and the two inverted ones are confirmed inverted at base and correctly\n  directed now.\n- All 18 new `UNMAPPED:` event markers check out; all 20 event names have zero occurrences under\n  `crates/`, as the markers claim.\n- Every counted claim in the design document: 16 accepted commands, 16 target refusals, 11 of 16\n  carrying no `naming.wire`, 5 request-enum modules, 26 entries / 9 / 17. All correct.\n- The `ess` pin 0.16.0 → 0.18.0: the committed tree regenerates byte-identically, `diff -ru` exit 0.\n- The unit's four lane counts (37 / 83 / 1132 / 75) verified independently.\n- Adding a command costs 4 files beyond the parser, three of them enforced. That narrowing is real.\n  What is free is the kind.\n\n## Findings, verbatim\n\n```findings\n- file: docs/design/19-the-cli-surface.md\n  line: 57\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page cites ess_claim_fence.rs:156 as what enforces the naming.wire rule and this diff moved that rule to lines 348-409, leaving the citation on a whitespace-splitting helper inside fn prose\n- file: ess/system/components.yaml\n  line: 112\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the specification carries the same citation to ess_claim_fence.rs:156 and it lands on the same unrelated helper line, which is the exact class the unit added every_citation_of_the_specification_resolves to close\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1085\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: an entry's kind is a claim the tree can contradict for 2 of the 19 non-Lifecycle entries, because the Read, Lifecycle, Flow and Unmodelled arms refuse only when the entry's own reason string volunteers a declared command name\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 961\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: words_the_specification_can_type lowercases the qualified name's last segment while both documents state the typed word is that segment verbatim and un-cased and name the eleven CamelCase words it produces, so the Unmodelled contradiction is inert for every wire-less command\n- file: crates/connectors-console/tests/adversary_readability_pass2.rs\n  line: 140\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the case quotes \"so that the last column starts on screen\" from output.rs:30 and this diff rewrote that header, so the sentence is nowhere in the file and the cited line now says something else\n- file: ess/system/components.yaml\n  line: 126\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the unspecified-path enumeration is a YAML comment that ess specify validate never reads, and the kind and reason a reviewer would need are not in it — they remain in a test file\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 704\n  category: judgement\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: every_citation_this_unit_wrote_resolves filters to .rs and .yaml while its sibling in ess_citation_fence.rs includes .md, so a .md line citation in the design document would be read by nothing — no such citation exists today\n```\n","title":"Adversary pass 1 against the rewritten CLI surface fences"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 1 — `story:cli-surface-fences-assert-what-they-claim`\n\nWorktree `wt-d6c9d0f84db6`, uncommitted over `7b2d680`. `adp:adversary`.\n214,285 tokens, 85 tool uses, 1,246 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 1252->1261, red 6\norigin: introduced 7, pre-existing 0, undecided 0\nwrote-outside-worktree: 7 paths (all under the assigned scratch root)\nneeds-coordinator: no\n```\n\n## Method\n\nThe brief told the adversary how the previous two passes over this code were beaten — by reading\nassertions and judging them sound — and prescribed construction instead. It carried\ncharacter-for-character copies of the contract's `Unspecified`, `UNSPECIFIED_PATHS`,\n`exception_list_refusals`, `parser_paths` and six more helpers into its probe, so the kind cases run\nthe shipped code rather than a paraphrase, with a control test proving the copies are still copies.\n\n## What it found\n\n**The escape hatch is narrower than the unit claimed, and the narrowing is in a different place.**\nRelabelling each of the 19 non-`Lifecycle` entries to `Lifecycle` with a neutral reason is refused\nfor **2** of them — the two `Grouping` ones, caught by the subcommand rule. The other 17 pass. The\n`Read | Lifecycle | Flow | Unmodelled` arm refuses only when the entry's own reason string\nvolunteers a `connectors.<domain>.<Command>` token, and that string is written by whoever wants the\nentry.\n\nThe unit's own refused `Forwarded` construction for `connection materialize` raises `[]` once the\nkind column alone is changed.\n\n**A third line-shift citation break in one day.** The diff inserted about 192 lines above\n`ess_claim_fence.rs:156`, and two documents cite that line as what enforces the `naming.wire` rule.\nIt was correct at base — `git show 7b2d680:… | sed -n 156p` is that function's signature. The unit's\nown `every_citation_this_unit_wrote_resolves` passes it because 156 is inside the file, and says in\nits own comment that it does not check the line means what the sentence says.\n\n**A contradiction between the code and both documents.** `cli_surface.rs:961` lowercases the\nqualified name's last segment; both documents say the typed word is that segment \"verbatim and\nun-cased\" and list the eleven CamelCase words it produces. The set holds `supervisechannel` and none\nof the eleven, so the `Unmodelled` contradiction is inert for every wire-less command.\n\n## What held\n\n- Every assertion in the five fence files swept for the guard-clause class: **none remain**. The\n  base's four are gone, and the two inverted ones are confirmed inverted at base and correctly\n  directed now.\n- All 18 new `UNMAPPED:` event markers check out; all 20 event names have zero occurrences under\n  `crates/`, as the markers claim.\n- Every counted claim in the design document: 16 accepted commands, 16 target refusals, 11 of 16\n  carrying no `naming.wire`, 5 request-enum modules, 26 entries / 9 / 17. All correct.\n- The `ess` pin 0.16.0 → 0.18.0: the committed tree regenerates byte-identically, `diff -ru` exit 0.\n- The unit's four lane counts (37 / 83 / 1132 / 75) verified independently.\n- Adding a command costs 4 files beyond the parser, three of them enforced. That narrowing is real.\n  What is free is the kind.\n\n## Findings, verbatim\n\n```findings\n- file: docs/design/19-the-cli-surface.md\n  line: 57\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page cites ess_claim_fence.rs:156 as what enforces the naming.wire rule and this diff moved that rule to lines 348-409, leaving the citation on a whitespace-splitting helper inside fn prose\n- file: ess/system/components.yaml\n  line: 112\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the specification carries the same citation to ess_claim_fence.rs:156 and it lands on the same unrelated helper line, which is the exact class the unit added every_citation_of_the_specification_resolves to close\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1085\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: an entry's kind is a claim the tree can contradict for 2 of the 19 non-Lifecycle entries, because the Read, Lifecycle, Flow and Unmodelled arms refuse only when the entry's own reason string volunteers a declared command name\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 961\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: words_the_specification_can_type lowercases the qualified name's last segment while both documents state the typed word is that segment verbatim and un-cased and name the eleven CamelCase words it produces, so the Unmodelled contradiction is inert for every wire-less command\n- file: crates/connectors-console/tests/adversary_readability_pass2.rs\n  line: 140\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the case quotes \"so that the last column starts on screen\" from output.rs:30 and this diff rewrote that header, so the sentence is nowhere in the file and the cited line now says something else\n- file: ess/system/components.yaml\n  line: 126\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the unspecified-path enumeration is a YAML comment that ess specify validate never reads, and the kind and reason a reviewer would need are not in it — they remain in a test file\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 704\n  category: judgement\n  severity: note\n  verdict: INFEASIBLE\n  origin: introduced\n  message: every_citation_this_unit_wrote_resolves filters to .rs and .yaml while its sibling in ess_citation_fence.rs includes .md, so a .md line citation in the design document would be read by nothing — no such citation exists today\n```\n","status":"active","title":"Adversary pass 1 against the rewritten CLI surface fences"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-fences-pass-1"},"payload":{"actor":"human:timo","at":1788537008933,"causation":"cmd-new-review-result-adversary-fences-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-fences-pass-1@1#0~3f2d90019d3b284e","executor":null,"recorded_at":"2026-09-04T15:50:08Z"}}
{"entity":"review-result","version":1,"id":"adversary-fences-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:cli-surface-fences-assert-what-they-claim"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000593","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788537008941,"causation":"cmd-review-result-adversary-fences-pass-1-reviews-story-cli-surface-fences-assert-what-they-claim","change":{"change":"related","relation":"reviews","target":"story:cli-surface-fences-assert-what-they-claim"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-fences-pass-1@1#0~f5e8f660dd1944b1","executor":null,"recorded_at":"2026-09-04T15:50:08Z"}}
{"entity":"review-result","version":1,"id":"adversary-fences-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 2 — `story:cli-surface-fences-assert-what-they-claim`\n\nWorktree `wt-d6c9d0f84db6`, over base `7b2d680`. `adp:adversary`.\n191,801 tokens, 73 tool uses, 1,013 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 45->48, red 3\norigin: introduced 6, pre-existing 0, undecided 0\nwrote-outside-worktree: 7 paths (all under the assigned scratch root)\nneeds-coordinator: yes\n```\n\n## The one that blocks a merge\n\n`crates/connectors-cli/tests/cli_surface.rs:1698` is tracked and opens\n`crates/connectors-cli/tests/adversary_fence_probe.rs` with a `read` that panics when the file is\nabsent. That file is untracked. `cargo test -p connectors-cli` is green in this working tree and\n**red in any clone of the branch that merges it**, panicking in\n`every_declaration_the_adversary_probe_copies_is_still_a_copy` before it compares anything.\n\n## The documented flow the derivation does not see\n\n`docs/design/19-the-cli-surface.md:115` says the twelve non-derived paths, `connect` among them,\nsend no protocol request and that the tree says nothing about them. The adversary walked\n`Command::Connect` → `connect::dispatch` → `LocalClient` → the requests those bodies build,\nasserting each hop so a rename fails loudly rather than emptying the case:\n\n```\nconnectors connect reaches 7 protocol requests through connect::dispatch and LocalClient\n  activate_candidate      CandidateActivate, CandidateSearch\n  begin_connect_session   ConnectSessionCreate\n  finish_connect_session  ConnectSessionStatus, Describe\n  materialize_admitted    Materialize\n  observations            ObservationSearch\n3 carry the naming.wire of a command connectors-service accepts — the derivation's own\ndefinition of Forwarded\n```\n\nThe derivation reads the dispatch arm and not its callee, so a verb whose arm delegates to\n`connectors-console` is invisible to it.\n\n## The unit's own edit, run backwards\n\n`docs/design/19-the-cli-surface.md:111` says the twelve tree-derived kinds \"rest on no sentence at\nall\". The adversary removed `` `CandidateSearch`, `` from the read-verb sentence **in memory only** —\nthe exact edit this unit's diff made — and re-ran the extraction. `connection candidates` derives\n`Read` from the shipped file and `Unmodelled` from the edited one. Eight of the twelve rest on a\nprose enumeration inside a YAML comment.\n\n## What held\n\n- All 12 derived kinds re-derived by hand from `lib.rs:672-869`: 12/12 agree with the shipped kinds,\n  and none agrees for a wrong reason.\n- The read-verb enumeration is true: `CandidateSearch` and `SessionReconcile` are genuine reads, and\n  the read/wire partition covers every variant of all three enums exactly once, `SessionSignal`\n  excepted and correctly `Unmodelled`.\n- Laundering a kind by adding a verb to the enumeration is refused — `operation signal` → `Read`\n  leaves `Unmodelled` unused and `every_kind_of_exception_is_used_…` catches it.\n- Renaming a leaf with `#[command(name = …)]` is refused.\n- The 14 re-synced declarations in the pass-1 probe are verbatim, verified independently.\n- `ess` 0.18.0 regenerates the committed tree byte-identically, `diff -ru` exit 0.\n\n## Findings, verbatim\n\n```findings\n- file: docs/design/19-the-cli-surface.md\n  line: 115\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page says the twelve non-derived paths including `connect` send no protocol request, and `connectors connect` builds seven through `connect::dispatch` and `LocalClient`, three of them carrying the `naming.wire` of a command `connectors-service` accepts.\n- file: docs/design/19-the-cli-surface.md\n  line: 111\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page says the twelve tree-derived kinds \"rest on no sentence at all\", and the eight `Read` ones are decided by a prose enumeration inside a YAML comment that this unit's own diff edited to change one of them.\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1698\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the tracked contract suite opens the untracked `adversary_fence_probe.rs` with a panicking `read`, so `cargo test -p connectors-cli` is green in this working tree and red in any clone of the branch that merges it.\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1117\n  category: judgement\n  severity: warning\n  verdict: INFEASIBLE\n  origin: introduced\n  message: the derivation's group names and protocol prefixes are hand-kept literals, the exact shape `deployment_protocol_modules` was corrected away from in this same diff, and `crates/protocol/src` declares five request enums rather than three - unreachable today because no fourth group command exists.\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1706\n  category: mutant\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the copy-control derives 14 shared declarations (all verbatim, independently verified) but guards on at least 11, so three copies may leave the comparison silently, and struct or pub fn declarations are invisible to its extractor entirely.\n- file: docs/design/19-the-cli-surface.md\n  line: 112\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: only `12 of the 26` is held to the derivation, and by a contains check, so the `2 more` grouping count and the named residual twelve can drift the moment the derivation's coverage changes.\n```\n","title":"Adversary pass 2 against the rewritten CLI surface fences"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 2 — `story:cli-surface-fences-assert-what-they-claim`\n\nWorktree `wt-d6c9d0f84db6`, over base `7b2d680`. `adp:adversary`.\n191,801 tokens, 73 tool uses, 1,013 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 45->48, red 3\norigin: introduced 6, pre-existing 0, undecided 0\nwrote-outside-worktree: 7 paths (all under the assigned scratch root)\nneeds-coordinator: yes\n```\n\n## The one that blocks a merge\n\n`crates/connectors-cli/tests/cli_surface.rs:1698` is tracked and opens\n`crates/connectors-cli/tests/adversary_fence_probe.rs` with a `read` that panics when the file is\nabsent. That file is untracked. `cargo test -p connectors-cli` is green in this working tree and\n**red in any clone of the branch that merges it**, panicking in\n`every_declaration_the_adversary_probe_copies_is_still_a_copy` before it compares anything.\n\n## The documented flow the derivation does not see\n\n`docs/design/19-the-cli-surface.md:115` says the twelve non-derived paths, `connect` among them,\nsend no protocol request and that the tree says nothing about them. The adversary walked\n`Command::Connect` → `connect::dispatch` → `LocalClient` → the requests those bodies build,\nasserting each hop so a rename fails loudly rather than emptying the case:\n\n```\nconnectors connect reaches 7 protocol requests through connect::dispatch and LocalClient\n  activate_candidate      CandidateActivate, CandidateSearch\n  begin_connect_session   ConnectSessionCreate\n  finish_connect_session  ConnectSessionStatus, Describe\n  materialize_admitted    Materialize\n  observations            ObservationSearch\n3 carry the naming.wire of a command connectors-service accepts — the derivation's own\ndefinition of Forwarded\n```\n\nThe derivation reads the dispatch arm and not its callee, so a verb whose arm delegates to\n`connectors-console` is invisible to it.\n\n## The unit's own edit, run backwards\n\n`docs/design/19-the-cli-surface.md:111` says the twelve tree-derived kinds \"rest on no sentence at\nall\". The adversary removed `` `CandidateSearch`, `` from the read-verb sentence **in memory only** —\nthe exact edit this unit's diff made — and re-ran the extraction. `connection candidates` derives\n`Read` from the shipped file and `Unmodelled` from the edited one. Eight of the twelve rest on a\nprose enumeration inside a YAML comment.\n\n## What held\n\n- All 12 derived kinds re-derived by hand from `lib.rs:672-869`: 12/12 agree with the shipped kinds,\n  and none agrees for a wrong reason.\n- The read-verb enumeration is true: `CandidateSearch` and `SessionReconcile` are genuine reads, and\n  the read/wire partition covers every variant of all three enums exactly once, `SessionSignal`\n  excepted and correctly `Unmodelled`.\n- Laundering a kind by adding a verb to the enumeration is refused — `operation signal` → `Read`\n  leaves `Unmodelled` unused and `every_kind_of_exception_is_used_…` catches it.\n- Renaming a leaf with `#[command(name = …)]` is refused.\n- The 14 re-synced declarations in the pass-1 probe are verbatim, verified independently.\n- `ess` 0.18.0 regenerates the committed tree byte-identically, `diff -ru` exit 0.\n\n## Findings, verbatim\n\n```findings\n- file: docs/design/19-the-cli-surface.md\n  line: 115\n  category: acceptance\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page says the twelve non-derived paths including `connect` send no protocol request, and `connectors connect` builds seven through `connect::dispatch` and `LocalClient`, three of them carrying the `naming.wire` of a command `connectors-service` accepts.\n- file: docs/design/19-the-cli-surface.md\n  line: 111\n  category: contract-drift\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the page says the twelve tree-derived kinds \"rest on no sentence at all\", and the eight `Read` ones are decided by a prose enumeration inside a YAML comment that this unit's own diff edited to change one of them.\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1698\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the tracked contract suite opens the untracked `adversary_fence_probe.rs` with a panicking `read`, so `cargo test -p connectors-cli` is green in this working tree and red in any clone of the branch that merges it.\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1117\n  category: judgement\n  severity: warning\n  verdict: INFEASIBLE\n  origin: introduced\n  message: the derivation's group names and protocol prefixes are hand-kept literals, the exact shape `deployment_protocol_modules` was corrected away from in this same diff, and `crates/protocol/src` declares five request enums rather than three - unreachable today because no fourth group command exists.\n- file: crates/connectors-cli/tests/cli_surface.rs\n  line: 1706\n  category: mutant\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: the copy-control derives 14 shared declarations (all verbatim, independently verified) but guards on at least 11, so three copies may leave the comparison silently, and struct or pub fn declarations are invisible to its extractor entirely.\n- file: docs/design/19-the-cli-surface.md\n  line: 112\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: only `12 of the 26` is held to the derivation, and by a contains check, so the `2 more` grouping count and the named residual twelve can drift the moment the derivation's coverage changes.\n```\n","status":"active","title":"Adversary pass 2 against the rewritten CLI surface fences"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-fences-pass-2"},"payload":{"actor":"human:timo","at":1788538997786,"causation":"cmd-new-review-result-adversary-fences-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-fences-pass-2@1#0~259b8769bcdd351f","executor":null,"recorded_at":"2026-09-04T16:23:17Z"}}
{"entity":"review-result","version":1,"id":"adversary-fences-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:cli-surface-fences-assert-what-they-claim"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000598","target":"01MEM0000000000000157"},"payload":{"actor":"human:timo","at":1788538997802,"causation":"cmd-review-result-adversary-fences-pass-2-reviews-story-cli-surface-fences-assert-what-they-claim","change":{"change":"related","relation":"reviews","target":"story:cli-surface-fences-assert-what-they-claim"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-fences-pass-2@1#0~5fcc8814b2178353","executor":null,"recorded_at":"2026-09-04T16:23:17Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":10,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-fences-pass-1","source":"review-result:adversary-fences-pass-1","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539784000,"causation":"cmd-evidence-story-cli-surface-fences-assert-what-they-claim-review_outcome-1788539784961","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-fences-pass-1","source":"review-result:adversary-fences-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-surface-fences-assert-what-they-claim@10#0~338e4f93eb2b232b","executor":null,"recorded_at":"2026-09-04T16:36:24Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":10,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-fences-pass-2","source":"review-result:adversary-fences-pass-2","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539785000,"causation":"cmd-evidence-story-cli-surface-fences-assert-what-they-claim-review_outcome-1788539785116","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-fences-pass-2","source":"review-result:adversary-fences-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-surface-fences-assert-what-they-claim@10#0~c80885629a336b31","executor":null,"recorded_at":"2026-09-04T16:36:25Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":11,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539785273,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788539785273","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@11#0~3c38eeaa97ca5b7f","executor":null,"recorded_at":"2026-09-04T16:36:25Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":12,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539785426,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788539785426","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@12#0~b4cf3db3df4c154f","executor":null,"recorded_at":"2026-09-04T16:36:25Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":13,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539785572,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788539785572","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@13#0~89d8d33d79ca6b79","executor":null,"recorded_at":"2026-09-04T16:36:25Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":14,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_budget_prose.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_budget_prose.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539785712,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788539785712","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@14#0~5f685685e2e62e21","executor":null,"recorded_at":"2026-09-04T16:36:25Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":15,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_budget_prose.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_budget_prose.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539785851,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788539785851","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@15#0~aaeec392701f9464","executor":null,"recorded_at":"2026-09-04T16:36:25Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":16,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_budget_prose.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":".github/workflows/release.yml"},{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"crates/catalog-build/tests/main/dependency_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_citation_fence.rs"},{"confidence":"cited","path":"crates/catalog-build/tests/main/ess_claim_fence.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/adversary_fence_probe_pass2.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface.rs"},{"confidence":"cited","path":"crates/connectors-cli/tests/cli_surface_drift.rs"},{"confidence":"cited","path":"crates/connectors-console/src/output.rs"},{"confidence":"cited","path":"crates/connectors-console/tests/adversary_budget_prose.rs"},{"confidence":"cited","path":"docs/design/19-the-cli-surface.md"},{"confidence":"cited","path":"ess/system/components.yaml"}]},"command":"update-entity","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788539786005,"causation":"cmd-protocol-artifact-scope-story-cli-surface-fences-assert-what-they-claim-1788539786005","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:cli-surface-fences-assert-what-they-claim@16#0~24a49004b44f0fcf","executor":null,"recorded_at":"2026-09-04T16:36:26Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":16,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"bash scripts/gate.sh sharded over merge commit b1d9e18: 13 steps, every exit 0, 484s, disk floor 80G. connectors-cli 49/49, connectors-console 86/86, catalog-build --test main 75/75, root 1132/1132.","target":"01MEM0000000000000160"},"payload":{"actor":"human:timo","at":1788547200000,"causation":"cmd-evidence-story-cli-surface-fences-assert-what-they-claim-test_result-1788540286332","change":{"change":"evidence","kind":"test_result","source":"bash scripts/gate.sh sharded over merge commit b1d9e18: 13 steps, every exit 0, 484s, disk floor 80G. connectors-cli 49/49, connectors-console 86/86, catalog-build --test main 75/75, root 1132/1132."},"correlation":"protocol-artifact-evidence","event_id":"story:cli-surface-fences-assert-what-they-claim@16#0~d3413d5a2d62d566","executor":null,"recorded_at":"2026-09-04T18:40:00Z"}}
{"entity":"story","version":1,"id":"cli-surface-fences-assert-what-they-claim","revision":17,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":2}}","target":"01MEM0000000000000160","to":"implemented"},"payload":{"actor":"human:timo","at":1788540286821,"causation":"cmd-move-story-cli-surface-fences-assert-what-they-claim-1788540286821","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:cli-surface-fences-assert-what-they-claim@17#0~4b97f7473ff33e7d","executor":null,"recorded_at":"2026-09-04T16:44:46Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000151","to":"proposed"},"payload":{"actor":"human:timo","at":1788541132440,"causation":"cmd-move-story-cli-first-level-groups-1788541132440","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:cli-first-level-groups@4#0~26eeabe1c5ef5981","executor":null,"recorded_at":"2026-09-04T16:58:52Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000151","to":"active"},"payload":{"actor":"human:timo","at":1788541132657,"causation":"cmd-move-story-cli-first-level-groups-1788541132657","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:cli-first-level-groups@5#0~438705f98c5491e9","executor":null,"recorded_at":"2026-09-04T16:58:52Z"}}
{"entity":"review-result","version":1,"id":"adversary-categorize-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 1 — `story:cli-first-level-groups`\n\nWorktree `wt-e9b482b00009`, uncommitted over `967062d`. `adp:adversary`.\n128,662 tokens, 60 tool uses, 875 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 54->61, red 7\norigin: introduced 7, pre-existing 0, undecided 0\nwrote-outside-worktree: 2 paths\nneeds-coordinator: yes — the tree moved under the adversary at 19:36\n```\n\n## Four regressions in the compatibility shim\n\nEach is a path that worked at the base commit and does not now.\n\n| construction | result |\n|---|---|\n| `connectors --output json <any moved word>` | all **11** entries fail; the shim reads `argv[1]`, finds `-o`, matches nothing |\n| `connectors help <moved word>` | **10 of 11** exit 2 `unrecognized subcommand`, and `help` is a word `connectors --help` advertises |\n| `connectors serve help` | rewritten to `serve local help` and refused, while `connectors help serve` prints `help` in the group's own `Commands:` list |\n| `connectors auth` | exits 2 naming no destination; `MOVED` carries `auth status` and nothing for `auth` |\n\nThe sharpest form, measured by hand:\n\n```\n$ connectors serve --config /nope/x.toml\nnote: `connectors serve` is now `connectors serve local`, and the old path works for one more release\nruntime: Connector configuration could not be read: No such file or directory (os error 2)\n\n$ connectors -o json serve --config /nope/x.toml\nerror: unexpected argument '--config' found\n```\n\nThe same argv means two different commands depending on whether a global flag precedes it.\n\nThe `serve help` case has a mechanism worth recording: the guard calls `find_subcommand` on\n`Cli::command()`, which the derive returns **unbuilt**. clap adds `help` in `_build_self`, so the\nguard cannot see the group's fourth command.\n\n## Two the coordinator caused\n\nThe operator instructed the `CLI_TOTAL_LINE_LIMIT` fence removed entirely — a line count that had\nbeen raised six times (856, 960, 966, 1006, 1014, 1127) and had never once moved code out of the\nbinary. The coordinator removed it while this adversary was running.\n\n- `docs/design/19-the-cli-surface.md:182` still gives that cap as the first of \"Two reasons, both\n  measured\" for keeping `ess/generated/clap/` outside the crate. The claim outlived the fence.\n- The removal shrank `architecture_fence.rs` from about 648 lines to 616, and the design document\n  cites `:607-621`. `cli_surface.rs::every_citation_this_unit_wrote_resolves` is **red** on the tree\n  as handed, so the unit's declared 53-green does not hold.\n\nThat is the fifth `path:line` citation broken by a line shift in one day, and the only one caused by\nthe coordinator's own edit rather than an agent's.\n\n## What held\n\n- `ess/generated/clap/` byte-identical under the installed `ess`; `diff -r` exit 0.\n- Completion scripts render for all five shells: bash 2735, zsh 2151, fish 401, powershell 762,\n  elvish 635 lines. The legacy `connectors completions fish` works and prints the note.\n- The guard's suppression direction has no wrong instance: `serve` is the only moved word that is\n  also a group, and `serve local|hosted|mcp` were never legacy paths.\n- Every `MOVED` entry driven bare with `--help` is green.\n- The root workspace suite including `catalog-build --test main` (75) is green.\n\n## Findings, verbatim\n\n```findings\n- file: crates/connectors-cli/src/lib.rs\n  line: 492\n  category: boundary\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the shim computes its leading token sequence from argv[1] without skipping the global -o/--output flag, so all eleven legacy paths fail outright when the documented format flag precedes the word that moved, and `connectors -o json serve --config X` refuses what `connectors serve --config X` runs.\n- file: crates/connectors-cli/src/lib.rs\n  line: 506\n  category: boundary\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the table is matched at argv[1] only, so `connectors help <moved word>` — the path `connectors --help` itself advertises — exits 2 with `unrecognized subcommand` for ten of the eleven entries.\n- file: crates/connectors-cli/src/lib.rs\n  line: 498\n  category: contract-drift\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the guard calls find_subcommand on an unbuilt Cli::command(), which has no clap-generated `help` subcommand, so `connectors serve help` is rewritten to `serve local help` and refused even though `connectors help serve` prints `help` in the group's own Commands list.\n- file: crates/connectors-cli/src/lib.rs\n  line: 468\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: MOVED carries the two-word `auth status` and no one-word `auth`, so `connectors auth` and `connectors auth --help` — a first-level group at the base commit — exit 2 naming no destination.\n- file: crates/catalog-build/tests/main/architecture_fence.rs\n  line: 12\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: CLI_TOTAL_LINE_LIMIT and its assertion were removed on the operator's instruction, and docs/design/19-the-cli-surface.md:182 still gives that cap as the first of two measured reasons for keeping ess/generated/clap outside the crate.\n- file: docs/design/19-the-cli-surface.md\n  line: 184\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the unit's own cli_surface.rs::every_citation_this_unit_wrote_resolves is red on the tree as handed because the page cites architecture_fence.rs:607-621 and that file now has 616 lines, so the declared 53-green does not hold.\n- file: crates/connectors-cli/tests/moved_paths_are_not_taught.rs\n  line: 141\n  category: mutant\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the printed-path fence requires the moved words to sit adjacent to `connectors `, so a global flag between them hides the site, and it misses crates/connectors-console/src/output.rs:176 which is inside the class the unit itself corrected at output.rs:449.\n```\n","title":"Adversary pass 1 against the categorized top level"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 1 — `story:cli-first-level-groups`\n\nWorktree `wt-e9b482b00009`, uncommitted over `967062d`. `adp:adversary`.\n128,662 tokens, 60 tool uses, 875 s.\n\n## Header, as returned\n\n```\nverdict: red\ncases: executed 54->61, red 7\norigin: introduced 7, pre-existing 0, undecided 0\nwrote-outside-worktree: 2 paths\nneeds-coordinator: yes — the tree moved under the adversary at 19:36\n```\n\n## Four regressions in the compatibility shim\n\nEach is a path that worked at the base commit and does not now.\n\n| construction | result |\n|---|---|\n| `connectors --output json <any moved word>` | all **11** entries fail; the shim reads `argv[1]`, finds `-o`, matches nothing |\n| `connectors help <moved word>` | **10 of 11** exit 2 `unrecognized subcommand`, and `help` is a word `connectors --help` advertises |\n| `connectors serve help` | rewritten to `serve local help` and refused, while `connectors help serve` prints `help` in the group's own `Commands:` list |\n| `connectors auth` | exits 2 naming no destination; `MOVED` carries `auth status` and nothing for `auth` |\n\nThe sharpest form, measured by hand:\n\n```\n$ connectors serve --config /nope/x.toml\nnote: `connectors serve` is now `connectors serve local`, and the old path works for one more release\nruntime: Connector configuration could not be read: No such file or directory (os error 2)\n\n$ connectors -o json serve --config /nope/x.toml\nerror: unexpected argument '--config' found\n```\n\nThe same argv means two different commands depending on whether a global flag precedes it.\n\nThe `serve help` case has a mechanism worth recording: the guard calls `find_subcommand` on\n`Cli::command()`, which the derive returns **unbuilt**. clap adds `help` in `_build_self`, so the\nguard cannot see the group's fourth command.\n\n## Two the coordinator caused\n\nThe operator instructed the `CLI_TOTAL_LINE_LIMIT` fence removed entirely — a line count that had\nbeen raised six times (856, 960, 966, 1006, 1014, 1127) and had never once moved code out of the\nbinary. The coordinator removed it while this adversary was running.\n\n- `docs/design/19-the-cli-surface.md:182` still gives that cap as the first of \"Two reasons, both\n  measured\" for keeping `ess/generated/clap/` outside the crate. The claim outlived the fence.\n- The removal shrank `architecture_fence.rs` from about 648 lines to 616, and the design document\n  cites `:607-621`. `cli_surface.rs::every_citation_this_unit_wrote_resolves` is **red** on the tree\n  as handed, so the unit's declared 53-green does not hold.\n\nThat is the fifth `path:line` citation broken by a line shift in one day, and the only one caused by\nthe coordinator's own edit rather than an agent's.\n\n## What held\n\n- `ess/generated/clap/` byte-identical under the installed `ess`; `diff -r` exit 0.\n- Completion scripts render for all five shells: bash 2735, zsh 2151, fish 401, powershell 762,\n  elvish 635 lines. The legacy `connectors completions fish` works and prints the note.\n- The guard's suppression direction has no wrong instance: `serve` is the only moved word that is\n  also a group, and `serve local|hosted|mcp` were never legacy paths.\n- Every `MOVED` entry driven bare with `--help` is green.\n- The root workspace suite including `catalog-build --test main` (75) is green.\n\n## Findings, verbatim\n\n```findings\n- file: crates/connectors-cli/src/lib.rs\n  line: 492\n  category: boundary\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the shim computes its leading token sequence from argv[1] without skipping the global -o/--output flag, so all eleven legacy paths fail outright when the documented format flag precedes the word that moved, and `connectors -o json serve --config X` refuses what `connectors serve --config X` runs.\n- file: crates/connectors-cli/src/lib.rs\n  line: 506\n  category: boundary\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the table is matched at argv[1] only, so `connectors help <moved word>` — the path `connectors --help` itself advertises — exits 2 with `unrecognized subcommand` for ten of the eleven entries.\n- file: crates/connectors-cli/src/lib.rs\n  line: 498\n  category: contract-drift\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: the guard calls find_subcommand on an unbuilt Cli::command(), which has no clap-generated `help` subcommand, so `connectors serve help` is rewritten to `serve local help` and refused even though `connectors help serve` prints `help` in the group's own Commands list.\n- file: crates/connectors-cli/src/lib.rs\n  line: 468\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: MOVED carries the two-word `auth status` and no one-word `auth`, so `connectors auth` and `connectors auth --help` — a first-level group at the base commit — exit 2 naming no destination.\n- file: crates/catalog-build/tests/main/architecture_fence.rs\n  line: 12\n  category: acceptance\n  severity: blocker\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: CLI_TOTAL_LINE_LIMIT and its assertion were removed on the operator's instruction, and docs/design/19-the-cli-surface.md:182 still gives that cap as the first of two measured reasons for keeping ess/generated/clap outside the crate.\n- file: docs/design/19-the-cli-surface.md\n  line: 184\n  category: contract-drift\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: the unit's own cli_surface.rs::every_citation_this_unit_wrote_resolves is red on the tree as handed because the page cites architecture_fence.rs:607-621 and that file now has 616 lines, so the declared 53-green does not hold.\n- file: crates/connectors-cli/tests/moved_paths_are_not_taught.rs\n  line: 141\n  category: mutant\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: the printed-path fence requires the moved words to sit adjacent to `connectors `, so a global flag between them hides the site, and it misses crates/connectors-console/src/output.rs:176 which is inside the class the unit itself corrected at output.rs:449.\n```\n","status":"active","title":"Adversary pass 1 against the categorized top level"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-categorize-pass-1"},"payload":{"actor":"human:timo","at":1788544126823,"causation":"cmd-new-review-result-adversary-categorize-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-categorize-pass-1@1#0~ac365a09fc5cf4ed","executor":null,"recorded_at":"2026-09-04T17:48:46Z"}}
{"entity":"review-result","version":1,"id":"adversary-categorize-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:cli-first-level-groups"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000603","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788544126833,"causation":"cmd-review-result-adversary-categorize-pass-1-reviews-story-cli-first-level-groups","change":{"change":"related","relation":"reviews","target":"story:cli-first-level-groups"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-categorize-pass-1@1#0~b6a9c5a800d989a2","executor":null,"recorded_at":"2026-09-04T17:48:46Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: the top level reads as categories, not as sixteen commands\n\n## Defect\n\n`connectors --help` lists 16 commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at the same level despite being five different activities, so the help output does\nnot tell an operator which of them they want.\n\nThere is no help-only fix. clap 4.6.6 renders exactly one `Commands:` section per level:\n`subcommand_help_heading` renames that section and does not partition it\n(`clap_builder-4.6.6/src/output/help_template.rs:403-416`), and `flatten_help` emits one heading per\nleaf with its arguments rather than a grouped index (`:878-936`). The grouping has to be real\nnesting.\n\n## Shape\n\nTop level goes 16 to 8. The three everyday verbs keep their paths, because they are the point of the\ntool and lengthening them costs every invocation.\n\n| group | members | was |\n|---|---|---|\n| `setup` | `init`, `connect`, `completions` | `connectors init`, `connect`, `completions` |\n| `inspect` | `doctor`, `providers`, `auth` | `connectors doctor`, `providers`, `auth status` |\n| `session` | `login`, `logout` | `connectors login`, `logout` |\n| `serve` | `local`, `hosted`, `mcp` | `connectors serve`, `serve-hosted`, `mcp` |\n\n`operation`, `connection`, `event` and `admin` stay at the top level.\n\n`auth` collapses: `AuthCommand` (`lib.rs:182-191`) has exactly one variant, so `connectors inspect\nauth` replaces `connectors auth status` rather than becoming a third level.\n\nFour subcommand enums hold the moved variants verbatim — the variants move, they are not copied.\n\n## Compatibility\n\nOld paths keep working for one release. In `run_from` a const table maps a legacy path to the one it\nmoved to before clap is handed the argv, and one stderr line names the new path. It compares whole\nliteral words and never reads an option's value: clap remains the only parser, and the rule that a\nRust CLI uses clap derive is not bent by it.\n\n**It does not read `argv[1]`, because the word a person names is not always `argv[1]`.** That was the\nfirst version and it was wrong in four ways, each a path that worked before the move:\n\n| typed | was |\n|---|---|\n| `connectors --output json <word>` | all eleven entries refused; `--output` is `global = true` |\n| `connectors help <word>` | ten of eleven refused, and `help` is a word `--help` advertises |\n| `connectors serve help` | rewritten to `serve local help` and refused |\n| `connectors auth` | refused, naming no destination |\n\nThe table is matched after stepping over the root's global options — read off the parser rather than\nnamed, so a global added later needs no edit here — and over `help`. `-o json`, `--output json`,\n`--output=json` and `-ojson` are one class and all four are stepped over.\n\nTwo argvs are left alone. One this release already reads, asked of a **built** `clap::Command`,\nbecause `help` is a subcommand only after `build`. And, under `help`, a single word naming a command\nof this release: `connectors help serve` prints the group, while a bare `serve` is the word that\nmoved.\n\nThe longest matching entry wins, so `auth` and `auth status` can both be in the table — `auth` was a\nfirst-level group whose one command moved — and the order of the table decides nothing.\n\n## Fence\n\n**The CLI line cap is gone.** `CLI_TOTAL_LINE_LIMIT` was removed on 2026-09-04 by operator\ninstruction. It had been raised at every one of the six times it fired — 856, 960, 966, 1006, 1014,\n1127 — so it never once moved code out of the binary, which is the only thing it was for. What it\ndid do was cost: raising it inserts lines into `architecture_fence.rs`, and citations into that file\nbroke that way twice in one day.\n\n`product_cli_is_a_thin_frontend` still bounds the frontend by what it may **link**\n(`CLI_DEPENDENCIES`) and what it may **declare** (the forbidden symbols). Those measure the property\ndirectly and refuse rather than negotiate.\n\n## Cross-repository\n\n`zwirn` forwards argv verbatim into `connectors_cli::run_from`\n(`zwirn/crates/agent-app/src/main.rs:124`) and pins this repository at rev `1e0eb9f`\n(`zwirn/Cargo.toml:25`), so `zwirn connectors …` changes only when that pin moves. Renaming something\nanother repository verifies is a coordinated migration with an ADR (`AGENTS.md:7-8`); the ADR and\nthe pin bump are a follow-up, named in the report, not a blocker for this story.\n\n## Acceptance\n\n- `connectors --help` lists 8 commands.\n- `connectors doctor` produces the same output as `connectors inspect doctor`, plus one stderr line\n  naming the new path.\n- The contract test passes, so the specification and the tree agree after the move.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-surface-contract`.\n"},"args":{"changes":{"body":"# Story: the top level reads as categories, not as sixteen commands\n\n## Defect\n\n`connectors --help` lists 16 commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at the same level despite being five different activities, so the help output does\nnot tell an operator which of them they want.\n\nThere is no help-only fix. clap 4.6.6 renders exactly one `Commands:` section per level:\n`subcommand_help_heading` renames that section and does not partition it\n(`clap_builder-4.6.6/src/output/help_template.rs:403-416`), and `flatten_help` emits one heading per\nleaf with its arguments rather than a grouped index (`:878-936`). The grouping has to be real\nnesting.\n\n## Shape\n\nTop level goes 16 to 8. The three everyday verbs keep their paths, because they are the point of the\ntool and lengthening them costs every invocation.\n\n| group | members | was |\n|---|---|---|\n| `setup` | `init`, `connect`, `completions` | `connectors init`, `connect`, `completions` |\n| `inspect` | `doctor`, `providers`, `auth` | `connectors doctor`, `providers`, `auth status` |\n| `session` | `login`, `logout` | `connectors login`, `logout` |\n| `serve` | `local`, `hosted`, `mcp` | `connectors serve`, `serve-hosted`, `mcp` |\n\n`operation`, `connection`, `event` and `admin` stay at the top level.\n\n`auth` collapses: `AuthCommand` (`lib.rs:182-191`) has exactly one variant, so `connectors inspect\nauth` replaces `connectors auth status` rather than becoming a third level.\n\nFour subcommand enums hold the moved variants verbatim — the variants move, they are not copied.\n\n## Compatibility\n\nOld paths keep working for one release. In `run_from` a const table maps a legacy path to the one it\nmoved to before clap is handed the argv, and one stderr line names the new path. It compares whole\nliteral words and never reads an option's value: clap remains the only parser, and the rule that a\nRust CLI uses clap derive is not bent by it.\n\n**It does not read `argv[1]`, because the word a person names is not always `argv[1]`.** That was the\nfirst version and it was wrong in four ways, each a path that worked before the move:\n\n| typed | was |\n|---|---|\n| `connectors --output json <word>` | all eleven entries refused; `--output` is `global = true` |\n| `connectors help <word>` | ten of eleven refused, and `help` is a word `--help` advertises |\n| `connectors serve help` | rewritten to `serve local help` and refused |\n| `connectors auth` | refused, naming no destination |\n\nThe table is matched after stepping over the root's global options — read off the parser rather than\nnamed, so a global added later needs no edit here — and over `help`. `-o json`, `--output json`,\n`--output=json` and `-ojson` are one class and all four are stepped over.\n\nTwo argvs are left alone. One this release already reads, asked of a **built** `clap::Command`,\nbecause `help` is a subcommand only after `build`. And, under `help`, a single word naming a command\nof this release: `connectors help serve` prints the group, while a bare `serve` is the word that\nmoved.\n\nThe longest matching entry wins, so `auth` and `auth status` can both be in the table — `auth` was a\nfirst-level group whose one command moved — and the order of the table decides nothing.\n\n## Fence\n\n**The CLI line cap is gone.** `CLI_TOTAL_LINE_LIMIT` was removed on 2026-09-04 by operator\ninstruction. It had been raised at every one of the six times it fired — 856, 960, 966, 1006, 1014,\n1127 — so it never once moved code out of the binary, which is the only thing it was for. What it\ndid do was cost: raising it inserts lines into `architecture_fence.rs`, and citations into that file\nbroke that way twice in one day.\n\n`product_cli_is_a_thin_frontend` still bounds the frontend by what it may **link**\n(`CLI_DEPENDENCIES`) and what it may **declare** (the forbidden symbols). Those measure the property\ndirectly and refuse rather than negotiate.\n\n## Cross-repository\n\n`zwirn` forwards argv verbatim into `connectors_cli::run_from`\n(`zwirn/crates/agent-app/src/main.rs:124`) and pins this repository at rev `1e0eb9f`\n(`zwirn/Cargo.toml:25`), so `zwirn connectors …` changes only when that pin moves. Renaming something\nanother repository verifies is a coordinated migration with an ADR (`AGENTS.md:7-8`); the ADR and\nthe pin bump are a follow-up, named in the report, not a blocker for this story.\n\n## Acceptance\n\n- `connectors --help` lists 8 commands.\n- `connectors doctor` produces the same output as `connectors inspect doctor`, plus one stderr line\n  naming the new path.\n- The contract test passes, so the specification and the tree agree after the move.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-surface-contract`.\n"},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788545244628,"causation":"cmd-protocol-artifact-body-story-cli-first-level-groups-1788545244628","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:cli-first-level-groups@6#0~f5844ed33d618033","executor":null,"recorded_at":"2026-09-04T18:07:24Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:492","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788553112000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788553112397","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:492","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@6#0~e9042a70e635fef0","executor":null,"recorded_at":"2026-09-04T20:18:32Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:506","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788553112000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788553112570","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:506","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@6#0~8cd857d2666e2b4a","executor":null,"recorded_at":"2026-09-04T20:18:32Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:498","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788553112000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788553112721","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:498","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@6#0~66408f0ff1f2edee","executor":null,"recorded_at":"2026-09-04T20:18:32Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:468","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788553112000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788553112993","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:468","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@6#0~b83e4e2886b96c3f","executor":null,"recorded_at":"2026-09-04T20:18:32Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/catalog-build/tests/main/architecture_fence.rs:12","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788553113000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788553113293","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/catalog-build/tests/main/architecture_fence.rs:12","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@6#0~c266044a3130b5d0","executor":null,"recorded_at":"2026-09-04T20:18:33Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"docs/design/19-the-cli-surface.md:184","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788553113000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788553113557","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"docs/design/19-the-cli-surface.md:184","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@6#0~c1d9e764eefee95b","executor":null,"recorded_at":"2026-09-04T20:18:33Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/tests/moved_paths_are_not_taught.rs:141","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788553113000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788553113731","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/tests/moved_paths_are_not_taught.rs:141","review":"review-result:adversary-categorize-pass-1","source":"review-result:adversary-categorize-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@6#0~cc3d7fe03c118944","executor":null,"recorded_at":"2026-09-04T20:18:33Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: the top level reads as categories, not as sixteen commands\n\n## Defect\n\n`connectors --help` lists 16 commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at the same level despite being five different activities, so the help output does\nnot tell an operator which of them they want.\n\nThere is no help-only fix. clap 4.6.6 renders exactly one `Commands:` section per level:\n`subcommand_help_heading` renames that section and does not partition it\n(`clap_builder-4.6.6/src/output/help_template.rs:403-416`), and `flatten_help` emits one heading per\nleaf with its arguments rather than a grouped index (`:878-936`). The grouping has to be real\nnesting.\n\n## Shape\n\nTop level goes 16 to 8. The three everyday verbs keep their paths, because they are the point of the\ntool and lengthening them costs every invocation.\n\n| group | members | was |\n|---|---|---|\n| `setup` | `init`, `connect`, `completions` | `connectors init`, `connect`, `completions` |\n| `inspect` | `doctor`, `providers`, `auth` | `connectors doctor`, `providers`, `auth status` |\n| `session` | `login`, `logout` | `connectors login`, `logout` |\n| `serve` | `local`, `hosted`, `mcp` | `connectors serve`, `serve-hosted`, `mcp` |\n\n`operation`, `connection`, `event` and `admin` stay at the top level.\n\n`auth` collapses: `AuthCommand` (`lib.rs:182-191`) has exactly one variant, so `connectors inspect\nauth` replaces `connectors auth status` rather than becoming a third level.\n\nFour subcommand enums hold the moved variants verbatim — the variants move, they are not copied.\n\n## Compatibility\n\nOld paths keep working for one release. `moved` rewrites a legacy path before the command runs, and\none stderr line names the path it moved to.\n\n**Nothing in it reads an option's syntax.** clap parses argv, and each parse it performs answers one\nquestion:\n\n1. **Is a word that moved present at all?** Unless one of the first `LEGACY_WINDOW` — 8 — words is\n   the first word of a `MOVED` row, no tree is built and nothing is allocated. Every current path\n   pays this and nothing else.\n2. **Does this release already read the argv?** The real tree is asked to parse it. Only two\n   refusals mean a word that moved: `InvalidSubcommand` — `doctor`, `auth status`, `help doctor` —\n   and `UnknownArgument`, which is `serve --config` at the group that took the old leaf's name.\n   Every other outcome is clap's to answer as it stands: a parse, a help request, a version\n   request, a missing value.\n3. **Which path was typed?** `read_one_word` reads the argv one word at a time, each read stepping\n   over the global options in front of that word — asked of the parser, not named here, so a global\n   added later needs no edit. `-o json`, `--output json`, `--output=json` and `-ojson` are one case,\n   because clap's option syntax is clap's. The argv handed on is rebuilt from what clap read: the\n   globals, `help` if it was there, the new words, then the tail verbatim.\n\nThe first version pre-scanned argv from `argv[1]` with a hand-written `spans` that reimplemented\nclap's four option-value spellings. That is the argv parsing a Rust CLI does not do, and it was\nwrong in six ways, each a path that worked before the move:\n\n| typed | was |\n|---|---|\n| `connectors --output json <word>` | all eleven entries refused; `--output` is `global = true` |\n| `connectors help <word>` | ten of eleven refused, and `help` is a word `--help` advertises |\n| `connectors serve help` | rewritten to `serve local help` and refused |\n| `connectors auth` | refused, naming no destination |\n| `connectors auth help` | the one-word `auth` row put the group's own `help` where `inspect auth` takes no positional |\n| `connectors auth -o json status` | a global between the two words of a row defeated the match |\n\n`serve` is in the table and does not shadow the group it names. The row fires only after the real\nparse refused, so bare `connectors serve`, `serve --help` and `serve -h` are the group — the way\nbare `setup`, `inspect` and `session` are — and `serve --config X` is the old leaf.\n\n`auth` is in the table twice, as the group a person typed and as the two-word path under it. The\nlongest matching row wins, so the order of the table decides nothing.\n\n`MOVED` and `moved` are `pub`. `tests/moved_paths_are_not_taught.rs` asks the shipped function\nwhether a written invocation names a path that moved, rather than restating its rules: the\nrestatement drifted once, which is how `connectors -o compact doctor` shipped, and a hand-copied\n`MOVED` in an adversary suite ran three cases against eleven rows of twelve.\n\n## Fence\n\n**The CLI line cap is gone.** `CLI_TOTAL_LINE_LIMIT` was removed on 2026-09-04 by operator\ninstruction. It had been raised at every one of the six times it fired — 856, 960, 966, 1006, 1014,\n1127 — so it never once moved code out of the binary, which is the only thing it was for. What it\ndid do was cost: raising it inserts lines into `architecture_fence.rs`, and citations into that file\nbroke that way twice in one day.\n\n`product_cli_is_a_thin_frontend` still bounds the frontend by what it may **link**\n(`CLI_DEPENDENCIES`) and what it may **declare** (the forbidden symbols). Those measure the property\ndirectly and refuse rather than negotiate.\n\n## Cross-repository\n\n`zwirn` forwards argv verbatim into `connectors_cli::run_from`\n(`zwirn/crates/agent-app/src/main.rs:124`) and pins this repository at rev `1e0eb9f`\n(`zwirn/Cargo.toml:25`), so `zwirn connectors …` changes only when that pin moves. Renaming something\nanother repository verifies is a coordinated migration with an ADR (`AGENTS.md:7-8`); the ADR and\nthe pin bump are a follow-up, named in the report, not a blocker for this story.\n\n## Acceptance\n\n- `connectors --help` lists 8 commands.\n- `connectors doctor` produces the same output as `connectors inspect doctor`, plus one stderr line\n  naming the new path.\n- The contract test passes, so the specification and the tree agree after the move.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-surface-contract`.\n"},"args":{"changes":{"body":"# Story: the top level reads as categories, not as sixteen commands\n\n## Defect\n\n`connectors --help` lists 16 commands in one block. `init`, `doctor`, `login`, `serve` and\n`operation` sit at the same level despite being five different activities, so the help output does\nnot tell an operator which of them they want.\n\nThere is no help-only fix. clap 4.6.6 renders exactly one `Commands:` section per level:\n`subcommand_help_heading` renames that section and does not partition it\n(`clap_builder-4.6.6/src/output/help_template.rs:403-416`), and `flatten_help` emits one heading per\nleaf with its arguments rather than a grouped index (`:878-936`). The grouping has to be real\nnesting.\n\n## Shape\n\nTop level goes 16 to 8. The three everyday verbs keep their paths, because they are the point of the\ntool and lengthening them costs every invocation.\n\n| group | members | was |\n|---|---|---|\n| `setup` | `init`, `connect`, `completions` | `connectors init`, `connect`, `completions` |\n| `inspect` | `doctor`, `providers`, `auth` | `connectors doctor`, `providers`, `auth status` |\n| `session` | `login`, `logout` | `connectors login`, `logout` |\n| `serve` | `local`, `hosted`, `mcp` | `connectors serve`, `serve-hosted`, `mcp` |\n\n`operation`, `connection`, `event` and `admin` stay at the top level.\n\n`auth` collapses: `AuthCommand` (`lib.rs:182-191`) has exactly one variant, so `connectors inspect\nauth` replaces `connectors auth status` rather than becoming a third level.\n\nFour subcommand enums hold the moved variants verbatim — the variants move, they are not copied.\n\n## Compatibility\n\nOld paths keep working for one release. `moved` rewrites a legacy path before the command runs, and\none stderr line names the path it moved to.\n\n**Nothing in it reads an option's syntax.** clap parses argv, and each parse it performs answers one\nquestion:\n\n1. **Is a word that moved present at all?** Unless one of the first `LEGACY_WINDOW` — 8 — words is\n   the first word of a `MOVED` row, no tree is built and nothing is allocated. Every current path\n   pays this and nothing else.\n2. **Does this release already read the argv?** The real tree is asked to parse it. Only two\n   refusals mean a word that moved: `InvalidSubcommand` — `doctor`, `auth status`, `help doctor` —\n   and `UnknownArgument`, which is `serve --config` at the group that took the old leaf's name.\n   Every other outcome is clap's to answer as it stands: a parse, a help request, a version\n   request, a missing value.\n3. **Which path was typed?** `read_one_word` reads the argv one word at a time, each read stepping\n   over the global options in front of that word — asked of the parser, not named here, so a global\n   added later needs no edit. `-o json`, `--output json`, `--output=json` and `-ojson` are one case,\n   because clap's option syntax is clap's. The argv handed on is rebuilt from what clap read: the\n   globals, `help` if it was there, the new words, then the tail verbatim.\n\nThe first version pre-scanned argv from `argv[1]` with a hand-written `spans` that reimplemented\nclap's four option-value spellings. That is the argv parsing a Rust CLI does not do, and it was\nwrong in six ways, each a path that worked before the move:\n\n| typed | was |\n|---|---|\n| `connectors --output json <word>` | all eleven entries refused; `--output` is `global = true` |\n| `connectors help <word>` | ten of eleven refused, and `help` is a word `--help` advertises |\n| `connectors serve help` | rewritten to `serve local help` and refused |\n| `connectors auth` | refused, naming no destination |\n| `connectors auth help` | the one-word `auth` row put the group's own `help` where `inspect auth` takes no positional |\n| `connectors auth -o json status` | a global between the two words of a row defeated the match |\n\n`serve` is in the table and does not shadow the group it names. The row fires only after the real\nparse refused, so bare `connectors serve`, `serve --help` and `serve -h` are the group — the way\nbare `setup`, `inspect` and `session` are — and `serve --config X` is the old leaf.\n\n`auth` is in the table twice, as the group a person typed and as the two-word path under it. The\nlongest matching row wins, so the order of the table decides nothing.\n\n`MOVED` and `moved` are `pub`. `tests/moved_paths_are_not_taught.rs` asks the shipped function\nwhether a written invocation names a path that moved, rather than restating its rules: the\nrestatement drifted once, which is how `connectors -o compact doctor` shipped, and a hand-copied\n`MOVED` in an adversary suite ran three cases against eleven rows of twelve.\n\n## Fence\n\n**The CLI line cap is gone.** `CLI_TOTAL_LINE_LIMIT` was removed on 2026-09-04 by operator\ninstruction. It had been raised at every one of the six times it fired — 856, 960, 966, 1006, 1014,\n1127 — so it never once moved code out of the binary, which is the only thing it was for. What it\ndid do was cost: raising it inserts lines into `architecture_fence.rs`, and citations into that file\nbroke that way twice in one day.\n\n`product_cli_is_a_thin_frontend` still bounds the frontend by what it may **link**\n(`CLI_DEPENDENCIES`) and what it may **declare** (the forbidden symbols). Those measure the property\ndirectly and refuse rather than negotiate.\n\n## Cross-repository\n\n`zwirn` forwards argv verbatim into `connectors_cli::run_from`\n(`zwirn/crates/agent-app/src/main.rs:124`) and pins this repository at rev `1e0eb9f`\n(`zwirn/Cargo.toml:25`), so `zwirn connectors …` changes only when that pin moves. Renaming something\nanother repository verifies is a coordinated migration with an ADR (`AGENTS.md:7-8`); the ADR and\nthe pin bump are a follow-up, named in the report, not a blocker for this story.\n\n## Acceptance\n\n- `connectors --help` lists 8 commands.\n- `connectors doctor` produces the same output as `connectors inspect doctor`, plus one stderr line\n  naming the new path.\n- The contract test passes, so the specification and the tree agree after the move.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-surface-contract`.\n"},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788556794122,"causation":"cmd-protocol-artifact-body-section-story-cli-first-level-groups-1788556794122","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:cli-first-level-groups@7#0~d572300300996e8d","executor":null,"recorded_at":"2026-09-04T21:19:54Z"}}
{"entity":"story","version":1,"id":"the-binary-says-what-it-carries","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: the binary says what it carries\n\n## Defect\n\nA `connectors` binary carries four independently versioned things and reports one of them. `--version`\nprints the workspace version (`crates/connectors-cli/src/lib.rs:36`). The other three are readable\nonly by failing:\n\n| what | where the version lives | how a person finds out today |\n|---|---|---|\n| the embedded catalog pack | `crates/catalog-reader/catalog.pack`, embedded at `crates/catalog-reader/src/lib.rs:73`; schema version recorded at `connectors.lock:6-9` | not at all |\n| the credential file-store format | `crates/connector-secrets/src/file.rs:88-92` (`VERSION = \"1\"`, v2 at `:44-47`) | a refusal at `file.rs:822-829` after a write is attempted |\n| hosted session metadata | `crates/connectors-client/src/identity.rs:36` (`METADATA_VERSION: u32 = 1`), checked at `:884` | `IdentityError::State`, which names no version |\n\nThe credential store migrates v1 to v2 on the first prepared-transaction write, and a 0.19.1 reader\nthen refuses v2 (`crates/connector-secrets/src/file.rs:30-35`). That makes downgrade-after-upgrade a\ndata hazard a person cannot see coming, because nothing prints which format is on disk.\n\n## Shape\n\nOne leaf: `connectors inspect upgrade`. It reads constants already compiled into the binary and\nprints them beside the install command the repository already has (`Taskfile.yaml:5,54-69`).\n\n`inspect` is the group whose declared summary is reading what is configured, what is connected and\nwhat cannot work (`ess/system/components.yaml:201-202`). The command writes nothing, opens no\nsocket, and downloads nothing.\n\n**It is an enumerated exception, not a declared command.** `connectors-cli` owns\n`connectors.target` alone, which declares no command, and `ESS-COMPONENT-004` refuses placing\nanother domain's command there (`ess/system/components.yaml:101-106,120-127`). So the path arrives\nas one `unspecified-path: inspect upgrade — read` line in `ess/system/components.yaml` and one\nmatching `UNSPECIFIED_PATHS` entry, which `crates/connectors-cli/tests/cli_surface.rs:98,429,459`\nholds equal in both directions, kind included.\n\nNo new dependency. Nothing in `CLI_DEPENDENCIES`\n(`crates/catalog-build/tests/main/architecture_fence.rs:304-330`) changes, which is what keeps this\nstory one parser arm and one console function rather than a crate.\n\n## Not in this story\n\n**No download and no version comparison.** Resolving the newest published release and replacing this\nbinary is `connectors setup upgrade`, and it is blocked rather than deferred: `SHA256SUMS` is\nproduced by the same job that uploads the archive (`.github/workflows/release.yml:276-282,300-313`),\nso it establishes integrity and not authenticity, and `crates/catalog-reader/README.md:41-49` — a\npublic file of this repository — states that b10x has no supported distribution channel and that the\nfirst must follow ADR 0019 with a signed bundle manifest. A `--check-remote` flag would also be\nreach into the outside world with no catalogued operation, no bound credential and no grant, which\nis what O1 forbids (`AGENTS.md:15`).\n\n**No configured catalog pack.** `[catalog] pack` and its digest are `epic:deployment-packs`.\n\n**No correction to design 02.** `docs/design/02-architecture.md:390` states \"Pre-v1 there are no\nrelease artifacts; the repo is the product\". Sixteen releases are published, the latest `v0.5.11`.\nThe sentence is false and amending it is a separate change, named here so it is not lost.\n\n## Acceptance\n\n- `connectors inspect upgrade` exits 0 and prints four version facts: the CLI version, the embedded\n  pack's schema version and digest, the credential-store format version, and the session-metadata\n  version.\n- Each printed value is read from the constant that governs it, not restated — a test changing\n  `connector-secrets`' `VERSION` changes the output.\n- The command opens no socket and writes no file. It runs with the state root absent.\n- `ess/system/components.yaml` and `UNSPECIFIED_PATHS` agree, and `ess/generated/clap` regenerates\n  byte-identically.\n- `connectors --help` still lists 8 commands.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-first-level-groups`, which is what makes `inspect` a group.\n","title":"The binary says what it carries"},"args":{"command":"create-entity","data":{"body":"# Story: the binary says what it carries\n\n## Defect\n\nA `connectors` binary carries four independently versioned things and reports one of them. `--version`\nprints the workspace version (`crates/connectors-cli/src/lib.rs:36`). The other three are readable\nonly by failing:\n\n| what | where the version lives | how a person finds out today |\n|---|---|---|\n| the embedded catalog pack | `crates/catalog-reader/catalog.pack`, embedded at `crates/catalog-reader/src/lib.rs:73`; schema version recorded at `connectors.lock:6-9` | not at all |\n| the credential file-store format | `crates/connector-secrets/src/file.rs:88-92` (`VERSION = \"1\"`, v2 at `:44-47`) | a refusal at `file.rs:822-829` after a write is attempted |\n| hosted session metadata | `crates/connectors-client/src/identity.rs:36` (`METADATA_VERSION: u32 = 1`), checked at `:884` | `IdentityError::State`, which names no version |\n\nThe credential store migrates v1 to v2 on the first prepared-transaction write, and a 0.19.1 reader\nthen refuses v2 (`crates/connector-secrets/src/file.rs:30-35`). That makes downgrade-after-upgrade a\ndata hazard a person cannot see coming, because nothing prints which format is on disk.\n\n## Shape\n\nOne leaf: `connectors inspect upgrade`. It reads constants already compiled into the binary and\nprints them beside the install command the repository already has (`Taskfile.yaml:5,54-69`).\n\n`inspect` is the group whose declared summary is reading what is configured, what is connected and\nwhat cannot work (`ess/system/components.yaml:201-202`). The command writes nothing, opens no\nsocket, and downloads nothing.\n\n**It is an enumerated exception, not a declared command.** `connectors-cli` owns\n`connectors.target` alone, which declares no command, and `ESS-COMPONENT-004` refuses placing\nanother domain's command there (`ess/system/components.yaml:101-106,120-127`). So the path arrives\nas one `unspecified-path: inspect upgrade — read` line in `ess/system/components.yaml` and one\nmatching `UNSPECIFIED_PATHS` entry, which `crates/connectors-cli/tests/cli_surface.rs:98,429,459`\nholds equal in both directions, kind included.\n\nNo new dependency. Nothing in `CLI_DEPENDENCIES`\n(`crates/catalog-build/tests/main/architecture_fence.rs:304-330`) changes, which is what keeps this\nstory one parser arm and one console function rather than a crate.\n\n## Not in this story\n\n**No download and no version comparison.** Resolving the newest published release and replacing this\nbinary is `connectors setup upgrade`, and it is blocked rather than deferred: `SHA256SUMS` is\nproduced by the same job that uploads the archive (`.github/workflows/release.yml:276-282,300-313`),\nso it establishes integrity and not authenticity, and `crates/catalog-reader/README.md:41-49` — a\npublic file of this repository — states that b10x has no supported distribution channel and that the\nfirst must follow ADR 0019 with a signed bundle manifest. A `--check-remote` flag would also be\nreach into the outside world with no catalogued operation, no bound credential and no grant, which\nis what O1 forbids (`AGENTS.md:15`).\n\n**No configured catalog pack.** `[catalog] pack` and its digest are `epic:deployment-packs`.\n\n**No correction to design 02.** `docs/design/02-architecture.md:390` states \"Pre-v1 there are no\nrelease artifacts; the repo is the product\". Sixteen releases are published, the latest `v0.5.11`.\nThe sentence is false and amending it is a separate change, named here so it is not lost.\n\n## Acceptance\n\n- `connectors inspect upgrade` exits 0 and prints four version facts: the CLI version, the embedded\n  pack's schema version and digest, the credential-store format version, and the session-metadata\n  version.\n- Each printed value is read from the constant that governs it, not restated — a test changing\n  `connector-secrets`' `VERSION` changes the output.\n- The command opens no socket and writes no file. It runs with the state root absent.\n- `ess/system/components.yaml` and `UNSPECIFIED_PATHS` agree, and `ess/generated/clap` regenerates\n  byte-identically.\n- `connectors --help` still lists 8 commands.\n- `bash scripts/gate.sh` exits 0.\n\n## Depends on\n\n`story:cli-first-level-groups`, which is what makes `inspect` a group.\n","status":"draft","title":"The binary says what it carries"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-binary-says-what-it-carries"},"payload":{"actor":"human:timo","at":1788558391200,"causation":"cmd-new-story-the-binary-says-what-it-carries","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-binary-says-what-it-carries@1#0~39aed150803f3ec1","executor":null,"recorded_at":"2026-09-04T21:46:31Z"}}
{"entity":"story","version":1,"id":"the-binary-says-what-it-carries","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000608","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788558391218,"causation":"cmd-story-the-binary-says-what-it-carries-derived_from-epic-cli-surface","change":{"change":"related","relation":"derived_from","target":"epic:cli-surface"},"correlation":"protocol-artifact-relate","event_id":"story:the-binary-says-what-it-carries@1#0~9ae54092349b5c3e","executor":null,"recorded_at":"2026-09-04T21:46:31Z"}}
{"entity":"story","version":1,"id":"the-binary-says-what-it-carries","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:cli-surface"},{"depends_on":"story:cli-first-level-groups"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000608","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788558391230,"causation":"cmd-story-the-binary-says-what-it-carries-depends_on-story-cli-first-level-groups","change":{"change":"related","relation":"depends_on","target":"story:cli-first-level-groups"},"correlation":"protocol-artifact-relate","event_id":"story:the-binary-says-what-it-carries@1#0~b5c0dc9fcc2d2422","executor":null,"recorded_at":"2026-09-04T21:46:31Z"}}
{"entity":"review-result","version":1,"id":"adversary-categorize-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 2 — `story:cli-first-level-groups`\n\nWorktree `wt-e9b482b00009`, uncommitted over `967062d`. `aep-drive:adversary`, model fable.\n164,619 tokens, 45 tool uses, 942 s.\n\n## Header, as returned\n\n```\nunit: story:cli-first-level-groups, legacy shim in crates/connectors-cli/src/lib.rs — worktree wt-e9b482b00009, branch impl/cli-first-level-groups, uncommitted over 967062d\nverdict: red (NEEDS-CHANGE)\ncases: executed 66→70, red 4\norigin: introduced 4 / pre-existing 0 / undecided 0\nwrote-outside-worktree: 2 paths (both under ~/.cache/claude-tmp/adv-shim-pass2/)\nneeds-coordinator: the `serve` + global-only finding is a fork between CHANGELOG.md:24-28 and lib.rs:467-470; which document wins is the unit's call, not mine\n```\n\n## 1. Diff — proof of what it touched\n\n`git --no-pager diff --stat` unchanged from hand-over: `36 files changed, 1283 insertions(+), 530\ndeletions(-)`, all the unit's. Its only write is one untracked test file:\n`?? crates/connectors-cli/tests/adversary_shim_pass5.rs`. No non-test path touched.\n\n## 2. Cases added\n\n`crates/connectors-cli/tests/adversary_shim_pass5.rs`, each run alone before the suite, all red:\n\n| case | asserts | red output (verbatim, run alone) |\n|---|---|---|\n| `the_double_dash_escape_is_not_a_word_that_moved` | `moved()` returns `None` for a word behind `--`; binary `-- doctor --help` exits 2 with no note | `` `connectors -- doctor` was rewritten to `connectors inspect doctor` `` … `` `connectors -- serve --config x` was rewritten to `connectors serve local --config x` `` … `` `connectors -- help doctor` was rewritten to `connectors help inspect doctor` `` (5 argvs) |\n| `serve_with_only_the_global_format_flag_serves_or_names_where_the_server_went` | for 4 spellings × 2 positions `moved()` lands on `serve local`; binary `serve -o json` serves or names `connectors serve local` | `` `connectors -o json serve` → None `` … `` `connectors serve -ojson` → None `` (8 argvs) |\n| `an_argument_neither_the_group_nor_the_leaf_declares_is_not_the_old_leaf` | `moved()` returns `None` for `serve --bogus/-V/--version/--hlep`; `serve --hlep` stderr equals `setup --hlep` stderr with the group name swapped | `` `connectors serve --bogus` was rewritten to `connectors serve local --bogus` `` … `` `connectors serve --hlep` was rewritten to `connectors serve local --hlep` `` (4 argvs) |\n| `a_positional_value_spelled_help_is_a_value_not_a_help_request` | `login help` / `connect help` hand on `help` as the positional | `` `connectors login help` is handed on as `connectors help session login`, not `connectors session login help` `` (2 argvs) |\n\n## 3. Suite run\n\nAfter part 2: `cd crates/connectors-cli && cargo test --no-fail-fast` → `error: 1 target failed`,\nnon-zero. `test result:` lines, all 14 binaries:\n\n| binary | result |\n|---|---|\n| unittests src/lib.rs | ok. 5 passed |\n| unittests src/main.rs | ok. 0 passed |\n| adversary_cli_cap_pass3 | ok. 1 passed |\n| adversary_fence_probe | ok. 6 passed |\n| adversary_fence_probe_pass2 | ok. 3 passed |\n| adversary_shim_pass3 | ok. 6 passed |\n| adversary_shim_pass4 | ok. 3 passed |\n| **adversary_shim_pass5** | **FAILED. 0 passed; 4 failed** |\n| cli_surface | ok. 20 passed |\n| cli_surface_drift | ok. 10 passed |\n| cli_surface_pass_two | ok. 6 passed |\n| first_level_groups | ok. 5 passed |\n| moved_paths_are_not_taught | ok. 1 passed |\n| doc-tests | ok. 0 passed |\n\n## 4. Findings\n\n\"before\" = `~/.cargo/bin/connectors` 0.5.11, the pre-release 16-command build, run with HOME/XDG\npointed at scratch.\n\n| # | file:line | what was measured | what reaches it | verdict / origin |\n|---|---|---|---|---|\n| 1 | `crates/connectors-cli/src/lib.rs:519` (`allow_external_subcommands(true)`, no positional) | `connectors -- doctor`: before → exit 2 \"unexpected argument 'doctor' found / tip: remove the '--'\"; now → note + doctor runs (exit 1, unhealthy). `-- serve --config x --help` → `Usage: connectors serve local`. `--` is dropped from the rebuilt argv | nothing found — the release before refused it, so no caller exists | CONFIRMED / introduced |\n| 2 | `crates/connectors-cli/src/lib.rs:606` (two-kind filter) vs `CHANGELOG.md:24-28` | before: `serve -o json`, `-o json serve`, `serve --output=json` each printed the readiness JSON and served (killed at 3 s, exit 124); now: `serve -o json` → \"'connectors serve' requires a subcommand\" exit 2, `-o json serve` → group help exit 2, no note either way. Real parse fails with `MissingSubcommand` / `DisplayHelpOnMissingArgumentOrSubcommand`, which stage 2 does not take | `connectors serve` with no config was the personal posture (`docs/design/02-architecture.md:113` at base); `-o json` was the documented way to get the readiness document as JSON | NEEDS-CHANGE / introduced |\n| 3 | `crates/connectors-cli/src/lib.rs:655` (guard only checks `next` is the group's) | `serve --hlep`: before → \"unexpected argument '--hlep' … Usage: connectors serve --help\"; now → note \"`connectors serve` is now `connectors serve local`\" + \"Usage: connectors serve local --help\". Same for `--bogus`, `-V`, `--version`. Control `setup --hlep` gets clap alone | anyone mistyping a flag at the `serve` group | CONFIRMED / introduced |\n| 4 | `crates/connectors-cli/src/lib.rs:636` (`help` taken at any walked position) | `login help` before → login to base `help` (exit 1 at discovery); now → `help session login`, prints help exit 0. `connect help` likewise | nothing found beyond a person typing it | CONFIRMED / introduced |\n\nFixes named, not applied: (1) refuse the rewrite when the reader consumed `--` — check\n`words.iter().any(|w| w == \"--\")` before the external word, or declare a positional; (2) either take\n`MissingSubcommand` at a `MOVED` group whose only arguments were globals, or amend CHANGELOG.md:24-28\nto list `serve` + global-only as the group (both resolutions leave the changelog and the code\nagreeing, which they do not now); (3) after the walk, require the first tail token to be a long/short\nthe *new leaf* declares before firing the `serve` row; (4) do not take `help` after a walked word\nwhose new leaf declares a positional.\n\n## 5. Attacked and held\n\n| class | driven | result |\n|---|---|---|\n| `LEGACY_WINDOW = 8` | repeated `-o` ×2/×3/×4, mixed spellings | not reachable: before-binary refuses any repeat for a real run (\"cannot be used multiple times\"); only `--help` variants printed help, because clap exits on help before validating |\n| stage 2 third error kinds | `--output doctor`, `--config doctor`, `-o \\xff doctor`, `serve-hosted` w/o `--config`, `serve --config` w/o value | clap answers as before, no note |\n| current path with `UnknownArgument` | `inspect providers --query doctor --bogus`, `serve local --bogus`, `serve --bogus local` | not rewritten (except the `serve --bogus` class, finding 3) |\n| `serve` matrix | bare, `--help`, `-h`, `help`, `help serve`, `local --help`, `--config x`, `--config=x`, `--state-root x`, `-o json --config x`, `--config x -o json`, `--config x local` | group where the group was meant, leaf where `--config`/`--state-root` present |\n| tail rebuild | globals both sides (`-o json doctor -o yaml`), `doctor -- --config x`, non-UTF-8 `--config $'\\xff\\xfe' --state-root $'\\xfe'` | acceptance example holds byte-for-byte: stdout identical, stderr identical after the note |\n| `help` shapes | `help auth`, `help auth status`, `auth help status`, `auth -o json help`, `help -o json auth status`, `help help doctor` | all land on `inspect auth` / clap's own refusal |\n| `pub fn moved` vs `run_from` | same function, same `command()`; zwirn (`agent-app/src/main.rs:195`) builds `argv[0]=\"connectors\"` | no disagreement; zwirn unaffected at pin `1e0eb9f` |\n| `moved_paths_are_not_taught` scan | grep of everything it does not read | one comment only: `scripts/check-local-identity-refused.sh:4` names `connectors serve-hosted`; `.github`, non-.rs assets under `src`, Dockerfile shell-form: nothing |\n| citation fence | preposition-phrased citations (\"`X` in `path:line`\"), `components.yaml:109` → `lib.rs:55-94` | none exist; the one checked lands on `enum Command` |\n\n## 6. Paths written outside the worktree\n\n- `~/.cache/claude-tmp/adv-shim-pass2/xdg/` — HOME/XDG for both binaries (contains\n  `b10x/connectors/connectors.sock` from the pre-release server runs, `l.out`, `l.err`, `c.out`,\n  `c.err`)\n- `~/.cache/claude-tmp/adv-shim-pass2/probe/` — scratch cargo project replicating the reader, with\n  its `target/` (61 MB total for the directory)\n\n## 7. Findings, verbatim\n\n```findings\n- file: crates/connectors-cli/src/lib.rs\n  line: 519\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: \"`connectors -- doctor` was refused before (exit 2, clap tip to remove `--`) and now runs `inspect doctor` with the `--` escape dropped from the rebuilt argv; `-- serve --config x` reaches `serve local`.\"\n- file: crates/connectors-cli/src/lib.rs\n  line: 606\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: \"`connectors serve -o json`, `-o json serve` and `serve --output=json` served with JSON readiness before and now exit 2 with no note naming `connectors serve local`, while CHANGELOG.md:24-28 promises every old path works with the global in front in any spelling.\"\n- file: crates/connectors-cli/src/lib.rs\n  line: 655\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: \"`connectors serve --hlep` (also `--bogus`, `-V`, `--version`) is rewritten to `serve local` and prints a note for a path not typed plus `Usage: connectors serve local --help`, where `setup --hlep` gets clap's group refusal alone.\"\n- file: crates/connectors-cli/src/lib.rs\n  line: 636\n  category: boundary\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: \"`connectors login help` and `connectors connect help` were a login to base `help` and a connect to provider `help` before, and are now rebuilt as `help session login` / `help setup connect` and print help, exit 0.\"\n```\n","title":"Adversary pass 2 against the rewritten categorize shim"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 2 — `story:cli-first-level-groups`\n\nWorktree `wt-e9b482b00009`, uncommitted over `967062d`. `aep-drive:adversary`, model fable.\n164,619 tokens, 45 tool uses, 942 s.\n\n## Header, as returned\n\n```\nunit: story:cli-first-level-groups, legacy shim in crates/connectors-cli/src/lib.rs — worktree wt-e9b482b00009, branch impl/cli-first-level-groups, uncommitted over 967062d\nverdict: red (NEEDS-CHANGE)\ncases: executed 66→70, red 4\norigin: introduced 4 / pre-existing 0 / undecided 0\nwrote-outside-worktree: 2 paths (both under ~/.cache/claude-tmp/adv-shim-pass2/)\nneeds-coordinator: the `serve` + global-only finding is a fork between CHANGELOG.md:24-28 and lib.rs:467-470; which document wins is the unit's call, not mine\n```\n\n## 1. Diff — proof of what it touched\n\n`git --no-pager diff --stat` unchanged from hand-over: `36 files changed, 1283 insertions(+), 530\ndeletions(-)`, all the unit's. Its only write is one untracked test file:\n`?? crates/connectors-cli/tests/adversary_shim_pass5.rs`. No non-test path touched.\n\n## 2. Cases added\n\n`crates/connectors-cli/tests/adversary_shim_pass5.rs`, each run alone before the suite, all red:\n\n| case | asserts | red output (verbatim, run alone) |\n|---|---|---|\n| `the_double_dash_escape_is_not_a_word_that_moved` | `moved()` returns `None` for a word behind `--`; binary `-- doctor --help` exits 2 with no note | `` `connectors -- doctor` was rewritten to `connectors inspect doctor` `` … `` `connectors -- serve --config x` was rewritten to `connectors serve local --config x` `` … `` `connectors -- help doctor` was rewritten to `connectors help inspect doctor` `` (5 argvs) |\n| `serve_with_only_the_global_format_flag_serves_or_names_where_the_server_went` | for 4 spellings × 2 positions `moved()` lands on `serve local`; binary `serve -o json` serves or names `connectors serve local` | `` `connectors -o json serve` → None `` … `` `connectors serve -ojson` → None `` (8 argvs) |\n| `an_argument_neither_the_group_nor_the_leaf_declares_is_not_the_old_leaf` | `moved()` returns `None` for `serve --bogus/-V/--version/--hlep`; `serve --hlep` stderr equals `setup --hlep` stderr with the group name swapped | `` `connectors serve --bogus` was rewritten to `connectors serve local --bogus` `` … `` `connectors serve --hlep` was rewritten to `connectors serve local --hlep` `` (4 argvs) |\n| `a_positional_value_spelled_help_is_a_value_not_a_help_request` | `login help` / `connect help` hand on `help` as the positional | `` `connectors login help` is handed on as `connectors help session login`, not `connectors session login help` `` (2 argvs) |\n\n## 3. Suite run\n\nAfter part 2: `cd crates/connectors-cli && cargo test --no-fail-fast` → `error: 1 target failed`,\nnon-zero. `test result:` lines, all 14 binaries:\n\n| binary | result |\n|---|---|\n| unittests src/lib.rs | ok. 5 passed |\n| unittests src/main.rs | ok. 0 passed |\n| adversary_cli_cap_pass3 | ok. 1 passed |\n| adversary_fence_probe | ok. 6 passed |\n| adversary_fence_probe_pass2 | ok. 3 passed |\n| adversary_shim_pass3 | ok. 6 passed |\n| adversary_shim_pass4 | ok. 3 passed |\n| **adversary_shim_pass5** | **FAILED. 0 passed; 4 failed** |\n| cli_surface | ok. 20 passed |\n| cli_surface_drift | ok. 10 passed |\n| cli_surface_pass_two | ok. 6 passed |\n| first_level_groups | ok. 5 passed |\n| moved_paths_are_not_taught | ok. 1 passed |\n| doc-tests | ok. 0 passed |\n\n## 4. Findings\n\n\"before\" = `~/.cargo/bin/connectors` 0.5.11, the pre-release 16-command build, run with HOME/XDG\npointed at scratch.\n\n| # | file:line | what was measured | what reaches it | verdict / origin |\n|---|---|---|---|---|\n| 1 | `crates/connectors-cli/src/lib.rs:519` (`allow_external_subcommands(true)`, no positional) | `connectors -- doctor`: before → exit 2 \"unexpected argument 'doctor' found / tip: remove the '--'\"; now → note + doctor runs (exit 1, unhealthy). `-- serve --config x --help` → `Usage: connectors serve local`. `--` is dropped from the rebuilt argv | nothing found — the release before refused it, so no caller exists | CONFIRMED / introduced |\n| 2 | `crates/connectors-cli/src/lib.rs:606` (two-kind filter) vs `CHANGELOG.md:24-28` | before: `serve -o json`, `-o json serve`, `serve --output=json` each printed the readiness JSON and served (killed at 3 s, exit 124); now: `serve -o json` → \"'connectors serve' requires a subcommand\" exit 2, `-o json serve` → group help exit 2, no note either way. Real parse fails with `MissingSubcommand` / `DisplayHelpOnMissingArgumentOrSubcommand`, which stage 2 does not take | `connectors serve` with no config was the personal posture (`docs/design/02-architecture.md:113` at base); `-o json` was the documented way to get the readiness document as JSON | NEEDS-CHANGE / introduced |\n| 3 | `crates/connectors-cli/src/lib.rs:655` (guard only checks `next` is the group's) | `serve --hlep`: before → \"unexpected argument '--hlep' … Usage: connectors serve --help\"; now → note \"`connectors serve` is now `connectors serve local`\" + \"Usage: connectors serve local --help\". Same for `--bogus`, `-V`, `--version`. Control `setup --hlep` gets clap alone | anyone mistyping a flag at the `serve` group | CONFIRMED / introduced |\n| 4 | `crates/connectors-cli/src/lib.rs:636` (`help` taken at any walked position) | `login help` before → login to base `help` (exit 1 at discovery); now → `help session login`, prints help exit 0. `connect help` likewise | nothing found beyond a person typing it | CONFIRMED / introduced |\n\nFixes named, not applied: (1) refuse the rewrite when the reader consumed `--` — check\n`words.iter().any(|w| w == \"--\")` before the external word, or declare a positional; (2) either take\n`MissingSubcommand` at a `MOVED` group whose only arguments were globals, or amend CHANGELOG.md:24-28\nto list `serve` + global-only as the group (both resolutions leave the changelog and the code\nagreeing, which they do not now); (3) after the walk, require the first tail token to be a long/short\nthe *new leaf* declares before firing the `serve` row; (4) do not take `help` after a walked word\nwhose new leaf declares a positional.\n\n## 5. Attacked and held\n\n| class | driven | result |\n|---|---|---|\n| `LEGACY_WINDOW = 8` | repeated `-o` ×2/×3/×4, mixed spellings | not reachable: before-binary refuses any repeat for a real run (\"cannot be used multiple times\"); only `--help` variants printed help, because clap exits on help before validating |\n| stage 2 third error kinds | `--output doctor`, `--config doctor`, `-o \\xff doctor`, `serve-hosted` w/o `--config`, `serve --config` w/o value | clap answers as before, no note |\n| current path with `UnknownArgument` | `inspect providers --query doctor --bogus`, `serve local --bogus`, `serve --bogus local` | not rewritten (except the `serve --bogus` class, finding 3) |\n| `serve` matrix | bare, `--help`, `-h`, `help`, `help serve`, `local --help`, `--config x`, `--config=x`, `--state-root x`, `-o json --config x`, `--config x -o json`, `--config x local` | group where the group was meant, leaf where `--config`/`--state-root` present |\n| tail rebuild | globals both sides (`-o json doctor -o yaml`), `doctor -- --config x`, non-UTF-8 `--config $'\\xff\\xfe' --state-root $'\\xfe'` | acceptance example holds byte-for-byte: stdout identical, stderr identical after the note |\n| `help` shapes | `help auth`, `help auth status`, `auth help status`, `auth -o json help`, `help -o json auth status`, `help help doctor` | all land on `inspect auth` / clap's own refusal |\n| `pub fn moved` vs `run_from` | same function, same `command()`; zwirn (`agent-app/src/main.rs:195`) builds `argv[0]=\"connectors\"` | no disagreement; zwirn unaffected at pin `1e0eb9f` |\n| `moved_paths_are_not_taught` scan | grep of everything it does not read | one comment only: `scripts/check-local-identity-refused.sh:4` names `connectors serve-hosted`; `.github`, non-.rs assets under `src`, Dockerfile shell-form: nothing |\n| citation fence | preposition-phrased citations (\"`X` in `path:line`\"), `components.yaml:109` → `lib.rs:55-94` | none exist; the one checked lands on `enum Command` |\n\n## 6. Paths written outside the worktree\n\n- `~/.cache/claude-tmp/adv-shim-pass2/xdg/` — HOME/XDG for both binaries (contains\n  `b10x/connectors/connectors.sock` from the pre-release server runs, `l.out`, `l.err`, `c.out`,\n  `c.err`)\n- `~/.cache/claude-tmp/adv-shim-pass2/probe/` — scratch cargo project replicating the reader, with\n  its `target/` (61 MB total for the directory)\n\n## 7. Findings, verbatim\n\n```findings\n- file: crates/connectors-cli/src/lib.rs\n  line: 519\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: \"`connectors -- doctor` was refused before (exit 2, clap tip to remove `--`) and now runs `inspect doctor` with the `--` escape dropped from the rebuilt argv; `-- serve --config x` reaches `serve local`.\"\n- file: crates/connectors-cli/src/lib.rs\n  line: 606\n  category: contract-drift\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: \"`connectors serve -o json`, `-o json serve` and `serve --output=json` served with JSON readiness before and now exit 2 with no note naming `connectors serve local`, while CHANGELOG.md:24-28 promises every old path works with the global in front in any spelling.\"\n- file: crates/connectors-cli/src/lib.rs\n  line: 655\n  category: boundary\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: \"`connectors serve --hlep` (also `--bogus`, `-V`, `--version`) is rewritten to `serve local` and prints a note for a path not typed plus `Usage: connectors serve local --help`, where `setup --hlep` gets clap's group refusal alone.\"\n- file: crates/connectors-cli/src/lib.rs\n  line: 636\n  category: boundary\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: \"`connectors login help` and `connectors connect help` were a login to base `help` and a connect to provider `help` before, and are now rebuilt as `help session login` / `help setup connect` and print help, exit 0.\"\n```\n","status":"active","title":"Adversary pass 2 against the rewritten categorize shim"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-categorize-pass-2"},"payload":{"actor":"human:timo","at":1788558548657,"causation":"cmd-new-review-result-adversary-categorize-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-categorize-pass-2@1#0~7c8646053a2aa74a","executor":null,"recorded_at":"2026-09-04T21:49:08Z"}}
{"entity":"review-result","version":1,"id":"adversary-categorize-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:cli-first-level-groups"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000615","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788558548671,"causation":"cmd-review-result-adversary-categorize-pass-2-reviews-story-cli-first-level-groups","change":{"change":"related","relation":"reviews","target":"story:cli-first-level-groups"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-categorize-pass-2@1#0~bfdc916980f3715c","executor":null,"recorded_at":"2026-09-04T21:49:08Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:519","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2","target":"01MEM0000000000000157"},"payload":{"actor":"human:timo","at":1788559700000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788559700067","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:519","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@7#0~0cc998d7e6303831","executor":null,"recorded_at":"2026-09-04T22:08:20Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:606","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2","target":"01MEM0000000000000157"},"payload":{"actor":"human:timo","at":1788559700000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788559700237","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:606","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@7#0~c803495da0e5145e","executor":null,"recorded_at":"2026-09-04T22:08:20Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:655","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2","target":"01MEM0000000000000157"},"payload":{"actor":"human:timo","at":1788559700000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788559700397","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:655","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@7#0~dee070337d068b56","executor":null,"recorded_at":"2026-09-04T22:08:20Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:636","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2","target":"01MEM0000000000000157"},"payload":{"actor":"human:timo","at":1788559700000,"causation":"cmd-evidence-story-cli-first-level-groups-review_outcome-1788559700553","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","reference":"crates/connectors-cli/src/lib.rs:636","review":"review-result:adversary-categorize-pass-2","source":"review-result:adversary-categorize-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@7#0~67cdac24903aded3","executor":null,"recorded_at":"2026-09-04T22:08:20Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/claude-tmp/gate.log","source":"bash scripts/gate.sh in wt-e9b482b00009","target":"01MEM0000000000000157"},"payload":{"actor":"human:timo","at":1788567600000,"causation":"cmd-evidence-story-cli-first-level-groups-test_result-1788559700720","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/claude-tmp/gate.log","source":"bash scripts/gate.sh in wt-e9b482b00009"},"correlation":"protocol-artifact-evidence","event_id":"story:cli-first-level-groups@7#0~3deb65f2e34f8e90","executor":null,"recorded_at":"2026-09-05T00:20:00Z"}}
{"entity":"story","version":1,"id":"cli-first-level-groups","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":11}}","target":"01MEM0000000000000157","to":"implemented"},"payload":{"actor":"human:timo","at":1788559703548,"causation":"cmd-move-story-cli-first-level-groups-1788559703548","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":11,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:cli-first-level-groups@8#0~c9c96d489bcded9d","executor":null,"recorded_at":"2026-09-04T22:08:23Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `aep artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","summary":"Keep GitLab credentials in Connectors while Substrate materializes one exact governed checkout.","title":"Broker read-only Git fetch sessions"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `aep artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","status":"draft","summary":"Keep GitLab credentials in Connectors while Substrate materializes one exact governed checkout.","title":"Broker read-only Git fetch sessions"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/broker-read-only-git-fetch-sessions"},"payload":{"actor":"human:timo","at":1788563371705,"causation":"cmd-new-story-broker-read-only-git-fetch-sessions","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:broker-read-only-git-fetch-sessions@1#0~fe73fa0d834c7c77","executor":null,"recorded_at":"2026-09-04T23:09:31Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:substrate-integration"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000642","target":"01MEM0000000000000070"},"payload":{"actor":"human:timo","at":1788563371720,"causation":"cmd-story-broker-read-only-git-fetch-sessions-decomposes-epic-substrate-integration","change":{"change":"related","relation":"decomposes","target":"epic:substrate-integration"},"correlation":"protocol-artifact-relate","event_id":"story:broker-read-only-git-fetch-sessions@1#0~b43cd89531cdd29e","executor":null,"recorded_at":"2026-09-04T23:09:31Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Broker read-only Git fetch sessions\n\n## Outcome\n\nWorkspace can establish a bounded Smart Git upload-pack session for one currently authorized GitLab project and exact commit without releasing the provider credential.\n\n## Acceptance\n\n- Creation revalidates Identity, Connection, Grant, project, reference, and commit.\n- A stable locator and transient authority admit only upload-pack discovery and fetch.\n- Provider credentials remain in Connectors and no Git byte-plane route is model- or public-ingress exposed.\n- Expiry, revocation, request and byte exhaustion, restart, and OAuth refresh produce typed outcomes.\n- The ESS domain in `ess/domains/git.yaml`, hosted server, official client, and tests agree.\n\n## Scope\n\nConnectors GitLab integration, hosted runtime, byte-plane listener, official client, ESS declaration, and deployment-facing configuration.\n"},"args":{"changes":{"body":"# Story: Broker read-only Git fetch sessions\n\n## Outcome\n\nWorkspace can establish a bounded Smart Git upload-pack session for one currently authorized GitLab project and exact commit without releasing the provider credential.\n\n## Acceptance\n\n- Creation revalidates Identity, Connection, Grant, project, reference, and commit.\n- A stable locator and transient authority admit only upload-pack discovery and fetch.\n- Provider credentials remain in Connectors and no Git byte-plane route is model- or public-ingress exposed.\n- Expiry, revocation, request and byte exhaustion, restart, and OAuth refresh produce typed outcomes.\n- The ESS domain in `ess/domains/git.yaml`, hosted server, official client, and tests agree.\n\n## Scope\n\nConnectors GitLab integration, hosted runtime, byte-plane listener, official client, ESS declaration, and deployment-facing configuration.\n"},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788563371888,"causation":"cmd-protocol-artifact-body-story-broker-read-only-git-fetch-sessions-1788563371888","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:broker-read-only-git-fetch-sessions@2#0~2df8241116d43019","executor":null,"recorded_at":"2026-09-04T23:09:31Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/connectors-client"},{"confidence":"cited","path":"crates/connectors-config"},{"confidence":"cited","path":"crates/connectors-runtime"},{"confidence":"cited","path":"crates/integration-gitlab"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"},{"confidence":"cited","path":"docs/design"},{"confidence":"cited","path":"ess/domains/git.yaml"},{"confidence":"cited","path":"ess/system.yaml"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/connectors-client"},{"confidence":"cited","path":"crates/connectors-config"},{"confidence":"cited","path":"crates/connectors-runtime"},{"confidence":"cited","path":"crates/integration-gitlab"},{"confidence":"cited","path":"crates/protocol"},{"confidence":"cited","path":"crates/server"},{"confidence":"cited","path":"crates/service"},{"confidence":"cited","path":"docs/design"},{"confidence":"cited","path":"ess/domains/git.yaml"},{"confidence":"cited","path":"ess/system.yaml"}]},"command":"update-entity","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788563372054,"causation":"cmd-protocol-artifact-scope-story-broker-read-only-git-fetch-sessions-1788563372054","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:broker-read-only-git-fetch-sessions@3#0~7ae58343b528bc97","executor":null,"recorded_at":"2026-09-04T23:09:32Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000154","to":"proposed"},"payload":{"actor":"human:timo","at":1788563372307,"causation":"cmd-move-story-broker-read-only-git-fetch-sessions-1788563372307","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:broker-read-only-git-fetch-sessions@4#0~4966d998cfeadb0e","executor":null,"recorded_at":"2026-09-04T23:09:32Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000154","to":"active"},"payload":{"actor":"human:timo","at":1788563372320,"causation":"cmd-move-story-broker-read-only-git-fetch-sessions-1788563372320","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:broker-read-only-git-fetch-sessions@5#0~104f3b924e4acd6a","executor":null,"recorded_at":"2026-09-04T23:09:32Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"scripts/gate.sh workspace matrix and final checks","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788563372000,"causation":"cmd-evidence-story-broker-read-only-git-fetch-sessions-test_result-1788563372510","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"scripts/gate.sh workspace matrix and final checks"},"correlation":"protocol-artifact-evidence","event_id":"story:broker-read-only-git-fetch-sessions@5#0~52974bf19bf89782","executor":null,"recorded_at":"2026-09-04T23:09:32Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"ess/system.yaml","source":"ess validate --path ess and ess compile --path ess --format json","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788563372000,"causation":"cmd-evidence-story-broker-read-only-git-fetch-sessions-test_result-1788563372748","change":{"change":"evidence","kind":"test_result","reference":"ess/system.yaml","source":"ess validate --path ess and ess compile --path ess --format json"},"correlation":"protocol-artifact-evidence","event_id":"story:broker-read-only-git-fetch-sessions@5#0~addf8b1ed5d90341","executor":null,"recorded_at":"2026-09-04T23:09:32Z"}}
{"entity":"story","version":1,"id":"broker-read-only-git-fetch-sessions","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000154","to":"implemented"},"payload":{"actor":"human:timo","at":1788563373027,"causation":"cmd-move-story-broker-read-only-git-fetch-sessions-1788563373027","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:broker-read-only-git-fetch-sessions@6#0~0b0b8d45f05226ae","executor":null,"recorded_at":"2026-09-04T23:09:33Z"}}
{"entity":"task","version":1,"id":"repair-release-lock-fixed-point","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `task` artifact, seeded by `aep artifact new task <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Task: <name>\n\n## What\n\n*The concrete unit of work, in the active voice. One task, one thing — if this needs an \"and\", it is\ntwo tasks or it is a story.*\n\n## Why\n\n*Which story or specification this serves. The `decomposes` or `implements` relation carries the\nedge; this says what would be lost by dropping the task.*\n\n## Done When\n\n*The observable condition that ends it. A task whose completion is a matter of opinion is one that\ngets marked done twice and finished never.*\n\n## Notes\n\n*Anything the next person needs and cannot derive: the file to start in, the command to run, the\ntrap somebody already fell into.*\n","summary":"Regenerate every satellite workspace lock after a release version change and prove the tag gate from a clean checkout.","title":"Repair the release lockfile fixed point"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `task` artifact, seeded by `aep artifact new task <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Task: <name>\n\n## What\n\n*The concrete unit of work, in the active voice. One task, one thing — if this needs an \"and\", it is\ntwo tasks or it is a story.*\n\n## Why\n\n*Which story or specification this serves. The `decomposes` or `implements` relation carries the\nedge; this says what would be lost by dropping the task.*\n\n## Done When\n\n*The observable condition that ends it. A task whose completion is a matter of opinion is one that\ngets marked done twice and finished never.*\n\n## Notes\n\n*Anything the next person needs and cannot derive: the file to start in, the command to run, the\ntrap somebody already fell into.*\n","status":"draft","summary":"Regenerate every satellite workspace lock after a release version change and prove the tag gate from a clean checkout.","title":"Repair the release lockfile fixed point"},"entity_type":"aep.task/v1","locator":"ep://planning/store/task/repair-release-lock-fixed-point"},"payload":{"actor":"human:timo","at":1788564142778,"causation":"cmd-new-task-repair-release-lock-fixed-point","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"task:repair-release-lock-fixed-point@1#0~9e3bbfec185c4eea","executor":null,"recorded_at":"2026-09-04T23:22:22Z"}}
{"entity":"task","version":1,"id":"repair-release-lock-fixed-point","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"story:broker-read-only-git-fetch-sessions"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000647","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788564142791,"causation":"cmd-task-repair-release-lock-fixed-point-decomposes-story-broker-read-only-git-fetch-sessions","change":{"change":"related","relation":"decomposes","target":"story:broker-read-only-git-fetch-sessions"},"correlation":"protocol-artifact-relate","event_id":"task:repair-release-lock-fixed-point@1#0~52ac70d6a4751ed2","executor":null,"recorded_at":"2026-09-04T23:22:22Z"}}
{"entity":"task","version":1,"id":"repair-release-lock-fixed-point","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000421","to":"proposed"},"payload":{"actor":"human:timo","at":1788564147163,"causation":"cmd-move-task-repair-release-lock-fixed-point-1788564147163","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"task:repair-release-lock-fixed-point@2#0~3483238c5c3d8395","executor":null,"recorded_at":"2026-09-04T23:22:27Z"}}
{"entity":"task","version":1,"id":"repair-release-lock-fixed-point","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000421","to":"active"},"payload":{"actor":"human:timo","at":1788564147215,"causation":"cmd-move-task-repair-release-lock-fixed-point-1788564147215","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"task:repair-release-lock-fixed-point@3#0~9f2d9b17b10a7a35","executor":null,"recorded_at":"2026-09-04T23:22:27Z"}}
{"entity":"task","version":1,"id":"repair-release-lock-fixed-point","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"local:connectors-0.6.2","source":"locked metadata matrix plus release-specific cold fetch and final gate","target":"01MEM0000000000000421"},"payload":{"actor":"human:timo","at":1788564413000,"causation":"cmd-evidence-task-repair-release-lock-fixed-point-test_result-1788564413500","change":{"change":"evidence","kind":"test_result","reference":"local:connectors-0.6.2","source":"locked metadata matrix plus release-specific cold fetch and final gate"},"correlation":"protocol-artifact-evidence","event_id":"task:repair-release-lock-fixed-point@3#0~c4bb9cb0fc6fd369","executor":null,"recorded_at":"2026-09-04T23:26:53Z"}}
{"entity":"task","version":1,"id":"repair-release-lock-fixed-point","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000421","to":"implemented"},"payload":{"actor":"human:timo","at":1788564413738,"causation":"cmd-move-task-repair-release-lock-fixed-point-1788564413738","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"task:repair-release-lock-fixed-point@4#0~f0fb3be2cc30858e","executor":null,"recorded_at":"2026-09-04T23:26:53Z"}}
{"entity":"story","version":1,"id":"hosted-session-completes-out-of-terminal-state","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000238","to":"proposed"},"payload":{"actor":"human:timo","at":1788566661085,"causation":"cmd-move-story-hosted-session-completes-out-of-terminal-state-1788566661085","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:hosted-session-completes-out-of-terminal-state@3#0~0d08cda0fc09671d","executor":null,"recorded_at":"2026-09-05T00:04:21Z"}}
{"entity":"story","version":1,"id":"hosted-session-completes-out-of-terminal-state","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000238","to":"active"},"payload":{"actor":"human:timo","at":1788566661117,"causation":"cmd-move-story-hosted-session-completes-out-of-terminal-state-1788566661117","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:hosted-session-completes-out-of-terminal-state@4#0~6cd5d0e238f5ef8d","executor":null,"recorded_at":"2026-09-05T00:04:21Z"}}
{"entity":"release-plan","version":1,"id":"connectors-0-6-3","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Release plan: Connectors 0.6.3\n\nClose the hosted-session terminal-state race, remove the now-settled ESS marker, run the complete gate, and publish an immutable patch release from main.\n","title":"Release Connectors 0.6.3"},"args":{"command":"create-entity","data":{"body":"# Release plan: Connectors 0.6.3\n\nClose the hosted-session terminal-state race, remove the now-settled ESS marker, run the complete gate, and publish an immutable patch release from main.\n","status":"draft","title":"Release Connectors 0.6.3"},"entity_type":"aep.release-plan/v1","locator":"ep://planning/store/release-plan/connectors-0-6-3"},"payload":{"actor":"human:timo","at":1788566661424,"causation":"cmd-new-release-plan-connectors-0-6-3","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"release-plan:connectors-0-6-3@1#0~23d15fca7314ed48","executor":null,"recorded_at":"2026-09-05T00:04:21Z"}}
{"entity":"release-plan","version":1,"id":"connectors-0-6-3","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000079","to":"active"},"payload":{"actor":"human:timo","at":1788566661753,"causation":"cmd-move-release-plan-connectors-0-6-3-1788566661753","change":{"change":"moved","decided_on":{},"from":"draft","to":"active"},"correlation":"protocol-artifact-move","event_id":"release-plan:connectors-0-6-3@2#0~bffd1efa9db6470c","executor":null,"recorded_at":"2026-09-05T00:04:21Z"}}
