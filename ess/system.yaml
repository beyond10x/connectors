format: ess/1
system: connectors
# ESS model version; the independent wire contract is v1alpha1.
version: v1
domains:
  - connectors.declarations
  - connectors.mutations
  - connectors.idempotency
  - connectors.credentials
  - connectors.refresh
  - connectors.credential_evidence
  - connectors.sessions
  - connectors.service_wire
