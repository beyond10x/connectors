format: ess/1
system: connectors
# ESS model version; implemented v1alpha1 and proposed v1alpha2 wire versions are independent.
version: v1
domains:
  - connectors.declarations
  - connectors.mutations
  - connectors.idempotency
  - connectors.credentials
  - connectors.refresh
  - connectors.credential_evidence
  - connectors.sessions
  - connectors.service_wire
  - connectors.delegation
  - connectors.connection_admission
  - connectors.auth_access
  - connectors.discovery
  - connectors.discovery_state
  - connectors.datasource_reads
  - connectors.artifact_provenance
  - connectors.auth_bindings
