[package]
name = "domain"
description = "Protocol-neutral Connectors domain types and proof-bearing execution plans."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
async-trait.workspace = true
# The S-041 state port and nothing behind it: grant evaluation reads revisioned tenant Grants
# through `StateStore`, and which backend answers is a deployment binding, not a domain concern.
connector-state = { version = "0.1.0", path = "../connector-state" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

