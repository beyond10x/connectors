[package]
name = "domain"
description = "Protocol-neutral Connectors domain types and proof-bearing execution plans."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
async-trait.workspace = true
# The S-041 state port and nothing behind it: grant evaluation and approval redemption read and
# claim through `StateStore`, and which backend answers is a deployment binding, not a domain
# concern.
connector-state.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true

