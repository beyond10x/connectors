# service

Pure Connectors use cases over domain proof types. The planner reads reviewed catalog data and
returns inert plans; credential reads, DNS, sockets, and driver calls are outside this crate.

