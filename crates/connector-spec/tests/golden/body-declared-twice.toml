# "The body is these named fields" and "the body *is* this schema" are two answers to one question.
# Nothing states how they merge, so an operation declaring both has no derivable request body and no
# derivable `input_schema` — it is refused here rather than resolved by guesswork (C-125).
id = "babelforce"
base_url = "https://api.babelforce.com"

[[operations]]
id = "babelforce.session.update"
description = "Fixture operation"
method = "PUT"
direction = "write"
path = "/v2/sessions/{id}"
risk = "medium"
idempotency = "idempotent"
effects = ["write", "network"]
interaction_shape = "unary"
protocol_driver = "http_v1"
placement_requirement = "connectors_deployment"
implementation_form = "built_in"
required_capabilities = ["public_network"]
auth = []

[[operations.params.path]]
name = "id"
required = true
schema = { type = "string" }

[[operations.params.body]]
name = "presence"
required = true
schema = { type = "string" }

[operations.params]
body_schema = { type = "object" }
