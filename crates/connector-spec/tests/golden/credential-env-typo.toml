# The same hole one field down: `envv` used to yield a credential with an empty `env` list and no
# complaint, so the failure surfaced at request time instead of at build time.
id = "zendesk"
base_url = "https://acme.zendesk.com"

[[auth]]
name = "zendesk.api_token"
scheme = "bearer"
envv = ["ZENDESK_API_TOKEN"]

[[operations]]
id = "zendesk.test"
description = "Fixture operation"
method = "GET"
direction = "read"
path = "/api/v2/users/me.json"
risk = "low"
idempotency = "idempotent"
effects = ["read", "network"]
interaction_shape = "unary"
protocol_driver = "http_v1"
placement_requirement = "connectors_deployment"
implementation_form = "built_in"
required_capabilities = ["public_network"]
auth = []
