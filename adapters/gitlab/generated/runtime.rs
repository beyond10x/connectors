// Generated by connectors-spec; ESS-generated inputs are used at dispatch.
use connectors_core::{Descriptor, Error, ErrorCode, Result};
use connectors_sdk::HttpResponsePrefix;
use connectors_sdk::{AuthenticatedHttp, HttpResponse};
use gitlab_types::requests::*;
use serde_json::Value;
use std::sync::Arc;
pub fn verify_descriptor(descriptor: &Descriptor) -> Result<()> {
    connectors_sdk::verify_handlers(
        descriptor,
        &[
            "project.get",
            "issues.list",
            "file.get",
            "pipelines.list",
            "pipeline.get",
            "pipeline.jobs",
            "job.get",
            "job.trace",
        ],
    )
}
pub struct ProjectGetRequestContext {}
pub struct IssuesListRequestContext {
    pub r#page: String,
}
pub struct FileGetRequestContext {}
pub struct PipelinesListRequestContext {
    pub r#page: String,
}
pub struct PipelineGetRequestContext {}
pub struct PipelineJobsRequestContext {
    pub r#page: String,
}
pub struct JobGetRequestContext {}
pub struct JobTraceRequestContext {}
pub trait Bindings: Send + Sync {
    fn prepare_project_get(&self, input: &ProjectGetRequest) -> Result<ProjectGetRequestContext>;
    fn finish_project_get(
        &self,
        input: ProjectGetRequest,
        context: ProjectGetRequestContext,
        response: HttpResponse,
    ) -> Result<Value>;
    fn prepare_issues_list(&self, input: &IssuesListRequest) -> Result<IssuesListRequestContext>;
    fn finish_issues_list(
        &self,
        input: IssuesListRequest,
        context: IssuesListRequestContext,
        response: HttpResponse,
    ) -> Result<Value>;
    fn prepare_file_get(&self, input: &FileGetRequest) -> Result<FileGetRequestContext>;
    fn finish_file_get(
        &self,
        input: FileGetRequest,
        context: FileGetRequestContext,
        response: HttpResponse,
    ) -> Result<Value>;
    fn prepare_pipelines_list(
        &self,
        input: &PipelinesListRequest,
    ) -> Result<PipelinesListRequestContext>;
    fn finish_pipelines_list(
        &self,
        input: PipelinesListRequest,
        context: PipelinesListRequestContext,
        response: HttpResponse,
    ) -> Result<Value>;
    fn prepare_pipeline_get(&self, input: &PipelineGetRequest)
    -> Result<PipelineGetRequestContext>;
    fn finish_pipeline_get(
        &self,
        input: PipelineGetRequest,
        context: PipelineGetRequestContext,
        response: HttpResponse,
    ) -> Result<Value>;
    fn prepare_pipeline_jobs(
        &self,
        input: &PipelineJobsRequest,
    ) -> Result<PipelineJobsRequestContext>;
    fn finish_pipeline_jobs(
        &self,
        input: PipelineJobsRequest,
        context: PipelineJobsRequestContext,
        response: HttpResponse,
    ) -> Result<Value>;
    fn prepare_job_get(&self, input: &JobGetRequest) -> Result<JobGetRequestContext>;
    fn finish_job_get(
        &self,
        input: JobGetRequest,
        context: JobGetRequestContext,
        response: HttpResponse,
    ) -> Result<Value>;
    fn prepare_job_trace(&self, input: &JobTraceRequest) -> Result<JobTraceRequestContext>;
    fn finish_job_trace(
        &self,
        input: JobTraceRequest,
        context: JobTraceRequestContext,
        response: HttpResponsePrefix,
    ) -> Result<Value>;
}
pub struct GeneratedAdapter<B: Bindings> {
    pub(crate) descriptor: Descriptor,
    pub(crate) http: Arc<dyn AuthenticatedHttp>,
    pub(crate) bindings: B,
}
#[async_trait::async_trait]
impl<B: Bindings> connectors_sdk::Adapter for GeneratedAdapter<B> {
    fn descriptor(&self) -> Descriptor {
        self.descriptor.clone()
    }
    async fn invoke(&self, operation: &str, input: Value) -> Result<Value> {
        let op = self.descriptor.operation(operation)?;
        connectors_sdk::validate(&op.input_schema, &input)?;
        match operation {
            "project.get" => {
                let args = ProjectGetRequest {
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_project_get(&args)?;
                let path = ["projects".to_owned(), args.r#project.to_string()];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [];
                let response = self.http.get(&segments, &query).await?;
                self.bindings.finish_project_get(args, context, response)
            }
            "issues.list" => {
                let args = IssuesListRequest {
                    r#cursor: input["cursor"].as_str().map(str::to_owned),
                    r#limit: input["limit"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_issues_list(&args)?;
                let path = [
                    "projects".to_owned(),
                    args.r#project.to_string(),
                    "issues".to_owned(),
                ];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [
                    ("order_by", "created_at".to_owned()),
                    ("page", context.r#page.clone()),
                    ("per_page", args.r#limit.to_string()),
                    ("sort", "asc".to_owned()),
                ];
                let response = self.http.get(&segments, &query).await?;
                self.bindings.finish_issues_list(args, context, response)
            }
            "file.get" => {
                let args = FileGetRequest {
                    r#path: input["path"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                    r#ref: input["ref"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_file_get(&args)?;
                let path = [
                    "projects".to_owned(),
                    args.r#project.to_string(),
                    "repository".to_owned(),
                    "files".to_owned(),
                    args.r#path.to_string(),
                ];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [("ref", args.r#ref.to_string())];
                let response = self.http.get(&segments, &query).await?;
                self.bindings.finish_file_get(args, context, response)
            }
            "pipelines.list" => {
                let args = PipelinesListRequest {
                    r#cursor: input["cursor"].as_str().map(str::to_owned),
                    r#limit: input["limit"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                    r#sha: input["sha"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_pipelines_list(&args)?;
                let path = [
                    "projects".to_owned(),
                    args.r#project.to_string(),
                    "pipelines".to_owned(),
                ];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [
                    ("order_by", "id".to_owned()),
                    ("page", context.r#page.clone()),
                    ("per_page", args.r#limit.to_string()),
                    ("sha", args.r#sha.to_string()),
                    ("sort", "desc".to_owned()),
                ];
                let response = self.http.get(&segments, &query).await?;
                self.bindings.finish_pipelines_list(args, context, response)
            }
            "pipeline.get" => {
                let args = PipelineGetRequest {
                    r#pipeline_id: input["pipeline_id"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                    r#sha: input["sha"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_pipeline_get(&args)?;
                let path = [
                    "projects".to_owned(),
                    args.r#project.to_string(),
                    "pipelines".to_owned(),
                    args.r#pipeline_id.to_string(),
                ];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [];
                let response = self.http.get(&segments, &query).await?;
                self.bindings.finish_pipeline_get(args, context, response)
            }
            "pipeline.jobs" => {
                let args = PipelineJobsRequest {
                    r#cursor: input["cursor"].as_str().map(str::to_owned),
                    r#limit: input["limit"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#pipeline_id: input["pipeline_id"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                    r#sha: input["sha"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_pipeline_jobs(&args)?;
                let path = [
                    "projects".to_owned(),
                    args.r#project.to_string(),
                    "pipelines".to_owned(),
                    args.r#pipeline_id.to_string(),
                    "jobs".to_owned(),
                ];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [
                    ("page", context.r#page.clone()),
                    ("per_page", args.r#limit.to_string()),
                ];
                let response = self.http.get(&segments, &query).await?;
                self.bindings.finish_pipeline_jobs(args, context, response)
            }
            "job.get" => {
                let args = JobGetRequest {
                    r#job_id: input["job_id"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#pipeline_id: input["pipeline_id"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                    r#sha: input["sha"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_job_get(&args)?;
                let path = [
                    "projects".to_owned(),
                    args.r#project.to_string(),
                    "jobs".to_owned(),
                    args.r#job_id.to_string(),
                ];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [];
                let response = self.http.get(&segments, &query).await?;
                self.bindings.finish_job_get(args, context, response)
            }
            "job.trace" => {
                let args = JobTraceRequest {
                    r#job_id: input["job_id"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#max_bytes: input["max_bytes"]
                        .as_i64()
                        .ok_or_else(|| Error::invalid("integer outside supported range"))?,
                    r#project: input["project"]
                        .as_str()
                        .ok_or_else(|| Error::invalid("invalid string input"))?
                        .to_owned(),
                };
                let context = self.bindings.prepare_job_trace(&args)?;
                let path = [
                    "projects".to_owned(),
                    args.r#project.to_string(),
                    "jobs".to_owned(),
                    args.r#job_id.to_string(),
                    "trace".to_owned(),
                ];
                let segments: Vec<&str> = path.iter().map(String::as_str).collect();
                let query = [];
                let response = self.http.get_prefix(&segments, &query, 512000).await?;
                self.bindings.finish_job_trace(args, context, response)
            }
            _ => Err(Error::new(
                ErrorCode::NotFound,
                "operation is not implemented",
            )),
        }
    }
}
